{
  "name": "@yeisme/dsh-ai-drama-director",
  "version": "0.1.0-rc.1",
  "description": "AI Drama Director Pack host and client units: context, typed /drama, events, Workbench handoff (V1 + V2 bridge), and Director preset",
  "license": "MIT",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "types": "./lib/index.d.mts",
      "default": "./lib/index.mjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "lib/**",
    "fixtures/**",
    "scripts/generate-bridge-fixtures.mjs",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json && tsdown src/index.ts --out-dir lib --format esm --dts --clean"
  },
  "devDependencies": {
    "tsdown": "^0.22.14",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
