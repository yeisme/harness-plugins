# @yeisme/dsh-client-ui-devtools

Browser half of `@yeisme/dsh-devtools`. It collects supported Web Performance entries in memory, combines them with the safe Host snapshot, and renders a bottom diagnostics panel with an overlay fallback. Export is generated by the browser application and blocked when the redaction scan finds forbidden content.
