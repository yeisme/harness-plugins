{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests import node:assert and vitest; the app code stays on types: [].
    "types": ["node"]
  },
  "include": ["tests/host/**/*.ts", "tests/shared/**/*.ts", "tests/client/**/*.ts", "src/client/globals.d.ts", "vitest.config.ts"]
}
