# 项目连续性桌面工作台实施 tasks

本文件是该切片唯一实施状态。proposal/design/specs 和产品/UI/接口正文已形成规划基线；以下代码、原型、测试和实际使用任务尚未完成。`openspec status` 的 artifact complete 不表示产品实现完成。

公共要求：每个任务复用现有框架、组件、状态 owner；新代码注释与 CLI/日志用英文，设计文档用中文。只运行该任务的 focused 检查，稳定后才运行第 8 组完整质量门。integration/component/system/e2e 均经现有 evidence runner，保留失败退出码。下面命名的新测试文件/selectors 必须在对应任务中实际创建；zero matched/skip 不可作为通过。

路径与依赖：contracts → services/adapters → UI → real scenarios → final verification。lane 仅表示可独立设计的路径，不授权子 Agent；重叠 paths 的写入串行进行，保留无关 dirty edits。任何已被其他 active change 拥有的实现只消费 handoff，不复制任务。

## 1. P0 合同与原型

- [x] 1.1 冻结 provider/consumer packet 与旧行为基线。Owner: Workbench contract；Lane: contracts；Dependencies: 根 owner packets（缺失则明确 blocked）；Paths: SDK contract fixtures、this design active-change matrix；Acceptance: 项目/Pinax/Runtime/成果/批授权各有 version/digest 或独立缺口，旧 schema/grant/route 不变；Validation: `openspec validate workbench-project-continuity-desktop-v1 --strict --no-interactive` 与 `bun test packages/task-sdk/test/project-continuity-contract.test.ts`；Expected: fixture conformance 与兼容断言通过；Failure re-check: packet 缺失不能被 sample/mock 替代真实 readiness。
- [x] 1.2 完成三个有明确 fixture 标识的交互原型。Owner: Workbench UI；Lane: prototype；Dependencies: 已批准 UI Contract；Paths: 本项目现有 gallery/prototype surface；Acceptance: 用户能重新打开项目找成果、观察并介入执行、比较候选；反馈更新 UI Contract；Validation: `bun run web:e2e --grep WB-PC-PROTOTYPE` 加三项人工任务走查；Expected: 有可复现 screenshot/行为记录且零真实 provider call；Failure re-check: 原型不能改变生产 capability/permission。 **Evidence (2026-09-06)**: 可执行原型走查落在 `apps/web/e2e/project-continuity-desktop.spec.ts` WB-PC-PROTOTYPE-001（三项入口：找成果 `data-project-continuity-section=artifacts`、观察会话 `=sessions`、续接 `准备继续|Prepare continuation`）；场景注册表 `scripts/test-evidence/scenarios.ts` 将该 id 标 executable。零真实 provider call：全部 `/v1alpha1/project-continuity/*` 与 Agent 摘要走 Playwright fulfill，不打 owner。生产 capability 不变：同文件 WB-PC-DESKTOP-001 / EVIDENCE-001 在 off 时零 continuity 流量、无 `data-project-continuity-posture`。e2e evidence `20260906142250-0768c30b-6065-4af2-abb3-e3b8e9b742c6` 5 passed（含 PROTOTYPE-001）；比较候选的真实 Diff 仍归 3.4，本切片只暴露入口不复制 owner 预览。
- [x] 1.3 在既有 runner 接入场景与证据断言。Owner: Workbench test infrastructure；Lane: evidence；Dependencies: 无；Paths: `scripts/test-evidence`、existing Playwright config/harness；Acceptance: WB-PC 场景记录 fixture/real、部署类别、版本/refs 和六件套，失败/零匹配不通过；Validation: `bun test scripts/test-evidence` 与 `bun run web:e2e --grep WB-PC-EVIDENCE`；Expected: passed/failed/blocked 可区分且有原退出码；Failure re-check: 不新建平行测试框架，不手写 JSON 运行收据。 **Evidence (2026-09-06)**: 场景注册表 `scripts/test-evidence/scenarios.ts`（15 场景：3 executable——WB-PC-CONTRACT/SDK/EVIDENCE，12 pending 各带 backingTask；fixture/real × local/remote 逐场景记录；阴性对照 pattern `WB-PC-NOMATCH` 保留）+ `scenarios.test.ts`（注册表封闭校验 + executable 场景在目标文件非零匹配 + real 场景必须 pending）。e2e `apps/web/e2e/project-continuity-evidence.spec.ts` WB-PC-EVIDENCE-001：capability-off 基线（compact rail/session rail 在位、`[data-project-continuity-posture]` 不存在、零 `/project-continuity/` 请求）。evidence run：registry `20260906101056-aff01914` passed；e2e `20260906101353-affe33f8` passed（grep 非零匹配 1 passed）；阴性对照 `20260906101155-3d86de26` exit 1「No tests found」（零匹配≠pass 证明）。runner/aggregate 未改，无平行框架。

## 2. P1 项目接口与连续性

- [x] 2.1 实现 project continuity SDK/models/normalizers。Owner: Workbench SDK；Lane: contracts；Dependencies: 1.1；Paths: `packages/task-sdk/src/project-*`、对应 test；Acceptance: 三个新方法、closed schemas、旧响应签名、scope/version/unsafe-ref 校验一致；Validation: `bun test packages/task-sdk/test/project-continuity.test.ts`；Expected: 正常/partial/拒绝用例通过；Failure re-check: 不向旧 closed response 填未声明字段，不接受 path/endpoint/credential。 **Evidence (2026-09-06)**: `packages/task-sdk/src/project-continuity-models.ts`（closed schemas + fail-closed normalizers：full 模式强制 owner-issued ref+revision≥1、context_only 显式缺席、七值动作/三值 recovery/六值 blocker 封闭枚举、unsafe key 拒绝）+ `project-continuity-client.ts`（`WorkbenchProjectContinuityClient` 三方法 GetProjectOverview/GetProjectContinuity/PrepareProjectContinuation，prepare 携带 idempotencyKey，响应恰好一个投影否则 contract_mismatch）+ `WorkbenchClient.projectContinuity` facade（2.5 BFF 注册前 unconfigured fail-closed，needs_contract 真值）。不修改 workbenchProjectMethods/proto（四面投影归 2.5），遵循 textDevelopment 同款 additive 先例。测试 `packages/task-sdk/test/project-continuity.test.ts`：fixture 全 cases 接受/全 invalid 拒绝 + fixture 外负例（v2 major/客户端拼装批准/prepare 请求形状）+ client wire/idempotency/负矩阵，10 pass。验证：`bun test packages/task-sdk` 672/0、`bun run typecheck` 0 错。
- [x] 2.2 实现分区项目总览。Owner: Workbench service；Lane: project-service；Dependencies: 2.1；Paths: `service/internal/projects`、approved projection adapters；Acceptance: 授权项目下聚合 artifacts/sessions/Tasks，owner 部分失败仅影响该分区，project 失权整体拒绝；Validation: `CGO_ENABLED=0 go test ./service/internal/projects/... -run TestProjectContinuityOverview -count=1`；Expected: 授权/分页/partial/空值 deterministic tests 通过；Failure re-check: 无 fallback Project、无领域数据副本。 **Evidence (2026-09-06)**: 新包 `service/internal/projects/continuity`（contract/overview）：`OverviewService.GetOverview` 四分区（artifacts/sessions/tasks/decisions）注入端口聚合——授权+租户 fence 先行、失权整体拒绝；provider nil→`section_provider_unbound`、`ErrNeedsContract`→`needs_contract`、其他错误→`section_read_failed`，仅降级该分区；分页 clamp（默认 20/上限 64）、空 ref 条目丢弃、越界 meta 收敛封闭值域、available 分区不带 reason（与 1.1 fixture 同不变量）；projectionRevision=max 分区 revision。测试 7 个（授权聚合/分区隔离/needs_contract reason/失权+跨租户整体拒绝/非法 scope/分页边界与空值/meta 归一化）。验证：`CGO_ENABLED=0 go test ./service/internal/projects/... -run TestProjectContinuityOverview -count=1` ok；全 projects 树 4 包 ok；gofmt/vet 干净。transport/BFF/registry 四面接线归 2.5。
- [x] 2.3 实现 Pinax typed continuity consumer。Owner: Workbench adapter；Lane: pinax-adapter；Dependencies: 2.1、Pinax provider packet；Paths: `service/internal/projects` 的窄 provider adapter 与 test；Acceptance: binding missing/ambiguous、source stale/conflict、context-only 保真，长记忆不自动写入；Validation: `bun scripts/test-evidence/run.ts --layer integration --project client/yeisme-workbench -- env CGO_ENABLED=0 go test ./service/internal/projects/... -run TestProjectContinuityPinax -count=1`；Expected: provider contract 与恢复路径有证据；Failure re-check: 禁止私有 vault/DB、human CLI output fallback。 **Evidence (2026-09-06)**: Pinax provider packet 已由 pinax 侧 `2026-09-06-pinax-workbench-continuity-projection-v1`（归档，commit 9dd6ab0）交付：`pinax continue workbench <projectRef> --json` typed facade（`pinax.workbench.continuity_projection.v1`）。Workbench 侧 `service/internal/projects/continuity/pinaxconsumer.go`：`PinaxContinuityConsumer` 实现 `ContinuityPackProvider`——argv 唯一构造点 `continue workbench <bindingRef> --json`（read-only，无 checkpoint/--yes），binary 只来自服务端配置 `WORKBENCH_PINAX_CONTINUITY_BINARY`（含空白即拒绝）；binding 解析走 deny-by-default 静态批准表（`WORKBENCH_PINAX_CONTINUITY_BINDINGS`，scope 三元组 fence，畸形条目整表拒绝），未登记 scope 不派子进程、保持 context_only setup blocker；合同门禁（schema/identity/version/projectRef 回显）不匹配 fail-closed（needs_contract/invalid_argument）；binding not_ready 只透传 Pinax authored recovery action（missing→binding_missing、disabled/invalid/ambiguous→binding_stale）；full pack 的 identity/revision 来自 evidence 观测时间（refresh-only，本地不缓存不续命），conflicts>0→source_conflict blocker、evidence 过期→binding_stale blocker+stale source，confirmedDecisionRefs 不编造；human output/非 JSON 解析失败直接报错不 fallback；长记忆零写入（只消费 read-only facade）。测试 `pinaxconsumer_test.go` 11 顶层（32 子测试）：full 投影/read-only argv/unlinked 不调 provider/not_ready recovery 全状态表/conflict+stale/合同门禁 4 例/human 拒绝/provider 错误码映射/exec 失败/binary path 门禁/service 集成 sanitize 不变量。验证：evidence run `20260906162503-d515b564` passed（exit 0，redaction 0，18.4s）；`CGO_ENABLED=0 go test ./service/internal/projects/... ./service/internal/runtime/ -count=1` 全 ok；runtime 接线（config+consumer 注入，未配置保持 nil fail-closed）与 `TestLoadConfigPinaxContinuityServerConfigOnly` 绿；opt-in 真二进制冒烟 `pinaxconsumer_smoke_test.go`（PINAX_SMOKE_BINARY，实测 9dd6ab0 真机 envelope 与解码器一致：not_found→context_only+recovery action 透传）。
- [x] 2.4 实现 prepare continuation。Owner: Workbench project/agent service；Lane: project-service；Dependencies: 2.2、2.3；Paths: existing project/agent/Context boundaries；Acceptance: 重验 refs/revisions，running→observe、unknown→reconcile、terminal→准备回合；重复 prepare 无执行，换 Runtime 保留旧 attempt；Validation: `CGO_ENABLED=0 go test ./service/internal/projects/... -run TestProjectContinuityPrepare -count=1`；Expected: 状态/幂等/失效准备结果用例通过；Failure re-check: prepare 不能创建工具调用或隐式 Context attach。 **Evidence (2026-09-06)**: `continuity/prepare.go` `PrepareService.PrepareContinuation`：封闭七值动作决策——授权失败→`request_permission`（errors.Is ErrPermissionRequired）；binding 缺失/歧义→`resolve_binding`；observed continuity 落后→`refresh_context`；无 sessionRef=显式新会话意图→`start_new_session`；选定 session running→`observe_existing`/unknown→`reconcile_existing`/terminal→`continue_session`（带版本条件）；无 session provider 时 fail-closed `ErrNeedsContract` 不猜状态；跨租户整体拒绝；provider 错误透传不伪造动作。无状态幂等：同一请求身份+同一解析状态→相同 prepareRef/preparedRevision/expiresAt（5min 有效窗）；不产生 provider call/Task/Context attach。测试 11 个（含幂等一致性与换 session 换身份）。验证：`CGO_ENABLED=0 go test ./service/internal/projects/... -run TestProjectContinuityPrepare -count=1` 11 pass。
- [x] 2.5 接通 BFF 与三种 wire transport。Owner: Workbench transport；Lane: transports；Dependencies: 2.1–2.4、1.3；Paths: registry、HTTP/gRPC/JSON-RPC、BFF 白名单；Acceptance: 四面相同 scope/error/receipt/幂等语义，浏览器同源，旧 routes 可用；Validation: `bun run test:contract` 与 `bun run test:integration`；Expected: 新方法 parity 与旧方法回归均通过并有 evidence；Failure re-check: transport 不拥有 operation-specific 状态机。 **Evidence (2026-09-06)**: 独立 proto `api/proto/workbench/projectcontinuity/v1alpha1/projectcontinuity.proto` 服务 `WorkbenchProjectContinuityService` 三 RPC（GetProjectOverview / GetProjectContinuity / PrepareProjectContinuation）；**不进** `WorkbenchProjectService` / `workbenchProjectMethods`。域层 `ContinuityService.GetContinuity` 在 Pinax pack unbound 时 fail-closed `context_only`+`binding_missing`（2.3 保持开放不阻塞）。Transport：HTTP `GET /v1alpha1/project-continuity/projects/{projectRef}/overview|pack`、`POST .../{projectRef}:prepare`；gRPC `projectcontinuitygrpc.Server`；JSON-RPC 方法名挂 `workbench.project.v1alpha1.` 前缀但 switch 到 `invokeProjectContinuity`（旧 project 方法组封闭）。Registry `ResourceServiceProjectContinuity` 四面投影 catalog 109→112；SDK descriptors 116→119 + HTTP factory + `WorkbenchClient.projectContinuity`。Capability `projectContinuityDesktopV1`/`projectContinuity` 默认 unavailable，仅 cohort+unified shell+`chatCanvasV2` 才 enabled。验证：focused Go（continuity/http/jsonrpc/registry/agent/runtime）绿；`bun run test:contract` 680/0；`bun run test:integration` evidence `20260906142541-73a602eb-cbbf-4fc0-b6e0-2d745350c5d8` passed（conformance+runtime，37.4s，redaction 0）。Transport 只 decode/map，状态机仍在 `projects/continuity`。

## 3. P1 桌面工作区与视图恢复

- [x] 3.1 扩展安全项目 view state。Owner: Workbench layout/repository；Lane: layout；Dependencies: 1.1；Paths: existing Layout service/models、GORM repository/migrations；Acceptance: principal/project 分离、refs/版本校验、旧 session layout fallback、无正文存储；Validation: `bun scripts/test-evidence/run.ts --layer integration --project client/yeisme-workbench -- env CGO_ENABLED=0 go test ./service/internal/repository ./service/internal/layout -run ProjectView -count=1`；Expected: additive migration/恢复/权限/回滚测试通过；Failure re-check: 不删旧列，不在业务层写 raw SQL，不跨项目恢复缓存。 **Evidence (2026-09-06)**: 域层 `service/internal/layout/project_view.go`（ProjectViewStateV1：safe refs/revisions/顺序/dock 宽度/anchor；≤16 文档去重、非法 ref/零 revision 丢弃、orphan active 清空、schema/device/width/anchor 越界拒绝；无正文存储）+ 持久化 `service/internal/repository/project_view.go`（`workbench_project_view_states` 表 + additive migration **0044_project_view_state**（checksum sha256:23d930bb…0881e，索引名避让 dataset-view 既有 idx_project_view_scope→idx_project_view_state_scope）；CAS expectedRevision（stale/conflict→ErrRevisionConflict）、scope 四元组 fence（tenant/principal/project/device 各自隔离）、写前 sanitize 拒绝即不落库）+ lifecycle 两处注册（lifecycle_catalog.go case + lifecycle/inventory.go stored 条目，分类 user_configuration 与 layout_state 同款）。测试：layout 4 + repository 4（迁移/恢复/CAS/scope fence/非法拒绝）。验证：`env CGO_ENABLED=0 go test ./service/internal/repository ./service/internal/layout -run ProjectView -count=1` ok（evidence run `20260906103051-cf8d9038` passed）；全 repository 包 42s ok（10 处 schema pin 随 0044 同步）；lifecycle/layout 全绿；gofmt 干净。
- [x] 3.2 接入单壳项目姿态与目录。Owner: Workbench Web；Lane: shell；Dependencies: 1.2、2.5、3.1；Paths: existing agent shell/layout/directory modules；Acceptance: explicit deep link 优先，其次最近成果，再续接 document；Chat/composer 保持唯一，重复 Pane focus/上限不变；Validation: `bun run web:e2e --grep WB-PC-DESKTOP`；Expected: 新 capability on/off 均有行为证据；Failure re-check: 不引入第二主壳/session stream 或重建 composer。 **Evidence (2026-09-06)**: 复用 `/agent` 单壳：`agent-conversation-workspace.tsx` 在 server-authored `projectContinuity` enabled 时写 `data-project-continuity-posture=on`，会话目录按 `projectId` 过滤，自动 `openPane({ kind: "projectContinuity", projectRef })`；session rail 用现有 `projectId` 分组头 `data-session-project-group`（不发明 per-session projectRef）。Chat/composer/session stream 未复制。Pane 注册 `project.continuity.v1` 为第 25 个 manifest，capability-gated。e2e WB-PC-DESKTOP-001（off：无 posture、零 continuity 请求）+ DESKTOP-002（on：posture+分组+compact rail 仍在）。evidence `20260906142250-0768c30b` 5 passed。
- [x] 3.3 实现续接摘要和恢复动作。Owner: Workbench Web；Lane: continuity-ui；Dependencies: 3.2；Paths: registered project continuity Pane、shared state components；Acceptance: 目标/成果/决定/阻塞/来源/下一步可见，partial 与 conflict 保真，prepare 不自动执行；Validation: `bun run web:e2e --grep WB-PC-CONTEXT`；Expected: 正常/缺 binding/失权/stale/unknown 全覆盖；Failure re-check: 不把空/error/ready 混淆，不制造连续性进度。 **Evidence (2026-09-06)**: `apps/web/src/workbench/agent/panes/direct/project/project-continuity-pane.tsx`：只读 overview+pack；StatusBlock 展示 objective/lastState；dl 展示下一步/阻塞/来源；四分区 artifacts/sessions/tasks/decisions 保真 availability+reasonCode；Continue 只 `prepareContinuation`（idempotencyKey），不 POST `/v1alpha1/tasks`。Pinax unbound 投影 `context_only`/`needs_contract`，不画成 ready。缺 scope→`needs_contract` unavailable；读失败→`unavailable`，不编造进度。e2e WB-PC-CONTEXT-001：pane+overview 可见，点击准备继续得到 `data-project-continuity-prepared=start_new_session`，taskSubmits=[]。evidence `20260906142250-0768c30b`（CONTEXT-001 先前 glob `*:prepare` 漏 query string，改为 `*:prepare*` 后绿）。失权/stale/unknown 的服务端封闭动作已在 2.4 覆盖；本切片 UI 消费同一投影。
- [ ] 3.4 接入成果预览、保存事实与 Diff。Owner: Workbench document consumer；Lane: documents；Dependencies: 3.2、一个真实 artifact owner packet；Paths: existing document/preview/editor/Review adapters；Acceptance: 选定成果可打开比较，saved 只来自 receipt，无写合同则只读，candidate 不自动晋级；Validation: `bun run web:e2e --grep WB-PC-REVIEW`；Expected: 保存未知、外部 revision 冲突与导出失败可恢复；Failure re-check: 不复制 Auctra/Scaena 状态或私有预览页。 **Re-check (2026-09-07，仍不勾——artifact owner 项目级投影缺位)**：1.1 fixture 仍 `auctra.screenplay`=`owner_working_copy_contract_missing`、`scaena.asset`=`owner_projection_contract_missing`。Auctra 当日 active changes（async-runtime / drama-assessment / opc-screenplay / story-graph / story-progression）均未交付 scope→artifact 列表；`auctra.text_working_copy.v1alpha1` 仍是 per-workingCopyRef。不以目录名或 text-studio 面猜测代行。3.5 只做切项目隔离/关页不取消，不打开成果 Diff。
- [x] 3.5 完成切项目、迟到流与重启恢复。Owner: Workbench state integration；Lane: shell；Dependencies: 3.2–3.4；Paths: existing query/session supervisor/selection/layout owners；Acceptance: 清理旧订阅与敏感缓存，保留各 session draft，关页不取消运行，owner unsupported 如实显示；Validation: `bun run web:e2e --grep WB-PC-RESTORE`；Expected: project switch/revoke/offline/restart 无串数据、重复 submit 或虚假 saved；Failure re-check: UI layout 恢复不能变为执行恢复。 **Evidence (2026-09-07)**：3.4 仍缺 owner packet，本切片只做壳隔离、不伪造成果 saved/Diff。Watch 请求 additive 可选 `projectId`（proto field 5，空=历史 workspace 流）；CatchUp 按 task.ProjectID 过滤；supervisor watch+refresh 透传 projectId。切项目：仅当 `workspaceRef|projectId` 变化时 abort 目录流、清 directory/tools query、丢掉绑定其他 projectRef 的 Pane；composer draft 仍按 sessionRef 保留。关页 `closeAgentPane`/`closePaneDocument` 只改 layout，不 `cancelTurn`。e2e WB-PC-RESTORE-001/002：切到 `proj_other` 旧分组消失、关续接 Pane 零 `/cancel`。evidence `20260907053826-6ca801e6-e493-4b8b-9b14-33a94a9f8bfb` passed（2/2，redaction 0）。成果 revoke/offline/restart 的 saved 事实仍归 3.4。

## 4. P1 有界自主执行

- [ ] 4.1 实现独立 action grant authority 扩展。Owner: Workbench ProposalAuthority；Lane: authority；Dependencies: 1.1、根 grant contract/owner acceptance；Paths: existing proposalauthority/registry/repository、SDK safe models；Acceptance: 显式批授权 proposal/receipt、期限/次数/范围/revoke，旧 grant 原义不变，创建/撤销复用 authority transport；Validation: `CGO_ENABLED=0 go test ./service/internal/proposalauthority/... -run BoundedActionGrant -count=1`；Expected: identity/scope/revision/legacy-grant 拒绝测试通过；Failure re-check: 不接受客户端自造 basis 或自动批准。 **Re-check (2026-09-06 下午，仍不勾——根 grant 合同未开)**：1.1 fixture 缺口 `bounded_action_grant_authority_pending` 复核成立：根仓 active changes 无 bounded action grant 合同（现存 `prompt-repository-native-source-unification-v1`/`scaena-anatomia-sonora-discovery-surface-v1` 均不相关），`workbench.bounded_action_grant.v1alpha1`（接口合同 §5）无 owner 冻结版本/digest。不单方面在消费侧发明批授权 basis/期限/revoke 语义；待根 grant contract change 或 owner acceptance 开门后按 1.1 流程重冻 fixture 再实现。
- [ ] 4.2 接入逐 action 原子准入。Owner: Workbench Task/owner adapter；Lane: admission；Dependencies: 4.1、Runtime/工具 effects packet；Paths: existing core/admission/adapters；Acceptance: 每个 action proposal 重验权限交集、预算预留、幂等、writer lease，revoke fence 与 unknown 结算一致；Validation: `bun scripts/test-evidence/run.ts --layer integration --project client/yeisme-workbench -- env CGO_ENABLED=1 go test -race ./service/internal/core ./service/internal/proposalauthority/... -run BoundedAction -count=1`；Expected: race/replay/revoke/overspend/unknown 无重复或越权；Failure re-check: 普通命令不得按 cwd 猜安全，unsupported effects 回原审批。
- [ ] 4.3 实现授权范围、介入与恢复 UI。Owner: Workbench Web；Lane: grant-ui；Dependencies: 4.1、4.2、3.2；Paths: existing Profile/Run/Review components 与 locales；Acceptance: 范围/预算/期限可检查，撤销阻止后续动作但不伪造取消，unknown reconcile，失权与 capability-off 保留原恢复；Validation: `bun run web:e2e --grep WB-PC-GRANT`；Expected: on/off/legacy/expired/revoked/unknown 流可完成；Failure re-check: 浏览器不推导 capability 或代用户点击批准。

## 5. P1–P3 真实执行与双部署

- [ ] 5.1 消费真实 Conversation Runtime 与部署端工具/成果。Owner: Workbench integration；Lane: real-runtime；Dependencies: 2.5、3.5、4.3、原 Chat-Canvas/Runtime owner 真实 gates；Paths: existing runtime facade/approved adapters/evidence；Acceptance: 一个真实 Agent 回合、声明 effects 的工具动作、owner 成果与 receipt，取消/partial/reconcile 可核验；Validation: `bun run web:e2e --grep WB-PC-REAL-RUNTIME` 经已显式授权测试 Profile/budget；Expected: real 或 truthful blocked，不自动使用付费 key；Failure re-check: reference adapter 不得用于此完成项。
- [ ] 5.2 验证同版 local/remote 服务栈。Owner: Workbench deployment integration；Lane: deployment；Dependencies: 5.1、managed Identity prerequisites；Paths: existing deployment/preview/managed harness，不新增产品模式；Acceptance: 两环境使用本部署工具与目录，远程 HTTPS/session/CSRF，loopback broker 不公开；Validation: `bun run web:e2e --grep WB-PC-DEPLOYMENT`；Expected: 两个部署 verdict/版本/receipt 独立，缺工具不跨端 fallback；Failure re-check: 本地 pass 不等于远程 pass，测试部署不隐含生产变更。

## 6. P2 共用内核场景

- [ ] 6.1 完成调研到报告续写。Owner: Workbench scenario integration；Lane: research；Dependencies: 5.1、Pinax/成果 provider ready；Paths: existing fixtures/approved renderers/E2E；Acceptance: 来源→报告→修改→关页/换会话→续写→导出，source conflict 可见；Validation: `bun run web:e2e --grep WB-PC-RESEARCH`；Expected: 场景真实 refs/receipt 与恢复证据；Failure re-check: 不把未核验引用计作可用上下文。
- [ ] 6.2 完成软件项目修改与验证。Owner: Workbench scenario integration；Lane: software；Dependencies: 5.1；Paths: existing CLI/Runtime/Review consumers 与 disposable project fixture；Acceptance: 范围内修改、测试输出、Diff 审阅、handoff，可恢复且无自动 push/publish；Validation: `bun run web:e2e --grep WB-PC-SOFTWARE`；Expected: workspace/operation receipt 可追溯；Failure re-check: 不将任意 terminal 命令放入低风险白名单。
- [ ] 6.3 复用 Text Development 的版本链。Owner: Workbench text consumer；Lane: text；Dependencies: 3.4、5.1、原 Text Development owner tasks；Paths: existing text/Screenplay document host；Acceptance: Working Copy→candidate→Checkpoint/Review 复用原 Auctra 权威，与项目续接衔接；Validation: `bun run web:e2e --grep WB-PC-TEXT`；Expected: 真实可用范围单列；Failure re-check: 该 owner 未 ready 就记录 blocked，不复制编辑器或 silent accept Canon。
- [ ] 6.4 复用多模态成果消费。Owner: Workbench media consumer；Lane: media；Dependencies: 3.4、5.1、选定 media owner packet；Paths: existing asset/preview/Review Panes；Acceptance: 参考→候选→比较→选定→导出，大文件状态与 owner 回执一致；Validation: `bun run web:e2e --grep WB-PC-MEDIA`；Expected: 所选 owner/operation 单独成熟度，不提升其他 owner；Failure re-check: 不恢复退役生产 UI/私有 iframe 或 fixture fallback。

## 7. P3 UI、性能与安全

- [ ] 7.1 收口 locale 与桌面可访问性。Owner: Workbench Web；Lane: UI-quality；Dependencies: 第 3/4 组稳定；Paths: locale source、touched UI、existing gallery/e2e；Acceptance: 1024/1280/1440/1920、200% zoom、IME、键盘、focus、reduced-motion 与 Axe serious/critical 0；Validation: `bun run compose:i18n`、`bun run check:i18n`、`bun run web:e2e --grep WB-PC-A11Y`；Expected: 中文主文案/英文同键，截图状态齐全；Failure re-check: 不直接改 generated bundle 或新增私有控件系统。
- [ ] 7.2 测量持续使用性能。Owner: Workbench performance；Lane: performance；Dependencies: 3.5、6.1、6.2；Paths: existing performance harness 与 focused hot paths；Acceptance: 固定机器/fixture 下 cached switch p95≤200ms、输入 p95≤100ms，60 分钟切换无持续订阅/DOM/heap 泄漏；Validation: `bun run web:e2e --grep WB-PC-PERFORMANCE`；Expected: 原始测量 artifact 与环境分离记录，owner 等待单独统计；Failure re-check: 无测量不能宣传达标，优化不引入新状态 owner。
- [ ] 7.3 验证 trust boundaries。Owner: Workbench security verification；Lane: security；Dependencies: 第 2–5 组稳定；Paths: existing SDK/service/BFF/security/e2e tests；Acceptance: 跨项目/tenant、SSRF/unsafe preview、path/credential/payload sentinel、grant replay/revoke、unknown 和 browser-direct-owner 拒绝；Validation: `bun run web:e2e --grep WB-PC-SECURITY` 与 `bun test packages/task-sdk/test/project-continuity-contract.test.ts`；Expected: 拒绝分支有证据且敏感 sentinel 0；Failure re-check: 不改宽 schema 或删除旧拒绝测试来通过。

## 8. P3 实际使用、回滚与最终交付

- [ ] 8.1 完成真实使用 proof。Owner: Workbench product validation；Lane: usage；Dependencies: 6.1、6.2、7.1–7.3；Paths: existing evidence runner 的人工任务输入/recording；Acceptance: ≥3 项目/≥2 场景、10 次续接至少 8 次≤30 秒、60 分钟零已确认丢稿和零重复操作，失败样本不删除；Validation: 经项目 runner 执行 `bun run web:e2e --grep WB-PC-USAGE` 并按 PRD 人工计时/校验；Expected: 自动技术证据与人工使用观察分别记录；Failure re-check: 模拟 fixture 或无人运行不能证明真实使用。
- [ ] 8.2 演练兼容与 capability rollback。Owner: Workbench integration；Lane: rollback；Dependencies: 5.2、7.3；Paths: existing feature gate/upgrade/restore tests；Acceptance: 关闭新增能力后旧 shell/深链/数据保留，在途原 Task 仍可查询/cancel/reconcile，新增 grant 停止准入；Validation: `bun run web:e2e --grep WB-PC-ROLLBACK`；Expected: local/remote 两类受控环境均有证据；Failure re-check: 不删除 owner 数据、历史 receipt 或旧 schema。
- [ ] 8.3 稳定后执行最终质量门。Owner: Workbench final verification；Lane: final；Dependencies: 5.2、6.1–6.4（允许专业场景如实 exploratory，但 required 首版门不得略过）、7.1–7.3、8.1、8.2；Paths: 本 change stable diff；Acceptance: required capabilities 有直接 evidence，旧回归无引入失败；Validation: `openspec validate --all --strict --no-interactive`、`buf lint`、`CGO_ENABLED=0 go test ./service/...`、`CGO_ENABLED=1 go test -race ./service/...`、`bun run typecheck`、`bun test`、`bun run build`、`bun run web:build`、`bun run web:test`、`bun run test:contract`、`bun run test:integration`、`bun run web:e2e`；Expected: 必需门通过，环境/既有/并发失败单独归因；Failure re-check: 不修无关业务代码来清理 global warning。
- [ ] 8.4 同步真实 readiness 与归档。Owner: Workbench documentation；Lane: closeout；Dependencies: 8.3；Paths: README、产品/UI/接口文档、owning specs；Acceptance: 本地/远程、fixture/real、各 owner/operation 明确，未完成专业任务转明确 owner handoff 而非删除；Validation: `openspec status --change workbench-project-continuity-desktop-v1`、`openspec validate workbench-project-continuity-desktop-v1 --strict --no-interactive` 和相关文档链接检查；Expected: 可进入 CLI archive lifecycle；Failure re-check: 不以 artifact complete 提前打勾或同步主 specs 成已交付。
