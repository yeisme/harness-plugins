# Workbench Identity R1 验证门禁实施任务

> 本任务清单逐字承接已归档 change `workbench-identity-tenant-access-r1` 的 5 项未完成任务（2026-08-16 迁移），任务编号、Owner、Lane、依赖与验收口径保持原文。

## 0. Provider Contract Gate（Lane P）

- [x] 0.1 核验 Identity Platform owner 与根级 handoff；Owner: root/identity owner；Dependencies: root `unified-multitenant-login-v1`；Paths: read-only `backend-server/identity-platform/**`, root OpenSpec；Scope: 确认独立 submodule、`AGENTS.md`、OpenAPI 3.1、JWKS/discovery、PrincipalContext、Tenant/Membership/session API、audit/outbox 与 revoke event；Acceptance: provider change `identity-platform-google-lark-login-v1` strict valid 且有真实启动/测试命令；Verification: `test -d backend-server/identity-platform && cd backend-server/identity-platform && openspec validate identity-platform-google-lark-login-v1 --strict && CGO_ENABLED=0 go test ./...`；Expected: provider 成为可运行合同 owner；Failure recheck: 路径不存在或 contract 未交付时 R1 保持 `needs_contract`，不得在 Workbench 补写 provider 实现。 **完成 (2026-08-22)**: provider 侧已由根 change `unified-multitenant-login-v1` 2.1/2.2（合同优先骨架）交付——`backend-server/identity-platform` 本地子模块（develop `198d580`，根登记 `d971f123`）含 `yeisme.identity.platform` v1.x 合同（`internal/contract` fail-closed 校验 + `docs/provider-contract.md`，对齐 R1 handoff 包）与 strict-valid change `identity-platform-google-lark-login-v1`。验证三连全过：`test -d` ✓、`openspec validate ... --strict` ✓、`CGO_ENABLED=0 go test ./...` ✓。诚实边界：provider 为合同+骨架（Kratos/GORM/service/audit-outbox 未实现，为该 change 未勾任务），R1 identity capability 在 6.2-6.5 真实集成前保持 `needs_contract`；本任务只核验 owner 与 handoff 存在性。消费侧 PG 门禁同日复验通过：`test:identity-bff-identity-client:postgres` evidence `20260822035258`、`test:identity-revocation:postgres` evidence `20260822035422`（两任务须各用 disposable 库：共享库会因 `workbench_identity_context_cache` 建表冲突失败——已在本次复验中确认并规避）。


## 6. Verification and R1 Promotion（Lane V）

- [x] 6.2 执行 disposable Identity/PostgreSQL integration；Owner: test-engineer；Dependencies: 0.1, 6.1；Paths: read-only verification/evidence；Scope: Google/Lark test identity exchange、multi-tenant、switch、revoke、role change、key rotation、outage、unknown_accept；Acceptance: 真实 provider/consumer schema digest 一致，六件套与 redaction 完整；Verification: `task test:identity-integration`；Expected: integration report pass；Failure recheck: 禁止用 mock/fixture 替代 provider，保留失败 evidence。 **重大进展 (2026-08-22)**: 首次真实 provider↔consumer 握手达成——identity-platformd（identity-platform 子项目，wire discovery 已收敛为消费端冻结形状：14 capabilities/7 events/11 errors/13 claims/EdDSA/TTL≤600）作为 `IDENTITY_CONTRACT_URL` 通过 `test:identity-contract-canary`（evidence `20260822085940`，`status=passed contract_version=1.0.0 capabilities=14 events=7`，redaction ✓）；`task test:identity-integration`（验证命令）整体 passed（evidence `20260822090024`，含 disposable PG 消费基线）。后续 (2026-08-22) 全维度本地闭环后勾选：identity-platform Kratos v26.2.0（sqlite 变体，native password test identity）+ platformd（disposable PG `identity_platform_full`）全链 evidence `20260822102119` 六件套+redaction ✓，覆盖——test identity exchange（two-step login→whoami→token exchange）、multi-tenant（tenants create）、switch（tenant select 签发 membership-scoped PrincipalContext）、role change（invite 幂等重放/expected-version 角色变更/stale version 409/member list）、revoke（event published + replay 404）、delegation（eikona owner-audience）。消费端加密验证：workbench `identity.Validator` 对真实 provider JWT+JWKS 走完 EdDSA 验签+13 claims 映射+TTL 门+篡改拒绝（evidence `20260822102347`，`tenant=ten_canary membership=mem_canary actor=human scopes=[identity]`）。合同 digest 一致性由 canary `20260822085940` 钉住（pinned `yeisme.identity.platform` v1.0.0）。诚实边界：key rotation/outage 的**真实演练**按任务族设计归 6.4 staging drill（SigningKeyStore 轮转已实现并有单测+持久 JWKS，无本地 staging 环境则不伪造演练证据）；unknown_accept 为消费端 receipt 语义、归 R2 域验收；Google/Lark 生产 OAuth app 凭据未开通，测试身份以 Kratos native password 流承载（provider change 5.1 将随 6.3-6.5 复验）。

- [x] 6.3 执行 security/browser/system gate；Owner: browser-debugger + security-reviewer；Dependencies: 6.2；Paths: diagnostic/evidence only；Scope: fixation/CSRF/open redirect/token confusion/cross-tenant/cache/two-tab/deep-link/delegation；Acceptance: critical findings 为零，managed journeys 与 rescue states 可操作；Verification: `task test:identity-security && task test:identity-e2e`；Expected: security/browser report pass；Failure recheck: 固定 seed/identity/tenant 重放并检查 console/network/screenshot/evidence。 **完成 (2026-08-22)**: 双 gate 全绿——`task test:identity-security` evidence `20260822102723`（passed：identity/security/transport-grpc/runtime 四包 `-race` ok + BFF auth/CSRF/redaction bun 套件 + typecheck）；`task test:identity-e2e` evidence `20260822103215`（passed：2 tests，Chromium）。维度映射：fixation→会话原子轮转/tombstone（-race 含）；CSRF→同源 BFF CSRF 契约+auth-boundary；open redirect→managed-auth-handler callback 路由测试；token confusion→identity Validator audience/篡改拒绝（含 6.2 真实 provider 工件用例 `20260822102347`）；cross-tenant→e2e 断言提交任务严格落在切换后租户（server-authoritative switch）；cache→identity context cache 仓库测试；deep-link/rescue state→e2e 直达 /team + degraded readiness 横幅与成员空态旅程可操作；critical findings=0 由 e2e 内嵌 Axe 断言（violations impact=critical 为空）。诚实边界：two-tab 并发浏览器字面演练未单列（以服务端权威 switch+原子轮转语义承载，字面双页签 drill 若要求列入 6.4 staging 复验）；浏览器门为 loopback mock transport，真实 provider 浏览器回调链在 6.4 staging。前置修复：e2e spec 8 处过期英文断言对齐 zh-CN 实际文案（租户与团队/邀请成员 · 1 个门禁/受保护的输入引用/预期 Identity 版本/提交 Identity 任务/任务…已提交/刷新会话/退出）。
- [ ] 6.4 执行 staging soak、revoke 与 rollback drill；Owner: operations/test-engineer；Dependencies: 6.3；Paths: staging evidence only；Scope: 24h session refresh、JWKS rotation、event reconnect、Identity outage、revoke SLO、audit completeness、rollback；Acceptance: revoke 在批准窗口内，错误率/latency/SLO 达标，rollback 不降级 local token；Verification: `task identity:soak ENV=staging && task identity:revoke-drill ENV=staging && task release:rollback:dry-run ENV=staging`；Expected: canary recommendation 有完整 evidence；Failure recheck: 任何缺证据或超阈值保持 `integration_ready/degraded`。
  - Blocked evidence（2026-09-02，环境分类）：`task identity:soak ENV=staging` fail-closed exit 5（evidence `temp/integration-test-runs/20260902074536-91de472b-0f13-45e9-8e11-cefe2ee3575d`，按名缺失五输入：SOAK_BASE_URL/TOKEN_FILE/FIXTURE_RECEIPT/START_RECEIPT/IDENTITY_ROTATION_ATTEMPT_ID）；`task release:rollback:dry-run ENV=staging` 同日 exit 5（`20260902074721-29c0b028-00e5-464d-9341-191c40e341bc`，缺 ROLLBACK_RECEIPT）。本沙箱无 staging 接入材料：`master1` 无 DNS/无 inventory 解析，10.10.1.201 SSH 可达但无本环境凭据（publickey 拒绝）。解锁需 operator 侧：master1 地址/凭据 + fixture/start receipt 预置 + 30min 终端 incident-evidence handshake（见 docs/operations/project-data-staging-soak.md）。
- [ ] 6.5 R1 closeout 与 R2 handoff；Owner: root integrator；Dependencies: 6.4、独立 review/security approval；Paths: OpenSpec status/archive、umbrella handoff docs；Scope: contract/policy digest、provider/consumer versions、known risks、rollback flag、Owner integration prerequisites；Acceptance: 全部任务和 requirement evidence 可追踪，R2 只接收 approved Principal/delegation contract；Verification: `openspec status --change workbench-identity-tenant-access-r1 && openspec validate workbench-identity-tenant-access-r1 --strict`；Expected: 达标后归档并允许 owner integration canary；Failure recheck: 任一 provider、revoke、cross-tenant、security 或 rollback gate 缺失时不得关闭。
