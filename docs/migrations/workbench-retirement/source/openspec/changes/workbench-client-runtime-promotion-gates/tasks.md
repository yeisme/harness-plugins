# Workbench Client Runtime 晋级门禁实施任务

> 本任务清单逐字承接已归档 change `workbench-client-runtime-adapter` 的 3 项未完成任务（2026-08-16 迁移），任务编号、Owner、Lane、依赖与验收口径保持原文。

## 3. Phase B broker-backed operations

- [ ] 3.1 Owner: Broker/Owner Implementers；Lane: dependency；Dependencies: shared broker + owner adapters；Scope: 发布 fixed registry、peer/facade ACL、descriptor/run/events/cancel/reconcile 与 resource limits；Acceptance: arbitrary executable/argv/cwd/env/PATH/symlink/replay 被拒绝，broker无 credential Resolve；Verification: broker `TestSecurity_`、race、build、integration evidence；Expected: dependencies pass；Current status: 2026-07-22 Workbench 仍通过 Go `replace` 与 TypeScript `file:` 消费本地 broker；`backend-server/client-runtime` 仅可识别为 commit `2a92ee5`，其 TypeScript package 仍为 private `0.1.0-dev`，Aigora 工作树为 `9acdf9d-dirty`，credentialctl 为 `v0.1.0-2-g143e1c6`。这些状态不能作为 tagged release 或独立 owner promotion 证据，因此本项保持 pending；Failure re-check: 任一失败或依赖未tag时保持 Workbench generate 默认关闭。

## 4. Verification, review and evidence

- [x] 4.2b Owner: Security Reviewer；Lane: security-phase-b-generate；Dependencies: 4.1b stable diff, Aigora 5.3；Scope: 独立审查 Workbench Task gate → broker stored admission/reconcile pump → Aigora canonical plan/worker → credentialctl/provider 边界，覆盖 approval replay、revision/credential drift、同一 run 多订阅者、cancel/restart replay、unknown 不重发、SSRF/redirect/DNS/private-IP、secret/provider payload/private path 与轮询资源上限；Acceptance: 无 P0/P1，瞬时 reconcile 错误不得投影伪终态，cancel 重启重放保持 terminal cancelled，broker/Workbench 永不获得 credential Resolve 权限；Verification: 三项目全部 `TestRuntimeSecurity_`、broker reconcile event/race tests、Aigora safe transport/grant/worker tests及稳定 diff审查；Expected: pass before generate promotion；Current evidence: 实现者自检已确认 exact admitted request 重用、terminal shortcut、EventHub dedupe、cancel replay 与 process-output敏感扫描通过，但不得替代独立复核；Failure re-check: 任一风险缺负向测试或出现 P0/P1 时关闭 generate flag并回到 owning change修复。**Independent review 2026-09-01**：依赖核验完成——Aigora 5.3 = aigora 归档 change `aigora-client-byok-runtime-adapter` 任务 5.3（已勾选，2026-07-21 独立复核无 P0/P1，详见其 verification.md）；4.1b 归档勾选复核一致。独立复核报告 `temp/security-review/promotion-gates-4.2b-20260901.md`：9 项威胁全部"有负向测试且通过"（file:line 证据在报告）；P0=0、P1=0、P2=3（依赖快照漂移归 3.1 承接、broker SSE facade-run 绑定加固、credentialctl V0.1 OS 用户信任边界文档化限制，均不触发回退）；scoped 测试原退出码全 0——workbench `TestRuntimeSecurity_` 26 项、client-runtime `Reconcile|Event|Race|Security`（含 -race）、aigora `SafeTransport|Grant|Worker|Security` 67 项（含 -race）、credentialctl `TestScopedGrantAndBrokerDenied`；无预存失败需甄别。结论 approve-with-followups。注：本复核针对当前实际链路（client-runtime 工作树 f54830f、aigora d5eb9267、credentialctl v0.1.0 tag），不构成 3.1 的 tagged release 证据，generate 仍保持默认关闭直至 3.1/4.4b 完成。
- [ ] 4.4b Owner: Product/Compatibility Review；Lane: promotion-phase-b；Dependencies: 3.4, 4.1b, 4.2b, 4.3b, Aigora owner evidence；Scope: 对 Aigora approval、event cursor、cancel、unknown reconcile、feature-flag rollback逐项验收；Acceptance: Workbench first-support 以一条真实 Aigora owner 链路逐 capability 晋级，不接受 fixture 作为替代证据；关闭 `WORKBENCH_RUNTIME_GENERATE_ENABLED` 与 Web canary 后 catalog/action立即消失，terminal history仍可读且任何 non-terminal task不自动重发；Verification: promotion receipt 引用真实 run IDs/commands/exit codes及一次显式 rollback rehearsal；Expected: 已证明 capability 可晋级，未证明 operation 保持 disabled；Current evidence: Web默认关闭/显式canary组件测试、e2e run `20260720145320-12ec3d03-d808-4cfd-9a75-a1d74829800f`，以及component run `20260720145505-213aa42d-6a9e-4c00-8c10-a4ed0925a0a7` 已证明service flag关闭后generate不注册、terminal history保留、running task从持久cursor恢复event watch且`StartRun`零调用；4.1b稳定全量复验已通过，本项仍因4.2b独立安全复核、Aigora owner release gate与可验证依赖tag未完成而保持pending；Failure re-check: 任一 Aigora operation 无证据、依赖未tag、安全复核未签字或rollback失败即保持 disabled。第二 owner 证据另作为跨 owner GA gate，不阻塞本 first-support。
