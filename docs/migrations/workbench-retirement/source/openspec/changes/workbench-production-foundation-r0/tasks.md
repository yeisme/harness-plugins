# Workbench Production Foundation R0 实施任务

下一批 P0 的代码所有权、依赖、失败语义与完成定义见 `details/next-p0-execution-plan.md`。

## Machine Task Index

- [x] 1.1 盘点并冻结 envelope/错误/能力；详见下方同编号任务。
  - **Evidence**: 2026-07-22 由协调者直接实现（记忆：agent 写代码不可靠，前台直接做+独立验证）。冻结 R0 通用 contract 字段（tenantRef/workspaceRef/resourceRef/version/expectedVersion/idempotencyKey/capability/freshness/cursor/correlationRef/receiptRef/evidenceRefs）+ 8 capability descriptor shell。新增 `packages/task-sdk/src/foundation-envelope.ts`（合同 `workbench.foundation.v1alpha1`；`FoundationResourceKind` 8 域、`FoundationEnvelope`、`FoundationCapabilityDescriptor`；`foundationCapabilityShells` 8 域全 state=needs_contract/schemaIdentity=null 的 additive shell；`findFoundationCapability` 查找；`normalizeFoundationCapabilityDescriptor`/`normalizeFoundationEnvelope` fail-closed normalizer——缺 tenantRef/workspaceRef/correlationRef→null、未知 enum→null、evidenceRefs 拒绝 `../`/`http://` 非 opaque ref；opaqueRefPattern 校验所有 ref）。新增 proto `api/proto/workbench/foundation/v1alpha1/foundation.proto`（3 enum + 2 message，无 service——R0 不实现业务 handler）+ JSON schema `api/schema/workbench/foundation/v1alpha1/foundation.schema.json`（capabilityDescriptor + envelope `$defs`，全 additionalProperties:false + opaqueRefPattern）。新增 `test/foundation-envelope.test.ts`（11 case：8 域 needs_contract 不伪装 available/未知 kind fail-closed/envelope 必填 ref 缺失→null/null ref 允许/enum 校验/evidenceRefs 拒私有路径/correlationRef 贯穿）。Verification：`buf lint` exit0、`buf generate` exit0（重生 foundation gen bindings）、`bun run test:contract` 118/0（+11 新）、`go build ./service/...` exit0、`openspec validate workbench-production-foundation-r0 --strict` valid。未实现 operation 一律 needs_contract，无业务正文、无空成功对象。
- [x] 1.2 扩展 Operation registry 与四 transport parity；详见下方同编号任务。
  - **Evidence**: 2026-07-22 由协调者直接实现 Go 侧（记忆：agent 写 Go 不可靠，前台直接做+独立验证）。新增 `service/internal/registry/foundation.go`：8 capability shell read 操作（`workbench.foundation.{asset,team,work_item,layout,board,search,audit,handoff}.read`），全 `ModeUnavailable` + 四 transport projections（SDK/HTTP/gRPC/JSON-RPC 自动投影，无 operation-specific transport handler）+ 非 mutation read（未实现 mutation 不创建 Task/receipt）+ `foundationUnavailableHandler` fail closed（`StatusFailed` + needs_contract 文案，绝不返回空成功）；schema 用 registry 支持的 primitive 格式（additionalProperties:false + string 属性，无 const/enum——registry parseSchema DisallowUnknownFields）。接线 `runtime.go` `RegisterFoundationCapabilities(operations)`。新增 `foundation_test.go`（2 case：8 域 ModeUnavailable+四 projection+handler fail-closed 逐项校验；catalog 暴露全部 8 域）。踩坑：registry schema 不支持 const/enum（DisallowUnknownFields）→ 改 primitive string；`Seal()` 无参、`registry.Get`（非 Lookup）、避免与 registry_test.go `allProjections` 冲突改名 `foundationProjections`。Verification（umask 077）：`registry`/`conformance`/`runtime` 三包 ok、`go build ./service/...` exit0；四 transport operation×transport matrix 由 conformance 自动覆盖。
- [x] 2.1 实现分层配置与启动校验；详见下方同编号任务。
  - **Evidence**: 2026-07-22 由协调者直接实现（记忆：agent 写 Go 不可靠，前台直接做+独立验证）。新增 `service/internal/config/userconfig.go`：approved JSON user-config schema（profile/http_address/grpc_address/allow_non_loopback_bind/data_path/database_url/identity_*/open_design_url/otel_endpoint/runtime flags，snake_case）+ `Load(path)`（`DisallowUnknownFields` 未知键 fail-fast + `io.EOF` trailing-content 拒绝 + secret 不回显：路径只保留 basename、错误剥离 `://`/`@`/`password`/`token` 片段）+ `ResolvePath(flag)`（flag > `WORKBENCH_CONFIG` env）。重构 `runtime.DefaultConfig` → `LoadConfig(user userConfig)`（precedence default < user-config < env；`firstNonEmpty` 合并 helper；env 覆盖 user；identity managed 三键 env>user>identity.Default）+ `AdaptUserConfig(config.UserConfig)→userConfig`（解耦 import 方向）。接线 `workbenchd/main.go`：`extractConfigFlag(os.Args)` pre-scan `--config`/`-config`（flag.Parse 前取路径）→ `config.Load` → `runtime.LoadConfig(AdaptUserConfig(...))`；load 失败 exit 2。新增 `config/userconfig_test.go`（8 case：env 覆盖文件/未知字段 fail-fast/空路径零值/缺失文件路径 redacted/非单一对象拒绝/flag>env 路径/redactPath/畸形 JSON）+ `runtime/config_layer_test.go`（4 case：user 层填充/env 覆盖 user/default 回退/managed+identity 经 user 注入）。Verification（umask 077）：`config`/`runtime`/`workbenchd` 全 ok、`go test ./service/...` 57 包 0 fail。managed 缺关键配置时 Validate 在启动期拦截（既有：postgres scheme + identity required + listener loopback）；local 默认 loopback + token file；secret 不入错误。
- [x] 2.2 建立 SQLite/PostgreSQL GORM profiles；详见下方同编号任务。
  - **Evidence**: 2026-07-23 解锁 disposable PostgreSQL（本机 PG14，建 workbench_test role+DB，URL 存 /tmp 0600）+ 协调者独立验收（umask 077，全量 `go test ./service/...` + PG 集成测试绿）。三 PG 集成测试 `TestPostgresRepositoryIntegration`/`TestPostgresBoardViewportRepositoryGate`/`TestPostgresBoardServiceTransactionGate` 确定性 PASS（schema/表/索引/事务 parity）。**修复 pre-existing board 冲突契约 bug**：PG MVCC 下并发失败方在预检 `CheckExpectedRevision` 返回 `DomainError{Code:ErrorVersionConflict}`（无 Is()），而 CAS 返 `boards.ErrRevisionConflict` sentinel——`beginGraphMutation` 统一映射成 sentinel（对标 layout 域），board 包 `-race` 无回归、PG 测试 x5 稳定。repository parity 两 profile 通过。（2026-07-24 更正：先前"CLI 不 seed schema_migrations"为误诊——实为查错表名 `schema_migrations` vs 真实 `workbench_schema_migrations`，CheckReady 在 CLI 迁移库上 PASS；真问题为 2.4 的两 backup bug，已修。）
- [x] 2.3 实现 migration/backfill framework；详见下方同编号任务。
  - **Evidence**: 2026-07-24 收口（subagent + 协调者独立验收）。新增 `service/internal/migrations/pg_integration_test.go`（8 PG 集成测试，gated on `WORKBENCH_TEST_POSTGRES_URL`）：lease 互斥×3、cursor 续跑、receipt 完整性+脱敏、跨版本 upgrade（合成 v1→v2）、过期 lease 再获取。**修真实生产 bug**：`AcquireMigrationLease`（migration_metadata.go）PG Read-Committed read-then-insert 竞态（两并发都获 lease）→ `pg_advisory_xact_lock` 序列化（SQLite 天然序列化跳过）；并发探测 x10 从 ~80% 失败转确定性 pass。fresh/upgrade/interrupted/resume/schema-mismatch + SQLite/PG matrix 两 profile 通过。全量 gate（带 PG）0 FAIL + openspec strict valid。
- [x] 2.4 建立 backup/restore verification contract；详见下方同编号任务。
  - **Evidence**: 2026-07-23/24 解锁 disposable PG（本机 PG14）后修两个 backup/restore PG 真 bug（均从未对真 PG 验过）并全链路验证：① `PGDATABASE: databaseURL`（整 URL 进 PGDATABASE）libpq 把整串当库名→pg_dump 连接失败，改 `postgresConnectionEnv` 拆 PGHOST/PGPORT/PGUSER/PGPASSWORD/PGDATABASE/PGSSLMODE（密码只进 PGPASSWORD env，不进 argv/manifest，脱敏不变）+ 同步 `backup_test.go`；② `pg_restore` 需显式 `-d <dbname>`（不像 pg_dump 可走 PGDATABASE env），`postgresRestoreCommand` 加 `-d`。**PG backup→restore-verify 全链路通过**：backup 产 manifest（schema 0015/checksum 20c7f7/artifact 161988B/SHA-256 79edfd，无 DSN/credential）→ restore-verify 恢复到 disposable `workbench_restore_verify` 库 → receipt `status=verified`（schema 0015 一致、artifact digest 一致、309ms），恢复库 `workbench_schema_migrations` 有 0015 行。CheckReady 在 CLI 迁移库上 PASS（先前"schema_migrations gap"为查错表名的误诊）。acceptance（默认不动 production + 无有效 restore evidence 时 readiness 失败）+ Expected（checksum 与 schema version 一致）达标，本任务关闭。retention/encryption/PITR/RPO-RTO drill 为 R0+ 增强（非 R0 acceptance gate），列为 follow-up。
- [x] 3.1 分离 health/readiness 与 graceful shutdown；详见下方同编号任务。
  - **Evidence**: 2026-07-22 并行 subagent 实现 + 协调者独立验收（记忆：agent 写 Go 须独立验证）。`umask 077` 下 `CGO_ENABLED=1 go test -race ./service/internal/runtime/...` ok、全量 `go test ./service/...` 0 FAIL、`openspec validate --strict` valid、`go build ./service/...` exit0。新增 `service/internal/runtime/worker_readiness.go`（`workerTracker`：required 集合构造期冻结避 TOCTOU；缺任意 required worker→/readyz 503 fail-closed；错误只含 worker 名不含 secret；`WORKBENCH_REQUIRED_WORKERS` CSV 配置；`ClientRuntimeGenerateEnabled` 自动注册 `event_supervisor`）+ `drain.go`（`streamTracker` 中间件：drain 后新 streaming 立即 503；活跃 SSE 经 child context 取消；check-and-register 同锁无 TOCTOU；`BeginDrain` 锁外 cancel 无死锁）+ `runtime.go` `Shutdown` 重构 5 步有界 drain：①`lifecycle.BeginDrain()` readiness 翻 false ②LB 传播有界等待（cap ≤ ctx deadline 1/3，0 跳过）③`streams.BeginDrain()` 取消活跃 SSE ④gRPC GracefulStop + ctx 到期 Stop ⑤`httpServer.Shutdown(ctx)`+Close 兜底；readiness 响应只回 ok/unavailable 不泄漏错误细节。新增 26 测试（worker_readiness 10 + drain 9 + shutdown_drain 8，其中 1 skip；集成测试连跑 x10 / workerTracker x20 稳定全绿）。**非阻塞 follow-up**：LB 传播真实 deploy 校验（nginx/k8s probe）、gRPC streaming 细粒度 context 取消（现依赖 GracefulStop 内置）、drain 拒绝 code 字面量 `rate_limited`→更贴 `unavailable`（503 状态码本身正确）。一次全量回归中 runtime 包出现间歇 phantom-FAIL（所有子测试 PASS 却包级 FAIL），后续 5+ 次未复现，A 新测试隔离压测稳定→判定为预存 flaky 测试，非本 session 引入。
- [x] 3.2 实现 structured logs、metrics 与 traces；详见下方同编号任务。
- [x] 3.3 建立 limits 与 managed security defaults；详见下方同编号任务。
- [x] 4.1 参数化 Taskfile；详见下方同编号任务。
  - **Evidence**: 2026-07-22 协调者复验（前序 session 已参数化，本 session 确认 acceptance 全部满足）。`Taskfile.yml` `SPEC_CHANGE: '{{default "workbench-production-foundation-r0" .SPEC_CHANGE}}'`（默认 R0，可覆盖）；`spec:status`/`spec:validate` 用 `{{.SPEC_CHANGE}}`；无硬编码旧 Open Design change id（`PREVIEW_PROJECT_ID` 为可覆盖 preview project，非 change 引用）；`production:guard` `DRY_RUN: '{{default "1" .DRY_RUN}}'`（release 命令默认只读）。scope 内命令齐备：`build`/`preview`(=dev)/`db:migrate`/`db:migrate:check`/`db:backup`/`db:restore:verify`/`db:restore:verify:release`/`release:manifest:*`/`release:handoff:*`/`release:openspec:*`/`production:ops:test` + 分层 `*:test` 命令（task --list 281 discoverable）。Verification 三命令独立复验通过：`task spec:status SPEC_CHANGE=workbench-production-foundation-r0`（4/4 artifacts complete）、`task spec:validate SPEC_CHANGE=workbench-production-foundation-r0`（valid）、`task --list` 281 tasks。默认 change 为 R0，release 命令默认只读，旧 Open Design 不再硬编码。
- [x] 4.2 建立 reproducible container 与 smoke；详见下方同编号任务。
  - **Evidence (2026-08-10)**: 当前工作树复验 `task container:contract:test`（5 pass/0 fail）、`task container:plan:test`（Go package pass）、`task build:reproducibility:test`（5 pass/0 fail）与 `task container:smoke PROFILE=worker`（exit 0）。Smoke 实际构建 pinned multi-target Dockerfile，检查 worker image `USER=65532`/entrypoint，并以 `--read-only --cap-drop=ALL --security-opt=no-new-privileges --network=none` 启动；worker 按预期以 `bootstrap_failed` fail-closed，stdout 为空且 stderr 不含测试凭据或本地 registry 路径。`dist/release/build-metadata.json` 与四个 container entrypoint artifacts 存在。该证据只关闭本地 container implementation/smoke，不替代 staging restore、DB outage 或生产 promotion。
- [x] 4.3 强化 evidence runner 与 readiness report；详见下方同编号任务。
  - **Evidence**: 2026-07-24（subagent + 协调者独立验收）。新 `service/cmd/workbench-readiness`（读真实 evidence/run metadata → 机读就绪报告 JSON/agent/explain/summary，4 gates `evidence_present/fresh/passing/digest_valid`，任一失败 exit 5，重算 SHA256 检 drift，绝对路径脱敏 `[REDACTED]`；13 测试）+ 强化 `scripts/test-evidence/run.ts`（interrupt-safe SIGTERM/SIGINT 保留 exit code、disk-full 容忍、`digest.json`/`redaction.json`、summary gate 聚合、六件套；14 测试）+ Taskfile `release:readiness` 改 evidence-based（workbench-readiness `--evidence-root`，ENV 默认 local）+ workbench-release 向后兼容修复（evidence_source.go 逐字段比较）。全量 gate 0 FAIL + typecheck + openspec strict valid。Agent 不手写状态；missing/failed/expired/drift=fail。
- [x] 5.1 执行 contract/integration/security/race gate；详见下方同编号任务。
  - **Evidence (2026-07-24)**: `openspec validate --strict` valid · `buf lint` exit0 · `bun run typecheck` exit0 · `bun test` 280 pass/5 skip/0 fail (64 files) · `bun run test:contract` 118 pass/0 fail · `bun run test:integration` exit0 (conformance+runtime under evidence runner) · `CGO_ENABLED=1 go test -race ./service/...`（带 disposable PG，umask 077）overall exit0、**0 FAIL、无 DATA RACE/panic/timeout**。现有 behavior 不回归、新增 capability 不伪造 available。evidence 完整。
- [ ] 5.2 执行 container/migration/restore/observability gate；详见下方同编号任务。
- [x] 5.3 独立 review/security review；详见下方同编号任务。
  - **Evidence (2026-07-24)**: 独立 review subagent（read-only）审未提交 diff（lease advisory-lock / backup PG env+pg_restore -d / board sentinel 映射 / admin wiring / workbench-readiness / evidence runner）+ committed 关键文件。**P0=0 / P1=0 / P2=8**（均不影响权限/数据完整性/回滚/evidence）→ **approved for R1/R2 consumption**。已修 2 个 P2：P2-3（admin listener 启动失败前优雅 Shutdown 主服务）+ P2-6（workbench-readiness `verifyDigests` 加路径穿越防御）。余 P2 follow-up（P2-2 board sentinel 一致性 pre-existing+CAS 兜底；P2-1/P2-5/P2-7/P2-8 次要）。`git diff --check` 无空格错。
- [x] 5.4 R0 Closeout；详见下方同编号任务。
  - **Evidence (2026-07-24)**: Closeout 审计完成。**12/16 task 强证据闭环**（1.1/1.2/2.1/2.2/2.3/2.4/3.1/4.1/4.3/5.1/5.3/5.4）。**4 项显式 blocked 未豁免**：3.2/3.3（R1 scope：approved issuer validator / 真实 collector / deploy 校验——interface+fail-closed stub 就位）、4.2（Docker daemon 不可达→container smoke 阻塞）、5.2（container gate 阻 Docker；migration/restore/observability 部分已验）。`workbench-readiness` 机读报告 status=ready（266 evidence run，4 gates pass）。全量 gate（带 PG）0 FAIL + `-race` 0 race + openspec strict valid + 独立 review approved。R0 production-foundation 核心 contract 生产就绪、R1/R2 可消费；无业务功能夹带（R0 仅合同+生产基础）。真实 archive 由用户决定。
  - **Re-check (2026-07-28)**: 3.2/3.3 收口（各见同编号 evidence），R0 现 **14/16 强证据闭环**。3.2——§7 OTel gate = disposable collector endpoint + delivery/outage evidence（非 deploy 级 collector），升级 OTLP delivery test 用官方 proto 解析真实 `ExportTraceServiceRequest` 校验 service.name，并修 TracerProvider/MeterProvider 未注入 `service.name` Resource 的 bug（导出 span 原 `unknown_service:<binary>`）。3.3——acceptance 逐项有测试覆盖，新增 slowloris 行为测试（`slow_client_test.go`），profile gate `managed_listener_gate_test` 6 test + `approved_issuer_test` 7 test 覆盖；§8 #6 不适用（3.3 非 §7 PG/OTel/container gate）。余 2 项仍 blocked：4.2/5.2 需 Docker daemon（sandbox 不可达，container smoke 无法执行），保持 partial。

  - **Re-check (2026-08-10)**: 4.2 的本地 reproducible container 与 worker smoke 已通过并关闭；R0 当前 15/16 任务有强证据。仅 5.2 仍保持 pending：当前证据没有 staging restore、DB outage、managed migration 或 production observability gate，不能用本地 Docker/SQLite happy path 替代。

## 执行规则

- R0 不修改 Pane/Asset/Team/WorkItem/Board 用户功能；只建立合同与生产基础。
- 新增复杂配置、migration、并发 lease、readiness、shutdown、redaction 和 protocol conversion 时使用中文注释说明不变量与失败语义。
- 同一 writer 同时拥有生产代码与相关测试；独立 reviewer/test-engineer 在稳定 diff 后验证。
- integration/system/security/performance 测试写 `temp/integration-test-runs/<run-id>/` 脱敏证据并保留原退出码。

## 1. Contract Foundation（Lane A）

### 1.1 盘点并冻结 envelope/错误/能力
- **Owner**: implementer
- **Dependencies**: umbrella 0.1
- **Paths**: `proto/**`, `schemas/**`, `packages/task-sdk/src/**`, `docs/interfaces/**`
- **Scope**: 冻结通用 refs/version/idempotency/cursor/correlation/receipt/error 与 Asset/Team/WorkItem/Layout/Board/Search/Audit/Handoff capability descriptors。
- **Acceptance**: additive；未实现 operation 明确 unavailable/needs_contract；不包含领域正文。
- **Verification**: `buf lint && bun run test:contract`
- **Expected**: schema/SDK contract tests 通过。
- **Failure recheck**: 比较当前 Task/Owner/Runtime contracts，删除重复 envelope 和 synthetic success。

### 1.2 扩展 Operation registry 与四 transport parity
- **Owner**: implementer
- **Lane**: A
- **Dependencies**: 1.1
- **Paths**: `service/internal/registry/**`, `service/internal/transport/**`, `packages/task-sdk/**`, `tests/conformance/**`
- **Scope**: 注册新 capability shell，自动投影 SDK/HTTP/gRPC/JSON-RPC 与稳定 errors。
- **Acceptance**: 无 operation-specific transport handler；未实现 mutation 不创建 Task 或成功 receipt。
- **Verification**: `bun run test:contract && CGO_ENABLED=0 go test ./service/internal/registry/... ./service/internal/transport/...`
- **Expected**: operation × transport matrix 完整。
- **Failure recheck**: 扫描 transport switch/handler 与 registry drift。

## 2. Configuration and Persistence（Lane B）

### 2.1 实现分层配置与启动校验
- **Owner**: implementer
- **Lane**: B
- **Dependencies**: 1.1
- **Paths**: `service/internal/config/**`, `service/cmd/workbenchd/**`
- **Scope**: flag/env/user-config/default precedence、local/managed profile、safe diagnostics 与 fail-fast validation。
- **Acceptance**: managed 缺关键配置时不绑定 public listener；local 默认 loopback；secret value 不进入错误。
- **Verification**: `CGO_ENABLED=0 go test ./service/internal/config/... ./service/cmd/workbenchd/...`
- **Expected**: precedence、invalid、secret redaction tests 通过。
- **Failure recheck**: fuzz DSN/URL/path/header 并扫描输出。
- **Current evidence (partial, 2026-07-20)**: `ProfileLocal`/`ProfileManaged`、`WORKBENCH_PROFILE` 与 `WORKBENCH_DATABASE_URL` 已接入；managed URL 只接受 PostgreSQL scheme 且验证错误不回显 credential。当前仍只有 flag/env/default，user-level config 与 approved managed auth/issuer 尚未完成，因此本任务不关闭。

### 2.2 建立 SQLite/PostgreSQL GORM profiles
- **Owner**: implementer
- **Lane**: B
- **Dependencies**: 2.1
- **Paths**: `service/internal/repository/**`, `service/internal/app/**`, `service/go.mod`, `service/go.sum`
- **Scope**: repository factory、pure-Go SQLite、PostgreSQL、pool/timeouts/transactions、profile parity。
- **Acceptance**: local `CGO_ENABLED=0`；managed 不依赖本地 path；普通 service 无 raw SQL。
- **Verification**: `CGO_ENABLED=0 go test ./service/internal/repository/... && task test:integration DB_PROFILE=postgres`
- **Expected**: repository parity 在两 profile 通过。
- **Failure recheck**: 检查 driver、transaction isolation、connection leak 和 GORM query scopes。
- **Current evidence (partial, 2026-07-20)**: `repository.OpenDatabase` 已选择 pure-Go SQLite 或 GORM PostgreSQL driver，并支持 `auto_migrate`/`external_migration` schema policy；local runtime 保留 additive auto migration，managed runtime 只连接、不修改 schema。`CheckReady` 检查连接、migration version/checksum 与六类必需 Task schema。`CGO_ENABLED=0 go build` 与 Go 定向/全量测试通过。`TestPostgresRepositoryIntegration` 已改为显式 external migration 流程，但当前环境无 disposable `WORKBENCH_TEST_POSTGRES_URL`/Docker，真实 Postgres parity 仍未验证，因此本任务不关闭。**2026-07-23 解锁 disposable PG（本机 PG14）后真实 parity 已验证**：三 PG 集成测试确定性 PASS；修复 board 冲突契约 bug（`graph_service.go beginGraphMutation` 把 `DomainError{Code:ErrorVersionConflict}` 映射为 `boards.ErrRevisionConflict` sentinel，统一预检与 CAS 两并发失败路径；board 包 `-race` 无回归）。repository parity 两 profile 通过，本任务关闭。（2026-07-24 更正：先前"schema_migrations-seed gap"为误诊——查错表名，CheckReady 在 CLI 迁移库实际 PASS。）

### 2.3 实现 migration/backfill framework
- **Owner**: implementer
- **Lane**: B
- **Dependencies**: 2.2
- **Paths**: `service/internal/migrations/**`, `service/cmd/workbench-migrate/**`, `service/internal/repository/**`
- **Scope**: id/checksum/range、expand/backfill/shadow/cutover metadata、cursor/lease/pause/resume 与 migration receipt。
- **Acceptance**: fresh/upgrade/interrupted/resume/schema mismatch；destructive migration 默认拒绝。
- **Verification**: `task db:migrate:check`
- **Expected**: SQLite/PostgreSQL migration matrix 通过。
- **Failure recheck**: 中断 backfill、并发 migration、checksum drift 后重跑。
- **Current evidence (partial, 2026-07-20)**: 新增 `workbench-migrate -command up|check`，使用环境中的 managed DSN，支持 additive fresh migration、不可变 migration id/checksum/applied time、只读 schema check 与 checksum drift fail-closed；managed runtime 不再调用 `AutoMigrate`。SQLite command tests 与 checksum mismatch tests 通过。**2026-07-22 补齐框架**（并行 subagent + 协调者独立验收，`-race`+全量绿、strict valid）：新包 `service/internal/migrations/`（`phase` expand/backfill/shadow/cutover/contract 阶段元数据 + 破坏性 fail-closed、可恢复 backfill `Cursor`(watermark/Advance/Complete)、`Lease`(并发互斥 `ErrLeaseHeld` + 过期抢占)+pause/resume、`Receipt`(链式 builder + DSN/path 脱敏 `<dsn>`/`<path>`)、`RollbackPolicy`(RejectByDefault 三重校验)、`Runner`(分批 backfill + context cancel 用 background ctx 存 cursor 返回 `ErrBackfillPaused` 续跑)）+ `repository/migration_metadata.go`（GORM `MigrationRunModel`/`MigrationCursorModel` additive 表 + lease/cursor/receipt CRUD + `EnsureMigrationMetadataTables`）+ `workbench-migrate` 新增 `status` 命令、`up` 接 lease/产 receipt。测试 migrations 32 + migrate 5 + repository metadata 11 全绿。**仍缺真实 PostgreSQL matrix**（lease/cursor/receipt 在 PG 的事务隔离、`CURRENT_TIMESTAMP` 兼容、跨版本 upgrade、PG advisory lock 替代）——无 disposable PG 环境不可验，按 §8 #6 保持 partial，本任务不关闭。**2026-07-23 解锁 disposable PG 后验证**：`workbench-migrate -command up`（fresh，应用 15 版本）+ `check`（checksum 通过）+ `status`（"no active lease" + succeeded run + receipt available）在真实 PG 上通过——receipt/lease/status 框架 PG 可用。**⚠️ 发现关键 gap（阻塞 2.3/2.4 生产就绪）**：CLI 把 schema 版本写入 `migration_runs`（B 的元数据表），**不 seed `schema_migrations` 表**（repository `CheckReady`/runtime `/readyz`/backup 查的表；`currentSchemaVersion=0015_task_idempotency_scope_v2` 与 CLI 末版一致但跟踪表不同）→ CLI 迁移的库上 CheckReady 失败。需设计决策：CLI `up` 额外 seed `schema_migrations(version=0015, checksum=currentSchemaChecksum)`，或 CheckReady 改读 migration catalog。lease/cursor/receipt 在 PG 的并发/事务隔离专项测试仍缺（现仅 SQLite）。按 §8 #6 保持 partial。**2026-07-24 更正**：上述"CLI 不 seed schema_migrations → CheckReady 失败"为**误诊**——查错表名（`schema_migrations` vs 真实 `workbench_schema_migrations`）；CLI 迁移引擎 `migration_engine.go:60` 本就 seed 该表，CheckReady 在 CLI 迁移库上实际 PASS。2.3 真·剩余项：lease/cursor/receipt 在 PG 的并发/事务隔离专项测试（现仅 SQLite）、跨版本 upgrade matrix。按 §8 #6 保持 partial。**2026-07-24 收口**（subagent + 协调者验收）：新增 8 PG 集成测试覆盖 lease 互斥/cursor 续跑/receipt/跨版本 upgrade/过期 lease；**修 `AcquireMigrationLease` PG 竞态 bug**（read-then-insert → `pg_advisory_xact_lock` 序列化，并发探测 x10 确定性 pass）。两 profile matrix 通过，acceptance 达标本任务关闭。

### 2.4 建立 backup/restore verification contract
- **Owner**: implementer
- **Lane**: B
- **Dependencies**: 2.3
- **Paths**: `service/internal/backup/**`, `service/cmd/workbench-backup/**`, `service/cmd/workbench-restore-verify/**`, `scripts/test-evidence/**`, `Taskfile.yml`, `docs/runtime/**`
- **Scope**: provider-neutral backup metadata、checksum、retention status、restore-to-disposable verification 和 safe receipt。
- **Acceptance**: 默认不操作 production；无有效 restore evidence 时 release readiness 失败。
- **Verification**: `task db:backup DB_PROFILE=local DB_DATA_PATH=var/workbenchd.db BACKUP_PATH=temp/backups/workbench.db && task db:restore:verify BACKUP_PATH=temp/backups/workbench.db`
- **Expected**: backup/restore checksum 与 schema version 一致。
- **Failure recheck**: 损坏、过期、错误 profile、缺 key 场景保持 fail-closed。
- **Current evidence (partial, 2026-07-20)**: 新增 `workbench-backup`、`workbench-restore-verify`、`db:backup` 与 `db:restore:verify`。SQLite 通过参数绑定 `VACUUM INTO` 生成私有一致性 artifact；PostgreSQL 固定使用 `pg_dump/pg_restore`，DSN 仅进入 `PGDATABASE` 环境，backup/restore path 通过 stdout/stdin 重定向。CLI 生成 manifest，记录 schema version/checksum 与 artifact size/SHA-256，且不含 DSN/源路径；restore 先验 checksum，再恢复到 disposable target 并执行 schema readiness，integration evidence 由 runner 生成。SQLite 真实 backup/restore、tamper、私有权限和 Task/Receipt/Event 业务记录保持测试通过；相对 backup/manifest/output 参数在 `command.txt` 与 `summary.json` 中统一脱敏。`CGO_ENABLED=0 go test ./service/...`、聚焦 race、`task build`、contract/security/observability tests 与 strict OpenSpec validation 均通过。真实 PostgreSQL restore、跨版本 migration、更多业务行级 invariant、retention/encryption/PITR 与 RPO/RTO drill 尚未完成，因此本任务不关闭。**2026-07-23 解锁 disposable PG 后**：发现并修复 backup/restore PG 路径真 bug——`PGDATABASE: databaseURL`（整 URL 进 PGDATABASE）使 libpq 把整串当库名→连接失败（2026-07-20 evidence 声称 work 但从未对真 PG 验）。修复为 `postgresConnectionEnv` 拆 PGHOST/PGPORT/PGUSER/PGPASSWORD/PGDATABASE/PGSSLMODE（密码只进 PGPASSWORD env，不进 argv/manifest，脱敏不变）+ 同步 `backup_test.go` 断言；手动 `pg_dump`/`PGDATABASE=裸名` 验证通过。**⚠️ 仍阻塞**：backup `checkDatabaseReady`（pg_dump 前）在 CLI 迁移库上失败，根因同 2.3 的 schema_migrations-seed gap（CheckReady 查 `schema_migrations`，CLI 未 seed）。待该 gap 修复后 PG backup/restore-verify 可收口。retention/encryption/PITR/RPO-RTO drill 仍缺。按 §8 #6 保持 partial。**2026-07-24 更正 + 修第二 bug + 全链路验证**：上述"checkDatabaseReady 在 CLI 迁移库失败 / schema_migrations gap"为**误诊**（查错表名，CheckReady 实际 PASS）；真因是第二个 backup/restore bug——`pg_restore` 需显式 `-d <dbname>`（无 `-d`/`-f` 报错），`postgresRestoreCommand` 加 `-d`（连接参数仍走 PG* env）。修后 **PG backup→restore-verify 全链路通过**（backup manifest schema 0015/artifact 161988B/SHA-256 无 DSN + restore-verify receipt `verified` + 恢复库有 0015 schema 行）。acceptance + Expected 达标，本任务关闭。retention/encryption/PITR/RPO-RTO drill 为 R0+ 增强 follow-up。

## 3. Runtime and Observability（Lane C）

### 3.1 分离 health/readiness 与 graceful shutdown
- **Owner**: implementer
- **Lane**: C
- **Dependencies**: 2.2, 2.3
- **Paths**: `service/internal/runtime/**`, `service/internal/app/**`
- **Scope**: liveness、DB/schema/registry/config readiness、drain 顺序与 bounded shutdown。
- **Acceptance**: DB/migration/registry 失败 ready=false；可选 Owner offline 不影响核心 ready；SSE/HTTP/gRPC 正确 drain。
- **Verification**: `CGO_ENABLED=1 go test -race ./service/internal/runtime/...`
- **Expected**: dependency injection 与 shutdown tests 通过。
- **Failure recheck**: 注入慢请求、stuck SSE、worker、DB close 与 panic。
- **Current evidence (partial, 2026-07-20)**: `/healthz` 保持 liveness；`/readyz` 已聚合 lifecycle、GORM database ping、migration version/checksum、必需 schema 与 Operation registry seal/digest，任一失败或 shutdown drain 开始后返回脱敏 `503`。聚焦测试覆盖组件聚合、错误脱敏、shutdown not-ready 与数据库连接关闭。2026-07-22 补齐（并行 subagent + 协调者独立验收，`-race`+全量绿、strict valid）：required-worker readiness（`workerTracker` fail-closed）+ SSE/HTTP/gRPC 有界 drain matrix（`streamTracker` 取消活跃 SSE + 5 步 Shutdown）+ LB 传播窗口（readiness 先翻 false，bounded wait ≤ ctx deadline 1/3）；新增 26 测试。acceptance 达标本任务关闭。**非阻塞 follow-up**：LB 真实 deploy 校验、gRPC streaming 细粒度取消、drain 拒绝 code 字面量微调。

### 3.2 实现 structured logs、metrics 与 traces
- **Owner**: implementer
- **Lane**: C
- **Dependencies**: 2.1, 3.1
- **Paths**: `service/internal/observability/**`, `service/internal/runtime/**`, `service/cmd/workbenchd/**`, `apps/web/src/lib/telemetry/**`
- **Scope**: correlation、request/DB/registry/Task metrics、OTel exporter、bounded failure 与 shared sanitizer。
- **Acceptance**: exporter down 不阻塞；无 token/raw payload/body/path/PII；metrics label 低基数。
- **Verification**: `task test:observability && task test:security`
- **Expected**: correlation、redaction、exporter outage tests 通过。
- **Failure recheck**: 注入超大 error、恶意 refs、exporter timeout 并扫描 artifacts。
- **Current evidence (partial, 2026-07-20)**: 新增统一 `observability.System`，使用 `slog` JSON handler 与 shared sanitizer，HTTP/JSON-RPC/gRPC 生成受控 `request_id`、OTel `trace_id`、`call_id`、稳定 operation、status 和 duration；原始 URL/query/body、request object、error detail、Authorization、DSN、credential 与私有路径不进入日志。调用指标只以 transport、固定 HTTP 路由族/受控 gRPC method 和 status class/canonical code 聚合。认证后的 `/v1alpha1/diagnostics/metrics` 返回 `workbench.observability.v1` call、lifecycle/database/registry readiness、Task status transition、DB query status/duration 与 pool gauge，未认证返回 `401`，且 no-store；Task transition 只在持久化成功后按固定状态计数，幂等 replay 不重复，不含 operation/Task/workspace/project/tenant/user label；GORM observer 不调用 SQL closure，只接收 `ok/not_found/error` 与 duration。首次加入真实 Task 提交的 component smoke 在 `20260720083434-8742a2fa-26a2-40b0-b251-57a9e432b2a9` 发现 GORM 默认 `record not found` SQL logger 写 stdout，失败 evidence 已保留；关闭 GORM 默认 logger 后，component evidence `20260720083914-06ec0b09-82b1-4e95-bcf3-d086cfbead29` 证明真实 binary 的 health/readiness、metrics 401/200、Task API、Task/DB metrics、SIGTERM、JSON-only stderr/stdout-empty，且无 SQL 或业务值泄漏。OTel SDK 使用有界 batch processor 与 export timeout，支持 `WORKBENCH_OTEL_EXPORTER_OTLP_ENDPOINT`，非法或含 userinfo endpoint 在启动前以脱敏错误拒绝；exporter failure 不阻塞 request，OTel 内部 error 也转换为 generic structured event。Integration evidence `20260720083030-8d270649-00de-4216-b7e0-db6a69906b36` 证明 official OTLP/HTTP exporter 向 disposable receiver 交付非空 protobuf span payload，且 receiver 返回 `503` 时业务 request 不阻塞、flush/shutdown 保持有界。staging collector 长时 outage/retry、OTLP metrics exporter、独立 admin listener、Web telemetry 和跨服务 propagation 尚未完成，因此本任务不关闭。**2026-07-23 补齐**（并行 subagent + 协调者独立验收，全量 `-race` 0 FAIL + strict valid）：OTLP/HTTP metric exporter（`metricProducer` 低基数 + bounded batch/timeout/exporter-down 非阻塞）+ staging collector 长时 outage/retry（bounded backoff + queue + shutdown flush deadline；长时 outage 不阻塞业务已验证）+ 跨服务 trace context 传播（W3C traceparent HTTP/gRPC inject/extract，无新依赖）+ Web/BFF propagation（`apps/web/src/lib/telemetry` traceparent + tracedFetch，18 测试）。27 Go + 18 Web 测试绿。**独立 admin listener**：`observability/admin_listener.go` 自包含组件（Bearer auth fail-closed、nil System/ReadinessFunc→503、安全头、bounded shutdown）**已接线**（2026-07-24 Goal C）：runtime 新增 `readinessProbes`/`isReady()`/`StartAdminListener()`，workbenchd 经 `WORKBENCH_ADMIN_ADDRESS`/`WORKBENCH_ADMIN_TOKEN` env 启动（空地址不启动）+ shutdown 流程关闭；wiring 测试绿（admin_listener_wiring_test.go）。**2026-07-28 收口**：§7「外部环境门禁」定义 OTel gate = *disposable collector endpoint* + *trace/metric outage and delivery evidence*（非 deploy 级 production collector）。现有测试已满足：`TestOTLPHTTPExporterDeliversCompletedSpan`（delivery）+ `TestOTLPHTTPExporterOutageIsBounded`（outage）+ `TestExporterFailureDoesNotBlockRequest`（非阻塞）+ `TestMetricsDiagnosticsReturnsVersionedLowCardinalitySnapshot`（低基数）+ `TestOTLPExporterRejectsUnsafeEndpointWithoutEchoingIt`（credential 不入 argv/log）使用 **official OTLP/HTTP exporter → disposable OTLP collector endpoint**。本次升级该 receiver 为**真实 OTLP-collecting endpoint**：用官方 `go.opentelemetry.io/proto/otlp/collector/trace/v1` + `google.golang.org/protobuf/proto` 解析收到的 protobuf 为 `ExportTraceServiceRequest`，校验 `ResourceSpans` 非空、含真实 span、且 `service.name` == 配置值——端到端 OTel 合规证据。**并修真实 bug**：`observability.New` 先前未给 TracerProvider/MeterProvider 注入 `service.name` Resource，导出 span 显示 `unknown_service:<binary>` 而非配置服务名；现 `resource.NewSchemaless(attribute.String("service.name", config.Service))` 注入 trace+metric provider（低基数单值），delivery test 校验 `service.name == "workbenchd"` 通过。Verification（umask 077）：`task test:observability`（observability+runtime ok）+ `task test:security`（1 pass）+ `-race` observability/runtime/transport 全 ok 0 race + `go vet` clean + `go build ./...` exit0。acceptance（exporter down 不阻塞 / 无 token·raw payload·body·path·PII / 低基数 label）全部达标；§7 OTel gate + §8 #6「real OTel 证据」满足（disposable collector endpoint 解析真实 OTLP protobuf + delivery/outage/non-blocking evidence）。本任务关闭。真实 OTel backend (Jaeger/Tempo) 长时 retention 为 R0+ 增强 follow-up。

### 3.3 建立 limits 与 managed security defaults
- **Owner**: implementer
- **Lane**: C
- **Dependencies**: 2.1, 3.1
- **Paths**: `service/internal/security/**`, `service/internal/runtime/**`, `apps/web/src/bff/**`
- **Scope**: auth/issuer、exact Origin/Host、bearer/no-Cookie CSRF policy、CSP、统一 body/header/query/batch/cursor/page limits、SSE semaphore/lease、HTTP timeouts 与 local loopback defaults。
- **Default contract**: body `1 MiB`、headers `32 KiB`、query `8 KiB`、JSON-RPC batch `32`、page `100`、SSE `32` connections/process、lease `5 minutes`；只允许通过配置收紧或设置批准的正数值。
- **Error contract**: body `413 request_too_large`；URI/query `414 request_uri_too_long`；SSE exhaustion `429 rate_limited` + bounded `Retry-After`；Host/Origin `403 forbidden`；错误和日志不得回显 raw input、credential、Owner payload、DSN 或 private path。
- **Profile gate**: local/当前 managed 只允许 loopback；R1 approved issuer/session validator 完成前，managed public listener 必须在 listen 前 fail-fast，禁止 local-session 公网回退。
- **Acceptance**: spoofed Host/Origin、Content-Length/chunked oversized body、query/cursor abuse、slow client、SSE exhaustion/timeout/cancel/shutdown、invalid issuer 与 auth outage 均 fail-closed；浏览器 Authorization/动态 Owner URL 被丢弃；lease 无泄漏。
- **Verification**: `task test:security:component && task test:security && CGO_ENABLED=0 go test ./service/internal/security ./service/internal/runtime ./service/internal/transport/... && CGO_ENABLED=1 go test -race ./service/internal/security ./service/internal/runtime ./service/internal/transport/...`
- **Expected**: unit、component abuse matrix 与 race 通过，并生成脱敏 evidence 六件套。
- **Failure recheck**: fuzz headers/origin/URL/body/cursor，注入 chunked body、stuck stream、client cancel、shutdown 和并发 lease，扫描跨租户与 secret 泄漏。
- **Current evidence (partial, 2026-07-20)**: private local-session token、loopback listener、bearer/no-Cookie/JSON-only mutation、exact Host/Origin structure/scheme/host/port 已统一到 `service/internal/security`。共享 limits 默认 body `1 MiB`、headers `32 KiB`、query `8 KiB`、SSE `32`/process、lease `5 minutes`，配置只能收紧；runtime 显式设置 `MaxHeaderBytes`，CLI 提供不含 credential 的 limit flags。REST 与 JSON-RPC 的 Content-Length/chunked oversized body 均返回稳定 `413 request_too_large`，query 返回 `414 request_uri_too_long`，application header budget 返回 `431 request_headers_too_large`，SSE exhaustion 返回 `429 rate_limited` + `Retry-After`；cancel、lease timeout 与 handler return 都释放 slot。Component evidence `20260720085425-07fb752d-c9fb-4ed8-b502-fb5493f55de6` 使用真实 binary 和收紧阈值验证 hostile Origin、body/header/query、两条 SSE 容量、第 3 条拒绝、cancel 后重入、lease 到期、SIGTERM 与日志 secret scan，六件套通过且 stdout/stderr 为空。Go transport/unit、Bun local-session 与 race matrix 通过。R1 approved issuer/session validator、managed public listener、Web/BFF Authorization/Owner delegation、response budget、distributed rate limit 与 auth outage integration 尚未完成，因此本任务保持 partial。**2026-07-23 补齐**（并行 subagent + 协调者独立验收，全量 `-race` 0 FAIL + strict valid）：managed 公网 listener **启动期 fail-fast**（`Start()` 在 `net.Listen` 前调 `requireApprovedIssuerForPublicListener`：managed+非 loopback+无 approved validator→exit 非零，绝不公网 local token 回退；`isNonLoopbackListenerAddress` 识别 `0.0.0.0`/`[::]`/公网 IP）+ approved issuer/JWKS validator **interface**（`security/approved_issuer.go`：`ApprovedIssuerValidator` interface + `ApprovedIssuerRegistry` 空=fail-closed + 不可变注册，不存 credential）+ BFF/service-delegation 边界（`apps/web/server/handler.ts` 剥离 `x-owner-url`/`x-target-url`/`x-backend-url` SSRF 注入头 + `www-authenticate` 响应头，不透传用户 Authorization）。7+6+1 测试绿。**仍 partial**：R1 真实 JWKS validator 实现（interface+fail-closed stub 已就位）、managed real-deploy 校验、response budget、distributed rate limit、auth outage integration、非阻塞 hardening（主机名非 IP listener 绕过 gate 的 defense-in-depth）。按 §8 #6 保持 partial。**2026-07-28 收口**：§7 外部门控仅列 PostgreSQL/restore/container/OTel；3.3 为传输层安全限制，**不依赖**其中任一（DB-agnostic、非 telemetry、profile gate fail-fast 已本地验证）。逐项核对 acceptance 证据：① spoofed Host/Origin fail-closed（security_test `TrustedHost`/`TrustedOrigin`）② oversized body（`HTTPPolicy` MaxBytesReader → 413）③ query/cursor abuse（MaxQueryBytes → 414）④ **slow client**——新增 `internal/runtime/slow_client_test.go::TestHTTPServerRejectsSlowClientHeaders`（slowloris 行为测试：partial header → ReadHeaderTimeout 到期服务端主动断连，150ms 确定性 PASS）+ runtime.go:816 生产 http.Server 配 ReadHeaderTimeout=5s/ReadTimeout=15s/IdleTimeout=60s ⑤ SSE exhaustion/timeout/cancel/shutdown（`StreamLimiter`+drain）⑥ invalid issuer fail-closed（`approved_issuer_test` 7 test：空 registry=fail-closed）⑦ auth outage fail-closed（`ErrIdentityUnavailable`→503 identity_unavailable，HTTP/JSON-RPC/gRPC 四 transport 测试）⑧ 浏览器 Authorization/动态 Owner URL 丢弃（BFF strip `x-owner-url` 等）⑨ lease 无泄漏（StreamLimiter defer release）。Profile gate（managed 公网 listener 启动期 fail-fast）由 `managed_listener_gate_test` 6 test 覆盖（fail-fast without approved issuer / loopback ok / local ok / succeeds with validator / fails empty registry / non-loopback detection）。Verification（umask 077）：`CGO_ENABLED=0 go test ./internal/security ./internal/runtime ./internal/transport/...` 全 ok + `-race` 全 ok 0 race + `go vet` clean。acceptance 全部达标；§8 #6 不适用（3.3 不在 §7 的 PG/OTel/container gate 内）。本任务关闭。R1 真实 JWKS validator、managed real-deploy、response budget、distributed rate limit 为 R0+ / R1 follow-up（非 R0 acceptance）。

## 4. Build and Developer Workflow（Lane D）

### 4.1 参数化 Taskfile
- **Owner**: implementer
- **Lane**: D
- **Dependencies**: 1.1
- **Paths**: `Taskfile.yml`, `package.json`, `README.md`, `docs/runtime/**`
- **Scope**: `SPEC_CHANGE`、dev/preview/build、分层测试、migration、backup/restore verify、release readiness 与 rollback dry-run。
- **Acceptance**: 默认 change 为 R0 或 umbrella；不再硬编码旧 Open Design；release 命令默认只读。
- **Verification**: `task --list && task spec:status SPEC_CHANGE=workbench-production-foundation-r0 && task spec:validate SPEC_CHANGE=workbench-production-foundation-r0`
- **Expected**: 命令可发现并运行。
- **Failure recheck**: 搜索旧 change id、真实 URL、credential 与未包 evidence runner 的长任务。
- **Current evidence (partial, 2026-07-20)**: `SPEC_CHANGE`、`build`、`test:unit`、`test:contract`、`test:integration`、`test:security`、`test:observability`、`test:postgres`、`db:migrate`、`db:migrate:check`、`db:backup` 与 `db:restore:verify` 已落地；build 同时产出 `workbenchd`、`workbench-migrate`、`workbench-backup` 与 `workbench-restore-verify`，数据库 URL 不进入 argv。Migration 与 SQLite backup/restore 本机流程、evidence path redaction 及既有 contract/security gate 通过。Container 与 release readiness targets 尚未实现，因此本任务不关闭。

### 4.2 建立 reproducible container 与 smoke
- **Owner**: implementer
- **Lane**: D
- **Dependencies**: 2.1, 3.1
- **Paths**: `Dockerfile`, `.dockerignore`, `deploy/**`, `scripts/container/**`
- **Scope**: pinned build inputs、non-root minimal image、web assets/workbenchd、health/readiness 与 graceful stop smoke。
- **Acceptance**: 镜像无源码/secret/token/local DB；local/managed config 清晰；SBOM/build metadata 可追溯。
- **Verification**: `task build && task container:smoke`
- **Expected**: image startup/readiness/shutdown 通过。
- **Failure recheck**: inspect layers、user、ports、signal、filesystem writes 与 embedded config。
- **Evidence (2026-08-10)**: 静态 container contract、container plan、reproducibility tests 与真实 Docker worker smoke 均通过；镜像非 root、只读/去能力/无网络、启动失败不泄漏敏感值，并保留可追溯 build metadata。staging/production container promotion 仍由 5.2 gate 控制。

### 4.3 强化 evidence runner 与 readiness report
- **Owner**: implementer
- **Lane**: D
- **Dependencies**: 3.2, 4.1
- **Paths**: `scripts/test-evidence/**`, `tests/evidence/**`, `docs/operations/**`
- **Scope**: contract digest、app/dependency versions、redaction status、原退出码、failure artifacts 与 machine-generated readiness report。
- **Acceptance**: Agent 不手写 summary；失败也保留六件套；release report 可追踪每个 gate。
- **Verification**: `bun test tests/evidence && task test:integration`
- **Expected**: success/failure/redaction/interrupt tests 通过。
- **Failure recheck**: 注入 secret、signal、disk full、invalid env 并检查 evidence 完整性。
- **Evidence (2026-07-24 complete)**: 新 `service/cmd/workbench-readiness`（读真实 evidence/run metadata 生成机读就绪报告 JSON/agent/explain/summary；4 gates `evidence_present/fresh/passing/digest_valid`，任一失败 exit 5；重算 SHA256 检 drift；绝对路径脱敏；13 测试）+ 强化 `scripts/test-evidence/run.ts`（interrupt-safe SIGTERM/SIGINT 保留 exit code、disk-full 容忍、`digest.json`/`redaction.json`、summary gate 聚合、六件套；14 测试）+ `release:readiness` 改 evidence-based + workbench-release 向后兼容修复（evidence_source.go 逐字段比较）。`bun test tests/evidence`(14) + `go test ./service/cmd/workbench-readiness`(13) + 全量 gate（带 PG）0 FAIL + typecheck + openspec strict valid。Agent 不手写状态；missing/failed/expired/drift=fail。

## 5. Verification and R0 Closeout（Lane E）

### 5.1 执行 contract/integration/security/race gate
- **Owner**: test-engineer
- **Lane**: E
- **Dependencies**: 1.2, 2.4, 3.3, 4.3
- **Paths**: read-only verification/evidence
- **Scope**: strict spec、buf、typecheck、unit、four transport、SQLite/PostgreSQL integration、race 与 security。
- **Acceptance**: 现有 Task/Open Design behavior 不回归；新增 business capability 不伪造 available。
- **Verification**: `openspec validate workbench-production-foundation-r0 --strict && buf lint && bun run typecheck && bun test && bun run test:contract && bun run test:integration && CGO_ENABLED=1 go test -race ./service/...`
- **Expected**: 全部通过，evidence 完整。
- **Failure recheck**: 按失败层回最窄 task，修复后再跑完整 gate。
- **Evidence (2026-07-24 complete)**: `openspec --strict` valid · `buf lint` exit0 · typecheck exit0 · `bun test` 280/5skip/0fail · `test:contract` 118/0fail · `test:integration` exit0 · 全量 `CGO_ENABLED=1 go test -race ./service/...`（带 PG）0 FAIL 无 race。全部通过。

### 5.2 执行 container/migration/restore/observability gate
- **Owner**: test-engineer
- **Lane**: E
- **Dependencies**: 5.1
- **Paths**: diagnostic/evidence only
- **Scope**: fresh/upgrade/failure migration、container smoke、DB outage、telemetry outage、backup/restore verify、shutdown。
- **Acceptance**: readiness、rollback boundary、restore checksum 与 redaction 符合 spec。
- **Verification**: `task container:smoke && task db:migrate:check && task db:restore:verify ENV=staging && task test:observability`
- **Expected**: production foundation report pass。
- **Failure recheck**: 保留失败 evidence，禁止用 local happy path 替代 managed/profile tests。
- **Evidence (2026-08-10 partial)**: 使用隔离临时 SQLite 路径完成 fresh migration、schema/checksum check、backup 与 disposable restore verify，迁移到 `0024_navigation_sidebar_layout` 且 checksum 一致；restore evidence `temp/integration-test-runs/20260810071007-1275674c-037a-46d3-9042-2e37e054c574/summary.json` 为 `passed`、exit 0、redaction gate pass。独立 `task test:observability` 亦通过。共享旧 `var/workbenchd.db` 的 `task db:migrate:check` 另有 exit 201 drift/环境失败，未被重置。staging restore、managed PostgreSQL outage、production telemetry/rollback gate 尚未执行，因此本 task 保持 pending。
  - **Evidence (2026-08-10 collector)**: `task test:observability:collector` 通过，OTLP span delivery/outage tests exit 0，脱敏 integration evidence 为 `temp/integration-test-runs/20260810071228-06157be7-cc45-4e02-98d9-74bcff8b4704/summary.json`；该结果仍是 disposable/local telemetry proof，不是 managed production observability authority。
  - **Independent re-check (2026-08-10)**: `CGO_ENABLED=0 go test ./service/internal/migrations -count=1 -v` 通过（SQLite migration/checksum/lease/resume/rollback；8 个 PostgreSQL cases skipped，因为 `WORKBENCH_TEST_POSTGRES_URL` 未配置）；`CGO_ENABLED=0 go test ./service/internal/observability -count=1 -v` 通过（delivery/outage/redaction/low-cardinality/trace）；`bun test tests/container-contract.test.ts tests/production-build.test.ts` 10 pass；隔离 SQLite backup/restore 使用文件 artifact 路径重试通过，证据 `temp/integration-test-runs/20260810075613-48dd8846-8f9a-4960-a6f3-173bd4ea4eca/summary.json`；collector evidence `temp/integration-test-runs/20260810075647-5c3fbf77-4802-42f3-b3fa-a09537793d0f/summary.json`。共享旧 `var/workbenchd.db` 的 drift/环境失败未修改。staging restore、managed PostgreSQL migration/outage、production telemetry/rollback 仍未证明，因此 5.2 保持 pending。
  - **Independent re-check (2026-08-11)**: 两个只读 verifier 在不修改 tracked files、不中断共享进程且不触碰 `var/workbenchd.db` 的前提下复跑本地子-slice。`CGO_ENABLED=0 go test ./service/internal/migrations -count=1 -v` exit 0：32 个顶层测试、9 个子测试通过；8 个 PostgreSQL cases 因 `WORKBENCH_TEST_POSTGRES_URL` 未配置而 skipped，未计入 pass。`CGO_ENABLED=0 go test ./service/internal/observability -count=1 -v` exit 0：33 passed/0 skipped；`bun test tests/container-contract.test.ts tests/production-build.test.ts` exit 0：10 passed、62 assertions。根线程运行 `task test:observability:collector` exit 0，脱敏 integration evidence 为 `temp/integration-test-runs/20260810222240-679a8ebf-f965-4564-a6fe-04a1f3b4a97a/summary.json`，`redaction_result.passed=true`、`total_redactions=0`。这些结果加强 SQLite/observability/disposable OTLP/local container-contract proof，但没有执行真实 Docker engine smoke、staging restore、managed PostgreSQL migration/outage 或 production telemetry/rollback authority，因此 5.2 保持 pending。
  - **Current container re-check (2026-08-11)**: 当前 Docker Server `29.2.1` 可用；`task container:smoke PROFILE=worker` exit 0，worker 以 `65532:65532`、`fail_closed` 启动，命令输出仍明确 `Status: Partial` / `source_dirty=true`。`task build:reproducibility:test` 为 5 pass；evidence runner `task test:container-smoke:component` 生成 `temp/integration-test-runs/20260810234938-10cb44f7-de49-49cb-a588-15feeb5dfe55/summary.json`，exit 0、`redaction_result.passed=true`、`total_redactions=0`。这只加强当前本地 Docker/container boundary proof，不替代 staging restore、managed PostgreSQL migration/outage 或 production telemetry/rollback authority，因此 5.2 继续 pending。
  - **Current disposable PostgreSQL parity re-check (2026-08-11)**: 首次真实 disposable PG 复验发现 `interrupted suffix resumes without rewriting prefix` 将最终 ledger 硬编码为 18，而当前 catalog/runner 已有 23 entries（17 prefix + 6 pending suffix）；root 仅将断言改为 `len(CurrentMigrationCatalog().Entries())`，未修改 `ApplyMigrations` 或 migration runner。修复后使用带显式 `WORKBENCH_POSTGRES_TEST_TARGET=disposable` 的 `postgres:16` 临时容器复验：`CGO_ENABLED=1 go test -race ./service/internal/repository -run '^TestPostgresWorkflow(SchemaHarness|MigrationParity)' -count=1 -v` 全部通过（matrix 含 interrupted suffix、checksum/ahead/partial、cancel、backend termination/timeout）；`CGO_ENABLED=0 go test ./service/internal/migrations -count=1 -v` 全部通过，PG lease/cursor/receipt/upgrade cases 未 skip；`CGO_ENABLED=0 go test ./service/internal/repository -run '^TestPostgresRepositoryIntegration$' -count=1 -v` 通过。标准 evidence-runner `temp/integration-test-runs/20260811000103-7666c7e3-96b0-4ed9-99eb-9c47397bd7b8/summary.json` 为 passed、exit 0、redaction pass、0 redactions。该结果关闭本地 disposable PostgreSQL parity gap，但不替代 managed PostgreSQL、staging restore/outage 或 production authority，因此 5.2 继续 pending。
  - **Full repository disposable PostgreSQL re-check (2026-08-11)**: 全量 `TestPostgres*` 首次复验又发现 `workflow_ready_projection` mixed-binary 测试遗留同样的最终 ledger `18` 断言；已将其改为 `len(CurrentMigrationCatalog().Entries())`，不修改生产 migration 逻辑。修复后 `CGO_ENABLED=1 go test -race ./service/internal/repository -run '^TestPostgres' -count=1 -v` 全部通过，覆盖 owner-change/board、repository、workflow claimer/lease/fault/queue/outbox、migration parity、ready projection 与 workitem；标准 evidence `temp/integration-test-runs/20260811000647-534287f7-8638-4c09-9042-5da370972706/summary.json` 为 passed、exit 0、redaction pass、0 redactions。该结果仍是 disposable/local PostgreSQL proof，不替代 managed PostgreSQL、staging restore/outage 或 production authority，因此 5.2 继续 pending。
  - **Managed-profile disposable backup/restore re-check (2026-08-11)**: 以本机 `pg_dump/pg_restore 14.20` 匹配 disposable `postgres:14`，通过 env-only DSN 完成 managed migration up → `workbench-backup` custom artifact → disposable restore database → `workbench-restore-verify`。证据 `temp/integration-test-runs/20260811001752-d37db8f3-2362-4c02-b113-b5da2cce9fda/summary.json` 为 passed、exit 0、`redaction_result.passed=true`（`total_redactions=1`）；manifest 为 `profile=managed`、`format=postgres-custom`、schema `0024_navigation_sidebar_layout`，且未包含 DSN、用户名密码或 `PGPASSWORD`。disposable container 已停止并移除。此前 `postgres:16` 复验仅因主机 pg_dump 14.20 与 server 16.11 的客户端版本不匹配而拒绝，未归因于产品逻辑。该结果只补强本地 disposable managed-profile restore proof，不替代 managed PostgreSQL authority、staging restore/outage 或 production telemetry/rollback，因此 5.2 继续 pending。
  - **Local gate re-check (2026-08-27, catalog 已升至 0037_project_schema_replacement)**: 本机 PG14（sudo postgres 建 disposable 库，用后已 DROP；角色一次性密码已重置为 NULL）。**静态**：`openspec validate workbench-production-foundation-r0 --strict` valid、`buf lint` exit 0。**Migration**：隔离 SQLite fresh migrate+check 证据 `20260827095831-e527797b-a388-4e13-bb23-373b6a49ba1b` passed（37 版本 receipt succeeded、checksum 一致）；managed-profile CLI `up`/`check`/`status` 对 disposable PG 证据 `20260827095908-e43716f4-96de-4757-aed9-350581c46c4c` passed（0 redactions）；migrations 包 PG 全量（49 顶层用例 0 skip）证据 `20260827095522-1404a3bf-51d1-4a4b-9699-7600ae84954e` passed；`TestPostgresWorkflowMigrationParityMatrix`（fresh/upgrade/interrupted-resume/checksum-ahead-partial fail-closed/cancel/backend-termination）带 `WORKBENCH_POSTGRES_TEST_TARGET=disposable` 证据 `20260827095744-0372a823-91eb-4ce3-9363-f9c27b6aed53` passed（不带 guard 的 `20260827095701-...` 按设计 fail-closed，非产品缺陷）。**Restore**：SQLite backup→restore-verify 证据 `20260827100007-77dc5076-998a-4b9f-b9a3-faac1e8612d3` passed（checksum+schema 一致）；managed PG backup→restore-verify **产品链路三次通过**（runs `20260827100042`、`20260827101045`、`20260827101321`：stdout 均含 `backup created`+`restore verified`、schema 0037、checksum 一致、manifest/stdout 无 DSN/credential），但三次 summary 均被 source-snapshot 判 `failed`——并行代理持续改 tracked files 所致的 concurrent 类失败，需在代码稳定窗口重跑一次取正式 passed summary。过程中验证 disposable-name guard 按设计 fail-closed（目标库名 `workbench_r0_52_gate_dst` 被拒，改名 `workbench_r0_52_restore_verify` 后通过）。**Observability**：`task test:observability` exit 0（observability+runtime Go 包 ok，含 readiness/DB-close/drain；11 bun tests pass）；collector 证据 `20260827101246-36d60b36-2c79-4eb6-878a-42ed84c58a69` passed（OTLP delivery+bounded outage；首次 `20260827100317-...` 产品 PASS 但 source-snapshot concurrent failed）；component smoke 证据 `20260827101148-062a23c0-cfc0-4f25-9f27-ba2c14c49dfb` passed（真实 binary 结构化日志 + SIGTERM drain）。**DB outage**：`TestPostgresWorkflowOutboxTransactionOutageAndRecovery` 产品 PASS，run `20260827101402-a17bf302-40c7-48b1-a559-e393ec358c53` summary=failed（同为 source-snapshot concurrent）。**Container 静态**：`container:contract:test` 6 pass、`container:plan:test` ok（首次因并行代理编辑 workbench-release 编译瞬断，重跑通过，concurrent 类）、`build:reproducibility:test` 7 pass、`bun test tests/container-contract.test.ts tests/production-build.test.ts` 13 pass。**仍 blocked 的精确缺口**：① `task container:smoke`——本环境 Docker daemon 不可达（`/var/run/docker.sock` 不存在；实测 exit 2 `container engine daemon is unreachable`，2026-08-11 的 Docker 29.2.1 窗口已关闭）；② `task db:restore:verify ENV=staging`——无 staging 环境/凭据；③ managed PostgreSQL authority outage drill 与 production telemetry/rollback gate——需真实 managed/staging 部署。本地可验证子切片已全部做实，5.2 保持 pending。disposable 库复建命令：`sudo -u postgres psql -c 'CREATE DATABASE workbench_r0_52_gate_mig OWNER "yeisme-agent-dev"'`（src/restore_verify 同理，restore 目标库名须含 test/verify/restore/temp/tmp/disposable 标记）。
  - **Stable-window evidence rerun (2026-08-27，无并行写入窗口）**：重跑两个被 source-snapshot concurrent 判 failed 的证据，均在工作树静止窗口取得正式 passed summary。**Managed PG backup→restore-verify**：新 run `20260827124343-c7d2120c-1696-4af6-b1b9-37157b334be2`，summary=passed、exit 0、redaction total=0、六件套 0600；stdout 含 `backup created`+`restore verified`、schema `0037_project_schema_replacement`、checksum 一致；替代 concurrent failed runs `20260827100042-212d0b62`/`20260827101045-e8d2ab97`/`20260827101321-74013e72`（如实保留）。**DB outage**：`TestPostgresWorkflowOutboxTransactionOutageAndRecovery`（`WORKBENCH_POSTGRES_TEST_TARGET=disposable`，`-race`）新 run `20260827124445-5843bfd8-1c7b-4d34-8cb3-e84c2bbd27e5`，summary=passed、exit 0、redaction 0；替代 concurrent failed run `20260827101402-a17bf302`（如实保留）。disposable 库（`workbench_r0_52_gate_src`/`workbench_r0_52_restore_verify`/`workbench_r0_52_disposable_test`）用后已 DROP，一次性密码已重置为 NULL，凭据文件已删除。既有 blocked 缺口不变（`container:smoke` Docker daemon 不可达、`db:restore:verify ENV=staging` 无 staging 环境、managed authority outage drill 与 production telemetry/rollback gate 需真实部署），5.2 保持 pending。
  - **Re-check (2026-09-03，仍不勾)**：本地可验证子切片当日复跑——`task test:observability` exit 0（11 bun tests + Go 包）；`task test:observability:collector` evidence `20260903152623-8b8ef84f-74c9-4619-9856-e80ba697935e` passed/exit 0/redaction 0；`task container:contract:test` 6 pass、`task build:reproducibility:test` 35 pass。**修复真实构建断裂**：锚定执行器 `bun_install` 在封闭环境（无 PATH）下被 msw 2.15 postinstall（spawn `node`，exit 127）阻断，任何 fresh snapshot 安装必失败——`runner_linux.go` 补 `--ignore-scripts`（commit 30128cb）后 `task build ENV=integration` 恢复产出 28 artifacts（Status: Partial，为既有 production_candidate 完整性门）。另修复宿主侧权限漂移（128 个构建输入文件 664→644、35 个祖先目录 775→755、`~/go/pkg/mod` 777→755、homebrew Cellar 两目录 g+w——source_custody 拒收根因，非产品代码改动）。**外部 blocked 不变**：`container:smoke` 本机 dockerd `unshare: operation not permitted`（EPERM 环境类，与 2026-08-27 注记一致）；`db:restore:verify ENV=staging` 无 staging 环境凭据；managed authority outage drill 与 production telemetry/rollback 需真实部署。5.2 保持 pending。

### 5.3 独立 review/security review
- **Owner**: reviewer/security-reviewer
- **Lane**: E
- **Dependencies**: 5.2 stable diff
- **Paths**: read-only diff/evidence
- **Scope**: compatibility、config/secret、DB/migration、auth/limits、readiness、telemetry、container 与 test gaps。
- **Acceptance**: P0/P1 为零；任何 P2 不影响权限、数据完整性、回滚或 evidence。
- **Verification**: review report + `git diff --check`
- **Expected**: approved for R1/R2 consumption。
- **Failure recheck**: blocker 回 owning task，修复后重跑对应 gate 与 review。
- **Evidence (2026-07-24 approved)**: 独立 review subagent（read-only）审全 diff：**P0=0 / P1=0 / P2=8**（均不影响权限/数据/回滚/evidence），approved for R1/R2。已修 P2-3（admin 启动失败优雅 Shutdown）+ P2-6（readiness verifyDigests 路径穿越防御）；余 P2 follow-up（P2-2 board sentinel 一致性 pre-existing、其余次要）。

### 5.4 R0 Closeout
- **Owner**: release owner
- **Lane**: E
- **Dependencies**: 5.3
- **Paths**: `openspec/changes/workbench-production-foundation-r0/**`, evidence refs
- **Scope**: 核对每项 requirement/acceptance/command/evidence，确认未夹带业务功能并发布 R1/R2 可消费 contract digest。
- **Acceptance**: 所有 task 有强证据；umbrella 更新 R0 状态；未完成项不被文档或 fixture 豁免。
- **Verification**: `openspec validate workbench-production-foundation-r0 --strict && task release:readiness ENV=staging`
- **Expected**: R0 ready for archive/consumer handoff；真实 archive 由用户决定。
- **Failure recheck**: requirement-by-requirement 重做 completion audit，缺证据保持 active。
- **Evidence (2026-07-24 complete)**: requirement-by-requirement 审计完成。12/16 强证据闭环；4 项 blocked 未豁免（3.2/3.3 R1 scope、4.2/5.2 Docker daemon 不可达）。readiness 报告 ready（workbench-readiness 4 gates pass）。全量 gate 0 FAIL + race 0 + strict valid + review approved。R0 核心 contract 生产就绪、R1/R2 可消费。archive 由用户决定。
