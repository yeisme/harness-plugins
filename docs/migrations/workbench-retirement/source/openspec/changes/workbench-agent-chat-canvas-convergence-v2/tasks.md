## 0. 文档、合同与兼容基线

- [x] 0.1 Owner: Workbench product/spec owner；Lane: contract-baseline；Dependencies: root design accepted；Scope: 完成 proposal、required-capability ledger、fit/split-owner/reject-now、first-support vertical slice 与 non-goals；Acceptance: 用户确认的真实 Chat、单一 shell、pending selection、soft-follow、atomic change-set 均保留；Validation: `openspec validate workbench-agent-chat-canvas-convergence-v2 --strict --no-interactive`；Expected: valid；Failure re-check: 不得把 required capability 降为 optional 或让 Workbench拥有 Conversation Runtime正文。
- [x] 0.2 Owner: Workbench architecture；Lane: contract-baseline；Dependencies: 0.1；Scope: 完成 `design.md`、双流 sequence diagram、`AgentWorkbenchLayoutV2`、typed facade、compatibility/rollback 与两个 details 附录；Acceptance: implementation 无 owner/layout/data-flow 决策缺口；Validation: OpenSpec strict；Expected: valid；Failure re-check: 三模式不得重新成为并列 shell，Task/content authority不得合并。
- [x] 0.3 Owner: Workbench spec owner；Lane: contract-baseline；Dependencies: 0.1；Scope: 完成 1 个新 capability 与 9 个现有 capability delta specs；Acceptance: 每个 requirement/scenario 可映射到 `details/acceptance-matrix.md` Test ID；Validation: OpenSpec strict；Expected: specs done；Failure re-check: modified requirement必须含完整 block，stable surface仅 additive。
- [x] 0.4 Owner: Workbench docs owner；Lane: docs；Dependencies: 0.2, 0.3；Scope: 同步 `docs/product/agent-workbench-blueprint.md`、`docs/ui/agent-first-workbench.md`、`docs/interfaces/agent-pi-workspace.md` 与 `docs/README.md`；Acceptance: 三真源引用 v2 change、Conversation Runtime root change、owner ledger、method matrix和 responsive contract；Validation: `git diff --check` + link/manual review；Expected: additive docs with no unrelated overwrite；Failure re-check: 不把计划中的 owner/runtime写成已 ready。

## 1. Conversation Runtime contract、SDK 与 BFF consumer

- [x] 1.1 Owner: Workbench SDK implementer；Lane: sdk-contract；Dependencies: Conversation Runtime provider contract digest；Paths: `packages/task-sdk/src/**`, `packages/task-sdk/test/**`；Scope: additive 定义 runtime profile、session Profile/grant、session/content、turn intent、Block/event/receipt/attempt types与 strict normalizers；Acceptance: unknown major/critical kind/unsafe field/oversize fail closed，旧 exports 不变；Validation: `bun test packages/task-sdk/test && bun run typecheck`；Expected: new contract tests green；Failure re-check: 不修改既有 field/type/enum语义，不将正文写入 Task models。
- [x] 1.2 Owner: Workbench SDK implementer；Lane: sdk-contract；Dependencies: 1.1；Paths: `packages/task-sdk/src/workbench-client.ts`, new conversation client module；Scope: 在 `WorkbenchClient.agent.conversation` 增加 listProfiles/create/revokeGrant/getSession/createTurnIntent/watchContent/retry/delete/export methods；Acceptance: same facade、typed options、idempotency/cursor/abort正确，Browser不能传 provider/process/credential fields；Validation: focused SDK client tests + `bun run test:contract`；Expected: transport requests match provider fixtures；Failure re-check: SDK facade不是第四 canonical transport，不改 `WorkbenchAgentClient` 旧签名。
- [x] 1.3 Owner: Workbench BFF implementer；Lane: bff-consumer；Dependencies: 1.2；Paths: `apps/web/server/**`；Scope: 实现 same-origin Conversation Runtime proxy、session/CSRF、scope resolution、size/timeout/SSE heartbeat、credential isolation与safe error mapping；Acceptance: Browser仅访问 Workbench，cross-tenant/SSRF/arbitrary endpoint/body overflow fail closed；Validation: focused BFF handler/security tests；Expected: valid profile/content responses and negative matrix green；Failure re-check: BFF不得拥有 Profile permission规则或持久正文，复杂边界逻辑写中文注释。
- [x] 1.4 Owner: Workbench BFF/Web state implementer；Lane: session-stream；Dependencies: 1.3, existing Task streams；Paths: selected-session supervisor/query modules；Scope: 合并 content/lifecycle 为一个 selected-session stream，保留两套 cursor/freshness/error并按 turn/attempt/sequence去重；Acceptance: reconnect不重复Block/Task event，单源失败只降级单源，session切换不取消后台turn；Validation: `WB-CC-STREAM-001` component/integration tests；Expected: deterministic merged projection；Failure re-check: 不开每消息/每Pane SSE，不从content推断Task终态。
- [x] 1.5 Owner: Workbench contract test owner；Lane: sdk-bff-conformance；Dependencies: 1.1-1.4；Paths: `tests/conformance/**`, `apps/web/server/**/*.test.ts`；Scope: 固定 provider fixture digest、SDK/BFF shape、cursor/error/redaction parity和legacy consumer compatibility；Acceptance: new optional fields ignored by old fixture reader，unknown critical拒绝；Validation: `bun run test:contract` + focused BFF tests；Expected: pass；Failure re-check: 不通过放宽normalizer/删除legacy assertion来清测试。

## 2. TaskService、grant 与 runtime adapter control plane

- [x] 2.1 Owner: Workbench Go service implementer；Lane: runtime-binding；Dependencies: root/Runtime Plane adapter contract, 1.1；Paths: `service/internal/registry/**`, `service/internal/runtime/**`, config surface；Scope: additive 注册默认关闭的 fixed Conversation Runtime owner/adapter/operation capability与 `WORKBENCH_AGENT_CHAT_CANVAS_V2_COHORT`；Acceptance: exact owner/adapter/version/provenance/readiness成立才可见，caller不能选 executable/provider/model；Validation: focused registry/runtime/config tests + `CGO_ENABLED=0 go test`；Expected: enabled/disabled/needs_contract matrix green；Failure re-check: 不复用reference readiness，不新增arbitrary process字段，复杂校验写中文注释。
- [x] 2.2 Owner: Workbench TaskService implementer；Lane: session-grant；Dependencies: 2.1；Paths: Task admission/authorization internals；Scope: 让有效 session grant 覆盖 ordinary read/chat permission，同时保留 Task idempotency/expected revision/budget和所有mutation/sensitive/overflow gate；Acceptance: `WB-CC-GRANT-001/002`，过期/revoke/profile drift fail closed；Validation: focused Go admission/TaskService tests；Expected: ordinary turn不再 `awaiting_permission`，越界仍gate；Failure re-check: grant不得授权mutation或caller身份，不改变其他Operation gate语义。
- [x] 2.3 Owner: Workbench service implementer；Lane: sealed-intent；Dependencies: 1.3, 2.2；Paths: Agent turn input/adapter/correlation repository；Scope: 将 `turnIntentRef`、grant/profile refs、content attempt correlation 作为 additive safe input/index，Workbench DB不存正文；Acceptance: restart/replay/idempotency可恢复同一safe ref，sentinel scan为0；Validation: focused agentruntime/repository/security tests；Expected: accepted Task可关联content attempt；Failure re-check: 不把 ciphertext/visible Block/provider payload写入Task/repository。
- [x] 2.4 Owner: Workbench service implementer；Lane: attempt-lifecycle；Dependencies: 2.3；Paths: Agent event/output projection与lifecycle mapping；Scope: 映射 content `partial`、known failure Retry attempt、unknown original-attempt reconcile、cancel状态并保持旧Task event兼容；Acceptance: `WB-CC-PARTIAL-001`、`WB-CC-UNKNOWN-001`；Validation: focused Go/SDK conformance tests；Expected: partial保留、retry新attempt、unknown无retry；Failure re-check: 不自动switch runtime/replay turn，不把content terminal当Owner receipt。
- [x] 2.5 Owner: Workbench service test owner；Lane: control-verification；Dependencies: 2.1-2.4；Paths: service tests only；Scope: 覆盖grant tamper/replay、budget、cross-tenant、safe input persistence、restart、cancel/reconcile和reference-dev separation；Acceptance: deterministic negative matrix；Validation: `CGO_ENABLED=0 go test ./service/internal/agentruntime ./service/internal/runtime ./service/internal/repository -count=1`；Expected: pass；Failure re-check: 环境/并行dirty failure先归因，不改无关业务逻辑。

## 3. 单一自适应 Shell 与 document dock

- [x] 3.1 Owner: Workbench Web implementer；Lane: layout-kernel；Dependencies: 0.3；Paths: new Agent layout reducer/model tests；Scope: 实现纯 `AgentWorkbenchLayoutV2` reducer，管理Session drawer、Chat rail、documents、Context rail、pending selection、soft-follow、focus return与compat view；Acceptance: deterministic state transitions、per-session restore、visible/split limit，无业务state；Validation: focused Vitest reducer tests；Expected: `WB-CC-LAYOUT/DOCK` unit green；Failure re-check: reducer不得持有Task/content/Board/proposal对象，复杂状态转换写中文注释。
- [x] 3.2 Owner: Workbench Web implementer；Lane: document-dock；Dependencies: 3.1；Paths: Pane registry/dock + Spatial document adapter；Scope: 把Canvas注册为closed first-class document，复用dedupe/tab/split/focus/limit/responsive contract；Acceptance: duplicate focus existing、close只改layout、Canvas/Panes同一dock；Validation: focused Pane/Spatial component tests；Expected: `WB-CC-DOCK-001` green；Failure re-check: 不创建第二Spatial query owner或第二dock engine。
- [x] 3.3 Owner: Workbench Web implementer；Lane: shell-composition；Dependencies: 3.1, 3.2；Paths: `agent-route.tsx` and shell composition modules；Scope: feature-gated v2 shell，移除v2路径的route sibling conversation/spatial与modebar，组合左Chat/中document/右Context；Acceptance: one shell/one composer/one Context rail，v1 fallback仍可用；Validation: focused Agent route/component tests；Expected: `WB-CC-LAYOUT-001` green；Failure re-check: 不覆盖当前并行OPC/production改动，中文注释解释兼容/owner边界。
- [x] 3.4 Owner: Workbench Web implementer；Lane: responsive-shell；Dependencies: 3.3；Paths: shell/session/context responsive components/styles；Scope: Session drawer默认收起可pin，1440三列、1024两面+Sheets、<1024 Chat+object/review lists；Acceptance: usable min widths、无page overflow、drawer开合不丢state；Validation: component + Playwright viewport tests；Expected: 1440/1024/390/200% matrix green；Failure re-check: 不用CSS隐藏已运行第二shell，不在窄屏mount完整Spatial editor。
- [x] 3.5 Owner: Workbench Web implementer；Lane: compatibility；Dependencies: 3.3；Paths: route ingress/layout migration tests；Scope: 保留 `?view=conversation|split|spatial-focus` 作为initial preference，旧browser-local mode安全映射到v2默认；Acceptance: stable deep links可达且不创建独立mode state；Validation: `WB-CC-LAYOUT-002` route/e2e tests；Expected: pass；Failure re-check: 不删除query values或重解释为权限/capability。
- [x] 3.6 Owner: Workbench accessibility implementer；Lane: shell-a11y；Dependencies: 3.3, 3.4；Paths: shell focus/keyboard/aria tests；Scope: Chat↔dock↔Context键盘路径、drawer/Sheet focus trap/restore、reduced motion、screen-reader live regions与drag menu equivalent；Acceptance: no focus steal、Axe serious/critical=0；Validation: focused component + Playwright Axe；Expected: `WB-CC-A11Y-001` green；Failure re-check: 不以隐藏focus outline或放宽Axe规则过关。

## 4. Session Profile、真实回答与 Timeline

- [x] 4.1 Owner: Workbench Web implementer；Lane: session-profile；Dependencies: 1.2, 2.2, 3.3；Paths: Conversation/Profile components + state；Scope: 首次Profile Sheet、推荐Pi/OMP/model、tool/context scope、budget/expiry/retention/soft-follow摘要与revoke/edit；Acceptance: server-authored state，普通in-grant send无逐回合permission，runtime unavailable可恢复；Validation: Profile component/e2e tests；Expected: `WB-CC-GRANT-001/002` UI green；Failure re-check: Browser不得生成grant或暴露credential/provider host。
- [x] 4.2 Owner: Workbench Web implementer；Lane: structured-content；Dependencies: 1.1, 1.4；Paths: conversation Block renderers/tests；Scope: allowlisted Markdown/code/table/quote/artifact/proposal/status renderer、streaming partial/final、copy/accessibility；Acceptance: unknown/HTML/script/unsafe URL fail closed；Validation: `WB-CC-CONTENT-001` component/security tests；Expected: safe Blocks render, sentinels absent；Failure re-check: 不使用dangerous HTML，不把provider frame/CoT当可见Block。
- [x] 4.3 Owner: Workbench Web implementer；Lane: timeline-hierarchy；Dependencies: 4.2；Paths: timeline/run detail modules；Scope: 回答优先，Task/thinking/tool/gate/receipt折叠到per-turn RunDetail，重复状态按canonical key合并；Acceptance: control facts全可达但不重复淹没回答；Validation: component snapshots/semantic assertions；Expected: attached screenshot failure mode不再重复三条相同拒绝；Failure re-check: 不隐藏unknown/gate/receipt truth，不用卡片墙。
- [x] 4.4 Owner: Workbench Web implementer；Lane: content-lifecycle；Dependencies: 1.4, 4.2；Paths: attempt controls/session actions；Scope: partial保留、Retry新attempt、unknown reconcile-only、delete/export/revoke actions与focus return；Acceptance: `WB-CC-PARTIAL-001/UNKNOWN-001`，删除正文不删Task refs；Validation: component + mocked contract + e2e；Expected: pass；Failure re-check: 不拼接attempt、不自动retry/switch runtime。
- [x] 4.5 Owner: Workbench locale owner；Lane: i18n；Dependencies: 3.4, 4.1-4.4；Paths: `api/locale/source/{zh-CN,en-US}/agent/**` + generated outputs；Scope: Profile、pending/attached、Block states、partial/retry/delete/export、soft-follow、layout/Sheet文案双语；Acceptance: primary Chinese-first、technical refs mono secondary、catalog placeholders齐全；Validation: `bun run compose:i18n && bun run check:i18n && bun run test:i18n`；Expected: pass；Failure re-check: 不直接编辑generated bundle，不以inline fallback完成。

## 5. Canvas selection、Context 与 proposal-first 回写

- [x] 5.1 Owner: Workbench Web implementer；Lane: selection-bridge；Dependencies: 3.1, 3.2；Paths: Spatial selection adapter + Composer pending tray；Scope: selection投影为per-session/per-draft pending refs、dedupe/remove/attach入口，draft与attached Pack不变；Acceptance: `WB-CC-SELECT-001`，pointer/keyboard一致；Validation: reducer/component tests；Expected: pass；Failure re-check: 不从DOM解析对象，不自动写Composer/Context。
- [x] 5.2 Owner: Workbench Context/service + Web implementer；Lane: exact-context；Dependencies: 5.1；Paths: Context typed request/service validation/UI recovery；Scope: exact-revision batch reauthorization与all-or-block attachment，stale/revoke/mismatch列出Refresh/Remove；Acceptance: `WB-CC-SELECT-002`，无auto-upgrade/silent omit；Validation: SDK/Go integration + component/e2e；Expected: pass；Failure re-check: caller refs不授权scope，复杂batch/错误映射写中文注释。
- [x] 5.3 Owner: Workbench artifact consumer implementer；Lane: artifact-context；Dependencies: 1.2, 5.2；Paths: Composer attachment model + approved owner adapters；Scope: safe artifact ref/revision进入同一pending/attach flow，raw upload由owner处理；Acceptance: stale grant阻止、arbitrary path/URL/bytes拒绝；Validation: contract/security tests；Expected: pass；Failure re-check: 不让Workbench拥有artifact bytes/signed URL/provider payload。
- [x] 5.4 Owner: Workbench Web implementer；Lane: soft-follow；Dependencies: 3.2, 4.1, 5.1；Paths: presentation/spatial intent resolver + Context rail；Scope: Profile-consented default soft-follow、live highlight/preview/Context projection、dirty/review/modal/replay/stale guards；Acceptance: `WB-CC-SOFT-001/002`，无focus/camera/context/draft/task mutation；Validation: resolver unit + component/Playwright；Expected: pass；Failure re-check: 不扩大`AgentPresentationIntentV1`到DOM/URL/arbitrary props，复杂guards写中文注释。
- [x] 5.5 Owner: Workbench Proposal/Spatial consumer implementer；Lane: atomic-changeset；Dependencies: 4.2, 5.4, existing ProposalAuthority；Paths: response proposal Block + Canvas preview + Review rail；Scope: one server-authored change-set整体preview/Accept/Reject/Request Changes/conflict/unknown reconcile；Acceptance: `WB-CC-CHANGE-001/002`，receipt前不显示persisted；Validation: SDK/component/Go integration/e2e；Expected: pass；Failure re-check: Browser不逐项删operation、不自动split/retry/accept。

## 6. Creative Production first-support vertical slice

- [x] 6.1 Owner: Workbench test/consumer implementer；Lane: contract-fixture；Dependencies: 1.5, 3.3, 4.2, 5.5；Paths: test fixtures/mocks only；Scope: 建立与Conversation Runtime provider digest匹配的Creative Production session/profile/content/selection/change-set fixtures；Acceptance: fixture覆盖完整golden path和offline/stale/partial/unknown，明确`fixture`不标real ready；Validation: focused fixture/normalizer tests；Expected: pass；Failure re-check: 不把synthetic Block/Board node显示为production evidence。
- [x] 6.2 Owner: Workbench Creative Production consumer implementer；Lane: creative-first-support；Dependencies: 6.1；Paths: Creative Lens adapter + generic selection/change-set mapping；Scope: 以scene/shot/asset safe refs走通通用pending/context/content/soft-follow/review，无领域字段进入layout/content kernel；Acceptance: same generic contracts可被其他Lens复用；Validation: component/integration tests；Expected: `WB-CC-CREATIVE-001` fixture phase green；Failure re-check: 不复制Scaena canonical state/action logic。
- [x] 6.3 Owner: Workbench browser test owner；Lane: vertical-browser；Dependencies: 4.5, 6.2；Paths: new Playwright spec；Scope: 从Profile→选择→附加→send→Blocks→soft-follow→change-set→receipt的完整浏览器流，覆盖1440与390 review路径；Acceptance: zero request/page errors、Axe serious/critical=0、focus/draft/state连续；Validation: `bun run web:e2e`；Expected: evidence bundle pass；Failure re-check: 不用route fixture绕过BFF/Task/Context交互。
- [ ] 6.4 Owner: cross-project integration owner；Lane: real-opt-in；Dependencies: root P1 canary, Runtime Plane adapter ready, 6.3；Scope: 显式opt-in真实Pi/OMP + Workbench + Creative Production safe refs，零自动mutation；Acceptance: real safe Block、bounded budget、partial/cancel/reconnect、soft-follow、proposal preview和receipt correlation；Validation: `bun run test:integration` plus dedicated opt-in system command frozen by provider handoff；Expected: pass or truthful blocked；Failure re-check: 无授权不调用provider，reference adapter/fixture不计real evidence。 **Re-check (2026-09-04，仍不勾——上游 real-runtime 依赖未就绪)**：依赖的根 change `agent-conversation-runtime-workbench-integration-v1` 2.3 `P1-real-canary` 保持 open（真实 Pi/OMP canary 未执行、Runtime Plane adapter 未 real-ready）；本沙箱无 provider 授权/credential，按 Failure re-check 纪律不调用 provider，reference adapter/fixture 不计 real evidence。Workbench 侧消费面（grant/sealed intent/receipt correlation）已由 6.3 fixture-phase e2e 与 7.1/7.2 focused 验证覆盖；real opt-in 待根 P1 canary 与 provider handoff 冻结的专用 opt-in 命令就绪后执行。7.4 按其自身 Dependency 条款（"6.4 or documented external blocker"）凭本记录继续。

## 7. 分层验证、证据与 closeout

- [x] 7.1 Owner: Workbench unit/component verifier；Lane: focused-verification；Dependencies: 1-6 implemented；Scope: 运行 acceptance matrix 对应SDK/reducer/component tests；Acceptance: 所有Test ID有deterministic result；Validation: `bun test packages/task-sdk apps/web/test`（按实际focused paths执行）；Expected: pass；Failure re-check: 测试路径/runner复用现有Vitest/Bun，不建平行框架。
- [x] 7.2 Owner: Workbench service verifier；Lane: service-verification；Dependencies: 2.5, 5.2, 5.5；Scope: Task/grant/sealed input/repository/restart/cancel/reconcile/tenant/security tests；Acceptance: `CGO_ENABLED=0`正常路径、race targeted path和sentinel 0；Validation: focused `go test` then `CGO_ENABLED=1 go test -race` on touched packages；Expected: pass；Failure re-check: race/environment/dirty worktree故障先分类，不改无关逻辑。
- [x] 7.3 Owner: Workbench browser verifier；Lane: browser-matrix；Dependencies: 3.6, 4.5, 6.3；Scope: 1440/1024/390/200%、keyboard、reduced motion、Axe、overflow、legacy `?view=`、v1 rollback；Acceptance: `WB-CC-LAYOUT/MOBILE/A11Y` 全绿并保存截图/trace；Validation: `bun run web:e2e`；Expected: pass evidence；Failure re-check: 截图不得含credential/private path/raw prompt/provider payload。
  - **Evidence (2026-09-04):** e2e evidence run `temp/integration-test-runs/20260904051106-a2b87e5d-296b-43df-afb9-4689b134137b`（`bun run web:e2e`，passed/exit 0，210 passed / 0 failed，截图/trace 经 Playwright attachments 落盘）。首次全量（`20260904041616`）9 失败经 worktree 基线（854bc0f）复现归因为既有失败（3.x 时代未跑全量 e2e 留下的债 + 产品演进使旧 spec 前提过时），本 lane 全部修复：conversation 查询加 chatCanvasV2 cohort 门（v1 无 424 噪声）、Follow Pi 按钮断点对齐 v2 rail（340px）、v2 Project pane 恢复 inspector/kanban/todo CLI open 入口（render-slot，共享组件 Agent 无感知）、agent-first/direct-panes/pi-workspace/studio-icon spec 对齐当前产品语义（boards/spatial mock、heading locator、CLI adapter 门、v2 project fixtures、contextMap 候选缺席、tooltip hover 驱动）。commit f8b4d1c + 57b88d3。
- [x] 7.4 Owner: Workbench integration verifier；Lane: evidence；Dependencies: 6.4 or documented external blocker；Scope: 通过现有 evidence runner执行contract/integration/e2e，保存六件套和artifacts；Acceptance: `summary.json`结果、原exit code、env redaction、failure artifacts完整；Validation: `bun run test:contract && bun run test:integration && bun run web:e2e`；Expected: pass or external-blocked with exact dependency；Failure re-check: 不手写evidence metadata，不把blocked伪造成passed。
  - **Evidence (2026-09-04):** 6.4 已按其条款记录 external blocker（根 P1 canary 未跑、无 provider 授权），7.4 凭该记录继续。三层证据：contract `20260904052003-ab8bb25a`（evidence runner --layer component 包裹 `bun run test:contract`，574 pass/0 fail）；integration `20260904051739-10397308`（conformance + runtime，ok/ok）；e2e `20260904051106-a2b87e5d`（210/0）。三目录均含 summary.json/command.txt/env.json/receipt.json/redaction.json/digest.json + stdout/stderr + artifacts，exit code 原样记录，env 经 runner redaction（DSN/token 不入 command.txt）。
- [x] 7.5 Owner: Workbench security/performance verifier；Lane: final-risk；Dependencies: 7.1-7.4 stable candidate；Scope: cross-tenant/SSRF/grant tamper/content sentinel/stream reconnect/memory/50k Canvas regression与no-duplicate-stream；Acceptance: P0/P1 security regression=0，Chat/Canvas交互无明显长任务回归；Validation: existing security smoke + targeted capacity/browser performance tests；Expected: pass；Failure re-check: 不记录正文/credential/provider frames，不以平均值掩盖tail regression。
  - **Evidence (2026-09-04):** 安全面：`bun test tests/security` 22/0（redaction/sentinel/spatial-replica security/local-session）；BFF `conversation-proxy/gateway-credential-isolation/gateway-handler/handler` 85/0；Go `service/internal/app -run 'SessionGrant|Grant'` + `agentruntime` + `runtime` 全 ok（grant tamper/tenant/sentinel 面为 2.2/2.5 既有测试复跑）。容量面：`task test:project-performance:evidence` 于 disposable PG 15440（operator 手工 provision sentinel+attestation，b863c2c 起测试目标需 provisioner-owned authority）evidence run `20260904051714-4186bc7b`：50k WorkItem 查询门 p95=20.1ms（预算 400ms）、200 流 watch 扇出门、Board 视口 45k 节点 far p95=11ms（预算 150ms）全部 PASS。no-duplicate-stream：`selected-session-stream/layout-stream-reconnect` 组件测试 17/0。浏览器内存/长任务/资源释放证据由 `spatial-replica-performance.spec.ts`（heap/disposal）随 7.3 e2e 全量绿承载。P0/P1 security regression=0。
- [x] 7.6 Owner: Workbench final verifier；Lane: full-gates；Dependencies: 7.1-7.5；Scope: 稳定candidate后一次性执行完整项目门；Acceptance: OpenSpec、typecheck、tests、build、i18n、Go、integration全部可归因；Validation: `openspec validate --all --strict --no-interactive && bun run typecheck && bun test && bun run build && bun run test:contract && bun run test:integration && bun run web:e2e && bun run check:i18n && CGO_ENABLED=0 go test ./service/...`；Expected: pass或列出pre-existing/concurrent/environmental failures；Failure re-check: 不为清全局告警修改无关业务逻辑。
  - **Evidence (2026-09-04):** 全门绿：openspec strict 69/0；typecheck OK；`bun test` 985 pass/0 fail（993 项含 8 skip）；`bun run build` OK（workbenchd/migrate/backup/restore-verify 等全产物）；contract 574/0（evidence `20260904052003-ab8bb25a`）；integration ok（`20260904051739-10397308`）；`bun run web:e2e` 210/0（`20260904051106-a2b87e5d`）；check:i18n OK（3735 server keys）；`CGO_ENABLED=0 go test ./service/... -count=1` 空载复跑全绿。归因记录：首轮 Go 全量 `service/internal/workers/authority TestServiceIdentityProbeAcceptsCurrentBoundReadiness` 失败（"authority readiness unavailable"）——隔离复跑 3/3 ok、空载全量复跑全绿，判定为与并行 perf/e2e 负载叠加的 1s 探针超时 environmental flake，非产品回归；未改任何业务逻辑。
- [x] 7.7 Owner: Workbench product/docs owner；Lane: closeout；Dependencies: 7.6, provider/consumer handoff receipts；Scope: 更新三真源的实际ready状态、记录breaking classification/rollback/evidence并归档change；Acceptance: reference/fixture/real/local-beta/production标签准确，未完成owner依赖保留blocked；Validation: `openspec validate workbench-agent-chat-canvas-convergence-v2 --strict --no-interactive && git diff --check`；Expected: archive-ready；Failure re-check: 没有真实Pi/OMP和三方证据不得宣称完成/production-ready。
  - **Evidence (2026-09-04):** 三真源已更新实际 ready 状态：`docs/product/agent-workbench-blueprint.md` §3.4（fixture/reference 已实现并验证、real canary blocked、Aigora amendment blocked、breaking_surfaces=[] 与 cohort capability 回滚路径）；`docs/ui/agent-first-workbench.md` §8.2（响应式矩阵/Axe/Profile send gate 落地证据 + blocked 面不渲染空壳）；`docs/interfaces/agent-pi-workspace.md`（消费面合同 fixture 全绿、additive safe refs）。标签诚实：fixture/reference=绿、real=blocked（6.4）、Aigora 消费实施=blocked（8.2-8.6）、local-beta/production=未宣称。**不归档**：6.4（真实 canary）与 8.x（Aigora amendment）依赖 owner 侧就绪后才能关闭，按 house 惯例 change 保持 active、任务留 Re-check 记录；核心消费面（1-7 lanes 除 6.4）与 spec 资产已达 archive-ready 质量，待根 P1 canary + Aigora digest 冻结后补 6.4/8.x 并归档。

## 8. 历史 Aigora ACP amendment（已由 Lane 9 supersede）

> 8.1–8.6 保留 2026-09-04 的审计与 blocked 事实，不再是 current implementation queue，也不得作为 Aigora Access 或 ACP readiness 证据。旧 change 名、旧 refs、ACP/Aigora 联合门与“ACP bridge”均由 9.1 的协议边界纠偏取代。

- [x] 8.1 Owner: Workbench contract owner；Lane: aigora-consumer-spec；Dependencies: root unified access-plane + Aigora Session Gateway owning spec；Paths: proposal/design/tasks and Agent consumer specs；Scope: 增加 Aigora profile/grant safe refs、ACP/OMP/model/MCP readiness、独立预算、safe deep link 和 owner boundary；Acceptance: 全部 additive，`breaking_surfaces=[]`；Validation: `openspec validate workbench-agent-chat-canvas-convergence-v2 --strict --no-interactive`；Expected: valid；Failure re-check: Workbench 不生成 grant、不读取长期 Key/Gateway token。
- [ ] 8.2 Owner: Workbench SDK/BFF implementer；Lane: aigora-access-context；Dependencies: Aigora access-context contract digest；Paths: existing WorkbenchClient/BFF conversation consumer modules；Scope: 消费 `aigora.access_context.v1alpha1`、session grant/descriptor safe projections和 deep link；Acceptance: cross-tenant、unknown schema、stale generation、arbitrary endpoint fail closed；Validation: contract/BFF security tests；Expected: provider/consumer digest match；Failure re-check: Browser 不直连 Aigora credential/Session Gateway process。 **Re-check (2026-09-04，仍不勾——provider 合同 digest 未冻结)**：依赖的 Aigora access-context contract digest 不存在——根 change `aigora-unified-agent-access-plane-v1` 1.2 只冻结了合同标识（`aigora.access_context.v1alpha1` 等）与规划级 API 面（`GET /v1/access/context` 等为 "SHALL plan"），字段级 wire 合同与跨项目 digest handoff（根 5.2）保持 open；Aigora owner change `aigora-acp-session-gateway-v1` 0/17、`/v1/access/context` 无任何实现代码。按单合同源纪律不在 Workbench 侧发明字段形状（避免第二 contract 源与返工）。Workbench 消费面 spec（8.1）已 additive 冻结，provider digest 冻结后即可按此实施。
- [ ] 8.3 Owner: Workbench Web implementer；Lane: session-profile-readiness；Dependencies: 8.2, existing Profile Sheet；Paths: existing Session Profile/Agent workspace components；Scope: 展示 ACP wire、OMP version、grant state、model range、MCP bridge readiness和三份独立预算；Acceptance: loading/disabled/pending/active/stale/degraded/forbidden/unknown/blocked/recovery 全覆盖；Validation: component + Playwright state matrix；Expected: no blank/ambiguous state；Failure re-check: 不合并预算，不由前端猜 role/capability。 **Re-check (2026-09-04，仍不勾——deps 8.2 open)**：硬依赖 8.2（Aigora access-context 合同 digest 未冻结）；在投影合同缺席时实现 readiness 状态矩阵只能由前端猜测 role/capability，直接违反本任务 Failure re-check。既有 Profile Sheet 的 conversation runtime/grant 状态面（loading/needs_profile_confirmation/active/expired/budget_exceeded）已在 6.3 e2e 覆盖。
- [ ] 8.4 Owner: Workbench session/control implementer；Lane: acp-controls；Dependencies: 8.2, Conversation Runtime owner adapter；Paths: existing session actions/timeline/approval modules；Scope: ACP create/load/resume/close/cancel、tool approval、content operations、grant revoke/replacement 和 unknown reconcile；Acceptance: ordinary reads in-scope，file/terminal/MCP mutation/external write proposal-first；Validation: component/service/integration tests；Expected: lifecycle matrix green；Failure re-check: unknown 不自动 retry、协议切换或副作用重放。 **Re-check (2026-09-04，仍不勾——deps 8.2 open)**：硬依赖 8.2 + Conversation Runtime owner 的 ACP adapter（根 `agent-conversation-runtime-workbench-integration-v1` 5.4 "未来在 owner change 实现 ACP session lifecycle" 保持 open）。无 ACP wire 合同即无 lifecycle 矩阵可实现；不预先拼接帧形状。
- [ ] 8.5 Owner: Workbench UI/i18n/a11y owner；Lane: dual-panel-handoff；Dependencies: 8.3-8.4；Paths: existing Agent locale/UI/browser tests；Scope: server-authored Aigora Key detail deep link、zh-CN/en-US、360/768/1280/1536、keyboard/focus/Axe/secret sentinel；Acceptance: no token/content/path in URL/DOM/screenshot/evidence；Validation: `bun run check:i18n && bun run web:e2e` through evidence runner；Expected: six-file evidence bundle；Failure re-check: A2A 未实现不显示可操作空壳。 **Re-check (2026-09-04，仍不勾——deps 8.3/8.4 open)**：Aigora Key detail deep link 需要 server-authored descriptor 投影（8.2/8.3 缺席）；按 Failure re-check，A2A/ACP 未实现期间不显示可操作空壳，deep link 面保持不渲染。
- [ ] 8.6 Owner: cross-project integration owner；Lane: aigora-acp-canary；Dependencies: Aigora/Conversation Runtime real-ready, 8.2-8.5；Scope: 验证 model 经 Aigora、MCP 经 Aigora `/mcp`+Gateway、ACP v1 OMP 17.0.2、cancel/partial/cursor/revoke/recovery；Acceptance: real canary 或 truthful blocked，ACP v2 无 runtime 明确 blocked；Validation: owner integration/system/e2e commands and `temp/integration-test-runs/<run-id>/` evidence；Expected: pass or exact blocker；Failure re-check: fixture/reference adapter 不得替代真实 first-support evidence。 **Re-check (2026-09-04，truthful blocked——exact blocker)**：Aigora Session Gateway 未 real-ready（owner change `aigora-acp-session-gateway-v1` 0/17，无 `/v1/access/context`、无 `/mcp` bridge 实现）；Conversation Runtime 侧 ACP adapter 为根 5.4 未来实现；本沙箱无 provider 授权。ACP v1（OMP 17.0.2）无 runtime → 明确 blocked；ACP v2 无 runtime → 明确 blocked。fixture/reference adapter 不计 first-support evidence，不伪造 canary。

## 9. Aigora Agent Access BFF saga 与双来源面板 amendment

- [x] 9.1 Owner: Workbench contract owner；Dependencies: corrected root access-plane、Identity audience、Aigora Sidecar、Runtime Plane binding changes；Paths: `proposal.md`, `design.md`, `tasks.md`, Agent consumer/workspace specs；Acceptance: 8.1–8.6明确 superseded；BFF-only identity/grant/ticket/launch、Aigora no-ACP、two-source cards、AG-UI retain-next 完整；Validation: `openspec validate workbench-agent-chat-canvas-convergence-v2 --strict --no-interactive`；Expected: valid；Failure re-check: 搜索旧 refs/change 名，仅允许本 historical section；current positive contract 不把 ACP/OMP 放入 Aigora。
- [ ] 9.2 Owner: Workbench SDK/BFF implementer；Dependencies: `identity-aigora-agent-access-audience-v1`, `aigora-agent-access-sidecar-v1`, `aigora-agent-access-binding-v1` implementation digests；Paths: existing WorkbenchClient/BFF conversation/identity/runtime consumer modules；Acceptance: saga 顺序为 session→exchange→context/grant/ticket→launch，renew约提前5m，close/revoke，partial failure/compensation/reconcile明确；Validation: contract/BFF service/security tests；Expected: provider/consumer digests match；Failure re-check: Browser/Task/localStorage/cookie/log不得含 exchange token、ticket、bearer、loopback、Key/Gateway token。
- [ ] 9.3 Owner: Workbench Web implementer；Dependencies: 9.2, Agent Framework descriptor；Paths: existing Session Profile/Agent workspace components；Acceptance: Agent Framework 与 Aigora Access 两张 source-labelled 卡分别覆盖 loading/disabled/pending/active/stale/degraded/forbidden/unknown/blocked/recovery；Validation: component + Playwright state matrix；Expected: no synthetic global-ready/blank state；Failure re-check: 不由前端猜 owner state，一个 source失败不清空另一个。
- [ ] 9.4 Owner: Workbench session/approval implementer；Dependencies: 9.2, Conversation Runtime owner adapter；Paths: session actions/timeline/ProposalAuthority/approval modules；Acceptance: ACP controls只消费 Agent Framework；MCP elicitation经 `approval_decide` 恢复原 call；Gateway/Workbench receipts相关但独立；Validation: component/service/integration approval matrix；Expected: exactly one `tools/call`；Failure re-check: 5分钟窗口超时、unknown或reconnect不得重提工具/协议/turn。
- [ ] 9.5 Owner: Workbench UI/i18n/a11y owner；Dependencies: 9.3-9.4；Paths: Agent locale/UI/browser tests；Acceptance: safe Aigora Key deep link、zh-CN/en-US、360/768/1280/1536、keyboard/focus/live region/Axe/secret sentinel完整，A2A/AG-UI未实现无空壳；Validation: `bun run check:i18n && bun run web:e2e` through evidence runner；Expected: six-file redacted evidence；Failure re-check: URL/DOM/screenshot/trace/evidence不含 token/ticket/content/path/endpoint/bearer。
- [ ] 9.6 Owner: Workbench + cross-project integration owner；Dependencies: 9.2-9.5, Agent Framework and Aigora real-ready；Paths: Workbench integration/system/e2e runner and `temp/integration-test-runs/<run-id>/`；Acceptance: Agent Framework ACP/Pi/OMP gate 与 Aigora Model/MCP egress gate 分开验证/显示，Responses streaming无证据 truthful blocked，revoke/recovery/unknown完成；Validation: owner integration/system/e2e commands；Expected: two independent pass/blocked verdicts；Failure re-check: fixture/reference adapter不得替代真实 evidence，Aigora pass不得提升 ACP，ACP pass不得提升 Responses streaming。
- [ ] 9.7 Owner: future Workbench protocol adapter owner；Dependencies: separate approved change；Paths: future BFF compatibility module only；Acceptance: AG-UI只投影 authorized content/Task/proposal/approval HTTP/SSE events，保留cursor/cancel/unknown，不进入Aigora或canonical state；Validation: future contract/security/integration tests；Expected: retain-next until implemented；Failure re-check: 本 change不显示操作控件，不把AG-UI当Agent Framework或Aigora readiness。
