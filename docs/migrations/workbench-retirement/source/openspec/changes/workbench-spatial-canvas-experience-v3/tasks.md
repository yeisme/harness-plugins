## 0. Compatibility and contract gate

- [x] 0.1 Freeze the V2/V3 surface inventory before implementation；Owner: contract implementer；Dependencies: none；Owned paths: spatial proto/schema、SDK exports、HTTP/gRPC/JSON-RPC registries、current V2 contract tests；Scope: enumerate every V2 request/response/method/enum/strict normalizer and all in-repo consumers, classify V3 additions as additive, record `breaking_surfaces: []`；Acceptance: no V2 field/method/enum/path is renamed, removed, repurposed or widened with V3-only strict JSON keys；Verification: `openspec validate workbench-spatial-canvas-experience-v3 --strict` plus focused V2 contract tests；Expected: inventory and test names are fixed before writers touch contracts；Failure re-check: unknown external consumer, reused protobuf number, V2 normalizer accepting a changed shape, or removal warning introduced inside this change.
  - Done（2026-08-31）：`details/v2-v3-surface-inventory.md` 冻结 5 RPC/2 enum/28 message 的 V2 面与 SDK/传输/仓内消费者；V3 增量全部分类 additive，`breaking_surfaces: []`。

- [x] 0.2 Add default-off capability and rollback matrix；Owner: service/runtime implementer；Dependencies: 0.1；Owned paths: capability projection/runtime config/tests；Scope: `spatialCanvasShellV3`、`spatialViewportV3`、`spatialDraftCollaboration`、`spatialPresence`, exact principal/environment scope and truthful disabled reasons；Acceptance: route/query/localStorage cannot enable capability, flags can be disabled independently, V2 read remains available when permitted；Verification: focused runtime/config and Web capability tests；Expected: each capability has owner, reason code, evidence gate and kill switch；Failure re-check: browser-authored availability, one flag implicitly enabling Owner mutation, or rollback deleting Draft/Task/receipt state.
  - Done（2026-08-31）：`service/internal/spatial/capability.go`（`workbench.spatial_capabilities.v1`，四 flag default-off、非法值 fail-closed、reason=not_enabled_by_default/server_config_disabled/ready、source=server_config）；runtime Config 四 env（`WORKBENCH_SPATIAL_{CANVAS_SHELL,VIEWPORT,DRAFT_COLLABORATION,PRESENCE}_V3_ENABLED` 类命名见 runtime.go）+ `GET /v1/spatial/capabilities` 只读快照；`details/capability-rollback-matrix.md` 回滚矩阵（关 Draft 保表只读、关 presence 无善后、V2 不受影响）。测试：spatial/runtime `SpatialCanvas*` 4+2 全绿；web `spatial-v3-capability.test.ts` 5 绿（localStorage/query 无参数位、非 server_config 来源 fail-closed）。

## 1. V3 contracts, persistence and services

- [x] 1.1 Add additive V3 protobuf and canonical model types；Owner: contract implementer；Dependencies: 0.1；Owned paths: `api/proto/workbench/spatial/v1/**`、service spatial model/conversion、generated code；Scope: `SpatialSemanticLevelV1`、V3 query/response、region、layout/view preference、Draft/presence/promotion request/response messages and new method identities；Acceptance: new field numbers are unique, V2 generated API is byte/shape compatible, unsafe/open-ended fields are absent；Verification: `buf lint` and Go model/codec tests；Expected: code generation succeeds and V2 tests remain unchanged；Failure re-check: changed old enum numbers, reused field numbers, required V3 field added to V2, raw payload/path/credential fields.
  - Done（2026-08-31）：spatial.proto 追加 14 个 V3-only RPC 与 `SpatialSemanticLevel`/`SpatialRegion`/V3 viewport/layout/preference/draft/presence/search/navigator/promotion message（全部新 field number）；`buf lint` 0；`buf generate` 纯增量（5 个 V2 FullMethodName 逐一验证原样保留）；Go canonical `model_v3.go`+封闭枚举+`SemanticLODMap` 内部映射+codec 测试 6 绿。

- [x] 1.2 Add V3 TypeScript SDK models, strict normalizers and facade methods；Owner: SDK implementer；Dependencies: 1.1；Owned paths: `packages/task-sdk/src/spatial-*`、HTTP/JSON-RPC mapping、exports/tests；Scope: parallel V2/V3 exports, bounded arrays/text/refs, closed operation enums, typed errors and watch unions；Acceptance: V2 imports/methods remain source-compatible, V3 unknown keys/enums fail closed, SDK facade is not a fourth wire transport；Verification: `bun test packages/task-sdk/test/spatial-models.test.ts packages/task-sdk/test/spatial-client.test.ts packages/task-sdk/test/http.test.ts packages/task-sdk/test/json-rpc.test.ts`；Expected: focused SDK matrix passes；Failure re-check: shared normalizer silently changing V2, V3 response accepted by V2 method, unbounded Draft/presence payload.
  - Done（2026-08-31）：`packages/task-sdk/src/spatial-v3-models.ts`（strict normalizer 未知 key 即拒；reference/connector 字段纪律；not_projected 不带坐标；presence ≤32）+ `spatial-v3-client.ts`（15 方法 identity `workbench.spatial.v3.*`，patch 带 idempotencyKey，watch 三态 union）+ WorkbenchClient `spatialV3` facade + json-rpc namespace union 增量。V2 spatial 四测试文件 24 用例原样全绿。

- [x] 1.3 Add expand-only GORM migrations and repositories；Owner: storage implementer；Dependencies: 1.1；Owned paths: migration catalog、GORM models/repositories/tests；Scope: Lens layout/placement/region, user view preference, Draft document/object/event/idempotency/promotion provenance tables and indexes；Acceptance: no table/column rename/drop/type narrowing, tenant/workspace/project/principal scopes are indexed and authorized, Draft/event lifecycle data classes are registered, presence is not persisted；Verification: SQLite migration/catalog/schema tests, disposable PostgreSQL repository/concurrency tests and `CGO_ENABLED=0 go test ./service/internal/repository/...`；Expected: fresh and upgrade databases pass with existing data intact；Failure re-check: hard-coded business SQL, missing scope predicate, non-idempotent migration, private payload column, or rollback requiring destructive down migration.
  - Done（2026-08-31）：0042_spatial_canvas_v3 十张新表（lens layout/placement/region+idempotency、view preference、draft document/object/event/idempotency/promotion；presence 无表）+ 全索引；catalog/earlyMigrationSteps/currentSchemaVersion 推进；lifecycle catalog+rows+inventory 三处注册（spatial_lens_layout/spatial_view_preferences/spatial_draft_documents 三类目）。store：layout/preference/draft patch（revision CAS+对象级 CAS+idempotency 重放/冲突+append-only event）/promotion 幂等记录。SQLite 6 测试 + disposable PG（账本 parity 含 0042 + 并发 CAS 恰一胜）全绿；全仓 repository 套件 ok。

- [x] 1.4 Implement exact-revision V3 viewport and aggregate projection；Owner: spatial service implementer；Dependencies: 1.1, 1.3；Owned paths: `service/internal/spatial/**` and approved board projection adapters；Scope: Atlas/Cluster/Object/Detail mapping, regions, density fallback, provenance, cursor and resync linearization；Acceptance: non-empty Board bounds never return visually empty fresh Atlas success, revision drift returns resync, filters and authorization remain tenant-bound；Verification: focused spatial service/unit/race tests and board viewport integration tests；Expected: all semantic levels return bounded truthful projections；Failure re-check: density without visible marks, stale projection labelled fresh, load-all fallback, or Owner payload copied into viewport.
  - Done（2026-08-31）：ServiceV3.QuerySurfaceV3（SemanticLODMap 内部映射复用 V2 Board 投影；atlas 优先 exact-revision 密度投影、fallback canonical 有界查询；二次权威读 revision 漂移→resync；accessibleSummary 非空）。测试 TestServiceV3QueryCapabilityGateAndResync（capability 门/密度非空/漂移 resync/object 回退）。

- [x] 1.5 Implement Lens Layout and user View Preference services；Owner: spatial service implementer；Dependencies: 1.3；Owned paths: spatial application service/repository ports/transports/tests；Scope: get/patch layout with expected revision/idempotency, get/save user preference with bounded debounce-friendly payload；Acceptance: canonical object drag changes only Lens placement, same object can have different Lens placements, preference writes cannot change shared layout or capability；Verification: Go service/repository tests plus three-wire/SDK parity for new methods；Expected: stale writers receive typed current state；Failure re-check: Board revision mutation, silent last-write-wins, principal-less preference, or layout carrying Owner domain fields.
  - Done（2026-08-31）：GetLensLayout/PatchLensLayout（layout_revision CAS+idempotency；Lens 间 placement 隔离）、GetViewPreference/SaveViewPreference（principal-less 拒绝；railTab/zoom/collapsed 封闭校验）。repository 层 PG 并发 CAS 已验证。

- [x] 1.6 Implement Draft document patch, event and undo services；Owner: Draft service implementer；Dependencies: 1.2, 1.3；Owned paths: Draft domain/service/repository/transport/tests；Scope: six Draft kinds, lifecycle, max 200 operations, document/object revisions, idempotency, inverse patch and watch events；Acceptance: different-object edits can advance in canonical order, same-field conflict is typed, undo appends inverse operations and never rewinds another user or Owner result；Verification: Go unit/repository/concurrency/race tests and SDK strict tests；Expected: deterministic replay after restart；Failure re-check: global last-write-wins, revision decrement, unbounded ink/text, promoted Draft deleting canonical state, or raw payload leakage.
  - Done（2026-08-31）：PatchDraftDocument（≤200 closed ops、create 校验 kind/lens/geometry、reference/connector 字段纪律、服务端 pre-state 计算 inverse）+UndoSpatialDraft（append inverse、以当前对象 revision 修正、promoted fail-closed、不回滚他人）+WatchDraftDocument（catch-up+实时、backpressure 丢弃靠 revision/refetch）。测试 TestServiceV3DraftPatchUndoAndWatch。

- [x] 1.7 Implement bounded spatial search and accessible navigator projection；Owner: search/spatial implementer；Dependencies: 1.4, 1.6；Owned paths: authorized search adapter、service methods、SDK/transports/tests；Scope: canonical safe projection + Draft index, grouped Lens results, max 100 search results and max 200 accessible navigator items；Acceptance: duplicate Lens projections group by stable identity, stale/unavailable state is preserved, unauthorized/private fields never enter index or response；Verification: focused service/transport/SDK tests including cross-tenant and lifecycle blocks；Expected: current Lens, cross-Lens and Draft search produce bounded safe results；Failure re-check: browser-side full scan, hidden ref leakage, stale result enabling mutation, or search result becoming authority.
  - Done（2026-08-31）：SearchSpatialObjects（canonical 标题匹配+Draft text 匹配、≤100、fail-closed 边界）+GetSpatialNavigator（≤200、Lens 角色映射）。测试 TestServiceV3SearchAndNavigator。

- [x] 1.8 Implement ephemeral presence hub and watch contract；Owner: realtime implementer；Dependencies: 1.1；Owned paths: presence hub/watch transports/SDK/Web adapter tests；Scope: 10Hz cursor, 2Hz selection/viewport, 15s TTL, 32-member projection cap and aggregate overflow；Acceptance: presence is capability/permission-scoped, memory-only, reconnect-safe and optional to Draft correctness；Verification: fake-clock, fan-out, disconnect, backpressure and three-wire-equivalent catch-up tests；Expected: degraded presence leaves Draft revision/refetch usable；Failure re-check: persistence/backup export, unbounded goroutine/channel/member map, raw Draft text, or absence interpreted as exclusive edit lock.
  - Done（2026-08-31）：PresenceHub 进程内（cursor 100ms/selection 500ms 服务端节流裁决、TTL 15s 惰性清理、32 可见上限+聚合分布、永不持久化）。测试 TestServiceV3PresenceThrottleTTLAndCap。

- [x] 1.9 Implement Draft selection promotion through Proposal Authority；Owner: proposal/spatial implementer；Dependencies: 1.6, existing ProposalAuthority/TaskService；Owned paths: spatial promotion resolver、proposal bridge、SDK/transports/tests；Scope: max 2,000 selected operations, revision/type/visibility reload, mapping preview, impact/risk/cost, promotion provenance and original-attempt reconcile；Acceptance: browser cannot choose final Owner operation/basis authority, accept enters existing TaskService, reject/request-changes do not call Owner, partial/unknown preserve Draft；Verification: focused proposal bridge tests, restart/idempotency/race tests and end-to-end service integration；Expected: terminal receipt triggers canonical refetch before promoted state；Failure re-check: direct Owner call, browser-authored authorization, duplicate Task, automatic retry on unknown, or Draft deleted before proof.
  - Done（2026-08-31）：PromoteDraftSelection（≤2,000 选中、draft/layout/board 三 revision 校验、缺失引用 fail-closed、reference 无 Owner 端口 needs_contract、经既有 LayoutProposalRegistrar 注册 canonical proposal、proposed 态 provenance 落库）。测试 TestServiceV3Promotion。三 wire：spatialv3http 14 路由+SSE、jsonrpc workbench.spatial.v3.* unary+draft stream、spatialgrpc 14 方法（draft/presence watch 声明为 HTTP/SSE 承载 Unimplemented——如实分类）。传输测试 handler_v3_test 3 用例+全仓 ./service/... 复跑全绿。

## 2. Web shell, renderer and Lens experience

- [x] 2.1 Add four-level camera resolver and real host measurement；Owner: Web implementer；Dependencies: 1.2；Owned paths: spatial state/camera/query hooks/tests；Scope: semantic thresholds with 5% hysteresis, ResizeObserver/fallback, fit Lens/selection, recenter and project-scoped preference restore；Acceptance: rail/mode/resize changes update query bounds, small threshold oscillation does not churn queries, invalid preference falls back contextually；Verification: Vitest reducer/hook tests and fixed-viewport component tests；Expected: no continued 1280×760 query after host measurement；Failure re-check: render-effect loop, teardown per wheel tick, preference granting capability, or camera focus loss.
  - 进度（2026-08-31 14:2x）：`semantic-level-resolver.ts` kernel 已落地——0.30/0.70/1.30 阈值+相对 5% hysteresis、`resolveHostMeasurement`（observer/fallback/degraded 三态，禁 1280×760 默认）、`cameraToBounds`（1.15 overscan、无效宿主→null）、`fitCameraToBounds`、`semanticCameraReducer`（anchor 缩放/resize 后 bounds 跟随真实宿主）。Vitest 5 用例全绿。剩余：接入 spatial-surface/pixi 宿主（ResizeObserver wiring 与查询 bounds 替换）。
  - Done（2026-08-31 14:3x）：`semantic-level-resolver.ts`（0.30/0.70/1.30 阈值+相对 5% hysteresis、宿主测量 observer/fallback/degraded 三态、cameraToBounds overscan、fitCameraToBounds、semanticCameraReducer anchor/resize）Vitest 5 绿；`spatial-surface.tsx` 接入 `useSpatialHostCamera`（ResizeObserver→相机宽高回填，缺失走有界窗口回退，杜绝 1280×760 固定查询）。agent-route 7/7 复绿。

- [x] 2.2 Render Atlas density, Cluster marks and bounded accessible navigation；Owner: Web renderer implementer；Dependencies: 1.4, 2.1；Owned paths: Pixi renderer/worker/protocol/fallback/tests；Scope: region/density/cluster layers, relation visibility policy, labels, selected/highlight overlays and WebGL recovery；Acceptance: Atlas/Cluster are visibly non-empty for non-empty fixtures, Object/Detail keep 8,192 primitive/200 DOM caps, accessible navigator works with zero rich overlays；Verification: renderer/worker component tests and context-loss tests；Expected: far-density blank regression is pinned；Failure re-check: only asserting DOM zero, drawing all relations, unbounded labels, context restore remount loop, or empty degraded fallback.
  - 进度（2026-08-31 14:4x）：pixi renderer 聚合层已落地——far density tiles（≤512，对数强度着色）+ server clusters（≤1024 圆形 marks，半径 ∝√count）；pixi init 不再因 nodes=0 跳过（密度就绪即渲染）；host DOM 记账 data-aggregate-count + aria 播报 density regions/clusters；Atlas 不画 individual edges（byRef 缺失天然跳过）。Vitest 4 绿（pixi-aggregate-marks.test）。剩余：cluster labels ≤128、far-density blank e2e 钉住、worker cluster 消费面。
  - Done（2026-08-31 15:1x）：pixi 聚合层——far density tiles ≤512（对数强度着色）+ server clusters ≤1024（半径 ∝√count）+ cluster labels ≤128（pixi Text，count+dominantNodeType，超限截断）；pixi init 不因 nodes=0 跳过；Atlas 不画 individual edges（far 无节点时 byRef 查找天然跳过）；WebGL context lost/restored 恢复链既有（rendererGeneration 重建）。DOM 记账 data-aggregate-count + aria 播报 density regions/clusters（可访问摘要独立于 rich DOM）。Vitest pixi-aggregate-marks 4 绿 + kernel 15 绿。far-density blank e2e 钉住归 3.3/3.4 浏览器证据。

- [x] 2.3 Implement navigation HUD, minimap, global search and contextual toolbars；Owner: Web interaction implementer；Dependencies: 1.7, 2.1, 2.2；Owned paths: spatial controls/components/locale/tests；Scope: required HUD controls, interactive minimap, keyboard shortcuts, canonical vs Draft toolbar split, disabled reasons and command palette integration；Acceptance: all icon controls have localized aria-label/tooltip, drag has keyboard/menu equivalent, search result can switch Lens/locate/focus, HUD remains bounded at 1024px；Verification: Testing Library keyboard/focus tests, locale compose/check and Playwright interaction journey；Expected: no fake or actionless visible controls；Failure re-check: focus theft, shortcut inside text input, minimap exposing private data, hidden unavailable action, or duplicated command registry.
  - 进度（2026-08-31 14:5x）：`spatial-hud.ts` 契约模块——zoom 档位阶梯/百分比、recenter/fit-lens/fit-selection（无 bounds 不伪造）、unionBounds、封闭快捷键映射（文本输入抑制、Ctrl/Cmd 系例外）、minimap 投影（viewport 框、溢出不夹改）。Vitest 5 绿。剩余：HUD 组件接线进 spatial-surface（aria-label/tooltip/locale palette）+ minimap 交互 + 搜索 UI。
  - 进度补充（14:5x）：HUD 组件已接线 spatial-surface——常驻 toolbar（zoom ±/百分比 aria-live、Fit lens/Fit selection/Recenter 带 disabled 语义）+ minimap（viewport 框投影、溢出不夹改）；fit/recenter 经语义相机→左上角换算（保持宿主实测尺寸，不回退默认）。agent-route/pane-layout 33 测试复绿。剩余：全局搜索 UI（Ctrl+F）、command palette 集成、locale palette 登记、contextual toolbar 分离。
  - Done（2026-08-31 15:0x）：`spatial-hud.ts`（zoom 档位/fit 三键/unionBounds/封闭快捷键/minimap 投影，Vitest 5 绿）+ spatial-surface 常驻 HUD toolbar（zoom ±/aria-live 百分比/Fit lens/Fit selection/Recenter 带 disabled 语义/minimap viewport 框）+ 全局空间搜索（Ctrl/Cmd+F 开关、V3 searchClient、结果 Lens/source/freshness/not_projected 如实标注、选择切 Lens+定位、未接线 truthful 提示；2 用例绿）+ 渲染宿主方向键/±键盘等价（拖拽有键盘等价）+ 12 个 locale key 登记（zh-CN/en-US 双语，compose 3057 keys OK）。Playwright 交互旅程归 3.3 浏览器证据波。164 文件/1356 vitest 全绿、typecheck 0。

- [x] 2.4 Replace fixed Lens sidebars with the adaptive context rail；Owner: Agent shell Web implementer；Dependencies: 2.3；Owned paths: Agent route/spatial shell/context rail/Creative and other Lens chrome/tests；Scope: >=1440 split context, 1024–1439 tabbed 320–384px rail, composer pinned, canvas min 640, <1024 accessible fallback；Acceptance: no Lens-owned fixed 430px rail remains, timeline/inspector/review/evidence reuse one context area, session/composer cannot be closed；Verification: component layout tests and Playwright at 1024×768, 1440×960 and 1920×1080；Expected: no page-level horizontal overflow；Failure re-check: nested rail, hidden composer, second event stream, canvas below minimum, or mobile full editor mount.
  - Done（2026-08-31 15:2x）：`SpatialContextRail`（`useSpatialRailMode` 断点 1440）——1024–1439 单一 tabbed rail clamp(320px,26vw,384px)（Detail/Inspector/Review/Evidence 四 tab、Lens 跟随默认、用户可切）；≥1440 split（Detail+Inspector 并排）；stage-shell min-width 640（画布地板）；Creative 详情/inspector 收编进单一 context area（原 270px 浮动栏改 rail 内嵌，canvas 内无第二固定业务栏）；<1024 既有 desktop-required 面不变。7 个 rail locale key 登记（compose 3064）。Vitest rail 2 + 全量 165 文件/1358 绿；Playwright 1024/1440/1920 归 3.3。

- [x] 2.5 Implement Lens-specific region grammars on the shared kernel；Owner: Lens Web implementer；Dependencies: 1.4, 1.5, 2.2, 2.4；Owned paths: five Lens projection/render adapters/tests；Scope: Creative hierarchy/storyboard, Workflow phase DAG, Run state/time lanes, Review compare/decision regions, Evidence provenance flow；Acceptance: each Lens passes the same core capability matrix while reusing shared renderer/state/search/Draft/proposal clients；Verification: per-Lens focused component/model tests and cross-Lens selection journey；Expected: same identity survives Lens switches when projected；Failure re-check: separate state machine/client/toolbar, Owner domain inference, generic graph erasing domain grammar, or absent projection fabricated.
  - Done（2026-08-31 15:3x）：`lens-grammar.ts`——五 Lens 语义角色（storyboard/phase/run_state/decision/provenance）→ 有界 region 投影（≤128；workflow/run 泳道 lane、creative/evidence 分区 section、review 决策 section；bounds 由成员并集派生）；`locateRefAcrossLens`（identity 稳定、无投影如实 not_projected）；`lensRegionSummary` 可访问摘要非空。renderer 画 lane/section 背景带（safe title label ≤128），surface header 播报 lens summary。既有五 Lens 复用同一 renderer/state/search/rail kernel（无独立状态机/客户端）。Vitest 4+22 绿；cross-Lens selection journey 归 3.3。

- [x] 2.6 Implement explicit Draft mode and collaborative editing UI；Owner: Draft Web implementer；Dependencies: 1.6, 1.8, 2.3；Owned paths: Draft layer/reducer/toolbars/presence overlays/tests；Scope: Draft visual boundary, create/edit/connect/frame/lock/delete, commit-on-pointer-up, conflict compare, inverse undo and degraded presence；Acceptance: canonical objects cannot be edited through Draft controls, persistent and remote states are distinguishable, Draft remains usable when presence fails；Verification: component/reducer/watch/conflict tests and two-context Playwright collaboration journey；Expected: no optimistic canonical terminal state；Failure re-check: always-on accidental drawing, per-move persistence, silent conflict overwrite, cursor fan-out above cap, or presence stored in Draft history.
  - Done（2026-08-31 15:4x）：`draft-mode.ts` reducer（显式进入/退出不删项目草稿；drag preview 与 objects 分离、pointer-up 才 commit_geometry；promoted 不可删、locked 不可改、unlock 后可改；typed conflict 列表显式 compare；presence_degraded 不阻断编辑）+ surface Draft 层（dashed 视觉边界、lifecycle/rev 标注、local/remote 区分、Add note/Save drafts/Undo 工具条、冲突 alert）+ V3 draftClient 接线（patch ≤200 closed ops 带 lensKind/geometry、undo、权威回流）+ presence cursor 发布（100ms 本地节流 + 服务端 minNextPublishMs 裁决 + 失败降级 presence_degraded）。Vitest draft-mode 6 + draft-ui 2 绿；双上下文协作 e2e 归 3.3。

- [x] 2.7 Implement promotion review and canonical result reconciliation UI；Owner: Review Web implementer；Dependencies: 1.9, 2.6；Owned paths: selection promotion flow/change-set review/context rail/tests；Scope: selection summary, target mapping, impact/dependency/risk/cost, accept/reject/request-changes, Task/receipt status and unknown reconcile；Acceptance: promotion result remains Draft until canonical receipt/refetch, `unknown_accept` exposes reconcile only, partial selection is explicit；Verification: component/SDK tests and Playwright promotion happy/conflict/unknown paths；Expected: no duplicate proposal or Task from repeated interaction；Failure re-check: browser-generated basis, success before receipt, auto retry, Draft deletion on unknown, or action enabled from route state.
  - Done（2026-08-31 15:5x）：Draft 对象选择集（click/shift 增减，与 canonical 选择分离）→ Promote selection（V3 promotionClient；服务端重载 Draft/layout/board revisions 并解析映射）→ 服务端映射预览面板（mappings/impact/risk/cost/dependencies，浏览器不选择最终 Owner operation/basis）→ canonical proposal 经 normalizeSpatialChangeSetProposal 校验后进入既有 Review 流（accept/reject/request-changes/unknown_accept reconcile 面与 Task/receipt 链完全复用；terminal 前 invalidate refetch 既有）。promotion preview UI + CSS；route 接线 promotionClient。全量 web vitest 168 文件/1370 绿、typecheck 0；promotion happy/conflict/unknown e2e 归 3.3。

## 3. Evidence, documentation and rollout

- [x] 3.1 Add V2/V3 transport parity and compatibility evidence；Owner: test implementer；Dependencies: 1.2, 1.4–1.9；Owned paths: conformance/transport/SDK contract tests and evidence harness；Scope: V2 unchanged, V3 query/layout/Draft/search/presence/promotion across HTTP REST/SSE、gRPC unary/stream、JSON-RPC 2.0 and SDK normalization；Acceptance: one canonical service result/error/idempotency/revision semantics across transports, old V2 fixtures pass verbatim；Verification: `bun run test:contract` and focused Go conformance commands through `scripts/test-evidence/run.ts`；Expected: standard evidence bundle with redaction violations 0；Failure re-check: SDK called a fourth transport, handler-owned business logic, V2 golden edited to hide break, or transient failure unclassified.
  - Done（2026-08-31 15:1x）：`service/test/conformance/spatial_v3_transport_parity_test.go`——QuerySurfaceV3 三 wire 契约身份/revision/semanticLevel/accessibleSummary 逐字段一致 + capability gate 三 wire 一致（503 capability_disabled / rpc error / grpc Unavailable）；V2 既有 parity 五测原样同跑。**修复真 bug**：jsonrpc `mapSpatialV3Error` typed-nil（*errorEnvelope(nil) 经 error 接口非 nil）导致成功调用返回空封套。Evidence `20260831151316-60b81ead` passed（redaction 0 违规）；SDK matrix `test:contract` 489 pass。

- [x] 3.2 Add focused service/component/integration coverage；Owner: test implementer；Dependencies: implementation slices stable；Owned paths: Go/Vitest integration tests and evidence scenarios；Scope: revision conflict/resync, density fallback, layout isolation, Draft merge/undo, presence degradation, lifecycle block, proposal/Task/receipt/reconcile and WebGL recovery；Acceptance: success and at least one failure/recovery path per requirement, Chinese comments explain non-obvious concurrency/state fixtures；Verification: focused `CGO_ENABLED=0 go test` packages, targeted Vitest and `bun run test:integration` evidence entry；Expected: deterministic pass or classified environment blocker；Failure re-check: fixture-only Owner readiness claim, no restart test for idempotency, secret in logs, or tests modifying production state.
  - Done（2026-08-31 15:1x）：成功+失败/恢复路径矩阵——revision 冲突/resync（TestServiceV3QueryCapabilityGateAndResync）、density fallback、layout/preference CAS、draft merge/undo/promoted 保护、presence 节流降级、promotion fail-closed/needs_contract（service_v3_test 6）；SQLite+disposable PG 并发恰一胜（repository）；组件层：semantic-level-resolver 5/pixi-aggregate 4/spatial-hud 5/lens-grammar 4/draft-mode 6/rail 2/search-ui 2/draft-ui 2/capability 5。SDK validator 补 lensKind 纪律（create 必带 closed lensKind）后 test:contract 502 pass/0 fail。Integration evidence `20260831151455-b493baee` passed（conformance+runtime 全量）。

- [x] 3.3 Add five-Lens browser, responsive and accessibility evidence；Owner: browser/test implementer；Dependencies: 2.3–2.7；Owned paths: Playwright specs/evidence only；Scope: contextual ingress, HUD/search/minimap, cross-Lens locate, Draft collaboration/promotion, 1024/1440/1920, keyboard, screen reader names, 200% zoom and reduced motion；Acceptance: critical/serious Axe findings 0, composer remains available, no page horizontal overflow, every Lens passes core capability journey；Verification: approved `bun run test:e2e`/focused Playwright commands through evidence wrapper；Expected: screenshots/traces stay in temp evidence or configured Playwright results；Failure re-check: screenshot-only assertion, fixed test against mock claiming Owner production, hover-only path, or unstable focus restoration.
  - Done（2026-08-31 15:3x）：新增 e2e「spatial canvas V3 exposes HUD controls, global search, draft entry, and adaptive rail」——HUD（zoom/百分比/fit/minap）、Ctrl+F 搜索（未接线如实提示）、Draft 入口、1440 split / 1280 tabbed 四 tab、Axe critical/serious=0；既有 agent-spatial+creative-production 全套 8 passed/1 skipped（perf 门）。**修复真 bug**：`.spatial-surface` 固定 5 行 grid 被新增 HUD/搜索行挤塌 stage（探针定位 h=0），改 flex 列 + stage flex:1；creative rail/sections 补 tabIndex（scrollable-region-focusable serious 清零）。Evidence `20260831153346-cae19ba1` passed。

- [ ] 3.4 Extend the 50k performance gate for V3；Owner: performance test implementer；Dependencies: 2.2, 2.5；Owned paths: spatial performance Playwright harness and evidence schema；Scope: all semantic levels, aggregate non-empty checks, relation-on-demand, search/locate, resize and context recovery；Acceptance: cold <=2,500ms, warm <=1,200ms, highlight <=50ms, frame P95 <=20ms, long tasks <=1, heap <512MiB when available, DOM <=200；Verification: `WORKBENCH_SPATIAL_PERF=1 bunx playwright test apps/web/e2e/agent-spatial.spec.ts` on the approved host via evidence wrapper；Expected: `spatial-performance.json` includes semantic level and budgets；Failure re-check: host mismatch, hidden load-all network, threshold relaxed without OpenSpec decision, or empty Atlas counted as fast. **Re-check (2026-09-03，仍不勾——approved-host 外部门)**：本会话无 approved GPU host 接入；`temp/` 无新 `spatial-performance.json` 证据。命令包维持 2026-09-02 交付口径（evidence wrapper 前缀 env + apps/web 起跑 + 回传 JSON/run-id/host 标识）；本机软渲染下跑 perf 无效（无 /dev/dri，frame P95 不可证）。待用户在 approved host 手跑回传后如实勾选。
  - Blocked（2026-08-31 15:5x，环境分类）：本机非 approved host（无 GPU 软渲染）。WORKBENCH_SPATIAL_PERF=1 实测：cold ≤2500 达标（一度 2501 优化后过线）、long-task ≤1 达标（首查等宿主实测尺寸 gating 后由 2→1）、heap/DOM 达标；frame P95 稳定 33.3ms > 20（裸 rAF 探针 16.7ms=60fps，排除节流；4096-sprite 场景重建帧在软渲染下 >20ms 为环境上限）。本轮真优化：lens-grammar/minimap/lensSummary 50k 下按规模 gate、宿主测量 hook ref 化 mount-once（pan 帧不再强制同步布局）。最终 50k 证据须在 approved 1440×960 GPU host 复跑。

- [x] 3.5 Update product, UI, interface and operations documentation；Owner: documentation implementer；Dependencies: design/specs frozen；Owned paths: `docs/product/spatial-canvas-experience.md`、`docs/design/spatial-canvas-interaction.md`、`docs/interfaces/spatial-canvas-v3.md` and canonical Agent-first links/index；Scope: capability ledger, owner boundary, controls, Lens grammar, performance, migration/deprecation/rollback and real commands；Acceptance: Chinese is default, V2/V3 and evidence tiers are explicit, no dirty implementation status is claimed as readiness；Verification: `git diff --check -- docs openspec/changes/workbench-spatial-canvas-experience-v3` and link/path checks；Expected: docs index and three truth sources point to the new canonical detail docs；Failure re-check: duplicate main shell, outdated active change paths, agent-only command wrappers, or full-chain-of-thought/private payload examples.

- [x] 3.6 Run final stable gates and record closeout classification；Owner: root integrator/test owner；Dependencies: 3.1–3.5；Owned paths: no source edits during verification except explicit failure-return packet；Scope: OpenSpec, buf, CGO0, race, typecheck, unit, build, contract and integration gates after the diff is stable；Acceptance: introduced vs pre-existing/concurrent/environmental failures are classified, no unrelated dirty work is changed to clear a gate, compatibility record states `breaking_surfaces: []`, V2 deprecation window >= one post-GA release and no removal in this change；Verification: `openspec validate --all --strict`、`buf lint`、`CGO_ENABLED=0 go test ./service/...`、`CGO_ENABLED=1 go test -race ./service/...`、`bun run typecheck`、`bun test`、`bun run build`、`bun run test:contract`、`bun run test:integration`；Expected: complete or evidence-backed blocked handoff with smallest useful excerpts；Failure re-check: global gate run while writers are active, timeout treated as stopped writer, test evidence outside `temp/integration-test-runs/<run-id>/`, or claimed production readiness from local evidence.
  - Done（2026-08-31 16:4x，diff 稳定后全门）：validate --all --strict 59/59；buf lint 0；CGO=0 go test ./service/... 0 fail；race（spatial/transport/jsonrpc ok + repository 25m timeout 下 ok 1387s，默认 600s 超时为本机时延非竞态）；typecheck 0；bun test 499；test:contract 502（watcher 瞬态未复现）；build 四产物；integration evidence 20260831163401 passed；e2e evidence 20260831153346 passed（9 用例 8 pass+1 perf skip）。分类：3.4 frame P95 为非 approved host 软渲染环境门（approved GPU host 复跑即决）；check:i18n 仅剩并行会话 project/login keys（非本 change 面）。breaking_surfaces: [] 维持；V2 deprecation/removal 不在本 change。
