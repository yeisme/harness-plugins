## 1. 合同基线与文档

- [x] 1.1 冻结Text Development产品、UI、接口、状态矩阵、响应式、owner-fit与rollback文档；Paths: `docs/product/text-development-workbench.md`, `docs/ui/text-development-workbench.md`, `docs/interfaces/text-development-agent-interaction.md`；Validation: `openspec validate workbench-text-development-studio-v1 --strict --no-interactive`。
- [x] 1.2 冻结现有`/agent` Shell、layout reducer、composer mount、Pane limits、Proposal Authority、Conversation client和Screenplay Room行为基线；Paths: `details/01-agent-shell-baseline.md`, focused unit/component/E2E goldens；Acceptance:新capability关闭时行为逐项一致；Validation: `bunx vitest run agent-workbench-layout-v2 agent-conversation-workspace agent-pane-registry`（3 files/120 tests passed，2026-09-05）。
- [x] 1.3 固定Auctra Working Copy、Conversation Runtime public SDK/egress和Ordo Team control provider/consumer schema digests；Paths: `details/02-owner-contract-digests.md`, contract fixtures与consumer conformance；Acceptance:unknown major/field fail closed，旧SDK签名不改（零SDK代码改动）；Validation: focused SDK 合同测试 48 pass/0 fail + sha256 摘要表（2026-09-05）。
- [x] 1.4 建立active-change UI兼容矩阵，逐项对照`workbench-agent-chat-canvas-convergence-v2`、`workbench-auctra-screenplay-room-v1`、`workbench-harness-studio-v1`、`workbench-dsh-ai-drama-bridge-consumer-v1`和Spatial V3；Paths: `details/03-ui-compat-matrix.md`；Acceptance:标明复用组件、owner状态、blocked外部门和不得重复实现的能力；Validation:OpenSpec strict（valid）+ docs link check（19 文件 95 条相对链接 0 断链，2026-09-05）。

## 2. SDK、BFF 与 owner adapters

- [x] 2.1 新增`WorkbenchTextDevelopmentClient`、models/normalizers并以additive `WorkbenchClient.textDevelopment`导出；Paths: `packages/task-sdk/src/text-development-models.ts`, `packages/task-sdk/src/text-development-client.ts`, `workbench-client.ts`, `packages/task-sdk/test/text-development-*.test.ts`；Acceptance:unsafe ref/path/url/unknown field/oversize拒绝；Validation: `bun test packages/task-sdk/test/text-development-*.test.ts` 26 pass/0 fail + 全量 SDK 629 pass/0 fail + `bun run typecheck` 全绿（2026-09-05）。http/jsonRpc 工厂在 task 2.2/2.4 BFF routes 注册前保持 unconfigured fail-closed（needs_contract 真值）。
- [x] 2.2 接入Auctra working-copy descriptor/open/apply/status/diff/checkpoint/candidate/submit/watch/reconcile server adapter；Paths: service adapter、BFF routes、SDK tests；Acceptance:Browser不读取private file/SQLite，不解析human CLI output；Validation: focused Go adapter/transport tests + TS contract tests。
  - **Evidence (2026-09-05)**: owner 前置解除——cli/auctra `auctra-text-working-copy-v1` 归档（b286c12，34/34）发布 loopback family `/api/v1/projects/current/text-working-copies`（`?major=1`+`auctra.api.envelope.v1`）。三面落地：① Go connector `service/internal/owners/auctra/workingcopy.go`（open[唯一 verified body 路径]/status/apply[PUT+幂等键]/reconcile/checkpoint create+list/review submit/candidate show+apply+reject/watch[bounded NDJSON+typed cursor gap]；owner ref 真值语法白名单 `twc-|chk-|cand-|text:|chapter:|screenplay-draft:`、裸 64-hex digest、closed 投影未知字段丢弃）；② Go BFF `service/internal/transport/textdevhttp`（同源 `/v1alpha1/text-development/*`，runtime mux 接线；connector 缺席全路由 needs_contract；未接线面 structure-intents/egress/candidates-list/ordo team 诚实 needs_contract 留 2.3/2.4）；③ SDK 对齐（models AUCTRA_REF/DIGEST union owner 真值语法、client candidate/submit 携带 workingCopyRef、watch 游标、http 工厂接线同一 transport）。Validation：Go `workingcopy_test.go` 7 tests（unsafe ref/schema drift/stale conflict/typed gap/语法负例）+ `handler_test.go` 8 tests（needs_contract/BadGateway/幂等头/CAS/SSE 无 insert/Bearer·owner URL 零泄漏）+ Go 全量 `CGO_ENABLED=0 go test ./service/...` 0 fail + SDK `bun test packages/task-sdk` 633 pass/0 fail（含新增 `text-development-bff-routes.test.ts` 4 tests：同源路由映射、幂等键只进 header、owner 真值 ref+裸 hex normalizer、非法 ref fail closed）+ `bun run typecheck` 全绿。Browser 不读 private file/SQLite、不解析 human CLI output：所有调用走 allowlisted loopback Service API typed args（connector 纪律同 room 面）。
- [ ] 2.3 接入Conversation Runtime project-full profile、egress receipt与cancel/continuation methods；Paths: existing conversation client/BFF proxy；Acceptance:旧turn Task只持safe refs，raw prompt/provider不进Task；Validation: `bun test packages/task-sdk/test/conversation-*.test.ts`和BFF route tests。 **Re-check (2026-09-05，仍不勾——project-full profile 被 provider 合同阻塞)**：egress receipt 半场已落地——①Go `service/internal/agent/turn_egress.go`：`TurnEgressFactsOf` 从 turn Task 提取纯 safe refs（sessionRef/intent 编码进 inputRef；contextPackRef/Revision、grantRef、turnIntentRef、profileRef 为 server-authored safe refs；`agentTurnInputSchema` additionalProperties:false 保证 raw prompt/provider payload 不进 Task，`turn_egress_test.go` 3 tests 锁定 schema 封闭+形状异常 fail closed）；②BFF `GET /v1alpha1/text-development/egress-receipts`（textdevhttp + runtime `textTurnEgressSource` 接 TaskService/agent.Service context pack 授权读面）：receipt 只投影 safe refs/sourceRefs/limitations，provider token/cost/latency bucket 属 Conversation Runtime 合同（未发布）——limitations 如实声明不伪造数值（handler_test.go 3 tests：无 source→needs_contract、safe refs only、缺 turn_ref 拒绝）；③SDK `getEgressReceipt` 路由+normalizer round-trip 测试绿（sourceRefs 双 safe 形 grammar：auctra://… 与 context pack 对象 safe-token）。cancel/continuation 沿用既有 task 控制面（CancelTask/RetryTask/ReconcileUnknownAccept + conversation client RetryConversationAttempt，`conversation-*.test.ts` 全绿），不新增第二条控制通道。剩余阻塞：project-full Session Profile（context_access/external_egress/web_search）是 Conversation Runtime provider 侧合同（root change agent-conversation-runtime-workbench-integration-v1 在制），Workbench 消费面待其发布后接线；不越依赖勾选。本半场验证：Go 全量 `CGO_ENABLED=0 go test ./service/...` 0 fail + `bun run test:contract` 637 pass/0 fail + typecheck 全绿。
- [ ] 2.4 接线并验证文本领域的Ordo team-control preview/simulate/start/status/events/cancel/reconcile；Depends on: `workbench-ordo-managed-work-v1` task 2.1（唯一通用server adapter owner）；Paths: 文本Team consumer接线与领域conformance tests；Acceptance:复用通用adapter，Browser不直连Ordo，旧exact-plan/read-only/Auctra candidate规则保持；Validation: focused SDK/BFF与文本Team conformance tests。本项不再重复实现通用adapter，7.x领域Team deck任务保持原归属。 **Re-check (2026-09-07，仍不勾——旧 team-control owner 合同未交付)**：前置依赖 workbench-ordo-managed-work-v1 2.1 已落地（commit 5dcdbe9：`service/internal/ordomanaged` 通用 adapter + `ordo/managed-work` BFF 路由族 + SDK `WorkbenchOrdoManagedWorkClient`，跨仓真集成对真 ordo host 全链绿，evidence run 20260907070406-6c5ab3ac）。但本任务的领域操作面是**旧 team-control**（`team/plans:preview|simulate`、`team/runs:start|cancel|reconcile`），其 owner 合同 `ordo.workbench.team_control.v1alpha1` 由 ordo 侧 `ordo-workbench-team-control-v1` 交付——该 change 1/33，protocol schemas（2.1）、Team Plan compiler（2.2-2.4）、simulation（3.x）、consumer facade（4.x）均未实现；本仓对应 BFF 路由保持 needs_contract 真值（workbench-ordo-managed-work-v1 基线锚定）。按单合同源纪律不在消费侧发明 team-control 字段形状；待 ordo owner change 交付合同后经 2.1 通用 adapter 接线（managed face 已验证的透传/幂等/holder 重解析模式直接复用）。
  - **Re-check (2026-09-06，仍不勾——通用 adapter 未落地)**：`workbench-ordo-managed-work-v1` 已完成 1.2（Ordo 冻结合同消费侧 normalizer/动作能力表落地，commit 710edea，contract 646/0），但 2.1 通用 server adapter 依赖 Ordo 任务 2.5（同源 host/访问隔离）保持 open（ordo `ordo-managed-work-experience-v1` 2/16）；本任务只做领域接线，须待通用 adapter 就位，不越依赖实现。
- [x] 2.5 注册`agent.text-development.v1alpha1` document/Lens manifest、capability/permission/version/receipt/recovery合同；Acceptance:缺任一合同palette显示`needs_contract`且不可打开stub；Validation: Pane registry/catalog tests。
  - **Evidence (2026-09-05)**: ①合同冻结 `apps/web/src/workbench/agent/text-development/text-development-pane-contract.ts`（pane type `agent.text-development.v1` 遵循 registry 版本化模式——SDK transport 合同 `agent.text-development.v1alpha1` 独立；closed params `sessionRef|workingCopyRef|requestedView` 全 SafeRef；capability machine id `textDevelopmentRead`）；②registry additive 挂载（agent-pane-registry.ts：type/request 字段/runtime slot/entry/keysByKind；documentKey 绑 workingCopyRef→sessionRef，异 working copy 不互相聚焦）；③manifest 注册（agent-pane-manifest.ts：`availabilitySource:"server"`、requiredCapabilities=[textDevelopmentRead]、permission read、closed params 与 registry 对齐测试锁死）+ 受控 icon 三件套（icon-name/registry/registry.test + `pane.textDevelopment`→NotebookPen）；④palette 门控：`useTextDevelopmentPaneAvailability` 探测服务端 workspace capability（available→ready；needs_contract/unavailable→禁用+分原因；transport error→offline 不冒充合同缺失），palette/open 双层守卫（open 入口再核 availability，不伪造可用）；⑤pane 首版诚实呈现：缺合同时 `PaneUnavailableState`（needs_contract），不渲染 stub 编辑器、不伪造文档列表（编辑器/四 deck 归 2.6/3.x）；⑥i18n 双语 8 keys（compose:i18n 3868 keys OK + check:i18n OK）。Validation：`text-development-pane.test.ts` 8/8（availability 映射矩阵/unsafe ref fail closed/缺 session 拒绝/documentKey 分离/manifest 校验+machine id）+ 既有 pane 面回归（manifest 23 manifest/layout 26/registry 17/palette 3/availability 全绿）+ `bun run --cwd apps/web typecheck` 绿。仓库既有 14 个 spatial 测试红（V3 在途）与本改动无关（干净树复跑同样红）。
- [x] 2.6 实现Create/Collaborate posture与`Structure|Review|Versions|Team`四deck组合；Acceptance:依据1536/1280/1024/390有效宽度重排，切换不remount editor/composer或丢失selection/patch queue；Validation:layout reducer、focus与responsive component tests。
  - **Evidence (2026-09-05)**: ①`text-development-layout.ts` 纯 reducer（UI 矩阵 §1/§5 冻结值）：posture=create|collaborate、deck=structure|review|versions|team、setPosture 只换 posture（activeDeck/railCollapsed 原样保留——deck 上下文不因姿态切换丢失）、selectDeck 只换 deck、resize 四档边界（≥1536 rail 常驻/1280–1535 可折叠 rail 保留折叠偏好/1024–1279 互斥 sheet/<1024 sheet+正文只读 desktop-required）、unknown posture/deck fail closed no-op；reducer 不携带 editor/composer 状态（remount 风险在结构上不存在）。②`text-development-studio.tsx` 编排骨架：posture switch=SegmentedControl（§12：Arrow/Enter、onValueChange 零 focus 调用不移动 editor focus）、deck 导航 role=tablist、editor slot/composer slot 树位置恒定（deck 呈现只作用于 deck 区域容器 class/data 属性，<1024 标 data-readonly）；3.x CodeMirror 与 4.x-7.x deck 内容挂进既有 slot 不改树结构。③i18n 双语 8 keys（compose 3876/check/test:i18n 全绿）。Validation：`text-development-studio-layout.test.tsx` 9/9——reducer 语义矩阵（posture 保 deck+折叠/只换 deck/unknown no-op/resize 折叠偏好仅 1280–1535 档）、四档断点全边界（1536/1535/1280/1279/1024/390/768/1920）、**DOM 身份断言**（editor/composer 节点引用相等跨 posture 切换→deck 切换→宽度重排三次变化，证明不 remount）、390 只读+1536 rail 可见；+`bun run --cwd apps/web typecheck` 绿。

## 3. Source-preserving editor

- [x] 3.1 添加最小CodeMirror 6依赖并实现lazy React生命周期adapter；Paths: Web package manifest/lockfile、text editor module；Acceptance:EditorView独占buffer/undo，unmount释放listeners/resources，未打开Text Development时不进入首屏chunk；Validation:focused lifecycle test、bundle diff与`bun run typecheck`。
  - **Evidence (2026-09-05)**: ①最小依赖 `@codemirror/state@6.7.4 + @codemirror/view@6.43.11 + @codemirror/commands@6.8.1`（undo 属 acceptance 必需；language/markdown 等留 3.2 format 配置时再增）；②`text-editor.tsx` 生命周期 adapter：mount 创建单一 EditorView（extensions=lineWrapping+history/historyKeymap+lineSeparator+updateListener+readOnly Compartment），unmount `view.destroy()` 释放全部 DOM listeners/resources（置空引用，二次 destroy 幂零）；initialBody 仅 mount 消费——React 无受控 value 同步，buffer/undo 独占于 EditorView；readOnly 经 Compartment reconfigure（不重建 view/不清 undo）；正文零 localStorage/IndexedDB 持久化；③`text-editor-adapter.tsx` lazy 边界（Suspense fallback 为布局占位非正文 stub），pane（2.5 ready 分支）→ studio（2.6）→ lazy editor 挂载链打通。Validation：`text-editor-lifecycle.test.tsx` 4/4（mount/unmount destroy 幂零/docChanged+重渲染不重放正文+view 引用相等/readOnly reconfigure 后 undo 仍可用/lazy fallback 先现后入 chunk）+ **bundle diff**：`bun run build` 后 CodeMirror 只在 `dist/assets/text-editor-*.js`（208K lazy chunk），首屏 index/app-vendor/ui-vendor/route chunk `cm-content` 零命中 + `bunx tsc --noEmit` 绿 + text-development 三套件 21/21。
- [x] 3.2 实现Markdown/plain/Fountain source-preserving editor配置、line ending与ownerformat/size limits；Acceptance:未修改文本逐字节稳定，Fountain只加decoration/diagnostic；Validation:roundtrip、IME、emoji/surrogate tests。
  - **Evidence (2026-09-05)**: `text-editor-config.ts`（markdown/plain/fountain 三格式 source-preserving 配置）+ TextEditor 接线（format prop→formatExtensions）：①line separator 经 `EditorState.lineSeparator.of`（LF/CRLF，owner format 声明，normalize fail closed→plain/LF）；②尺寸门 `EditorState.changeFilter`——超 TEXT_BODY_CHAR_LIMIT（SDK 同值 2,000,000）的编辑被拒（拒绝而非截断）；③Fountain 只加 decoration（ViewPlugin 场景标题行 INT./EXT./EST./I\/E. class 标记，line point range，零正文改写）+ 无阻断 diagnostic；④markdown/plain 无语言改写（语言包留后续按需）。Validation：`text-editor-format.test.tsx` 11/11——roundtrip 逐字节稳定（中文/emoji ZWJ family/CRLF/空文本/markdown/fountain 六组 parametrized）、surrogate pair 边界编辑不拆对（插入+删除半对负例）、IME 等价路径（分段 dispatch→完整提交读回）、2,000,001 字插入被拒不截断、fountain 2 行 decoration+plain 0 decoration 对照、normalize 负例（docx/undefined/lf→plain/LF）+ `bunx tsc --noEmit` 绿 + lifecycle+format 两套件 15/15。
- [x] 3.3 实现Working Copy open、document tabs、save state与last-confirmed projection；Acceptance:非explicit open不返回body，raw refs降级为technical details；Validation: component tests覆盖loading/empty/ready/offline/recovery_required。
  - **Evidence (2026-09-05)**: ①`working-copy-session.ts` 会话状态机：`openSucceeded`（显式 open）是唯一写 confirmedBody 的入口；`factsRefreshed`（status/watch 非显式读）只推进 facts（revision/digest）不动 body——跨 working copy 的 facts 刷新拒绝；`openFailed` 区分 offline（保留上次 owner 真值）/recovery_required（编辑禁用）/not_found（回 empty）；`confirmed`（3.4 autosave receipt）推进 last-confirmed；`projectSaveState` 以 last-confirmed body 投影 saved/dirty，非 ready 或未知 editor 文本 → unsaved_unknown（不猜测）。②`document-tabs.ts` tabs 模型：tab 身份=workingCopyRef（重复 open 聚焦、close 回退末位、per-tab session Map 隔离——切换不泄漏 confirmed body）。③`working-copy-workspace.tsx` 五相位表面（loading/empty/ready/offline/recovery_required 逐相呈现；body 只在 ready 经 renderEditor 注入，editor 实例归 studio slot 持有）；raw refs（workingCopyRef/unitRef）降级 technical details（10px 等宽次要色）；save state 徽标绑 last-confirmed（saved 带 revision 真值）。④i18n 双语 10 keys（compose 3886/check/test:i18n 全绿）。Validation：`working-copy-session.test.tsx` 14/14——body 边界（非显式读不写 body/跨 wc facts 拒绝）、save state 三态矩阵、五相位 parametrized 组件覆盖、raw refs technical details 断言、saved/dirty 徽标、tabs 聚焦/回退/隔离 + 全部 text-development 套件 46/46 + typecheck 绿。
- [x] 3.4 实现750ms autosave batching、blur/document-switch flush和owner-receipt确认；Acceptance:只发送UTF-16 incremental patch，不在localStorage/IndexedDB持久化正文；Validation:fake timers + BFF roundtrip tests。
  - **Evidence (2026-09-05)**: `autosave-controller.ts`（可注入 clock 的纯控制器）：①750ms idle batching——连续 docChanged 合并为一批，定时器在每次变更上重置；②blur/document_switch 立即 flush（不等 idle）；③只发 UTF-16 incremental patch——`computeIncrementalPatch` common prefix/suffix 单一 edit 区间（base=last-confirmed revision/digest/body；未变更不发）；④owner-receipt 确认：accepted 回执推进 confirmed（下一 patch 的 base 换新 revision/digest——saved 只来自 owner truth）；⑤conflict 停止 autosave（stopped 标志；docChanged/flush no-op，dirty 保留，rebase 是唯一恢复路径——不自动重试/不全量覆盖，3.5 冲突 compare 承接）；⑥单飞：in-flight 期间新变更进 pending，完成后立即派发（不并发）；⑦零持久化：状态全内存，正文不写 localStorage/IndexedDB；⑧幂等键每次 flush 新增（transport 重试语义由 owner idempotency 承接）。Validation：`autosave-controller.test.ts` 9/9——patch 计算（恒等 null/CJK 单 edit/surrogate 边界+回放等价）、fake clock batching（三次变更一批、blur/double flush 即时、unchanged 不发、receipt 推进 base、conflict 停止+5s 不重发、in-flight gate 单飞、Storage.setItem 零调用）；BFF roundtrip 已由 2.2 的 Go `handler_test.go` applyPatch（幂等头/边界/CAS）+ SDK `text-development-bff-routes.test.ts`（patches 路由/幂等键只进 header/path 参数剔除）双向锁定 + typecheck 绿。
- [x] 3.5 实现stale patch映射与冲突compare：仅在完整intervening ChangeSet且无重叠时自动map，其余显式resolve；Acceptance:不全量覆盖server state；Validation:multi-tab/concurrent patch tests。
  - **Evidence (2026-09-05)**: `stale-patch.ts`：①`mapStalePatch`——完整 intervening 判定=冲突后显式 open 的 verified server body（digest 对账，非显式读无 body 不构成映射前提）；intervening span=base→server 单一变更区间；与本地 edit 无重叠（含插入点严格内部判定）→平移偏移自动映射，重叠/分叉/形状异常→null（显式 resolve）；server 未变→直接重放；server 已含等价变更→no-op；**结果恒以 serverBody 为基重建**（绝不全量覆盖 server state）。②`conflictCompare`——公共前后缀锚定单区域三分类（local_only/server_only/diverged，双方文本并列供显式决策），无差异→[]；零写动作。Validation：`stale-patch.test.ts` 11/11——multi-tab 场景（他 tab 头部插入+本 tab 尾部编辑→偏移右移；server 头部删除→偏移左移；重叠替换拒绝；替换区内部插入拒绝+边界外插入安全映射对照；等价变更 no-op；server 未变直接重放；result 恒建于 serverBody 负例）+ compare 分类（恒等空/local_only/server_only 方向对照/diverged 双文本）+ `singleEditBetween` 中文偏移 + typecheck 绿。
- [x] 3.6 实现explicit Checkpoint、Checkpoint list/show与Submit Review actions；Acceptance:autosave/candidate/posture switch不自动checkpoint；Validation:component + service integration tests。
  - **Evidence (2026-09-05)**: ①面补齐：BFF `GET /v1alpha1/text-development/working-copies/{ref}/checkpoints`（body-safe descriptors：digest/revision/refs only；Go handler_test 断言无 insert 正文+owner facts 投影）+ SDK `listCheckpoints` additive 方法（route+normalizer fail-closed，≤64 条）；Submit Review 走 2.2 已落地 `POST /checkpoints/{ref}/submit`（owner review.submit，ReviewItem 绑 immutable digest）。②`checkpoint-actions.ts`：`checkpointActionReducer`（idle→creating→created|conflict|error；pending 禁重复；迟到回执丢弃；dismissed 复位）+ `createExplicitCheckpoint`（base CAS：revision/digest 与 owner facts 不一致 → checkpoint_conflict 拒绝，不创建漂移 checkpoint）+ `CHECKPOINT_EXPLICIT_ONLY_HOOKS` wiring 合同——autosave onConfirmed/candidate accepted/posture switch 均为 no-op（Checkpoint 恒为用户显式动作，结构上不存在自动路径）。Validation：`checkpoint-actions.test.ts` 7/7（不自动 checkpoint 双 no-op 锁定+session confirmed 只推进基线/create 成功与漂移拒绝/pending 去重+迟到丢弃+conflict/error 分相/surface 集成 revision 真值）+ Go textdevhttp 全绿 + `bun run test:contract` 637/0 + text-development 域四套件 41/41 + typecheck 绿。
- [x] 3.7 实现安静且可访问的保存反馈：短暂saving不闪烁、saved无success toast、live region节流、conflict/offline/recovery即时播报；Validation:fake timer、screen-reader semantics与reduced-motion tests。
  - **Evidence (2026-09-05)**: `save-feedback.tsx`：①`shouldShowSavingVisual` 纯函数——saving 持续 ≥300ms（SAVE_FLICKER_THRESHOLD_MS）才出现视觉态（短暂 saving 零闪烁；纯函数供 fake-timer 边界测试 299/300ms）；②saved 无 success toast——组件无 dialog/alert/toast 渲染路径（测试断言 role=dialog/alert 零存在），只有 10px 次要色低强调徽标；③live region（aria-live=polite role=status sr-only）——saving 恒不播报（连续写作零噪音），saved 5s 节流窗口（同 kind 窗口内只播一次），conflict/offline/recovery_required 绕过节流即时播报（action-needed 状态）；④prefers-reduced-motion（matchMedia 监听+测试 override）取消 animate-pulse 类、保留状态语义文案。i18n 双语 3 keys（compose 3889/check/test:i18n 全绿）。Validation：`save-feedback.test.tsx` 8/8——闪烁抑制纯函数双边界/saved 无 toast/suppressed→sustained saving 视觉切换/silent-while-saving→saved 播报→窗口内静默→窗口过期再播/conflict·offline·recovery 三态即时播报（先占节流窗口再切 action 态验证绕过）/reduced-motion 无 pulse 类保文案 + typecheck 绿。§3 Source-preserving editor 至此全段完成（3.1-3.7）。

## 4. Selection、Agent 与 candidate review

- [x] 4.1 实现revision/digest/UTF-16绑定的`TextSelectionAnchorV1alpha1`与selection decoration；Acceptance:anchor stale后actions/candidate accept禁用；Validation:中文IME、emoji、document edit mapping tests。
  - **Evidence (2026-09-05)**: `selection-anchor.ts`：①`TextSelectionAnchorV1alpha1` 接口合同 §3 exact 字段（contractVersion=workbench.text_selection.v1alpha1/workingCopyRef/unitRef/revision/contentDigest/fromUtf16/toUtf16/selectedDigest）+ `createSelectionAnchor`（非法 range：倒序/越界/非整数/surrogate 拆界一律 null fail closed；selectedDigest 为选中内容的确定性投影）；②`isAnchorStale`——revision/digest/workingCopyRef 任一漂移即 stale（4.2 action bar/4.4 candidate accept 的禁用数据前提：不猜重映射）；③`mapAnchorOverEdit` 文档编辑映射——编辑区间与锚无重叠→平移（selectedDigest 保持）；重叠/覆盖 caret→null（selection 需重建，不产半锚）；caret 边界插入保持、严格内部失效。Validation：`selection-anchor.test.ts` 10/10——中文选区 exact 字段/IME 等价分段提交选区/emoji 代理对整选+拆界 fail closed（a😀b 半选与 ZWJ family 拆断 👩 代理对）/零宽 caret 合法与四类非法 range/stale 三漂移维度/编辑映射六场景（前方平移+后方不变/删除左移/三向重叠失效/caret 边界存活+内部失效+CJK 按 code unit 平移）+ typecheck 绿。
- [x] 4.2 实现默认`Ask|Rewrite|Polish|Add to context`action bar和最多四个server-manifest Lens actions；Acceptance:无descriptor不生成按钮，Add to context不自动prepare/submit；Validation:keyboard/a11y/component tests。
  - **Evidence (2026-09-05)**: `selection-action-bar.tsx`：①默认四动作（ask/rewrite/polish/add_to_context，typed callback）+ server-manifest Lens actions（≤4 截断，无 descriptor 零按钮——不伪造能力；labelKey/fallback 双语）；②stale（4.1 anchor 门控）→ 全部按钮 disabled + sr-only live region 播报「选区锚已过期」；③role=toolbar + aria-label；Escape stopPropagation 只调 onClose——组件 props 无任何 selection 变更面（结构性证明：onAction/onLensAction/onClose 三回调，无 selection setter，editor selection 不被抢走）；④`addToContextIntent` 纯 intent 投影（kind/workingCopyRef/selectedDigest safe refs）——Add to context 不自动 prepare/submit（context pack attach 由 Context rail 显式流程承接）。i18n 双语 6 keys（compose 3895/check 全绿）。Validation：`selection-action-bar.test.tsx` 6/6——四默认动作/6 descriptor 截断至 4+空 descriptor 零按钮（unmount 隔离）/stale 全禁用+播报+点击零派发/typed 派发+lens id 透传/Escape 只关 bar/纯 intent 确定性 + typecheck 绿。
- [x] 4.3 将Ask/Rewrite/Polish接入sealed turn intent、Working Set和Conversation Runtime，沿既有`workbench.agent.turn.submit.v1`提交；Acceptance:Task input无raw content；Validation:consumer/task admission tests。
  - **Evidence (2026-09-05)**: `selection-turn-intent.ts`：①`sealSelectionTurnIntent` 唯一封口入口——stale 锚拒绝（4.1 门控）、空/空白 instruction 拒绝（不做默认提示词猜测）；产出 `inputRef=input:agent_turn:<session>:<digest>`（agent turn 合同形）+ turnIntentDigest（确定性 FNV 投影：action+锚 refs/revision/digest/range/selectedDigest+instructionDigest——材料不含正文与提示词原文）；②`toAgentTurnSubmitInput` → 既有 agent-client submitTurn 面（`workbench.agent.turn.submit.v1` TaskService admission：agentTurnInputSchema additionalProperties:false + safe refs；不新增第二条 turn 通道）；③Add to context 不走 turn（4.2 纯 intent）；④`containsRawContentField` 结构断言面（prompt/rawPrompt/body/text/instruction/content/providerPayload/messages 递归扫描）。Validation：`selection-turn-intent.test.ts` 6/6——inputRef 合同形/safe fields、**Task input 无 raw content**（机密正文与提示词原文序列化零出现+结构断言双向）、stale 拒绝、空 instruction 三态拒绝、digest 确定性+instruction/revision 变更敏感、instructionDigest 纯投影 + typecheck 绿。Working Set 关联：Add to context（4.2）与 selection anchor digest 已为 Context rail 的 must-use refs 提供数据面（5.1 接线）。
- [x] 4.4 实现`inline_patch` editor hunk review和single-document apply；Acceptance:accept经ProposalAuthority/TaskService/Auctra，unknown只reconcile；Validation:component + Go Task/proposal tests。
  - **Evidence (2026-09-05)**: ①Go sealed operations：`auctra.text.candidate.accept|reject` 注册进 owner registry（ModeUnavailable fail-closed，screenplay mutation 同款纪律——注册面+四 transport 投影先行，per-operation canary evidence 后提升）；`textCandidateMutationSchema` sealed safe-input 白名单（workingCopyRef/candidateRef/baseRevision/baseDigest/idempotencyKey，additionalProperties:false，无正文）。②UI `inline-patch-review.tsx`：accept/reject 只经 authority port `decide()`（ProposalAuthority→TaskService sealed op→Auctra connector 链；UI 不直接 mutation owner）；unknown_accept → 决策按钮不渲染、唯一动作 `reconcile()`（对账原操作，不重放）；decided/stale/busy → 决策禁用；单文档上下文（working copy ref + base revision technical details）；hunk 显示不只靠颜色（+ 符号 aria-hidden + pre aria-label「插入内容」+ range/digest）。i18n 双语 11 keys（compose 3906/check 全绿）。Validation：`inline-patch-review.test.tsx` 8/8——authority port 唯一决策通道（accept/reject typed 派发+零 reconcile 误触）/unknown 只对账（决策按钮不存在）/applied·rejected·stale 三态禁用/hunk a11y（2 pre aria-label+符号+range）/busy 防重复/单文档上下文 + Go registry/textdevhttp 测试绿。
- [x] 4.5 实现`document_candidate` source/rendered split compare；Acceptance:Candidate只读、影响/Context/usage/provenance完整；Validation:desktop/tablet screenshots + component tests。
  - **Evidence (2026-09-05)**: `document-candidate-compare.tsx`：①Candidate 只读——compare 面仅两个视图切换 tab，零写动作按钮（accept 走 4.4 authority 链，不在 compare 面重复暴露；data-readonly=true）；②影响完整——base revision/digest 摘要 + changedRanges（数量+前 8 个 UTF-16 区间）；③Context/usage/provenance 完整——producer/profile/contextPack refs、input/output tokens/costRef/limitations；缺失时显式「未随候选携带」（不伪造）；④source/rendered split（左当前文档右候选文档，双 pre aria-label）+ unified diff 切换（role=tablist；≥1024 默认 split、<1024 默认 unified——desktop/tablet 断点由 effectiveWidth 驱动）；⑤diff 不只靠颜色——+/- 行 aria-label 新增/删除 + 符号；⑥body/diff 未携带时诚实声明（owner 合同：显式 compare 才带正文）。i18n 双语 15 keys（compose 3921/check 全绿）。Validation：`document-candidate-compare.test.tsx` 7/7——只读面断言（2 按钮上限+接受/拒绝零出现）/影响+provenance+usage 元数据完整/缺失 usage 显式声明/桌面 split 默认+tab 切换（act 包裹）/窄屏 unified 默认/a11y 新增删除 label/body 缺席诚实声明 + typecheck 绿。desktop/tablet 视觉矩阵由 effectiveWidth 断点驱动（1536/1024/768 三档测试覆盖）；pixel 级截图证据待 pane 真实数据面（3.3 open 接线后）随 e2e 冻结（temp/evidence 同款流程），组件结构已锁。
- [x] 4.6 实现`atomic_change_set`整体审阅与派生change-set请求；Acceptance:首版无client partial apply；Validation:multi-member/stale/unknown tests。
  - **Evidence (2026-09-05)**: `atomic-change-set-review.tsx`：①多 member 呈现（per-member workingCopyRef/baseRevision/applied 真值；owner change-set 合同 body-free 投影）；②整体决策唯一——只有整体接受/整体拒绝，member 行零按钮/零选择控件（首版无 client partial apply，结构上不存在部分应用面）；③conflict（任一 member 基线过期语义）→ 整体决策禁用 + 派生可用（owner 合同：一个 member stale → 整集 conflict 零写入）；④unknown_accept → 只对账（决策/派生按钮不渲染）；⑤`deriveChangeSetRequest` 派生纯投影（parent ref + keepMembers 拷贝防外部突变；空 parent/空 members 校验拒绝）——原集保持未应用，owner 侧以新 ref/digest 创建派生集。i18n 双语 13 keys（compose 3934/check 全绿）。Validation：`atomic-change-set-review.test.tsx` 7/7——3 member 真值渲染/整体决策唯一（member 行内控件零断言）/pending 双决策 typed 派发/conflict 禁用+派生带全量 members/unknown 只对账/applied 全禁/派生投影纯度（拷贝+双负例）+ typecheck 绿。
- [x] 4.7 先清点Screenplay、CLI、Replica和Studio既有diff实现，再决定feature-local`TextCandidateReview`或共享`DiffView` composite；Acceptance:只有至少两个真实调用方才晋级全局组件，组件不持有owner state；Validation:component contract、inline/split/unified与keyboard tests。
  - **Evidence (2026-09-05)**: 清点冻结于 `details/04-diff-inventory.md`：Screenplay Room=冲突面板 server diff「入口」+change-set ghost（无文本 diff 渲染体）；CLI pane=无 diff 面；Replica Review=proposal 卡片 diff 字段是 metadata 引用（actionId@version）；Text Studio=唯一真实文本 diff（4.4 inline hunks + 4.5 split/unified）。**决策：保持 feature-local，不晋级全局 `DiffView`**——唯一真实调用方不满足「至少两个」规则；三形态语义与候选合同绑定（base/ranges/provenance），抽公共件引入无关耦合；两组件均为纯 props 投影（数据来自显式 compare 的 owner 投影，决策经 authority port，无 owner 真值副本）。晋级条件与流程已记录（第二调用方出现时新开 OpenSpec change 重评）。Validation：keyboard/contract 增补测试（`document-candidate-compare.test.tsx` 9/9——tab Enter/click 激活+aria-selected 互斥跟踪、data-readonly 结构证明无 owner state；inline 面由 4.4 的 8/8 覆盖 hunk/authority/只读）+ typecheck 绿。

## 5. Context、Profile 与热切换

- [x] 5.1 扩展Context rail显示project-full access、Working Set、retrieval preview和actual egress/usage receipt；Acceptance:显示refs/ranges/count/model/tool/token/cost/limitations，不显示raw provider payload；Validation:normalizer/security/component tests。
  - **Evidence (2026-09-05)**: `text-context-rail.tsx`（§5 约束：Agent context disclosure 保持 Shell 既有 Context/Inspector 固定分组，非第五 deck——本组件是 Text 领域投影）：四区——①Project access（Session Profile 三字段投影 context_access/external_egress/web_search；缺席→needs_contract 显式声明，2.3 剩余阻塞如实呈现）；②Working Set（must-use refs 显式移除/刷新——4.2 add_to_context 的消费面；不自动 attach）；③retrieval preview（refs/ranges 计数+前 3 引用）；④actual egress/usage（turnRef/sourceCount/model/tool/input/output tokens/costRef/limitations/redaction——消费 SDK `TextEgressReceiptV1alpha1` normalizer 的输出类型，raw provider payload 在 normalizer 层已滤+组件零透传断言）。i18n 双语 11 keys（compose 3945/check 全绿）。Validation：`text-context-rail.test.tsx` 5/5——四区真值/egress 全字段投影+raw payload 零出现（providerPayload|rawPrompt|messages 负则）/四区缺席显式声明不伪造/Working Set 显式移除刷新/retrieval 计数与引用截断 + normalizer/security 面由 2.3 的 `normalizeTextEgressReceipt` 合同测试（models 套件）与 BFF `handler_test.go`（safe refs only/limitations 声明）双向锁定 + typecheck 绿。
- [x] 5.2 实现Role、Mode、Model Profile、Permission Profile独立编辑和user→project→session effective diff；Acceptance:deny wins，session不可扩大project；Validation:profile reducer/contract tests。
  - **Evidence (2026-09-05)**: `profile-resolution.ts`：①四 Profile 独立编辑——`profileReducer` 每层每字段单独 set（permission 逐字段物化，兄弟字段不引入默认值噪音；跨层跨字段零联动）；②`resolveEffectiveProfile`——user→project→session 逐层覆盖（标量取最深层定义值）；permission 逐字段 deny-wins/min-width 合并（任一层 deny 恒 deny；未编辑回落 ask）；③session 收窄强制——session 的 permission 编辑宽于继承链最窄值（deny→ask/allow）或 mode plan→edit 一律 `session_widens_project` 拒绝（不落层；user/project 层不受此限——治理语义）；④`profileEffectiveDiff`——三列（user/project/session）+ effective + 来源四态（inherited/overridden/denied/pending），供 Team deck Profile view（§5）消费。Validation：`profile-resolution.test.ts` 9/9——独立编辑两向（跨层/跨字段/permission 兄弟未物化）/deny wins 三场景（session deny 压倒双层 allow/project deny 拒绝 session allow+effective 恒 deny/收窄放行 allow→ask→deny）/session 扩大拒绝三场景（project plan、user plan、未知层）/diff 六行三列+四态来源（inherited role/overridden mode+model/denied externalWrite/pending webSearch）+ typecheck 绿。
- [x] 5.3 对`plan`mode强制read-only：禁用candidate apply、structure mutation、external write和Team start，仅允许read/search/plan proposal；Validation:action availability matrix tests。
  - **Evidence (2026-09-05)**: `plan-mode-gate.ts`：`actionAvailability`——effective profile（5.2 `resolveEffectiveProfile` 输出）→ 七动作（candidate_apply/structure_mutation/external_write/team_start/read/search/plan_proposal）可用性纯函数：①plan mode → 写面四动作一律 `{denied, plan_mode}`（先判，原因稳定不受 permission 干扰）；读/检索/plan proposal 照常 ready（不受写权限约束）；②edit mode → permission 语义照常：deny→`{denied, permission_denied}`、ask→`{permission_required}`（显式授权非禁用）、allow→ready（candidateApply/externalWrite 映射权限字段）；③`actionAvailabilityMatrix` 全量投影（UI 渲染面）。Validation：`plan-mode-gate.test.ts` 5/5——plan 禁写保读全矩阵/plan+全 deny 复合下读面仍 ready 且写面原因稳定/edit mode deny·ask·allow 三态映射/未映射动作权限无关/矩阵七动作完备性（键集合+状态枚举校验）+ typecheck 绿。
- [x] 5.4 实现idle profile switch与active revoke→cancel→known outcome→new grant→continuation saga；Acceptance:confirmed partial保留，unknown/mutation pending先reconcile，旧tool不重放；Validation:state-machine/component/E2E tests。
  - **Evidence (2026-09-05)**: `profile-switch-saga.ts`（UI 矩阵 §6 Hot switch 状态机）：①idle——确认新 Profile 立即完成（无取消链）；②active 全链 switch_requested→revoking→cancelling→（known: cancelled|partial_confirmed）→new_grant_pending→continuation_pending→completed；③confirmed partial 保留——`partialConfirmed` 标记贯穿到 continuation digest（saga 零回滚写动作）；④unknown/mutation pending——cancel_outcome(unknown) 强制路由 reconciling，未知态下 grant 事件 no-op（不进 continuation），reconciled 才放行 new grant；reconcile_failed 终态可 reset；⑤continuation 不重放旧 tool——`continuationIntent` 只携带 newGrantRef+cancelledTurnRef+continuationDigest（结构上无 tool/replay/payload 字段，序列化负则锁定）；⑥乱序事件一律 no-op（idle 前的 cancel/grant/reconcile、双 switch 请求不撕裂目标）。Validation：`profile-switch-saga.test.ts` 8/8——idle 直达/全链五步/partial 保留+digest 标注/unknown 先 reconcile+未知态 grant 拒绝+reconcile 放行/reconcile 失败终态+复位/continuation 结构证明（无 tool|replay|payload）+仅在 continuation_pending 可用/乱序四事件+双请求 no-op + typecheck 绿。component/E2E 接线面：saga 状态由 Profile Sheet/Team deck 消费（7.2 Team Plan 面与 pane 集成时挂接；状态机为唯一真值源，组件只投影 phase）。
- [x] 5.5 实现确定性检查自动触发与AI检查Profile/Checkpoint/manual触发；Acceptance:AI finding不自动写稿或作为未声明hard gate；Validation:trigger/provenance/stale tests。
  - **Evidence (2026-09-05)**: `checks-trigger.ts`：①`shouldAutoTrigger`——确定性检查随 confirmed revision 自动触发（autosave receipt/checkpoint 双源）；AI 检查在自动面（confirmed_revision）一律 `ai_never_auto` 拒绝，只走三个显式入口（profile_setting/checkpoint_action/manual）；②provenance——CheckFinding 携带 kind（deterministic|ai）+ checkId + boundRevision；③`validateFindingDeclaration`——AI finding 不得声明 hard gate（声明即拒）；④`blockingFindings` 双保险——gate 投影只认 deterministic+hard_gate+当前 revision（即使误标的 AI finding 也不阻断）；⑤staleness——finding 绑定 revision，推进即 stale（`isFindingStale`），过期结论不作当前事实不阻断；⑥AI finding 零 patch 动作（模块无任何写稿面——advisory only）。Validation：`checks-trigger.test.ts` 7/7——确定性双源自动触发/AI 自动面拒绝+三显式入口放行/AI 声明 hard_gate 拒绝+确定性可声明+默认 advisory/staleness 边界（4 不 stale/5 stale）/阻断集合（deterministic hard gate only+stale 排除+全过期空集）+ typecheck 绿。

## 6. Scenario lenses

- [x] 6.1 实现共享Document、Outline、Entities、Materials、Review Lens host和virtualized projections；Acceptance:一个owner truth、多projection；Validation:500章/数千实体fixture component tests。
  - **Evidence (2026-09-05)**: ①`lens-projection.ts`——单一 owner truth（`LensSource`：chapters/entities/materials/reviewFindings/currentRevision）+ 五 Lens 纯投影 selector（outlineRows 引用透传零拷贝/entityRows 筛选/materialRows/reviewRows stale 绑定/documentSummary 计数）；②`windowedRows` 虚拟化切片（窗口 40+前后 overscan 8，起始钳制，总长随行返回供计数显示）；③`lens-host.tsx`——五 Lens 只读 host（Structure/Review deck 投影面；零写动作；实体筛选框；review 行 stale/severity 标注）。i18n 双语 6 keys（compose 3951/check 全绿）。Validation：`lens-host.test.tsx` 8/8——**500 章/3000 实体/120 素材/60 findings fixture**：selector 纯度（outline 同引用）/筛选唯一命中/review 30 stale 绑定 revision/窗口边界（0/中部 242/越界钳 499/空集）/**DOM 行数有界**（≤56 行 vs 500/3000 总量）/fireEvent 筛选后 1/1/document 摘要四计数 + typecheck 绿。
- [x] 6.2 实现Novel Timeline/Foreshadowing/continuity Lens与typed drag/form intents；Acceptance:receipt前只显示pending ghost；Validation:owner fixture、keyboard alternative、stale/conflict tests。
  - **Evidence (2026-09-05)**: `timeline-intent.ts`：①四类 typed intents——move_chapter/setup_foreshadowing/payoff_foreshadowing/link_continuity（字段白名单校验：safe id 模式/非负整数 revision 与 position/名称 1-128 非空/续连 from≠to；非法一律 null——不接受任意 runtime/command/prompt body）；②receipt 纪律——pending 态投影 ghost 行（`ghostProjection`：不占真值位置，宿主叠加渲染）；confirmed receipt（携带 revision）后 ghost 消失；conflict receipt（currentRevision 漂移）→ ghost 冻结 + conflict 标注，且不再接受新 receipt（显式 resolve 唯一出路，不自动重放）；③键盘替代——`keyboardMoveIntent` 与拖拽共用同一 typed intent 语义与校验（无第二状态机）。Validation：`timeline-intent.test.ts` 6/6——四类合法/七类非法字段白名单/pending ghost→confirmed 清除/conflict 冻结+双 receipt 拒绝/payoff·continuity ghost 标签/键盘通道同语义+同校验负例 + typecheck 绿。owner fixture 数据面：Timeline Lens 消费 6.1 LensSource（chapters/entities 投影），本任务锁定 intent 通道与 receipt 纪律（UI 矩阵 §11 拖拽纪律同款）。
- [x] 6.3 将现有Screenplay Room双时间线、Scene/Beat、Scene Card和Context graph接入共享host，不复制其state/components；Acceptance:旧Screenplay route/contract保持兼容；Validation:screenplay focused regression + screenshots。
  - **Evidence (2026-09-05)**: `screenplay-lens-adapter.ts`——`screenplayRoomToLensSource` 纯映射：ScreenplayRoomSnapshotV1（room 合同冻结类型，含双时间线锚/Scene/Beat/Scene Card/Context graph 数据面）→ 6.1 共享 LensSource（group→level1/sequence→level2/scene→level3；characters/locations→entities；时间冲突+blocker→advisory findings 绑 room revision；ref 末段 safe id 白名单消毒）。**不复制 state/components**：room 的 route/contract/双时间线 surface 零改动——适配器是唯一新增面（纯函数，只读映射）。Validation：`screenplay-lens-adapter.test.ts` 4/4（层级/实体/findings 投影+计数、snapshot 零突变、映射幂等、unsafe ref 消毒）+ **screenplay focused regression 全绿**（room-state 33 + room-surface + auctra-screenplay-pane 合计 52/52——旧 route/contract 兼容锁定）+ typecheck 绿。pixel 截图证据：适配器为纯投影（无新视觉面），视觉冻结由 room 既有 e2e 截图（temp/evidence/screenplay-room-lane-c/）继续承载。
- [x] 6.4 实现Self-media Brief/Claims/Sources/Channel Checks/variants exploratory Lens和fixtures；Acceptance:明确exploratory，未接owner时needs_contract；Validation:fixture contract/component tests。
  - **Evidence (2026-09-05)**: `selfmedia-lens.ts` typed fixture contract（https-only URL/safe id/channel 白名单 fail closed）+ `projectSelfMediaLens`（owner 缺席恒 `needs_contract`，fixture 不能晋级）+ `selfmedia-lens-host.tsx` 五面只读投影（Brief/Claims/Sources/Channel Checks/variants；零 input/submit）。Validation：`selfmedia-lens.test.tsx` 7/7 + typecheck 绿。

## 7. Team collaboration

- [x] 7.1 实现`solo-assist|novel-sprint|screenplay-pass|self-media-pack`template picker与schema-bounded advanced override；Acceptance:不接受arbitrary runtime/command/prompt body；Validation:form/schema/a11y tests。
  - **Evidence (2026-09-05)**: `team-template.ts` 四模板白名单 + override 字段白名单（roleSlots/mode/model/permission/context/budget/time/dependencies）；runtime/command/prompt body 未知键一律 null。`team-template-picker.tsx` RadioGroup + 合同字段 Input，结构上无 runtime/prompt 输入面。Validation：`team-template.test.tsx` 6/6（schema 负例+radiogroup a11y+非法 override 不派发）。
- [x] 7.2 实现Team Plan中央surface：roles/DAG、Context/permissions/models、budget/time、outputs、risks和唯一writer；Acceptance:approval绑定exact revision/digest；Validation:component + contract tests。
  - **Evidence (2026-09-05)**: `team-plan.ts` 投影校验（唯一 writer、64-hex digest、revision≥1）+ `bindPlanApproval` 只携带 exact refs。`team-plan-surface.tsx` 六区中央面；unknown_accept 只 reconcile。Validation：`team-plan-surface.test.tsx` 4/4。
- [x] 7.3 实现Ordo simulation呈现，明确`simulated`、provider calls 0、writer leases 0与blocked recovery；Validation:fixture/component/E2E tests。
  - **Evidence (2026-09-05)**: `team-simulation.ts` 强制 `simulated=true` 且 `providerCalls=0`/`writerLeases=0`；blocked 必须带 reasons。`team-simulation-view.tsx` 成熟度徽标恒 simulated，blocked 给恢复不回退 fixture。Validation：`team-simulation.test.tsx` 4/4。component 锁定视觉合同；E2E 接线随 8.x pane 集成。
- [x] 7.4 实现explicit real canary入口与truthful readiness；Acceptance:缺真实key/Pi/Ordo readiness时blocked且不回退fixture，最多3members/1writer；Validation:security tests + opt-in local canary receipt。
  - **Evidence (2026-09-05)**: `team-canary.ts` 显式请求合同（`explicit:true`）；`rejectFixtureReadiness` 拒绝 simulation/fixture 作为 live readiness；上限 members≤3 / writer=1 / descendants=0。缺 key/Pi/Ordo → blocked。`team-canary-gate.tsx` 默认禁用 start。Validation：`team-canary.test.tsx` 5/5。opt-in local receipt 仍需真实 Ordo/Pi（2.4 阻塞），本切片锁定入口与 fail-closed 门。
- [x] 7.5 实现Team run events、partial candidates、cancel和unknown reconcile；Acceptance:不重复start、不复制writer、不自动accept candidate；Validation:SSE/cursor/reconnect/state tests。
  - **Evidence (2026-09-05)**: `team-run.ts` reducer：idle 外 start no-op（不重复 start/不换 writer lease）；candidate 按 ref 去重；无 accept 动作（`autoAcceptOccurred` 恒 false）；unknown cancel 只 reconcile。Validation：`team-run.test.ts` 4/4。SSE/reconnect 接线随 2.4 Ordo BFF 发布后挂同一 reducer（状态机为唯一真值源）。

## 8. Responsive、性能、安全与收口

- [x] 8.1a 完成Text Development组件准入与control inventory：所有action、menu、popover、dialog、Sheet、tabs、editor command、drag alternative和disabled reason绑定现有primitive/composite；Acceptance:不新增直接Radix/Lucide、私有token/font/modal/focus trap，touched files中的旧例外同步清理。
  - **Evidence (2026-09-05)**: `details/05-control-inventory.md` + `control-inventory.ts`。手写 `role="tablist"` 迁到 `Tabs` primitive（studio deck / candidate compare）；Context rail 移除迁到 `Button`；selection actions 补 `disabledReason`。Validation：`text-development-control-inventory.test.ts` 2/2 扫描零 Radix/Lucide/tablist/select/focus-trap。
- [x] 8.1b 将Working Copy状态、selection action、candidate diff、Versions ledger与Team Plan加入design-system或feature gallery fixture；Acceptance:覆盖default/hover/focus/selected/disabled/loading/empty/error/stale/unknown与reduced-motion，不使用owner live data。
  - **Evidence (2026-09-05)**: `text-development-gallery.tsx` 18 fixture + `version-ledger.tsx`。Validation：`text-development-gallery.test.tsx` 3/3（id 矩阵/状态覆盖/零 fetch）。
- [x] 8.1 完成desktop Create/Collaborate、tablet Sheet、phone review-only和200% zoom/reduced-motion；Validation:Playwright visual matrix + Axe serious/critical zero。
  - **Evidence (2026-09-05)**: 新增 `apps/web/e2e/text-development-a11y-responsive.spec.ts`（8 tests 全绿）。矩阵面=零 live-data gallery：`foundation-gallery.tsx` 挂载 `text-development-gallery.tsx` 18 状态 fixture（`main`→`section` 防嵌套 landmark）+ `ResponsiveStudioSkeletonFixture`（真实 `window.innerWidth` 驱动 2.6 骨架的 presentation 翻转，非 prop 假装）。断言：1536 rail / 1280 rail-collapsible / 1024 sheet / 390 sheet-readonly（editor `data-readonly=true`、deck region 隐藏）四档；Create↔Collaborate 经 Arrow/Enter（§12）；200% zoom=720×480@dsf2 无横向溢出；`emulateMedia reduced`+`?motion=reduced` 双路。Axe serious/critical 每步=0。**矩阵抓出并修复两个真实 a11y bug**：① studio 骨架与 candidate compare 的 Radix Tabs 触发器 `aria-controls` 悬空（无 TabsContent id）——改为整个骨架包进复合 Tabs Root、手摆 TabsList/TabsTrigger、面板 `TabsContent forceMount`（§12 `Tabs` 原语不变；candidate compare 显隐由 Radix data-state 驱动，`effectiveView=hasBodies?view:'unified'`）；② 编排条 390 宽下 min-content≈459px 撑破页面——`flex-wrap` + gallery 轨道 `minmax(0,1fr)`。相邻面回归：`foundation-gallery.spec.ts` 5/5（快照按新 fixture 面重生成）+ `harness-a11y-responsive.spec.ts` 5/5。`bun run typecheck` 绿；`vitest` text-development+gallery 套件 34/34。预存失败说明：全量 vitest 中 14 个 `spatial-*` 失败为 react-router `<Router>` 上下文缺失（spatial-surface.tsx `useSearchParams` 无 Router 包裹），与本任务文件零交集，归属 spatial lane，不在本任务处理。
- [x] 8.2 使用100万中文字、约500章、数千实体fixture验证active-document/virtualized rendering、autosave和Lens交互；Acceptance:无per-keystroke全书序列化/全图重绘；Validation:browser performance test和heap/disposal evidence。
  - **Evidence (2026-09-05)**: 百万字级前后缀下 `computeIncrementalPatch` 只序列化单字区间（JSON <80B）；500 章/3000 实体 `windowedRows` ≤56 DOM。Validation：`text-development-performance.test.ts` 2/2。heap/disposal 随 8.1 Playwright 矩阵补浏览器证据。**浏览器证据已补 (2026-09-05)**：8.1 e2e "deck switch is DOM-stable and bounded"——posture/deck 连续切换经 JS 属性探针证明 editor slot 不 remount、全页 DOM 节点 <6000、chromium `performance.memory.usedJSHeapSize` 记录且 <512MiB（spec 第 153 行起，矩阵 8/8 pass）。
- [x] 8.3 完成security测试：cross-project/tenant、unsafe refs/URLs/paths、XSS labels、oversize body/patch、credential/raw prompt/provider/private args sentinel与browser direct-owner denial。
  - **Evidence (2026-09-05)**: javascript/file URL、command/prompt body、跨租户 path 一律 fail closed；turn intent 序列化无 raw content/`<script>`；源码扫描无 fetch/WebSocket/credential/direct-owner。Validation：`text-development-security.test.ts` 5/5。
- [x] 8.4 经`scripts/test-evidence/run.ts`运行component/integration/e2e并写入`temp/integration-test-runs/<run-id>/`六件套；失败保留原exit code和脱敏artifacts。
  - **Evidence (2026-09-05，三层齐全)**：component 层 `apps/web/temp/integration-test-runs/20260905163107-57d8b0d2-53c4-40bf-8ca2-410ea66c9290/`（8.x 聚焦 Vitest，summary passed、redaction PASS）；integration 层 `temp/integration-test-runs/20260905203427-f73365ac-724f-45f7-bead-cf9d9e38b777/`（`bun run test:integration`=CGO0 go test conformance+runtime，passed/exit 0，六件套齐全）；e2e 层 `temp/integration-test-runs/20260905203547-7ffa8ac3-578e-438a-a9b0-985f7b605c53/`（`bun run web:e2e` 全量 Playwright，**232 passed / 11 skipped / 0 failed**、exit 0，六件套+digest+receipt 齐全；skip 项为 env 门控 perf/real-host spec，如实 skip）。三层均经 `scripts/test-evidence/run.ts` 落盘，含 redaction 扫描与 source snapshot 绑定。
- [ ] 8.5 在真实Auctra/Pi/Runtime/Ordo服务与用户级key下完成显式dev preview canary；Acceptance:无mock fallback，所有capability maturity逐项记录。
- [ ] 8.6 完成连续60分钟Novel authoring proof，覆盖edit/selection/candidate/checkpoint/restart/team simulation/mobile review并证明零丢稿。
- [ ] 8.7 运行`openspec validate --all --strict --no-interactive && bun run typecheck && bun test && bun run build && bun run web:test && bun run test:contract && bun run test:integration && bun run web:e2e && bun run check:i18n`，执行capability off/cleanup/old `/agent` rollback rehearsal并完成closeout。
  - **Partial gate evidence (2026-09-06，仍不勾——8.5/8.6 外部门 + e2e 抖动)**：本地门当日实跑——`openspec validate --all --strict` 75/75 ✓、`bun run typecheck` ✓、`bun test` **1090/0** ✓、`bun run build` ✓、`bun run web:test` **252 files/2088** ✓、`bun run test:contract` **659/0** ✓、`bun run test:integration` evidence run passed ✓、`bun run check:i18n` OK ✓；`bun run web:e2e` 两轮 234-235 passed / 2-3 failed，失败集逐轮不同（agent-pi 667 单独复跑即过；daily-ops-real-backend/workbench 430x932 轮换出现）=共享主机高并发（45 进程+并行会话在途）资源竞态抖动，另 project-continuity-desktop.spec 稳定失败属并行会话在途面（6/29），均非本 change 面回归；全绿 e2e 需在低负载窗口或专用 host 复跑。8.7 勾选仍被 8.5（真实服务 canary）/8.6（60 分钟 proof）外部门与 closeout 条款阻塞，不越依赖。
