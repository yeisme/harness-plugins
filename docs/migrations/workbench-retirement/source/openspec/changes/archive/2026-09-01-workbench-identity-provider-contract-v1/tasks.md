# Workbench Identity Provider HTTP 合同实施任务

## 1. Slice 1：消费端 HTTP Provider + runtime 接线（本切片）

- [x] 1.1 定义 provider HTTP 线上合同（`workbench.identity.provider.v1alpha1`）：五个 REST 端点、closed `providerRequest`/envelope 形状、错误映射表、loopback-or-HTTPS URL policy、bounded response、脱敏要求，写入 design.md。；Owner: workbench owner；Dependencies: R1 `workbench.identity.v0.1` consumer 合同；Paths: `openspec/changes/workbench-identity-provider-contract-v1/design.md`；Scope: 合同先行，作为 identity-platform 侧（slice 2）实现输入；Acceptance: 合同与 `service/internal/identity/service.go` 的 Provider 五方法及 providerError 语义逐项对齐；Verification: `openspec validate workbench-identity-provider-contract-v1 --strict`；Expected: strict valid；Failure recheck: 合同与 domain 合同漂移时以 `service.go` 为准修订 design。
- [x] 1.2 实现 `service/internal/identity/http_provider.go`：`Provider` 五方法、`NewHTTPProvider` fail-closed 构造（URL 缺失/policy 拒绝 → ErrNeedsContract）、closed 解码（DisallowUnknownFields/单值/1 MiB 上限）、边界计数、contractVersion 必须等于 `workbench.identity.v0.1`、401/403/5xx/网络错误映射；Owner: workbench owner；Dependencies: 1.1；Paths: `service/internal/identity/http_provider.go`（新增文件，不改既有文件的既有函数）；Scope: 传输形状 + 边界校验，domain 校验复用 Service 既有验证器；Acceptance: `var _ Provider = (*HTTPProvider)(nil)` 编译断言成立，错误语义与 `providerError` 对齐；Verification: `gofmt -l` + `CGO_ENABLED=0 go vet ./service/internal/identity/`；Expected: 干净通过；Failure recheck: 任何错误映射偏差以 `providerError` 语义为准修正。
- [x] 1.3 交付 `http_provider_test.go` 负矩阵与正路径：httptest 模拟合同端点；URL policy 负矩阵（缺 URL/私网 http/userinfo/query/非 http scheme；HTTPS origin 与 loopback 正路径）；CheckReady 正路径 + wrong version/unknown field/非 JSON/不可达；Session 端到端（含 Service.validateSession principal 回显）；ListTenants 负矩阵（wrong version/unknown field/invalid tenant/duplicate/invalid page token/trailing JSON/503/超 256 条）；ListMembers（cross-tenant member/403/401）；AllowedActions（tenant 漂移）；超大响应；redirect 拒绝；Owner: workbench owner；Dependencies: 1.2；Paths: `service/internal/identity/http_provider_test.go`；Scope: 只测新文件行为，不改既有测试；Acceptance: 全部用例通过且断言精确错误哨兵（errors.Is）；Verification: `CGO_ENABLED=0 go test ./service/internal/identity/ -count=1`；Expected: ok（含既有 44+ 用例不回归）；Failure recheck: fixture 漂移时直接序列化 `projectPrincipal` 输出而非手写 JSON。
- [x] 1.4 runtime 最小接线（default-off）：`Config.IdentityProviderURL` 字段、`LoadConfig` managed 分支读 `WORKBENCH_IDENTITY_PROVIDER_URL`（local 分支恒空）、`Validate` 对称校验（local 禁止携带；managed 携带时跑 URL policy fail-fast）、`New()` 仅 managed + URL 非空时构造 provider 注入 `identity.NewService`，为空保持 nil；Owner: workbench owner；Dependencies: 1.2；Paths: `service/internal/runtime/runtime.go`（只做上述最小接线）；Scope: 不改任何既有函数的既有行为；Acceptance: URL 为空时 `New()` 行为与现状一致（provider=nil → needs_contract fail-closed），runtime 包全量测试不回归；Verification: `CGO_ENABLED=0 go build ./service/cmd/workbenchd` + `CGO_ENABLED=0 go test ./service/internal/runtime/ -count=1`；Expected: build exit 0、runtime 包 ok；Failure recheck: 若并行在途编辑导致编译失败，记录于报告不越租约修复。
- [x] 1.5 OpenSpec change strict valid 且切片报告落盘；Owner: workbench owner；Dependencies: 1.1-1.4；Paths: `openspec/changes/workbench-identity-provider-contract-v1/**`；Scope: proposal/design/tasks/specs/details 齐备；Acceptance: `openspec validate workbench-identity-provider-contract-v1 --strict` 通过；Verification: 同左；Expected: valid；Failure recheck: spec delta 与实现行为不一致时以实现实际行为修订 spec。

## 2. Slice 2：identity-platform 侧端点（cross-repo owner）

- [x] 2.1 在 `backend-server/identity-platform` 仓按本 change design.md 实现 GET ready + 四个 POST 端点：接受 `X-Workbench-Provider-Contract` 头与 closed providerRequest，返回 R1 domain 形状；Owner: identity-platform owner（cross-repo change）；Dependencies: slice 1 合同冻结（1.1）；Paths: `backend-server/identity-platform/**`（该仓当前有 34 个未提交在制品文件，开工前须先与 owner 协调在制品边界）；Scope: 端点、DB 读取、policy 计算、审计归该仓；Acceptance: 对 workbench 消费端 httptest 矩阵镜像的服务端集成测试通过；Verification: 该仓 change 的验证命令 + 与本仓消费端的跨仓联测；Expected: 五端点对本合同双向兼容；Failure recheck: 任何合同歧义回本 change 修订 design 并在消费端补负矩阵用例。
  - identity-platform `internal/api/provider.go`：GET ready + session/tenants/members/actions 四 POST（transport 头强校验、closed 请求体、session 撤销/存在 fence、membership expected-version fence、scope-based 授权策略、authorityKey 签名钥指纹）；tenant.Member 增 Ref；provider_test.go 5 测试；全仓 go test PASS。
- [x] 2.2 消费端真实联调（staging 或 disposable compose）：workbenchd 以 `WORKBENCH_IDENTITY_PROVIDER_URL` 指向真实 identity-platformd，Readiness 晋级 `ready`，project 授权 mutation 不再 `identity_unavailable`；Owner: identity-platform owner + workbench owner；Dependencies: 2.1；Paths: 联测 evidence（`temp/integration-test-runs/`）；Scope: 真实栈，不用 fixture；Acceptance: Readiness `identity_provider` check 为 ready 且无 contract mismatch；Verification: `task daily-ops-integration:real-stack:scenario` 前置链路（或等价真实栈探针）；Expected: provider 链路真实可用；Failure recheck: 漂移/不可达保持 fail-closed，保留脱敏 evidence。
  - temp/real-stack-provider-scenario.sh（fresh identity-platformd + disposable PG + eikona canary + workbenchd WORKBENCH_IDENTITY_PROVIDER_URL + mint --scopes/--membership-version）：真实授权链全通（catalog owner 晋级、workitem create 真实 PG 行、eikona delegation→owner receipt、SSE/receipts/脱敏）。结构性修复：workitems `_` 占位符折叠、Task 权威链 0041 迁移（session/scopes/auth-window 四列+执行期 principal 恢复）、场景 policy 种子。system evidence 20260830090100-39c102fc passed/redaction passed。

## 3. Slice 3：R3 8.2 解锁（gated on slice 2）

- [x] 3.1 `task daily-ops-integration:real-stack:scenario` 转绿：submit 授权链（workitems IdentityAuthorizer → identity Service.AllowedActions → HTTPProvider → 真实 provider）端到端通过，8.2 结构性阻塞解除；Owner: workbench owner；Dependencies: 2.2；Paths: `service/test/dailyopsintegration/**`（evidence only）、`openspec/changes/workbench-daily-operations-r3-gates/tasks.md` 8.2 勾选由该 change owner 执行；Scope: 只验证，不改 runner（evidence runner 租约在外部代理）；Acceptance: scenario exit 0、submit 返回非 identity_unavailable；Verification: `task daily-ops-integration:real-stack:scenario`；Expected: PASS；Failure recheck: 任何漂移保持 fail-closed，8.2 保持 pending。
  - TestRealIdentityOwnerDailyOpsSystem PASS（32.9s）；workbench 受影响包（app/repository/workitems/identity/transport/http）全绿；8.2 结构性阻塞解除。

## Slice 1 收口（主代理统一验收 2026-08-28）

- 1.1-1.5 勾选依据（主代理独立复跑，非仅代理自报）：`openspec validate workbench-identity-provider-contract-v1 --strict` valid；`CGO_ENABLED=0 go test ./service/internal/identity/ -count=1` ok（含新负矩阵，既有用例零回归）；`CGO_ENABLED=0 go test ./service/internal/runtime/ -count=1` ok（default-off 等价性）；`CGO_ENABLED=0 go build ./service/cmd/workbenchd` exit 0；runtime.go diff 主代理逐行复核（URL 空 → `NewService(profile, auth, nil)` 与现状一致；URL 配置但 policy 拒绝 → startup fail-fast 不回退 nil）。切片报告：`details/slice-1-executor-report.md`。
- Slice 2/3 保持未勾：identity-platform 侧端点属该仓 owner 的 cross-repo change（该仓 34 个在制品文件须先协调边界）；8.2 解锁 gated on 2.2 真实联调。
