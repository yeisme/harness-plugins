## 1. 产品与合同冻结

- [x] 1.1 完成 Show Control Room 产品文档、owner fit 与 Scaena no-expansion 边界。
- [x] 1.2 冻结 Show Home、Episode Board、Asset Wall、Review Inbox、Run/Evidence、Delivery 信息架构。
- [x] 1.3 定义 ShowWorkspaceProjectionV1、ShowActionDescriptorV1、ShowReceiptV1 和 DSH handoff schema。Evidence：Go closed 合同 `service/internal/showcontrol/projection.go`（context/stages/owners/entities/blockers/actions/receipts 全 closed struct + strict codec：未知字段、越界计数、非法 opaque ref/枚举/文本整体拒绝 ErrInvalidShowProjection；上限 blockers≤3、entities≤64、summary≤280 字符），SDK 镜像 `packages/task-sdk/src/show-control-room-models.ts`（typed models + `parseShowWorkspaceProjection` closed codec，contractVersion 字面量 `workbench.show_control_room.v1alpha1` 两端一致）；DSH handoff schema 即既有 `workbench.harness.dsh_bridge.v1alpha1`（`service/internal/showcontrol/handoff.go` + SDK harness validate.ts，本轮不变）。测试：`service/internal/showcontrol/projection_test.go`（round trip/未知字段/unsafe ref/枚举/越界/nil 归一）、`packages/task-sdk/test/show-control-room-projection.test.ts`。
- [x] 1.4 将新 Operation 注册进统一 registry，并生成 SDK/HTTP/gRPC/JSON-RPC parity 资产。Evidence：`service/internal/registry/showcontrol.go` 注册 `show.create_proposal`/`show.create_execute`（四 projection、permission/cost/idempotency gate；proposal authority 关闭时 ModeUnavailable fail-closed），runtime 接线 `service/internal/runtime/runtime.go`；parity 资产 `service/test/conformance/show_create_proposal_parity_test.go`（HTTP/gRPC/JSON-RPC catalog+gate+closed schema 一致）与 `packages/task-sdk/test/show-control-room-client.test.ts`（SDK typed client 合同）。integration 证据：`temp/integration-test-runs/20260827091115-9f4cf79a-c241-4a88-8b74-b49a0afe429e/`（passed）。

## 2. Service 与 owner consumer

- [x] 2.1 实现 Show workspace query composer，只保存 safe refs 和 readiness。Evidence：composer `service/internal/showcontrol/composer.go`（`ComposeShowWorkspaceProjection` 只读合成：context 只含 opaque refs + server 重验 revision、8 阶段 unknown 轨不推断进度、owner-by-owner readiness 确定性折叠；entities/blockers/actions/receipts 在 owner 合同落地前保持空，不伪造）；registry 只读 Operation `show.workspace_projection.get`（`service/internal/registry/showcontrol.go`：Mutation=false 无 permission/cost/idempotency mutation gate、四 projection、closed schema `{showRef,episodeRef}`；handler 持久化只含 task artifacts 的 safe refs + readiness 摘要与 `show-projection:` receipt ref）；ContextRevisionSource 生产绑定 `NewOwnerContextRevisionSource`/`NewOwnerReadinessProjection`（owners.Service 授权裁剪只读切面，资源不可见/版本形状非法/读取失败一律 fail closed）接线于 `service/internal/runtime/runtime.go`。测试：`service/internal/showcontrol/composer_test.go`、`service/internal/registry/showcontrol_test.go`、三面 parity `service/test/conformance/show_workspace_projection_parity_test.go`（catalog 元数据+closed schema 拒绝+未绑定 fail-closed 终态在 HTTP/gRPC/JSON-RPC 一致）；SDK `getShowWorkspaceProjection`（`packages/task-sdk/src/show-control-room-client.ts`）。integration 证据：`temp/integration-test-runs/20260827103326-ecf31901-59f2-42f6-8991-df84cfaaf9de/`（passed）；component 证据（含 `/show-control-room` 路由接线 `show-control-room-route.tsx` + `apps/web/src/app.tsx` + `navigation/model.ts` 注册）：`temp/integration-test-runs/20260827103242-9b281f82-941f-49e0-a03d-f138563126e6/`（passed）。本轮复查补充：bound composer 真实 roundtrip 暴露出 local profile 控制面 task 不携带 TenantRef（空值无法过投影 context 自检）的真实缺陷——handler 已把空 TenantRef 归一为 `tenant:local`（对齐 `boards.LocalTenantRef` 先例；managed profile 不受影响），见 `service/internal/registry/showcontrol.go`；新增三面 roundtrip 测试 `service/test/conformance/show_workspace_projection_roundtrip_test.go`（bound composer 经 HTTP/gRPC/JSON-RPC succeeded、readiness 诚实折叠 degraded、artifacts/receipt 只含 safe refs 与 `show-projection:` digest ref、同 key 幂等 replay）与 SDK facade 真实 server roundtrip `packages/task-sdk/test/show-control-room-server-roundtrip.test.ts`（spawn 真实 workbenchd，production runtime 接线下 projection 成功消费）；integration 证据：`temp/integration-test-runs/20260827170518-9517f72b-8744-46b7-a31f-31a2c076a925/`（passed）。
- [x] 2.2 实现 Create Show proposal，通过 Proposal authority 与 TaskService 提交。Evidence：handler `service/internal/registry/showcontrol.go`（closed 校验 → proposal 注册，不调用 owner mutation）；registrar `service/internal/proposalauthority/bridge/show_registrar.go`（canonical proposal，TargetOperation 固定 `show.create_execute`，accept 仍经 orbit.proposal.accept → TaskService）；绑定 `service/internal/proposalauthority/wiring/wiring.go`；测试 `service/internal/registry/showcontrol_test.go`、`service/internal/proposalauthority/bridge/show_registrar_test.go`。
- [x] 2.3 实现 owner segment strict validation、redaction、size limit 和 fail-closed fallback。Evidence：`service/internal/showcontrol/segment.go`（`ParseShowOwnerSegment` strict codec：2KB 大小上限 → forbidden 字段名扫描（credential/token/raw_prompt/provider_payload/private_path）→ closed 字段集合（ownerRef/readiness/summary）→ 域级封闭校验，任何偏离整体拒绝 ErrInvalidShowSegment；`ShowOwnerSegmentFallback` redacted contract_mismatch 替代段——ownerRef 仅形状安全时保留，summary 为常量低基数文本；`SanitizeShowOwnerSegments` composer ingress 边界：非法段折叠 fallback 不放行不丢弃，段数超 16 上限整体 fail closed）；composer 接线 `service/internal/showcontrol/composer.go`（合成前过 SanitizeShowOwnerSegments；owner catalog 适配器从「静默清空越界 summary」改为 redacted contract_mismatch fallback）。测试：`service/internal/showcontrol/segment_test.go`（round trip/未知字段/credential 字段名/URL ref/枚举越界/超限 summary/大小上限/fallback 脱敏/composer 折叠不声称 available）。integration 证据：`temp/integration-test-runs/20260827160727-892e12c0-83e7-4c7f-85ee-5d922e92c9ca/`（passed）。
- [x] 2.4 实现 review action、idempotency、unknown/partial/reconcile 与 owner receipt。Evidence：领域层 `service/internal/showcontrol/review.go`（`ValidateShowReviewActionInput` closed 校验——descriptor/action/target refs + accept|reject|repair 枚举，repairSummary 仅 repair 可携带且有界脱敏；`CheckShowReviewDescriptor` 提交时重验：descriptor 缺失/读取失败/形状漂移/actionRef 或 targetVersion 漂移/已过期一律 ErrShowReviewDescriptorStale 拒绝提交并要求重新读取 projection；`SettleShowReviewReceipt` 把 owner receipt 映射为 decided|reconcile_required，unknown/partial/reconcile_required 禁止自动重试，回执偏离 closed 合同 fail closed）；registry Operation `show.review_action.submit`（`service/internal/registry/showcontrol.go`：mutation 过 permission/cost/idempotency 四重 gate + 四 transport 投影 + closed schema additionalProperties:false；handler 在 descriptor source/action port 未绑定时 needs_contract fail-closed，reconcile 语义映射 StatusUnknownAccept/StatusPartial，artifacts 携带 idempotency identity 与 owner receipt ref）；SDK `submitShowReviewAction`（`packages/task-sdk/src/show-control-room-models.ts` + `show-control-room-client.ts`，unknown_accept/partial 如实透传不自动重试）。测试：`service/internal/showcontrol/review_test.go`、`service/internal/registry/showcontrol_test.go`（review 段）、三面 parity `service/test/conformance/show_review_action_parity_test.go`（catalog 元数据+closed schema 拒绝+统一 gate 幂等停在 awaiting_permission）、`packages/task-sdk/test/show-control-room-client.test.ts`（review 段）。integration 证据：`temp/integration-test-runs/20260827160727-892e12c0-83e7-4c7f-85ee-5d922e92c9ca/`（passed）。本轮复查补充（runtime 接线）：owner 合同审计结论——Scaena connector 只有只读 snapshot 投影（无 ShowActionDescriptorV1 descriptor 合同、无 review decision typed action 合同），Auctra 在 owners catalog 中保持 needs_contract；因此 `service/internal/runtime/runtime.go` 生产绑定显式 fail-closed adapter `service/internal/showcontrol/review_binding.go`（`NewNeedsContractReviewDescriptorSource`/`NewNeedsContractReviewActionPort`：错误包装 ErrShowReviewActionUnavailable、诊断文本归一为低基数脱敏常量，不伪造 receipt），handler 据此把 needs_contract 与 descriptor stale（「re-read the projection」）/owner 执行失败三种语义显式区分。测试：`service/internal/showcontrol/review_binding_test.go`、registry `TestShowReviewActionBoundFailClosedAdaptersStayNeedsContract`、conformance `TestShowReviewActionBoundFailClosedAdaptersAcrossGate`（统一 permission/cost gate 放行后 handler needs_contract 终态，不落结算 artifacts/receipt）与真实 server roundtrip（`packages/task-sdk/test/show-control-room-server-roundtrip.test.ts`，gate → needs_contract）。integration 证据：`temp/integration-test-runs/20260827170518-9517f72b-8744-46b7-a31f-31a2c076a925/`（passed）。
- [x] 2.5 实现 DSH context handoff validator 和 context revision gate。Evidence：`service/internal/showcontrol/handoff.go`（dsh_bridge.v1alpha1 closed 校验 + `CheckHandoffContextRevision`：source 未绑定/读取失败/漂移/缺 revision 一律 reconcile_required），decision-time 第二道经 BasisRef（mapSafety 漂移 fail closed）；测试 `service/internal/showcontrol/handoff_test.go`。web connected 层证据：`apps/web/test/show-control-room-create-show-entry-connected.test.tsx`，component 证据 `temp/integration-test-runs/20260827091457-c702a7b1-fd58-428c-b809-2d205a6fa7a2/`（passed）。

## 3. Web 产品面

- [x] 3.1 实现 Create Show、Show Home 和 Series Bible projection。Evidence：多步向导 `apps/web/src/workbench/show-control-room/create-show-wizard.tsx`（基本信息/格式与篇幅/方向与参考/确认提交四步，收集 genre/audience/language/format/aspectRatio/episodeDurationSec/episodeCountMin~Max/visualDirection/referenceRefs/budgetPosture 全量 closed 字段；本地预校验 fail closed 不触网；确认步如实汇总并声明「不代表 show 已创建」）；connected 接线 `create-show-entry-connected.tsx`（合同 available 才渲染向导，提交仍经 TaskService gate → Proposal authority）；Show Home `show-home-panel.tsx`（每挂载重读 server 投影：8 阶段轨诚实 unknown、owner-by-owner readiness、server-authored 摘要与 projection/receipt refs；composer 未绑定/查询失败渲染 needs_contract/offline fail-closed 面板，不渲染阶段或 owner 内容；SDK 查询结果新增 server-authored summary 字段，客户端只展示不推断）；Series Bible `series-bible-panel.tsx`（Auctra 合同未签约 → 静态 needs_contract fail-closed，不渲染任何 bible 内容）；路由接线 `show-control-room-route.tsx`（showRef + scope 齐备时挂载 Show Home）。component 测试：`apps/web/test/show-control-room-create-show-wizard.test.tsx`、`show-control-room-show-home.test.tsx`、更新 `show-control-room-create-show-entry-connected.test.tsx` 与 `show-control-room-route.test.tsx`；component 证据：`temp/integration-test-runs/20260827160615-2cc183a3-f29d-455c-bf32-b2817425faf1/`（passed，44 测试）。
- [x] 3.2 实现 Episode Board 与 Asset Wall。Evidence：`apps/web/src/workbench/show-control-room/episode-board-panel.tsx`（每挂载重读 show.workspace_projection.get：server-authored 摘要、owner-by-owner readiness 与 projection/receipt refs 真实消费；查询失败/composer 未绑定渲染 needs_contract/offline fail-closed，不渲染实体或 owner 内容；Episode→Scene→Shot 实体投影与 open/compare/review/repair/handoff 动作在 owner entity/descriptor 合同落地前如实空态，不伪造剧集结构、不把未验证 production fact 拼接成成功状态）；`asset-wall-panel.tsx`（Eikona asset projection 合同未签约 → 静态 needs_contract fail-closed，rights/lineage/accepted/binding/reuse count/stale reason 一律不展示，与 Series Bible 同一先例）。component 测试 `apps/web/test/show-control-room-episode-board.test.tsx`（available/needs_contract/offline×2）。路由挂载属 3.4–3.5 并行切片所有权，本轮不动 `show-control-room-route.tsx`。
- [x] 3.3 实现 exception-first Review Inbox、compare 与 repair proposal。Evidence：`apps/web/src/workbench/show-control-room/review-inbox-panel.tsx`——connected `ReviewInboxPanel` 每挂载重读 server 投影，failed/error → needs_contract/offline fail-closed；succeeded 时 exception-first 聚合只来自 server 投影携带的例外事实，当前查询结果不携带例外项（owner review 合同落地前 server 保持为空）→ 诚实空态，不伪造待办、不渲染无功能控件，普通成功状态留在 Show/Episode 投影。`ReviewInboxItemCard` 是 descriptor 驱动纯展示组件（connected 层只传 server descriptor，当前恒空；测试以 fixture descriptor 驱动，对应 design.md Migration 第 1 步批准的 fixture+needs_contract 路线）：展示 category/owner/target@version/expiry 与 risk/cost/rights/owner-authored 确认与候选差异摘要（compare 内容以 server-authored confirmationSummary 为准，不伪造 side-by-side）；accept/reject/repair 经 `submitShowReviewAction` typed action（repair 携带有界脱敏 repairSummary，空摘要禁提交不触网）；presentation state machine new→viewed→confirming→submitting→settled/decision_submitted/reconcile_required/stale/error；unknown_accept/partial 保留 idempotency identity 与 owner receipt ref 并禁止自动重试（reconcile 动作=重新读取投影）；task failed（descriptor 过期/漂移或 owner 合同未签约）→ stale，要求重新读取 projection 并重新确认，不做字符串推断区分。component 测试 `apps/web/test/show-control-room-review-inbox.test.tsx`（11 例）。
- [x] 3.4 实现 Run/Evidence、cost/ETA、receipt/reconcile。Evidence：`apps/web/src/workbench/show-control-room/run-evidence-panel.tsx`——connected `RunEvidencePanel` 每挂载重读 `show.workspace_projection.get`（当前唯一已注册只读合同，经 facade 不直连 owner）；succeeded 只展示投影查询的控制面事实（task/projection/receipt refs + server-authored 摘要），并如实标注该 receipt 是投影查询自身的 transport receipt 而非 owner run receipt；Ordo run 时间线（DAG/attempt/lease/approval/verification/failure class）、Aigora provider job/cost 预估与实际/ETA/cancel、owner receipt 列表与 reconcile 动作的 server 合同均未签约 → 三个子区 needs_contract fail-closed（脱敏 diagnostic），明确 unknown/partial 结算禁止自动重试；composer 未绑定/查询失败渲染 needs_contract/offline 诊断面板，不渲染任何子区内容，不伪造 run、成本或 ETA 数字。路由接线 `show-control-room-route.tsx`（随 Show Home 挂载）。component 测试 `apps/web/test/show-control-room-run-evidence.test.tsx`（succeeded/failed=needs_contract/offline 三态，含「不伪造 ETA 数字」断言）；component 证据：`temp/integration-test-runs/20260827173946-3ae5a2fe-5e93-42c0-89b0-8c8833b9f790/`（passed，18 测试，exit 0）。
- [x] 3.5 实现 Delivery、Next Episode reuse 和 owner deep links。Evidence：`apps/web/src/workbench/show-control-room/delivery-panel.tsx`——connected `DeliveryPanel` 每挂载重读 `show.workspace_projection.get`；succeeded 展示投影查询控制面事实，Scaena delivery readiness（frozen consumer，明确不根据文件存在或 HTTP 2xx 推断已交付）、输出包/字幕/音轨/NLE handoff/回滚说明、Next Episode 跨集 accepted refs reuse、owner deep links（必须由 owner 合同授权签发，不拼接不渲染任何外部链接）在合同签约前全部 needs_contract fail-closed；failed/error → needs_contract/offline 诊断，不渲染子区。component 测试 `apps/web/test/show-control-room-delivery.test.tsx`（三态 + 「面板内零 <a> 链接」断言）；component 证据：`temp/integration-test-runs/20260827173946-3ae5a2fe-5e93-42c0-89b0-8c8833b9f790/`（passed，exit 0）。
- [x] 3.6 完成 desktop/tablet/mobile、keyboard、focus、screen reader 与 reduced motion。Evidence：`apps/web/src/styles.css` 追加 `.show-control-room` scoped 块（主区域改 grid+overflow:auto 修复多面板被 `.workbench-main{overflow:hidden}` 裁切；长 opaque ref `overflow-wrap:anywhere` 防横向溢出；1023/767 断点间距与字号；`:focus-visible` 可见焦点环；`prefers-reduced-motion` 全域降级）；`show-control-room-route.tsx` 主区域 `tabIndex={0}`（axe scrollable-region-focusable：小断点可滚动容器必须可键盘聚焦）；面板均为语义 section + aria-label + role=status，状态一律可见文本（非仅颜色）。e2e 验证见 5.5（三断点无横向溢出 + axe critical/serious 零违例 + 纯键盘向导推进 + reduced-motion）。

## 4. Evidence

- [x] 4.1 添加 unit/codec/service/transport parity 测试。Evidence（覆盖当前已注册的 show 操作面 create_proposal/create_execute/workspace_projection.get/review_action.submit）：unit `service/internal/showcontrol/{projection,segment,review,review_binding,handoff,composer,proposal}_test.go`；codec 为 projection closed codec 测试 + SDK `packages/task-sdk/test/show-control-room-projection.test.ts`；service `service/internal/registry/showcontrol_test.go`；transport parity `service/test/conformance/show_{create_proposal,review_action,workspace_projection}_parity_test.go` + 本轮新增 `show_workspace_projection_roundtrip_test.go`（bound composer 真实 roundtrip 与 fail-closed review 全 gate 链路）；SDK facade 真实 server roundtrip `packages/task-sdk/test/show-control-room-server-roundtrip.test.ts`。后续 3.2–3.5 新增操作面时 parity 测试须随同扩展。integration 证据：`temp/integration-test-runs/20260827170518-9517f72b-8744-46b7-a31f-31a2c076a925/`（passed，exit 0）。
- [x] 4.2 添加 component tests 覆盖 available/needs_contract/stale/unknown/partial/offline。Evidence：`apps/web/test/show-control-room-episode-board.test.tsx`（available=投影 succeeded / needs_contract=task failed / offline=transport 拒绝与 result error 分支）与 `apps/web/test/show-control-room-review-inbox.test.tsx`（connected 层 available/needs_contract/offline；descriptor 驱动卡片层 accept→settled、reject→decision_submitted gate 链、repair 摘要必填+脱敏提交、stale=server 拒绝结算要求重读重确认、unknown=unknown_accept→reconcile_required 保留 idempotency identity 不自动重试、partial→reconcile_required、offline/needs_contract submit error 如实呈现不伪造成功）。存量 44 例 show-control-room 组件测试同步回归通过。component 证据：`temp/integration-test-runs/20260827173752-789935bb-1f6d-40f8-b4a9-ed0cf3b1e61b/`（passed，20 测试，exit 0）。
- [x] 4.3 添加 DSH -> Workbench -> owner receipt E2E。Evidence（当前合同阶段的诚实可达链）：`apps/web/e2e/show-control-room-dsh-review-receipt.spec.ts`（2 测试，真实 app + 统一 Task 控制面 page.route mock，mock 形状镜像 server handler 真实产出，不直连 owner）——① DSH handoff 入口（closed 参数齐全）→ Create Show 真实四步向导 → typed proposal 提交 → 控制面事实如实呈现（taskId/awaiting_permission/「等待 1 个 gate」/「不代表 show 已创建」），并断言 entrySource=dsh 与展平 handoff* refs 原样随提交传给 server 重验（浏览器不做定位决策）；② show.workspace_projection.get 成功链：server-authored 摘要（readiness=degraded，不推断为 available）、projection ref、transport receipt ref 在 Show Home/Run & Evidence/Delivery/Review Inbox footer 如实展示，owner segments 只以 safe ref + readiness 摘要呈现，Review Inbox 对「投影不携带 action descriptor」呈现诚实空态且不渲染无功能 review 控件。owner review receipt 结算（unknown_accept/partial → reconcile_required → authoritative receipt）在真实 app 的可达层即上述诚实空态；结算状态机由 `apps/web/test/show-control-room-review-inbox.test.tsx`（fixture descriptor 驱动真实 ReviewInboxItemCard，11 例）与 `service/test/conformance/show_review_action_*_parity_test.go`（三面 gate/终态 parity）覆盖，真实 server 链路由 `packages/task-sdk/test/show-control-room-server-roundtrip.test.ts`（spawn 真实 workbenchd，gate → needs_contract fail-closed）覆盖；descriptor 随投影下发与真实 owner receipt e2e 待 owner review 合同签约后扩展。e2e 证据：`temp/integration-test-runs/20260828110200-6bf05ca3-3222-4760-bfb3-583975e29e6c/`（passed，exit 0，redaction passed，2 断点截图 artifacts）。
- [x] 4.4 添加两集复用 scenario 和脱敏 product-study export。Evidence：`apps/web/src/workbench/show-control-room/product-study.ts`——`ShowStudyEventV1` closed 事件模型（task_start/task_end/stage_transition/blocker/review_outcome/reuse_refs/export_handoff；只含 safe refs、低基数 category token、计数与时间戳，模型无自由文本字段 → by construction 脱敏；未知字段/不安全 ref/越界计数整体 fail closed contract_mismatch）；`computeCrossEpisodeReuse` 只统计「第一集已 accepted refs 被第二集实际使用」的交集（数量 + owner 类型去重，候选/未验证 refs 不计入，重复复用只计一次）；`buildShowStudyExport` 聚合 task start/end、stage transitions、blocker categories、review outcomes、reuse refs count 与 export/handoff outcome，并导出首次可审结果时间、完整路径标志与 redaction 声明，跨 show 聚合/空事件流/reuse_refs 缺计数一律拒绝。测试 `apps/web/test/show-control-room-product-study.test.ts`：两集复用 scenario（Episode 1 accepted 角色/风格/声音/规则 refs，Episode 2 复用 3 个 + 1 个未 accepted 候选 → count=3、owners=[eikona,sonora]）+ export 序列化递归键白名单扫描（无内容正文/prompt/payload/credential/path 字段）+ fail-closed 拒绝分支。成熟度诚实：本项为 fixture 级证据，非 observed-user evidence，不宣称真实评分目标达成。component 证据：`temp/integration-test-runs/20260827173752-789935bb-1f6d-40f8-b4a9-ed0cf3b1e61b/`（passed，exit 0）。
- [x] 4.5 为 integration/component/e2e 写入 temp/integration-test-runs/<run-id>/ 六件套。Evidence：统一走 `scripts/test-evidence/run.ts`（summary/command/stdout/stderr/env/artifacts + digest/receipt/redaction，脱敏开启）。本轮新增：component run `temp/integration-test-runs/20260827173946-3ae5a2fe-5e93-42c0-89b0-8c8833b9f790/`（passed，18 测试：run-evidence/delivery/route/show-home，exit 0）；e2e run `temp/integration-test-runs/20260827174041-bdd6002a-05db-42f1-825f-8148f1815135/`（passed，9 测试，exit 0，artifacts 含 1440/768/390 三断点脱敏截图 PNG）。既有 integration run（task 4.1 `20260827170518-…`、task 4.2/4.4 component run `20260827173752-…`）保持有效。

## 5. Verification

- [x] 5.1 openspec validate workbench-ai-drama-show-control-room-v1 --strict --no-interactive。
- [x] 5.2 bun run typecheck && bun test && bun run build。Evidence：2026-08-27 本轮全部通过（typecheck 双 tsc 无错误；bun test 721 pass / 0 fail；build 产出 dist/ Go 二进制，exit 0）；另 apps/web 全量 vitest 145 文件 1169 测试通过。
- [x] 5.3 bun run test:contract && bun run test:integration。Evidence：test:contract 429 pass / 0 fail；test:integration 写入 `temp/integration-test-runs/20260827103326-ecf31901-59f2-42f6-8991-df84cfaaf9de/`（status=passed，exit 0）。
- [x] 5.4 CGO_ENABLED=0 go test ./service/... 。Evidence：2026-08-27 本轮全量通过（exit 0，无 FAIL），含新增 showcontrol projection/composer、registry query handler 与 conformance 三面 parity。
- [x] 5.5 focused Playwright、a11y 与跨入口恢复测试。Evidence：`apps/web/e2e/show-control-room-a11y-responsive.spec.ts`（9 测试全过）：三断点 1440×960/768×1024/390×844 responsive baseline（needs_contract 可见文本、无横向溢出、axe critical/serious 零违例、断点截图写入证据 artifacts）；纯键盘路径（合同 available mock 下向导「下一步」focus+Enter 推进）；reduced-motion baseline；跨入口恢复（agent 缺 scope→missing_scope fail-closed、dsh 缺 resourceRef→contract_mismatch 整体拒绝、非法 showRef→contract_mismatch 不渲染面板）；恢复语义（reload 后三面板重新经 server 查询投影，SubmitTask 计数 ≥2 倍增长，不依赖缓存状态）。统一 Task 控制面 REST 调用以 page.route mock 驱动确定性场景，不直连 owner。e2e 证据：`temp/integration-test-runs/20260827174041-bdd6002a-05db-42f1-825f-8148f1815135/`（passed，exit 0）；失败截图/trace 属本地诊断产物（temp/playwright-results/），不提交。
