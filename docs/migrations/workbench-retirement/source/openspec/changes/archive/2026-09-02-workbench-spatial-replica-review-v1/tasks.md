## 0. 基线、ownership 与设计冻结

- [x] 0.1 记录实施开始时的 dirty-worktree、现有 Agent/Spatial/Show Control changes、依赖版本与 path lease；指定一个 tracked-file writer 串行拥有 `api/**`、`service/internal/spatialreplica/**`、`packages/task-sdk/src/spatial-replica-*` 和 `apps/web/src/workbench/agent/spatial/replica-review/**`，不得覆盖并行用户修改。验收：基线清单能区分 introduced/pre-existing/concurrent changes；命令：`git status --short`、`git diff --name-only`；失败复查：发现重叠 lease 时停止写入并重新划界。 **Evidence (2026-09-01)**: `details/01-baseline-ownership.md`——develop@`81cc221` 快照 320 entries（152 M/19 D/149 ??，tracked diff 171），introduced/pre-existing/concurrent 三类划分 + 12 个 active change 快照；依赖基线（react 19.1.1、@tanstack/react-virtual ^3.14.10、pixi ^8.13.2、`three` 未安装）；四路径族各自串行单写，`api/**` 指定合同层会话（wave 1.x）为唯一 tracked-file writer，agent.proto/spatial.proto 当前属并行修改、只允许 append。
- [x] 0.2 冻结 `workbench.spatial_replica_workspace_projection.v1alpha1`、五个 Pane type、owner segment、PTS/timebase、coordinate/scale、action/receipt、error/state enum 与 proto field number；按 additive experimental stable-surface 分类记录兼容与 rollback。验收：无旧 contract/Pane/route 改义或删除；命令：`openspec validate workbench-spatial-replica-review-v1 --strict`；失败复查：任何 rename/retype/repurpose 必须拆独立 evolutionary change。 **Evidence (2026-09-01)**: `details/02-contract-freeze.md`——冻结新 proto 包 `workbench/spatialreplica/v1alpha1/spatialreplica.proto`（5 rpc）、19 个 enum 值号、~30 个 message 字段号（top-level projection 1–19、action wrapper 1–13 内嵌共享 `ActionDescriptorV1` 零重定义、watch envelope 对齐既有 `resync_required`/cursor 惯例）、五个 Pane closed params 与 preferred width、12 个 error code + transport 映射、additive/experimental/default-off/rollback 分类；`openspec validate --strict` PASS，本波未改 `api/**`/源码。
- [x] 0.3 将 `docs/ui/spatial-replica-review-workspace.md` 的 wireframe、component tree、control inventory、state matrix、breakpoint、a11y、screenshot 与 performance gate 作为 UI 实现前置输入，并建立 proposal/design/spec/task 双向链接。验收：每个 deliver-now capability 能映射到 UI region、contract 和测试；命令：`git diff --check -- docs/product docs/interfaces docs/ui openspec/changes/workbench-spatial-replica-review-v1`；失败复查：缺少交互或状态时先补设计，不临场创建第二套模式。 **Evidence (2026-09-01)**: `docs/ui/spatial-replica-review-workspace.md`（§1–§17 既有冻结复核）+ 新增 §18 capability→UI region→contract→测试 映射表（11 个 deliver-now capability + 横切行）、§19 per-state 口径、§12 补 `error` 与 `unsupported_version` 行（17 行 = responsive-a11y 16 必需状态 + owner-panels unsupported_version）、header 建立 proposal/design/tasks/details/specs 双向链接（proposal/design 侧反向链接已加）；`git diff --check` 相关路径 clean。
- [x] 0.4 冻结 server-owned capability truth table：projection read、media preview、3D viewport、inquiry、Anatomia correction、Scaena stage mutation、review/freeze、Auctra Pane、Scaena Pane、receipt stream 均独立 default-off，且 exact-principal scope 不得由 URL/localStorage/build flag 提权。验收：每项具有 enabled/disabled/needs_contract/offline/revoked 行为与 rollback；命令：`CGO_ENABLED=0 go test ./service/internal/config ./service/internal/runtime -count=1`；失败复查：任一 browser-only enable path 视为 fail-open。 **Evidence (2026-09-01)**: `details/04-capability-truth-table.md`——10 行 truth table（capability id + env flag + prereq 链 + enabled/disabled/needs_contract/offline/revoked 行为 + rollback），enable = server env flag（`true|false|空` 非法即 startup fail-closed，对齐 `spatial.ResolveSpatialCanvasCapabilities` 模式）AND exact-principal cohort `WORKBENCH_SPATIAL_REPLICA_COHORT`，无 browser-only enable path；基线命令于 `service/` 执行 `ok internal/config 0.005s`/`ok internal/runtime 48.430s`（capability 行为实现测试属 task 2.8）。
- [x] 0.5 冻结 P1 local fixture 与依赖预算：单 shot、1–2 actors、browser-budgeted proxies/camera、最多 10 个 overlay/track groups；只批准 lazy `three` core behind `ReplicaViewportAdapter`，暂不引入 R3F/Drei/第二 scene store。验收：记录现有 Web bundle baseline 和 spatial chunk budget；命令：`bun run --cwd apps/web build`；失败复查：若 core adapter 不能满足需求，先回写 design 并给出 bundle/API evidence，不直接扩依赖。 **Evidence (2026-09-01)**: `details/05-p1-fixture-dependency-budget.md`——P1 fixture（1 shot 含 cut+gap VFR、2 actors（16-joint + root）、8-keyframe camera、≤24/64 primitives、≤10 overlay/track groups）与数值预算表；`three` 核实未安装（package.json/bun.lock 均无）→ 本波不安装，task 5.1 引入 core+包内 GLTFLoader、禁 R3F/Drei/第二 scene store，`spatial-replica-3d` lazy chunk gzip ≤250 KiB；bundle 基线已录（主入口 gzip 463.59 kB、ui-vendor 199.02 kB 等，`✓ built in 12.82s`，dirty-worktree 基线口径已注明）。

## 1. Additive contract、schema 与 SDK facade

- [x] 1.1 在 `api/proto/workbench/**` 与 `api/schema/workbench/**` 添加 projection、owner segment、safe media/artifact manifest、PTS map、stage primitive、timeline event、inquiry result、Pane capability、`SpatialReplicaActionDescriptorV1` owner binding 与 receipt/cursor DTO；wrapper 必须引用既有共享 `ActionDescriptorV1` 且不重定义其字段/语义，所有新增字段使用 bounded safe refs，不包含 credential/private path/raw prompt/Provider payload/artifact bytes。验收：proto/schema 可解析且新增字段 additive；命令：`buf lint && buf generate`；失败复查：未知/不安全字段或第二套 availability/permission 必须 fail closed。 **Evidence (2026-09-01)**: 新建 `api/proto/workbench/spatialreplica/v1alpha1/spatialreplica.proto`（package `workbench.spatialreplica.v1alpha1`，18 enum + 36 message + service `WorkbenchSpatialReplicaService` 5 rpc，字段号逐条按 02-contract-freeze.md 落地；wrapper `SpatialReplicaActionDescriptor.action_descriptor = 2` 原样内嵌 `workbench.agent.v1alpha1.ActionDescriptorV1`，零重定义）与 `api/schema/workbench/spatialreplica/v1alpha1/spatialreplica.schema.json`（snake_case wire、19 enum def 全 closed、35 object def 全 `additionalProperties:false`、05 号预算数值内嵌）。`buf lint` exit 0；`buf generate` exit 0，产物 `service/gen/workbench/spatialreplica/v1alpha1/{spatialreplica.pb.go,spatialreplica_grpc.pb.go}` 入库。语义偏差记录：02 §2 表内枚举值为逻辑短名，proto 标识符按 buf STANDARD 加 `SPATIAL_REPLICA_*` 前缀（值号不变），wire JSON 仍用冻结短名（Go/TS/Schema 三面同表）。
- [x] 1.2 实现 contract-level validator 与预算：principal/project/episode/shot scope、owner/contract/version/digest、safe-ref syntax、allowed host/path、field allowlist、payload/collection/depth limits、numeric-vs-preview artifact 用途。验收：unsafe URL、signed URL、local path、credential、unknown field 与超预算 payload 均返回 redacted `contract_mismatch`；命令：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... -run 'Contract|Safe|Budget' -count=1`；失败复查：不得只在 React 层过滤。 **Evidence (2026-09-01)**: `service/internal/spatialreplica/contract/`（contract/enums/saferef/deeplink/budget/validate/wire 七文件）：SafeRef 语法 `^[a-z][a-z0-9-]*:[a-z0-9][a-z0-9._:-]{0,127}$` + closed scheme {ana,auc,sca,wb} + owner↔scheme 绑定；deep link https-only + host/path 前缀 allowlist + query/fragment/userinfo/port 全拒；`DefaultBudget` 冻结 05 号数值（segments 5/layers 10/objects 64/nodes 500/receipts 100/actions 24/question 512/input_json 16KiB/projection 256KiB/inline 4096…）；`ValidateWorkspaceProjection/OwnerSegment/StageProjection/ActionDescriptor/ExecuteRequest/AskRequest/ReceiptPage/WatchEnvelope` 等全量校验（PTS 区间半开、stage P1 坐标约定、METADATA_ONLY 空 safe_text、wrapper base 一致性、input_json 深度/集合/safe-vocabulary）；wire codec（protoreflect）对未知字段/未知枚举名/编号一律 redacted `contract_mismatch`。preview ref 落 `wb:preview:`、numeric artifact 落 `wb:artifact:` 命名空间，跨用途 fail closed。命令 PASS：`ok ... contract 0.075s` + `go vet` 0；全部错误 detail redacted（sentinel 不回显）。
- [x] 1.3 在 `packages/task-sdk` 添加 `spatial-replica-models.ts`、`spatial-replica-client.ts` 并挂入 `WorkbenchClient.spatialReplica`；公开 typed projection/read/watch/inquiry/action/reconcile facade，同时阻止 preview ref 进入 numeric artifact API。验收：现有 SDK exports 兼容，Three 类型不进入 public model；命令：`bun test packages/task-sdk/test/spatial-replica-*.test.ts`；失败复查：任何 `unknown as` 或 display string 驱动逻辑必须移除。 **Evidence (2026-09-01)**: `packages/task-sdk/src/spatial-replica-models.ts`（19 wire enum 词汇 + 全 DTO camelCase 模型 + 双形态 strict normalizer：同读 snake_case 与 camelCase 键、未知键/枚举/scheme/预算一律 null；复用既有 `ActionDescriptorV1` 类型零重定义）与 `spatial-replica-client.ts`（5 rpc 方法表、请求经 `toWire` 转 snake_case wire、idempotency key 走 TransportCallOptions、watch 以 envelope 形态 yield 且 resync 不伪造 snapshot、`validateNumericArtifactRef` 拒绝 preview ref）。挂载 additive：`workbench-client.ts`（import + readonly 字段 + 构造参数 + unconfigured fail-closed transport + jsonRpc namespace `workbench.spatialreplica.v1alpha1`）与 `index.ts` 2 行 export；`json-rpc.ts` namespace 联合类型加 1 成员。命令 PASS：`bun test packages/task-sdk/test/spatial-replica-*.test.ts` 22 pass 0 fail；`bun test packages/task-sdk` 全量 522 pass 0 fail；`bun run typecheck` exit 0。无 `unknown as`，无 Three 类型。
- [x] 1.4 增加 proto↔JSON Schema↔Go↔SDK 四层 conformance fixtures，覆盖 exact tuple、partial segment、VFR/gap、coordinate/scale、four-layer truth、unknown、action descriptor、receipt cursor 与 forward-compatible unknown enum。验收：同一 fixture 在所有解析面产生等价 bounded model；命令：`bun run test:contract`；失败复查：解析差异先修合同/生成链，不在 transport 做 operation-specific patch。 **Evidence (2026-09-01)**: 共享 fixture `packages/task-sdk/test/fixtures/spatial-replica/conformance.json`（16 valid + 14 invalid + 4 camelCase 变体，覆盖 exact_tuple 四 owner identity/digest、partial_segment needs_contract 不遮蔽、VFR 非均匀 inline 帧距 + cut/gap/discontinuity、right_handed+Y/-Z 与 relative→metric_verified scale 梯度、evidence/candidate/production_override/frozen 四层真相、action wrapper、receipt `srw:` cursor/gap、unknown 字段/枚举、unknown availability 等）。四层消费：JSON Schema 层=内嵌 draft2020-12 子集 validator（bun 测试内）；SDK 层=双形态 strict normalizer 等价断言；Go 层=`contract/conformance_test.go`（fixture→proto decode→validate→canonical encode 幂等 + camel/snake canonical 字节一致 + schema enum 与 Go wire 表漂移核对）；proto 层=buf lint/generate 产物本身。命令 PASS：`bun run test:contract` 525 pass 0 fail（含 4 个 pre-existing spawned-server 失败经 `buf generate` 刷新 stale gen 后复绿——见 1.1 evidence：r4 lane proto 已改而 gen 未重生成，非本波引入）。
- [x] 1.5 添加 contract sentinel，证明 owner payload body、screenplay/storyboard canonical body、ReplicaStage geometry copy、raw evidence bytes、credential、private locator、Provider payload、biometric template/face embedding、未授权真实身份标签和跨项目 person key 不能进入 Task/receipt/projection persistence DTO。验收：sentinel string 不出现在 schema snapshot、browser fixture、stdout/stderr 或 evidence；命令：`bun test tests/security/spatial-replica-contract-sentinel.test.ts`；失败复查：泄漏时先缩小 DTO/allowlist，再检查日志与 fixture。 **Evidence (2026-09-01)**: `tests/security/spatial-replica-contract-sentinel.test.ts` 11×9 拒绝矩阵（11 类 sentinel × projection/receipt/event/watch/action/execute/ask-request/ask-response/reconcile/executeResponse 全部 null）+ 反向对照（sentinel-free 形态全通过）+ 文件扫描（proto/schema wire 合同不含 sentinel 字段名与值；共享 fixture 不含 sentinel 值本体——invalid 用例以中性 `future_field` 表达 unknown-key 语义；SDK models 不含 sentinel 值）。Go 侧镜像 `TestContractSentinelsNeverPassTheContractLayer`（10 类 sentinel wire 注入 → redacted `contract_mismatch` 且错误文本不含 sentinel 材料）；Go/SDK input_json forbidden-key 词表同步扩展（owner_payload/screenplay_body/storyboard_body/geometry_vertices/evidence_bytes/person_key/real_person）。命令 PASS：4 pass 0 fail。

## 2. Go composition service、owner adapters 与单一 stream

- [x] 2.1 在 `service/internal/spatialreplica/**` 建立 domain、ports、composition service、capability policy 与 wiring；service 只拥有可重建 read composition 和 action routing，不新增 canonical repository/migration。验收：包依赖不反向进入 owner domain，`CGO_ENABLED=0` 可构建；命令：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... -count=1`；失败复查：出现 domain payload 持久化需求时回到 no-fourth-state 边界。 **Evidence (2026-09-01)**: 新建 `service/internal/spatialreplica/`（capability.go/errors.go/ports.go/media.go/cache.go/composition.go/actions.go/service.go/stream.go + 七个 *_test.go）：ports.go 只暴露 Anatomia/Scaena/Auctra projection 端口 + TaskReceipt/ActionSubmit/ActionReconcile 端口（不自建第二套 task 通道），包依赖仅 contract + gen + security，不 import 任何 owner domain；无 repository/migration/持久化（唯一内存态是 bounded cache/media registry/stream log，均可丢弃重建）。命令 PASS：`ok internal/spatialreplica 0.216s` + `ok .../contract 0.065s`；`CGO_ENABLED=0 go build ./...` exit 0。
- [x] 2.2 实现 Anatomia、Scaena、Auctra 的批准 public-contract adapters 与 deterministic local fixtures；adapter 只返回 safe projections/capabilities/receipts，owner contract 缺失时返回 `needs_contract`，不得读私有数据库、目录、CLI human output 或调用真实 Provider。验收：三类 owner 可分别表达 readiness `available/degraded/offline/needs_contract/permission_required/unavailable` 与 currentness `current/last_confirmed/stale/revoked/unknown`；命令：`CGO_ENABLED=0 go test ./service/internal/adapters/... ./service/internal/spatialreplica/... -run 'Replica|Owner' -count=1`；失败复查：fixture 必须带显式 `fixture` provenance，不能开启 production capability。 **Evidence (2026-09-01)**: `service/internal/adapters/spatialreplica_fixture.go`（+test）：三 owner fixture adapter 对准 02 §11 冻结合同占位（anatomia.spatial_evidence_projection.v1 / scaena.replica_stage_projection.v1 / auctra.screenplay_projection.v1），默认 needs_contract 不携带 payload（mock 不冒充 ready）；readiness/currentness 全谱经 `SpatialReplicaFixtureState` 显式表达（测试覆盖 available/degraded/offline/permission_required/unavailable × current/last_confirmed/stale/revoked/unknown）；所有 segment/inquiry 携带 `fixture_provenance` limitation；P1 预算内（PTS 3 segment+288 VFR 帧距、44 stage objects≤64、5 evidence layers≤10、scene/beat 深度 screenplay）；sentinel 扫描 10 类 credential/private-path/provider 词全部不命中；owner locator（MediaLocator）只进 server 侧 media registry、经 composition 换取 `wb:preview:` opaque ref（测试断言 locator 不出现在投影 wire）。命令 PASS：`ok internal/adapters` + `ok internal/spatialreplica`（-run 'Replica|Owner'）。runtime 接线采用 available+payload（capability 全 default-off + cohort 门控下，显式开 flag 即本地 fixture 演练；见 2.8 evidence）。
- [x] 2.3 实现 exact composition：闭合 project/episode/shot/source identity，计算 per-segment freshness/last-confirmed 与 `composition_token`，保留 non-atomic 声明；identity/version/digest 不一致时 fail closed。验收：partial owner 不遮蔽其他 current segment，display name 不参与 join；命令：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... -run 'Compose|Identity|Freshness' -count=1`；失败复查：发现 silent fallback 或 inferred join 即失败。 **Evidence (2026-09-01)**: composition.go：adapter 回执 `IdentityClaims`（project/episode/shot/source），closeIdentity 要求全部非空 claims 逐字段一致（分歧→bounded identity_mismatch；display name 无 join 路径）；Scaena stage 的 evidence binding resource_ref 必须闭合 Anatomia segment source ref；每 segment 独立 2s deadline + 合同校验（违例折叠 contract_mismatch、错误折叠 offline/needs_contract/permission_required，其余 segment 照常）；`composition_token`=sha256(sorted segment identities+capability digest+binding 集)；atomicity 固定 non_atomic_owner_projection；assemble 后整投影过 `contract.ValidateWorkspaceProjection` fail closed。测试覆盖 exact tuple（4 segment READY/token 64-hex）、identity mismatch、partial 不遮蔽（auctra offline→PARTIAL+source/stage 保留）、stale currentness 保留 last_confirmed 且禁用 stage action、payload 违例折叠。命令 PASS。
- [x] 2.4 实现 bounded in-memory/ephemeral projection cache，并在读取、capability event、action submit 时进行 currentness fence；`composition_token` 只做 stale detection，不作为 mutation authority。验收：cache eviction/restart 不丢 canonical data，stale cache 不能放行动作；命令：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... -run 'Cache|Token|Revalidate' -count=1`；失败复查：若需要 durable state，只允许复用既有 Task/event safe index并补独立设计。 **Evidence (2026-09-01)**: cache.go：LRU+TTL（3s/64 条默认）+generation fence（InvalidateAll 递增 generation，旧代条目一律 miss）；读取面 GetWorkspace 命中 fresh cache（TTL 内合法），capability event（RefreshCapabilities）与 Shutdown 触发 InvalidateAll+media.Clear；action submit（ExecuteAction）与 stream snapshot 一律 `composeFresh` 绕过 cache 重取 owner current state——测试证明 TTL 内 cache 提供旧 token 但旧 expected version/binding 提交被 fail closed（rejected/conflict，零 submit 调用），token 变化仅在 owner version 变化后出现。无 durable state。命令 PASS。
- [x] 2.5 实现一个 workspace-scoped event stream，聚合 safe owner availability/freshness 与 Task/receipt event，分别保留 workspace cursor 和 owner cursor/sequence；支持 resume、dedupe、backpressure，并复用既有 `resync_required` gap control/error + snapshot recovery。验收：Browser connection count 不随 Pane/owner 数增长；命令：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... ./service/internal/runtime/... -run 'Stream|Cursor|Backpressure|Reconnect' -count=1`；失败复查：无法连续恢复时必须刷新 snapshot，不猜测 terminal state。 **Evidence (2026-09-01)**: stream.go：每 workspace 一个 server-owned log+单 pump（聚合 SEGMENT_STATE/TASK_EVENT/OWNER_RECEIPT/CAPABILITY_CHANGE），事件携 `srw:<seq>` workspace cursor + owner cursor/sequence（owner-originated 才有，绝不宣称全局 exactly-once）；resume：订阅与 replay 快照同锁原子（不重不漏），gap/retention 越界/未来位点 → `resync_required` envelope（reason cursor_retention_exceeded/cursor_ahead_of_stream，不伪造 snapshot，client 重新 GetWorkspace）；dedupe：sequence/event_ref 稳定，replay 与 live 同一 log；backpressure：subscriber channel 有界（64），满载 terminate 该连接（重连按 cursor replay/resync），绝不阻塞其他订阅或越过缺口；capability off（ReceiptStream）→ closeAll 停流。测试含单 log/多订阅（3 watcher→1 log 3 subs）、task/receipt 事件、断线重连续传不重复。命令 PASS（含 ./internal/runtime 匹配面）。
- [x] 2.6 在 shared registry/service 上注册 HTTP、gRPC、JSON-RPC projection/read/watch/inquiry/action/reconcile operations，并保持 scope、error、idempotency、cursor 与 receipt 语义一致；transport handler 不含业务分支。验收：四调用面 parity fixture 通过；命令：`CGO_ENABLED=0 go test ./service/internal/transport/... -run 'SpatialReplica|Parity' -count=1 && bun run test:contract`；失败复查：transport 差异回收到 service/codec。 **Evidence (2026-09-01)**: 三 transport 全部落 Go 服务端：`transport/spatialreplicahttp`（GET workspaces/{ws}/shots/{shot}、SSE workspaces/{ws}/events?shot_ref&after_cursor、POST inquiry:ask / actions:execute / actions:reconcile、GET media/{ref}:resolve、GET capabilities 只读）、`transport/spatialreplicagrpc`（5 rpc 全实现）、`transport/jsonrpc/spatial_replica.go`（namespace workbench.spatialreplica.v1alpha1，unary 4 + /rpc/stream watch；共享 handler.go 仅 additive 挂载/前缀/isMutation 两行）。请求/响应全部经 canonical codec（contract.DecodeWireJSON/EncodeWireJSON，未知字段/枚举 fail closed）；错误映射三面同表（02 §8：400/403/409/410/412/422/503 ↔ InvalidArgument/PermissionDenied/Aborted/NotFound/FailedPrecondition/ResourceExhausted/Unavailable ↔ JSON-RPC -32602/-32003/-32009/-32014/-32001/-32015/-32013）；parity 测试断言 HTTP/gRPC/JSON-RPC 对同一 Get 输出字节相同的 canonical wire（第四面 SDK 由 `bun run test:contract` 530 pass 0 fail 覆盖）；parity 差异修复回收 codec 层：watch envelope `resync_required` 冻结为恒在场（wire.go alwaysEmitBoolFields，对齐 wave1 conformance fixture），否则 Go codec 零值省略会让 SDK normalizer fail closed。SDK http 路由（本任务明确遗留）注册于 `packages/task-sdk/src/http.ts` 5 条（Get/Watch/Ask/Execute/Reconcile，workspace_ref 由路径承载+bodyOmit，watch query 键 snake_case），新增 `spatial-replica-http-routes.test.ts` 5 pass。命令双 PASS。
- [x] 2.7 在 `apps/web/server/**` 添加 same-origin source/preview/range proxy，实施 session auth、CSRF、scope、method/header/body/range/timeout/content-type/redirect/host allowlist；不把 owner credential/private locator 传给 browser。验收：range playback 可用，SSRF/redirect/header smuggling/oversize 均拒绝；命令：`bun test apps/web/server/spatial-replica-media-proxy.test.ts`；失败复查：任何 client-supplied upstream URL 直接作为目标都视为 blocker。 **Evidence (2026-09-01)**: `apps/web/server/spatial-replica-media-proxy.ts`（路由 `/bff/spatial-replica/media/{opaque_ref}`）+ handler.ts 挂载（local token / managed session cookie+credential 两条路径，均先过既有 session/auth 面）：client-supplied upstream URL 结构性不可能（opaque ref 字符集拒 '/'?'%'，且上游只来自 backend `media/{ref}:resolve`——scope/capability/cohort/registry/expiry 全在 Go 侧复验）；method 仅 GET/HEAD（mutation 面 405，无 CSRF 面）、query/body 拒绝、Range 单区间+方向+span 预算（默认 64MiB）、resolve/fetch 双超时（10s/30s）、redirect manual（3xx 拒绝）、上游 origin ∈ BFF env allowlist（`WORKBENCH_SPATIAL_REPLICA_MEDIA_ORIGINS`）+https-only+无 userinfo、response 仅放行 content-type/length/range/etag/last-modified/accept-ranges（set-cookie/X-Internal-Host 剥离，测试断言）、content-type 必须与 resolve 一致；capability flag `WORKBENCH_SPATIAL_REPLICA_MEDIA_PREVIEW_ENABLED`!==true 即 503 capability_disabled（fail closed，任何其他取值不启用）。命令 PASS：8 pass 0 fail；`bun test apps/web/server/` 全量 147 pass 0 fail。Go 侧 resolve 端点+MediaRegistry（issue/resolve/revoke/expiry/origin 校验）在 2.1/2.6 交付。
- [x] 2.8 将 projection/stream capability 绑定到 server exact-principal cohort 与现有 runtime shutdown；capability off/revoked 时停止 stream、清 cache、撤销 descriptors 并保持 conversation/Task/receipt。验收：on→off 不删除 canonical owner data且 client 收敛到 unavailable；命令：`CGO_ENABLED=0 go test ./service/internal/config ./service/internal/runtime ./service/internal/spatialreplica/... -run 'Capability|Rollback|Shutdown' -count=1`；失败复查：browser 仍可使用旧 descriptor 即失败。 **Evidence (2026-09-01)**: capability.go：十行 truth table 解析器（flag 仅 true/false/空，非法 startup fail-closed）+ exact-principal cohort（CSV env `WORKBENCH_SPATIAL_REPLICA_COHORT`，`Availability = flag AND prereq 链 AND cohort`；URL/query/localStorage/HTTP 200 无参与路径）；runtime.go additive：Config 十字段+cohort、LoadConfig env、New() resolve fail-closed + 组装 + 三 transport 挂载 + 只读 `/v1alpha1/spatial-replica/capabilities`（no-store）、Shutdown() 调 SpatialReplicaService.Shutdown()。off/revoke 收敛（RefreshCapabilities/Shutdown）：停全部 workspace stream（订阅 channel 关闭）、InvalidateAll cache、Clear preview refs、descriptor 随重 compose 撤销（旧 binding 提交→rejected capability_disabled/action_not_available，submit 零调用——测试覆盖『token 未变但 capability revoked 仍拒绝』）；re-enable 后同一 shot 以相同 segment 身份重建（canonical 数据无损）；reconcile 不受 capability off 影响（Task/receipt 保留，runtime 端口测试覆盖 idempotency-mismatch fail closed + 未注册 operation 诚实 needs_contract）。命令 PASS（config/runtime/spatialreplica 三包匹配面全绿）。cohort principal 形态：`wb:user:<subject|membership>`（transport 从认证上下文派生）。注：`go test ./internal/config` 该 -run 无匹配用例（no tests to run，ok）。

## 3. Pane registry 与统一 Agent workspace 组合

- [x] 3.1 在现有 canonical Pane registry additive 注册五个 versioned Pane type，定义 closed parameter schema、preferred width、availability、error boundary、focus return 与 hard limit；不新增 route/shell/registry/composer。验收：unknown version/unsafe params fail closed；命令：`cd apps/web && bunx vitest run agent-pane-registry`；失败复查：发现第二 registry 或 direct owner client 时回退实现。 **Evidence (2026-09-01)**: 合同冻结在 `apps/web/src/workbench/agent/spatial/replica-review/replica-pane-contract.ts`（五 pane type 字符串唯一来源、`replicaPaneParamSchemas` closed allowlist/required/identity、`replicaClosedParams` 在 registry 通用 allowlist 之上叠加 Spatial Replica SafeRef `^[a-z][a-z0-9-]*:[a-z0-9][a-z0-9._:-]{0,127}$` + scheme {ana,auc,sca,wb} 收窄、`replicaDocumentKey` 只由必填 ref 组成、`replicaPaneCapabilities` requiredAction=truth table machine id）；additive 挂载：`agent-pane-registry.ts`（agentPaneTypes spread + runtime interface/registeredComponents/5 entries/keysByKind、AgentPaneRequest +6 ref 字段且 sessionRef 转 optional——replica Pane 是 workspace 域身份，keysByKind 不采集）、`agent-pane-manifest.ts`（+5 manifest，closedParams 与 registry 逐字段一致，desktopWidthHint 720/420/480/520/480=02 §5 preferred width 上限）、`agent-pane-host.tsx`（+5 runtime slot）、`agent-pane-dock.tsx`（主 Pane solo width clamp(560px,48vw,720px)）、icons +5（Boxes/ScanSearch/ScrollText/LayoutGrid/ReceiptText）。availability：`replicaPanePaletteAvailability` 诚实禁用（transport 投影=wave 2.6、server capability=2.8 未落地→needs_contract 禁用，palette 保留条目不打开 stub）；error boundary=`ReplicaPaneErrorBoundary`（Pane 内捕获、焦点回本 Pane heading）；focus return 复用 dock/pane 既有语义；hard limit 4 不变。命令 PASS：`bunx vitest run agent-pane-registry` 17 pass 0 fail（含新增 exact resolve/unsafe params/unknown version fail-closed 用例）；连带 `agent-pane-manifest`/`agent-pane-layout`/`agent-pane-dock`/`agent-pane-availability`/icons registry 同步更新后全绿。
- [x] 3.2 创建 `apps/web/src/workbench/agent/spatial/replica-review/**` 主 Pane skeleton，组合 episode/shot navigator、source region、viewport region、timeline、Inspector entry 和 status strip；复用 Agent shell、session/layout reducer、Task client 与 one workspace stream。验收：mount/unmount 不 remount composer 或创建新 session；命令：`cd apps/web && bunx vitest run spatial-replica-pane`；失败复查：模式切换导致 draft/session 丢失时先修 ownership。 **Evidence (2026-09-01)**: `replica-review/` 新建 12 文件（contract/state/navigation/projection/state-surface/error-boundary/review-pane/owner-pane-shell/inspector/auctra-screenplay/scaena-storyboard/owner-receipts）。主 Pane `replica-review-pane.tsx` 按 UI spec §3.1 组合 ReplicaScopeBar（episode/shot selector+步进、mode/scale badge、owner freshness 组、辅助 Pane 入口）+ 视图 tabs（source/viewport/compare，空态不吞）+ SourceEvidenceRegion（media/preview ref 元信息+图层 legend+可见性 toggle）+ ReplicaViewportRegion（DOM object mirror 骨架+selection）+ ReplicaTimelineRegion（transport/PTS 骨架）+ ReplicaSelectionStrip；数据流：`replicaQueryKeys.workspace` 共享 query（五 Pane 同 key、staleTime 15s）+ `startReplicaWorkspaceWatch` 单 stream 控制器（每 workspace 去重、resync→invalidate snapshot fence；transport ready 前 enabled=false）；状态全部经 `replicaWorkspaceReducer`（context 注入缝 `ReplicaWorkspaceStateContext/DispatchContext` + `children` 组合缝供 4.x/5.x/6.x 子控制器）。命令 PASS：`bunx vitest run spatial-replica-pane` 5 pass 0 fail（含同 QueryClient 双 mount getWorkspace 仅 1 次调用、empty 保留 3 tabs 结构性 UI）。
- [x] 3.3 实现 exact owner-ref episode/shot navigation、deep-link parsing 与 local selection reset；禁止标题/数组位置 join，shot switch 必须处理 dirty draft、media callbacks、Three resources 与 stream projection。验收：deleted/remapped ref 显示 stale/not_found，不自动选同名对象；命令：`cd apps/web && bunx vitest run spatial-replica-navigation`；失败复查：任何 guessed fallback 都fail closed。 **Evidence (2026-09-01)**: `replica-navigation.ts`：`findShotByExactRef`（ref 相等唯一 join 路径）、`neighborShotRef`（先 exact 定位当前，not_found 不按位置猜）、`searchShotRefsByLabel`（仅搜索/highlight，永不产生导航目标）、`parseReplicaReviewDeepLink/serializeReplicaReviewDeepLink`（4 closed query keys，任一 replica 键出现即要求 workspace/episode/shot 三键全 SafeRef，非法整体 fail closed）、`planReplicaShotSwitch`（dirty draft→guard_required；通过后返回 projection/media/viewport/timeline reset 计划 + media/viewport remount key）、`replicaAuxiliaryPaneParams`（辅助 Pane closed refs 推导）；主 Pane 组件级：切换先 removeQueries 旧 shot key + region remount key 更换（media cache reset 缝，4.x/5.x 接管真实资源）、guard 确认才 discard+commit、取消保留当前 shot、deleted ref→selector 显式 not-in-index + `data-replica-state="stale"` + 步进禁用、selection/reset 为 reducer 公共 action。命令 PASS：`bunx vitest run spatial-replica-navigation` 11 pass 0 fail（纯函数 7 + 组件级 4）。
- [x] 3.4 实现统一 workspace state reducer，只保存 PTS、overlay visibility、3D camera、selection、expanded groups、Pane layout 与 session-local typed draft；logout/scope revoke/capability off/shot switch 清理不适用状态。验收：reload 后从 owner projection 重建且不恢复 canonical stage copy；命令：`cd apps/web && bunx vitest run spatial-replica-state`；失败复查：localStorage/IndexedDB/server 写入 owner payload 即失败。 **Evidence (2026-09-01)**: `replica-workspace-state.ts`：closed shape 仅 7 顶层字段（`REPLICA_WORKSPACE_STATE_KEYS`：pts/overlayVisibility/camera/selection/expandedGroups/viewLayout/draft），PTS cursor 区分 confirmed/requested（requested 不作 evidence frame）、camera display-only（focusObjectRef 非.owner mutation）、draft=session-local typed（bindingRef/actionId/baseOwnerVersion/fields/dirty）；`shot/switch` 清 per-shot 派生状态但保留 view tab（结构性 UI 不吞）；`session/resetAll`（logout/scope revoke/capability off）回初始；未知 action（含 stage/screenplay canonical 注入形状）一律 no-op；`isReplicaWorkspaceState` runtime 守卫拒绝 closed 键集外/缺失；模块无任何 localStorage/IndexedDB/server 写入（测试 spy 断言）。命令 PASS：`bunx vitest run spatial-replica-state` 7 pass 0 fail。
- [x] 3.5 为主 Pane 与所有 owner Pane 实现 loading/empty/partial/stale/error/revoked/permission_required/offline/needs_contract/unsupported/limit states，保留 last-confirmed 与可做/不可做/recovery 文案。验收：一个 Pane fault 不 crash shell 或伪装其他 owner fallback；命令：`cd apps/web && bunx vitest run spatial-replica-states`；失败复查：generic error 遮蔽 owner segment 状态时补状态模型。 **Evidence (2026-09-01)**: `replica-state-surface.tsx` 覆盖 UI spec §12 全部 17 行（`replicaSurfaceStates`=SpatialReplicaPaneState 10 + region/action 子状态 7）：每状态 machine 值 mono chip（English invariant，`data-replica-state` 供测试/e2e 断言）+ title/reason/recovery 三段文案（zh-CN/en-US catalog `agent.replica.state.<state>.*`，7 状态另有 action 按钮 label）；清空型（revoked/permission_required/unsupported_version/limit_reached）不透传 children，保留型（loading/empty/partial/stale/offline/sync_degraded/view_too_large/webgl_unavailable/duplicate_action/unknown_accept/error/navigation_away）保留结构性内容；navigation_away 双路径（丢弃/留在当前镜头）。主 Pane 相位映射 `deriveReplicaPanePhase`+`replicaErrorPhase`（closed error code→六态、网络错误→offline 不当合同缺失；测试逐态断言清空/保留语义）；partial 不遮蔽可用 segment（auctra offline 时 anatomia/scaena current 照常）。owner Pane 外壳 `replica-owner-pane-shell.tsx`：per-owner segment readiness 独立推导（deriveOwnerSegmentPhase）、auctra pane 无 shot 参数（02 §5 冻结）→从共享 query cache 解析主 Pane 已建立的 shot（不发起第二次读，缓存未暖→loading 等待）；error boundary 测试证明渲染 fault 被吞在 Pane 内。命令 PASS：`bunx vitest run spatial-replica-states` 31 pass 0 fail（17 行逐行 + 主 Pane 6 错误态 + partial + owner 6 用例）。i18n：`api/locale/source/{zh-CN,en-US}/agent/replica-review.json` 各 +137 键（additive，machine enum/ref/action/error 保持 English）+ catalog-policy +137；`bun run compose:i18n` OK（3201 keys，生成物含并行 lane film-production 源键）；`bun run check:i18n` 失败项全部 pre-existing（project.*/loginMethods/projectSchema/agent.spatial.review.accepted，R3-R5 与并行 lane 基线，无任何 agent.replica.* 键）；`bun run test:i18n` 5 pass。

## 4. PTS 播放、evidence overlays 与虚拟化 timeline

- [x] 4.1 实现 `PtsMap`/timebase/frame interval client utilities，覆盖 VFR、cut、gap、duplicate/discontinuity，并禁止 `time * fps` 作为 evidence/stage frame identity。验收：table-driven fixtures 与 owner PTS map 完全一致；命令：`cd apps/web && bunx vitest run spatial-replica-pts`；失败复查：浮点边界必须用 rational/timebase 或 owner interval解决。 **Evidence (2026-09-01)**: `apps/web/src/workbench/agent/spatial/replica-review/replica-pts-map.ts`——BigInt 有理数核（gcd 约分 + 交叉相乘比较 + timescale 归一化：`{12,1}≡{360000,30000}`）；`createReplicaPtsIndex`（segment 排序分类、discontinuity 声明覆盖→gap、inline interval 精确累积、gap/段间洞游标 snap 到下一 content 起点（cut/gap 后 PTS 显式跳变不插值）、`value<=0` interval 生成零宽 duplicate 帧、coverage 外停止）；`lookupReplicaFrameAt`（binary search，返回 frame/gap/unverifiable，绝不猜最近帧）、`stepReplicaFrame`（exact 相邻帧 + 跨 gap 落到 main-01 起点）、`replicaPtsIndexRange`/`replicaPtsKey`/`mediaSecondsToPtsTarget`（秒→tick 单位换算，非帧号推算）；帧边界 PTS 优先以 owner segment timescale 表达（wire 风格，`bigRationalToPts` preferredTimescale exact 整除路径）。测试 `test/spatial-replica-pts.test.ts` 16 pass：Go fixture 精确镜像 table-driven（3 segment + 288 帧 100/101/104 循环，帧边界 0/100/201/305/405/506、末帧 29176→29280）、「禁止 time×帧率」断言（PTS 200/300 分别落 frame 1/2，均匀假设会给 2/3）+ 结构性守卫（同步层 5 个源文件扫描无 `fps`/`frameRate` 标识）、gap/out-of-range/coverage 外 unverifiable、duplicate 零宽帧、CFR fixture 边界。命令 PASS：`bunx vitest run spatial-replica-pts` → 16 passed。
- [x] 4.2 实现 source player controller，以 `requestVideoFrameCallback`（受控 fallback）生成 confirmed PTS cursor，并统一驱动 overlay、3D pose、timeline 与 Inspector。验收：播放/seek/frame-step 在 VFR fixture 中 drift 不超过一个 source frame；命令：`cd apps/web && bunx vitest run spatial-replica-player`；失败复查：无法确认帧时进入 `sync_degraded`，不显示 inferred evidence。 **Evidence (2026-09-01)**: `replica-player-controller.ts`——rVFC 首选（`attach` 探测 + 自续 callback 循环 + cancelVideoFrameCallback 清理）；confirmed cursor 对齐到 owner map 解析帧的 exact 起点（drift=|mediaTime−frameStart|≤一帧）；无 rVFC 时受控 fallback（100ms 定时器 + currentTime 插值）恒发 `requested` origin=`fallback_sample` 并进入 `sync_degraded:frame_confirmation_unavailable`（显式 requested≠confirmed）；`seekToPts` 先 requested cursor 再改 currentTime、帧回调后 confirmed；`stepFrame`/`seekShotEdge`/`retryMedia`；media error code 4→`unsupported_codec`、1–3→`media_error`（只报告不抛出）。接线：`replica-source-region.tsx`（video 经 callback ref attach，`/bff/spatial-replica/media/{encoded opaqueRef}` same-origin 代理）+ `replica-review-pane.tsx` ReplicaRegions 创建唯一 controller，cursor 全部经 workspace state `pts/set` dispatch——overlay/3D/timeline/Inspector 共享 `state.pts`。3.x 遗留同步落地：`REPLICA_PANE_TRANSPORT_READY` 翻转为 true（2.6 SDK HTTP 路由 + 2.8 capability/cohort 已落地），ready 相位 workspace watch 真实启动（resync envelope→invalidate 重取），palette 门禁收敛到 context refs（无会话 refs 时以 `replicaNeedsContext` 诚实禁用）。测试 `test/spatial-replica-player.test.tsx` 8 pass（fake HTMLMediaElement + rVFC stub：confirmed/requested/seek/step/gap 降级/fallback/媒体错误/detach no-op + 组件级 dispatch 接线）；watch/palette 断言在 `spatial-replica-pane.test.tsx`。命令 PASS：`bunx vitest run spatial-replica-player` → 8 passed。
- [x] 4.3 实现 grayscale/depth/mask/flow/skeleton/camera/quality layer registry，分别显示 lineage、PTS coverage、coordinate/unit/scale、quality/currentness、limitations，并区分 numeric artifact 与 preview rendition。验收：display-only layer 不产生 metric/production claim；命令：`cd apps/web && bunx vitest run spatial-replica-overlays`；失败复查：缺 metadata 时 layer 必须 claim-limited。 **Evidence (2026-09-01)**: `replica-overlay-registry.ts`——`REPLICA_OVERLAY_KIND_PROFILES` 七类 kind 各自声明 coordinate expectation/unit expectation/语义注记/默认 limitation（机器值 English）；descriptor 携带 lineage（snapshotRef/bundleRef/numericArtifactRef/lineageRefs）、ptsCoverage、coordinate（frameRef/unit/handedness/complete）、quality（profileRef/currentness/boundedMetrics）、currentness、limitations；claim 三分类：numeric（artifact+coordinate/unit/lineage 齐备）/preview_only（仅 preview，派生 `display_only_preview` limitation，`metricClaimAllowed=false`）/claim_limited（metadata-only 或 scale unknown，派生 `scale_tier_unknown`）；预算 `maxEvidenceLayers`≤10（超出截断 + `layer_budget_exceeded` 显式呈现，不静默丢弃）；`replicaOverlayCoversPts` BigInt 精确 coverage 判定（coverage 外=layer gap）。渲染：`replica-source-region.tsx` legend 行（claim 徽章 + 展开元数据 dl：lineage/coverage/coordinate/unit-scale/quality/limitations + display-only title 提示）与 overlay stack（仅当前 confirmed PTS coverage 内的可见 preview 层渲染 `<img>`，coverage 外隐藏不插值）。测试 `test/spatial-replica-overlays.test.tsx` 5 pass（七类 profile、numeric/preview_only/claim_limited 三态与 metricClaimAllowed、coverage 端点半开、13 层超预算截断、组件 legend 元数据展开）。命令 PASS：`bunx vitest run spatial-replica-overlays` → 5 passed。
- [x] 4.4 使用现有 virtualization 能力实现 camera/actor/joint/keyframe/contact/occlusion/audio/quality/receipt tracks 与 event list，支持 range window、bounded overscan、seek、step、jump-event、search/select。验收：large fixture 不实例化全部 joint samples且 exact PTS/provenance 保留；命令：`cd apps/web && bunx vitest run spatial-replica-timeline`；失败复查：DOM 节点或 render time 随全量 sample 线性失控时优化 windowing。 **Evidence (2026-09-01)**: `replica-timeline.ts`——SDK 冻结 10 TrackKind 按 UI spec §9.1 顺序分组（含空 track 行）；`replicaTimelineEventsFromProjection` 只从投影携带 exact PTS 的内容派生（source segments（cut/gap 显式成段）、stage objects ptsRange→camera/actor/joint/contact/occlusion marker 轨、layer ptsCoverage→quality_track、receipts→wall-clock 显式非 PTS 键），keyframe/contact/occlusion/audio per-event feed 留 `extraEvents` 注入缝（6.x），无均匀撒点/无时间×帧率；`filterReplicaTimelineEvents` range window、`replicaEventIdentityKey`（eventRef+归一化 PTS，label 不参与）。`replica-timeline-region.tsx`——复用仓库既有 `@tanstack/react-virtual`（cli-run-workspace/semantic-table 同款）滚动事件列表（overscan 6、224px 视口、每行 PTS/provenance/trackKind）；lane 事件 chip 按窗口定位 + 每 lane 24 上限 `+N` 溢出标记；track 折叠（`groups/toggle`，aria-expanded）；搜索（ref/label 过滤不改变身份）；键盘 Space/←→（exact frame step）/Shift←→（jump event：select+seek）/Home/End，input 内不抢键；事件行 click/Enter 经 4.5 coordinator 选择并 seek。测试 `test/spatial-replica-timeline.test.tsx` 10 pass（2000+ joint samples 仅实例化 ~13 行、PTS/provenance 保留、10 轨道族、窗口过滤、identity、折叠、搜索、pointer/键盘路径）。命令 PASS：`bunx vitest run spatial-replica-timeline` → 10 passed。
- [x] 4.5 实现跨 player/overlay/viewport/timeline/Inspector 的 stable selection coordinator，object/event selection 都映射 exact owner ref + PTS，不把 local camera/selection 写成 owner mutation。验收：keyboard 与 pointer 选择产生同一 projection；命令：`cd apps/web && bunx vitest run spatial-replica-selection`；失败复查：任何 display label 作为 identity 即失败。 **Evidence (2026-09-01)**: `replica-selection.ts`——`replicaSelectObjectAction`/`replicaSelectEventAction`（exact owner safeRef + truthLayer + PTS，keyboard 与 pointer 共用同一构造函数→同一 workspace action）；`replicaSelectionIdentityKey`＝`objectRef|eventRef|<约分 PTS 键>`（timescale 归一化，display label/kind 一律不进键，空选择→null）；`replicaFocusObjectAction` 只 dispatch `camera/focusObject`（local presentation），断言 focus 不触碰 selection（viewport orbit/zoom/focus 不是 selection identity）。接线：pane viewport mirror、timeline 事件行、selection strip（identity 键以 `data-replica-selection-identity` 暴露给 5.x/6.x）。测试 `test/spatial-replica-selection.test.tsx` 4 pass（action 形状、identity 归一化/不同 PTS 不同键/label 无关、focus≠selection、组件级 pointer click 与 keyboard Enter 产出同一 identity）。命令 PASS：`bunx vitest run spatial-replica-selection` → 4 passed。
- [x] 4.6 增加 sync degradation、media error、layer gap、unsupported codec 与 partial artifact recovery，确保 source 可用时仍可审阅，错误层不冻结整个 shell。验收：每类 failure 有 visible limitation 与 recovery；命令：`cd apps/web && bunx vitest run spatial-replica-sync-degraded`；失败复查：空白画面/静默插值均不合格。 **Evidence (2026-09-01)**: `replica-degradation.ts`——六类 closed failure 枚举（sync_degraded/media_error/layer_gap/unsupported_codec/partial_artifact/preview_expired）逐类 notice（reasonCode/messageKey/recovery/detail）：`classifyReplicaMediaFailure`（MediaError code 4→unsupported_codec（open_owner_source，不隐式换 renderer）、1–3→media_error（retry_media））、`replicaSyncDegradationNotice`（pts_gap/frame_unverifiable/frame_confirmation_unavailable…，不插值 evidence）、`replicaLayerGapNotice`、`replicaPartialArtifactNotice`（缺 numeric_artifact/coordinate_frame/unit/lineage_refs/scale_tier 面板）、`replicaPreviewExpiryNotice`（expiresAtUnixMs 过期/60s 临期→refresh_projection 换新 preview ref）。渲染：`replica-source-region.tsx` notice 条（`data-replica-failure` 逐类 chip + 重试媒体/刷新投影 recovery 按钮 + 不可表示时不渲染按钮）；layer gap 行内 `layer_gap` 标记、coverage 外 preview 层隐藏（无过期帧）；timeline/tabs/legend 在全部六类下常驻（错误层不冻结 shell）。测试 `test/spatial-replica-sync-degraded.test.tsx` 9 pass（closed 六类枚举 + 纯函数映射 + 逐类组件呈现/恢复动作/shell 存活）。命令 PASS：`bunx vitest run spatial-replica-sync-degraded` → 9 passed。scoped 全量：`bunx vitest run spatial-replica` → 10 files / 111 tests 全绿；`bun run typecheck` 0 error；`bun run --cwd apps/web build` 成功；`openspec validate workbench-spatial-replica-review-v1 --strict --no-interactive` valid。

## 5. Three viewport、truth Inspector 与资源门禁

- [x] 5.1 仅在 viewport capability enabled 时 lazy-load `three` core 和 `ReplicaViewportAdapter`；React 拥有 DOM/controls，adapter 拥有 scene/render/picking/disposal，wire/SDK 不暴露 Three 类型。验收：普通 `/agent` bundle 不加载 spatial 3D chunk；命令：`bun run --cwd apps/web build`；失败复查：静态顶层 import 或全局 scene store 必须移除。 **Evidence (2026-09-01)**: `three@0.185.1` + `@types/three@0.185.0`（devDep，锁版本）入 `apps/web` dependencies（`bun add` exit 0，锁文件更新）。架构：`replica-viewport-contract.ts` 是唯一跨边界类型面（scene input/camera view/adapter 接口/资源 stats/工厂签名，零 Three 类型）；`replica-viewport-adapter.ts` 是 `spatial-replica-3d` chunk 唯一入口（React 只经 `loadReplicaViewportAdapter()` dynamic import 到达）；`replica-viewport-region.tsx` 拥有 toolbar/mirror/canvas DOM 与全部 controls（camera/selection/PTS/visibility 单向同步进 adapter），adapter 拥有 scene/render/picking/disposal，无第二 scene store。capability 门：truth table 行 3 `spatialReplicaViewport3d` 只读快照（same-origin `GET /v1alpha1/spatial-replica/capabilities`，2.8 落地端点），读取失败=pending→fail closed disabled（不加载 chunk）；另加 >=1024 宽度门与 `view_too_large` budget 门。vite `manualChunks` 新增 `spatial-replica-3d`（three + 包内 GLTFLoader + adapter 本体）。命令：`bun run --cwd apps/web build` exit 0——`spatial-replica-3d` chunk **158.2 KiB gzip**（zlib 口径；vite 报 160.91 kB）≤250 KiB 预算；`scripts/spatial-replica-bundle-budget.mjs` 双重断言 PASS：①GLTFLoader 标记仅存在于 3d chunk；②从 index.html 入口做静态 import 图闭合，`spatial-replica-3d` 不在静态闭包（只可 dynamic 到达）。组件级：capability off/读取失败/窄屏/budget 超限四路 `createReplicaThreeViewportAdapter` 零调用（`bunx vitest run spatial-replica-viewport-lifecycle` 13 pass）。
- [x] 5.2 将 Scaena safe primitive graph 映射为 camera、ground、proxy geometry、actor placeholder、skeleton/rig pose、contact/occlusion 与 candidate ghost summary，并保留 coordinate/scale/provenance。验收：选择任一 object 可回溯 exact owner ref；命令：`cd apps/web && bunx vitest run replica-viewport-adapter`；失败复查：不得读取私有 GLB/path 或静默丢几何继续声称一致。 **Evidence (2026-09-01)**: `replica-viewport-adapter.ts` 把 13 种 `SpatialReplicaObjectKind` 全部建图（camera→frustum 线框 gizmo、ground→grid、box/capsule/bounds/proxy→fill+edge、actor_placeholder→可折叠 group、skeleton_joint→sphere+骨架采样、contact_marker→实心标记、occlusion_marker→wireframe unknown 区域、candidate_ghost→半透明 amber ghost、derivative_ref→GLB）。wire 是 metadata-only primitive graph（无顶点数据）——placeholder 布局由 objectRef 的 FNV-1a 稳定哈希派生（跨会话一致，documented presentation 约定），coordinateFrame.handedness 镜像 X 轴、scaleTier/limitations 随 scene input 保留（呈现归 scope bar/Inspector）；真实几何/metric 恒归 owner numeric artifact，不读私有 GLB/path。pose/path 采样纯函数导出：`sampleReplicaSkeletonPose`（16-joint humanoid 模板；`interpolationAllowed=false` snap 到 8Hz sample 档、不插值成 evidence）与 `sampleReplicaCameraPath`（8-keyframe 含 hold 段）。PTS coverage：ptsRange 外对象 `visible=false`（隐藏不删除）。拾取：Raycaster（Line threshold 收紧到 0.05）+ userData.objectRef 向上回溯 → exact owner ref；`setScene` 整体替换时旧 ref 失效（不残留）。budget：`decideReplicaViewportBudget` declaredCost>maxPrimitives → `view_too_large`（不静默降采样）。glb_preview derivative 经 same-origin `/bff/spatial-replica/media/{ref}` fetch + 包内 GLTFLoader parse，失败 → `derivative_load_failed` truthful limitation。命令 PASS：`bunx vitest run spatial-replica-viewport-adapter` 13 pass（真 Three 对象图 + mock renderer 注入：kind 建图/pick 回溯/coverage 可见性/pose 采样确定性/setScene 替换/dispose/context loss/renderer 失败/GLB 成败两路）。
- [x] 5.3 实现 viewport toolbar、orbit/fit/reset/view preset、object visibility/search/select 与 DOM accessibility mirror；drag/orbit 必须有 keyboard/menu 等价路径。验收：WebGL canvas 不是唯一信息源；命令：`cd apps/web && bunx vitest run replica-viewport-accessibility`；失败复查：canvas-only selection 或无 label object 阻断合入。 **Evidence (2026-09-01)**: `replica-viewport-region.tsx`：toolbar（orbit ±15°/±10° 键盘可达按钮、zoom、front/side/top/机位 preset、fit all/fit selection/reset、azimuth/elevation/distance 三个带 label 的数值输入——keyboard/menu/numeric 三条等价路径，全部只 dispatch `camera/set`/`camera/focusObject` 本地 presentation）；DOM accessibility mirror：五语义分组（cameras/actors/scene/markers/candidates）、搜索（ref/label/kind 过滤 + shown/total 如实计数）、逐对象 select 按钮（aria-pressed）+ 可见性 checkbox + provenance 文本（kind · truthLayer · ownerVersion）+ `data-replica-visual-availability`（covered/no_sample/always/unknown——coverage 外对象标记保留、不从 a11y tree 消失）+ jump-to-PTS。canvas `role=img` + accessible name（对象数 + 指向对象树）；canvas pick 与 DOM 选择走同一 `replicaSelectObjectAction`（pointer/keyboard 同一 identity）；双击 mirror 行 → `camera/focusObject`（focus≠selection）。命令 PASS：`bunx vitest run spatial-replica-viewport-accessibility` 7 pass。
- [x] 5.4 处理 WebGL init failure/context loss/restore、shot switch、Pane close 和 capability off，释放 geometry/material/texture/render loop/observer/subscription/media callback。验收：failure 时 object list/timeline/Inspector/review 仍可用，资源计数无界增长为零；命令：`cd apps/web && bunx vitest run replica-viewport-lifecycle`；失败复查：context restore 不得复用 stale owner projection。 **Evidence (2026-09-01)**: 生命周期全覆盖：①WebGL init 失败 → `webgl_unavailable` 状态面 + 对象树/搜索保留 + retry renderer（仅 renderer，mountEpoch 递增重挂，不重放 owner action）；②context loss → 停渲染循环 + 状态面；restore → adapter 清空 scene（stats 归零）+ 回调 `onRefreshProjection` 重取当前投影（旧 stale 投影几何不复用，新 stage 到达才 setScene）；③shot switch：`viewportRemountKey`（3.3 缝）卸载 region → dispose → 新 region 以新投影 setScene（主 Pane 集成测试验证两次挂载）；④Pane close/unmount → dispose；⑤capability off：`refetchOnWindowFocus:"always"` + pane stream snapshot fence 同时 invalidate capability query（capability_change 被动收敛）→ mountAllowed 翻转 → dispose 且不再挂载。dispose 幂等：geometry/material/texture/render loop（rAF cancel）/observer（ResizeObserver disconnect）/canvas context（forceContextLoss）全释放，`workbench:replica-disposed` User Timing mark 携带归零计数；rAF 在无视觉环境退化为 16ms timer（jsdom 兼容）。media callback：player controller 生命周期沿用 4.2（pane 卸载随 region 卸载）。命令 PASS：`bunx vitest run spatial-replica-viewport-lifecycle` 13 pass。perf harness 补充：连续 3 次 shot switch + close → 4 个 disposed marks 全零计数（见 5.6）。
- [x] 5.5 实现 Evidence/Candidate/Production Override/Frozen 四层 Inspector、unknown preservation、capture profile、scale tier、claim ladder、quality/currentness/limitations 和 selected completion provenance。验收：candidate selected 不删除 unknown，未支持 metric 时不显示一比一结论；命令：`cd apps/web && bunx vitest run spatial-replica-inspector`；失败复查：颜色/图标不能替代文本 truth labels。 **Evidence (2026-09-01)**: `replica-inspector-truth.ts` 纯函数 + `replica-inspector-pane.tsx` 完整内容（3.x 骨架替换）：四层 truth stack 每层呈现 layer name+currentness（文本）、owner ref/version、PTS/range、lineage（layer snapshotRefs / candidate provenance / frozen receipt）、quality profile、limitations codes、Evidence 层的 unknown 保留行（occlusion_marker refs + claimScopes.excludedRegionRefs）。claim ladder（`replicaClaimLadderForTier`，UI spec §10.3 冻结文案）：unknown→layout_only、relative→relative_spatial（允许 left/right/front/behind；禁 metric distance/one-to-one；升级=calibrated scale+current camera solve+review receipt）、metric_approximate→bounded metric、metric_verified→metric within tolerance；capture profile 缺失一律收紧 layout_only（`metricClaimAllowed=false` + `data-replica-metric-denied` 文本注记，无一比一结论）。Production Override = owner receipt confirmed selected candidate（provenanceRefs 呈现；未选中诚实空态 `data-replica-truth-empty`）；Frozen 只呈现 stage.frozen（frozenRef/stageVersion/receiptRef），未冻结诚实空态。selected completion provenance：选中对象聚合 kind/truth/PTS/candidate selected/frozen 版本。SDK DTO 直接映射（`buildReplicaInspectorModel` 输入即 stage+layers+selection），无本地 canonical 推断；全部 truth label 为文本（颜色仅辅助）。命令 PASS：`bunx vitest run spatial-replica-inspector` 10 pass（四层齐备/candidate selected 不删 unknown/诚实空态/frozen/claim 收紧/selected provenance/纯函数）。
- [x] 5.6 建立 local viewport performance harness，记录 first 3D frame、selection feedback、frame-time p50/p95、long task、heap、resource disposal 与 bundle delta；只报告环境化本地 evidence，不声明 production SLO。验收：P1 fixture 无 repeated >50ms main-thread long tasks且 selection feedback 100ms 内可见；命令：`bun run --cwd apps/web e2e:spatial-performance`；失败复查：超预算先降低 scene/render cost，不删除 provenance/unknown。 **Evidence (2026-09-01)**: harness：`e2e/spatial-replica-performance.spec.ts`（WORKBENCH_SPATIAL_PERF=1 门控）+ `playwright.replica-performance.config.ts`（dev server 模块图 + headless Chromium SwiftShader）+ `scripts/spatial-replica-bundle-budget.mjs`；`package.json` 的 `e2e:spatial-performance` 扩为三段聚合（canvas/replica/budget 各自退出码，任一非零整体非零）。挂载方式：dev server 下经 `/src/...` dynamic import 真实 pane 模块（依赖 URL 从转换产物解析保证同实例），以生产注入缝提供 scope/双 shot 索引；P1 fixture 内嵌（40 stage objects：2 actor+16 joints+18 proxies+markers+ghost、8-keyframe camera、declaredCost 40/64）。实测（SwiftShader `ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (SubZero))`、1440×960、dev 模块图口径如实标注，run-to-run 有波动）：first 3D frame **2288–5286ms**（含 three 模块冷加载，dev 未压缩 ESM，四轮观测区间）；selection feedback **26–37ms <100ms gate PASS**（pointer 路径，aria-pressed 可见）；frame-time p50 **0.9–1.1ms**/p95 **2.8–5.9ms**/max ≤55.9ms（30 dirty-driven 帧）；long task >50ms **0 次**（PerformanceObserver longtask）gate PASS；heap 记录为 informational（GC 未强制，两轮 45.1→43.9MB / 48.3→47.7MB，disposal gate 以资源计数为准）；连续 3 次 shot switch + close → **4 个 disposed marks 全零计数**（rendererActive=false、geometry/material/texture/observer=0）。bundle delta：`spatial-replica-3d` 158.2 KiB gzip（zlib 口径；vite 报 160.91 kB）≤250 预算；three 不在任何其他 chunk（GLTFLoader 标记 + 静态 import 图闭合双查）；其余 chunk 相对 0.5 基线 delta 如实记录于 `temp/spatial-replica-bundle-budget.json`（主入口 463.59→510.05 kB gzip 为共享 dirty worktree 上本 change 1–5 波 + 并行 lane 共同贡献，未归因单一变更）。evidence 落盘：`temp/spatial-replica-performance-evidence.json` + `temp/spatial-replica-bundle-budget.json`（脱敏、0o600、temp/ 下）。命令：replica 段 exit 0、budget 段 exit 0；canvas 段（agent-spatial.spec.ts，非本任务面）在本环境 exit 1——`WORKBENCH_TOKEN_FILE` 缺失 + 并行 creative-production lane 在制品使 Spatial Surface 呈 fallback（introduced/pre-existing 分类：pre-existing/concurrent，v3 lane 已知 approved-host 依赖）。不声明 production SLO。

## 6. Inquiry、owner Panels 与 canonical action path

- [x] 6.1 实现 Anatomia typed spatial inquiry UI/SDK flow，返回 answer、Snapshot/bundle lineage、PTS/range、coordinate/unit、confidence、limitations、evidence refs；禁止持久化 raw prompt/reasoning/Provider payload，回答不直接 mutation。验收：unknown/insufficient evidence 不被 completion candidate 补成事实；命令：`cd apps/web && bunx vitest run spatial-replica-inquiry`；失败复查：缺 lineage/claim scope 时只显示 limited answer。 **Evidence (2026-09-01)**: `replica-inquiry.tsx`——Inspector 问询区（UI spec §3.2 SpatialInquiry）：presets 来自投影 `inquiry` capability（未投影诚实 needs_contract 不发 ask）、bounded question（max_question_length）、pending 禁双击；SDK typed 面 `spatialReplica.ask`（workspaceRef/shotRef/question/ptsRange/selectedRefs/expectedSnapshotRef）；结果呈现 status machine chip、answer（服务端 answer key 的 closed 本地映射，未注册 key 以 mono 引用并明示不猜测正文）、Snapshot/bundle lineage、PTS range（formatReplicaPts）、coordinate frame+unit、confidence、limitations codes、evidence refs；lineage/claim scope 缺失（无 bundle/PTS/coordinate）→ `data-replica-inquiry-limited` 只显示 limited answer；unknown/insufficient_evidence 不被补成事实（答案区不渲染）；错误只显示 envelope code；question 仅存 session-local state（不落日志/store，unmount 即弃）、回答纯 read 不触发 mutation/投影写入。测试 `test/spatial-replica-inquiry.test.tsx` 10 pass（typed request exact 断言/结果全字段/unknown 不补事实/limited/错误 code/pending 禁双击/answer key 未注册诚实/maxLength/Inspector 集成无额外投影读）。命令 PASS：`cd apps/web && bunx vitest run spatial-replica-inquiry` → 10 passed。
- [x] 6.2 实现 `agent.auctra-screenplay.v1`，只显示 Auctra approved metadata/text projection、canonical version、scene/beat/line refs、review/allowed actions/deep link；contract 不足时 truthful metadata-only/needs_contract。验收：无 iframe/private page/filesystem fallback；命令：`cd apps/web && bunx vitest run auctra-screenplay-pane`；失败复查：任何正文来自非 owner-safe contract 即阻断。 **Evidence (2026-09-01)**: `auctra-screenplay-pane.tsx` 6.2 完整内容：Scene→Beat→Line 树（`buildScreenplayTree` 按 parentRef 挂接 + orderKey 排序，`data-replica-screenplay-node/-depth`，本地不改写 structure）；owner truth header（ref/version/currentness/depth grant）；关联绑定区（episode/shot/storyboard/stage refs + screenplay_driven 对齐注记）；review/allowed actions（owner=auctra 的 server-authored inventory 行，无 action 诚实空态不伪造入口）；approved deep link（`replicaSafeDeepLinkUrl` 与 SDK allowlist 同规则二次校验：https/无 credential query/无端口/host+path prefix，未批准 → 拒绝渲染 href + redacted 注记，URL 不发送给浏览器）；metadata-only/needs_contract 沿 3.x 外壳真实呈现（无 iframe/filesystem/私有页面 fallback）。测试 `test/auctra-screenplay-pane.test.tsx` 14 pass（树层级嵌套/depth machine 值/binding 行/allowed actions+空态/deep link 批准与拒绝（evil host/http/错 prefix/token query/端口）/metadata-only 不显示正文/screenplay_driven 注记/纯函数树与 6 形 deep link 校验）。命令 PASS：`cd apps/web && bunx vitest run auctra-screenplay-pane` → 14 passed。
- [x] 6.3 实现 `agent.scaena-storyboard.v1`，展示 storyboard/SceneGEO/ReplicaStage/motion/review/freeze safe state，并明确 `reference_evidence`/`screenplay_driven`；Auctra stale 时不静默换 mode。验收：mode、binding version、stage currentness 和 allowed actions 可见；命令：`cd apps/web && bunx vitest run scaena-storyboard-pane`；失败复查：Workbench 不实现 Scaena domain fallback。 **Evidence (2026-09-01)**: `scaena-storyboard-pane.tsx` 6.3 完整内容：五 tab（Storyboard/SceneGEO/ReplicaStage/Motion/Candidates，`data-replica-storyboard-tab/-panel` 本地 presentation）；header+summary 呈现 mode/binding version/review state/scale/objects/budget/quality/currentness；`reference_evidence` 明确 evidence binding refs 不伪造 screenplay；`screenplay_driven` + Auctra binding 非 current → `data-replica-storyboard-screenplay-stale` 注记 + 依赖 current screenplay 的 review/freeze 动作 gated（`data-replica-action-screenplay-gated`，availability 呈现 stale）且 mode 不变（不静默切 reference mode）；ReplicaStage tab 列 objects（kind/truthLayer/ownerVersion/PTS）+ freeze safe state（frozenRef/stageVersion/receiptRef/frozenAt；未冻结诚实空态不提前显示冻结成功）；Motion 无投影诚实 none（不推断）；Candidates selected/provenance/quality。allowed actions 汇总行接 server-authored inventory（title 含 target/version/confirmation）。Workbench 不实现 Scaena domain fallback（stale/offline/needs_contract 走 3.x 外壳状态面）。测试 `test/scaena-storyboard-pane.test.tsx` 8 pass。命令 PASS：`cd apps/web && bunx vitest run scaena-storyboard-pane` → 8 passed。
- [x] 6.4 实现 `agent.owner-receipts.v1`，组合 Task/owner events并分别显示 owner sequence、workspace cursor、event/observed time、status/currentness/gap；不得宣称 global exactly-once。验收：interleaved/gap/late receipt fixtures 可 reconcile；命令：`cd apps/web && bunx vitest run owner-receipts-pane`；失败复查：gap 后 terminal state 必须保持 unknown。 **Evidence (2026-09-01)**: `owner-receipts-pane.tsx` 6.4 完整内容：`buildReceiptRows`（observed time 倒序浏览；同 owner 更新 observed 行 sequence 缺号 >1 → `data-replica-receipt-sequence-gap`；event time 早于同 owner 更早 observed 行最大 event time → `data-replica-receipt-late`，两遍扫描纯函数）+ 行级呈现 source owner、owner sequence、workspace cursor、observed/event 双时间（分列 `data-replica-receipt-observed/-event-time`）、status/currentness、Task/receipt ref、resource ref/version；来源 owner 汇总行；gapDetected → marker + 刷新投影对账入口 + 受影响行 terminal 保持 unknown（`data-replica-receipt-reconcile` reconcile-only 注记）；页脚明示按 observed time 浏览非 owner 全局事务序（不合成全局 exactly-once，不同 owner sequence 互不比较）。测试 `test/owner-receipts-pane.test.tsx` 8 pass（交错 receipt 分列/gap+unknown 保持/缺号+迟到 marker/Task-receipt 组合/来源汇总/纯函数 3 用例）。命令 PASS：`cd apps/web && bunx vitest run owner-receipts-pane` → 8 passed。
- [x] 6.5 实现 server-authored `SpatialReplicaActionDescriptorV1` resolver 与 UI control inventory，复用既有共享 `ActionDescriptorV1` 的 availability/target Operation/reason/recovery/scope，并将 evidence correction→Anatomia、stage/motion/completion/review/freeze→Scaena、screenplay action→Auctra、Agent intent→ProposalAuthority；浏览器不可构造 endpoint/action/permission。验收：base descriptor 与 owner binding 任一 unknown/stale/revoked/mismatch 都 fail closed，且不存在第二套 action authority；命令：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... -run 'Action|Descriptor|Route' -count=1 && cd apps/web && bunx vitest run spatial-replica-actions`；失败复查：owner switch 只能由 current wrapper canonical owner 决定。 **Evidence (2026-09-01)**: `replica-action-inventory.ts` 纯函数 resolver：`replicaActionRoute` closed 前缀映射（anatomia.evidence→Anatomia、scaena.stage 族（proxy/keyframe/retarget/completion/review/freeze）→Scaena、auctra.screenplay→Auctra、agent.→ProposalAuthority；未知前缀 null fail closed）；`resolveReplicaActionInventory` 复用共享 `ActionDescriptorV1` 的 availability（normalizeAvailability）/reason/recovery（零重定义），叠加 wrapper fail-closed 合成（base 非 ready 透传、过期→stale/descriptor_expired、路由 owner 与 wrapper owner 不一致→stale/owner_binding_mismatch——owner switch 只能由 current wrapper canonical owner 决定，浏览器不构造 endpoint/action/permission）；`replicaActionIdempotencyKey` 确定性 bounded key。UI control inventory：`replica-action-section.tsx`（主 Pane 动作区 + Inspector 经 `ReplicaWorkspaceDispatchContext` 子控制器渲染）：每行 actionId/owner 路由/availability chip/blocked cause，hover/focus 经 title+detail 行显示 target/expected version/confirmation/execution path/revision；`replicaActionsForTruthLayer`/`ReplicaTruthLayerActions` 把 Inspector 四层 truth stack 的 allowed action 行（5.x 预留位）接到真数据。测试 `test/spatial-replica-actions.test.tsx` 12 pass（路由/mismatch/过期与非 ready/proposal_then_task/idempotency key/UI 行+title+offline 禁用/Inspector 分层 action/诚实空态）。命令 PASS：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... -run 'Action|Descriptor|Route' -count=1` ok + `cd apps/web && bunx vitest run spatial-replica-actions` → 12 passed。
- [x] 6.6 实现 ephemeral typed edit draft、visible diff/base version/schema validation、navigation-away/Pane close/logout guard；draft 不进 Workbench canonical DB/localStorage。验收：dirty draft 不被 shot switch 静默丢弃或跨 shot 复用；命令：`cd apps/web && bunx vitest run spatial-replica-draft`；失败复查：schema 变化时进入 stale draft 而非自动迁移含义。 **Evidence (2026-09-01)**: `replica-workspace-state.ts` draft additive（descriptorRevision/inputSchemaRef/baseFields 快照）+ `replica-action-inventory.ts` `REPLICA_ACTION_FIELD_SPECS`（P1 已注册 action 的 closed typed fields；未注册 action id fail closed 无字段）+ `validateReplicaDraftInput`（required/type/maxLen/unknown field + SDK `validateSpatialReplicaActionInputJson` bounded JSON 同规则）；`replica-action-section.tsx` draft 面板：action/owner/target、base version+currentness、Not submitted/stale 标记、typed fields、validation issues、before/after diff 表（`data-replica-draft-diff-row`）、descriptor/schema/base version 漂移→`data-replica-draft-state=stale`（submit 禁用、diff 保留可读、不自动迁移含义）；guard：shot switch 走既有 planReplicaShotSwitch navigation_away（dirty 不静默丢弃、确认后清 per-shot draft 不跨 shot 复用）、dirty 时 beforeunload（浏览器 navigation）、Pane close/覆盖草稿显式确认（`data-replica-draft-close-guard` 丢弃/保留）、logout/scope revoke=session/resetAll 全清；draft 只存 session reducer（不写 canonical DB/localStorage/sessionStorage）。测试 `test/spatial-replica-draft.test.tsx` 11 pass。命令 PASS：`cd apps/web && bunx vitest run spatial-replica-draft` → 11 passed。
- [x] 6.7 将 direct user action 接入 server revalidation→TaskService→owner adapter，携带 exact target、expected version、idempotency key、confirmation 与 receipt；pending 时保留 last-confirmed projection，不 optimistic 修改 Frozen/current。验收：success/conflict/rejected/duplicate 均有对应 Task/owner receipt；命令：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... ./service/internal/core/... -run 'Replica|ExpectedVersion|Idempotency' -count=1`；失败复查：receipt 前任何 canonical UI 更新即失败。 **Evidence (2026-09-01)**: `replica-action-controller.ts` `useReplicaActions`：submit 只携带 server-authored descriptor 的 bindingRef/actionId/descriptorRevision/targetRef/expectedOwnerVersion + 确定性 idempotency key + bounded inputJson，经 SDK `executeAction`（TransportCallOptions.idempotencyKey 由 SDK 注入）→ server revalidation→TaskService→owner adapter；confirmation none 直接提交、confirm/review_required 走审阅差异确认块（target/expected version/idempotency key 预览/receipt contract）；accepted/duplicate → onCanonicalRefresh（invalidate 共享 workspace query，fresh projection 呈现 owner 确认版本），rejected/conflict 保持 last-confirmed 投影不重取（无 optimistic canonical 修改，pending 仅本地指示+已提交 diff）。主 Pane `ReplicaRegions` 挂动作区（refreshCanonical 注入缝）。测试 `test/spatial-replica-actions.test.tsx` submit 流 4 用例（exact request 断言/确认块/idempotency key 预览/conflict 不刷新/localStorage 空）。命令 PASS：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... ./service/internal/core/... -run 'Replica|ExpectedVersion|Idempotency' -count=1` ok + vitest（同 6.5 文件）。
- [x] 6.8 实现 duplicate pending detection 与 `unknown_accept` reconcile；连接中断后禁用等价重提，只能用原 idempotency/Task/receipt/current projection 收敛。验收：owner accepted+response lost fixture 只产生一次 mutation；命令：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... -run 'Duplicate|UnknownAccept|Reconcile' -count=1 && cd apps/web && bunx vitest run spatial-replica-reconcile`；失败复查：自动 retry 或 fabricated cancelled/failed 均阻断。 **Evidence (2026-09-01)**: `replica-action-controller.ts`：duplicate pending detection（`replicaDuplicateKey`=binding+action+target+base version+input digest；非 terminal 提交在途时 submit 返回 duplicate_local 不触达 transport——ref/state 双写保证 re-render 前连续调用可见）；transport 失败/response loss → unknown_accept（reasonCode response_lost，不推断成功/失败、不伪造 cancelled/failed），保留原 idempotency key，等价重提禁用；reconcile 只查原 task ref+原 key（`reconcileAction`），无 task ref 时禁用并诚实等待投影/回执回流；resolved→terminal 呈现+ownerVersion+canonical 收敛（fresh projection），unresolved 保持 unknown 可再 reconcile；duplicate 响应呈现 existing Task 不建第二个 mutation；`replicaSubmissionInFlight` 纯函数（submitting/accepted 未 reconcile/duplicate/unknown 在途；rejected/conflict/accepted+resolved terminal）。receipts pane gap 行保持 unknown（联动 6.4）。测试 `test/spatial-replica-reconcile.test.tsx` 9 pass（response lost 单次 mutation 尝试/reconcile 原语义+resolved 收敛/未收敛可再查/单次断言 duplicate/gap unknown/controller hook duplicate_local）。命令 PASS：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... -run 'Duplicate|UnknownAccept|Reconcile' -count=1` ok + `cd apps/web && bunx vitest run spatial-replica-reconcile` → 9 passed。
- [x] 6.9 将 Agent 生成与高风险 action intent 接入现有 ProposalAuthority；proposal approval 仅授权后续 current descriptor revalidation，不直接改 owner state。验收：hover/focus/replay 不触发 proposal accept 或 owner mutation；命令：`CGO_ENABLED=0 go test ./service/internal/proposalauthority/... ./service/internal/spatialreplica/... -run 'Replica|Proposal' -count=1 && cd apps/web && bunx vitest run spatial-replica-proposal`；失败复查：ProposalAuthority 与 TaskService 状态机不得重复。 **Evidence (2026-09-01)**: `replica-proposal.tsx`（`ReplicaProposalSummary` server-authored 投影 + `useReplicaProposalFlow` decision hook + `ReplicaProposalCard` 只读审阅卡）与 `replica-action-section.tsx` proposal 区：proposal_then_task/agent 前缀 action 无 direct submit 入口（`data-replica-action-proposal-required`）；卡片呈现 proposalRef/risk/impact/evidence refs/diff（actionId@expected version）/owner target/requiredDecision；decision 走既有 ProposalAuthority `decideProposal` typed seam（与 change-set-review 同形状，主 Pane `proposal`/`proposalActions`/`expectedProposalRevision` 注入缝；不重复 TaskService 状态机）；approve 只调 decideProposal（确定性 `sra-proposal-<digest>` key），不调 executeAction、不重取投影（approval 仅授权后续 current descriptor revalidation——owner state 只由 Task/receipt 改写）；deciding 禁 replay；decision unknown 仅 reconcile 语义（不自动重试不换 key）；hover/focus/mouseOver 不触发 decision。测试 `test/spatial-replica-proposal.test.tsx` 7 pass。命令 PASS：`CGO_ENABLED=0 go test ./service/internal/proposalauthority/... ./service/internal/spatialreplica/... -run 'Replica|Proposal' -count=1` ok + `cd apps/web && bunx vitest run spatial-replica-proposal` → 7 passed。

## 7. Responsive、accessibility、i18n 与视觉状态

- [x] 7.1 按 UI Spec 实现 `>=1800` simultaneous、`1440–1799` main tabs + one auxiliary Pane、`1024–1439` labelled Sheet、`<1024` no-full-3D substitute；不得用任意 resize heuristic 改变 canonical state。验收：五个固定 viewport 无页面级水平溢出；命令：`cd apps/web && bunx playwright test --grep 'spatial replica responsive'`；失败复查：窄屏缺少 review/reconcile 等价路径即失败。 **Evidence (2026-09-01)**: 断点合同落在 `replica-responsive.ts`（`REPLICA_BREAKPOINTS` 1800/1440/1024 + `replicaLayoutTierForWidth` 纯函数 + `useReplicaLayoutTier`/`useReplicaMediaQuery` matchMedia hook）；主 Pane `replica-review-pane.tsx` 按 tier 分支：simultaneous（source/3D 并排 `min-[1800px]:grid-cols-2`，tablist 收起为 sr-only 说明）、tabs（3 view tabs + timeline 常驻，既有行为）、sheet（labelled Sheet tablist 四 tabs：Source/3D/Timeline/Inspector，`data-replica-view-tabs-mode="sheet"`）、mobile（viewport 窄门 substitute + `replica-inspector-summary.tsx` 摘要/owner deep link/动作区锚链接）。分支 presentation-only：tier 翻转不 dispatch canonical（测试断言 selection 跨 1920→390→1920 保留）；无第二 shell（唯一 `[data-replica-pane]`）。命令 `cd apps/web && bunx playwright test --grep 'spatial replica responsive'` → 7 passed（五 viewport 0 整页水平溢出；宿主页注入真实 tokens/motion/styles.css，量真实盒模型）；组件侧 `spatial-replica-responsive.test.tsx` 12 tests（断点边界/zoom 有效宽/tier 逐档/迁移不变式）。
- [x] 7.2 完成 keyboard navigation、visible focus、landmark/heading/ARIA、dialog/sheet trap/Escape/return、desktop complementary no-trap、DOM object mirror 与 live announcement。验收：keyboard-only 可完成导航→select→inspect→review/reconcile，Axe serious/critical 为零；命令：`cd apps/web && bunx playwright test --grep 'spatial replica accessibility'`；失败复查：focus 丢失或 canvas-only control 阻断。 **Evidence (2026-09-01)**: `replica-focus.tsx`（`ReplicaFocusDialog`：Tab/Shift+Tab trap + Escape + initial focus 安全项 + close return focus）接管 draft 确认与 close guard（`replica-action-section.tsx`）；pane 级键盘路径（Space/←→/Shift+←→/Home/End//）在 `replica-review-pane.tsx`（输入框/select 不抢键；timeline 区已处理键经 defaultPrevented 去重；`nextReplicaTimelineEvent` 共享事件跳转语义）；`replica-live-announcer.tsx` polite live region 宣告选择/相位，revoke/permission 收敛时焦点移回可聚焦 safe heading（`data-replica-pane-heading`）；事件列表行改 `role=option`+`aria-selected`（原 `role=listitem`+`aria-pressed` 违反 aria-allowed-attr，axe critical）。命令 `--grep 'spatial replica accessibility'` → 4 passed：keyboard-only 导航→select→inspect→review 全程无 pointer、dialog trap/Escape/return 实测、desktop complementary no-trap（有界 Tab 遍历离开 pane 到 sentinel）、Axe（@axe-core/playwright）default 状态 1920 与 390 critical/serious=0。组件侧 `spatial-replica-keyboard-a11y.test.tsx` 8 tests。
- [x] 7.3 支持 200% zoom、reduced motion、touch target 与 drag/orbit/resize 的 toolbar/menu/numeric/keyboard substitute；WebGL unavailable 时保持 object/event list。验收：zoom/reduced-motion fixture 无不可达 control 或依赖动画的状态表达；命令：`cd apps/web && bunx playwright test --grep 'spatial replica zoom|spatial replica reduced motion'`；失败复查：不得通过禁止 zoom 解决布局。 **Evidence (2026-09-01)**: reduced-motion：`useReplicaPrefersReducedMotion` + pane/viewport/timeline `data-replica-*-reduced-motion` 标记；事件跟随滚动（`virtualizer.scrollToIndex`）按 reduce 用 `behavior:"auto"`（默认 smooth）；adapter 本无缓动（camera/选择即时），状态以文本/ARIA 表达。200% zoom：CSS 像素视口减半经 matchMedia 重估同一断点分支（`replicaEffectiveZoomWidth` 纯函数 + e2e 半宽等价口径 1920@200%→960/mobile、2880@200%→1440/tabs、2560@200%→1280/sheet，无整页溢出、submit/reconcile 控件可达未禁用）。touch target：sheet/mobile tier 控件 44px（`data-replica-touch-targets` + styles.css unlayered 下限，e2e 量 boundingBox ≥44px@768/390；桌面保持密度）；orbit/zoom/resize 的 toolbar/menu/numeric/keyboard 替代沿 5.x 三路径，7.x 补 zoom/reduced-motion/touch 断言。WebGL unavailable/capability off：对象/事件列表与 DOM mirror 保留（`spatial-replica-zoom-motion-touch.test.tsx` 9 tests）。命令 `--grep 'spatial replica zoom|spatial replica reduced motion'` → 6 passed。
- [x] 7.4 为所有 defined state 建立 deterministic component stories/fixtures，包括 partial+unknown_accept 组合、dirty draft、limit reached、sync degraded、view too large、WebGL loss。验收：state matrix 每行有 fixture、copy 与 recovery action；命令：`cd apps/web && bunx vitest run spatial-replica-state-matrix`；失败复查：新增状态必须先更新 UI Spec/spec。 **Evidence (2026-09-01)**: `spatial-replica-state-matrix.test.tsx`（8 tests）以既有 fixture 测试模式（repo 无 storybook）建 deterministic stories：17 行逐行（machine 值 + 三段文案 + 有主恢复动作的状态渲染按钮）、partial+unknown_accept 组合（workspace stale/partial 卡 + Auctra offline segment 独立呈现 + 回执 Pane unknown_accept reconcile-only，不折叠为单一 generic error）、dirty draft（not_submitted + diff + close/navigation guard 双路径）、limit reached（copy+recovery、内容不透传不自动替换）、sync degraded（source region notice + 刷新投影 recovery + 结构保留）、view too large（budget 超限数值如实 + object list 替代 + owner 深链 + 不挂 canvas）、WebGL loss（webgl_unavailable + 列表/搜索/重试渲染器）。story 身份回链 fixture/contract version（`workbench.spatial_replica_workspace_projection.v1alpha1`）。命令 `cd apps/web && bunx vitest run spatial-replica-state-matrix` → 8 passed。
- [x] 7.5 将所有 human-facing label、truth/recovery、confirmation 与 a11y text 加入现有 zh-CN/en-US modular catalogs；machine enum/ref/action/error 保持 English，不以 display text 驱动逻辑。验收：locale parity、placeholder 和 protected truth copy gate 通过；命令：`bun run compose:i18n && bun run check:i18n && bun run test:i18n`；失败复查：代码注释按 `AGENTS.md` 使用必要的英文 invariant comment，中文只用于用户文档/界面，不写双语逻辑常量。 **Evidence (2026-09-01)**: 审计 6.x 后全部 human-facing 文案：272 个在用 `agent.replica*` 键全在 zh-CN/en-US `agent/replica-review.json`（6.x 已入 337）；补 7.1/7.2/7.3 新增 18 键（`agent.replica.{paneHeading,live.*,summary.*,view.{timeline,inspector,sheetTabsAria,simultaneousNote},draft.closeGuardAria}`）→ 每 locale 355 键；catalog-policy.json 先手工登记（sorted insert，placeholders 与两侧文案一致）再 compose。machine enum/ref/action/error 保持 English（状态名/truth layer/actionId/contract version 不翻译、不驱动逻辑）。`bun run compose:i18n` → OK（3419 keys）；`bun run test:i18n` → 5 passed；`bun run check:i18n` → agent.replica 缺失 0（既有非 replica 红为并行 lane 基线）。
- [x] 7.6 建立 1920×1080、1440×900、1280×800、768×1024、390×844 screenshot matrix，覆盖 default、partial/stale、selection、draft、confirmation、unknown accept、WebGL failure、mobile substitute；截图使用确定性 safe fixture。验收：每张 artifact 可回链 UI Spec state 与 fixture/contract version；命令：`cd apps/web && bunx playwright test --grep 'spatial replica visual matrix'`；失败复查：截图中不得出现 credential/private path/raw prompt/Provider payload。 **Evidence (2026-09-01)**: `e2e/spatial-replica-visual-matrix.spec.ts` + 共享 harness `e2e/spatial-replica-mount.ts`（dev-server 真实 pane 模块 + 真实样式链 + route.fulfill safe fixture；SwiftShader 软渲染口径如实标注）。矩阵：5 viewport（1920×1080/1440×900/1280×800/768×1024/390×844）× state（default、partial/stale、selection、dirty draft、confirmation、unknown accept、WebGL failure[窄屏 skip=宽度门本就替代]、mobile substitute）→ 34 张 PNG 落 `temp/spatial-replica-visual-matrix/`（0600，不入库）+ `manifest.json`（每 artifact 记录 viewport/state/PNG 尺寸/字节数 + contractVersion `workbench.spatial_replica_workspace_projection.v1alpha1` + fixture id + 命令 + SwiftShader 注记）；manifest 测试校验 PNG IHDR 宽=viewport。fixture 无 credential/private path/raw prompt/Provider payload。命令 `cd apps/web && bunx playwright test --grep 'spatial replica visual matrix'` → 35 passed, 2 skipped（确定性：固定 fixture/视口/动作序 + `animations:"disabled"`）。

## 8. 分层测试、安全与 integration evidence

- [x] 8.1 完成 Go domain/service/adapters/transport 单元与 integration tests，覆盖 exact join、partial/freshness、scope isolation、budget、cache fence、cursor gap、backpressure、action routing、expected version、idempotency、duplicate/unknown/reconcile。验收：CGO0 与 focused race 通过；命令：`CGO_ENABLED=0 go test ./service/internal/spatialreplica/... ./service/internal/transport/... -count=1 && CGO_ENABLED=1 go test -race ./service/internal/spatialreplica/... -count=1`；失败复查：race/timeout 需保存最小复现，不以重跑掩盖。 **Evidence (2026-09-01)**: 收口核对：2.x 已覆盖 exact join/partial/freshness（service_test Compose*）、cache fence（cache_test + RevalidateActionFenceBypassesStaleCache）、cursor gap/resync + backpressure + 单 log 多订阅（stream_test）、action routing/expected version/idempotency/duplicate/unknown/reconcile（action_test 6.7/6.8）、budget（contract 包）、capability rollback（capability_test）。本波补齐缺口：①`scope_isolation_test.go`（transport 级，2 测试）——跨 principal（同租户不同 subject/跨租户不同 subject/未认证）在 HTTP/gRPC/JSON-RPC 三面 fail closed（403 permission_denied ↔ PermissionDenied ↔ -32003；未认证 400 identity_mismatch ↔ InvalidArgument ↔ -32602，URL query 伪 cohort 不提权；cohort principal 三面可用对照）+ media resolve 跨 workspace 复用/非 preview namespace/path 注入全部 bounded 拒绝且不回显 upstream；②`TestSpatialReplicaSecurityRedactsOwnerFailureDetail`——owner 原始错误（credential/private path/provider URL sentinel）不进投影 wire；③`TestComposeDoesNotMutateOwnerReturnedSource`（race 回归）。**实现 bug 最小修复（真 DATA RACE）**：`composition.go` readOwnerSegment 曾直接写 owner 返回的共享 source 消息（MediaPreview 附加），与 stream pump 并发 compose 构成数据竞争——race detector 在 `-count=2` 复现（最小复现记录：integration evidence run `20260901174041` 同窗口；直接复现日志 /tmp/race-run-2.log 样例）；修复 = 附加 preview ref 前 `proto.Clone`（owner outcome 视为 read-only 输入），回归测试证明 buggy 变体 DATA RACE+断言失败、修复后绿。次要修复：`stream_test` backpressure 用例在并行满载（evidence run 观测 ~10x 减速）下快订阅排空 goroutine 被饿死导致假红——注入节奏 5ms→10ms + 终态等待 2s→5s（语义不变：慢订阅零消费仍溢出终止，run 20260901174041 失败证据保留）；missionbriefgrpc 测试缺 testify 依赖阻塞 `./internal/transport/...` 接受命令——`go mod tidy` additive 补入（并行 lane 遗留）。命令双 PASS（CGO0 spatialreplica+transport 全绿、race 全绿）；evidence：integration run `20260901174213-051bbf1e-0a69-442c-b091-860c68253005`（passed）。
- [x] 8.2 完成 SDK contract/parity tests，覆盖 HTTP/gRPC/JSON-RPC、safe normalization、preview/numeric type boundary、stream resume、future enum 与 redaction。验收：四 transport 同输入输出等价；命令：`bun test packages/task-sdk/test/spatial-replica-*.test.ts tests/conformance/spatial-replica-transports.test.ts`；失败复查：compat 差异在 contract/service 层修复。 **Evidence (2026-09-01)**: 新建 `tests/conformance/spatial-replica-transports.test.ts`（4 测试，SDK 消费面真实 workbenchd spawn）：①default-off 服务器上 read/watch × HTTP/JSON-RPC 四条调用路径等价 fail closed + capabilities 只读快照 default-off 如实；②02 §8 冻结 15 个 wire 错误码穿过两个 SDK transport 归一化不降级 internal（HTTP `{error:{code}}` closed-domain 形态 + JSON-RPC data.code 形态）+ JSON-RPC transport-code-only 诚实降级矩阵（-32602→invalid_argument、-32013/-32003/-32001 无 data.code→internal，transport code 跨 namespace 复用不构成稳定码）；③flags-on + exact-principal cohort 服务器上非 cohort principal（local token 无 subject）read/mutation 等价 fail closed + capabilities 快照 enabled 证明 flags 生效；④未知字段注入 HTTP 面 400 contract_mismatch ≡ JSON-RPC /rpc wire data.code contract_mismatch（真实 wire 断言）。既有覆盖核对：gRPC 面 parity 由 Go `TestSpatialReplicaTransportParityWire`（2.6）承担；safe normalization/future enum 前向兼容/preview vs numeric 边界（models/conformance fixtures 27 测试）与 stream resume/resync envelope（client.watchWorkspace + http-routes SSE 5 测试）本波全量复跑。**实现 bug 最小修复（3 处，均由新测试证明）**：①SDK StableErrorCode 词表缺 10 个冻结码（capability_disabled/identity_mismatch/stale_projection/revoked/sync_degraded/view_too_large/duplicate_action/unsupported_version/offline/spatial_replica_unavailable）→ 归一化降级 internal（models.ts additive）；②http.ts errorEnvelope 只认带 message 的对象形态——closed-domain `{error:{code}}` 无 message 时丢失 typed code（补第三分支：稳定码在场即保留，message 退化为 code）；③JSON-RPC `mapSpatialReplicaJSONRPCError` 缺 `*contract.Error` 分支（HTTP 有）——identity_mismatch 等精确码落入 sentinel default 丢失；`decodeSpatialReplicaParams` 硬编码 invalid_argument——data.code 现保留 canonical codec 精确码（transport code -32602 不变，冻结表 02 §8 口径）。命令 PASS：31 tests（27 SDK + 4 conformance）0 fail；`bun run test:contract` 530 pass 0 fail；evidence：integration run `20260901174213-051bbf1e-0a69-442c-b091-860c68253005`（passed）。
- [x] 8.3 完成 React/Vitest/Testing Library/MSW component tests，覆盖 Pane registry、PTS、overlays、timeline virtualization、viewport lifecycle、Inspector、owner panels、draft/action/reconcile、focus 和 state matrix。验收：测试不依赖真实 Provider/owner runtime且无隐式 mock fallback；命令：`bun run --cwd apps/web test`；失败复查：production code 默认 mock 或 test-only authority 必须移除。 **Evidence (2026-09-01)**: 覆盖矩阵核对（tasks 列举面 → 测试文件）：Pane registry=`agent-pane-registry`（17，含 unsafe params/unknown version fail closed）、PTS=`spatial-replica-pts`（16，含「禁 time×帧率」结构性守卫）、overlays=`spatial-replica-overlays`（5）、timeline 虚拟化=`spatial-replica-timeline`（10，2000+ samples 仅实例化窗口行）、viewport lifecycle=`spatial-replica-viewport-lifecycle`（13，dispose 幂等/资源归零/capability off 收敛）、Inspector=`spatial-replica-inspector`（10，四层 truth/claim ladder）、owner panels=`auctra-screenplay-pane`+`scaena-storyboard-pane`+`owner-receipts-pane`、draft/action/reconcile=`spatial-replica-actions`（12）+`spatial-replica-draft`（11）+`spatial-replica-reconcile`（9）+`spatial-replica-proposal`（7）、focus=`spatial-replica-keyboard-a11y`（8）+`spatial-replica-states`（31）+`spatial-replica-state-matrix`（8，17 行逐行）——replica 家族 27 文件全部在库（3.x–7.x 各波交付）。本波补缺：①`auctra-screenplay-pane` +1 XSS label 渲染断言（15 测试）——hostile safeText（`<img onerror>`/`<script>`/`javascript:`）以文本节点呈现、零 script/img 元素挂载、全局无执行痕迹；②`spatial-worker-kernel`（并行 r4 lane 已提交用例）在全套件并行负载下二次 dynamic import 超 5s 默认超时假红——per-test 超时提到 20s（断言不变，test-only）。mock 纪律核对：全部组件测试经 vi.spyOn typed client/QueryClient seed/受控 fetch mock——无真实 Provider/owner runtime 依赖、production code 无默认 mock 路径。命令 PASS：`bun run --cwd apps/web test` → vitest 194 files/1643 tests 全绿 + bun server 147 pass 6 skip；evidence：component run `20260901173934-7826e3b4-a7a8-4f35-873b-21b6ffe6c5b2`（passed）。
- [x] 8.4 完成 Playwright critical paths：desktop review、VFR sync、keyboard/a11y、tablet/mobile substitution、unknown accept reconcile、capability revoke、WebGL loss 与 visual matrix；仅覆盖关键 e2e，不把所有单元场景升级成 browser test。验收：固定环境 zero unexpected console/network failure、Axe serious/critical zero；命令：`bun run web:e2e`；失败复查：保留 trace/screenshot 到诊断目录并分类 introduced/pre-existing/environmental。 **Evidence (2026-09-01)**: 新建 `e2e/spatial-replica-critical-paths.spec.ts`（5 测试，复用 `spatial-replica-mount.ts` dev-server 真实 pane 模块模式；harness additive 扩展 `projectionByShot` per-shot 投影 + `viewport3dEnabled` capability 覆盖）：①desktop review 全链——shot 17→18 精确 ref 导航（投影 exact shotRef 回流）→ viewport DOM mirror 对象选择 → selection strip + identity 键 → typed action 草稿（字段/diff/valid）；②VFR sync——逐帧 interval 累积步进（100/101/104 非均匀 → .003/.006/.010 非等差时间码，均匀帧率会给 .003/.006/.009）+ 声明 gap snap（.010 → 4.000 跳过 90000–120000）+ cursor kind 诚实 requested + 事件行 exact PTS/provenance；③unknown accept reconcile——execute 返回 unknown_accept+task ref → 仅可 reconcile 不可重试、等价重提 duplicate_local 零第二次 transport 调用、reconcile 复用原 idempotency key 且收敛 terminal；④capability revoke——on→off（visibilitychange 触发 always refetch，TanStack v5 focusManager 只听 visibilitychange）→ viewport 收敛 capability_disabled、canvas 卸载、对象树/时间轴存活；⑤rollback rehearsal（8.7 共用）——capability off from start 网络 断言零 `spatial-replica-3d` chunk 请求。7.x 四 spec 复跑全绿（keyboard/a11y=accessibility 4、tablet/mobile substitution=responsive 7、zoom/reduced-motion 6、visual matrix 35+2skip、WebGL loss=visual matrix webgl failure 行 + lifecycle 组件 13）；Axe serious/critical=0 沿 7.2。`bun run web:e2e`（全套件）：184 passed/11 skipped/7 failed——7 个失败全部非 replica 面、分类 concurrent（agent-direct-panes CLI palette 条目应为禁用实为 enabled：committed bb7e048 `cli: true` availability 翻转与并行 r3-gates/daily-ops lane 的 dirty diff（use-agent-pane-availability dailyOps 翻转、agent-conversation-workspace 重复 Conversation 文本、pi-workspace rail 在制品）所致；本 session 未触碰任何相关文件；trace/screenshot 已留存 temp/playwright-results/）。replica 面 e2e 全绿：scoped run 57 passed（4+7+6+35+5）；evidence：e2e run `20260901174256-f8fb5ea4-9506-4b50-95d8-8da094105d82`（passed）+ 全套件 web:e2e run `20260901174559-b9552a0e-2636-4224-b864-5cb75b74b628`（failed：184 passed/11 skipped/7 concurrent 分类如上，失败证据 schema-valid 保留原退出码）。
- [x] 8.5 完成安全测试：cross-tenant/project/episode/shot、SSRF/redirect/header/range、unsafe deep link、unknown field、oversize payload、XSS label、credential/private path/raw prompt/Provider sentinel、stale/revoked descriptor 与 browser direct-owner denial。验收：所有 failure 返回 bounded redacted code且日志/evidence 无 sentinel；命令：`bun test tests/security/spatial-replica-security.test.ts && CGO_ENABLED=0 go test ./service/internal/spatialreplica/... ./service/internal/transport/... -run 'Security|Isolation|SSRF|Redact' -count=1`；失败复查：发现泄漏先停止 evidence publication并清理诊断 artifact。 **Evidence (2026-09-01)**: 新建 `tests/security/spatial-replica-security.test.ts`（6 测试 70 断言，bun）：①10 种 hostile ref（http/带 token query/file 绝对路径/`/etc/passwd`/SMB/javascript/ref 带 query/fragment/userinfo/超长）× 4 个 scope 字段（workspace/shot/project/episode）全部被 SDK 投影 normalizer fail closed；②label 超 maxLabelChars 拒绝 + 合同内 hostile label（`<script>`）保持 inert string 且不改变对象身份；③unknown field 与集合超 maxSegments fail closed；④cross-scope join 纪律——exact owner ref 之外（同 label/同尾缀/跨 episode 不带 episode ref/URL 形态）一律不命中；⑤deep link 12 种 hostile 形态（http/evil host/错 prefix/query token/端口/userinfo/fragment/javascript/file/scope 穿越路径/非法 label key/超长）全拒；⑥BFF media route 前缀独占（`media-evil`/`../media`/v1alpha1 混淆全拒）。Go 侧（-run 匹配面）：`TestSpatialReplicaScopeIsolationAcrossPrincipalsAtTransport`（跨 principal 三 transport）+ `TestSpatialReplicaMediaResolveScopeIsolationAtTransport`（跨 workspace 复用/非 preview namespace/未知 ref/path 注入，无 upstream 回显——SSRF 面 transport 级）+ `TestSpatialReplicaSecurityRedactsOwnerFailureDetail`（owner 错误 credential/private path/provider sentinel 不进 wire）。既有分层复跑：BFF media proxy 8 测试（SSRF origin allowlist/redirect 拒绝/header 最小集/multi-range 与超限 range/method/query/body）、contract sentinel 11×9 矩阵（credential/private path/raw prompt/Provider payload）、stale/revoked descriptor fail closed（capability_test 2.8）。XSS 渲染面：auctra pane vitest +1（见 8.3）。命令双 PASS（bun 6 pass 0 fail；Go 匹配包全 ok）；evidence：component run `20260901173934-7826e3b4-a7a8-4f35-873b-21b6ffe6c5b2` 含 security 面，日志/证据无 sentinel（runner redaction 报告 0 泄漏）。
- [x] 8.6 使用项目 runner 为 component/integration/e2e 写入 `temp/integration-test-runs/<run-id>/summary.json`、`command.txt`、`stdout.log`、`stderr.log`、`env.json`、`artifacts/`，保留原退出码并脱敏。验收：成功与失败 run 都有 schema-valid evidence，且只声明 local fixture 层级；命令：`bun run test:integration && bun run web:e2e`；失败复查：缺 evidence 或日志含敏感字段时 run 不可用于晋级。 **Evidence (2026-09-01)**: Taskfile 新增三个 scoped evidence 任务（`test:spatial-replica:component/integration/e2e`，全部经 `scripts/test-evidence/run.ts` 六件套 runner）。run-id 清单（六件套 + digest.json/redaction.json/receipt.json 齐备、原退出码保留、redaction 报告 0 敏感泄漏）：component `20260901173934-7826e3b4-a7a8-4f35-873b-21b6ffe6c5b2`（passed：replica vitest 家族 + agent-pane-registry + SDK + security + media proxy）；integration `20260901174213-051bbf1e-0a69-442c-b091-860c68253005`（passed：Go spatialreplica/adapters/transport 全树 CGO0 + focused race + SDK/conformance parity）；e2e `20260901174256-f8fb5ea4-9506-4b50-95d8-8da094105d82`（passed：replica Playwright 57 tests，SwiftShader 软渲染口径如实标注）；失败 run 亦 schema-valid 保留：integration `20260901174041-ab4ffdd7-cd33-4e2f-bd1a-d68116d9e1e3`（failed：backpressure 满载假红最小复现，修复后复绿）与全套件 web:e2e `20260901174559-b9552a0e-2636-4224-b864-5cb75b74b628`（failed：7 个 concurrent 分类，见 8.4）。验收命令对：`bun run test:integration` → run `20260901174435-0105afe9-42b0-4a70-b3e7-d24cbb8bd158`（passed：service/test/conformance + internal/runtime）&& `bun run web:e2e` → run `20260901174559-…628`（exit 1，concurrent 失败已分类并保留原退出码——失败 run 的 evidence 完整性即本任务验收面）。层级声明：全部 run 仅声明 local fixture/loopback 层级（dev-server + deterministic safe fixture + :memory: sqlite），无任何 production/staging 声明。
- [x] 8.7 执行 capability on/off、stream stop、draft/cache/renderer cleanup、ordinary `/agent` regression 与 no-Three-load rollback rehearsal。验收：关闭 change 后 conversation、existing Panes、Task/proposal/receipt 保持可用且无 destructive migration；命令：`bun run typecheck && bun test && bun run build`；失败复查：rollback 依赖删除 owner state 或数据库降级时重新设计。 **Evidence (2026-09-01, 收口复跑后勾选)**: 前次未勾选根因（外部红灯）已解除：归档 lane 协调者已从 `canonicalRequirementChangeIDs`（service/cmd/workbench-release/requirements.go）移除已归档的 `workbench-owner-backend-integrations` 行（line 337 残留引用仅为 requirementOwnerRef 查找表、与 canonical 列表无耦合，无影响）。命令链复跑全绿：`bun run typecheck` exit 0（根 tsc + apps/web typecheck）；`bun test` 853 pass/8 skip/0 fail（144 files，含 release-state 恢复绿；先前 851 pass/1 fail）；`bun run build` exit 0（CGO0 四二进制 workbenchd/workbench-migrate/workbench-backup/workbench-restore-verify）。断言面核对（均已在库并在 8.x 波全绿，本波逐文件复核）：capability on/off + stream stop（Go `CapabilityRollbackOnToOffConvergesUnavailable`/`ShutdownStopsStreamsAndFailsClosed`/`CapabilityActionRevokeFailsClosedOnOldDescriptor`；e2e revoke 用例 on→off viewport 收敛 capability_disabled、canvas 卸载、对象树/时间轴存活）；draft/cache/renderer cleanup（`spatial-replica-viewport-lifecycle` 13 测试 dispose 幂等/资源归零/shot 切换旧 adapter dispose；e2e revoke 后 `[data-replica-pane] canvas` count=0）；no-Three-load rollback rehearsal（`spatial-replica-critical-paths.spec.ts:192`——capability off from start，page.request 网络 断言零 `spatial-replica-3d` chunk 请求 + review 链 shot 导航/对象选择可用，run `20260901174256-f8fb5ea4-9506-4b50-95d8-8da094105d82` passed）；ordinary `/agent` regression 断言在库确认——conversation（agent-first.spec.ts 三视口 conversation-first workspace + Message Agent composer + thread history）、existing Panes（agent-direct-panes.spec.ts：work items/project workspace/workflows pane 打开与真实 server 行）、Task/proposal/receipt（agent-direct-panes automation happy path 观察 Server receipt + unknown run 仅 reconcile + receipt:unknown 呈现；agent-spatial.spec.ts review inspector "No pending proposal" 空态）——8.4 全套件 run `20260901174559-…628` 中该面除 7 个 concurrent 失败外全绿（分类见 8.4，均源自并行 lane dirty diff）；无 destructive migration 复核（spatialreplica 零 migration 文件——untracked migration_* 属并行 lane（canvas_v3/projection_lifecycle/projection_retry/task_authority/migrationcompat），default-off env capability flag + additive proto/SDK/Pane，rollback=关 flag 零 owner state 依赖）。

## 9. 最终验证、独立审阅与 owner handoff

- [x] 9.1 在稳定 diff 上运行格式、生成、schema 与 strict OpenSpec gates；不得为清理无关 dirty-worktree failure 修改其他业务逻辑。验收：change 和全仓 OpenSpec valid、`git diff --check` clean；命令：`buf lint && openspec validate workbench-spatial-replica-review-v1 --strict && openspec validate --all --strict && git diff --check`；失败复查：分类为 introduced/pre-existing/concurrent/environmental 后只修本 change。 **Evidence (2026-09-01)**: 稳定 diff 上五门全过：①`buf lint` exit 0（零 finding，含新包 workbench/spatialreplica/v1alpha1）；②生成物一致性——`buf generate` 复跑前后 service/gen 全部 46 个 .go 文件 md5 逐一相同（byte-identical，非仅时间戳），结论 gen 与当前 proto 完全同步（tracked M 的 agent.pb.go/spatial.pb.go 等为先前波次 proto append 的已再生残留、非本波缺口；`?? service/gen/workbench/spatialreplica/` 为本 change 新增 gen）；③`openspec validate workbench-spatial-replica-review-v1 --strict --no-interactive` valid；④`openspec validate --all --strict --no-interactive` 全仓 60 passed/0 failed；⑤`git diff --check` 全 tracked diff clean（零 whitespace/conflict marker，scope ≥ 本 change 触碰路径）；⑥gofmt——本 change 自有 Go 面（service/internal/spatialreplica、adapters/spatialreplica_fixture*、transport/spatialreplicahttp、transport/spatialreplicagrpc、gen/workbench/spatialreplica）全 clean，其中修复一处 introduced 纯格式问题（service.go Service struct 字段对齐 padding，`gofmt -w` 零逻辑变化）后 clean；仓内另余 6 个未格式化文件全部分类 concurrent 未动：backup.go（GA-R5 4.2 lane tracked M）、canvas_v3_test.go/dailyopshttp views.go/worker_service_identity_test.go/operations connector.go（并行 lane untracked）、temp/diagnostics/spatial-pa-submit（r4 lane，temp/ 独立 module）——遵「不改他 lane 业务逻辑/在制品」纪律未代格式化。
- [x] 9.2 运行最终项目质量门禁：`CGO_ENABLED=0` build/test、focused race、TypeScript、SDK/contract、Web build/component、integration/e2e evidence；只在实现稳定后运行一次完整 gate。验收：命令与结果写入最终 evidence packet；命令：`CGO_ENABLED=0 go test ./service/... && CGO_ENABLED=1 go test -race ./service/internal/spatialreplica/... && bun run typecheck && bun test && bun run build && bun run test:contract && bun run test:integration && bun run web:e2e`；失败复查：不得用跳过/弱化 test 作为修复。 **Evidence (2026-09-01, 单窗口完整跑)**: 完整数字：①`CGO_ENABLED=0 go test ./service/... -count=1`（umask 077）终态 exit 0、203 包全 ok 零 fail（终跑 -p 8；期间主机 load 377–533/128 CPU 重超载）；两次默认并行全量各出现一次互不重复的负载 flake，均甄别为 environmental/pre-existing 且 3/3 孤立复跑绿、包面本 change 零触碰——workflows/outbox `TestEngineBacklogLagDrainAndStartupTimeoutFailClosed`（20ms startup deadline 被饿死，误返回 unavailable 而非 timeout）与 workers/authority `TestServiceIdentityProbeAcceptsCurrentBoundReadiness`（2s readiness 窗口，该目录有并行 service-identity lane 在制品）。唯一 introduced 修复：`TestCanonicalRequirementChangeIDsMatchR0R5ReleaseMapping`——协调者修复 8.7 外部红灯时只改了 `canonicalRequirementChangeIDs` 未同步 Go 测试 expected 列表（不完整外部修复），补齐 1 行测试期望对齐（零业务逻辑），包级复跑绿并计入终态全绿。②focused race：`CGO_ENABLED=1 go test -race ./service/internal/spatialreplica/... ./service/internal/transport/spatialreplicahttp/... ./service/internal/transport/spatialreplicagrpc/... -count=1` 全 ok（grpc 包 no test files，其 parity 经 http 包 TestSpatialReplicaTransportParityWire 的跨 transport 调用覆盖）；race 复跑暴露并修复一处真测试鲁棒性缺陷：parity wire 测试依赖默认 3s projection cache TTL 把三次顺序 transport 调用 pin 在同一 composition 上，race+满载下相邻调用间隔观测 5.9s 越过 TTL → recompose 产生新毫秒时间戳（expires_at/composed_at/confirmed_at 全 +5943ms）与新随机 `wb:preview:*` ref（media.go rand 派生非时间派生）→ 字节断言假红（非 DATA RACE）；修复=parityService 经既有 `WithProjectionCache(NewProjectionCache(30s,…))` 选项 pin TTL（test-only 装配；TTL 过期/generation fence 语义仍由 cache_test+capability fence 测试独立覆盖，无断言弱化），CGO0 包级复跑同绿。③`bun run typecheck` exit 0。④`bun test`（全根）853 pass/8 skip/0 fail（144 files/861 tests）。⑤`bun run build` exit 0（CGO0 四二进制）。⑥`bun test packages/task-sdk` 527 pass/0 fail（72 files）。⑦`bun run test:contract` 530 pass/0 fail（73 files）。⑧`bun run --cwd apps/web build` exit 0（vite 16.4s）。⑨`bun run --cwd apps/web test`：vitest 194 files/1644 tests 全绿 + bun server 147 pass/6 skip/0 fail。⑩`bun run test:integration` → evidence run `20260901181913-6cd7c8c0-c90d-4467-b85b-d57480ce5939` passed（service/test/conformance + internal/runtime）。⑪replica e2e scoped（`task test:spatial-replica:e2e`，--grep "spatial replica"）：57 passed/3 skipped/0 failed → evidence run `20260901182030-a575d6df-ddc8-448a-94d9-19815098d5c0` passed。全套件 web:e2e 未重跑：8.4 全套件 run `20260901174559-…628`（184 passed/11 skipped/7 failed）已把 7 个失败全数分类 concurrent（并行 lane dirty diff，文件仍在树上未收口、非本 change 触碰），按门禁政策 concurrent 分类不阻塞。
- [x] 9.3 对稳定 diff 做 correctness/security/accessibility/performance review，重点检查 no-fourth-state、single-stream、exact PTS、unknown preservation、owner action authority、same-origin、resource disposal 与 truthful claims；修复 findings 后复审。验收：无剩余 P0–P2 finding，P3/deferral 进入 tasks/debt 并有 owner；命令：`git diff --stat && git diff --check`；失败复查：review 必须基于最终稳定 diff。 **Evidence (2026-09-01, 独立审阅会话)**: `details/10-independent-review.md`——与实现波次不同会话的逐面源码审阅（非只读测试断言）。五个重点面全部 PASS（file:line 证据）：no-fourth-state（浏览器七键白名单 reducer+runtime 守卫 replica-workspace-state.ts:66-74/198-207、draft ephemeral、服务端 composition/cache 零持久化可重建、无 migration）；single-stream（模块级 controller map 每 workspace 一条 replica-projection.ts:223-241、五 Pane 共享 query key、服务端单 log/pump、owner cursor/workspace cursor 分列）；exact PTS（全 BigInt 有理数交叉相乘+gcd、无 time×fps 路径（grep 证伪）、gap/duplicate 显式成段、lookup 三态不猜最近帧、confirmed 对齐帧起点/requested 不当 evidence）；unknown preservation（transport 失败→unknown_accept reconcile-only 不重投不换 key、duplicate pending 阻止等价重提、reconcile 不受 capability off 影响、无 taskRef 诚实不可收敛）；owner boundary（十 capability truth table fail-closed 执行+cohort、credential/private path 不入 wire（opaque preview ref 换取）、media 双层 allowlist、deeplink host allowlist、transport 层 scope isolation）。四维抽查：correctness（proto.Clone race 修复 composition.go:148、错误码 HTTP/JSON-RPC/gRPC 三面同表+conformance parity 测试、exact-ref join）、security（SSRF 双层 allowlist+manual redirect、单 bounded range、redaction/sentinel 矩阵）、accessibility（dialog focus trap+Escape+return、键盘替代（orbit 按钮/数值输入/timeline 键盘）、DOM object mirror+live announcer+axe e2e）、performance（3D chunk 158.2≤250 KiB gzip+three 静态图隔离双查、timeline 虚拟化 2000+ 样本 ~13 行、dispose 资源计数归零 e2e）。**findings：P0=0、P1=0（无需修复，稳定 diff 未被审阅会话改动）；P2=3 记录为 follow-up**——P2-1 浏览器 stream 终止后无自愈（replica-projection.ts:204-248，follow-up：afterCursor 重启+backoff）、P2-2 media registry 相对 locator 多 origin 时绑定不确定（media.go:145-151/228-233，follow-up：Issue 期绑定或拒绝）、P2-3 非 3D chunk +30 KiB delta 预算只记录不执法（bundle-budget.mjs；index-main +56.16 KiB 混编归因，follow-up：干净树复测归因）；另 4 项 P3 观察在案。验证：独立会话复跑 `bunx vitest run spatial-replica`（23 files/241 tests 绿）+ `CGO_ENABLED=0 go test ./service/internal/spatialreplica/... ./service/internal/adapters/... ./service/internal/transport/spatialreplicahttp/... ./service/internal/transport/spatialreplicagrpc/... ./service/internal/transport/jsonrpc/... -count=1` 全 ok；truthful claims 复核三文档分层一致无夸大；`openspec validate workbench-spatial-replica-review-v1 --strict --no-interactive` valid。
- [x] 9.4 更新长期 PRD/interface/UI docs、`docs/README.md`、OpenSpec closeout/evidence refs 与 root owner delivery packet，明确 additive/default-off/rollback、真实 owner dependencies 和未证明项。验收：文档不声称 Provider、真实 Auctra/Scaena runtime、replica video、production 或一比一 metric 已完成；命令：`openspec status --change workbench-spatial-replica-review-v1 && git diff --check -- docs openspec/changes/workbench-spatial-replica-review-v1`；失败复查：local fixture、contract、runtime、production evidence 必须分层。 **Evidence (2026-09-01)**: 长期文档三件已在前置波次建成并核对本波终态：`docs/product/spatial-replica-review-workspace.md`（PRD，306 行）、`docs/ui/spatial-replica-review-workspace.md`（UI spec，637 行）、`docs/interfaces/spatial-replica-review.md`（前后端/owner 合同，531 行），三者均已列入 `docs/README.md` 索引（产品与平台/视觉设计/接口与运行时三节各一行，diff 内可辨识）。诚实分层核对通过：三文档均显式 disclaim——PRD §非目标/§9「所有 evidence 明确标注 local fixture；没有 Provider、真实 owner runtime 或 production claim」+「本 PRD 不证明真实 Provider、世界模型、…、metric 一比一、生产运行或跨 owner live integration」；interfaces 文档「所有 local fixture 必须明确标注 fixture，不得被当作真实 Anatomia/Auctra/Scaena runtime、Provider 或 production evidence」+ `needs_contract` 门；UI spec「只证明 Workbench local contract/interaction，不暗示真实 owner runtime、Provider 或 production ready」+ 性能 fixture 非 production SLO。additive/default-off/rollback 语义长期化：change README 增补 rollout 段（十 capability 独立 default-off、server capability 唯一 enable authority、无 DB migration、rollback=关 flag、fixture adapter/`needs_contract` 现状）+ 新建 `details/09-closeout-evidence.md`（9.4 主体交付）——汇总 8.x 全部 6 个 evidence run-id（component 173934/integration 174213/e2e 174256/8.6 验收 174435/失败保留 174041 与 174559）+ 9.2 门禁两 run（181913/182030）、9.2 完整数字（go 203 包/race/bun 各套件）、9.1 四门结果、concurrent/external 分类账（web:e2e 7 失败、两次 go 负载 flake、gofmt 6 文件、8.7 外部红灯修复链）、未证明项四条（Provider/真实 runtime/replica video/一比一 metric/local fixture 层级，供 9.5 owner packet 取用）、9.3 diff 范围摘要（新增 64+ replica 命名面 + 3 文档 + tracked additive 追加清单 + 并行 lane 混编边界）。root owner delivery packet 未做（归 9.5 下一波）。验收命令：`openspec status --change workbench-spatial-replica-review-v1` 4/4 artifacts complete；`git diff --check -- docs openspec/changes/workbench-spatial-replica-review-v1` clean；新增文档零 conflict marker/trailing whitespace。
- [x] 9.5 仅在 Workbench 本地合同、浏览器 evidence 与安全门禁通过后，产出给 Anatomia/Auctra/Scaena owner 的 consumer feedback packet；不得把外部 owner 仓实施、Provider 调用或 production promotion 列为本 change 可代办完成项。验收：每个 dependency 标明 owner、required contract/version、current status、blocking effect 与独立 change；命令：`openspec validate workbench-spatial-replica-review-v1 --strict`；失败复查：owner contract 未发布时保持 `needs_contract`，不以 mock 晋级。 **Evidence (2026-09-01)**: `details/11-owner-consumer-packet.md`——给 Anatomia/Auctra/Scaena 三个 owner 侧的 consumer feedback packet，前提（9.2 门禁+9.3 审阅通过）满足后产出。内容：①统一消费边界（只读 server-authored safe projection、不复制 canonical、无 migration、浏览器永不直连、mutation 恒回 owner canonical、needs_contract truthful）；②逐 owner 对接点——Anatomia（`anatomia.spatial_evidence_projection.v1`+`anatomia.spatial_inquiry.v1`+`anatomia.correction.receipt.v1`：exact owner PTS map/七类 layer numeric-preview 分立/identity claims/freshness 三态独立/inquiry lineage 语义）、Scaena（`scaena.replica_stage_projection.v1`+stage/review/freeze receipt：evidence binding 闭合/coordinate frame P1 冻结/scale tier+claim scopes/browser budget ≤64/typed Task expected-version+idempotency+reconcile-only）、Auctra（`auctra.screenplay_projection.v1`：safe text depth 授权 ≤500 nodes/deep-link 约束）；③预算表引用 details/05 §2、capability flags 引用 details/04 十行；④owner 需后续提供清单（真实合同实现、media origin allowlist 配置、deep-link host 批准值、exact-principal cohort 协调）；⑤evidence 只引用本地 8.x/9.2 run-id（173934/174256/174435/181913/182030 等）并重申未证明项四条（Provider/真实 runtime/replica video/一比一 metric/非 production）——零外部实施、零 Provider 调用、零 production 晋级声明；⑥验收用 dependency ledger 9 行（每行 owner/required contract+version/status/blocking effect/独立 change）。命令：`openspec validate workbench-spatial-replica-review-v1 --strict --no-interactive` valid。
