## 1. Contract freeze and ownership gates

- [x] 1.1 Record the current dirty-worktree baseline, active Agent/UI changes, and explicit path leases; assign one writer for `api/proto/workbench/agent/**`, `service/internal/agent/**`, `packages/task-sdk/src/agent-*`, and one serialized writer for `apps/web/src/workbench/agent/**` so no overlapping source writers run concurrently.
- [x] 1.2 Confirm the canonical Workbench versioned pane registry and freeze the v1 pane IDs/closed parameter schemas for `agent.context.v1`, `agent.run.v1`, `agent.review.v1`, `agent.evidence.v1`, `agent.operations.v1`, and `agent.context-map.v1`; stop implementation if the registry owner cannot approve one canonical resolver.
- [x] 1.3 Freeze `AgentSessionWorkspaceSummaryV1`, `AgentSessionPresentationV1`, directory cursor, attention precedence, intent kinds, failure reason codes, proto field numbers, JSON schema IDs, HTTP paths, gRPC RPCs, and JSON-RPC method names before generating code.
- [x] 1.4 Document the selected boundary that session remains Task-derived, the projection is rebuildable, presentation metadata is principal-scoped UI state, and empty sessions/raw drafts remain browser-local; add a contract test that prevents Task/Owner fields from entering presentation writes.
- [x] 1.5 Define server-authoritative capability/cohort flags for read-only unified shell, directory stream, presentation suggestions, Follow Pi, and tool-action canary; prohibit production URL-query or browser-only flags from granting unavailable backend capability.

## 2. Session projection and presentation persistence

- [x] 2.1 Add additive proto/schema/domain types for session workspace summaries, projection freshness, attention, activity cursor, current-principal presentation metadata, update field masks, read acknowledgement, typed conflicts, and safe reason codes.
- [x] 2.2 Add forward-only GORM migrations and indexes for rebuildable session projections, projector high-water marks, principal-scoped presentation metadata, idempotency records, and read cursors using the pure-Go SQLite path with `CGO_ENABLED=0`.
- [x] 2.3 Implement the session projection repository and projector from canonical Agent turn Tasks plus safe event/proposal/receipt indexes, including deterministic attention precedence, bounded summaries, stable cursor pagination, and no raw prompt/provider/tool payload persistence.
- [x] 2.4 Implement projection rebuild and startup reconciliation from canonical records, expose lag/high-water freshness, and add tests proving rebuild does not mutate Tasks, attempts, receipts, proposals, or Owner state.
- [x] 2.5 Implement principal-scoped presentation get/update with server-resolved identity, session authorization, field-mask validation, title safety validation, expected revision/ETag, idempotency replay, and conflicting-digest rejection.
- [x] 2.6 Implement archive policy so settled sessions can be hidden per principal while active Task, pending review, or `unknown_accept` sessions return a typed blocked reason without changing Task state.
- [x] 2.7 Implement `AcknowledgeAgentSessionRead(seenThroughCursor)` with server-side cursor comparison and concurrency tests proving newer activity remains unread.
- [x] 2.8 Add repository/service tests for tenant/workspace/principal isolation, unauthorized session refs, unsafe titles, duplicate keys, stale revisions, archive blocking, projection lag, pagination stability, and no N+1 page loading.

## 3. Agent service, transports, and SDK parity

- [x] 3.1 Add shared Agent service methods for workspace summary list/search/filter, presentation get/update, read acknowledgement, and directory catch-up without putting operation-specific business rules in transport handlers.
- [x] 3.2 Add HTTP endpoints and typed error/status mappings for the new Agent methods, including `If-Match`/expected revision, idempotency key, cursor pagination, archived filter, and safe conflict responses.
- [x] 3.3 Add gRPC messages/RPCs and generated code for the new methods while preserving existing `workbench.agent.v1alpha1` field numbers and compatibility projections.
- [x] 3.4 Add JSON-RPC 2.0 methods and shared registry/service projection for identical request, revision, idempotency, pagination, authorization, and error semantics.
- [x] 3.5 Extend `packages/task-sdk` Agent models/client with workspace summaries, presentation update/ack, safe directory cursor, typed conflicts, and feature capability projection; keep existing `listSessions/listTurns/context/turn` calls compatible.
- [x] 3.6 Add four-transport parity tests and schema conformance fixtures for successful reads/writes, stale revision, duplicate idempotency replay, conflicting digest, unauthorized scope, unsafe title, archive blocked, and read-cursor race.

## 4. Workspace-level session directory stream

- [x] 4.1 Project bounded `session.directory.updated` activity from safe Task/gate/proposal/receipt transitions, coalescing routine progress while preserving proposal, gate, terminal, partial, failed, and `unknown_accept` attention.
- [x] 4.2 Implement one workspace-scoped durable directory stream with cursor resume/catch-up and authorization filtering; do not open or require one stream per session.
- [x] 4.3 Add SDK/browser support for directory SSE resume plus bounded query refresh fallback and last-confirmed/degraded state; other transports must expose equivalent cursor catch-up semantics.
- [x] 4.4 Add concurrency/backpressure/restart tests for event ordering, cursor durability, deduplication, coalescing, reconnect, principal isolation, projection lag, and a bounded connection count of directory stream plus selected-turn stream.

## 5. Unified Agent-first Web workspace

- [x] 5.1 Refactor `agent-conversation-workspace.tsx` under one serialized Web writer into a unified workspace shell, session rail, conversation column, timeline/turn groups, composer, pane host, and responsive sheet layer without changing authoritative Task semantics.
- [x] 5.2 Evolve `ThreadSidebar` into `AgentSessionRail` with New, search, pinned/recent/unread/archived filters, safe title rename, pin/unpin, archive/unarchive, attention badges, last-confirmed state, pagination, empty/loading/error/degraded states, and accessible overflow actions.
- [x] 5.3 Permit session switching and browser-local new drafts while another session is running, waiting for review, or `unknown_accept`; keep the affected session's status/attention visible and restore its timeline from durable cursors without cancelling or duplicating Tasks.
- [x] 5.4 Preserve per-session in-memory draft, timeline scroll, selected output, open pane, and event cursor while switching; do not persist raw drafts/history to server or localStorage and do not claim restoration after reload.
- [x] 5.5 Correct optimistic boundaries so a draft/submitting affordance is not committed as a user turn before Task acceptance, Stop is not displayed as cancelled before confirmation, and read acknowledgement is not shown before server observation.
- [x] 5.6 Replace the local fixed `AgentPaneKind` expansion path with aliases into the approved versioned pane registry and mount Context, Run, Review, Evidence, Operations, and optional Context Map through closed safe params only.
- [x] 5.7 Reuse `ObjectInspector`, Context components, `ActionDescriptorStatus`, `ActionRecoveryButton`, output/evidence components, and Operations Orbit typed-facade views; block production mock fallback and remove duplicate composer/event subscriptions from the alternate spatial shell.
- [x] 5.8 Implement the legacy desktop/tablet/mobile responsive baseline at 1440x960, 1024x768, and 390x844 with the specified rail/pane widths, single-overlay rule, focus restoration, 44px touch targets, 200% zoom behavior, and internal responsive Operations lists/tables. Desktop multi-Pane composition is superseded into 10.5; tablet/mobile remain one-Sheet-at-a-time.
- [x] 5.9 Add Chinese-first `zh-CN` strings and `en-US` fallback for every new state, reason, tooltip, action, dialog, live announcement, and recovery path while preserving opaque IDs, Owner names, commands, receipts, paths, and user input verbatim.
- [x] 5.10 Add focused component tests for session organization, running/unknown session switching, archive blocking, read ack visibility, directory disconnect, no hover/focus authorization, draft preservation, optimistic truth, pane semantics, keyboard navigation, and reduced motion.
  **Evidence (2026-08-12)**: `apps/web/test/agent-conversation-workspace.test.tsx` 现有 44 个 focused component test，覆盖 session organization、running/`unknown_accept` switching、typed archive blocking、server-authored unread visibility、directory stream disconnect/capability removal、per-session draft/timeline/pane/event-cursor preservation、Task acceptance/Stop optimistic boundaries和 desktop/tablet/mobile pane semantics。新增明确回归：① `Shift+Enter` 仅写入本地换行，未修饰 Enter 才提交；② presentation suggestion 通过键盘 Enter 激活，关闭 pane 后焦点回到真实 suggestion trigger；③ session row hover/focus 仅使用已加载 bounded safe projection，`listTurns`、`listTurnEvents`、Context prepare/get、read ack、submit、accept 调用计数保持不变；④在 `prefers-reduced-motion` 下实际输入 composer、打开/关闭 tablet modal pane并验证语义、Escape、焦点恢复与草稿保持。`apps/web/test/desktop-a11y.test.ts` 精确解析 Agent TaskService reduced-motion media block，确认只关闭滚动/transition delay/spinner motion且不含隐藏语义内容规则。Verification：`bunx vitest run test/agent-conversation-workspace.test.tsx test/agent-presentation-intent-resolver.test.ts test/agent-route.test.tsx test/desktop-a11y.test.ts` 4 files / 91 tests PASS；`bun run typecheck` PASS；`openspec validate workbench-agent-pi-workspace-v1 --strict` PASS；`git diff --check` PASS。独立 reviewer 未发现 P0/P1；其 reduced-motion P2 已通过上述组件行为和精确 CSS block 断言修复。此证据仍是 focused local/component proof，不代表 real Pi/Owner Provider、deployment 或 production readiness。

## 6. Pi presentation intent contract and resolver

- [x] 6.1 Add additive `AgentPresentationIntentV1` proto/schema/domain models with closed enums/targets, source refs, sequence, registry version, expiry, expected Context Pack revision, safe draft template, and `suggest|follow_eligible` policy.
- [x] 6.2 Implement server validation/redaction that rejects arbitrary maps, DOM selectors, IDs, XPath, URLs, HTML, JavaScript, CSS, keyboard/form injection, dynamic components, raw tool args, credentials, private paths, cross-scope refs, and unknown intent kinds without echoing unsafe values.
- [x] 6.3 Project validated intents additively on safe Agent output/event models so rejecting an intent never prevents the base finding/question/proposal/handoff from rendering.
- [x] 6.4 Extend SDK and contract fixtures for presentation intents, safe failure codes, forward-compatible unknown kinds, expiry, scope/revision validation, and redaction sentinel coverage.
- [x] 6.5 Implement the browser `AgentPresentationIntentResolver` against the canonical pane registry with scope/revision/expiry/availability validation, `(sessionRef,intentRef,sourceSequence)` dedupe, replay-safe behavior, and bounded local diagnostics.
- [x] 6.6 Render default suggestion cards for open pane, focus safe ref, show evidence, request review, and prefill draft; every activation must revalidate current server/registry state and preserve base output on failure.
- [x] 6.7 Implement temporary per-session Follow Pi, default off and non-persistent, allowing only live foreground `open_pane` and `show_evidence` when no dirty editor/modal/review exists; keep `focus_safe_ref` suggest-only, never move keyboard focus, and never auto-apply replayed history.
- [x] 6.8 Implement `prefill_draft` as explicit-only insertion into an empty same-session composer, with bounded safe text, no overwrite, no Context attachment, no submit, and no Task creation.
- [x] 6.9 Implement `request_review` as explicit-only navigation to the current server-authored Review projection; it must never accept/reject/retry/reconcile or revive stale availability.
- [x] 6.10 Add Web/unit/security tests for every allowed kind, every forbidden target class, unknown version/kind, stale Context Pack, cross-workspace ref, registry mismatch, expiry, duplicate/replay, typing/modal guards, Follow Pi shutdown, focus safety, and no backend activity on hover/focus.

## 7. Context, tool review, and Task authority integration

- [x] 7.1 Expose an optional last Context Pack safe ref/revision in the session summary and implement explicit reauthorize/refresh-or-detach UI; never auto-attach or silently substitute a new revision on session selection or intent application.
- [x] 7.2 Unify proposal cards, Review pane, session attention, Board/Inspector action presentation, and composer tool availability on the same server-authored `ActionDescriptorV1` status/reason/recovery mapping.
- [x] 7.3 Audit the current proposal authority so accept requests contain only proposal ref, expected proposal revision, decision, and idempotency key; server-side code must reload basis refs, descriptor, expected versions, and scope before creating any Task. If this authority is not present, keep the action canary `needs_contract` and open a separately owned prerequisite change.
- [x] 7.4 Complete the implementation dependency `workbench-agent-proposal-authority-v1`, then verify its sealed `orbit.proposal.accept`, target Operation, Owner adapter, permission/cost/expected-version/idempotency gates, and receipt/reconcile path with a real approved adapter before enabling action controls; UI/component/reference-adapter success is insufficient. **Direct dependency evidence (2026-08-23):** proposal authority tasks are 40/40 and its 14-requirement main spec is synced. Component/restart/race bundle `temp/integration-test-runs/20260823151219-bc36bd64-521c-4a26-90c9-c23416130e5a/` proves exact-principal read/decision/reconcile/tool cohorts, three-wire + SDK parity, sealed `proposalDecisionRef`, process-restart rollback, and no new claim after decision-off. Real approved adapter bundle `temp/integration-test-runs/20260823150803-736d20d2-d3bc-4200-9417-ed46397f6259/` uses a fresh persisted Identity signing key/JWKS plus Eikona process and disposable `proj_canary`: one canonical `orbit.proposal.accept` Task crosses permission/cost/expected-version/idempotency gates, delegates only to exact `eikona.generation.submit`, returns canonical accepted proposal revision/decision ref, succeeds with owner receipt `own_c8ca15938f6afe2fd59ba393`, and retains reconcile semantics without static HMAC/browser credential forwarding. Browser bundle `temp/integration-test-runs/20260823151115-d96d9e6a-3449-4ca1-ba22-2165e9b73fa0/` passes 6/6 for accept/reject/request-changes/conflict reload/unknown original-attempt reconcile/decision rollback. Action controls are enabled only for the exact final cohort + selected ready descriptor; all other Owner/action combinations remain disabled/`needs_contract`. These are local component/browser/real-loopback tiers, not staging/production claims.
- [x] 7.5 Enforce per-session `unknown_accept` reconcile-only behavior across timeline, rail, composer, Review pane, and intents while allowing navigation and authorized work in other sessions; add tests proving no replay, fallback Task, provider switch, automatic cancellation, or fabricated terminal state.
- [x] 7.6 Add negative tests proving presentation intents cannot invoke tool submit, proposal accept, Task submit/cancel/reconcile, Owner deep links without approved descriptors, or any mutation outside TaskService.

## 8. Canary, migration, and rollback

- [x] 8.1 Run projection shadow comparison against current derived `ListAgentSessions` on deterministic fixtures and a bounded local dataset; classify and resolve differences in count, ordering, last state, attention, and safe summary without rewriting canonical records.
- [x] 8.2 Enable the unified workspace as read-only for a server-authorized canary cohort, retain the existing conversation fallback, and capture route/transport/process ownership plus flag-off rollback evidence.
- [x] 8.3 Enable directory background behavior only after stream resume, unread race, connection bound, and degraded-state evidence pass; do not conflate this with Pi runtime or tool execution readiness.
- [x] 8.4 Enable presentation intents in suggest-only mode, then a narrower Follow Pi canary beginning with `open_pane/show_evidence`; expand to `focus_safe_ref` only after browser focus/typing/reduced-motion evidence passes.
- [x] 8.5 Enable tool-action canary only after the independent Owner/adapter/proposal authority gate in 7.3–7.4 passes; otherwise retain truthful read-only Review and `needs_contract` states.
- [x] 8.6 Perform rollback drills that disable each capability layer independently, return `/agent` to the old conversation workspace, stop projectors/intent consumers, preserve all projection/presentation tables and cursors, and rebuild projections without deleting or modifying Tasks, attempts, receipts, proposals, evidence, or unknown outcomes.
- [x] 8.7 Update `docs/ui/agent-first-workbench.md` and interface/testing documentation with the final unified shell, session/presentation authority table, intent allowlist, state matrix, canary gates, rollback steps, and evidence-tier wording.

## 9. Independent verification and evidence

- [x] 9.1 Run `openspec validate workbench-agent-pi-workspace-v1 --strict --no-interactive`, `openspec validate --all --strict`, `buf lint`, and schema/contract generation drift checks.
- [x] 9.2 Run focused Go tests for Agent projection/presentation/stream/service/transports with `CGO_ENABLED=0`, then `CGO_ENABLED=1 go test -race` for affected service packages; attribute unrelated dirty-tree failures separately.
- [x] 9.3 Run focused Bun/Vitest tests for SDK and Web, then `bun run typecheck`, `bun test`, `bun run build`, `bun run test:contract`, and `bun run test:integration`, preserving unrelated baseline failures as separate evidence.
- [x] 9.4 Run Playwright user journeys at 1440x960, 1024x768, and 390x844 for new/search/rename/pin/archive, running-session switch, background unread, reconnect/resume, Context refresh, suggestion/Follow Pi, proposal review, Stop, `unknown_accept` reconcile, fallback flag, and rollback.
- [x] 9.5 Capture keyboard-only, screen-reader landmark/live-region, Axe serious/critical, 200% zoom, reduced-motion, focus restoration, touch-target, and no-page-horizontal-overflow evidence for the unified shell and every pane mode.
- [x] 9.6 Run security/redaction tests across proto/schema validation, database rows, logs, SSE, SDK fixtures, screenshots, receipts, and evidence using prompt/provider/tool/credential/private-path/signed-URL sentinels plus cross-tenant/principal attacks.
- [x] 9.7 Measure a 50-session page and directory-update fixture for query count, N+1 absence, memory/connection bounds, and p95 latency against a recorded baseline; do not claim a performance target without the fixture and environment metadata.
- [x] 9.8 Write a redacted evidence bundle under `temp/integration-test-runs/<run-id>/` containing commands, versions, process/port ownership, contract parity, migration/rebuild, browser traces/screenshots, security scans, performance measurements, canary/rollback results, and explicit evidence-tier classification.
- [x] 9.9 Obtain independent reviewer, browser-debugger, test-engineer, and security-reviewer sign-off on the stable diff; keep focused-local, browser/E2E, real Pi/Owner Provider, deployment, and production proof as separate completion claims.

## 10. Agent-first plugin Pane visual baseline and migration

- [x] 10.1 Register the three Eikona reference renders under `prompts/product/ui-reference/workbench-agent-pane/`, record their run/artifact provenance, and freeze image 02 as the primary layout reference, image 01 as the conversation reference, and image 03 as the review/evidence reference.
- [x] 10.2 Rewrite `docs/ui/agent-first-workbench.md` around one persistent Agent conversation plus a bounded plugin Pane dock, including owner-fit, required-capability ledger, responsive behavior, accessibility, state/recovery, and acceptance criteria.
- [x] 10.3 Remove the obsolete untracked rain-gate preview artifacts from the active worktree through a recoverable move and archive the superseded spatial-board preview document without deleting reusable Owner/Panes contracts.
- [x] 10.4 Add a closed Pane plugin catalog plus pure session-scoped layout transitions for open, focus, close, duplicate prevention, visible-limit handling, hard maximum, and split-depth constants; cover them with focused unit tests before integrating UI composition.
- [x] 10.5 Integrate the layout state into `AgentConversationWorkspace` so desktop supports one to three visible Panes by default, the conversation remains the layout anchor, the fourth request is explicit `limit_reached`, and tablet/mobile retain one labelled Sheet at a time.
- [x] 10.6 Add the compact icon rail and command palette for opening registered Panes, searching sessions/commands, and showing disabled `needs_contract` candidates without dynamic imports, arbitrary URLs, shell commands, or browser-authoritative availability.
- [x] 10.7 Make `/agent` the default product entry only after the server-authored navigation/capability contract is updated; demote Overview, Orbit, Boards, Studio, Gateway, and professional pages to registered Pane, advanced route, or approved Owner deep link without breaking stable deep links.
- [x] 10.8 Split the oversized Agent conversation implementation into shell, session rail, timeline, composer, Pane dock, and responsive Sheet modules while preserving Task/event/Context/proposal truth and avoiding a second client or subscription owner.
- [x] 10.9 Add component and Playwright evidence comparing structure, density, Pane limits, command-palette keyboard behavior, focus restoration, 200% zoom, reduced motion, Axe, and responsive states against the three Eikona references; do not treat screenshot similarity as contract or production proof.
- [x] 10.10 Freeze `docs/product/agent-workbench-blueprint.md` as the application truth, rewrite `docs/interfaces/agent-pi-workspace.md` as the durable Browser→BFF→services→Owner contract, and add the project-scoped `agent-workbench-product-design` skill so future product/UI/backend changes cannot silently restore parallel shells or browser-authoritative actions.
- [x] 10.11 Wire the local preview end to end: harden the preview health probe with the agent surface (stale workbenchd is never adopted), default the preview to the in-process Pi reference adapter plus local unified-shell/directory/suggestion cohorts, and report pi readiness honestly ready when that adapter is enabled (`workbench-agent-runtime-chat` readiness clause); verify a real turn through UI submit → gate approval → event stream → directory projection.
- [x] 10.12 Close the browser→server contract gaps proven by the live preview: expose the bounded event `code` additively across proto/HTTP/SSE/JSON-RPC and the SDK (timeline projects proposals/segments from `code` with summary fallback), default `projectId`/`workspaceId` to the local workspace identity, and send the genesis `expectedRevision` so turn submit satisfies the owner-mutation precondition.
- [x] 10.13 Remove the double left rail on `/agent` (global sidebar suppressed there, reachable via the window-bar palette), enforce the conversation ≥560px layout budget against the pane dock, and convert the conversation header to container queries so controls degrade instead of overlapping.
- [x] 10.14 Merge build-time composed/compatibility locale catalogs even when the server bundle is unreachable, eliminating mixed-language fallback in offline/degraded mode; update the i18n provider tests.
- [x] 10.15 Raise timeline density toward reference 01 using only real data: flat work-record rows with timestamps, per-run event rows from stored/live events, artifact chips from `basisRefs`, and mono for IDs/refs; anything needing a server plan/stepper projection stays `needs_contract` and unimplemented.
- [x] 10.16 Add desktop pane tab strip + dock status bar, pane icons/permission badges in the palette, composer context chips and a slash menu wired only to real callbacks, and route ⌘K inside `/agent` to the pane palette while suppressing the global one; collapse the empty state to one primary CTA.
- [x] 10.17 Unify the token layer into one dark semantic system: physical `--color-*` values in `@theme`, semantic `--wb-*` re-anchored as pure `var(--color-*)` references (add the four missing physical tokens and fix the undefined `--color-danger` references), locked by token/foundation contract tests (`docs/design/design-system-unification.md`).
- [x] 10.18 Converge agent conversation/rail components onto the semantic tokens, the design-system icon registry (agent-side names added), and `wb-motion-*` recipes with reduced-motion degradation, without changing selectors, props, or the visual blacklist invariants.
- [x] 10.19 Upgrade the pane layout module to a bounded layout tree (placement region path/order/size, depth ≤2 as a real structural constraint) with pure reorder/move/resize operations and typed rejections, keeping the existing open/focus/close/replace semantics and the flat `panes` projection for compatibility (`docs/design/pane-interaction-model.md`).
- [x] 10.20 Implement the dock interaction surface per the model: tab drag reorder/move, edge dropzone split creation, Escape/invalid-drop cancel with zero layout change, pane menu equivalents, keyboard shortcuts registered in the shortcut registry, keyboard separator resize via `resizeStep`, preflight limit/depth validation before mount, live-region announcements, and pane header version/freshness from real data only.
- [x] 10.21 Introduce the first-party plugin pack contract: `AgentPaneManifestV1` + fail-closed validator (forbidden component/URL/credential keys), the pane catalog built from six built-in manifests with the public catalog API unchanged, and the plugin ecosystem design + author guide (`docs/design/plugin-ecosystem.md`).
- [x] 10.22 Raise the timeline to real agent interaction density (dsh-referenced, real events only): the reference adapter emits a fixed 6-stage/8-event sequence with interruptible deterministic pacing (thinking → tool call start/end pairs → segment → proposal → succeeded), and the timeline projects thinking disclosure rows, merged one-line tool cards with real start/end durations, and an elapsed-time run footer; unknown codes keep safe fallback text.
- [x] 10.23 Apply the Agent visual language (`docs/design/agent-visual-language.md`): flat hairline-separated timeline sections instead of boxed cards, ghost buttons, one floating composer, quieter dock/palette chrome, minimal empty state, and the darker canvas/hairline physical tokens; selectors, interaction logic, and data projections unchanged.

**Scope change note (2026-08-15):** Evidence recorded for 5.1–5.10 proves the previous single-Pane Agent workspace and remains useful for transport, truth, accessibility, and responsive fallback behavior. It does not prove the new desktop multi-Pane composition, compact product rail, command palette, `/agent` default-route migration, or Eikona-aligned visual hierarchy; those claims stay open under 10.5–10.9.

**Implementation note (2026-08-15, 10.5–10.9):** Multi-Pane dock、rail/palette、`/agent` 默认入口与组件拆分已实现并通过验证：57 个 conversation 组件测试、dock/palette/layout 专项测试、`agent-pi-workspace.spec.ts` 8/8 与 `agent-first.spec.ts` 6/6 Playwright 通过；Eikona 对照截图（非合同证据）在 `apps/web/temp/playwright-results/eikona-compare/`。命令面板触发采用 rail Plugins 与 header `+ Pane` 入口；全局 `⌘K` 仍归导航 palette，避免双弹层。服务端 navigation catalog（Go）与客户端 fallback 同步降级 Overview/Orbit/Boards/Workflows/Harness/Gateway 为 `optional`，深链不变。Palette 的 pane availability 目前来自真实本地前提（activeTurn、timeline outputs、注册合同）；合同 §10 的 per-pane server-projected availability 仍是后续依赖。10.10 的 `agent-workbench-product-design` skill 本次已真实创建（root `.skills/yeisme/project-development/` 源 + profile + 双 runtime 同步）。

## Evidence notes

- Managed Web fallback and safety-copy follow-up (2026-08-12): `/agent` now classifies failed server capability discovery without exposing raw transport text. `authorization_unavailable` renders a protected `needs-contract` explanation, `permission_denied` renders a distinct access-denied next action, and all other failures render a temporary-unavailable state; every case remains in the existing conversation workspace, performs no automatic retry, and cannot enable directory, tools, presentation suggestions, or Follow Pi. A separate audit also found that the `unknown_accept` missing-version guard used `agent.conversation.control.reconcileNeedsRefresh` without a catalog entry; the zh-CN/en-US protected security copy is now authored and generated instead of relying on an ungoverned fallback literal. `compose:i18n` and `check:i18n` pass with 2,338 parity-checked keys, composer tests pass 5/5, the correctly configured `apps/web` Agent route/workspace suites pass 57/57, TypeScript, strict change validation, and diff checks pass. A mistakenly root-invoked temporary Vitest 4 run lacked the Web jsdom configuration and is discarded as command-environment error; the project-owned Vitest 3.2.7 rerun is the applicable evidence. This is focused local UI/i18n proof only. The production Agent authorization binding remains intentionally unset until Identity freezes the ResourceType/ActionID contract, so 2.5, 3.6, 4.2, 8.2, and 8.3 remain open and managed rollout remains `needs_contract`.

- Managed resource-authorization audit (2026-08-12, hardened in the current continuation) reopened 2.5, 3.6, 4.2, 8.2, and 8.3. The generic Identity `security.ResourceAuthorizer` now exists, but the Identity owner has frozen only `workbench.project + task.*`; the Agent workspace/project `ResourceType` and read/write `ActionID` contract remains unapproved. `agent.Service` now owns one explicit `AgentSessionResourceAuthorizationBinding` seam and routes legacy catalog, workspace list/shadow, presentation get/update, read acknowledgement, directory catch-up, and live watch through the same server-side authorization helper. Local loopback remains available. Managed callers without an injected approved binding or during Identity outage now fail closed as `authorization_unavailable` (HTTP 503/gRPC Unavailable/JSON-RPC stable error); an explicit Identity denial remains `permission_denied`. Focused Agent, HTTP/gRPC/JSON-RPC, conformance, SDK, TypeScript, and diff checks pass. Runtime intentionally does not inject invented Agent action IDs, so managed read-only/directory capabilities remain default-off and `needs_contract`; these tasks stay open until the Identity owner freezes the contract and an authenticated managed canary proves it.

- Completion audit (2026-08-12) corrected the prior `65/65` metadata-only state. Tasks 7.4 and 8.4 are reopened because no real approved Owner adapter or server-authorized presentation/Follow Pi canary evidence exists; action controls remain fail-closed and `needs_contract`/unavailable. Tasks 9.1–9.9 are reopened because current evidence is fragmented: the Agent Pi Playwright file still contains skipped/comment-only journeys, the available successful spatial-browser bundle belongs to another change, no current 50-session p95/environment baseline exists, and no single redacted Agent Pi evidence bundle or complete independent sign-off set exists. Focused local/component evidence recorded below remains valid, but it is not substituted for browser, Provider, deployment, production, performance, or final sign-off proof.
- Closeout supersession (2026-08-23): the dated statements below that 7.4/8.5 still await proposal authority describe the pre-PA state and are superseded only by the direct dependency evidence on task 7.4 and the PA 8.6 handoff. The new evidence proves local component/browser/real-loopback readiness for the exact Eikona action; staging, deployment, production, and all non-selected Owner/action combinations remain unproved or `needs_contract`.
- 9.1 was re-established on 2026-08-12 from the current dirty tree: `openspec validate workbench-agent-pi-workspace-v1 --strict --no-interactive`, `openspec validate --all --strict` (25/25), `buf lint`, and `CGO_ENABLED=0 go run ./service/cmd/schema-export --output api/schema --check` all exited 0. An isolated `buf generate` under `/tmp/tmp.6VQeLOWQ8V` produced no semantic Agent Go/grpc difference after excluding the generator-version comment; the only raw diff was `protoc-gen-go v1.36.11` versus the currently resolved `v1.36.12`. Tool versions: OpenSpec 1.3.1, buf 1.66.1, Go 1.26.4. No generated workspace file was overwritten merely to normalize tool metadata.
- 9.2 was re-established on 2026-08-12. `CGO_ENABLED=0 go test` passed for `service/internal/agent`, `repository`, Agent HTTP/gRPC/JSON-RPC transports, `agentruntime`, `runtime`, and `service/test/conformance`. Under `CGO_ENABLED=1 -race`, Agent, all three transports, agentruntime, runtime, and conformance passed. The initial whole-repository-package race run timed out after ten minutes in unrelated dirty-tree WorkItem/migration coverage (`TestAddWorkItemDependencyDuplicate`), so it is not attributed to this change. A subsequent exact Agent repository race selection covering session workspace projection/presentation, pagination/query bound, shadow comparison, cursor/event migrations, catch-up, concurrent rebuild serialization, coalescing, same-millisecond ordering, slow-consumer disconnect, restart resume, and tenant isolation passed in 52.8s. This proves the affected Agent repository race boundary while preserving the unrelated broader repository baseline separately.
- 9.3 was re-established on 2026-08-12 with explicit baseline attribution. Agent SDK focused tests pass 16/16; Agent Web pane/resolver/route/workspace/a11y focused tests pass 105/105. `bun run typecheck`, `bun run build`, and `bun run test:contract` pass, with contract coverage at 276/276 including the live typed Agent session workspace transport test. `bun run test:integration` passes and writes `temp/integration-test-runs/20260812015422-ee892a16-af46-4f60-9568-5403d379e186`. The required root `bun test` command was also run but remains red on the shared dirty-tree baseline: 544 pass, 7 skip, 36 fail, 6 collection errors. The failures are not in the Agent focused suites; they include a user-owned `temp/browser-agent-5-9/final-current.spec.ts` collected without Playwright, a five-second `tests/security/local-session.test.ts` timeout, and Web Vitest/jsdom source tests incorrectly executed by Bun from the repository root (`document is not defined`, missing Web-relative CSS paths, unavailable Vitest globals). This baseline is retained as red evidence rather than repaired or presented as a green Agent result.
- 9.4–9.5 were re-established on 2026-08-12 with a non-skipped fixture-backed Chromium suite in `apps/web/e2e/agent-pi-workspace.spec.ts`. Five journeys pass at 1440x960, 1024x768, 390x844, and a documented 720px CSS-width proxy for 200% browser zoom: server-confirmed new/search/rename/pin/archive plus archive blocking; running-session switch without cancellation or read acknowledgement; directory stream reconnect using the last delivered opaque `agent_cursor:resume`; explicit Context prepare/refresh; keyboard suggestion activation and temporary Follow Pi shutdown; read-only `needs_contract` Review; Stop confirmation; `unknown_accept` reconcile with one submit and no retry; and enabled-to-disabled capability rollback that returns to the legacy conversation mode, disables Follow Pi, and clears attached Context UI. Keyboard focus entry/Escape restoration, semantic rail/timeline/pane landmarks and polite live regions, reduced motion, 44px targets, no page horizontal overflow, and Axe serious/critical zero are asserted. A dark Agent-control-plane-local warning token override fixes the discovered contrast issue without changing the global light-surface warning token. Redacted command evidence passed at `temp/integration-test-runs/20260812024942-1a84b55f-9c5b-46d7-9a34-b66321a6439f`; later independent browser reruns also pass 5/5 with retained local traces. This remains local fixture/browser evidence, not native browser-chrome zoom, assistive-technology manual proof, authenticated Provider/Owner, deployment, or production readiness.
- 9.6 was re-established on 2026-08-12 as focused local/integration security evidence. Canonical Task `authority_subject_ref` ownership now hides cross-principal sessions across repository list/get/update/read-ack/directory catch-up/watch, with HTTP, gRPC, and JSON-RPC existence-hiding tests and no attacker-scoped presentation rows. Go contract/repository/HTTP tests reject prompt/provider/tool/credential/private-path and raw, percent-encoded, double-encoded, HTML-entity, Unicode-confusable, protocol-relative, backslash, AWS, Google, and signed-URL title forms before persistence, idempotency, response, log, or SSE projection; safe prose remains accepted. The TypeScript SDK applies the corresponding fail-closed normalization, and focused SDK/security/evidence tests pass 24/24. A Chromium `/agent` fixture injects the same sentinel classes and proves they are absent from rendered text/HTML and observed outbound requests while the unsafe workspace summary is dropped to a truthful safe state. The trace-on browser run passed 1/1; its PNG is digest-recorded in the redacted six-file evidence run `temp/integration-test-runs/20260812051116-f3065907-8ec1-442e-98cd-6a5edd884ed1`, and a binary/text sentinel scan over that run and `temp/playwright-results` returned no matches. Focused `CGO_ENABLED=0` tests, affected Agent repository race tests, Agent HTTP/gRPC/JSON-RPC race tests, `bun run typecheck`, and strict OpenSpec validation pass. This is not real Provider, deployment, or production security proof; independent security-reviewer sign-off remains separately required by 9.9.
- 9.8 was established on 2026-08-12 at `temp/integration-test-runs/20260812052004-fceeac45-9037-4bde-90a4-203ad01820f7`. Its runner-generated summary, command, environment, redaction report, and SHA-256 digest collect `agent-pi-workspace-evidence-manifest.json`, which records revision and dirty-tree digest; Go/Bun/OpenSpec/buf versions; a point-in-time local listener check for ports 4273/8080/9090; contract/runtime, migration/rebuild, five-journey browser/rollback, security screenshot/trace, and 50-session performance evidence references; canary states; and explicit evidence tiers. The manifest classifies focused local and fixture-backed browser evidence as proved, while real Pi/Owner Provider remains `unproved_needs_contract` and deployment/production remain unproved. The point-in-time port check observed no listeners and is explicitly not deployment or production evidence. The collected manifest digest is `sha256:5b7d24b21acfd25b6b8bc18b4fc8de38ab143730278464ccc8e76552a8e714b9`.
- 9.8 was reopened after independent security review found that the prior aggregate run checked only child-file presence and did not recompute or bind child status/digests to one source snapshot; its manifest also referenced a browser trace that the aggregate run did not collect. The prior bundle remains historical evidence but is insufficient for final aggregation. A replacement must parse every child summary, require matching passed status/zero exit, verify child and artifact SHA-256 values, bind all selected evidence to the repaired source snapshot, and either collect each referenced trace or omit the claim.
- Replacement 9.8 evidence was established on 2026-08-12 at `temp/integration-test-runs/20260812062935-8702b825-7ff4-4692-89b0-1c8bab78c7c2`. The new fail-closed verifier in `scripts/test-evidence/aggregate.ts` rejects unbound legacy runs and verifies child directory/run-ID identity, passed status, zero exit, exact core-file and artifact sets, recomputed SHA-256 values, digest counts, redaction policy, bounded regular files, symlink/path escape, and a matching collected `source-snapshot.json`. Four newly generated repaired-snapshot children cover focused contract/conformance/SDK/Web checks (`20260812062514-d9e1af6e-c4ce-4485-8145-d7175b367889`), affected race/security checks (`20260812062514-d930c1e7-6bd3-41f0-a70d-4d000e7fcf65`), trace-on browser journeys and sentinel screenshot (`20260812062710-93be7ba9-21ef-447f-8cd6-2fea022945d8`), and the 50-session performance fixture (`20260812062747-0fd5ec05-9a44-4c2f-bc96-8713af8e8766`). All bind to revision `f6cf1579923455255775add708ca909e854167fd` and dirty-tree digest `sha256:4d846c59b7944a74b595ef0ed14b05f26911682f009feee1e4ca28c73c3a408f`. The aggregate manifest digest is `sha256:04b951fe793d446be44a18e0a12fa7f0f1b7c59e987fb1fffc51058ae712f1b7`; the collected Playwright trace digest is `sha256:335f14b8fdc7f49a42b65272b104438af3931aec5409dcd833c4d3298863db57`. Evidence tiers remain explicit: focused local and fixture browser proved; real Owner/Provider `unproved_needs_contract`; deployment and production unproved.
- A first 9.9 sign-off set on 2026-08-12 was invalidated before completion when a delayed independent review identified concrete monotonicity and schema-parity risks. The checkbox remains open until equal-revision projection writes, delayed directory-summary merges, and `archivedOnly` schema parity are reproduced or closed, followed by a fresh stable-snapshot sign-off set. The v2 evidence model itself remains fail-closed; real Pi/Owner Provider, deployment, and production remain explicitly unproved.
- 9.7 was established on 2026-08-12 as a first local dirty-tree SQLite/component baseline, not a target or production-capacity claim. `TestAgentPiWorkspacePerformanceBaseline50Sessions` builds exactly 50 Agent sessions, warms the measured calls, retains 15 single-call samples, and computes nearest-rank percentiles. The recorded artifact `temp/agent-pi-workspace-performance/20260812-signed-url-repair/metrics.json` reports list p50 1.376 ms and p95 2.592 ms; directory catch-up p50 10.777 ms and p95 13.619 ms. The GORM observer records exactly three list queries for both page size 1 and 50, proving bounded cardinality-invariant list behavior for this fixture. Runtime evidence records heap/malloc deltas, a one-connection SQLite pool with zero wait delta, and a directory configuration of one slot/one buffered event that accepts the first stream, rejects at capacity, disconnects a slow consumer, and releases the slot for reuse. Environment metadata includes Go 1.26.4, linux/amd64, source revision, dirty-tree state, and a dirty-tree digest. The evidence runner accepted the artifact at `temp/integration-test-runs/20260812035927-e88febd6-318b-43de-a2a3-89cabb5e5975`, and an independent performance reviewer verified the artifact digest, rank math, timing scope, and repeated focused passes. Fifteen samples make p95/p99 coarse maxima; process-wide heap deltas and serial in-memory SQLite pool stats do not prove leak freedom, contention behavior, Postgres performance, managed capacity, deployment, or production SLOs.
- 1.1–1.3 are frozen in `docs/interfaces/agent-pi-workspace.md`; the current dirty-tree baseline was inspected before implementation and the Web Agent source lease remains serialized for a later slice.
- The canonical pane resolver is `apps/web/src/workbench/desktop/registry/pane-registry.ts`; no second registry or dynamic component path is introduced.
- 1.5 is projected by `service/internal/agent/service.go`: every capability is default-off, the read-only shell and directory stream use independent exact-principal server cohorts, tool canary remains unavailable, and presentation suggestions/Follow Pi remain disabled until their independent gates exist; no URL query or browser state can grant these capabilities.
- The backend projection slice includes rebuild derivation, high-water persistence, and a bounded startup reconciliation pass in `service/internal/agent/session_workspace_projection.go`, `service/internal/repository/agent_session_workspace.go`, and `service/internal/runtime/runtime.go`. Startup enumerates at most 128 tenant/workspace scopes plus one truncation sentinel, runs under a fixed two-second timeout before listener bind, fails closed on unsafe scopes, and leaves incomplete scopes unknown/lazy-rebuildable with bounded reason-code logging. Durable directory events and Web integration remain pending tasks.
- An unknown projection high-water can be bootstrapped once from canonical Tasks on the shared list path; the bootstrap remains projection-only and preserves Task input/status.
- 2.3 derives from tenant/workspace-scoped Agent Tasks plus bounded proposal-code and receipt-timestamp indexes only. A source read that exceeds the explicit rebuild limit returns `resync_required` and writes no partial `fresh` high-water state.
- 2.4 is covered by a real `Runtime.Start` SQLite fixture that rebuilds a fresh session projection and deep-compares the canonical Task, attempt, receipt, and proposal event records before and after startup. The projector has no Owner adapter dependency and Workbench stores no Owner canonical state; startup writes only the rebuildable projection/high-water tables. Scope truncation refuses all writes, unexpected errors log only safe reason codes, and focused `CGO_ENABLED=0` plus `-race` tests pass.
- 2.8 focused repository/service coverage includes cross-tenant/workspace/principal isolation, safe title/field validation, idempotency and stale revision conflicts, archive blocking, degraded/unknown/truncated projection states, stable pinned/unread keyset pagination, and a 50-session page with at most three queries (directory page, batched presentations, high water).
- 3.2–3.6 are wired through the shared Agent service, HTTP, gRPC, JSON-RPC, generated Agent protobufs, and the typed SDK while retaining the legacy session/turn calls. `service/test/conformance/agent_session_workspace_parity_test.go` exercises one SQLite-backed state across HTTP/gRPC/JSON-RPC for success, replay, stale revision, conflicting idempotency, unsafe title, archive blocking, authorization, and newer-activity unread behavior. `tests/conformance/sdk-transports.test.ts` starts a real reference-enabled `workbenchd`, submits and authorizes a typed Agent turn to materialize a session, and exercises live SDK list/get/update/replay/stale/unsafe-title/read-ack plus finite HTTP/JSON-RPC directory catch-up semantics.
- 3.1 directory catch-up is a shared service/repository rule: it reauthorizes server-side scope, performs a context-cancellable serialized projection-only reconcile, and returns at most 100 safe activities ordered after an opaque cursor. The live directory stream composes these same finite durable windows, so reconnect/query fallback and streaming do not diverge. Concurrent HTTP/JSON-RPC reads initially exposed a SQLite projection-write race; the service-level reconciliation gate fixes it without transport retries, and an 8-caller file-backed SQLite race fixture plus live SDK parity pass.
- 4.1 projects `session.directory.updated` from the canonical Task event index joined to the rebuildable safe session summary. It never emits event summaries, replay inputs, raw provider/tool fields, credentials, or private paths. Proposal, gate, terminal, partial, failed, cancel/reconcile, and `unknown_accept` transitions remain critical; only adjacent routine progress for the same session is coalesced. Cursor ordering uses one domain encoder over `(created_at milliseconds, event sequence, task ID)`, and resume filtering is completed in domain code after an inclusive SQL time bound so SQLite timestamp normalization cannot replay a boundary event. Focused tests cover critical retention, progress coalescing, eight concurrent file-backed callers, and one-at-a-time resume across three events sharing the same millisecond. Full affected `CGO_ENABLED=0` packages, focused repository/gRPC race tests, live SDK conformance, and TypeScript typecheck pass. Tasks 4.3–4.4 remain open for browser fallback/reconnect and broader restart/backpressure/connection-count evidence.
- 4.2 uses `WatchAgentSessionDirectory` as one workspace-scoped cursor loop over the canonical durable event index rather than introducing a second broker authority. Every pass resumes through the same authorization and opaque cursor comparator, so a commit between passes is caught on the next bounded poll and process restart needs only the client's last confirmed cursor. Runtime controls bound total streams and each output queue; cancellation releases its slot, and a consumer that cannot drain within the fixed send bound terminates with a typed slow-consumer error. HTTP SSE, gRPC server streaming, JSON-RPC SSE, and the typed SDK use this shared live path; HTTP/JSON-RPC advertise `X-Workbench-Stream-State: live-durable`. The capability and both finite/live service reads remain default-off and require the independent exact-principal directory cohort; implementation alone did not promote the cohort, while 8.3 records its scoped local promotion evidence. File-backed SQLite tests cover historical-to-live delivery, stream-limit rejection, cancellation/slot reuse, and slow-consumer termination. Live SDK conformance opens the directory stream under an explicit local test cohort before resolving a gate and observes the resulting session update, then aborts explicitly. Full affected `CGO_ENABLED=0`, transport/conformance, TypeScript, and focused four-package `-race` checks pass. Browser reconnect/query-refresh fallback and broader runtime shutdown/restart measurements remain in 4.3–4.4.
- 4.3 uses `AgentDirectorySupervisor` for exactly one workspace watch. It treats every cursor as opaque, resumes only from the last delivered server cursor, records last-confirmed time, and publishes explicit `connecting`, `live`, `degraded`, `offline`, and `stopped` states. Stream error/end or `resync_required` triggers only bounded workspace-summary refresh and bounded reconnect; fallback pages update safe summaries and last-confirmed state but never advance or fabricate the stream cursor. `AgentConversationWorkspace` merges these server-authored summaries over the retained legacy `listSessions` route, renders truthful Live/Connecting/Degraded/Offline plus last-confirmed status in the rail, and aborts the supervisor on cleanup. Unit tests cover cursor resume, retry ceilings, resync, degraded projection preservation, live rail state, degraded safe-summary fallback, and explicit abort. Real HTTP SDK conformance exercises the supervisor across a gate transition. No raw prompt/provider/tool fields are introduced.
- 4.4 evidence covers every named axis. Same-millisecond sequence tests prove event ordering and deduplication; critical-transition fixtures prove routine coalescing without losing proposal/gate/terminal attention; eight concurrent callers plus slow-consumer tests cover concurrency/backpressure; a file-backed fixture closes and reopens both SQLite store and Agent service then resumes at sequence 2 without replaying sequence 1; managed tenants sharing one workspace ref remain isolated; SDK fallback preserves degraded projection freshness without claiming live; supervisor tests cover reconnect and cursor retention. The browser component test asserts exactly one workspace directory watcher plus one selected-turn watcher and verifies both transport `AbortSignal`s terminate on cleanup. Focused `CGO_ENABLED=0`, repository `-race`, SDK conformance/unit, Web component, and TypeScript checks pass; browser viewport/Playwright proof remains a later 9.4 evidence tier rather than being implied here.
- 5.1 keeps `/agent` on one `AgentConversationWorkspace`/TaskService control plane and removes the alternate `AgentOperationsWorkspace` mount from the route. The unified component exposes one semantic `AgentSessionRail`, one conversation column and `role=log` timeline, one session-scoped composer, and at most one registry-backed pane host; a focused structure test asserts those exact cardinalities and that the old `Message Pi Agent` cockpit composer is absent. Drafts, panes, active turns, live events, Context Pack/phase/message, submit-pending state, historical events/errors, and their asynchronous callbacks are keyed by `sessionRef`, so delayed Context preparation, delayed history, or a pending submit in session A cannot attach state to or disable session B. A regression prepares Context independently in A and B, detaches B, and proves A remains attached. `git show HEAD` confirms the pre-change fallback was already `AgentConversationWorkspace`; capability-off still uses that conversation route while additive directory/tool/context workspace capabilities fail closed, rather than preserving a second shell. Focused route/component coverage passes 32/32, TypeScript and diff checks pass, and an independent reviewer approved. Tasks 5.7 alternate spatial-shell cleanup/component reuse, 5.8 responsive semantics, and browser/production evidence remain open.
- 5.2 upgrades the rail into a server-organized directory: 250ms bounded search, Recent/Pinned/Unread/Archived predicates, cursor pages, New, attention/unread badges, query-observed last-confirmed truth, degraded legacy safe-summary fallback explicitly marked organization-unknown, and native focusable overflow actions for safe rename, pin/unpin, and archive/unarchive. Archived pages now use a typed `archivedOnly` predicate propagated through SDK HTTP, HTTP handler, Go domain/repository, protobuf/gRPC, and generated bindings; repository filtering occurs in SQL before the page limit and has a page-size-one regression. Presentation mutations wait for server CAS/idempotency responses, read back network-unknown outcomes, confirm matching state, or offer an explicit same-key retry; typed archive/revision/idempotency rejections never claim success. Page changes clear stream/first-page increments and filtered pages use query `observedAtUnixMs`/freshness instead of fabricated live state. Focused Web coverage passes 35/35, TypeScript, buf lint, repository/HTTP/gRPC Agent tests, SDK HTTP/client tests, strict OpenSpec validation, and diff checks pass. Browser focus/visual evidence remains with later responsive/E2E gates rather than being claimed here.
- 5.3 replaces the single global active-turn/event state with session-scoped in-memory maps while keeping raw drafts browser-only. Running sessions no longer disable rail selection or New; switching aborts only the selected turn's transport stream and never calls cancel/pause or mutates the Task. Returning to the session restarts its one detail stream with `afterSequence` equal to the highest already observed server sequence, while a per-task seen set suppresses replay duplicates and the directory stream continues to carry background attention. `unknown_accept` blocks only its own composer with a reconcile-only explanation; other sessions and New remain available, and Stop is not exposed for the uncertain state. Component coverage submits a running turn, switches sessions without cancellation, preserves the other session's draft, returns to the running timeline with cursor resume, and separately verifies `unknown_accept` isolation. The combined focused Web suite now passes 10/10 with TypeScript checks; the remaining per-session view-state acceptance is covered by 5.4.
- 5.5 keeps the raw draft in the disabled submitting composer and does not append either the user block or run card until `SubmitAgentTurn` returns a server-authored Task acceptance. A rejected submission retains the draft and adds only a safe error banner. Stop leaves the visible Task state unchanged while cancellation is pending, then adopts only the returned server status (`cancel_requested` in the focused fixture); it never fabricates `cancelled`. This workspace does not render a read-acknowledged claim from local selection or focus, so no acknowledgement is shown without a future server observation. Focused component tests cover pending acceptance, accepted commit, rejected draft retention, and pending-versus-confirmed Stop; the combined suite now passes 10/10 with TypeScript checks. Read-ack presentation itself remains part of the broader session-rail work rather than being claimed as implemented here.
- 5.4 stores drafts, live timeline blocks, active turns/events, pane intent (including the selected output ref), and timeline view position only in component memory keyed by `sessionRef`; the existing per-Task seen-sequence map remains the durable-cursor resume source within the mounted browser workspace. Session selection snapshots `scrollTop` plus bottom-stick behavior before navigation, clears stale focus-return ownership, and restores the destination session synchronously after render. Component coverage opens a proposal Review pane, records a non-bottom timeline position, switches to another session with its own draft, then returns and observes the same selected output pane, scroll position, and `afterSequence` resume without cancellation or duplicate Task creation. A fresh component mount proves the raw draft and pane are absent, and no production code writes these values to localStorage or the service. The focused suite passes 10/10 with TypeScript checks; this is explicitly mounted-session recovery, not reload restoration.
- 5.6 registers the six frozen Agent pane IDs on the existing `desktop/registry/pane-registry.ts` authority with unique static bridge components, closed per-pane params, safe document keys, unknown-version failure, and no dynamic import. `resolveAgentPane` now retains the canonical entry component, and `AgentPaneHost` mounts that component inside a bounded runtime provider instead of selecting content through local `kind` branches. Each registered component can invoke only its matching Context, Run, Review, Evidence, Operations, or Context Map runtime slot; Operations and Context Map remain truthful `needs_contract`/unavailable views. Registry coverage proves component identity is retained and an Operations component cannot invoke any other slot. The focused registry/resolver/component suite passes 36/36 with Web TypeScript checks.
- 6.5 adds a renderer-local `AgentPresentationIntentResolver` that first reuses the SDK contract validator, then rechecks authorized refs, current session/source identity, Context Pack revision, expiry, canonical pane resolution, and caller-provided current pane availability. It deduplicates by `(sessionRef,intentRef,sequence)`, marks replayed or suggest-policy deliveries `suggest_only`, classifies live `follow_eligible` delivery without applying it, and keeps only a bounded diagnostic ring containing status/reason/kind/sequence rather than candidate payloads. Known pane domains with unsupported versions receive `unsupported_safe_view`; arbitrary values remain `unsafe_view_request`. Unit coverage passes 10/10 for valid pane resolution, dedupe, replay safety, expiry, scope, stale revision, version handling, unsafe URL-like refs, availability, and diagnostic redaction. The resolver has no Task/proposal/Owner methods and therefore cannot mutate execution state; suggestion rendering and activation remain open under 6.6.
- 6.6 renders all five allowed presentation kinds as explicit suggestion cards on the base output. Click activation re-runs SDK validation plus the browser resolver against the current session, source output, authorized safe refs, Context Pack revision, expiry, pane registry, and availability; rejection adds only a bounded reason banner and leaves the base output visible. Operations resolves to a truthful `needs_contract` view, while a stale Context Map request is rejected without a backend call. There are no hover/focus activation handlers. Focused registry/resolver/component coverage passes 35/35 with Web TypeScript checks.
- 6.7 obtains the `followPi` gate from the canonical workspace-summary response rather than a URL or browser-only capability flag. Consent is off by default, stored only as the currently followed `sessionRef`, cleared on session change, and naturally absent after reload/remount. The live selected-turn watcher may apply only resolver-classified `follow_eligible` `open_pane` or `show_evidence` effects; `focus_safe_ref`, replay, `request_review`, `prefill_draft`, and attention remain suggest-only. A shared guard blocks dirty composer, active Review, or external modal states without consuming the suggestion. Automatic panes have no trigger and the mobile focus effect runs only for explicit user-triggered opens, so Follow Pi does not focus the Close control; a polite bounded announcement reports the reversible visual change. Component/unit coverage proves server gating, default-off consent, live application, replay/allowlist and interaction guards, no Context/proposal backend calls, no automatic focus move, and a manual-only focus-safe-ref path. Current canary evidence is recorded under 8.4.
- 6.8 keeps `prefill_draft` suggest-only and same-session: a click inserts only the validated bounded template when that composer's draft is empty. A second click against a dirty composer is refused without overwriting text, preparing or attaching Context, submitting a turn, or creating a Task. SDK fixtures drop unsafe draft/tool fields, and focused Web assertions verify `submitTurn` and `prepareContextPack` remain untouched.
- 6.9 maps `request_review` only to the current output's registry-resolved Review pane. The focused fixture exposes a server-projected proposal whose ActionDescriptor is `needs_contract`; navigation succeeds, the Accept control remains separately disabled, and no `acceptProposal` call occurs. Intent activation contains no reject, retry, reconcile, proposal mutation, or availability-revival path.
- 6.10 combines SDK contract, resolver, registry, and Web component security coverage. The resolver matrix maps all six allowlisted kinds to bounded effects and exercises unknown pane version, unknown/future kind, registry contract mismatch, stale/missing Context revision, expiry, session/source mismatch, unauthorized cross-workspace refs, unavailable panes, duplicate delivery, and replay/suggest-only classification. A browser-control sentinel fixture includes CSS selector, element ID, XPath, URL, HTML, JavaScript, CSS, keyboard injection, form submission, dynamic component name/props, raw tool args, credential, and private path; the safe effect contains none of those values. Follow guards cover disabled consent, dirty draft, active Review, external modal, and disallowed auto kinds. Component tests prove default/server-disabled behavior, explicit shutdown, session-switch shutdown without reactivation, automatic focus safety, suggestion keyboard/click parity, and no Context preparation, proposal acceptance, cancellation, or reconciliation on hover/focus. Focused Web tests pass 49/49 and SDK contract tests pass 6/6; TypeScript checks pass. This remains focused jsdom/unit evidence rather than Playwright, screenshot, cross-tenant service, or production security proof under 9.4–9.6.
- 7.1 completes the existing additive summary fields rather than creating a second Context authority. The Go projector decodes only the sealed Agent turn ReplayInput fields `contextPackRef` and `contextPackRevision`, requires a safe ref and positive revision, and projects the latest valid pair while ignoring malformed/unsafe input. A focused fixture proves an arbitrary URL and Provider sentinel cannot enter the summary, and the projector does not mutate canonical Tasks. SDK coverage retains the typed last-pack pair. Web reads it only from the canonical workspace-summary response and renders it as a detached hint; session selection performs no Context API call and does not set the active pack. `Reauthorize last context` calls the authorized read and attaches only when the returned revision exactly matches the recorded revision; a newer revision produces a truthful stale message without substitution. `Explicitly refresh last context` sends the recorded expected revision, while `Detach context` clears only browser attachment state and leaves the server summary intact. Focused `CGO_ENABLED=0` Agent tests, SDK 6/6, Web 51/51, i18n, and TypeScript checks pass; Context browser/Provider evidence remains open.
- 7.2 now uses one server-authored `ActionDescriptorV1` mapping across proposal cards, registry-mounted Review, session attention, Output Capsule/Activity Rail, Inspector, and composer tool availability. `AgentToolDescriptor` gained an additive optional descriptor in Go/JSON Schema/SDK and a new additive gRPC `ListAgentTools` DTO/RPC; HTTP and JSON-RPC retain their existing read routes. `ListTools` resolves every descriptor server-side and derives the released three-state availability only as a compatibility mirror. The current `ToolActionCanary=unavailable` forces every tool non-ready, and scoped identity failures return a bounded `scope_unavailable` descriptor with no tenant/workspace provenance. SDK normalization requires exact action ID plus a non-empty exact target Operation match, otherwise dropping the descriptor and failing closed to `needs_contract`. The unified composer requests the authorized `workspaceRef`, renders shared status/recovery text as non-interactive catalog items, keeps chat usable when the catalog is unavailable, and exposes no tool submit/prepared-call/Owner action or hover/focus side effect. Focused CGO-disabled Agent plus HTTP/gRPC/JSON-RPC tests, SDK 5/5, Web conversation/activity/handoff 32/32, TypeScript, i18n (2296 keys), buf lint, schema parse, strict OpenSpec, and diff checks pass. An independent verifier reproduced the gates and an independent reviewer approved after two findings—target identity fail-open and swallowed scope errors—were repaired. This proves the read-only local contract/UI slice only; 7.4 and 8.5 remain gated on real proposal authority, approved Owner adapter, Provider, receipt/reconcile, deployment, and production evidence.
- 7.3 confirms the current proposal path is not authoritative enough to enable actions. Both Agent Web shells send browser-projected `basisRefs` and omit an explicit decision; the SDK maps descriptor revision into generic `expectedOwnerVersion`, while `TaskService` reloads operation/policy/project/idempotency state but does not reload the proposal, descriptor, basis visibility, proposal revision, or expected domain versions before Task creation. The registered `orbit.proposal.accept` operation remains unavailable and its handler fails closed, so generic permission/cost gates prove no Owner execution authority. The action canary therefore remains `needs_contract`/unavailable. The separately owned `openspec/changes/workbench-agent-proposal-authority-v1/` now contains complete proposal/spec/design/tasks and is the required implementation dependency for 7.4; this change continues to own only Agent-first presentation and consumption.
- 7.5 makes the source turn's server-authored `unknown_accept` override any older proposal descriptor state in the unified Review pane and in the acceptance callback itself. Timeline and Run expose only the original reconcile path, the composer is disabled only for the affected session, the rail retains canonical unknown attention while navigation remains available, and Activity/Inspector already suppress accept/retry/cancel in favor of reconcile. Presentation-intent tests continue to prove no mutation on hover/focus or replay. Focused conversation/activity/handoff coverage now passes 31/31 and asserts no second submit, proposal accept, automatic cancel, Retry control, or fabricated terminal state; TypeScript and i18n checks pass. This is focused local evidence, not Owner/Provider reconciliation proof.
- 8.1 adds an internal authenticated shadow report that rebuilds only projection rows, compares the retained `ListAgentSessions` semantics with the new directory over the same tenant/operation-filtered bounded source universe, and classifies count, exact activity-derived ordering, last-turn state, attention precedence, and bounded safe-summary differences. A 120-session SQLite dataset exercises multi-page listing, foreign-tenant/non-Agent exclusion, proposal activity enrichment, sentinel redaction, long-summary truncation, and deep canonical Task/event/attempt/receipt snapshots before and after. Deterministic fixtures prove late proposal activity cannot replace the latest turn identity, `unknown_accept` cannot be downgraded to waiting review, unexplained differences remain unresolved, exactly 256 sessions compare successfully, and 257 fail closed before projection writes. Full affected `CGO_ENABLED=0`, focused `-race`, strict OpenSpec, and diff checks pass; an independent reviewer approved after four findings were repaired. This remains focused local shadow evidence, not browser, Provider, deployment, or production proof.
- 8.2 makes `workbenchd` the only rollout owner through `WORKBENCH_AGENT_PI_WORKSPACE_READ_ONLY_COHORT` and the independent `WORKBENCH_AGENT_DIRECTORY_STREAM_COHORT`; both are empty by default, ignore unsafe refs, and match only the authenticated exact principal. HTTP, gRPC, JSON-RPC, and SDK project the shared service decision, while finite catch-up and live directory reads enforce the directory cohort before rebuild or stream allocation. `/agent` always retains the conversation workspace and selects unified read-only mode only after a current successful server capability response; URL/build/storage flags cannot elevate it. Loading, first-load error, refetch error, or flag-off removes directory/tool caches, Follow Pi state, and attached/recorded Context Pack affordances before returning to legacy mode. Focused service/config/runtime, repository/transport, 26 Web route/component tests, TypeScript, live typed SDK, strict OpenSpec, diff checks, and independent review pass. The documented process restart preserves canonical Tasks/events/receipts/Owner state while allowing normal startup to refresh rebuildable projection/cursor/high-water rows. This is focused local flag-on/off evidence only; browser E2E, operational restart rehearsal, deployment, Provider, and production remain unproven. Directory promotion is separately evidenced under 8.3.
- 8.3 promotes only the independently authorized directory background layer for the scoped local canary. Runtime startup with both exact-principal cohorts enabled returns `directoryStream=enabled` and serves durable catch-up; default-off and service authorization tests still reject non-cohort catch-up/watch before rebuild or stream allocation. SDK supervisor evidence proves opaque-cursor resume, resync without adopting a control cursor, bounded refresh/reconnect, degraded/offline truth, preserved degraded projection freshness, and immediate `stopped` convergence when a fallback page reports that the directory capability was removed. Repository and four-transport parity prove a read acknowledgement through an older cursor cannot clear newer activity. Web preserves the server-authored unread marker, does not acknowledge on session selection, owns one directory stream plus one selected-turn stream, aborts both on cleanup, renders last-confirmed/degraded state, and stops the active directory signal after server-authored capability removal without a manual query invalidation. Focused runtime/repository `CGO_ENABLED=0`, repository `-race`, transport parity, SDK 5/5, Agent Web route/component 28/28, live typed SDK, TypeScript, strict OpenSpec, diff checks, and independent review pass. Browser/Playwright, a real process-restart rehearsal, deployment, Provider, production, eventual foreground visible-cursor acknowledgement UX, Pi runtime, and tool-action readiness remain separate and unproven.
- 5.7 removes the unreachable `AgentOperationsWorkspace`, legacy `AgentCockpit`, browser flag helper, and their direct shell tests so `/agent` has no alternate composer, Context preparation owner, or turn-event subscription. The unified registry panes now reuse `ContextCanvas`, extracted `ObjectInspectorContent`, shared ActionDescriptor status/recovery, `EvidenceBlock`, and the real `OrbitRuntime`; Operations mounts only for a closed server-authored `projectRef` plus `owner:<id>` safe target, otherwise it remains `needs_contract`. `OrbitView` and `createOrbitPaneRegistry` require explicit facades, while tests inject mocks explicitly, eliminating production implicit mock fallback. Evidence mapping preserves `offline` and `expired` as non-ready states after independent review caught the initial fail-open presentation. Focused Agent tests pass 85/85, Orbit tests 21/21, TypeScript, strict change validation, and diff checks pass; the follow-up reviewer reports no remaining P0-P2 findings. Browser/E2E, real Owner/Provider, deployment, and production proof remain open under 5.8 and 9.4-9.9.
- 5.8 implements the fixed responsive contract: desktop uses a 248px inline rail and exact registry-pane widths (Context 360px, Run 400px, Review 420px, Evidence/Context Map 480px, Operations 528–560px); tablet uses a 288px left modal rail plus right modal pane; mobile substitutes each with one full-screen modal while preserving the hidden pane session state. Modal focus enters the close control, Tab remains contained, Escape restores the real trigger, body scroll locks, and all visible Agent/global locale controls meet 44px. Operations and Work Chart use labelled mobile record lists and internally scrolling desktop tables without page overflow. Playwright passes 4/4 at 1440x960, 1024x768, 390x844, plus a documented 720px effective-width proxy for 200% with reduced motion; focused responsive/Orbit coverage passes 81/81, TypeScript, strict OpenSpec, and diff checks pass. Read-only browser reruns report zero serious/critical Axe findings and no overflow; independent review findings for rail Escape propagation and changing server-authored Orbit default panes were repaired and re-reviewed. This is local fixture/browser evidence only: native browser zoom, server-authorized live Operations, Owner/Provider, deployment, and production remain unproven under later gates.
- 5.9 adds 38 previously missing Agent/Orbit messages, including directory connectivity, session organization/recovery, archive conflict, modal aria, uncertain-turn composer, and responsive record-list states, to the modular zh-CN/en-US sources and protected catalog policy where truth or recovery semantics must not be overridden. Regeneration produces 2,334 parity-checked server/Web keys. `check:i18n` now scans every quoted literal in known Agent/Orbit message families, including indirect constants, with only two explicit registry protocol literals allowlisted; that stronger gate exposed and repaired the previously hidden `archiveBlocked` and `presentationConflict` fallbacks. Composer coverage passes 5/5 and confirms key parity, placeholder retention, Chinese-first copy, English fallback, and preserved technical terms. Focused Agent/Orbit tests pass 85/85 and TypeScript passes. Playwright passes 5/5 across the responsive matrix plus a bilingual 1440x960 route using the real generated catalog metadata; it verifies zh-CN startup, en-US switching, unchanged `pi.local`, Scaena, session/task/operation/receipt/context safe refs and raw typed user input, zero HTTP >=400/request failures/page errors, and zero serious/critical Axe findings. Independent source and browser reviews pass. Private paths, credentials, raw prompts, and Provider payloads remain intentionally absent rather than being inserted merely to test translation; this remains local fixture evidence, not Owner/Provider, deployment, or production proof.
- 6.4 adds a typed SDK validation result with bounded `unsupported_safe_view`, `unsafe_view_request`, `scope_mismatch`, `stale_view_request`, and `expired_view_request` reasons while preserving the legacy null-returning normalizer. Contract fixtures cover valid projections, unknown/future kinds, expiry, session/source scope, Context Pack revision mismatch, arbitrary raw tool/provider fields, and draft sentinels such as script, credential, private path, provider payload, API key, and raw URL. This is SDK/contract evidence only; the browser resolver, Follow Pi, and browser security evidence remain open in 6.5–6.10.
- Focused evidence currently covers `CGO_ENABLED=0` and race tests for Agent/repository/transports, SDK tests, `bun run typecheck`, `buf lint/generate`, schema JSON parsing, and strict OpenSpec validation. It does not prove browser/E2E, real Pi/Owner Provider, deployment, or production readiness.
- 8.4 local canary closeout (2026-08-23): `WORKBENCH_AGENT_PRESENTATION_SUGGESTIONS_COHORT` and `WORKBENCH_AGENT_FOLLOW_PI_COHORT` are exact-principal, default-off server-only cohorts. `followPi=enabled` requires the same authenticated principal to be present in the read-only, presentation-suggestions, and Follow Pi cohorts; a Follow-only principal remains disabled, unsafe refs are ignored, clearing any prerequisite rolls the capability back without session writes, and tool/proposal mutation capabilities stay unavailable. The first browser canary auto-applies only `open_pane/show_evidence`; `focus_safe_ref` remains suggest-only despite `follow_eligible`, and automatic panes never receive keyboard focus. Redacted component evidence `temp/integration-test-runs/20260823124343-861f3b6c-874b-4a95-8938-b0f323fecdfa/` passed in 178.843s across CGO0/race Agent/runtime/repository/transports, SDK, 108 Web tests, and typecheck. Redacted Chromium evidence `temp/integration-test-runs/20260823124857-f3452caf-2ed9-4f5d-9fa8-068884063eab/` passed 9/9 in 43.584s with directory resume, responsive fallback, reduced motion, bounded Evidence auto-open, focus containment, Axe serious/critical zero, and enabled-to-disabled rollback. Tasks 8.2–8.4 are therefore closed for the local server-authorized canary; no authenticated Owner/Provider, staging, deployment, production, native browser-chrome zoom, manual assistive-technology, or `focus_safe_ref` auto-promotion evidence is claimed.
