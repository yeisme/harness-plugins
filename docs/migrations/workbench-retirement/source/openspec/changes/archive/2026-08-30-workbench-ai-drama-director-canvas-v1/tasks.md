## 1. Projection 与画布基础

- [x] 1.1 定义 Director Workspace 投影类型（`DramaContextRef`/`ArtifactRef` 节点边、owner/version/freshness）于 `packages/task-sdk/` 与 service 层，additive exports，不复制 owner 状态机。 **Evidence (2026-08-30)**: `packages/task-sdk/src/director-canvas-{models,client}.ts`（合同 `workbench.director_canvas.v1alpha1`；`DramaContextRefV1`/`ArtifactRefV1`/`DirectorGraphEdgeV1`/`DirectorGraphV1`/`DecisionTokenV1`/`EditorDiffV1` + envelope 封闭 codec + 提交校验镜像）；service 层 `service/internal/showcontrol/directorcanvas.go`（closed 枚举/上限/校验/Marshal envelope）+ `service/internal/registry/directorcanvas.go`（四 operation 注册：`show.director_graph.get`/`show.editor_diff.get` 只读，`show.decision_action.submit`/`show.external_edit.handoff` mutation 过 idempotency/permission/cost gate；owner 口未绑定 fail closed needs_contract）+ runtime.go 接线；`WorkbenchClient.directorCanvas` additive 注入。测试：showcontrol/registry 两包 Go 测试 + SDK `test/director-canvas.test.ts` 11/11。
- [x] 1.2 在 `/agent` Unified Spatial Creative Runtime 内实现 Director Workspace 模式与 `Show -> Episode -> Scene -> Shot/Asset` 可缩放语义画布；不新增独立路由，不恢复 `/show-control-room`。 **Evidence (2026-08-30)**: `apps/web/src/workbench/agent/spatial/director-canvas/`（确定性状态机 `director-canvas-state.ts` 的 `update(state,event)`/`render(state)` + 面板 + closed 持久化）；挂载于 Creative Production Lens（`creative-production-lens.tsx` additive 分区），门控 = 显式 prop / `VITE_WORKBENCH_DIRECTOR_CANARY` env / `/agent?director-canvas=1` URL canary；无新路由。zoom 语义层级 + 节点点击自动下钻（select 即 focus+zoom）；组件测试 `test/director-canvas-panel.test.tsx` 8/8（含 lens 门控两例）。
- [x] 1.3 实现画布本地状态（layout、camera、selection、expanded set）的保存与恢复；持久化层不含任何领域关系或接受终态。 **Evidence (2026-08-30)**: `director-canvas-persistence.ts`——单槽 `workbench.director_canvas.v1:<workspace>:<project>:<show>`；closed 字段集合（zoomLevel/focusRef/expanded/selectedRef，≤128 expanded，opaque ref 校验），未知字段（含任何 receipt/decision/edges 载荷）写入/恢复均整体拒绝（fail closed）；状态机 `restore_local_state` 事件 scope 不匹配即丢弃，投影重建后 unknown ref 诚实丢弃。测试：`test/director-canvas-state.test.ts`「本地持久化面 closed 合同」+ panel 测试「本地持久化恢复」。
- [x] 1.4 实现 selection inspector：选 scene 定位关联 shots/assets/review decisions/delivery readiness，全部经 owner refs refetch；owner 缺字段时标记 degraded 而非本地补全。 **Evidence (2026-08-30)**: `buildInspector`（director-canvas-state.ts）——关联集合全部由投影边派生（episode 聚合两有界层级）；relatedDecisions 只按 token.targetRef 匹配；delivery readiness 折叠 min 语义；任何非 available 已观测 readiness 或 needs_contract → degraded 标注。测试：状态机「zoom + focus：episode 层渲染 scene；scene 选中后 inspector…」+ e2e 断言。

## 2. 共享决策箱 consumer

- [x] 2.1 接入 owner-authored decision token 的 typed action 消费，覆盖费用、版权、canonical accept、外编 apply、final export 五类决定；mutation 全部经 `TaskService` permission/cost/expected-version/idempotency gate。 **Evidence (2026-08-30)**: `show.decision_action.submit`（registry RequiresIdempotency/Permission/Cost + Mutation）；提交时重验 decision token（`CheckDecisionToken`：owner/target/version/kind 漂移或过期 → stale 拒绝——expected-version 语义由 targetVersion 重验承载）；五类 closed 枚举 `DecisionKindCost|Rights|CanonicalAccept|ExternalEditApply|FinalExport`（SDK `validateDecisionActionInput` 五类全过 + 未知动词拒绝测试）。owner 合同未签约 → needs_contract fail closed（registry 测试）。
- [x] 2.2 实现 visual diff 批准面与幂等 receipt 刷新；已终态 token 返回 stale/already_decided 时只 refetch，不重复 mutation。 **Evidence (2026-08-30)**: token.visualDiffRef 在决策条目呈现（visual diff 批准面）；`SettleDecisionAction` 把 owner 的 already_decided 结算为幂等成功（返回原 receipt，不重复 mutation）；面板 receipt 处理只 `reread()`（refetch 投影）；unknown_accept/partial → reconcile_required 禁自动重试。测试：SDK client「carries the closed input and returns control-plane facts」+ 状态机「decision_receipt：settled/already_decided/reconcile/stale 四态」+ panel「决策提交流」+ e2e 断言（submit 恰 1 次、graph refetch ≥2、already_decided 相位呈现）。
- [x] 2.3 保证本地 dismissed/selected 状态不进入 receipt、不被记录为 accepted；与 DSH 共用同一 decision identity 的跨端用例由契约测试固定。 **Evidence (2026-08-30)**: 状态机 `decision_local_dismiss/select` 只写入展示集合（receipt 事实只由 decision_receipt/decision_error 事件携带——状态机测试断言本地集合不改变 decision 状态机）；panel 测试「本地 dismiss 只折叠展示；不产生任何提交事实」（submitDecisionAction 调用数为 0）；跨端 decision identity 契约：`test/director-canvas-state.test.ts`「2.3 跨端 decision identity 契约」+ SDK closed 输入形状测试（identity/kind/target/version 同构，漂移 fail closed）。

## 3. 外编 handoff

- [x] 3.1 对 frame-level trim、track edit、transition、effect、color 请求呈现受控外编 handoff action，导向 Scaena delivery bundle 流程；画布内不创建 timeline 编辑状态。 **Evidence (2026-08-30)**: `show.external_edit.handoff`（closed operationKind 枚举五种；未知 timeline 类动词 closed 拒绝——Go/SDK 测试）；面板 shot 层呈现五个受控 handoff action → `OpenExternalEditHandoffPort`（Scaena bundle owner 口；未绑定 needs_contract fail closed）→ summary envelope 携带 handoff_ref + follow_up `scaena.delivery_bundle.open`；画布状态机 handoff 只有 open/submitting/submitted/failed 展示相位，无任何 timeline 编辑状态。测试：registry「opens bundle entry」+ panel「外编 handoff 流」+ e2e 断言 handoff_ref。
- [x] 3.2 消费 Scaena grouped semantic diff 与 quarantine report 投影，视觉呈现支持组并标注隔离组；分组 accept/reject 经决策箱 decision token 提交。 **Evidence (2026-08-30)**: `show.editor_diff.get` 只读 composer（EditorDiffSource 未绑定 needs_contract）；`EditorDiffV1` 分组投影（groupRef/sceneRef/trackRef/changeType/changedRefs≤16/quarantined + quarantine_summary）经 envelope 封闭 codec；面板 diff 区呈现组并标注隔离组（`data-testid=director-diff-quarantined-*`），组条目注明 accept/reject 经决策箱 decision token（decisionKind=external_edit_apply）提交。测试：registry「fails closed without scaena binding」+ panel「分组 diff 投影：隔离组标注」+ e2e。

## 4. 证据与回滚

- [x] 4.1 组件级 golden snapshot 覆盖语义画布关键渲染状态与选区状态；状态转换由确定性 `update(state, event)` / `render(state)` 测试覆盖。 **Evidence (2026-08-30)**: `test/director-canvas-state.test.ts` 10 用例（确定性 update/render：层级渲染/计数/选区 inspector/版本漂移 stale/degraded/本地集合不变量/handoff 状态机/diff 隔离/持久化 closed 合同/跨端 identity）+ `test/director-canvas-panel.test.tsx` 8 用例（needs_contract 诚实态/ready 渲染+持久化恢复/already_decided 幂等/本地 dismiss/外编流/diff/lens 门控两例）。
- [x] 4.2 Playwright e2e 覆盖进入 Director Workspace、打开 decision、执行 handoff、刷新 receipt 完整路径；证据写 `temp/integration-test-runs/<run-id>/`（summary.json、command.txt、stdout.log、stderr.log、env.json、artifacts/），脱敏 secret/raw prompt/绝对路径，失败保留原始 exit code。 **Evidence (2026-08-30)**: `task test:director-canvas-e2e`（Taskfile 证据任务）run `20260830163116-9b2047b9-9118-4a45-be5d-f341bc107d94` exit 0 status passed——六件套齐全（summary/command/stdout/stderr/env/artifacts+digests+redaction receipt），artifacts 含 roundtrip 与 rollback-off 两张截图；2/2 用例：门开全链（进入 Workspace → 下钻 scene → inspector → decision accept already_decided 幂等 receipt → refetch → frame_trim handoff → bundle receipt → 分组 diff 隔离标注）与回滚路径（无 canary 参数不挂载、既有五面板不受影响）。
- [x] 4.3 验证命令：`cd client/yeisme-workbench && openspec validate workbench-ai-drama-director-canvas-v1 --strict --no-interactive`、`bun run typecheck`、`bun test`、`CGO_ENABLED=0 go test ./service/...` 全绿；回滚路径 = 关闭 Director Workspace flag 回到现有 `/agent` 默认，不回写 owner state。 **Evidence (2026-08-30)**: 四命令全绿——strict validate PASS；typecheck（根+web）0 错；`bun test --timeout 60000` 795 测试 0 fail（注：本机满载下默认 5s 超时会使既有子进程型用例偶发超时，单文件复跑即绿，非本 change 引入）；`CGO_ENABLED=0 go test ./service/... -count=1` 全 ok。回滚：`?director-canvas` 参数/env/prop 三层门默认关（e2e rollback 用例验证），关闭即回到既有 lens，无迁移、无删除、无 owner state 回写。
