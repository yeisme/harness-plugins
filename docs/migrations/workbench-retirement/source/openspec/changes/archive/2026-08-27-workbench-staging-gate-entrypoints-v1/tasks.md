# Workbench Staging 门禁入口实施任务

## 1. 入口与合同

- [x] 1.1 新增 `scripts/staging-gate-entrypoints.ts`（fail-closed preflight + 分发）；Owner: operations；Dependencies: 无；Paths: `scripts/staging-gate-entrypoints.ts`；Scope: `identity-soak`/`rollback-dry-run` 子命令、缺失输入只报变量名、exit 5 fail-closed、输入齐全时 spawn 既有 `workbench-project-soak`/`workbench-release` 并透传 exit code；Acceptance: 缺任一必需输入即拒绝且不泄露值；Verification: `bun test tests/staging-gate-entrypoints.test.ts`；Expected: 测试通过；Failure recheck: 输出含输入值或 exit code 被吞则视为合同违反。**完成 (2026-08-27)**：`scripts/staging-gate-entrypoints.ts` 交付；`missingGateInputs` 纯函数只返回缺失变量名，`executeGate` 在缺输入时 exit 5、未知子命令 exit 2、成功路径透传二进制 exit code；脚本与 evidence runner 均不输出输入值。
- [x] 1.2 新增 `tests/staging-gate-entrypoints.test.ts`；Owner: test-engineer；Dependencies: 1.1；Paths: `tests/staging-gate-entrypoints.test.ts`；Scope: fail-closed 缺失清单、无值泄露、exit code、成功路径 spawn 参数；Acceptance: bun test 全绿；Verification: `bun test tests/staging-gate-entrypoints.test.ts`；Expected: pass；Failure recheck: 不以 mock 冒充二进制真实执行语义。**完成 (2026-08-27)**：9/9 pass（未知 gate 拒绝、fail-closed 名单无值泄露、file 输入缺失检测、identity-soak 分发参数与 exit 透传、rollback-dry-run 两段 spawn 顺序与短路、gate 定义对齐）。
- [x] 1.3 Taskfile.yml 追加 `identity:soak` 与 `release:rollback:dry-run` 两个 target；Owner: operations；Dependencies: 1.1；Paths: `Taskfile.yml`（仅追加，不改既有任务）；Scope: `production:guard`（read/plan）+ run.ts system 证据包装 + preflight 分发；Acceptance: `task identity:soak ENV=staging` 与 `task release:rollback:dry-run ENV=staging` 在缺 staging 输入时 fail-closed 并写脱敏证据到 `temp/integration-test-runs/<run-id>/`；Verification: 本地执行两命令确认非零退出与证据包；Expected: fail-closed 证据完整；Failure recheck: 缺证据或静默成功均不得关闭。**完成 (2026-08-27)**：两 target 追加于 Taskfile.yml 末尾；本地无 staging 输入时二者均 exit 5 fail-closed——`identity:soak` evidence `20260827053514-95338f49-bcae-4969-8ac1-5a2ceb99250b`（status=failed、exit_code=5、redaction 0 命中通过、stderr 仅列 5 个缺失变量名）、`release:rollback:dry-run` evidence `20260827053641-e8ada763-3b0e-4548-bd36-000f5b3d5947`（status=failed、exit_code=5、stderr 仅列 `WORKBENCH_STAGING_ROLLBACK_RECEIPT`）；六件套（summary/command/stdout/stderr/env/artifacts+digest/receipt/redaction）完整。

## 2. 一致性同步

- [x] 2.1 核验 registry 状态翻转对 runbook 聚合 gate 的影响；Owner: operations；Dependencies: 1.3；Paths: read-only 核验；Scope: `release:rollback:dry-run` registry 状态 planned→available；Acceptance: runbook 四个事件行仍引用其他 planned/provider-blocked target，聚合 Current gate 不变，`docs/operations/production-incident-runbook.md` 与 `runbook_test.go`/`incident_drill_test.go` 零改动；Verification: `CGO_ENABLED=0 go test ./service/cmd/workbench-release -count=1 && task runbook:validate`；Expected: 全绿；Failure recheck: 若聚合 gate 翻转，必须同步 runbook 与合同测试而不是改 registry 定义充数。**完成 (2026-08-27)**：逐 target 核验 15 个 referenced target 的 Taskfile 存在性——deploy:validate、deployment:lookup、deployment:receipt:validate、tenant:cutover:plan、release:canary、disaster-recovery:drill、release:decision:validate 仍 absent（planned），db:backup/db:restore:verify 仍 provider_blocked；四个引用 `release:rollback:dry-run` 的事件行聚合 gate 均保持 `planned`/`provider_blocked` 不变，runbook 与两个 Go 合同测试零改动。`go test ./service/cmd/workbench-release -count=1` ok；`task runbook:validate` Ready（15 referenced、4 available、2 diagnostic、7 planned、2 provider-blocked）。
- [x] 2.2 OpenSpec strict 验证与证据记录；Owner: test-engineer；Dependencies: 1.3、2.1；Paths: evidence only；Scope: `openspec validate workbench-staging-gate-entrypoints-v1 --strict`、两入口 fail-closed 运行证据六件套、相关既有测试回归（`bun test tests/production-task-contract.test.ts tests/taskfile-docs-contract.test.ts`、`task test:incident-drill:component`）；Acceptance: strict 通过、证据包 redaction 完整、无既有测试回归；Verification: 上述命令实跑；Expected: 全绿；Failure recheck: 任一回归不关闭本 change。**完成 (2026-08-27)**：`openspec validate workbench-staging-gate-entrypoints-v1 --strict` valid；回归 `bun test tests/production-task-contract.test.ts tests/taskfile-docs-contract.test.ts tests/release-gates.test.ts tests/evidence/run.test.ts` 39/39 pass；`task test:incident-drill:component` pass（evidence `20260827053631-54d33633-a661-4f3c-8c71-74e3634c495a`，INC-CONFIG 仍 blocked exit 5）；`bun run typecheck` 通过。
