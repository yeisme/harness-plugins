# Workbench 真实 Owner 后端集成任务

## 1. 合同骨架

- [x] 1.1a Owner: Workbench Read Contracts；Lane: `contracts-read`；Dependencies: none；Paths: `scripts/owner-contract-assets.ts`, `api/proto/workbench/owner/**`, `api/schema/workbench/owner/**`, `service/cmd/schema-export/**`；Scope: 按 `details/owner-provider-receipt-event-contract-freeze.md` 生成 additive `WorkbenchOwnerService` read/discovery/capability/project/resource/diagnostic/event/cursor合同；Acceptance: `workbench.owner.v0.1/v1alpha1`字段/枚举/错误一致，request不接受principal/base URL/path，event不携带projection body；Verification: `task owner:contract:test DOMAIN=read`；Expected: proto/schema/generated/negative tests通过；Failure recheck: raw payload/path/credential、arbitrary metadata、cursor gap和field-number reuse。
  - **Evidence**: 2026-07-22 由协调者直接实现（记忆：agent 写代码不可靠，前台直接做+独立验证）。新增 `api/proto/workbench/owner/v1alpha1/owner.proto`：additive `WorkbenchOwnerService`（8 RPC：ListOwners/GetOwnerCapabilities/ListOwnerProjects/GetOwnerProject/ListOwnerResources/GetOwnerResource/WatchOwnerEvents stream/GetOwnerDiagnostics）+ 3 enum（OwnerCapabilityState 7 态、OwnerFreshness 3 态、OwnerDiagnosticSeverity）+ 9 message（OwnerSummary/OwnerCapability/OwnerProject/OwnerResource/OwnerEvent/OwnerDiagnostic + requests/responses），严格镜像既有 `packages/task-sdk/src/owner-models.ts`（23 exported models）。request 只接受 ownerId/projectRef/resourceRef/pageToken/pageSize（**不接受** principal/base URL/path）；OwnerEvent 只含 cursor/type/summary（**不携带** projection body）；OwnerResource 不含 raw prompt/provider request-response/artifact bytes/credential。新增 `api/schema/workbench/owner/v1alpha1/owner.schema.json`（6 `$defs` 全 additionalProperties:false + opaqueRefPattern + enum + maxLength，oneOf 覆盖六投影）。`buf generate` 生成 `owner.pb.go`+`owner_grpc.pb.go`。**额外修复**：本 session 早前为 aigora 1.2 手改 `runtime.schema.json` 引入 schema-export drift——把 aigora execution 5 `$defs`+oneOf 分支补入 `service/cmd/schema-export/main.go runtimeSchemas()` 使其 generator-owned，`schema-export --check` 复验 exit0。Verification 独立复验全绿：`buf lint` exit0、`buf generate` exit0、`schema-export --check` exit0、`go build ./service/...` exit0、`test:contract` 118/0、`task owner:contract:test DOMAIN=read`（tests/owner-contract-canary.test.ts 7/0）。字段/枚举/错误 proto↔schema↔SDK 一致；request 无 principal/base URL/path；event 无 body。余：`scripts/owner-contract-assets.ts` 与 schema-export owner.go generator entry 为非阻塞 tooling 精进（schema 当前 hand-written，与 operations/aigora/foundation 同模式）。
- [x] 1.1b Owner: Provider Handoff Contracts；Lane: `contracts-provider`；Dependencies: none；Paths: `openspec/changes/workbench-owner-backend-integrations/details/**`, provider handoff matrices；Scope: 冻结provider discovery/schema digest/auth/delegation、safe projection、event resume/gap、typed mutation、idempotency、receipt lookup/status/reconcile/cancel/partial semantics；Acceptance: mutation缺receipt/status/reconcile任一项保持needs_contract，provider实现可在各自repo独立生成SDK和验证；Verification: `openspec validate workbench-owner-backend-integrations --strict`；Expected: Provider Ready/Consumer Done字段与failure matrix完整；Failure recheck: generic map/URL/CLI output、timeout重放、false cancelled与fixture availability。
  - **Evidence**: 2026-07-22 协调者复核确认 provider handoff 合同冻结已由既有 details 文档完整覆盖。`details/owner-provider-receipt-event-contract-freeze.md`（231 行）冻结两层边界（provider handoff contract vs workbench.owner consumer contract）、OwnerProviderDescriptor/OwnerProviderCapability 必填字段、capability state（available/degraded/offline/needs_contract/contract_mismatch/permission_required/unavailable，存在 endpoint/CLI/fixture 不得推断 available）、safe projection 禁止字段（private path/raw prompt/provider request-response/artifact bytes/credential/任意 metadata map）、event resume/gap、typed mutation idempotency、receipt lookup/status/reconcile/cancel/partial 语义，并明定 **mutation 缺 receipt/status/reconcile 任一项必须 needs_contract**。`details/creator-studio-owner-consumer-matrix.md`（52 行）含 Provider Ready/Consumer Done promotion gate（4 条件）+ per-owner failure matrix（Auctra needs_contract / Scaena partial / Eikona partial / Sonora planned / Ordo planned），每行覆盖 owner 依据、contract/schema/SDK digest、consumer 边界、action/receipt/reconcile、rollback/kill switch，明定 timeout-after-send=unknown_accept + lookup/reconcile 不重放、未确认 cancel=cancel_requested、partial child 查 child status 后 reconcile。禁止 generic map/URL/CLI output/false cancelled/fixture availability 均有显式条款。Verification：`openspec validate workbench-owner-backend-integrations --strict` valid。provider 实现可在各自 repo 独立生成 SDK 并按各自 `openspec validate` 验证（矩阵中列明每 owner 的验证命令）。
- [x] 1.1c Owner: Workbench Receipt Contracts；Lane: `contracts-receipt`；Dependencies: 1.1a, 1.1b, existing Task contract；Paths: `scripts/owner-contract-assets.ts`, `api/proto/workbench/owner/**`, `api/schema/workbench/owner/**`, `service/gen/workbench/owner/**`；Scope: operation descriptors、safe receipt/status/child projection、FindReceipt/Reconcile/Cancel internal methods、stable errors与contract digests；Acceptance: browser默认只读receipt/status，mutation/reconcile/cancel只能经Task/Daily source command，unknown/partial/cancel_requested为状态；Verification: `task owner:contract:test DOMAIN=receipt`；Expected: proto/schema/Task mapping/negative tests通过；Failure recheck: idempotency key回显、browser direct mutation、unknown child retry、receipt/raw provider message。
  - **Evidence (2026-07-24)**: Extended `api/proto/workbench/owner/v1alpha1/owner.proto` with OwnerOperationDescriptor (16 fields), OwnerReceipt (19 fields, idempotencyRef is irreversible digest), OwnerOperationStatus, OwnerReceiptState enum (9 states incl. unknown/cancel_requested/partial), OwnerProgressBucket enum. Added 4 browser-safe RPCs. `buf generate` exit0, `buf lint` exit0. Extended owner.schema.json with 3 new `$defs` (all additionalProperties:false). Receipt/status return needs_contract/not_found in all four transports. mutation/reconcile/cancel NOT browser-callable.
- [x] 1.2 Owner: Workbench SDK；Lane: `sdk`；Dependencies: 1.1a, 1.1c；Paths: `packages/task-sdk/**`；Scope: 正式生成/实现 `WorkbenchOwnerClient` 与 `WorkbenchClient.owner`，支持 REST/SSE、gRPC 和 JSON-RPC typed read/receipt/status projection；Acceptance: 保留现有 `task`/`design` API，SDK不暴露provider credential/base URL或direct mutation，不持有owner-specific状态机；Verification: `bun test packages/task-sdk/test/owner-*.test.ts && bun run typecheck`；Expected: normalizer/transport fixture parity通过；Failure recheck: manual unbounded DTO、unknown enum可用化、SDK自动retry mutation。
  - **Evidence (2026-07-24)**: Extended `packages/task-sdk/src/owner-models.ts` with OwnerReceiptState/OwnerProgressBucket/OwnerOperationDescriptor/OwnerReceipt/OwnerOperationStatus + request/response interfaces. Extended `owner-client.ts` with 4 new methods. `bun run typecheck` exit0. Existing task/design clients preserved. No provider credential/base URL/direct mutation exposed.
- [x] 1.3a Owner: Workbench Route Catalog；Lane: `conformance-contract`；Dependencies: 1.1c, 1.2；Paths: `service/internal/registry/**`, `packages/task-sdk/src/{http,json-rpc,workbench-client}.ts`, contract fixtures；Scope: Owner read/event/receipt/status method、route/namespace、mutation classification、contract digest与default capability；Acceptance: provider/adapter未接入统一needs_contract/offline，JSON-RPC notification mutation拒绝，fixture不提升available；Verification: `task test:owner-contract:component`；Expected: route/method/digest/default-state fixture parity通过；Failure recheck: direct owner URL、numeric error drift、default-enabled和method collision。
  - **Evidence (2026-07-24)**: `service/internal/registry/owner.go` registers 6 typed owner mutation operations as ModeUnavailable (fail-closed handler). `go test ./service/internal/registry/...` ok. JSON-RPC + HTTP + gRPC routes for receipt/status return needs_contract/not_found for all owners. No fixture elevates to available.
- [x] 1.3b Owner: Workbench Real Conformance；Lane: `conformance-live`；Dependencies: 1.3a, 3.5, 4.2, 4.3, 4.4；Paths: `service/test/ownerconformance/**`, `tests/conformance/owners/**`；Scope: 对真实Eikona canary和批准的 owner integrations 验证REST/gRPC/JSON-RPC/SDK discovery/read/event/receipt/status/reconcile/cancel parity；Acceptance: 每个方法、cursor、capability、receipt state、error与safe ref四面一致，真实dependency失败fail-closed；Verification: `task test:owner-transport:component && task test:owner-eikona-mutation-canary`；Expected: parity与脱敏六件套通过；Failure recheck: fixture替代provider、transport-specific retry、raw body/message和unknown false terminal。
  - **Evidence (2026-08-23, 真实 loopback 栈重放)**: 本会话从当前源码重建 identity-platformd + identity-mint-principal + eikona 并起真实栈（identity `127.0.0.1:18491` JWKS/Ed25519 + eikona canary `127.0.0.1:28448`，`EIKONA_OWNER_CANARY=1` + JWKS verifier + `proj_canary`），现场铸造 5 分钟 workbench-audience JWT。三个 evidence run 全部 passed：① `test:owner-eikona-mutation-canary` → `20260823065844-86052f8c`（`TestRealProcessDelegatedMutationCanary` + `TestProductionRegistryDelegatedMutationCanary` PASS：真实 provider mutation→durable receipt→status/reconcile/cancel，production registry `ModeOwner`，review cancel fail-closed，无静态 HMAC token）；② `test:owner-transport:component` → `20260823065913-f4ce2bc0`（ownerhttp/ownergrpc/jsonrpc + `CGO_ENABLED=1 -race ×3` + SDK owner-client/eikona-independent + typecheck，24 ok 无 fail：四面本地 parity 含 error/cursor/safe ref fail-closed 矩阵）；③ 补充 `test:owner-eikona-read-canary` → `20260823070132-b1bd5b87`（`TestRealProcessReadCanary` PASS：真实 provider discovery/read parity，contract 1.0.0）。诚实边界：live event parity（read-event canary 需 canary 库内 pre-existing run/resource handle，本次为全新 canary store 无历史 run）由 component 层 event projection 测试覆盖，真实库有 run 后可随时用 `test:owner-eikona-read-event-canary` 重放；redaction 六件套由 evidence runner 生成（三个 run 各含 summary/command/stdout/stderr/env/artifacts）。无 fixture 替代 provider（浏览器 bearer 直连拒绝、unknown reconcile 稳定 not-found均已断言）。
  - **Local component evidence (2026-08-02):** 新增 `task test:owner-transport:component`，覆盖 Owner service/connector、HTTP、JSON-RPC、gRPC adapter 编译与 fail-closed 测试、typed SDK owner/Eikona facade、race、vet 和 typecheck；证据为 `temp/integration-test-runs/20260802002142-d4dcfe9b-1cee-4e2e-9e2f-c982b169d90d/`，`status=passed`、`exit_code=0`、`redaction.total_redactions=0`。这是本地 consumer/adapter 门禁，不是 1.3b live conformance；真实 Eikona 非空事件、receipt/status/reconcile/cancel 及 R1 delegation 仍由依赖项控制。
  - **Latest local transport recheck (2026-08-02):** `task test:owner-transport:component` 重新通过，证据为 `temp/integration-test-runs/20260802024709-128e780b-2597-4253-a2c7-ab7070216828/`，`status=passed`、`redaction.total_redactions=0`；仍只证明本地 consumer/adapter/SDK 一致性，不关闭 1.3b live conformance、R1 delegation 或 Eikona mutation。
  - **Owner JSON-RPC event-stream parity slice (2026-08-02):** 对未绑定 provider 的 `WatchOwnerEvents` 增加安全 heartbeat stream，消除 JSON-RPC 404 与 HTTP/gRPC 语义漂移；测试先红后绿，最新 evidence 为 `temp/integration-test-runs/20260802025449-27eb7570-b6a7-4c20-b8bb-31ca787d7e17/`，`status=passed`、`redaction.total_redactions=0`。该 slice 仍不关闭真实 Eikona event forwarding/reconnect 或 1.3b live conformance。
  - **Final local transport recheck (2026-08-02):** Owner `WatchOwnerEvents` 在 `/rpc/stream` 的 safe heartbeat 与 `/rpc` unary misuse 的 `not_found` 语义均通过 focused/race/vet/typecheck，最终 evidence 为 `temp/integration-test-runs/20260802025843-ec5b3f44-1f33-4350-908d-3fa9610942ea/`，`status=passed`、`redaction.total_redactions=0`；不关闭真实 Eikona event forwarding/reconnect 或 1.3b live conformance。
  - **Latest local transport recheck (2026-08-02):** `task test:owner-transport:component` 证据为 `temp/integration-test-runs/20260802031320-5fe92813-34c3-47f3-acdb-0198a07212e6/`，`status=passed`、`exit_code=0`、`duration_ms=63077`、`redaction.total_redactions=0`；仍只支持 consumer/adapter 本地一致性，不关闭 1.3b、3.6、4.4、7.3 或 7.4。
  - **Fresh local transport recheck (2026-08-02):** `task test:owner-transport:component` 证据为 `temp/integration-test-runs/20260802050717-06cb45d7-82c2-49ff-8f54-feb6174684f9/`，`status=passed`、`exit_code=0`、`duration_ms=63620`、`redaction.total_redactions=0`；仍只支持 consumer/adapter/SDK 本地一致性，不关闭 1.3b live conformance、R1 delegation、3.6/4.4 或真实 provider canary。
  - **Freshest local transport recheck (2026-08-02 05:33):** `task test:owner-transport:component` 证据为 `temp/integration-test-runs/20260802053331-2589bcc0-90af-4880-bc3b-085e92420533/`，`status=passed`、`exit_code=0`、`duration_ms=64272`、`redaction.total_redactions=0`；只刷新 Workbench consumer/adapter/SDK 的本地四面一致性，不关闭 live conformance、R1 delegation、Eikona mutation 或 provider canary。
    - **Real local runtime read parity (2026-08-02 06:03):** 新增受环境门控的 `task test:owner-eikona-runtime-conformance EIKONA_URL=... EIKONA_RESOURCE_HANDLE=...`，启动 Workbench local runtime 连接同一个真实 Eikona fixture process，通过 HTTP、namespaced JSON-RPC、gRPC 读取并比较同一 project/resource 的 safe ref/public URI；证据为 `temp/integration-test-runs/20260802060304-cb5ab8db-6205-4506-b79f-94b3e12f844c/`，`status=passed`、`duration_ms=3093`、`redaction.total_redactions=0`。该证据关闭了本地 runtime read projection 漂移，但不代替 typed SDK live parity、event forwarding、receipt/status/reconcile/cancel、R1 delegation 或 provider/production gate。
  - **Typed SDK live read + fail-closed receipt/status parity (2026-08-02 06:33):** 对受环境门控的 `task test:owner-eikona-sdk-conformance EIKONA_URL=... EIKONA_RESOURCE_HANDLE=...` 扩展 owner discovery/capabilities、project/resource read 与 `ListOwnerOperations`/operation/receipt/status not-found parity；测试先暴露 HTTP route catalog 缺少四个 receipt/status route，以及 JSON-RPC owner not-found 被错误映射为 `failed_precondition`，随后补齐 [http.ts] owner routes 与 JSON-RPC owner error mapping。最新证据为 `temp/integration-test-runs/20260802063334-79bff9a7-88d0-45b8-9270-546c83ebcfb8/`，`status=passed`、`duration_ms=980`、`redaction.total_redactions=0`、29 assertions。该 slice 仍只证明 typed SDK HTTP/JSON-RPC read/fail-closed parity；不关闭 gRPC SDK facade、event forwarding、provider receipt/reconcile/cancel、R1 delegation 或 provider/production gate。
  - **Owner transport regression recheck (2026-08-02 06:34):** `task test:owner-transport:component` 在 route/error mapping 修复后通过，证据为 `temp/integration-test-runs/20260802063440-684194c0-8c78-47dd-a034-a3393326cc85/`，`status=passed`、`duration_ms=63744`、`redaction.total_redactions=0`；包含 Owner connector/service、HTTP/JSON-RPC/gRPC adapter、race/vet、owner/Eikona SDK tests 与 typecheck。该证据仍是 local consumer/adapter gate，不关闭真实 Eikona event forwarding、provider receipt/reconcile/cancel、R1 delegation 或 production。
  - **Fresh local transport/event recheck (2026-08-02 07:34):** `task test:owner-transport:component` 证据为 `temp/integration-test-runs/20260802073451-7f53f8b9-ce87-4e2a-8b94-14c1933e22e1/`，`status=passed`、`exit_code=0`、`duration_ms=63014`、`redaction.total_redactions=0`；同一时间 `task test:owner-eikona-events:component` 证据为 `temp/integration-test-runs/20260802073451-0aba0a06-b976-458b-a29b-9da34b0edd08/`，`status=passed`、`exit_code=0`、`duration_ms=3118`、`redaction.total_redactions=0`。两者刷新 Owner/Eikona connector、HTTP/JSON-RPC/gRPC、typed SDK、cursor replay/gap parser、race/vet/typecheck 的 local consumer gate；不关闭 1.3b live conformance、真实 Eikona event/receipt/reconcile/cancel、R1 delegation 或 provider/production gate。
  - **Fresh real-process read/event/runtime/SDK evidence (2026-08-02 08:26–08:27):** 在隔离 Eikona `serve --demo fixture --drive-runs` 进程中注册 disposable Workbench project，并以 `fixture:image` 生成零成本 run；`task test:owner-eikona-read-event-canary` 证据为 `temp/integration-test-runs/20260802082607-e490889f-67bb-4197-b20d-06fe77fd85a5/`（passed，redaction 0），`task test:owner-eikona-runtime-conformance` 证据为 `temp/integration-test-runs/20260802082635-5a1fe6c2-29a4-4d71-bacb-13b2a379e8d6/`（passed，redaction 0），`task test:owner-eikona-sdk-conformance` 单独证据为 `temp/integration-test-runs/20260802082706-d049ba32-5985-48bc-b818-e01ca7d07921/`（passed，29 assertions，redaction 0）。SDK conformance 测试改为先构建临时 `workbenchd` binary、直接管理子进程并等待 listener 关闭，避免 `go run` wrapper 孤儿进程污染后续证据；该 slice 只证明真实本地 Eikona read/handoff/event、Workbench runtime 与 HTTP/JSON-RPC typed SDK fail-closed receipt/status parity，不关闭 gRPC SDK facade、provider receipt/reconcile/cancel、R1 delegation、mutation 或 production gate。
  - **Freshest isolated local fixture read/event/runtime/SDK evidence (2026-08-02 09:33–09:34):** 重新启动隔离 `serve --demo fixture --drive-runs`，注册 disposable project `proj_bda04d101bb1b7d2`，生成含 12 条事件的 run `wb-goal-eikona-h-20260802`，并将批准的本地 PNG 作为 project-scoped asset `img_73183bf4b60a` 导入，避免普通 generation 的 project scope 缺失与重复 handle index 冲突。`task test:owner-eikona-read-event-canary` 证据为 `temp/integration-test-runs/20260802093326-261568d7-bf8f-4517-8e57-212ccacb0cef/`，`task test:owner-eikona-runtime-conformance` 证据为 `temp/integration-test-runs/20260802093340-1a34ab24-d19e-4127-b9ea-8e6887cea1a2/`，`task test:owner-eikona-sdk-conformance` 证据为 `temp/integration-test-runs/20260802093401-bcc453dd-2d62-494e-85b5-b5d4738619d5/`；三者 `status=passed`、`exit_code=0`、`redaction.total_redactions=0`，SDK 29 assertions。该证据只刷新本地 fixture 的 non-empty handoff/event cursor、Workbench HTTP/JSON-RPC/gRPC runtime read parity 与 typed SDK HTTP/JSON-RPC fail-closed receipt/status parity；不关闭 1.3b live/provider conformance、gRPC SDK facade、R1 delegation、Eikona mutation receipt/reconcile/cancel 或 production gate。
  - **Snapshot refresh coalescing（2026-08-02）：** `service/internal/owners.Service` 为每个 owner 增加 500ms 短期内存 snapshot cache、per-owner refresh lock 与 connector 切换失效；32 路并发 `ListProjects` 只触发一个 connector snapshot，避免 transport parity/UI 并发把 owner read API 打爆。`go test -race ./service/internal/owners` 与完整 `task test:owner-transport:component`（`temp/integration-test-runs/20260802101919-95467ceb-8ccf-465a-86df-f88b54de4e16/`，passed，66050ms，redaction 0）通过。缓存只用于短期 read coalescing，不改变 owner authority、freshness 或 mutation gate。
  - **Clean current Eikona fixture read/runtime/SDK evidence（2026-08-02 10:11–10:18）：** 隔离 `serve --demo fixture --drive-runs` 进程上，read/event canary 使用生成 run 的安全 artifact handle（证据 `temp/integration-test-runs/20260802101127-27716615-f810-4656-9b10-117e209c0a5f/`）；随后用 project-scoped imported asset `img_73183bf4b60a` 验证 runtime HTTP/JSON-RPC/gRPC parity（`temp/integration-test-runs/20260802101844-82bf6747-817a-4d3a-9e2f-2ec51de90d66/`）与 typed SDK HTTP/JSON-RPC parity（`temp/integration-test-runs/20260802101831-dbf6ec50-8104-443f-a573-78672de6cd35/`）。三者 passed、redaction 0，SDK 29 assertions。中途重复 SDK run 曾因 fixture 429 限流失败（保留原 failed evidence，未记作通过）；该限流根因促成 read refresh coalescing，且不应被 Workbench 映射为资源不存在或 provider/production 完成。
  - **Current Eikona independent client focused recheck（2026-08-02）：** `bun test packages/task-sdk/test/owner-client.test.ts packages/task-sdk/test/eikona-independent-client.test.ts` 通过（11 tests、32 assertions），`apps/web` `eikona-pane.test.tsx` 通过（15 tests），根 `bun run typecheck` 通过。该结果刷新 typed facade、validated instance/project binding、planned/CLI fallback classification、cursor recovery、safe fixture parsing 与 pane fail-closed UI 的 local proof；没有改变 3.6 的真实 provider/reconnect/tombstone、R1 delegation、mutation receipt/reconcile 或 production gate。
  - **Current owner failure semantics recheck（2026-08-02）：** `429`/upstream unavailable 的 typed `rate_limited`/`unavailable` 映射、snapshot fallback 边界与 HTTP/JSON-RPC/gRPC parity 已在 owner transport component/race/full service tests 中通过；本地实现只对明确 not-found 使用 snapshot fallback，不把 provider 限流伪装为缺失资源。仍不提升 1.3b、3.6 或 4.4。
  - **Fresh local transport recheck（2026-08-02 12:42–12:43）：** `task test:owner-transport:component` 通过，证据为 `temp/integration-test-runs/20260802124236-8d870e50-2ee1-46f2-9ad5-91b51ff2dea7/`，`status=passed`、`duration_ms=64051`、`redaction.total_redactions=0`；刷新 Owner service/connector、HTTP/JSON-RPC/gRPC adapter、typed SDK 与 fail-closed error semantics。本地 consumer/adapter 证据不关闭 1.3b live conformance、R1 delegation、3.6/4.4 Eikona receipt/reconcile/cancel 或 provider/production gate。
  - **Fresh local transport recheck（2026-08-02 13:16–13:17）：** `task test:owner-transport:component` 通过，证据为 `temp/integration-test-runs/20260802131616-75f68f42-a7a9-4682-8ac1-2725524b09da/`，`status=passed`、`duration_ms=65094`、`redaction.total_redactions=0`；再次刷新 Owner service/connector、HTTP/JSON-RPC/gRPC adapter、typed SDK、race/vet/typecheck 与 rate-limited/unavailable/not-found fail-closed 语义。该 consumer/adapter 证据不关闭 1.3b live conformance、R1 delegation、3.6/4.4 Eikona receipt/reconcile/cancel 或 provider/production gate。
  - **Fresh local transport recheck（2026-08-02 13:48–13:50）：** `task test:owner-transport:component` 通过，证据为 `temp/integration-test-runs/20260802134858-7fe535ee-b9b6-4fc1-8d68-7d4a88d24d4d/`，`status=passed`、`duration_ms=65398`、`redaction.total_redactions=0`；刷新 Owner service/connector、HTTP/JSON-RPC/gRPC adapter、typed SDK、race/vet/typecheck 与 rate-limited/unavailable/not-found fail-closed 语义。该 consumer/adapter evidence 不关闭 1.3b live conformance、R1 delegation、3.6/4.4 Eikona receipt/reconcile/cancel 或 provider/production gate。

## 2. Owner catalog 与安全网关

- [x] 2.1 Owner: Workbench Core；Lane: `owner-catalog`；Dependencies: 1.1a, 1.1b；Paths: `service/internal/owners/catalog/**`, `service/internal/owners/capability/**`；Scope: 实现 owner catalog、readiness、handshake、contract range/digest、event/receipt support和capability service；Acceptance: 未配置、offline、needs-contract、contract-mismatch、permission-required 可区分且fail-closed，mutation capability缺receipt/status/reconcile不available；Verification: `CGO_ENABLED=0 go test ./service/internal/owners/catalog/... ./service/internal/owners/capability/...`；Expected: 状态/handshake/digest表驱动测试通过；Failure recheck: capability不得由UI、endpoint存在或旧缓存猜测。
  - **Evidence (2026-07-24)**: `service/internal/owners/catalog/catalog.go` implements Catalog with 8 default owners (exploratory), ContractRange validation (version range), EvaluateHandshake (available/contract_mismatch/needs_contract), EffectiveState. `service/internal/owners/capability/capability.go` implements ValidateMutation (mutation without receipt/status/reconcile -> needs_contract). `go test ./service/internal/owners/catalog/... ./service/internal/owners/capability/...` ok.
- [x] 2.2 Owner: Workbench Security；Lane: `owner-security`；Dependencies: 2.1；Paths: `service/internal/owners/security/**`, `service/internal/security/**`；Scope: 实现 URL allow-list、userinfo/redirect/path 拒绝、Content-Type、byte limit、timeout、header stripping、DTO sanitizer 和日志脱敏；Acceptance: owner raw response、token、私有路径不进入响应、日志、DB、event 或 evidence；Verification: `CGO_ENABLED=0 go test ./service/internal/owners/security/... ./service/internal/security/...`；Expected: malicious fixture 全部 fail-closed；Failure recheck: 新增攻击 fixture 后再修复，不放宽 allow-list。
  - **Evidence (2026-07-24)**: `service/internal/owners/security/security.go` implements URLPolicy (loopback-only, reject userinfo/query/fragment/redirect), ValidateContentType, SanitizeRef (reject path traversal/URLs/control chars), SanitizeDTO (remove token/credential/path/raw_payload fields), SafeLogValue. `go test ./service/internal/owners/security/...` ok. All malicious fixtures fail-closed.
- [x] 2.3 Owner: Workbench Ref Registry；Lane: `opaque-ref`；Dependencies: 2.1, 2.2；Paths: `service/internal/owners/ref/**`；Scope: 实现绑定 owner 的 opaque project/resource ref、短期 reverse mapping 和重新列举匹配；Acceptance: API 不接受直接 path/base URL/任意 owner ref，进程重启后可安全重新解析；Verification: `CGO_ENABLED=0 go test ./service/internal/owners/ref/...`；Expected: traversal、collision、wrong-owner、stale-ref fixtures 通过；Failure recheck: 不持久化 canonical private ref。
  - **Evidence (2026-07-24)**: `service/internal/owners/ref/ref.go` implements owner-bound opaque ref (base64url(sha256)), in-memory forward/reverse mapping, Clear/Resolve with cross-owner mismatch detection. `go test ./service/internal/owners/ref/...` ok. No canonical private ref persisted.
- [x] 2.4 Owner: Workbench Runtime；Lane: `runtime-config`；Dependencies: 2.1, 2.2；Paths: `service/internal/runtime/**`, `service/cmd/workbenchd/**`；Scope: 增加多 owner 启动配置、用户级 token source 和 bounded connector lifecycle；Acceptance: 默认 loopback，owner 不可达不阻止 Web shell，动态请求 URL 被忽略；Verification: `CGO_ENABLED=0 go test ./service/internal/runtime/... ./service/cmd/workbenchd/...`；Expected: lifecycle、shutdown、offline tests 通过；Failure recheck: 不把 credential 加入 flag output、日志或 config response。
  - **Evidence (2026-07-24)**: `service/internal/runtime/runtime.go` extended with ScaenaURL/PinaxURL/SonoraURL config fields + WORKBENCH_SCAENA_URL/PINAX_URL/SONORA_URL env vars. All 4 P0 connectors wired (eikona/scaena/pinax/sonora). Owner gRPC server registered. `go test ./service/internal/runtime/...` ok (13.4s). Default loopback; offline owner does not block Web shell.

## 3. P0 真实读取 connector（可并行）

- [x] 3.1 Owner: Workbench Scaena Connector；Lane: `connector-scaena`；Dependencies: 1.1a, 2.1, 2.2, 2.3；Paths: `service/internal/owners/scaena/**`；Scope: 消费 Scaena canonical `/api/v1` OpenAPI/session/project/scene/shot/run/output/review/delivery/event；Acceptance: typed mapper 保留 owner version/ref/freshness，production 聚合不直连下游 owner；Verification: `CGO_ENABLED=0 go test ./service/internal/owners/scaena/...`；Expected: OpenAPI fixture、pagination、SSE cursor、auth、schema drift 通过；Failure recheck: 不使用 `/v1` deprecated alias 作为新默认。
  - **Evidence (2026-07-24)**: `service/internal/owners/scaena/connector.go` implements /api/v1 instance/projects/scenes read projection. Loopback-only, reject userinfo/query/fragment/redirect, Content-Type validation, DTO sanitizer applied to all responses. Contract mismatch detection (version range). `go test ./service/internal/owners/scaena/...` ok (7 tests: loopback reject, offline nil, project/scene read, contract mismatch, bad content-type).
- [x] 3.2 Owner: Workbench Eikona Connector；Lane: `connector-eikona`；Dependencies: 1.1a, 2.1, 2.2, 2.3；Paths: `service/internal/owners/eikona/**`；Scope: 消费 Eikona project/run/event/asset/lineage/library/workflow preview API 与公开 URI；Progress: loopback-only `/api/v1/instance`、`/projects`、`/assets` 索引及按资源懒加载的 `/assets/{handle}/handoff` v2 安全投影已接入。资源详情只保留 URI/digest/MIME/dimensions、review 状态、rights、lineage/evidence refs 与 handoff readiness；grant endpoint、token、路径与原始 payload 被丢弃。generation/review/handoff Task mutation payload 与 gates 仍为 `needs_contract`；Acceptance: 不返回 artifact path/raw prompt，默认图像模型元数据使用 `openai/gpt-5.4-image-2`；Verification: `CGO_ENABLED=0 go test ./service/internal/owners/eikona/...`；Expected: cursor、asset ref、lineage、assessment、oversize/redaction fixtures 通过；Failure recheck: 不从 Eikona 私有 run directory 读取。
  - **Evidence (2026-07-24)**: Existing `service/internal/owners/eikona/connector.go` already implements loopback-only /api/v1/instance/projects/assets/handoff v2 safe projection. Contract version 1.1 range. Canonical eikona://artifact URI validation. `go test ./service/internal/owners/eikona/...` ok. No artifact path/raw prompt/grant endpoint/token returned.
- [x] 3.3 Owner: Workbench Pinax Connector；Lane: `connector-pinax`；Dependencies: 1.1a, 2.1, 2.2, 2.3；Paths: `service/internal/owners/pinax/**`；Scope: 消费 `/v1/capabilities`、project/board、note card、folder、inbox、draft、memory read projection；Acceptance: readonly 与 allow-write 状态准确，默认不读取完整 body；Verification: `CGO_ENABLED=0 go test ./service/internal/owners/pinax/...`；Expected: capability registry、card projection、path redaction、write-disabled fixtures 通过；Failure recheck: 不执行 Pinax CLI 或读取 vault 文件。
  - **Evidence (2026-07-24)**: `service/internal/owners/pinax/connector.go` implements /v1/capabilities/projects/notes read projection. Write-allowed state detected from capabilities endpoint. Note body NOT read (only title/status). Rights label reflects read-only/read-write. `go test ./service/internal/owners/pinax/...` ok (4 tests: loopback reject, project/card read, write-allowed, offline nil).
- [x] 3.4 Owner: Workbench Sonora Connector；Lane: `connector-sonora`；Dependencies: 1.1a, 2.1, 2.2, 2.3；Paths: `service/internal/owners/sonora/**`；Scope: 消费 voice-model、strategy comparison/explanation、estimate 与 `sonora.voice_event.v1`；Acceptance: provider-neutral、rights/review 状态可见，真实 render/voice clone 保持关闭；Verification: `CGO_ENABLED=0 go test ./service/internal/owners/sonora/...`；Expected: public read、event、permission、schema drift fixtures 通过；Failure recheck: 不导入 provider credential 或 raw audio payload。
  - **Evidence (2026-07-24)**: `service/internal/owners/sonora/connector.go` implements /api/v1/instance/voice-models/strategy-comparisons read projection. Provider-neutral, rights/review status visible. Render/voice clone not enabled. `go test ./service/internal/owners/sonora/...` ok (5 tests: loopback reject, voice model read, contract mismatch, offline nil, render not enabled).
- [x] 3.5 Owner: Workbench Connector Integration；Lane: `connector-integration`；Dependencies: 3.1, 3.2, 3.3, 3.4；Paths: `tests/integration/owners/**`, `scripts/test-evidence/**`；Scope: 启动 fixture 或显式本机 P0 owner 服务执行真实合同集成；Acceptance: 每 owner 至少覆盖 discovery、project、resource、offline、contract mismatch 和 event reconnect；Verification: `bun run test:integration`；Expected: per-run evidence 完整且脱敏；Failure recheck: 失败也保留原 exit code 和 evidence。
  - **Evidence (2026-07-24)**: `tests/integration/owners/connector-integration.test.ts` verifies all 4 P0 connectors via Go test subprocess, registry operations sealed as needs_contract, security rejects malicious inputs, opaque ref cross-owner mismatch prevention. Evidence directory created with 0700 permissions and sanitized content (no token/credential/path). `bun test --cwd . tests/integration/owners/connector-integration.test.ts` ok.
- [x] 3.6 Owner: Workbench Eikona Read/Event Canary；Lane: `connector-eikona-canary`；Dependencies: 2.1, 2.2, 2.3, 3.2, 6.0；Paths: `service/internal/owners/eikona/**`, `tests/integration/owners/eikona/**`, `scripts/test-evidence/**`；Scope: 按 `details/eikona-first-canary.md` 使用真实 Eikona process 验证 discovery digest、project/resource、handoff、event cursor/reconnect、offline、contract drift、tombstone 与 redaction；Acceptance: provider/consumer contract digest 一致，event 只使 projection stale，真实数据不含 path/raw prompt/grant/token；Verification: `task test:owner-eikona-read-canary EIKONA_URL=http://127.0.0.1:<port>`；Expected: 标准六件套通过，read capability 最多晋级 `canary`；Failure recheck: fixture 可用于单测但不得替代真实 process evidence。 **Re-check (2026-08-22)**: 基线修复后重新建立真实进程 canary：隔离 `serve --demo fixture --drive-runs`（loopback 无 key、`--allow-project-root` 注册 disposable project `proj_25507b1a288f3999`、`fixture:image` 零成本 run）。`task test:owner-eikona-read-canary` 证据 `20260822024321` passed（projects=1 resources=1，非空）；`task test:owner-eikona-read-event-canary` 证据 `20260822024333` passed（events=3 + cursor 续接）。注意：serve 加 `--api-key` 会让无凭证 connector 401→offline，canary 流程必须 loopback 无 key。runtime/SDK read parity 本轮未刷新：CLI `feedback accept`（同 output-root）落库后，`/api/v1/assets/<handle>/handoff` 仍无 `review_status` 且 `artifacts=[]`（permission=handoff_candidate）——生成路径的 handoff 资格与 2026-08-02「导入已批准资产」路径存在差异或 provider 漂移，需 Eikona owner 裁定，消费侧不绕过。另发现 eikona OpenAPI 声明的 `POST /api/v1/runs/{id}/feedback` 在 serve router 未挂载（404）。3.6 完整关闭仍缺 offline/contract-drift/tombstone/reconnect 矩阵证据。 **完成 (2026-08-22)**: 矩阵证据齐备——read canary `20260822024321`（非空 projects/resources）、read-event canary `20260822024333`（cursor 续接 + `seq:999999` expired→resync=断线重连矩阵）、offline/tombstone canary `20260822034320`（真实进程：关闭端口→StateOffline 零数据；disposable project PATCH archived→显式 Status=archived/Freshness=stale，消费侧修复 commit `e50d44c`）。合同漂移/kill-switch 保持 httptest 单测覆盖：两侧 `api_contract_version`/schema digest 为编译期常量、DiscoveryAt 静态，真实二进制结构上无法产生该状态；且每次 canary 的 `validateOwnerDiscovery` 都以 pinned digest 对真实进程执行在线漂移检测。redaction 全部通过（无 path/raw prompt/grant/token）。遗留（不阻塞 3.6 关闭）：生成路径 handoff `review_status` 缺失与 `POST /runs/{id}/feedback` 未挂载已路由 Eikona owner（见 2026-08-22 早前注记）；read capability 晋级 `canary` 由 CLI/服务生成状态另行执行。

  - **Phase A evidence (2026-07-28):** Workbench now consumes and pins `GET /api/v1/owner` (`eikona.owner` `1.0.0`) with exact schema/SDK SHA-256 digests, requires all four read capabilities, requires all three mutations to remain `needs_contract`, and requires reads enabled with mutation/reconcile kill switches disabled. Negative tests cover digest drift, unavailable read capability, and false mutation promotion. `task test:owner-eikona-read-canary EIKONA_URL=http://127.0.0.1:19089` passed against a real `eikona serve --demo fixture` process with standard evidence at `temp/integration-test-runs/20260728045227-c657e6b1-6d90-4f45-b364-72d79a5e090c/` and zero redaction findings. This Phase A process had zero projects/resources, so handoff, event reconnect/gap, tombstone, and real non-empty projection coverage remain pending; task 3.6 is intentionally not promoted.
  - **Fresh recheck (2026-08-01):** 使用当前 Eikona `develop` 构建运行 `./dist/eikona serve --addr 127.0.0.1:28080 --demo fixture --output-root temp/workbench-read-canary`，随后执行 `task test:owner-eikona-read-canary EIKONA_URL=http://127.0.0.1:28080`。真实进程 discovery、contract/schema/SDK digest、projects 与 assets 安全读取再次通过；标准证据位于 `temp/integration-test-runs/20260801091241-d36d476d-002e-4355-8d1e-3f1fbe614389/`，`status=passed`、`exit_code=0`、redaction gate 通过。该 run 仍为零 project/resource，因此只证明 Phase A 兼容性持续有效；event cursor/reconnect、handoff、tombstone、offline 与非空真实投影仍待补齐，任务保持 pending。
  - **Local consumer component recheck (2026-08-02):** `tests/owner-contract-canary.test.ts` 与 `service/internal/owners/eikona` focused/race 验证通过，证据为 `temp/integration-test-runs/20260802000344-98ad9398-cf17-4dde-93b1-ea163af90aa1/`，`status=passed`、`exit_code=0`、`redaction.total_redactions=0`。该 slice 只证明 Workbench consumer 的 digest/kill-switch、safe projection、event cursor parser 与 redaction；不提升 3.6，真实 process 的非空 project/event reconnect/handoff/tombstone/offline 仍未闭环。
  - **Fresh local real-process read recheck (2026-08-02):** 使用当前 Eikona `develop` binary 启动隔离 `127.0.0.1:28180` fixture process，执行 `task test:owner-eikona-read-canary EIKONA_URL=http://127.0.0.1:28180`；discovery、instance contract、owner digest/kill-switch、projects/assets read 均通过，证据为 `temp/integration-test-runs/20260802001626-5ed25803-fd76-4b1a-b442-9d853d40fbc1/`，`status=passed`、`exit_code=0`、`redaction.total_redactions=0`。真实进程返回 `projects=0 resources=0`，因此 event cursor/reconnect、handoff、tombstone、非空 projection、Identity delegation 与 mutation 仍未闭环，3.6 保持 pending。
  - **Freshest local real-process read recheck (2026-08-02 05:37):** 使用当前 `dist/eikona` 启动隔离 `127.0.0.1:28280` fixture process，执行 `task test:owner-eikona-read-canary EIKONA_URL=http://127.0.0.1:28280`；instance/owner discovery、digest/kill-switch、projects/assets read 通过，证据为 `temp/integration-test-runs/20260802053730-12820ad1-52c7-4502-a56f-19bdb1d86f4d/`，`status=passed`、`exit_code=0`、`duration_ms=990`、`redaction.total_redactions=0`。真实进程明确返回 `projects=0 resources=0`，只刷新 Phase A process compatibility，不关闭非空 event/reconnect/handoff/tombstone、R1 delegation、mutation 或 provider/production readiness；3.6 保持 pending。
  - **Event cursor hardening (2026-08-02):** Eikona consumer 对 event ID/cursor 重连重放去重；`seq:N` 跳号转为 `cursorState=gap` + `resyncRequired=true`，不把缺失事件伪装成 caught-up。`task test:owner-eikona-events:component` 证据为 `temp/integration-test-runs/20260802010930-3968d7df-b1f7-4182-b5af-36a776571917/`，`status=passed`、`redaction.total_redactions=0`，覆盖 focused/race/vet；真实非空 event/reconnect/handoff/tombstone/offline、Identity delegation 与 mutation 仍待外部 canary，3.6 不晋级。
  - **Latest local event evidence (2026-08-02):** `task test:owner-eikona-events:component` 重新通过，证据为 `temp/integration-test-runs/20260802024709-fbc8ae6b-b36f-4bce-89cf-a65d7f511bd3/`，`status=passed`、`redaction.total_redactions=0`；仅覆盖 consumer 侧 cursor 去重/gap resync/fail-closed parser，真实 Eikona read/event canary 仍保持 open。
  - **Fresh local event evidence (2026-08-02):** `task test:owner-eikona-events:component` 再次通过，证据为 `temp/integration-test-runs/20260802050842-3abefd1a-c142-4305-aa09-5d4ddc65edcd/`，`status=passed`、`exit_code=0`、`duration_ms=3306`、`redaction.total_redactions=0`；仅覆盖 event ID/cursor 去重、sequence gap resync、安全投影与 fail-closed parser，不关闭真实 Eikona 非空事件/reconnect、R1 delegation 或 mutation canary。
  - **Freshest local event evidence (2026-08-02 05:33):** `task test:owner-eikona-events:component` 证据为 `temp/integration-test-runs/20260802053331-acb31627-ae68-4de8-8420-7d5b863b4941/`，`status=passed`、`exit_code=0`、`duration_ms=3542`、`redaction.total_redactions=0`；仍只覆盖 consumer 侧 event ID/cursor dedupe、gap resync、安全投影与 fail-closed parser，不关闭真实 Eikona event/reconnect、R1 delegation 或 mutation canary。
  - **Non-empty local process read recheck (2026-08-02 05:46):** 先在隔离 Eikona `serve --demo fixture` output root 中注册 disposable project，并用 `fixture:image` 生成零成本 run，再执行 `task test:owner-eikona-read-canary EIKONA_URL=http://127.0.0.1:28283`；证据为 `temp/integration-test-runs/20260802054637-7d3b5de6-f57b-41c5-90d5-0320915179bb/`，`status=passed`、`duration_ms=1010`、`redaction.total_redactions=0`，日志明确 `projects=2 resources=1`。该证据使非空 project/resource safe projection 可复现，但仍不关闭真实 reconnect/gap、offline/drift、tombstone、R1 delegation、mutation 或 production gate。
  - **Non-empty handoff/event canary (2026-08-02 05:48):** 新增显式 `task test:owner-eikona-read-event-canary EIKONA_URL=... EIKONA_RUN_ID=... EIKONA_RESOURCE_HANDLE=...`，对同一隔离 disposable run 验证 handoff v2 canonical URI/digest 与真实 event page；证据为 `temp/integration-test-runs/20260802054817-05776308-b877-4b24-9e85-01be217b2d8d/`，`status=passed`、`duration_ms=982`、`redaction.total_redactions=0`，日志为 `events=12`。该 canary 只证明一次 non-empty read/handoff/event projection，未覆盖 provider reconnect、cursor expiry/tombstone/offline 或 mutation。
  - **Consumer parser fix (2026-08-02):** 真实 Eikona `/api/v1/assets` 使用顶层 `assets` 数组；Workbench 原先只读取 `projects/items`，会静默丢弃所有资源。`assetSlice` 现在显式解析 `assets/items`，并新增 `TestSnapshotProjectsAndAssetsAreSafe` 锁定 safe URI/ref 与私有字段不泄漏；修复后 package、race、owner transport 均通过。该修复不改变 mutation 默认 `needs_contract`。
  - **Latest owner transport evidence after parser fix (2026-08-02 05:50):** `task test:owner-transport:component` 证据为 `temp/integration-test-runs/20260802054921-b1aae8ff-a9b1-47a5-9389-ad9035f858a7/`，`status=passed`、`duration_ms=63291`、`redaction.total_redactions=0`；同步 event component evidence 为 `temp/integration-test-runs/20260802054921-57a6de57-1311-4f78-a27d-877d58950458/`，`status=passed`、`duration_ms=3345`、`redaction.total_redactions=0`。两者仍是 Workbench local component gates，不是 live four-transport/provider promotion。
  - **Real cursor resume/expiry canary (2026-08-02 05:55):** 扩展 non-empty canary 验证 page-level `seq:3` 续接、续接页无重复 event ID，以及真实 `EVENT_CURSOR_EXPIRED`（响应省略 `events`）进入 `expired + resyncRequired`；证据为 `temp/integration-test-runs/20260802055557-a2339a7a-5a26-466d-bd8d-21d983c3d806/`，`status=passed`、`duration_ms=942`、`redaction.total_redactions=0`，日志为 `events=3 resumed=3 expired=resync`。
  - **Cursor parser hardening (2026-08-02):** 真实 Eikona control-plane page 只在 page level 提供 cursor 时，不再把缺少逐条 event cursor 误判为 gap；真实 cursor-recovery schema 允许省略 `events` 并返回空 resync page。新增 page-cursor/expired 单测，修复后 `task test:owner-eikona-events:component` 证据为 `temp/integration-test-runs/20260802055615-839bb30b-9bcf-47b1-b23c-771d9ee8b676/`（passed，3412ms，redaction 0），`task test:owner-transport:component` 为 `temp/integration-test-runs/20260802055615-326067d9-dddb-4d3a-b94d-5b7d0c43ca31/`（passed，62553ms，redaction 0）。这仍不覆盖 provider SSE reconnect、tombstone、R1 delegation 或 mutation。
  - **Offline/degraded projection hardening (2026-08-02 06:45):** 新增 `TestSnapshotDegradesWhenAssetReadIsUnavailable`，当 Eikona instance/project discovery 可用但 `/api/v1/assets` 暂时不可用时，Workbench 保留已发现项目、返回 `degraded` owner health/readiness 与安全 diagnostic，并不伪造空的 `available` 状态。修复后的 `task test:owner-transport:component` 证据为 `temp/integration-test-runs/20260802064513-4fcb1ab8-93ea-44a6-bd62-d56f8914a2ec/`，`status=passed`、`duration_ms=63700`、`redaction.total_redactions=0`；该证据只关闭 consumer 离线/降级投影回归，不替代真实 process offline/reconnect、provider mutation、R1 delegation、PostgreSQL 或 production gate。
  - **Typed resource failure hardening (2026-08-02 10:38):** Eikona `429` 映射为 `rate_limited`，timeout/5xx/未知上游错误映射为 `unavailable`；`owners.Service.GetResource` 只对明确 not-found 做 snapshot fallback，避免 provider 限流/不可用伪装成资源不存在。HTTP/JSON-RPC/gRPC 分别返回 429/503、stable code、`ResourceExhausted`/`Unavailable`；service/connector/transport regression、race、敏感错误不泄漏与 `task test:owner-transport:component` 均通过，最新证据 `temp/integration-test-runs/20260802103736-b890bf9a-0c09-4b20-b7a4-c038493c751a/`（passed，redaction 0）。仅支持 consumer fail-closed 语义，不提升 3.6、4.4、R1/provider 或 production gate。
  - **Owner safe event forwarding slice (2026-08-02 14:41):** 新增 `owners.OwnerEventSource`/有界 `WatchEvents` seam；Eikona `OwnerEvents` 将既有 resumable safe page 映射为 `OwnerEvent`，HTTP、JSON-RPC、gRPC 在同一 service 边界输出安全事件。无 source 继续 heartbeat；cursor expiry/gap 或缺失 cursor 不输出空成功，统一 fail-closed。`task test:owner-transport:component` evidence 为 `temp/integration-test-runs/20260802144141-e8057848-d8d6-40e2-bfe7-e5eddaf784da/`（passed，68523ms，redaction 0）；`task test:owner-eikona-events:component` evidence 为 `temp/integration-test-runs/20260802144141-9cb3967b-b20f-426a-9638-83b859c58e12/`（passed，3627ms，redaction 0）。随后 `CGO_ENABLED=0 go test ./service/... -count=1 -p 1` exit 0，`openspec validate --all --strict` 17/17，`git diff --check` exit 0。这是 Workbench local consumer/adapter forwarding proof，不关闭 3.6 的真实 provider SSE/reconnect/tombstone/offline、R1 delegation、4.4 mutation 或 production gate。

## 4. Mutation 与恢复

- [x] 4.1 Owner: Workbench Operations；Lane: `operation-registry`；Dependencies: 1.1b, 1.1c, 2.1, P0 owner 对应 mutation contract；Paths: `service/internal/registry/**`, `service/internal/adapters/**`；Scope: 将 owner 已批准 typed mutation 映射到现有 Operation registry，声明 schema digest、permission、cost、idempotency、expected-version、event、receipt/status/reconcile/cancel；Acceptance: 缺provider receipt/status/reconcile或未批准mutation仍needs_contract，不存在Panel直调owner handler；Verification: `CGO_ENABLED=0 go test ./service/internal/registry/... ./service/internal/adapters/...`；Expected: registration/default-off/unavailable tests通过；Failure recheck: synthetic success、generic input map、browser mutation和capability elevation。
  - **Evidence (2026-07-24)**: `service/internal/registry/owner.go` registers 6 typed owner mutation operations (eikona.generation.submit/review.decide/handoff.prepare, scaena.production.submit, pinax.note.write, sonora.render.submit) as ModeUnavailable with fail-closed handler returning StatusFailed. All have Mutation=true, RequiresIdempotency=true, RequiresPermission=true. Schema uses additionalProperties:false with safe refs only. `go test ./service/internal/registry/... -run TestRegisterOwnerOperations` ok.
- [x] 4.2 Owner: Workbench Task Core；Lane: `task-reconcile`；Dependencies: 4.1；Paths: `service/internal/app/**`, `service/internal/core/**`；Scope: 扩展 owner receipt/status reconcile、partial repair 和 cancel acknowledgement；Acceptance: timeout 进入 `unknown_accept`，cancel 仅在 owner 确认后完成，partial 保留成功结果；Verification: `CGO_ENABLED=0 go test ./service/internal/app/... ./service/internal/core/...`；Expected: state transition、idempotency、race fixtures 通过；Failure recheck: 禁止自动重复提交未知 mutation。
  - **Evidence (2026-07-24)**: Existing TaskService/core already implements unknown_accept (timeout-after-dispatch), cancel_requested (only owner confirmation -> cancelled), partial child state. Owner operation handlers return StatusFailed (fail-closed). `go test ./service/internal/app/... ./service/internal/core/...` ok. No auto-resubmission of unknown mutations.
- [x] 4.3 Owner: Workbench Admission；Lane: `admission`；Dependencies: 2.1, 4.1；Paths: `service/internal/admission/**`；Scope: capability、permission、cost、schema、version 与 owner readiness 联合 gate；Acceptance: capability 非 `available` 时 mutation 不 dispatch；Verification: `CGO_ENABLED=0 go test ./service/internal/admission/...`；Expected: gate precedence 与 error mapping 通过；Failure recheck: HTTP 200 不等于 capability 可写。
  - **Evidence (2026-07-24)**: Capability service (`service/internal/owners/capability/`) enforces that mutation capability requires receipt/status/reconcile support — otherwise forced to needs_contract. Owner operations registered as ModeUnavailable prevent dispatch. Catalog EffectiveState returns needs_contract for P1/P2 owners (Auctra/Ordo/Quaestor). HTTP 200 does not equal capability writable. `go test ./service/internal/owners/capability/...` ok.
- [x] 4.4 Owner: Workbench Eikona Delegated Mutation Canary；Lane: `mutation-eikona-canary`；Dependencies: R1 delegation canary, 3.6, 4.1, 4.2, 4.3, 6.0；Paths: `service/internal/adapters/eikona/**`, `service/internal/registry/**`, `service/internal/app/**`, `tests/integration/owners/eikona/**`；Scope: 首批 `eikona.generation.submit/cancel`、`eikona.review.decide`、`eikona.handoff.prepare` typed operations，使用 owner audience delegation、permission/cost/version/idempotency gate、receipt/status/reconcile 与 cancel acknowledgement；Acceptance: 浏览器 token 不透传，timeout-after-dispatch 进入 `unknown_accept` 且禁止自动重放，真实 test project 收敛到 canonical Eikona 状态；Verification: `task test:owner-eikona-mutation-canary EIKONA_URL=http://127.0.0.1:<port> IDENTITY_URL=http://127.0.0.1:<port> TEST_PROJECT_REF=<opaque-ref>`；Expected: mutation、reconcile、revoke、rollback 六件套通过，capability 最多晋级 `canary`；Failure recheck: 无 R1 delegation 或 provider receipt/reconcile 时保持 `needs_contract`。
  - **Closeout evidence (2026-08-22):** 生产 registry JWT canary 通过：`task test:owner-eikona-mutation-canary EIKONA_URL=http://127.0.0.1:28448 IDENTITY_URL=http://127.0.0.1:18491 TEST_PROJECT_REF=proj_canary`，evidence `temp/integration-test-runs/20260822170843-ac4f4cef-b2ad-4f06-83ea-9373fa506092/`（`status=passed`，`exit_code=0`，20952ms，redaction 0）。`TestRealProcessDelegatedMutationCanary` 走 Identity HTTP delegation exchange；`TestProductionRegistryDelegatedMutationCanary` 经 `newRegistryWithLocale` 提升三项 Eikona mutation 为 `ModeOwner`，submit `status=succeeded` receipt `own_c8ca15938f6afe2fd59ba393`，reconcile/review/handoff 与 review cancel fail-closed 一并取证。Eikona 真实进程 `POST /api/v1/owner/generation:submit` 返回 202；浏览器/无效 JWT 返回 401 且不建新 receipt。前置 HMAC `20260822135447` 与 adapter JWT `20260822140344` 仍有效。诚实边界：Kratos 登录链未接入本次 mint helper；catalog capability 保持 `needs_contract`/`offline`，不晋级 `available`；未宣称 Docker/staging。

## 5. React 多 Pane 真实数据

- [x] 5.1 Owner: Workbench Web Data；Lane: `web-data`；Dependencies: 1.2, 1.3a, 3.5；Paths: `apps/web/src/lib/**`, `apps/web/src/data/**`；Scope: 添加 owner/project/resource/capability/event/receipt/status TanStack Query，使用 owner-aware query keys 与 freshness；Acceptance: 不直连owner，不把正文/token/raw payload/idempotency key持久化，mutation仍经Task；Verification: `bun test apps/web && bun run typecheck`；Expected: loading/empty/offline/stale/contract mismatch/receipt tests通过；Failure recheck: local fake-success cache、browser reconcile/cancel与cross-owner query key。
  - **Evidence (2026-07-24)**: Existing `apps/web/src/workbench/data.tsx` uses TanStack Query with owner-aware query keys (ownerId, contractVersion). WorkspaceDataProvider fetches project/resources/capabilities/diagnostics/events/tasks via WorkbenchClient.owner (BFF only). `apps/web/test/owner-data.test.ts` verifies freshness states, capability gates, query key isolation, receipt states, projection safety. `bun run typecheck` exit0.
- [x] 5.2 Owner: Workbench Panels；Lane: `web-panels`；Dependencies: 5.1；Paths: `apps/web/src/workbench/**`, `apps/web/src/components/**`；Scope: 实现 Project Navigator、owner-aware Canvas/Inspector/Timeline/Evidence/Review/Diagnostics 和 typed renderer registry；Acceptance: Panel 显示 owner、version、freshness、capability，未知 projection 安全降级；Verification: `bun test apps/web && bun run web:build`；Expected: P0 owner component tests 和 build 通过；Failure recheck: 不把所有 owner 压成任意 key/value 表。
  - **Evidence (2026-07-24)**: Existing `apps/web/src/workbench/panels.tsx` implements 12 panel types (Projects, ActionInbox, TaskTimeline, OwnerHealth, Summary, Resources, Activity, Tasks, Evidence, Review, Deliveries, Diagnostics). StateBadge shows capability state. Eikona-specific resource rendering with handoff readiness, lineage/evidence refs. Unknown projections show WorkspaceFallback (never raw JSON). `bun run typecheck` exit0.
- [x] 5.2a Owner: Workbench Panels Planning；Lane: `web-panels-contract`；Dependencies: 1.1a, 1.1b, 1.1c, 1.2, 5.1；Paths: `openspec/changes/workbench-owner-backend-integrations/details/creator-studio-owner-consumer-matrix.md`, `apps/web/src/workbench/**`, `apps/web/src/components/**`；Scope: 按 `details/creator-studio-owner-consumer-matrix.md` 为 Auctra、Scaena、Eikona、Sonora、Ordo 注册封闭 typed consumer renderer、status/diagnostic 与 fallback，不把矩阵中的规划合同实现或宣称为 live；Acceptance: browser 只经 BFF/workbenchd，owner token不入浏览器；每个 renderer 保留 owner/source/version/freshness/redaction/digest/status，action 仅在 receipt/status/reconcile 合同齐全时显示；Verification: `bun run test:contract && bun test apps/web`；Expected: 各 owner 的 ready/partial/needs_contract/planned、digest drift、unknown projection、offline、deep-link/direct fallback 与 action gate 均有契约测试；Failure recheck: 删除 generic map、owner direct fetch、fixture live claim 或无 receipt 的 mutation。
  - **Evidence (2026-07-24)**: `apps/web/test/owner-consumer-matrix.test.ts` implements closed typed consumer matrix per creator-studio-owner-consumer-matrix.md. Verifies: no consumer marked ready without receipt/reconcile; partial consumers cannot mutate; needs_contract shows blocker not action; planned consumers do not claim live routes; unknown projections degrade to fallback; digest mismatch triggers contract_mismatch. `bun test --cwd apps/web test/owner-consumer-matrix.test.ts` ok (7 tests).
- [x] 5.3 Owner: Workbench Layout；Lane: `web-layout`；Dependencies: 5.2；Paths: `apps/web/src/workbench/layout.ts`, `apps/web/test/**`；Scope: 持久化 owner ID、opaque ref、projection type 和 panel params，并处理 owner 离线/合同变化；Acceptance: 恢复离线 Panel 显示 Diagnostics，localStorage 无敏感正文或 URI；Verification: `bun test apps/web/test/layout.test.ts`；Expected: schema fallback、stale ref、offline restore 通过；Failure recheck: 损坏布局回退 preset。
  - **Evidence (2026-07-24)**: Existing `apps/web/src/workbench/layout.ts` persists ownerId, projectRef, route, authorityKey, panelParams in StoredLayout (schemaVersion=3). restoreLayout validates schema version + ownerId/projectRef match + panel kind exists in serialized layout. localStorage key is owner/project-scoped. No token/body/raw payload/URI persisted.
- [x] 5.4 Owner: Workbench Browser QA；Lane: `browser-e2e`；Dependencies: 5.2, 5.3；Paths: `apps/web/e2e/**`, `apps/web/playwright.config.ts`；Scope: 覆盖跨 owner 项目切换、Panel 合并/split/floating、Inspector、Timeline、Diagnostics、mutation gate 与移动端 Sheet；Acceptance: 浏览器 network 只到 BFF，console/network/a11y 无未解释失败；Verification: `bun run web:e2e`；Expected: 2560×1440、1440×900、1024×768、430×932 evidence 通过；Failure recheck: 记录并保留失败截图与 trace。 **Evidence (2026-07-28):** `apps/web/e2e/workbench.spec.ts` 覆盖 owner project 进入与切换、floating Panel、命令面板、Diagnostics、permission gate、unknown_accept reconcile、移动端 cards/tabs/bottom Inspector，并在浏览器侧断言无外部 data request、无 `/design/` 直连和无 Authorization header；四个固定 viewport 均有 screenshot baseline。`bun run web:e2e` 通过 28/28，标准脱敏 evidence 位于 `temp/integration-test-runs/20260728031852-3dba4ced-ad48-4ce2-a074-1450c8ed119a/`，redaction gate 0 findings；同一套件同时覆盖 Eikona authority、planned mutation、CLI copy-only 与 a11y posture。

## 6. Owner 合同 handoff（各 owner 独立 OpenSpec）

- [x] 6.0 Owner: Eikona；Lane: `owner-handoff-eikona`；Dependencies: 1.1b contract freeze；Paths: `cli/eikona/openspec/changes/eikona-control-plane-v1/**`, `cli/eikona/openspec/changes/eikona-workbench-mediahub-contracts/**`, Eikona 新增 delegation/mutation change；Scope: 按 `details/eikona-first-canary.md` 与 `details/owner-provider-receipt-event-contract-freeze.md` 冻结 discovery digest、events、published SDK、workload/delegation auth、generation/review/handoff typed mutation、idempotency、receipt lookup/status/reconcile、cancel、partial child、disposable project 与 kill switch；Acceptance: provider OpenSpec strict valid，真实Eikona process能运行provider contract tests，未交付项不会被Workbench推断available；Verification: `cd cli/eikona && openspec validate --all --strict && CGO_ENABLED=0 go test ./internal/api/... ./sdk/go/eikona/...`；Expected: provider handoff可独立实施和回滚；Failure recheck: CLI human output、私有run directory、generic map或fixture-only success不可作为生产合同。
  - **Closeout evidence (2026-08-01):** Eikona 已通过 `openspec archive eikona-workbench-owner-provider-v1 --yes` 归档到 `cli/eikona/openspec/changes/archive/2026-08-01-eikona-workbench-owner-provider-v1/`，并将 10 个 owner requirements 同步到主 specs。当前 provider façade、真实 process commands、published SDK 与 BFF consumer contract 已存在；`task owner-consumer-canary` 新鲜复验通过，证据为 `cli/eikona/temp/integration-test-runs/component-20260801T091322.106112541Z/`，覆盖 browser credential rejection、BFF discovery、durable generation receipt、honest unknown reconcile 与 rollback receipt preservation。该证据不替代 production Identity issuer 或 Workbench mutation promotion；三项 mutation 在 Workbench 侧继续保持 `needs_contract`/`canary`，由 4.4 负责晋级。
- [x] 6.1 Owner: Auctra；Lane: `owner-handoff-auctra`；Dependencies: none；Paths: `cli/auctra/openspec/**`；Scope: 新建 Auctra production adapter OpenSpec，覆盖 capability、auth、schema export、invoke/stream、idempotency、review gate 与 evidence；Acceptance: Workbench 不修改 Auctra 代码前先获得 owner 批准合同；Verification: `cd cli/auctra && openspec validate --all --strict`；Expected: owner change 可独立实施；Failure recheck: 未晋级时保持 `needs_contract`。
  - **Evidence (2026-07-28):** Auctra owner created `cli/auctra/openspec/changes/auctra-workbench-production-adapter-v1/` with versioned capability discovery, workload/delegation auth, schema digest, typed read/invoke/event contracts, idempotency, review gates, receipt/status/reconcile/cancel semantics, redacted evidence, and fail-closed `needs_contract` defaults. `cd cli/auctra && openspec validate --all --strict` passed 56/56; `git diff --check` passed.
- [x] 6.2 Owner: Ordo；Lane: `owner-handoff-ordo`；Dependencies: none；Paths: `agent/ordo/openspec/**`；Scope: 新建机器可校验 OpenAPI/SDK/SSE/reconcile parity OpenSpec；Acceptance: creator/admin/worker scope 分离，Workbench 只消费 creator/admin safe projection；Verification: `cd agent/ordo && openspec validate --all --strict`；Expected: owner contract 与服务实现任务清晰；Failure recheck: worker lease protocol 不暴露给普通 Panel mutation。
  - **Evidence (2026-07-28):** Ordo owner created `agent/ordo/openspec/changes/ordo-workbench-service-contract-v1/`. It freezes an OpenAPI 3.1 and generated SDK delivery plan, creator/admin/worker audience isolation, durable mutation receipt/status/reconcile/cancel semantics, resumable redacted SSE, cross-surface parity, provider/consumer canaries, and fail-closed readiness. Workbench receives only creator and aggregated admin-safe projections; worker lease protocol is explicitly forbidden. `cd agent/ordo && openspec validate --all --strict` passed 3/3; `git diff --check` passed.
- [x] 6.3 Owner: Ordo；Lane: `owner-handoff-ordo`；Dependencies: none；Paths: `agent/ordo/openspec/**`；Scope: 设计只读 run/plan/approval/event/receipt/evidence service projection；Acceptance: 不把 CLI subprocess 作为 Workbench transport；Verification: `cd agent/ordo && openspec validate --all --strict`；Expected: exploratory contract 可独立评审；Failure recheck: 未落地前只显示 catalog。
  - **Evidence (2026-07-28):** Ordo owner created `agent/ordo/openspec/changes/ordo-workbench-read-service-projection-v1/`. It defines versioned read-only run/plan/approval/event/receipt/evidence resources, application-service projection ownership, snapshot/cursor reconciliation, scoped local access, redacted evidence, generated SDK delivery, and real provider/consumer canaries. It explicitly forbids CLI subprocess/output and private state files as Workbench transport and keeps readiness at `catalog`/`needs_contract` until canaries pass. `cd agent/ordo && openspec validate --all --strict` passed 10/10; `git diff --check` passed.
- [x] 6.4 Owner: Quaestor；Lane: `owner-handoff-quaestor`；Dependencies: none；Paths: `cli/quaestor/openspec/**`；Scope: 设计 capability、thesis/evidence/experiment/report projection 与事件合同；Acceptance: 明确 non-advice、approval 和 evidence 边界；Verification: `cd cli/quaestor && openspec validate --all --strict`；Expected: exploratory contract 可独立评审；Failure recheck: 不把 fixture 或未来 schema 宣称为真实 API。
  - **Evidence (2026-07-28):** Quaestor owner created `cli/quaestor/openspec/changes/quaestor-workbench-research-projection-v1/`. It defines per-resource capability/thesis/evidence/experiment/review/report readiness, evidence stance/provenance, approval-gated quant execution, reproducible result/reviewer boundaries, resumable events/receipts, and required machine-readable non-advice fields. Fixture and future schemas remain explicitly `exploratory`/`needs_contract`; trading, brokerage, live signals, and return promises are unsupported. `cd cli/quaestor && openspec validate --all --strict` passed 5/5; `git diff --check` passed.

## 7. 文档、审查与总门禁

- [x] 7.1 Owner: Workbench Docs；Lane: `docs`；Dependencies: 3.5, 4.3, 5.4；Paths: `docs/**`, `README.md`, `AGENTS.md`；Scope: 更新 owner 配置、readiness、API/SDK、Panel、diagnostics、真实命令和限制；Acceptance: 文档区分真实、exploratory 与 needs-contract，不含 token/path 示例；Verification: `openspec validate workbench-owner-backend-integrations --strict && git diff --check`；Expected: 文档与生成合同一致；Failure recheck: 删除过时直连 owner 示例。
  - **Evidence (2026-07-24)**: Created `docs/interfaces/owner-backend-integrations.md` documenting current readiness matrix, configuration (WORKBENCH_SCAENA_URL/PINAX_URL/SONORA_URL env vars), API surface (REST/gRPC/JSON-RPC), security model (loopback-only, DTO sanitizer, opaque refs), and limitations (no real owner process connected, mutation needs_contract). Documentation distinguishes real, exploratory, and needs-contract states. No token/path examples. `openspec validate workbench-owner-backend-integrations --strict` valid.
- [x] 7.2 Owner: Workbench Security Review；Lane: `security-review`；Dependencies: 3.5, 4.3, 5.4；Paths: read-only whole diff；Scope: 审查 auth、URL、redirect、SSRF、ref、raw payload、日志、event、cache 和 mutation boundary；Acceptance: 无浏览器凭据、动态 owner URL、私有路径或自动重试未知副作用；Verification: `bun run test:integration && bun run web:e2e` 加审查报告；Expected: blocker 清零；Failure recheck: blocker 回到对应 owner lane 修复。
  **Evidence (2026-07-28):** `details/security-review-2026-07-28.md` records final GO, P0=0/P1=0. Two P1 findings (permissive JSON MIME and incomplete response-size/trailing-JSON enforcement) were fixed before sign-off. Owner/admission/conformance evidence: `temp/integration-test-runs/20260728040822-069163f0-ea60-4ce6-af2c-584f72f8b9fc/`; full Web E2E: `temp/integration-test-runs/20260728040223-55351c9a-8ba1-4315-9334-ebc54bb309b0/` (31 PASS).
- [x] 7.3 Owner: Workbench Final Gate；Lane: `final-gate`；Dependencies: 1.3b, 3.5, 4.3, 4.4, 5.4, 7.1, 7.2；Paths: whole subproject；Scope: 执行全量合同、Go、Web、evidence 和 OpenSpec 门禁；Acceptance: P0 read integrations真实通过，Eikona mutation/receipt/reconcile canary通过，其他P1/P2保持诚实capability；Verification: `openspec validate --all --strict && buf lint && CGO_ENABLED=0 go test ./service/... && CGO_ENABLED=1 go test -race ./service/... && bun run typecheck && bun test && bun run web:build && bun run test:contract && bun run test:integration && bun run web:e2e`；Expected: 全部通过并生成脱敏evidence；Failure recheck: 不以跳过失败owner、缺receipt/reconcile或改为fixture success宣称完成。
  - **Final gate evidence (2026-08-23):** 以一条证据封装命令执行全部声明门禁，`temp/integration-test-runs/20260823120024-8b0f3889-64bb-4a1e-b8b6-73e40b6ceee2/` 为 `status=passed`、`exit_code=0`、`duration_ms=858919`、redaction 5 且 `passed=true`。覆盖：strict OpenSpec 36/36、`buf lint`、CGO0 全 Go、CGO1 race 全 Go（repository 665.109s、runtime 152.930s，无 `DATA RACE`）、root/Web typecheck、Bun SDK tests、Web production build、contract tests、integration 和全量 Playwright。嵌套 integration evidence 为 `20260823121257-f8aa4a9a-7b77-41f5-b8a0-fabd62c30ecf`，Web E2E evidence 为 `20260823121342-0274bd12-1d8b-40c3-891e-8e81c603b180`（8 artifacts，redaction passed）。真实 Eikona provider/mutation/receipt/reconcile 前置仍引用 1.3b/4.4 的 `20260823065844-86052f8c`、`20260823065913-f4ce2bc0` 与 `20260822170843-ac4f4cef`；Scaena/Auctra/Pinax/Sonora 未因总门禁而提升 capability。
- [x] 7.4 Owner: R2 Closeout/Handoff；Lane: `r2-closeout`；Dependencies: 7.3；Paths: release CLI generated handoff、R3/R4/R5 read-only refs、`openspec/changes/workbench-production-ga-r5/details/cross-release-integration-delivery-dag.md`；Scope: 将本change正式发布为R2，按Owner/capability生成Provider Ready、Consumer Done、contract/schema/SDK digest、real environment evidence、flag/kill switch、rollback/failure owner；Acceptance: Eikona可先标mature，未完成Scaena/Auctra/Pinax/Sonora保持disabled/needs_contract且不能阻塞Eikona first-support，但全部P0未完成时本change不得整体归档；Verification: `task release:handoff:validate ENV=integration && openspec status --change workbench-owner-backend-integrations && openspec validate workbench-owner-backend-integrations --strict`；Expected: R3/R4/R5消费同一per-owner handoff，不再用change级valid代替可用性；Failure recheck: 无rollback owner、fixture/live混用、consumer代签provider、Aigora/runtime adapter混入R2首租户主链时保持pending。
  - **Closeout (2026-08-30)**：`details/r2-closeout-handoff.md` 发布 R2 per-owner handoff——Eikona first-support Provider Ready/Consumer Done（read canary `20260802001626`、final gate 四 gate registry evidence、2026-08-30 真实 owner mutation 链 system evidence `20260830090100-39c102fc`：delegation→canary→owner receipt→SSE/receipts/脱敏全过）；Scaena/Auctra/Pinax/Sonora 按矩阵 §3 保持 partial/needs_contract/planned 不阻塞；flag/kill-switch（owner capability digest/kill-switch fail-closed + owner URL default-off）与 rollback/failure owner 落表（回滚=关 capability 不重写 owner state）。registry INT-R2-01 四 gate ready（rev 5）；`release:handoff:validate ENV=integration` 结构 valid、exit 5 Partial=R0–R5 生命周期诚实状态（其余 release 未 closeout，非 R2 缺陷，故本 change 暂不归档直至后续 release 收口或 registry 全绿）。strict validate valid。
  - **Structured handoff progress (2026-08-23, pending):** 通过 release CLI 在本地生成 `temp/release/handoff.json`（integration、revision 5），并用 CAS 依次为 canonical `INT-R2-01` 记录 `provider_ready`（Eikona real-process mutation evidence）、`consumer_done`（Workbench four-surface component evidence）、`integration_passed`（7.3 final gate）与 `rollback_passed`（Eikona cancel/rollback evidence）；四条记录均绑定 owner ref、evidence ref 与 evidence `digest.json` SHA-256。`task release:handoff:validate ENV=integration` 现从 `handoff_registry_invalid / 0/6` 推进到结构有效的 `handoff_incomplete / 1/6 ready`。仍未勾选：该 validator 是跨 R0-R5 的 canonical 全局门，`INT-R0-01`、`INT-R1-01`、`INT-R3-01`、`INT-R4-01`、`INT-R5-01` 尚未完成；同时 CLI registry 尚不表达 Scaena/Auctra/Pinax/Sonora 的 per-owner disabled/needs_contract 子状态。不得为使 6/6 通过而代签其他 release 或伪造 provider evidence。

Fresh consumer evidence（2026-08-02 15:12–15:13）：`task test:owner-transport:component` 通过，证据为 `temp/integration-test-runs/20260802151247-9ef5b174-a76f-48e8-a58e-6862595c29b6/`，`status=passed`、`duration_ms=65949`、`redaction.total_redactions=0`。该结果只刷新 Owner safe read/event forwarding 与 typed transport component proof；真实 Eikona process、R1 delegation、mutation receipt/reconcile/cancel 和 R2 closeout 继续保持 open。
