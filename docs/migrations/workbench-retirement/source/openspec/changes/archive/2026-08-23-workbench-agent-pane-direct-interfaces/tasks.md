# Workbench Agent Pane Direct Interfaces Tasks

执行规则：Phase 1（注册+渲染器）零后端改动（已以测试断言验证）；Phase 2（传输投影，2026-08-22 追加）允许且仅允许：SDK `http.ts` 路由表条目与 `service/internal/transport/workitemshttp` 只读/受控 mutation wire 投影——业务规则（authorization/版本 CAS/acceptance gate）仍全部在既有 `wiservice.Service`，不新增 Operation、状态机或第二执行权威。tracked-file 写入由单一 implementer 顺序推进；所有新增复杂逻辑必须写中文注释说明不变量、失败语义与恢复条件。

Lane：`S` spec/docs，`W` Web/UI，`V` Verification。顺序：1 → 2 → 3 → (4, 5 并行) → 6 → 7 → (8, 9 并行) → 10。

## 1. 注册与声明

- [x] 1.1 Owner: implementer；Lane: W；Dependencies: none；Paths: `apps/web/src/workbench/agent/agent-pane-registry.ts`；Scope: `agentPaneTypes` 增加 assets/workItems/workflows/dailyOps/identity/gateway/cli/projectWorkspace 8 kind，`REQUESTED_VIEWS` additive 增加 `table|kanban|todo|inbox|approvals|activity|deliveries`，注册 8 个 entry（closed params/`standardDocumentKey("sessionRef")`/requiredAction 按 design 表）；Acceptance: 未知参数/枚举值 fail-closed，TS 穷尽性编译通过；Validation: `bun run typecheck` + 扩展 `agent-pane-registry.test.ts`；Expected: 同 sessionRef 重复解析返回同 document；Failure re-check: 无动态组件/URL/shell 字段。
- [x] 1.2 Owner: implementer；Lane: W；Dependencies: 1.1；Paths: `apps/web/src/workbench/agent/agent-pane-manifest.ts`；Scope: `AgentPaneManifestGroup` + `"workspace"`、ICON_NAMES +8、8 个 `defineManifest`（closedParams 与 registry 完全一致、`mobilePresentation:"sheet"`、`permission:"read"`、真实 digest）；Acceptance: 模块加载 fail-closed 校验通过，manifest↔registry closedParams 一致性测试通过；Validation: `agent-pane-manifest.test.ts`；Expected: 无 invalid manifest 可交付；Failure re-check: manifest 与 locale 键必须同 commit。
- [x] 1.3 Owner: implementer；Lane: W；Dependencies: 1.2；Paths: `apps/web/src/workbench/agent/agent-pane-catalog.ts`, `apps/web/src/workbench/agent/panes/pane-command-palette.tsx`；Scope: catalog group 投影 + palette `GROUP_ORDER`/`groupLabel` 增加 workspace；Acceptance: 分组为扁平 closed list，新条目按 design 表落组；Validation: `agent-pane-palette.test.tsx`；Expected: 分组顺序稳定；Failure re-check: 不引入嵌套分组。

## 2. 图标与双语文案

- [x] 2.1 Owner: implementer；Lane: W；Dependencies: none；Paths: `apps/web/src/design-system/icons/{icon-name.ts,registry.ts,registry.test.ts}`；Scope: 新增 `pane.assets|pane.workItems|pane.workflows|pane.dailyOps|pane.identity|pane.gateway|pane.cli|pane.project` 图标（复用现有 lucide 导入，零新依赖）；Acceptance: registry 测试覆盖新名字；Validation: `task web:test -- registry.test`；Expected: 无新增依赖；Failure re-check: 不引入第二 icon 体系。
- [x] 2.2 Owner: implementer；Lane: W；Dependencies: 1.1；Paths: `api/locale/source/{zh-CN,en-US}/agent/pane.json`, `api/locale/source/{zh-CN,en-US}/agent/detail.json`, `apps/web/src/i18n/messages.generated.ts`；Scope: catalog 标题/描述、`agent.pane.palette.group.workspace`、disabled 原因（identityUnavailable/gatewayOffline/gatewayNeedsContract/cliNeedsContract）、各 pane 四态与列/分区标题；Acceptance: 双语齐备、compose 后生成 bundle 一致；Validation: locale compose + `bun run typecheck` + i18n 测试；Expected: 生产路径无新增 inline fallback 作为完成证据；Failure re-check: source/catalog/generated 三处一致。

## 3. 可用性与 palette 接线

- [x] 3.1 Owner: implementer；Lane: W；Dependencies: 2.2；Paths: `apps/web/src/workbench/agent/panes/direct/use-agent-pane-availability.ts`；Scope: 两层可用性——静态 `TRANSPORT_READY` 表（镜像 SDK http.ts 路由与服务端 transport 挂载：identity/workflows/gateway=true；assets/dailyOps/workItems/projectWorkspace=false，等待 R3 传输投影）+ 运行时探测 `identity.getReadiness()`/`gateway.getOverview()`（identity: ready|integration_ready|degraded→启用，needs_contract|unavailable|contract_mismatch→禁用；gateway: available→启用，needs_contract|degraded|unknown→禁用，error/transport error→offline）；Acceptance: 映射矩阵有中文注释且被测试覆盖，`retry:false`+staleTime；Validation: 新 `agent-pane-availability.test.ts`（12 用例通过）；Expected: 探测不随击键扇出、无传输投影的面 fail-closed；Failure re-check: 不把浏览器猜测当服务端能力、不伪造传输可用。
- [x] 3.2 Owner: implementer；Lane: W；Dependencies: 3.1, 1.3；Paths: `apps/web/src/workbench/agent/agent-conversation-workspace.tsx`；Scope: `paletteItems` switch 增加 8 分支（传输已注册的面启用、identity/gateway 走 hook、无传输投影的面 needs_contract 传输原因、cli 恒禁用），`selectPalettePane` 仅打开传输已注册的 kind（dailyOps 默认 `requestedView:"inbox"`、projectWorkspace 默认 `"table"`，当前均被传输门控防御性不可达）；Acceptance: 复用 `showResolvedPane`/preflight，不改上限；Validation: `agent-pane-palette.test.tsx` + `agent-conversation-workspace.test.tsx`；Expected: cli/未传输面选择不打开 pane；Failure re-check: 禁用条目必须可见且有原因。

## 4. 直连 Pane 渲染器（每 pane 一个垂直切片）

- [x] 4.1 Owner: implementer；Lane: W；Dependencies: 3.2；Paths: `apps/web/src/workbench/agent/panes/direct/direct-pane-shared.tsx`, `workitems-pane.tsx`；Scope: client-subset 接口、scope hook、共享 query-key 工厂、错误→四态映射；workItems pane：服务端分页列表 + 行选中 inspector + create/update/transition mutation（类型化冲突恢复）；Acceptance: 四态齐备、与桌面 pane 共享缓存；Validation: `direct-workitems-pane.test.tsx`（5 用例）；Expected: 冲突保留选择不自动重试；Failure re-check: 不把 canonical 记录写 localStorage。
- [x] 4.2 Owner: implementer；Lane: W；Dependencies: 4.1；Paths: `panes/direct/assets-pane.tsx`；Scope: listAssets 只读投影 + 详情 inspector（collections/savedViews 留待传输投影落地后按需扩展）；Acceptance: 空态/不可用诚实，无伪造行、无 Owner 私有路径；Validation: `direct-assets-workflows-identity-gateway-panes.test.tsx`（assets 3 用例）；Expected: query error → PaneUnavailableState；Failure re-check: 不显示 Owner 私有路径。
- [x] 4.3 Owner: implementer；Lane: W；Dependencies: 4.1；Paths: `panes/direct/workflows-pane.tsx`；Scope: definitions 列表 + runs 列表 + 选中 run 的 `listRunEvents`（仅选中时拉取）；Acceptance: 切换 run 不重放 mutation；Validation: `direct-assets-workflows-identity-gateway-panes.test.tsx`（workflows 2 用例）；Expected: 事件分页/空态正确；Failure re-check: 不为每 run 建独立 SSE。
- [x] 4.4 Owner: implementer；Lane: W；Dependencies: 4.1；Paths: `panes/direct/daily-ops-pane.tsx`；Scope: inbox/approvals/activity 分区（requestedView 切换）+ `decideApproval`/`executeInboxAction` mutation（幂等键、部分失败诚实）；Acceptance: 双击不产生重复动作；Validation: `direct-daily-ops-pane.test.tsx`（4 用例）；Expected: unknown 状态只显示恢复入口；Failure re-check: 不自动重试 unknown_accept。
- [x] 4.5 Owner: implementer；Lane: W；Dependencies: 3.1；Paths: `panes/direct/identity-pane.tsx`；Scope: principal/session/tenants/members + readiness 检查 + allowedActions 只读；Acceptance: degraded 显示降级横幅；严格只读；Validation: `direct-assets-workflows-identity-gateway-panes.test.tsx`（identity 2 用例）；Expected: readiness 状态如实呈现；Failure re-check: 无任何 mutation 入口。
- [x] 4.6 Owner: implementer；Lane: W；Dependencies: 3.1；Paths: `panes/direct/gateway-pane.tsx`；Scope: overview/backends/approvals/activity 只读投影（capabilityState 横幅）；Acceptance: 严格只读，needs_contract/degraded 诚实呈现；Validation: `direct-assets-workflows-identity-gateway-panes.test.tsx`（gateway 2 用例）；Expected: 复用 gateway 视图模型映射（可导入部分）；Failure re-check: 不出现 decide/refresh 动作。
- [x] 4.7 Owner: implementer；Lane: W；Dependencies: 4.1-4.6；Paths: `apps/web/src/workbench/agent/panes/agent-pane-host.tsx`；Scope: 为 8 个 kind 添加渲染分支（cli → `() => null` + 注释，contextMap 先例）；Acceptance: 未知 kind fail-closed；Validation: `bun run typecheck` + host 相关测试；Expected: cli 分支永不渲染内容；Failure re-check: 不注册 stub 渲染器。

## 5. Project Workspace 首批视图

- [x] 5.1 Owner: implementer；Lane: W；Dependencies: 4.1；Paths: `panes/direct/project/use-workitem-stream.ts`；Scope: `watchWorkItemEvents` SSE hook（AbortController 清理 + sequence 去重 → `invalidateQueries`）；Acceptance: 卸载/关闭 pane 后流停止；Validation: `project-workspace-pane.test.tsx` 断言共享缓存（切换视图仅一次列表请求）；Expected: 一个事件源刷新全部视图；Failure re-check: 不在隐藏 pane 泄漏订阅。
- [x] 5.2 Owner: implementer；Lane: W；Dependencies: 5.1；Paths: `panes/direct/project/table-view.tsx`；Scope: 服务端分页表格（title/status/priority/assignee/updatedAt）、行选中 inspector、字段编辑（updateWorkItem fieldMask）；Acceptance: 分页游标正确、编辑冲突类型化恢复；Validation: `project-workspace-pane.test.tsx`（table 用例）；Expected: 不整页下载记录做客户端过滤；Failure re-check: 系统字段禁用有原因。
- [x] 5.3 Owner: implementer；Lane: W；Dependencies: 5.1；Paths: `panes/direct/project/kanban-view.tsx`；Scope: status 分组卡片列、键盘/菜单移动（transitionWorkItem expectedVersion）、冲突保留选择；Acceptance: 无 dnd 库、无 ghost 假成功；Validation: `project-workspace-pane.test.tsx`（kanban 用例）；Expected: 移动失败回列并提示；Failure re-check: 占位卡不得冒充已落位。
- [x] 5.4 Owner: implementer；Lane: W；Dependencies: 5.1；Paths: `panes/direct/project/todo-view.tsx`；Scope: dueAt 分区（今天/即将/无日期/已完成）、完成 transition、blocked/acceptance 诚实禁用；Acceptance: 无本地完成状态；Validation: `project-workspace-pane.test.tsx`（todo 用例）；Expected: 完成必须来自服务端确认；Failure re-check: 不伪造 checked。
- [x] 5.5 Owner: implementer；Lane: W；Dependencies: 5.2-5.4；Paths: `panes/direct/project/project-workspace-pane.tsx`；Scope: requestedView 驱动视图切换 + 共享选择状态；Acceptance: 切换视图不重订阅/重放 mutation；Validation: 视图收敛测试；Expected: 同一事件周期内三视图一致；Failure re-check: 不建第二 reducer 复制 canonical 状态。

## 6. 跨 change 切片记录与文档

- [x] 6.1 Owner: implementer；Lane: S；Dependencies: 4.7；Paths: `openspec/changes/workbench-agent-cli-pane-v1/design.md`；Scope: 增加"切片说明"：7.1 注册脚手架由本 change 提前交付，7.1 保持未勾；Acceptance: 只增注不改任务原文；Validation: `openspec validate workbench-agent-cli-pane-v1 --strict`；Expected: 后续实现者扩展而非重注册；Failure re-check: 无第二 MODIFIED 冲突。
- [x] 6.2 Owner: implementer；Lane: S；Dependencies: 5.5；Paths: `openspec/changes/workbench-project-data-workspaces-v1/design.md`；Scope: 增加"预切片说明"：6.1 注册与 Table/Kanban/Todo 预切片提前交付，任务保持未勾；Acceptance: 只增注不改任务原文；Validation: `openspec validate workbench-project-data-workspaces-v1 --strict`；Expected: 服务端 ProjectQuery 语义落地后替换客户端分组；Failure re-check: 预切片不冒充完整合同。
- [x] 6.3 Owner: implementer；Lane: S；Dependencies: 6.1, 6.2；Paths: `docs/product/agent-workbench-blueprint.md`, `docs/ui/agent-first-workbench.md`, `docs/interfaces/agent-pi-workspace.md`；Scope: §6.4 目录表更新（Assets→deliver-now 等 + 新行，Files/Browser Preview 保持 retain-next）、§5.2 首批 Pane 表 + 尾段、§5.4 pane↔接口映射加行；Acceptance: 文档与实现一致、CLI 标注 needs_contract；Validation: 人工 review + 文档内命令核对；Expected: Assets 移出"暂不伪造"名单；Failure re-check: 不宣称未交付能力。

## 7. 验证与收口

- [x] 7.1 Owner: implementer；Lane: V；Dependencies: 6.3；Paths: 测试文件（见 spec）；Scope: 全部 Vitest（registry/manifest/palette/availability/组件）+ Playwright（扩展 `agent-first.spec.ts` palette 断言、新增 `agent-direct-panes.spec.ts` mock 直连接口）+ 断言 `packages/task-sdk` 零改动；Acceptance: 全绿；Validation: `openspec validate --all --strict && bun run typecheck && task web:test && bun test packages/task-sdk && bun run build`；Expected: 无跳过用例冒充通过；Failure re-check: Playwright 失败截图不入 git。
- [x] 7.2 Owner: implementer；Lane: V；Dependencies: 7.1；Paths: 本 change tasks；Scope: 勾选完成项并复核验收；Acceptance: 每项有对应验证证据；Validation: 复跑关键命令；Expected: 诚实勾选；Failure re-check: 不得勾选未验证项。

## 8. Phase 2 传输投影落地（2026-08-22）

- [x] 8.1 Owner: implementer；Lane: B；Dependencies: 7.2；Paths: `service/internal/transport/workitemshttp/{handler.go,handler_test.go}`；Scope: WorkItem 直连 RPC wire 投影——GET list（pageToken↔Offset，pageSize clamp ≤100）、GET get、POST update（fieldMask 边界）、POST transition；mutation 必须携带 Idempotency-Key（缺失 400）；进程内 TTL 幂等重放缓存（replayed=true，丢缓存由版本 CAS 兜底）；错误映射 not_found/version_conflict/invalid_argument/permission_denied/capability_unavailable；blockers 不进 wire（domain 无时间戳，不伪造 createdAt）；Acceptance: 8 个 handler 测试通过（wire 形状、401/400/404/405/409、幂等重放、fieldMask 边界）；Validation: `go test ./internal/transport/workitemshttp/ -count=1`；Expected: wire 与 SDK normalizer 逐字段对齐；Failure re-check: 本层不复制任何业务规则。
- [x] 8.2 Owner: implementer；Lane: B；Dependencies: 8.1；Paths: `service/internal/runtime/runtime.go`；Scope: mux 挂载 `/v1alpha1/workitems` 与 `/v1alpha1/workitems/`（protect 中间件：security.HTTP principal + rate limit）；Acceptance: 全服务构建通过；Validation: `go build ./...`；Expected: 复用既有 workItemService（managed→IdentityAuthorizer / local→ScopeAuthorizer）；Failure re-check: 不新建 service 实例。
- [x] 8.3 Owner: implementer；Lane: C；Dependencies: 8.1；Paths: `packages/task-sdk/src/http.ts`, `packages/task-sdk/test/http.test.ts`；Scope: ListWorkItems/GetWorkItem/UpdateWorkItem/TransitionWorkItem 路由（mutation 走 POST action 路径 + bodyOmit workItemRef/idempotencyKey，幂等键经 Idempotency-Key header）；Acceptance: 路由合同测试断言 URL/method/body/幂等头；Validation: `bun test packages/task-sdk/test/http.test.ts`；Expected: watch 路由不注册（服务端无事件源，前端 stream hook 优雅降级）；Failure re-check: 不为未实现端点注册路由。
- [x] 8.4 Owner: implementer；Lane: W；Dependencies: 8.3；Paths: `apps/web/src/workbench/agent/panes/direct/use-agent-pane-availability.ts`, `apps/web/e2e/agent-direct-panes.spec.ts`；Scope: TRANSPORT_READY 翻转 workItems/projectWorkspace=true（assets/dailyOps 保持 false——无路由且 dailyOps mutation 无服务实现）；e2e 新增 workItems pane 打开渲染真实行 + project workspace 打开；Acceptance: 6 个 e2e 通过（含 dailyOps 仍 fail-closed）；Validation: `bunx playwright test e2e/agent-direct-panes.spec.ts`；Expected: palette 直接启用、pane 四态来自真实接口；Failure re-check: 不以 fixture 冒充服务端能力。
- [x] 8.5 Owner: implementer；Lane: S/V；Dependencies: 8.1-8.4；Paths: 本 change spec/design/proposal、`openspec/changes/workbench-project-data-workspaces-v1/design.md`、docs 三件；Scope: spec delta 增传输投影 requirement、design 决策 2 更新 Phase 2 状态、ledger 行更新（workItems/projectWorkspace 传输已注册）、project change 预切片注更新、blueprint/ui/interfaces 文档行更新；Acceptance: strict 验证通过、文档与实现一致；Validation: `openspec validate --all --strict` + 复核；Expected: 诚实记录 assets/dailyOps 仍未落地；Failure re-check: 不宣称未交付能力。

## 9. Phase 2 真实栈验证与 bug 修复（2026-08-22）

- [x] 9.1 Owner: implementer；Lane: B/V；Dependencies: 8.1-8.4；Paths: `service/internal/transport/workitemshttp/handler.go`, `handler_test.go`；Scope: 修复真实栈 bug——local profile 的 principal 是 session-scope 语义（`security.Authenticator.Authenticate` 构造 `Principal{Scope: local-session}`，Subject 为空），handler 原 `principal.Subject == ""` 检查会把真实 local 栈全部 401（组件/e2e 注入或 mock principal 测不到）；改为与 task-plane 同约定（principal 存在性即鉴权结果），新增 `TestLocalSessionScopePrincipalAccepted` 回归；Acceptance: 真实栈 smoke 全过；Validation: `go test ./internal/transport/workitemshttp/ -count=1`；Expected: local-session principal 被接受；Failure re-check: 不以 Subject 非空作为鉴权条件。
- [x] 9.2 Owner: implementer；Lane: V；Dependencies: 9.1；Paths: evidence（真实进程 smoke，非提交物）；Scope: 启动真实 workbenchd（local profile + SQLite + session token），经 Task 控制面（`workbench.daily.workitem.mutation` + permission gate resolve）创建真实工作项后 smoke wire 层 8 项：list（真实 principal+存储）/ transition（changed=true v2）/ 同幂等键重放（replayed=true 不重复执行）/ 过期版本 409 / 缺幂等键 400 / 无 bearer 401 / update fieldMask 边界（mask 外 priority 不生效）/ GET 单条；Acceptance: 8/8 符合合同；Validation: curl smoke against `127.0.0.1:8791`（进程已停、临时目录已清）；Expected: wire 合同在真实 auth/存储路径成立；Failure re-check: mock 证据不替代真实栈。
