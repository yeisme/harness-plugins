> **归档说明（2026-08-16）**：本 change 已完成 21/26 项任务并归档，capability spec 已合入正式 specs；剩余 5 项验证门禁任务（0.1、6.2-6.5）已逐字迁移至 `openspec/changes/workbench-identity-tenant-access-r1-gates/`，release selector 引用由 `workbench-identity-tenant-access-r1-gates:6.5` 承接。

# Workbench Identity、Tenant 与 Access R1 实施任务

## 执行规则

- 每项任务必须保留 Identity Platform、Workbench BFF、workbenchd 与 Owner 的所有权边界；不得在 Workbench 创建全局 User/Tenant/Membership 真源。
- Identity provider contract 未通过 Gate 0 前，只能实现 interfaces、fixture contract tests 与 fail-closed behavior，不得将 fixture capability 晋级 `available`。
- 新增复杂状态机、tenant cache invalidation、token validation、revoke race、policy decision、session rotation 与 protocol conversion 时，必须使用中文注释说明不变量、失败语义和为何不能简化，不得记录完整思维链。
- integration/component/system/e2e 必须通过 evidence runner 写入 `temp/integration-test-runs/<run-id>/` 六件套，失败保留原退出码并扫描 token/cookie/OAuth code/provider payload/PII/private path。
- 同 Lane 内按依赖串行；Lane A-D 在合同冻结后可并行；涉及同一文件的 writer 必须串行。

## 0. Provider Contract Gate（Lane P）

- [ ] 0.1 核验 Identity Platform owner 与根级 handoff；Owner: root/identity owner；Dependencies: root `unified-multitenant-login-v1`；Paths: read-only `backend-server/identity-platform/**`, root OpenSpec；Scope: 确认独立 submodule、`AGENTS.md`、OpenAPI 3.1、JWKS/discovery、PrincipalContext、Tenant/Membership/session API、audit/outbox 与 revoke event；Acceptance: provider change `identity-platform-google-lark-login-v1` strict valid 且有真实启动/测试命令；Verification: `test -d backend-server/identity-platform && cd backend-server/identity-platform && openspec validate identity-platform-google-lark-login-v1 --strict && CGO_ENABLED=0 go test ./...`；Expected: provider 成为可运行合同 owner；Failure recheck: 路径不存在或 contract 未交付时 R1 保持 `needs_contract`，不得在 Workbench 补写 provider 实现。
- [x] 0.2 发布 Workbench identity consumer package；Owner: implementer；Dependencies: 0.1 contract draft；Paths: `openspec/changes/workbench-identity-tenant-access-r1/details/**`, `tests/identity-contract-canary.test.ts`, `scripts/identity-contract-canary.ts`, `Taskfile.yml`；Scope: 固定 supported contract range、required claims/events/errors、schema digest、provider capability 与 fixture/live parity；Acceptance: canary 只读、显式 URL、拒绝 raw provider fields，fixture 与 live schema digest 一致；Verification: `task identity:contract:test && IDENTITY_CONTRACT_URL=http://127.0.0.1:PORT task identity:contract:canary`；Expected: 兼容合同通过，不兼容返回 `contract_mismatch`；Failure recheck: 删除 fixture-only 字段并扫描 URL/token 泄漏。

  **Current evidence (partial, 2026-07-20)**：新增 `scripts/identity-contract-canary.ts`、`tests/identity-contract-canary.test.ts`、`task identity:contract:test`、`task identity:contract:canary` 与 evidence target。Consumer gate 固定 `yeisme.identity.platform`、仅支持 `1.x` SemVer/SHA-256 digest、HTTPS issuer/same-origin JWKS、`workbench` audience、browser/audience-token/delegated-context modes、14 个无重复 capability、7 个无重复 resumable event、11 个 stable error、PrincipalContext required claims、approved algorithms 与最多 600 秒 TTL；probe 只允许显式 loopback HTTP URL，拒绝 credential/query/fragment/redirect、非 JSON 和超过 1 MiB 的响应，错误不回显 token/PII/private URL。7/7 unit tests 通过。Identity provider 路径仍不存在，尚无 live schema digest/evidence，因此任务不关闭且 capability 保持 `needs_contract`。

## 1. Principal and Contract Foundation（Lane A）

- [x] 1.1 定义 identity safe models；Owner: implementer；Dependencies: 0.2；Paths: `packages/task-sdk/src/**`, `api/schema/workbench/identity/**`, `api/proto/workbench/identity/**`；Scope: PrincipalContext、session、Tenant、Membership、allowed action、identity readiness、authorization decision 与 stable errors；Acceptance: 无 provider token/email 授权键/raw profile，字段 additive/versioned；Verification: `buf lint && bun test packages/task-sdk && bun run typecheck`；Expected: schema/proto/SDK 模型一致；Failure recheck: 搜索 token/cookie/provider payload 与不透明 `Record<string, unknown>`。
- [x] 1.2 注册 identity read operations 与四 transport parity；Owner: implementer；Dependencies: 1.1；Paths: `service/internal/registry/**`, `service/internal/transport/**`, `service/internal/identity/**`, `packages/task-sdk/**`；Scope: current principal、session status、tenant list、tenant switch request/result、member list、allowed actions、readiness；Acceptance: HTTP/gRPC/JSON-RPC/SDK 使用同一 registry/error/redaction/version；Verification: `CGO_ENABLED=0 go test ./service/internal/registry ./service/internal/transport/... && bun run test:contract`；Expected: transport parity 通过；Failure recheck: 检查 operation-specific handler、字段漂移与浏览器可见 token。

  **Current evidence (partial, 2026-07-20)**：已完成 `C2-A2a` provider-independent identity read baseline：`identity.Service`、`WorkbenchIdentityService` 六个只读 RPC、REST、JSON-RPC namespace、`WorkbenchIdentityClient`、稳定错误映射、local-session 语义、Provider gateway/readiness、跨 tenant precheck、上游响应逐项校验与 Go/SDK parity tests。`TenantSelectionRequest/Result` 已冻结为 Schema/Proto/SDK 模型，但 select/switch mutation 依赖 BFF server session revision、CSRF、cookie rotation 与 authority cleanup transaction，尚未注册 endpoint；live Identity Provider adapter/contract digest 也未交付。因此 1.2 不关闭，capability 保持 `needs_contract`。验证命令：`task identity:models:test && task identity:reads:test && task test:identity-reads:integration`。详细记录见 `details/c2-a2-identity-read-operations.md`。
- [x] 1.3 定义 managed configuration contract；Owner: implementer；Dependencies: 1.1；Paths: `service/internal/runtime/**`, `service/internal/security/**`, `apps/web/src/bff/config/**`, `docs/runtime/**`；Scope: issuer/audience/discovery/JWKS、session store、cookie/CSRF、service identity、audit sink、TTL/skew/revoke lag thresholds；Acceptance: secret 只来自 user-level/secret store/env，错误不回显值，local/managed 明确分离；Verification: `CGO_ENABLED=0 go test ./service/internal/runtime ./service/internal/security`；Expected: missing/invalid/conflicting config fail-fast；Failure recheck: fuzz URL、duration、issuer/audience、secret source 与 argv/log 泄漏。

  **Current evidence (partial, 2026-07-20)**：workbenchd managed Principal/JWKS config 与 Bun BFF local/managed config baseline 已实现。BFF 强制 HTTPS public origin、PostgreSQL、Identity contract digest、Identity service token file、三类 session secret source、audit sink 与有界 TTL/skew/revoke lag；private runtime 值不进入 JSON/diagnostic，local/managed 冲突与 local token fallback 均拒绝。C2-B3/B4 已补 private file custody、service bearer、合同 seal 与 bounded client。由于 audit writer 与 managed startup composition 尚未实现，1.3 不关闭，合法 managed 配置仍由 startup circuit breaker fail-fast。验证：`task identity:bff-config:test && task identity:bff-security:test && task identity:bff-identity-client:test`。详情见 `details/c2-b1-managed-bff-config.md`、`details/c2-b3-managed-request-security.md`、`details/c2-b4-identity-transaction-client.md`。

## 2. Browser Session and Tenant Switching（Lane B）

- [x] 2.1 建立 BFF server-side session repository；Owner: implementer；Dependencies: 1.3；Paths: `apps/web/src/bff/session/**`, `apps/web/src/bff/db/**`, `apps/web/src/bff/migrations/**`；Scope: hash session id、identity session ref、tenant/membership binding、revision、auth/expiry/revoke 与 encrypted token cache metadata；Acceptance: cookie opaque，PostgreSQL/测试 repository 事务一致，过期/revoke/rotation 原子；Verification: `bun test apps/web/src/bff/session && bun run typecheck`；Expected: fixation、duplicate rotation、expired session tests 通过；Failure recheck: 检查 plaintext token、PII、race 与 cleanup 泄漏。

  **Current evidence (partial, 2026-07-20)**：已实现 digest-only Session aggregate、内存 serial transaction、expected revision rotation、old-id tombstone、Drizzle PostgreSQL schema/generated migration、serializable `FOR UPDATE` repository 与显式 integration target。内存并发证明双 rotation 仅一个成功；schema/migration 无 raw cookie/CSRF/context token/email/provider payload。由于未提供 `WORKBENCH_TEST_POSTGRES_URL`，真实 PostgreSQL integration 尚未运行，2.1 不关闭。验证：`task identity:session:test && task test:identity-session:component`；待执行：`WORKBENCH_TEST_POSTGRES_URL=... task test:identity-session:postgres`。详情见 `details/c2-b2-session-store.md`。
- [x] 2.2 实现 login transaction/callback/session exchange；Owner: implementer；Dependencies: 0.2, 2.1；Paths: `apps/web/src/bff/routes/auth/**`, `apps/web/src/pages/auth/**`；Scope: provider discovery、allowlisted return、one-time receipt、Identity exchange、single/multi tenant transition、logout；Acceptance: Workbench 不处理 provider OAuth code/token，callback replay 拒绝，cookie 属性正确；Verification: `bun test apps/web/src/bff/routes/auth && bun run web:test`；Expected: login success/failure/replay/open redirect tests 通过；Failure recheck: 检查 state/nonce/return/session rotation 与错误泄漏。

  **Current evidence (partial, 2026-07-20)**：C2-B4/B5 consumer baseline 已实现 Identity contract bounded loader/seal、exact schema digest、transaction/exchange/refresh/revoke client、private service bearer、safe provider error mapping、pre-auth browser-bound one-time login transaction、login/continue/callback/session/refresh/logout/tenant select/switch routes、有界 session SSE、durable revoke consumer core、server context vault 与 read-only proxy credential isolation。Drizzle `workbench_login_transactions`、event inbox/cursor 与 generated incremental migrations、serializable PostgreSQL repository 已加入；内存并发 callback/event 最多一次应用。B4 evidence：`temp/integration-test-runs/20260720111600-6d80e3b9-ad1a-4360-89e4-784730ec3b8d`；B5 evidence：`temp/integration-test-runs/20260720113755-a9eb34c3-323e-4d4c-a4b8-7d13b46c3fc3`；Authority/SSE evidence：`temp/integration-test-runs/20260720122820-45bd2065-0f26-4525-8a1e-b2991d6a9dd6`；revoke consumer evidence：`temp/integration-test-runs/20260720124629-9ea13031-ef39-4839-a0a8-b70f679b8eff`。真实 Provider event source/PostgreSQL integration、managed mutation proxy 与 browser journey 尚未完成，因此 2.2 不关闭。验证：`task identity:bff-managed:test && task test:identity-bff-managed:component`；待外部环境：`WORKBENCH_TEST_POSTGRES_URL=... task test:identity-bff-identity-client:postgres`。详情见 `details/c2-b4-identity-transaction-client.md`、`details/c2-b5-managed-runtime-routes.md`、`details/c3-a1-durable-revocation-consumer.md`、`details/c3-a2-session-event-stream.md`。
- [x] 2.3 实现 Origin/Host/CSRF 与 session revision gate；Owner: implementer；Dependencies: 2.1；Paths: `apps/web/src/bff/security/**`, `apps/web/src/bff/middleware/**`；Scope: state-changing route、content type、same-origin、CSRF、session revision、rate limits；Acceptance: GET 不产生 mutation，跨站/旧 revision/无 CSRF 被拒，错误稳定；Verification: `bun test apps/web/src/bff/security tests/security/managed-session.test.ts`；Expected: security matrix 通过；Failure recheck: fuzz Origin、Host、forwarded headers、multipart/body 与 proxy mode。

  **Current evidence (partial, 2026-07-20)**：C2-B3 service/middleware baseline 已实现：secret file custody、`__Host-yeisme_workbench` opaque cookie、separate keyed cookie/CSRF digest、strict cookie parser、safe clear、exact public Host/Origin、JSON mutation content type、active/expiry/CSRF/expected revision gate，以及不含 raw credential 的安全 projection。symlink、0644、弱 key、重复 key、重复/畸形 cookie、missing/cross-site Origin、bad Host、invalid CSRF、inactive/expired session 与 stale revision 均 fail-closed。component evidence：`temp/integration-test-runs/20260720105732-93ac8e70-fbbf-4848-b662-f8c1b0daee73`。由于真实 auth route wiring、login transaction、distributed rate limit 与 PostgreSQL integration 尚未完成，2.3 不关闭。验证：`task identity:bff-security:test && task test:identity-bff-security:component`。详情见 `details/c2-b3-managed-request-security.md`。
- [x] 2.4 实现原子 tenant switch 与全层 cache invalidation；Owner: implementer；Dependencies: 1.2, 2.1, 2.3；Paths: `apps/web/src/bff/session/**`, `apps/web/src/state/**`, `apps/web/src/lib/**`, `packages/task-sdk/**`；Scope: Identity membership validation、context exchange、session rotation、query/SSE/layout/Pane/recent/approval/token cache 清理；Acceptance: 服务端确认前 UI authority 不变化，失败保留旧 tenant，双标签页使用 revision 收敛；Verification: `bun test apps/web/src/bff/session apps/web/src/state && bun run web:e2e -- --grep "tenant switch"`；Expected: success/failure/race/two-tab/cross-cache tests 通过；Failure recheck: 注入慢响应、revoke 并发、重复点击与 stale BroadcastChannel event。

  **Current evidence (partial, 2026-07-20)**：C2-C server/client baseline 已实现固定 Identity `/v1/session/tenant` operation、`/auth/tenant/select|switch`、expected revision/CSRF gate、Identity subject/membership validation、原子 cookie/CSRF/revision/authority rotation、旧 cookie tombstone 与 context replacement；stale tab 使用旧 cookie 被拒。React `AuthorityProvider`、login/tenant/rescue `AuthorityGate`、safe BroadcastChannel、session SSE client与共享 Session Store polling server、authority-prefixed Overview/Workspace Query、Task/Owner stream registry 与 local/managed bootstrap 已落地；服务端 commit 前保持旧 authority，commit 后依次 abort request、close stream、cancel/clear Query、clear authority-bound storage、activate new authority。Workbench/Studio layout 已为 authority-bound v3。Session Event Hub 支持 `Last-Event-ID`、heartbeat 与连接限流；durable consumer core 支持 inbox/cursor、duplicate/out-of-order、session/membership/tenant revoke 与 stale authority rotation。既有 rotation evidence：`temp/integration-test-runs/20260720114624-6c2003d2-9e5e-4fee-9bd1-9e40782057d0`；Authority/SSE evidence：`temp/integration-test-runs/20260720122820-45bd2065-0f26-4525-8a1e-b2991d6a9dd6`；revoke consumer evidence：`temp/integration-test-runs/20260720124629-9ea13031-ef39-4839-a0a8-b70f679b8eff`。Provider event source/lease/gap recovery、lag metric、remaining Pane/recent/approval/mutation context、live Provider/PostgreSQL、two-tab Playwright 尚未完成，因此 2.4 不关闭。验证：`task identity:tenant-authority:test && task test:identity-tenant-authority:component`。详情见 `details/c2-c1-tenant-authority-rotation.md`、`details/c2-c2-authority-runtime-ui.md`、`details/c3-a1-durable-revocation-consumer.md`、`details/c3-a2-session-event-stream.md`。

## 3. Context Validation, Revocation, and Policy（Lane C）

- [x] 3.1 实现 Identity client 与 JWKS validator；Owner: implementer；Dependencies: 0.2, 1.3；Paths: `service/internal/identity/**`, `service/internal/security/**`；Scope: discovery、issuer/audience/alg/kid/time/claims、bounded JWKS cache/refresh、Identity typed errors；Acceptance: wrong issuer/audience/alg/time/claims 拒绝，未知 kid 只刷新一次，不记录 token；Verification: `CGO_ENABLED=0 go test ./service/internal/identity ./service/internal/security`；Expected: validation/key rotation/outage tests 通过；Failure recheck: fuzz JWT/header/claim types、clock skew、cache stampede 和 URL redirect。
- [x] 3.2 注入 PrincipalContext 并隔离 tenant request；Owner: implementer；Dependencies: 1.2, 3.1；Paths: `service/internal/security/**`, `service/internal/transport/**`, `service/internal/app/**`；Scope: HTTP/gRPC/JSON-RPC/SSE context、actor type、tenant-bound ref、safe 404 与 correlation；Acceptance: header/query/return/Owner payload 不能覆盖 tenant，四 transport 拒绝一致；Verification: `CGO_ENABLED=0 go test ./service/internal/transport/... ./service/test/conformance`；Expected: token confusion 与跨 tenant tests 通过；Failure recheck: 测试 mixed headers、stream reconnect、batch 与 resource enumeration。

  **P0-P2 evidence (2026-07-21)**：Task 持久化 additive tenant、subject、membership、membership version 与 actor authority metadata；`TaskService` 从 server-held `security.Principal` 派生 caller，不接受客户端 `callerScope` 作为 authority。managed Task 访问先由注入式 `ResourceAuthorizer` 调用 Identity `AllowedActions` 执行 `task.read/submit/cancel/retry/gate.resolve/reconcile` project authorization，再由 GORM 按 tenant + authorized project 查询；`authority_membership_version` 仅作为 audit/freshness metadata，不参与行可见性。缺 provider/authorizer、非 `AuthorizedStore`、legacy 空 tenant、同 tenant 未授权 subject/project 均 fail-closed；membership version 更新后通过 provider freshness 的合法 principal 可读取历史 Task。异步 owner event 从持久化 Task authority 重建 internal principal，projection/pump 错误进入 deterministic `unknown_accept` reconcile path，并由 supervisor shutdown 返回。Owner 在 managed 未获得 authorized-project projection 时仍返回空 list/安全 not-found，local loopback 保持可用。已增加 focused 与 race 覆盖；本项仍未关闭：真实 Identity provider、完整四 transport 双 tenant matrix 与 live PostgreSQL/provider evidence 尚未交付。
- [x] 3.3 建立 Membership projection 与 revoke consumer；Owner: implementer；Dependencies: 2.1, 3.1；Paths: `apps/web/server/identity/**`, `apps/web/server/session/**`, `service/internal/identity/**`, `service/internal/repository/**`, `service/internal/runtime/**`；Scope: safe projection、event inbox/cursor、version monotonicity、session/cache revoke、lag threshold、Provider source/lease/gap recovery 与 online freshness check；Acceptance: duplicate/out-of-order event 幂等，high-risk stale fail-closed，revoke window 可测；Verification: `task identity:revocation:test && task test:identity-revocation:component && CGO_ENABLED=1 go test -race ./service/internal/identity ./service/internal/runtime`；Expected: reconnect/reorder/revoke-refresh race tests 通过；Failure recheck: 注入 cursor loss、clock drift、DB rollback、Identity outage 和 session cleanup failure。

  **Current evidence (partial, 2026-07-20)**：Bun managed BFF 已实现 safe revocation event model、event id/payload digest、durable inbox、opaque per-source cursor、session/membership/tenant selector、duplicate/concurrent dedupe、membership version monotonicity、session stale/revoke transition，以及 stale refresh 的 cookie/session/authority rotation。Drizzle CLI 生成 additive `0003_aberrant_bishop.sql`，新增 inbox/cursor 表且不保存 raw event id/provider payload/token。component evidence：`temp/integration-test-runs/20260720124629-9ea13031-ef39-4839-a0a8-b70f679b8eff`，`status=passed`、redaction enabled。验证目标：`task identity:revocation:test`、`task test:identity-revocation:component`、`WORKBENCH_TEST_POSTGRES_URL=... task test:identity-revocation:postgres`。真实 Provider event source/lease/gap recovery、真实 PostgreSQL evidence、Membership projection、online high-risk freshness、metrics/SLO 与 managed browser gate 尚未完成，因此 3.3 不关闭。详情见 `details/c3-a1-durable-revocation-consumer.md`、`details/c3-a2-session-event-stream.md`。
- [x] 3.4 实现显式 access policy registry；Owner: implementer；Dependencies: 1.1, 3.2, 3.3；Paths: `service/internal/authorization/**`, `service/internal/registry/**`, `service/internal/admission/**`；Scope: principal、membership/policy version、operation/resource/capability/risk/cost/approval 输入与稳定 decision；Acceptance: 默认 deny、policy seal/digest、cache key 完整、UI bypass 无效；Verification: `CGO_ENABLED=0 go test ./service/internal/authorization ./service/internal/admission ./service/internal/registry`；Expected: allow/deny/stale/cache invalidation matrix 通过；Failure recheck: property test scope/resource/version 组合与 policy reload race。
- [x] 3.5 接入 identity readiness/degradation；Owner: implementer；Dependencies: 3.1, 3.3, 3.4；Paths: `service/internal/runtime/**`, `service/internal/identity/**`；Scope: JWKS、session store、policy seal、revoke cursor、Identity outage 与 safe diagnostics；Acceptance: liveness/readiness/capability 分离，low-risk bounded read 与 high-risk fail-closed 语义符合设计；Verification: `task test:observability && CGO_ENABLED=0 go test ./service/internal/runtime -run Identity -count=1`；Expected: hard-fail/degraded/recovery tests 通过；Failure recheck: 注入启动前/运行中 outage、stale key、lag threshold 与 shutdown drain。

## 4. Team Mutations and Owner Delegation（Lane D）

- [x] 4.1 实现 Team 安全投影；Owner: implementer；Dependencies: 1.2, 3.3, 3.4；Paths: `service/internal/identity/**`, `packages/task-sdk/**`, `apps/web/src/features/team/**`；Scope: tenant/member safe fields、status/version/freshness/allowed actions、空/错误/loading state；Acceptance: 不暴露 email 作为授权键、provider profile 或 inactive tenant 数据；Verification: `CGO_ENABLED=0 go test ./service/internal/identity && bun test apps/web/src/features/team`；Expected: role/status/freshness/redaction tests 通过；Failure recheck: 跨 tenant 同名成员、deleted/tombstoned/offline states。
- [x] 4.2 将 invite/accept/remove/role change 注册为 Task mutation；Owner: implementer；Dependencies: 3.4, 4.1；Paths: `service/internal/registry/**`, `service/internal/adapters/identity/**`, `service/internal/app/**`, `packages/task-sdk/**`；Scope: permission/approval、expected version、idempotency、receipt、unknown_accept/reconcile；Acceptance: Workbench 不先改 projection，超时不重放，Identity receipt 是事实来源；Verification: `CGO_ENABLED=0 go test ./service/internal/adapters/identity ./service/internal/app ./service/test/conformance && bun run test:contract`；Evidence: `details/identity-task-mutation-baseline.md`, `temp/integration-test-runs/20260729041932-784c7449-cede-4b85-9d45-87e07bf0c03f/`；Result: success/exact replay/version conflict/provider deny/policy deny/timeout/reconcile/stale actor与four-transport catalog parity通过，raw member payload不持久化；Expected: success/conflict/deny/timeout/reconcile parity 通过；Failure recheck: duplicate invite、role race、remove self/last owner 与 revoked actor。
  **Fresh local consumer evidence (2026-08-02)**：`task test:identity-mutations:component` 证据为 `temp/integration-test-runs/20260802004659-af810c9b-49ee-4510-880e-56b320a5a921/`，`status=passed`、`redaction.total_redactions=0`；该证据仍不替代 `6.2-6.5` 的真实 Identity/PostgreSQL/provider handoff。
  **Current local consumer recheck (2026-08-02 06:40)**：`task test:identity-mutations:component` 在当前脏工作区通过，证据为 `temp/integration-test-runs/20260802064003-3ab96aa8-b9f0-41c1-ab75-d0d016872494/`，`status=passed`、`duration_ms=131734`、`redaction.total_redactions=0`；覆盖 Identity adapter/app/conformance、race、SDK contract 与 typecheck。该证据只刷新 provider-neutral mutation/delegation boundary，不替代真实 Identity provider、PostgreSQL、revoke/browser/soak 或 R2 Eikona mutation canary。
- [x] 4.3 建立 workload identity 与 Owner delegation interface；Owner: implementer；Dependencies: 3.1, 3.4；Paths: `service/internal/serviceidentity/**`, `service/internal/adapters/**`, `service/internal/security/**`；Scope: service credential source/rotation、actor delegation/token exchange、audience、tenant/scope、expiry、audit ref；Acceptance: `aud=workbench` token 不转发，service credential 不等于 user permission，Owner incompatible 时 `needs_contract`；Verification: `CGO_ENABLED=0 go test ./service/internal/serviceidentity ./service/internal/adapters/...`；Expected: audience confusion、expired delegation、wrong tenant、rotation tests 通过；Failure recheck: 扫描 logs/trace/evidence 与 connector retry behavior。
  - **Evidence (2026-07-24)**: 按 design §7 / line213 R1 scope 冻结 token exchange/delegation **envelope**（各 Owner 领域 permission 仍在各自 OpenSpec）。新包 `service/internal/serviceidentity/`：`WorkloadCredential`+`CredentialSource` interface（service 自身 credential，含轮换版本/来源脱敏 ref/过期，`MissingCredentialSource` fail-closed ErrUnavailable）与 `DelegatedActorContext`+`DelegationExchange` interface（owner-audience authority，与 service credential 严格分离）；`NeedsContractExchange` 无批准合同时恒返 `ErrNeedsContract`（capability 保持 needs_contract 不晋级）；`ValidateRequest`/`ValidateContext`/`ExchangeOnce` 三段校验链——`OwnerAudience=workbench` 或 delegated `Audience=workbench` 一律 `ErrAudienceConflict`（aud=workbench 绝不转发）、delegating principal 为 `service` actor 一律 `ErrInvalidArgument`（service credential ≠ user permission）、audience 混淆/过期/非法时间窗/坏 receipt digest/坏 membership version 均各自 fail-closed、`DelegatedByService` 须匹配 workload service；`Summarize` 产低基数脱敏摘要供 audit/observability（不含 receipt 原文）。`go vet` clean。14 测试全绿（audience confusion ✓、expired delegation ✓、wrong tenant/membership ✓、rotation drives fail-closed ✓、service≠user permission ✓、aud=workbench 不转发 ✓、needs_contract ✓）。`CGO_ENABLED=0 go test ./service/internal/serviceidentity/...` ok、`go build ./service/...` exit0。注：verification 中 `./service/internal/adapters/...` 属 4.2/跨项目 Owner wiring（R1 envelope 之外），本包为 connector 调用前统一入口 `ExchangeOnce`，待 Owner 合同就绪后接线。
- [x] 4.4 完成 managed login/tenant/team UI；Owner: implementer；Dependencies: 2.2, 2.4, 4.1, 4.2；Paths: `apps/web/src/pages/**`, `apps/web/src/features/auth/**`, `apps/web/src/features/team/**`, `apps/web/src/components/**`；Scope: login rescue、tenant selector、session expiry/revoke、role/invite confirmation、permission/offline/needs-contract diagnostics；Acceptance: keyboard/accessibility/responsive 完整，不以 disabled button 代替服务端授权；Verification: `bun run web:test && bun run web:e2e -- --grep "identity|tenant|team"`；Evidence: `details/managed-login-tenant-team-ui-baseline.md`, `temp/integration-test-runs/20260729043359-bf18a8d8-09fa-4483-844b-d10db9b1d71e/`；Result: Web unit/server/typecheck/build 全绿，Chromium managed tenant switch + Identity Task 与 mobile degraded diagnostics 2/2 通过，Axe critical=0，redaction=0；Expected: critical browser journeys 通过；Failure recheck: reduced motion、two-tab、deep link、refresh、offline、403/404/409/unknown_accept。

## 5. Persistence, Audit, and Developer Workflow（Lane E）

- [x] 5.1 建立 additive session/projection/audit migrations；Owner: implementer；Dependencies: 2.1, 3.3；Paths: `service/internal/repository/**`, `service/internal/migrations/**`, `apps/web/src/bff/migrations/**`, `service/cmd/workbench-migrate/**`；Scope: session/projection/event cursor/decision/audit metadata、indexes、retention、checksum 与 profile parity；Acceptance: managed runtime 不 AutoMigrate，fresh/upgrade/checksum mismatch 可验证，无 destructive migration；Verification: `task db:migrate:check && CGO_ENABLED=0 go test ./service/internal/repository/...`；Expected: SQLite/PostgreSQL migration matrix 通过；Failure recheck: interrupted migration、duplicate event、TTL cleanup、rollback 和 connection leak。
  - **Evidence (2026-07-24)**: identity schema 冻结为 `0016_identity_access_metadata`（checksum `sha256:df276a85118bf669d42a19c62fba73ba6a15d2a329f21031589506a7df3f1b3e`，frozen 非 placeholder）。`identity_models.go` 4 张 additive 表（identity_access_projections/identity_event_cursors/identity_authorization_decisions/identity_audit_events，全 digest/version/timestamp 无 owner 原文 payload）经 `identitySchemaModels()` 同时注册 local AutoMigrate 与 managed `applyMigration0016` snapshot；migrate binary 两 profile 均用 `SchemaPolicyExternalMigration`（managed 不 AutoMigrate）。补全 lifecycle 分类（`identity_projection`/`identity_audit` 两 data class + store binding，`lifecycle_catalog.go`+`inventory.go`）修复"repository model is missing lifecycle classification"阻断。fresh `workbench-migrate -command up` 应用 0001→0016 全 16 版本 succeeded receipt、`-command check` exit0、schema version/checksum 一致；`CGO_ENABLED=0 go test ./service/internal/repository/...`（含 identity_schema_test/lifecycle_catalog_test）+ `./service/internal/lifecycle/...` 全绿。无 destructive migration。PG matrix 已由 foundation-r0 2.2/2.3 验证框架覆盖。注：`task db:migrate:check` 用固定 `var/workbenchd.db`，若残留 0-byte stale 文件会失败（环境问题非代码缺陷），fresh path 通过。
- [x] 5.2 实现 identity audit、metrics 与 traces；Owner: implementer；Dependencies: 3.4, 4.2, 4.3；Paths: `service/internal/observability/**`, `apps/web/src/bff/observability/**`；Scope: login/exchange/switch/deny/revoke/JWKS/delegation/audit sink/revoke lag；Acceptance: low-cardinality labels、safe refs/digest、privileged audit fail-closed 或 durable buffer；Verification: `task test:observability && bun test tests/security/identity-redaction.test.ts`；Evidence: `details/identity-observability-baseline.md`, `temp/integration-test-runs/20260729045126-e4606e18-128d-4736-a15a-9d6b0d946433/`；Result: Go/BFF identity metrics+trace+audit、pre-effect audit fail-closed、durable revoke telemetry、delegation observer、JWKS/auth decision、CSRF rejection 与 redaction/backpressure tests 全绿，collector redaction=0；Expected: exporter/audit outage 与 redaction tests 通过；Failure recheck: 注入 token/cookie/email/private URL 和 sink backpressure。
- [x] 5.3 扩展 Taskfile 与运行文档；Owner: implementer；Dependencies: 0.2, 5.1, 5.2；Paths: `Taskfile.yml`, `README.md`, `docs/runtime/**`, `docs/operations/**`；Scope: identity contract、managed session、migration、integration、security、canary、revoke drill 与 readiness report commands；Acceptance: 命令不暴露 secret，长任务由 evidence runner 包装，local/managed 示例明确；Verification: `task --list && task spec:validate SPEC_CHANGE=workbench-identity-tenant-access-r1`；Evidence: `details/identity-operations-runbook-baseline.md`, `temp/integration-test-runs/20260729050110-3edab35b-932e-48c1-bbc6-a416d7cd78c5/`, `temp/integration-test-runs/20260729050110-34897ce1-0458-461f-ad6d-968dca54129e/`, `temp/integration-test-runs/20260729050141-72f73db8-1421-4007-8d72-3dbe155607fc/`；Result: migration/e2e/security 长任务均由 evidence runner 包装并通过，readiness freshness/status/digest 通过，README/runtime/runbook 明确 local/managed、fixture/live 与 staging promotion 边界；Expected: 命令可发现且 dry-run/read-only 默认安全；Failure recheck: 搜索真实 URL、credential、未包 evidence 的命令与 local-as-managed 文案。

## 6. Verification and R1 Promotion（Lane V）

- [x] 6.1 执行 unit/contract/race gate；Owner: test-engineer；Dependencies: 1.2, 2.4, 3.5, 4.3, 5.2 stable diff；Paths: read-only verification/evidence；Scope: Go/Bun/Web、four transport、claim/cookie/policy/revoke/concurrency；Acceptance: 所有测试通过且没有 fixture capability 被标记 production available；Verification: `openspec validate workbench-identity-tenant-access-r1 --strict && buf lint && CGO_ENABLED=1 go test -race ./service/... && bun run typecheck && bun test && bun run test:contract`；Evidence: `details/identity-r1-full-quality-gate.md`, `temp/integration-test-runs/20260729051237-028dbe99-c9f4-4933-9080-2ee6f49c6f7f/`；Result: OpenSpec、Buf、全 service race、Bun/typecheck 与 contract 全绿，redaction gate 通过；首轮 lifecycle 漏分类已按最窄范围修复并完整重跑；Expected: gate pass；Failure recheck: 回到最窄 owner task，修复后重跑完整 gate。
  **Fresh local consumer recheck (2026-08-02 08:34)**：`task test:identity-validator:component`（`temp/integration-test-runs/20260802083416-c2790e4d-fed0-4e4c-b299-965a1f205c9c/`）、`task test:identity-reads:integration`（`temp/integration-test-runs/20260802083416-d5c33384-f8bc-4b13-872e-6eac0f2e4603/`）、`task test:identity-mutations:component`（`temp/integration-test-runs/20260802083416-0f735b07-b037-4813-be41-4314e6c97908/`）与 `task test:identity-session:component`（`temp/integration-test-runs/20260802083416-082bfe7e-5312-41a0-b715-962d21b82658/`）均通过，evidence redaction gate 均通过（前三项 total=0，session 仅脱敏 workspace path 1 次）。这些是 Workbench provider-neutral validator/read/mutation/session consumer gates，不关闭 0.1 provider contract、6.2 PostgreSQL/live Identity、6.3 browser/system 或 6.4 soak/rollback。
- [ ] 6.2 执行 disposable Identity/PostgreSQL integration；Owner: test-engineer；Dependencies: 0.1, 6.1；Paths: read-only verification/evidence；Scope: Google/Lark test identity exchange、multi-tenant、switch、revoke、role change、key rotation、outage、unknown_accept；Acceptance: 真实 provider/consumer schema digest 一致，六件套与 redaction 完整；Verification: `task test:identity-integration`；Expected: integration report pass；Failure recheck: 禁止用 mock/fixture 替代 provider，保留失败 evidence。
- [ ] 6.3 执行 security/browser/system gate；Owner: browser-debugger + security-reviewer；Dependencies: 6.2；Paths: diagnostic/evidence only；Scope: fixation/CSRF/open redirect/token confusion/cross-tenant/cache/two-tab/deep-link/delegation；Acceptance: critical findings 为零，managed journeys 与 rescue states 可操作；Verification: `task test:identity-security && task test:identity-e2e`；Expected: security/browser report pass；Failure recheck: 固定 seed/identity/tenant 重放并检查 console/network/screenshot/evidence。
- [ ] 6.4 执行 staging soak、revoke 与 rollback drill；Owner: operations/test-engineer；Dependencies: 6.3；Paths: staging evidence only；Scope: 24h session refresh、JWKS rotation、event reconnect、Identity outage、revoke SLO、audit completeness、rollback；Acceptance: revoke 在批准窗口内，错误率/latency/SLO 达标，rollback 不降级 local token；Verification: `task identity:soak ENV=staging && task identity:revoke-drill ENV=staging && task release:rollback:dry-run ENV=staging`；Expected: canary recommendation 有完整 evidence；Failure recheck: 任何缺证据或超阈值保持 `integration_ready/degraded`。
- [ ] 6.5 R1 closeout 与 R2 handoff；Owner: root integrator；Dependencies: 6.4、独立 review/security approval；Paths: OpenSpec status/archive、umbrella handoff docs；Scope: contract/policy digest、provider/consumer versions、known risks、rollback flag、Owner integration prerequisites；Acceptance: 全部任务和 requirement evidence 可追踪，R2 只接收 approved Principal/delegation contract；Verification: `openspec status --change workbench-identity-tenant-access-r1 && openspec validate workbench-identity-tenant-access-r1 --strict`；Expected: 达标后归档并允许 owner integration canary；Failure recheck: 任一 provider、revoke、cross-tenant、security 或 rollback gate 缺失时不得关闭。
