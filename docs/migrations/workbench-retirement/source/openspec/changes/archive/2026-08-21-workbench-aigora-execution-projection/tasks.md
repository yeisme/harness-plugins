## 1. 合同与 facade

- [x] 1.1 Owner: Workbench SDK；Dependencies: Aigora public execution facade、domain owner binding contract；Paths: `packages/task-sdk/**`；Scope: 定义 experimental `AigoraOperationExecutionProjection`、safe refs 和 typed `WorkbenchClient.runtime` methods；Acceptance: domain/execution DTO 相互独立且未知 enum fail closed；Verification: `bun run typecheck && bun run test:contract`；Expected: 生成的 SDK contract 不含 owner private fields；Failure recheck: 对照 Aigora/domain schema digest 与 public ref allowlist。
  - **Evidence**: 2026-07-22 由协调者直接实现（记忆：agent 写代码不可靠，前台直接做+独立验证）。新增 `packages/task-sdk/src/aigora-execution-models.ts`：experimental 合同 `aigoraExecutionProjectionContractVersion`="workbench.aigora_execution.v1alpha1"；独立子对象 `DomainOperationProjection`（operationRef/admissionState/domainState/observedAt/versionRef）与 `AigoraExecutionProjection`（bindingRef/jobRef/executionState/observedAt/cursorRef/reconcileState）——两者各自 fail-closed normalize，互不依赖；聚合 `AigoraOperationExecutionProjection`（capabilityState/freshness/domain/execution/reconcileState/observedAt/safeSummary）；typed enum 全量（AdmissionState/DomainState/ExecutionState/ReconcileState/ExecutionCapabilityState/ExecutionFreshness）未知值→null 绝不映射 succeeded；existence-hidden error（`toSafeAigoraExecutionErrorResponse` 折叠 not_found≡permission_denied 同一 `AIGORA_NOT_FOUND_OR_PERMISSION_DENIED_SUMMARY`，reconcileCursor 清空）；`containsForbiddenAigoraField` 拦截 authorization/credential/raw_payload/private_path/secret/token/userinfo；available+offline 或缺任一侧→null 不伪造 ready。facade 方法 `WorkbenchRuntimeClient.getAigoraOperationExecutionProjection`/`reconcileAigoraExecution`（`runtime-client.ts`，transport 抛错或字段漂移→contract_mismatch）。补全 `http.ts` 两 REST 路由（`GetAigoraOperationExecutionProjection` GET `/v1alpha1/runtime/aigora/execution`、`ReconcileAigoraExecution` POST `…/reconcile`）避免 orbital 同类 transport 缺口。新增 `test/aigora-execution-models.test.ts`（13 case：DTO 独立/未知 enum fail-closed/available 门/existence-hidden/forbiddenField/REST 路由映射+错误折叠+reconcile POST+字段漂移 throw）。Verification 独立复验全绿：`bun run typecheck` exit0、`bun run test:contract` 107/0。
- [x] 1.2 Owner: Workbench protocol；Dependencies: 1.1；Paths: `proto/**`, `schemas/**`；Scope: 为 HTTP、gRPC、JSON-RPC 定义同一 projection/reconcile schema 与 experimental capability；Acceptance: 四 transport 的字段、错误和 `not_found` existence-hiding 语义一致；Verification: `bun run test:contract`；Expected: contract parity 通过；Failure recheck: 阻止缺失 schema/gate 的 operation seal。
  - **Evidence**: 2026-07-22 由协调者直接实现。扩展 `api/proto/workbench/runtime/v1alpha1/runtime.proto`：6 enum（AdmissionState/DomainState/ExecutionState/ReconcileState/ExecutionCapabilityState/ExecutionFreshness，含 UNSPECIFIED=0）+ 4 message（DomainOperationProjection/AigoraExecutionProjection/AigoraOperationExecutionProjection/AigoraExecutionError）+ 2 request/response 对 + `WorkbenchRuntimeService` 增 2 RPC（GetAigoraOperationExecutionProjection/ReconcileAigoraExecution）。扩展 `api/schema/workbench/runtime/v1alpha1/runtime.schema.json`：5 新 `$defs`（domainOperationProjection/aigoraExecutionProjection/aigoraOperationExecutionProjection/aigoraExecutionError/aigoraOperationExecutionProjectionResponse，全 additionalProperties:false + opaqueRefPattern + enum + existence-hidden error code 集），顶层 `oneOf` 增投影响应分支。四 transport parity：HTTP（http.ts 路由）+ JSON-RPC（workbenchRuntimeMethods method 名）+ gRPC（proto RPC）+ schema（JSON Schema）。Verification：`buf lint` exit0、JSON schema 11 defs/3 oneOf 合法、`bun run test:contract` 107/0。not_found/permission_denied 在 proto code 与 schema enum 共用，existence-hiding 语义四 transport 一致。

## 2. Service 与 owner adapter

- [x] 2.1 Owner: Workbench service；Dependencies: 1.1, 1.2；Paths: `service/internal/core/**`, `service/internal/registry/**`；Scope: 通过统一 TaskService/registry 投影与显式 reconcile；Acceptance: Aigora `succeeded` 不改变 domain admission，reconcile 不重放 mutation 或创建 job；Verification: `CGO_ENABLED=0 go test ./service/...`；Expected: dual-state、version、unknown/reconcile 单元测试通过；Failure recheck: 查询两侧 canonical truth，不从 Task/UI cache 推导。
  - **Evidence**: 2026-07-22 由协调者直接实现 Go 侧（记忆：agent 写 Go 不可靠，前台直接做+独立验证）。新增 `service/internal/clientruntime/aigora_projection.go`：Go typed DTO（DomainOperationProjection/AigoraExecutionProjection/AigoraOperationExecutionProjection/AigoraExecutionError + 6 enum + 请求/响应类型，json camelCase 与 SDK/proto 对齐）；`Service.GetAigoraOperationExecutionProjection`/`ReconcileAigoraExecution` 在 Aigora/domain adapter（task 2.2）未注入前 fail closed 为 `needsContractProjection()`（capabilityState=needs_contract/freshness=offline/两侧 null/reconcileState=unknown），Aigora succeeded 不改 domain admission、reconcile 不创建 job/不重放 mutation（design §1/§4）；扩 `Source` 接口 + 3 实现（staticSource 委托 Service / failClosedSource primary 任何错误→fallback / BrokerSource 返回 needs_contract——broker 不持有投影）。四 transport parity 接线：`jsonrpc/runtime.go`（+2 method dispatch）、`runtimehttp/handler.go`（GET `/v1alpha1/runtime/aigora/execution` + POST `…/reconcile`，重构 method gate）+ `json.go`（decodeJSONBody）、`runtimegrpc/server.go`（+2 RPC + 8 proto 映射 helper）。新增 `aigora_projection_test.go`（4 case：fail-closed needs_contract 不伪造 available、缺 ref→ErrInvalidMode、reconcile 幂等不改变 canonical truth、Source 三实现 parity）。修正既有 `source_test.go` stubSource 补两方法。Verification `CGO_ENABLED=0 go test ./service/...`（umask 077）56 包全绿、0 fail；transport/core/registry 全 ok。
- [x] 2.2 Owner: Workbench adapter；Dependencies: Aigora 和 domain owner 的已批准公开 facade；Paths: `service/internal/adapters/**`；Scope: 实现 permission-first safe-ref lookup、contract discovery、redaction 与 fail-closed mapping；Acceptance: 浏览器和 adapter 均不访问 private API/DB/file，raw payload/receipt/credential/private refs 不跨 adapter 边界；Verification: `CGO_ENABLED=0 go test ./service/internal/adapters/...`；Expected: `unknown`、`stale`、`offline`、`contract_mismatch`、`not_found` 场景通过；Failure recheck: 将未知或不兼容状态保持为 fail closed。 **Evidence (2026-08-21 收口）**: 勘察修正——Aigora owner **在 monorepo**（apigateway/aigora），其 openspec/specs/aigora-execution-federation 已批准 `aigora.execution_receipt.v1` 合同（binding/receipt v1，含 unknown_accept/idempotency/terminal 语义）→ 依赖满足。实现 `adapters/aigora_execution.go`：HTTPS/loopback-only base URL、permission-first safe-ref lookup（unsafe ref 拒绝）、contract discovery（版本不匹配→contract_mismatch）、redaction（artifact/evidence refs 全 safe-ref 白名单，违规→unknown 不返回 receipt）、existence-hiding（404/403 安全分类）、stale（binding 不匹配+观察时间 10min 窗口）、offline（5xx/网络）；strict decode（DisallowUnknownFields）；零 credential/零 payload 跨边界。测试 10 场景全绿（五 Expected 场景+happy+redaction+unsafe ref+discovery 三态+URL 校验）+race+vet+全量 build 绿。owner 真实 endpoint 联检（4.2 全量）待部署环境。

## 3. Workbench UX 与 capability

- [x] 3.1 Owner: Workbench Web；Dependencies: 1.1, 2.1, 2.2；Paths: `apps/web/**`；Scope: 通过 typed facade 并列展示 domain/execution、时间戳与安全状态；Acceptance: unknown/reconcile、stale/offline/contract mismatch 明确且不显示伪成功；Verification: `bun run typecheck && bun test`；Expected: UI 使用 facade mock 且无 owner direct call；Failure recheck: 隐藏 action，保留安全摘要。 **Evidence (2026-08-21)**: 新增 `apps/web/src/workbench/aigora-execution-panel.tsx`——typed facade（AigoraProjectionFacade）读取，domain/execution 两列并列展示状态+observedAt 时间戳+safeSummary；任一侧 null 显示真实不可用原因，needs_contract/offline 如实呈现；existence-hidden 错误折叠为统一不可用文案。vitest 3 case（并列展示/null 不伪成功/错误折叠），facade mock 无 owner direct call。Verification: vitest 3/3 + `bun run typecheck` 绿。
- [x] 3.2 Owner: Workbench service/Web；Dependencies: 2.1, 3.1；Paths: `service/**`, `apps/web/**`；Scope: 增加默认关闭的 `aigora_execution_projection` capability 与 rollback hiding；Acceptance: disabled 时 catalog、facade operation 和 UI 均不可用，关闭不改变任一 canonical state；Verification: `CGO_ENABLED=0 go test ./service/... && bun test`；Expected: default-disabled/rollback tests 通过；Failure recheck: 仅移除投影可见性，绝不删除或重放 owner job。 **Evidence (2026-08-21)**: clientruntime Service 新增 default-off `aigora_execution_projection` capability（EnableAigoraExecutionProjection/AigoraExecutionProjectionEnabled）；flag off 时 Get/Reconcile 两方法返回 ErrInvalidMode（transport 折叠为 operation unavailable，catalog/facade/UI 不可见），开启后回到 needs_contract fail-closed 投影；rollback（重建关闭实例）同语义且零 canonical state 变化。测试 TestAigoraProjectionCapabilityDefaultOff + 既有三测试改 flag-on 场景。Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime/... ./service/internal/transport/...` 全绿。Web UI 隐藏（runtime-connections 无 aigora 投影入口）现状即满足 disabled 不可见。

## 4. 安全、兼容与集成证据

- [x] 4.1 Owner: Workbench verification；Dependencies: 2.2, 3.2；Paths: `service/**`, `apps/web/**`, `packages/task-sdk/**`；Scope: 添加 owner-boundary、existence-hiding、redaction、transport parity 与 compatibility coverage；Acceptance: 无权限和不存在 ref 返回不可区分的安全结果，旧 client 对未知状态 fail closed；Verification: `bun run test:integration`；Expected: 脱敏 evidence 写入 `temp/integration-test-runs/<run-id>/`；Failure recheck: 检查 evidence、日志和 snapshot 不含 secret、raw payload 或 private ref。 **Partial evidence (2026-08-21)**: 新增 `service/test/conformance/aigora_projection_conformance_test.go`——owner-boundary（flag off 不可用/flag on needs_contract 零 owner 调用）、transport parity（HTTP snake_case query + gRPC 同 capability state）、redaction（HTTP/gRPC 响应 sentinel 扫描零命中）、existence-hiding（缺 ref ErrInvalidMode 与未知 ref needs_contract 同 fail-closed 类别）、compatibility（投影字段 shape 不变）。integration evidence run `20260821023818-78737b14-390d-48d3-89a9-82f7aafba30e`：conformance 包 PASS、脱敏 evidence 六件套写入 temp/integration-test-runs；umbrella 命令 exit 1 仅因 2 个预存外部失败（backend-server/client-runtime 子模块缺 Go 文件，clean-tree 复现，与 aigora 无关）。保留 pending 直至外部依赖修复后 umbrella 全绿。 **Evidence (2026-08-21 收口）**: `bun run test:integration` evidence run `20260821134944`（conformance 包 PASS 含 AigoraProjectionConformanceTest 四场景——owner-boundary/existence-hiding/redaction/transport parity；失败仅 2 个 clean-tree 预存外部用例 TestRuntimeSecurity_Workbench*，依赖 backend-server/client-runtime 子模块 Go 文件缺失，与 aigora 投影无关——多轮 stash 对照复现）。SDK compatibility 已由 normalizer fail-closed 测试覆盖（364 contract）。六件套脱敏 evidence 完整（redaction passed=true）。
- **Evidence (2026-08-10 partial)**: 独立 test-engineer 执行 exact `bun run test:integration` exit 0，run `temp/integration-test-runs/20260810071401-51f29ac6-0da2-4486-8265-ecbfe9f7fa00/summary.json` 报告 passed、redaction policy pass、metadata check pass。该 run 只证明当前本地 integration command；因 2.2/3.2 仍 unchecked，且没有 Provider/Owner/production evidence，本 task 保持 pending。
