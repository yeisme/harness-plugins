# Workbench Agent CLI Pane V1 Tasks

执行规则：本 change 不授权当前会话自动启动子 agent。未来执行时，tracked-file 写入必须由单一 implementer 或显式非重叠路径租约推进；test-engineer/reviewer 只验证稳定 diff。所有新增或修改的复杂逻辑、状态机映射、并发、进程生命周期、错误处理、边界检查、协议转换和非显然 fixture 必须写中文注释，说明不变量、失败语义与恢复条件，不记录完整推理链。

并行 lanes：`G` 准入/依赖，`C` 合同，`B` Backend/Task，`H` Host Runtime，`W` Web/UI，`A` Agent/Authority，`V` Verification/Release。`C` 完成前不写 production implementation；`B/H/W/A` 只在合同稳定且路径租约不重叠时并行。实施顺序默认 Slice A（Operation-backed Pane）→ Slice B（Agent collaboration）→ Slice C（Host Runtime）。

## 0. 准入、依赖与首批 catalog 冻结

- [x] 0.1 Owner: root integrator；Lane: G；Dependencies: none；Paths: `openspec/changes/workbench-agent-cli-pane-v1/details/command-inventory.md`；Scope: 枚举现有 sealed Operations、CLI binaries、output modes、mutation/effect、permission/cost/version/idempotency、cancel/reconcile、owner、当前 tests 与生产 readiness；Acceptance: 每个候选命令标记 `operation|host_cli|needs_contract|reject` 并有证据；Validation: `go test ./service/internal/registry/... && rg -n -- '(--json|--agent|--events|--explain)' service/cmd scripts`；Expected: 无把 human-only CLI 晋级为 runnable；Failure re-check: UI 引用或 copy fallback 不等于 sealed capability。
  - **Evidence (2026-08-23):** `details/command-inventory.md` 按 registry 实测声明（`service/internal/registry/{daily,owner,gateway,foundation,operations}.go`）与 14 个 binary 的 flag 实测完成四档分类：`operation`（daily workitem read/mutation、inbox/approval/activity/delivery read、eikona.generation.submit（PA 8.1 选定）、orbit.proposal.accept（PA 8.2 后））；`host_cli`（diagnostics/config-check/readiness 首选 canary，lifecycle/operation-contract 次批，board/workflow-contract/schema-export 需先补结构化输出）；`needs_contract`（daily asset/collection/delivery 等 ModeUnavailable op、scaena/pinax/sonora、gateway 3 op、foundation 探针）；`reject`（backup/migrate/restore-verify/release/worker/workbenchd daemon 与生产效应 binary、scripts/*.ts、synthetic 探针）。**关键发现：无任何 binary 支持 `--events` NDJSON**（158 处命中全为 --json/--agent/--explain，6 个 binary 支持三模式）——Slice C canary 由 5.4 补齐或声明 non-streaming。Validation：`CGO_ENABLED=0 go test ./service/internal/registry/... -count=1` ok + rg 158 命中。human-only CLI 未晋级。
- [x] 0.2 Owner: product + root integrator；Lane: G；Dependencies: 0.1；Paths: proposal/design/details only；Scope: 冻结 Slice A 的 5–8 个 Operation-backed commands、Slice B 的 2 个 Agent scenarios、Slice C 的 2–3 个 Host canary，确认 required-capability ledger 无静默删改；Acceptance: 每个 canary 有 owner、scope、effect/risk、test tenant、rollback 和真实 receipt/evidence；Validation: `openspec validate workbench-agent-cli-pane-v1 --strict`；Expected: first slice 不依赖任意 shell/PTY；Failure re-check: 无 structured output/owner receipt 的命令保持 `needs_contract`。
  - **Evidence (2026-08-23):** `details/catalog-freeze.md` 冻结：Slice A 7 个 Operation-backed commands（6 个 daily Workbench-owned + `eikona.generation.submit` contract-gated，每项含 owner/scope（`t:cli` disposable seed、`proj_canary` dry-run）/effect-risk/rollback/receipt）；Slice B 2 个 Agent scenarios（B1 只读 grant 自动执行链、B2 bounded_write→canonical proposal 链，对应 design 决策 5 四类动作）；Slice C 3 个 Host canary（diagnostics/config-check/readiness，全部只读三模式非流式，`--events` 缺口由 5.4 收敛）。Host Runtime 首批 = 同机 pure-Go sidecar（design §337）。Ledger 语义核对无删改；`needs_contract`/`reject` 分类维持。Validation：`openspec validate workbench-agent-cli-pane-v1 --strict --no-interactive` PASS。首期无任意 shell/PTY 依赖。
- [x] 0.3 Owner: architect/root integrator；Lane: G；Dependencies: 0.1；Paths: `openspec/changes/workbench-agent-cli-pane-v1/details/host-runtime-decision.md`；Scope: 决定 production Host Runtime 是同机 pure-Go sidecar 还是企业 runner provider，并冻结 handshake、hostSessionRef、receipt/lookup/reconcile、health、mount/env/secret/network/resource contract；Acceptance: canonical owner、部署/身份/升级/回滚和 Workbench adapter 边界明确；Validation: design review + `openspec validate workbench-agent-cli-pane-v1 --strict`；Expected: 不把 host process/file state 归入 browser 或 Workbench UI；Failure re-check: 无法证明 isolation/receipt 的 provider 不进入 Slice C。
  - **Evidence (2026-08-23):** `details/host-runtime-decision.md` 决策：首批 = 同机 pure-Go sidecar（理由：Go stdlib 零依赖增量、workbench-worker 同机独立进程先例、Slice C canary 全只读、provider 是 `clihost.Provider` interface 的第二实现而非分叉）。冻结十面 contract：handshake（binding digest 校验）、hostSessionRef、argv-only dispatch（无 client executable/cwd/env/PTY）、receipt/lookup/reconcile（unknown 只查原 request digest、cancel 区分 requested/cancelled）、health（liveness≠readiness，失败零 dispatch）、mount/env/secret（allowlist + 不回传）、network（canary loopback-only）、resource ceiling（客户端不可放宽）、diagnostics（JSON stderr 分离）、升级/回滚（binding 兼容矩阵 + kill switch drain）。Workbench adapter 边界：interface 隔离、sidecar 无业务 DB/credential/human session；Host endpoint 不入 browser bundle。浏览器/UI 不持有 host process/file state。Validation：strict validate PASS。
- [x] 0.4 Owner: dependency-auditor + implementer；Lane: G；Dependencies: 0.2；Paths: `apps/web/package.json`, `bun.lock`, `service/go.mod`, decision docs；Scope: 证明首版复用现有 Pane/UI/virtualization/Go stdlib，无需 xterm、PTY、Rust、cgo 或新增 shell library；Acceptance: 每个新增依赖有 version/license/supply-chain/React19/CGO decision；Validation: `bun install --frozen-lockfile && CGO_ENABLED=0 go test ./service/...`；Expected: 默认零新增 runtime dependency；Failure re-check: 不为“终端外观”引入终端执行能力。
  - **Evidence (2026-08-23):** `details/dependency-decision.md`：xterm/node-pty/Rust/shell 库全部 reject（非交互、非 PTY、argv-only、stdlib 可覆盖）；表格/虚拟化复用既有组件（`@tanstack/react-table` 等归 project-data-workspaces 0.3 审计，不归属本 change）；`service/go.mod` git diff 零变更（Go 侧 stdlib only，发布路径 `CGO_ENABLED=0`）；xterm/pty/rust/shell 关键字在 go.mod/package.json/bun.lock grep 零命中。Validation：`bun install --frozen-lockfile` PASS；`CGO_ENABLED=0 go test ./service/... -count=1` **155 packages ok / 0 FAIL**。零新增 runtime dependency。

## 1. Command、Intent、Result 与 Host 合同

- [x] 1.1 Owner: implementer；Lane: C；Dependencies: 0.2；Paths: `api/proto/workbench/cli/**`, `api/schema/workbench/cli/**`；Scope: 定义 `CommandDescriptorV1`、catalog/digest、closed argument schema、availability/effect/risk/agent policy/resource/output contracts；Acceptance: client projection无 executable/argv/cwd/env/secret/path/URL，closed enums和limits完整；Validation: `buf lint && bun run test:contract`；Expected: proto/JSON Schema snapshots一致；Failure re-check: unknown union/field必须fail closed。 **Evidence (2026-08-23):** 新增 `workbench.cli.v1alpha1` Proto/JSON Schema 与生成 Go binding，冻结 Operation/Host 两类 execution、read/bounded-write/dangerous、risk、Agent policy、confirmation、availability、typed argument/cardinality/ref class、resource ceiling 与 human/agent/json/events/explain 单 projection 输出合同；客户端 schema 不含 executable/argv/cwd/env/private path/URL/credential/raw output/Owner payload 等字段，所有 object `additionalProperties:false` 且 enum/数组/字节/时长/分页上限闭合。新增 `cli-command-contract.test.ts` 89 assertions；`buf lint`、生成包 `CGO_ENABLED=0 go test` 与完整 `bun run test:contract` 367/367 通过。
- [x] 1.2 Owner: implementer；Lane: C；Dependencies: 1.1；Paths: same CLI proto/schema roots；Scope: 定义 immutable `CliCommandIntentV1`、prepare/get requests、safe argument/context/expected-version refs、TTL/version/digest与stable errors；Acceptance: Task persist input只需`inputRef`/context/limits，raw secret/path/env不在schema；Validation: `buf lint && bun run test:contract`；Expected: invalid/expired/stale schema cases可表达；Failure re-check: arbitrary JSON map或freeform command字段立即阻断。 **Evidence (2026-08-23):** `Prepare/GetCliCommandIntent` 与 `CliCommandIntentV1` 已冻结 immutable intentRef/digest、descriptor revision、scope/Context Pack/expected-version refs、typed scalar/list oneof、bounded resource request、safe argument summaries、origin/initiating principal/delegation refs、TTL/version/timestamps和prepare idempotency。stable error enum覆盖 stale/expired/invalid/permission/cost/delegation/runtime/concurrency/output/redaction/version/idempotency/cursor/unknown；schema无 `Struct`/map/freeform command/raw protected value、本机位置或环境字段，所有 union 以 oneOf/oneof 闭合。
- [x] 1.3 Owner: implementer；Lane: C；Dependencies: 1.1, 1.2；Paths: same CLI proto/schema roots；Scope: 定义 `CommandSessionProjectionV1`、`CliCommandRunProjectionV1`、`CliCommandResultProjectionV1`、pagination/cursor/outputTruncated/artifact/receipt refs；Acceptance: run status严格引用Task status，不创建第二 lifecycle enum；Validation: `buf lint && bun run test:contract`；Expected: summary/facts/events/artifacts/explain/details同源；Failure re-check: raw stdout不作为status字段。 **Evidence (2026-08-23):** session/run/result、List runs/Get result、25-run cursor pagination、typed safe facts、bounded events/artifacts/actions/evidence/receipts、Explain/Details summaries、permission-checked canonical JSON/console artifact refs、outputTruncated 与 process exit disposition 已加入；Proto直接 import `workbench.task.v1alpha1.TaskStatus`，JSON Schema通过外部 `$ref` 指向 canonical Task status，未定义第二 lifecycle enum，raw stdout/stderr不进入 status 或内嵌 result。`cli-command-contract.test.ts` 增至 141 assertions；`buf lint`、`buf generate`、生成包 CGO0 compile 与完整 contract 367/367 通过。
- [x] 1.4 Owner: implementer；Lane: C/H；Dependencies: 0.3, 1.1, 1.2；Paths: `api/proto/workbench/clihost/**`, `api/schema/workbench/clihost/**` 或批准的 provider contract roots；Scope: 定义 Host dispatch/cancel/lookup/readiness、argv-safe binding refs、resource policy、hostSession/receipt/output stream；Acceptance: 无 command string/client executable/cwd/env/stdin/PTY，unknown accept可lookup；Validation: contract lints/golden；Expected: provider可被替换且语义稳定；Failure re-check: diagnostics与protocol stdout必须分离。 **Evidence (2026-08-23):** 新增 `workbench.clihost.v1alpha1` Proto/JSON Schema 与生成 Go binding，冻结 readiness、dispatch、cancel、lookup/reconcile、Watch output stream、opaque intent/binding/value/mount/policy refs、resource/network policy、hostSession/receipt 和 unknown-accept lookup 语义；provider observation 明确不构成第二套 Task lifecycle。客户端/Host 公共合同不声明 command string、client executable、argv、cwd、env、stdin、PTY、private path、URL 或 credential 字段；输出只允许有界且已由 provider 脱敏的 frame，并由 Workbench 再次脱敏，`PROTOCOL` 与 `DIAGNOSTIC` channel 强制分离。`cli-host-contract.test.ts` 80 assertions、`buf lint`、`buf generate`、生成包 `CGO_ENABLED=0 go test` 与完整 `bun run test:contract` 368/368 通过。
- [x] 1.5 Owner: implementer；Lane: C；Dependencies: 1.1-1.4；Paths: `packages/task-sdk/src/cli-*.ts`, generated models/codecs；Scope: 生成/实现严格 normalizer、unknown value fail-closed、stable error和safe ref validation；Acceptance: SDK不暴露Host private binding，不接受shell string；Validation: `bun test packages/task-sdk && bun run typecheck && bun run test:contract`；Expected: model/codec golden通过；Failure re-check: 不用类型断言绕过closed schema。 **Evidence (2026-08-23):** 新增公开 `cli-models.ts`，以构造式 strict codecs 覆盖 catalog/descriptor、typed arguments、prepared intent、immutable intent、canonical Task-derived run/session/result 与 CLI error；每个 object 精确键匹配，所有 union/enum/digest/safe ref/limits/唯一性/nullable 语义均 fail closed，Agent intent 额外强制 delegation binding，raw console 仅能由 safe artifact ref 表达。SDK 不导出 `clihost` 或 Host dispatch/binding codec，prepare request 的 command/shell/executable/argv/cwd/env/stdin/PTY/hostBinding 等未知字段全部拒绝；实现无 `as unknown as` 或 DTO 类型断言绕过。CLI 新错误码加入共享 `StableErrorCode` 判定。新增 `cli-models.test.ts` 41 assertions；`bun test packages/task-sdk` **371/371**、根与 Web typecheck、完整 `bun run test:contract` **374/374** 通过。
- [x] 1.6 Owner: implementer；Lane: C/W；Dependencies: 1.1-1.3；Paths: locale sources/manifests/generated bundles；Scope: 新增 CLI catalog、effect/risk、gates、states、errors/recovery、tabs、Agent origin 的 zh-CN/en-US 文案；Acceptance: UI无新增硬编码生产文案，reason code只作secondary metadata；Validation: locale compose command + `bun run typecheck && bun test`；Expected: source/catalog/generated一致；Failure re-check: inline fallback不作为完成证据。 **Evidence (2026-08-23):** 新增 `agent/cli.json` zh-CN/en-US **79 个同构键**，覆盖 Pane 标题/说明、catalog、primary actions、effect/risk、permission/cost/decision/delegation gates、catalog 与 canonical Task states、17 个 stable errors、10 个 recovery hints、Summary/Facts/Events/Artifacts/Explain/Details/Console tabs 和 user/Agent origin；安全/状态/错误键在 catalog policy 中受保护，reason code 未被用作主文案。compose 生成 catalog、Go bundles 与 Web module，`check-i18n.ts` 仅把既有 `agent.cli.v1`/`agent.cli.read` 标为非文案合同 literal，没有 inline fallback。Validation：compose/check **2748 keys / 22 namespaces / 2748 authored en-US**，locale golden 5/5、typecheck PASS、完整 `bun test` **638 pass / 7 environment-gated skip / 0 fail**。

## 2. Command Registry 与 Prepared Intent Backend

- [x] 2.1 Owner: implementer；Lane: B；Dependencies: 1.1；Paths: `service/internal/cli/domain/**`, `service/internal/cli/registry/**`；Scope: 实现 descriptor/binding domain、closed validators、catalog seal/sort/digest、Operation projection与availability派生；Acceptance: 缺schema/gate/output/binding时拒绝seal或`needs_contract`，descriptor只能收紧Operation policy；Validation: `CGO_ENABLED=0 go test ./service/internal/cli/... -run 'Descriptor|Catalog|Seal' -count=1`；Expected: deterministic digest和fail-closed矩阵通过；Failure re-check: 不维护第二份手工command/action表。 **Evidence (2026-08-23):** 新增 CLI domain/registry：closed argument/resource/output/policy validators、Operation/Host source bindings、排序 seal、descriptor/catalog SHA-256 revision、clone-safe read surface。Operation command 仅引用 canonical sealed Operation 与其 schema ref，effect/risk/Agent/confirmation/event/cancel/reconcile 只能相等或收紧；available permission operation 缺 scopes、schema drift、unknown enum/output、resource 越界均拒绝 seal。缺失/Unavailable Operation 或缺失/disabled/not-ready Host binding 仅投影 `needs_contract`；Host binding ref/digest 不进入 public catalog，但 binding digest 会参与 descriptor revision，provider drift 必然使 intent stale。Focused matrix **5 top-level + 6 subtests PASS**，并与 `service/internal/registry` 共同 CGO0 验证通过；catalog 注册顺序变化不改变 digest。
- [x] 2.2 Owner: implementer；Lane: B；Dependencies: 1.2；Paths: `service/internal/repository/cli_command_models.go`, migrations/catalog/snapshots；Scope: 新增 immutable/TTL safe intent GORM model、scope/expiry/digest/idempotency indexes与additive migration；Acceptance: 无raw secret/path/env/owner payload，pure-Go SQLite可fresh/upgrade；Validation: `CGO_ENABLED=0 go test ./service/internal/repository -run 'CliCommandIntent|Migration' -count=1`；Expected: constraint/expiry/rollback tests通过；Failure re-check: 不引入ordinary raw SQL或DROP/rename/narrowing。 **Evidence (2026-08-23):** 新增 migration `0030_cli_command_intents` 与 5 张 additive GORM 表：immutable intent metadata、closed typed arguments/scalars、safe summaries、expected-version refs；主表提供 scope/expiry/tenant+digest/idempotency scope+key 索引，创建 API 只有 create/get，15 分钟 TTL、scope-hidden lookup、expiry、exact replay、changed-request idempotency conflict 与 digest conflict 均 fail closed。typed scalar 以 enum/bool/int/number/text/reference 闭合列保留 `false`/`0` presence，拒绝多字段 union、control/credential/private-path/URL sentinel 与 oversized metadata；合法 shell 元字符按普通 text 原样保存，符合后续 argv-only、不经 shell 解释的合同。client projection仍只读取 safe summaries，不暴露 protected typed values。migration fresh/0029→0030 upgrade 保留既有数据，无 DROP/rename/narrowing；schema/lifecycle catalog 显式登记为 `cli_command_intents` 短操作窗口、manifest-only、无 owner payload。Focused repository tests PASS，完整 `CGO_ENABLED=0 go test ./service/... -count=1` **全通过**。
- [x] 2.3 Owner: implementer；Lane: B；Dependencies: 2.1, 2.2；Paths: `service/internal/cli/service/**`；Scope: 实现 `PrepareCliCommandIntent` 的 principal/scope、descriptor revision、typed args、Context Pack、expected versions、resource limits、redaction与canonical digest；Acceptance: rejected值不进error/log/evidence，intent immutable且TTL；Validation: focused unit/integration tests；Expected: valid/invalid/stale/expired/cross-tenant/oversized cases通过；Failure re-check: free text/secret ref需要二次redaction与bounds。 **Evidence (2026-08-23):** 新增 `cli/service` preflight：identity 只从 `security.Principal` context 派生，local 固定为 `local`/`local:user`，managed tenant/principal exact bind；Agent origin 只能由 server-side `WithAgentAuthority` 注入 initiating principal/delegation，client DTO 无身份逃逸字段。服务读取当前 sealed descriptor 与 tenant/workspace-scoped Context Pack，校验 revision/expiry/contract，按 descriptor 字段顺序归一 enum/bool/int/number/text/ref 与 single/list，保留 false/0 presence，unknown/missing/union/cardinality/bounds/control/private path/URL/secret sentinel 均在 repository create 前 fail closed。合法 shell 元字符作为普通 typed text 保留；protected ref 仅进受限存储，Prepare 投影只返回 `[redacted]` summary。canonical wire 形状固定字段+domain separator，输入乱序产生同 argument/request digest 并 exact replay；resource ceiling 与 `maxSerializedBytes` 阻断 oversized；bounded-write/dangerous 强制 expected owner version，并经可注入 safe reader exact compare。所有错误只含稳定 code/静态摘要，不回显 rejected/Owner/SQL 值。Focused matrix（valid/replay/local/Agent/invalid/stale/expired/cross-tenant/oversized/version/redaction）PASS；完整 `CGO_ENABLED=0 go test ./service/... -count=1`、CLI+repository race-focused PASS，strict OpenSpec 与 `git diff --check` PASS。
- [x] 2.4 Owner: implementer；Lane: B；Dependencies: 2.2, 2.3；Paths: CLI query/session projector services；Scope: 实现 descriptor list/get、intent get、Task-derived run list/result lookup、opaque cursor和scope trim；Acceptance: session projection可重建，不保存第二状态，权限撤销后不可读取历史敏感投影；Validation: `CGO_ENABLED=0 go test ./service/internal/cli/... -run 'List|Session|Cursor|Scope' -count=1`；Expected: pagination/restart/revoke通过；Failure re-check: no N+1或跨tenant Task扫描。 **Evidence (2026-08-23):** `service/internal/cli/service/query_{types,service}.go` now exposes sealed-catalog descriptor list/get, scoped intent lookup, and Task-derived run/result projection contracts. Opaque cursors bind tenant/workspace/project/session/runtime/scope plus catalog revision and expire after 10 minutes; a restarted service rebuilds the first page from source truth and rejects process-local stale cursors. Managed reads re-authorize before touching repositories/sources, so revoked access is existence-hidden and produces zero source calls. Intent projections return safe summaries only; protected typed values remain redacted. Run pages require one scope-filtered source batch per page, validate every canonical `core.Task`, reject cross-tenant/scope data, and derive status/version from Task truth without a second lifecycle or N+1 lookup. Result projection similarly uses one source call, validates bounded facts/events/artifacts/receipts, derives terminal state from the canonical Task, and recomputes a stable digest. The concrete Task metadata adapter remains intentionally owned by 3.3. Evidence passed: focused pagination/session/cursor/scope tests, restart/revoke/cross-tenant/single-batch assertions, `CGO_ENABLED=0 go test ./service/... -count=1`, focused CLI race tests, `openspec validate --all --strict`, and `git diff --check`.
- [x] 2.5 Owner: implementer；Lane: B/A；Dependencies: 2.1, existing permission/proposal contracts；Paths: CLI authorizer/gate evaluator；Scope: 映射 user/system-agent actor、initiating principal、effect/risk、roles/scopes、confirmation、permission/cost与expected version；Acceptance: client role/identity忽略，Agent默认prepare-only，descriptor只能收紧；Validation: permission matrix tests；Expected: user/read/write/dangerous/expired grant/role revoke矩阵通过；Failure re-check: UI hidden/disabled不能替代server deny。 **Evidence (2026-08-23):** `service/internal/cli/service/authorization.go` is now the server-side command policy/gate evaluator used by `PrepareCliCommandIntent`. The prepare DTO contains no actor, identity, role, effect, risk, gate, or submit-path overrides; actor and initiating principal are derived only from authenticated `security.Principal` plus server-only `WithAgentAuthority`. Current membership roles/scopes can be reloaded through `CommandAuthoritySource`, with exact tenant/principal/membership version/expiry checks so revoked roles or stale membership fail before repository writes. Optional `security.ResourceAuthorizer` checks the descriptor's sealed `actionId` against the project resource; UI availability never substitutes for this deny. User read/write/dangerous decisions preserve sealed effect/risk and deterministically map `none|user|approver` to Task permission/cost gates plus expected-version requirements. Agent decisions always record `system-agent` and initiating principal: `never` denies, writes/dangerous require the proposal submit path, and reads default to prepare-only. Only a current server-side grant bound to tenant/workspace/project/session/runtime/scope, initiating principal, command/group, expiry, and resource ceilings yields `delegated_task`; missing, expired, revoked, mismatched, or undersized grants remain prepare-only. Task 8.4 still owns the concrete approved grant/receipt store. Descriptor source policy remains non-loosenable through the sealed registry and its policy tests. Permission matrices cover user/read/write/dangerous, role/scope/resource revoke, expired/mismatched grants, Agent prepare/proposal/delegated routes, permission-before-unsafe-argument/version ordering, and no rejected-value leakage. Evidence passed: focused CLI registry/service tests, focused CLI race tests, `CGO_ENABLED=0 go test ./service/... -count=1`, `openspec validate --all --strict`, and `git diff --check`.

## 3. TaskService 与 Operation-backed Commands（Slice A）

- [x] 3.1 Owner: implementer；Lane: B/C；Dependencies: 1.1-1.3, 2.1, 2.3；Paths: `service/internal/cli/operations/**`, `service/internal/registry/**`；Scope: 将首批 sealed Operations 投影为 `executionKind=operation` descriptors，编译prepared intent为target Operation input；Acceptance: 保留原schema/gates/idempotency/owner adapter/receipt，不spawn subprocess；Validation: registry + focused operation integration tests；Expected: direct API与CLI Pane Task语义一致；Failure re-check: command layer不得修改Owner input/state规则。 **Evidence (2026-08-23):** added `service/internal/cli/operations` with an ID-selection-only projector and immutable intent compiler. `ProjectSealedOperations` accepts only selected canonical Operation IDs; schema, action/target identity, effect/risk, Agent policy, confirmation, permission/cost requirements, events/cancel/reconcile, availability, and source revision all derive from the sealed Operation catalog, so there is no second command/action policy table. Every CLI argument contract is closed and contains only explicitly declared primitive/typed-array Operation fields. A canonical Operation may remain open for an existing direct API, but the CLI exposes only its declared typed surface; unknown properties can never enter an intent, and the original Operation validator remains the final authority. Tenant/workspace/project/session/runtime/scope, Context Pack, idempotency, and declared resource fields are server-injected and cannot be forged as arguments; empty/untyped or wrongly typed injected contracts fail before catalog seal. `ModeUnavailable` remains `needs_contract`. `Compiler` reloads the current descriptor, rejects expired/stale/corrupt intents, reconstructs typed JSON, and delegates final canonicalization/digest/schema checks to the original `registry.ValidateInput`; it returns the original Operation object unchanged, including handler/adapter, mutation, idempotency, permission/cost, event and lifecycle semantics, and never invokes a handler, adapter, owner API, shell, or subprocess. The internal intent record now also reloads its already-persisted idempotency key/request digest for the execution facade without adding them to public projections. Integration evidence covers real `PrepareCliCommandIntent → GORM load → original Operation compiler`, context-forgery rejection, deterministic input digest, unavailable and typed-open-subset handling, and unchanged Operation handler/gate flags. The concrete multi-category execution matrix remains owned by 3.4. Evidence passed: focused CLI/registry/repository tests, complete `CGO_ENABLED=0 go test ./service/... -count=1`, focused race tests, `openspec validate --all --strict`, and `git diff --check`.
- [x] 3.2 Owner: implementer；Lane: B；Dependencies: 3.1；Paths: CLI execution application service + existing TaskService facade；Scope: 实现统一 `SubmitPreparedCommand` helper/facade，严格通过`SubmitTask`，计算actor/intent/idempotency/expected-version映射；Acceptance: duplicate exact request返回同Task，changed digest冲突；Validation: focused idempotency/double-click tests；Expected: exactly-one Task lineage；Failure re-check: 不在handler或UI直接调用adapter。 **Evidence (2026-08-23):** `service/internal/cli/execution/service.go` now resolves the authenticated tenant before intent lookup, loads the immutable intent by its exact tenant/workspace/project/session/runtime/scope key, re-runs execution-time identity/delegation, role, permission, cost, resource, Context Pack, expiry, and expected-owner-version gates, then compiles through the sealed original Operation and submits only through the existing `app.TaskService.SubmitTask` facade. The service never invokes an Operation handler or owner adapter directly. User and valid delegated-Agent task paths map server-authored actor/context, project mode, expected owner version, bounded resource ceilings, and a stable command execution idempotency key into one canonical Task lineage; prepare-only and proposal decisions stop before compilation/submission for their later 8.3/8.4 integrations. A read-only `ReplayTaskSubmission` path rechecks current authority and returns only an already-existing exact Task without creating or dispatching; this replay probe runs before mutable owner-version/project-mode checks, so a completed double-click still returns the original Task after the owner version advances. New submissions still recheck the latest expected version immediately before `SubmitTask`. A changed compiled intent digest, expected owner version, or safe CLI metadata under the same execution key conflicts and does not execute again. Tests cover actual Prepare -> SQLite intent -> compiler -> TaskService execution, missing delegation and expired authentication before repository access, execution-time principal/Context Pack/resource/version rechecks, project-mode mapping, exact replay after version advance, and blocked prepare/proposal routes. Task safe CLI metadata and persisted submission correlation remain owned by 3.3. Evidence passed: focused CLI/application idempotency tests, focused race tests, `CGO_ENABLED=0 go test ./service/... -count=1`, `openspec validate --all --strict`, and `git diff --check`.
- [x] 3.3 Owner: implementer；Lane: B；Dependencies: 2.4, 3.1, 3.2；Paths: Task metadata safe refs/event projection；Scope: 为command Task关联session/runtime/scope/command revision/origin safe metadata并保持现有存储边界；Acceptance: ListCliCommandRuns无需新状态表即可过滤，task不存typed raw payload；Validation: repository/service restart tests；Expected: close/reopen/restart均可派生；Failure re-check: 不把session presentation变成execution authority。 **Evidence (2026-08-23):** canonical `core.Task` now carries an optional bounded `CLITaskMetadata` value containing only intent/session/runtime/scope refs, command ID, descriptor revision, origin, and submission digest. `SubmitPreparedCommand` supplies this server-authored correlation to `TaskService`; Task submission validates every ref/digest/origin, includes the metadata in replay equivalence, and persists no compiled typed CLI input in `Task.ReplayInput`. The metadata is presentation/correlation only and is not consulted for permission, cost, expected-version, admission, dispatch, or owner authority. Additive migration `0031_task_cli_metadata` adds eight nullable columns and composite `idx_task_cli_scope` to `task_models`, freezes the earlier 0015 Task snapshot, preserves predecessor rows, and creates no CLI run/session/event lifecycle table. `repository.List/GetCliTaskProjection` filters the canonical Task table by exact authorized tenant/workspace/project/session/runtime/scope and batch-loads Task events/artifacts/receipts; the first page is a fixed four-query path independent of run count, with deterministic descending keyset pagination. `TaskRunProjectionSource` derives run status/version/summary/result/event/artifact/receipt projections exclusively from that canonical snapshot. A file-backed SQLite integration closes and reopens the database and query service, reconstructs both paginated runs and result/event history, rejects a changed metadata replay, hides another session, and verifies the typed input marker never persists. Evidence passed: focused migration/repository/service/restart/idempotency tests, `CGO_ENABLED=0 go test ./service/... -count=1`, focused repository/CLI/application race tests, `openspec validate --all --strict`, and `git diff --check`.
- [x] 3.4 Owner: implementer；Lane: B/V；Dependencies: 3.1-3.3；Paths: service component/integration tests；Scope: 覆盖project/task/workflow/diagnostics至少5个Operation-backed commands的prepare→Task→event→artifact/receipt；Acceptance: permission/version/idempotency/partial/error与原Operation一致；Validation: `CGO_ENABLED=0 go test ./service/... -run 'CliCommand.*Operation' -count=1`；Expected: Slice A backend green；Failure re-check: synthetic/fixture Owner不能冒充real capability promotion。 **Evidence (2026-08-23):** `TestCliCommandDailyOperationsPreserveTaskSemantics` exercises the frozen Slice A Workbench-owned catalog through six real registered Operation identities: WorkItem read/mutation plus Inbox, Approval, Activity, and Delivery reads. Each path uses sealed Operation projection, immutable Prepare intent, SQLite reload/compiler, `SubmitPreparedCommand`, canonical Task events/receipts, Task-derived run listing, and result projection. The matrix proves successful, partial, and handler-error terminal states; handler error details/private paths are reduced to the existing safe Task summary. WorkItem mutation preserves the original permission/idempotency/event flags, remains blocked at `awaiting_permission` until the canonical Task gate is resolved, executes exactly once under double-click, returns the completed original Task after the observed version advances, and rejects a distinct stale prepared intent before Task creation. The daily mutation Operation keeps its existing open direct-API compatibility but now explicitly declares its primitive WorkItem action fields, allowing the CLI to expose a closed typed subset without accepting nested/arbitrary properties. This is component evidence for Workbench-owned Operations only: unavailable Daily mutations and the contract-gated Eikona Owner candidate remain unpromoted and were not replaced by a fixture Owner. Evidence passed: exact requested `CGO_ENABLED=0 go test ./service/... -run 'CliCommand.*Operation' -count=1`, complete `CGO_ENABLED=0 go test ./service/... -count=1`, focused race tests, strict OpenSpec validation, and `git diff --check`.

## 4. Host Runtime 与 `workbench.cli.host.run.v1`（Slice C）

- [x] 4.1 Owner: implementer；Lane: B/C；Dependencies: 1.2, 1.4, 2.1, 2.3, 0.3；Paths: `service/internal/registry/**`, `service/internal/adapters/clihost/**`；Scope: 注册 sealed `workbench.cli.host.run.v1`，声明owner mutation、safe input、permission/cost/idempotency/events/stream/cancel/reconcile和四投影；Acceptance: Task只存intent/context/limits，lifecycle handler完整；Validation: registry seal tests + `bun run test:contract`；Expected:缺任一handler/schema/projection拒绝启动；Failure re-check: generic operation不得允许client command string。
  - **Evidence (2026-08-23):** 新增 sealed `workbench.cli.host.run.v1`（owner mutation、`PersistSafeInput`、permission/cost/idempotency、events/stream、cancel/reconcile、SDK/HTTP/gRPC/JSON-RPC），closed input 仅含 `inputRef`、Context Pack ref/revision 与 bounded limits；缺 handler/lifecycle/schema/projection 均 fail closed。`adapters/clihost` 只消费 typed protobuf provider request，dispatch 不确定进入 `unknown_accept`，reconcile 仅 lookup original request，cancel 先 lookup 且不把已终止 success 伪造成 cancelled。TaskService 仅为该 Operation 在带 CLI metadata 时持久化 safe envelope，重启后 gate/cancel/reconcile 可恢复；其他 CLI compiled arguments 仍不落 `ReplayInput`。验证：focused/race Go tests PASS；`CGO_ENABLED=0 go test ./service/... -count=1` PASS；CLI Host contract test PASS；strict OpenSpec PASS。`bun run test:contract` 三次均为 373/374，通过 CLI Host 全部断言；剩余失败位于既有 `tests/conformance/sdk-transports.test.ts` Agent read-ack/shared-state 时序（首次为 watcher 5s timeout，后两次为 unread ack），与 Host diff 无调用路径，保留为全局门禁待修。
- [x] 4.2 Owner: implementer；Lane: H；Dependencies: 1.4, 0.3；Paths: 独立 pure-Go Host Runtime package/cmd owned by approved design；Scope: 实现readiness/dispatch/lookup/cancel、hostSessionRef/request digest/receipt、structured JSON logs和graceful shutdown；Acceptance: loopback/private identity或provider auth明确，protocol stdout干净，`CGO_ENABLED=0`；Validation: focused Go component tests + startup/health smoke；Expected: start/ready/drain/shutdown证据完整；Failure re-check: 不在HTTP handler长时间执行process。
  - **Evidence (2026-08-23):** 新增纯 Go `service/internal/clihost` 与独立 `service/cmd/workbench-cli-host`。sidecar 提供 typed gRPC readiness/dispatch/lookup/cancel/watch，deterministic request digest、stable opaque `hostSessionRef`、revisioned receipt、bounded async runner goroutine、concurrency gate、deadline、drain/shutdown ownership；dispatch handler 只登记 run 后立即返回，不执行长进程。监听严格 loopback，所有 unary/stream RPC 需 constant-time bearer provider auth；正常 stdout 为空，生命周期/拒绝日志为 JSON stderr 且不记录 token。真实 TCP smoke 覆盖 unauthenticated deny→authenticated ready→dispatch→typed output stream；组件测试覆盖 exact replay、digest conflict、concurrency、cancel_requested≠cancelled、lookup、degraded/mismatch/draining、shutdown。验证：focused `CGO_ENABLED=0` 与 race PASS，`go vet` PASS，no-shell string scan PASS，新增 command 包含在全量 `CGO_ENABLED=0 go test ./service/... -count=1` PASS，strict OpenSpec PASS。默认 command runner 为 degraded `UnavailableRunner`，直到 4.3/4.6 binding 显式注入，不伪造 executable readiness。
- [x] 4.3 Owner: implementer；Lane: H；Dependencies: 4.2, 2.1；Paths: Host binding/argv encoder/process runner；Scope: 以`exec.CommandContext(executable, argv...)`执行allowlisted binary/fixed prefix/typed args，拒绝shell、client executable/cwd/env、control path；Acceptance: shell metacharacter保持一个argv、无expansion/pipe/redirection/substitution；Validation: testscript/component no-shell fixture；Expected: argv golden与negative injection tests通过；Failure re-check: 搜索`sh -c`, `bash -c`, `cmd /C`, PowerShell expression和string command APIs。
  - **Evidence (2026-08-23):** 新增 server-private binding catalog/digest、固定 executable/argv prefix/cwd、ordered typed argument specs、opaque value resolver 与 argv encoder；catalog 拒绝相对 executable、shell binaries、`-c`/`/C`/PowerShell control args、duplicate/unknown arguments 和 digest drift。`ProcessRunner` 唯一构造点为 `exec.CommandContext(ctx, binding.Executable, argv...)`，设置 binding-owned cwd、空 env、nil stdin，无 shell/PTY/string command/client path。raw stdout/stderr 只能进入强制 `OutputProjector`，runner 不直接投影。真实 Go helper component fixture 传入含 `;`、pipe、`$()`、redirection 与 glob 的值，golden 证明其保持单一 argv，零 expansion/side-effect file，且 inherited env sentinel 为空。focused CGO0/race tests PASS；source scan 仅命中上述 `exec.CommandContext`，未命中 shell/string command API。
- [x] 4.4 Owner: implementer；Lane: H；Dependencies: 4.2, 4.3；Paths: Host workspace/env/secret/network/resource policy modules；Scope: 解析opaque mount/artifact/secret refs，应用allowlist env、network、duration/output/CPU/memory/process/concurrency ceilings；Acceptance: client不能放宽policy，secret never returned，timeout终止process group；Validation: policy/timeout/output/secret sentinel integration tests；Expected:无orphan/leak/secret exposure；Failure re-check: Host inherited env和absolute path必须测试。
  - **Evidence (2026-08-23):** 新增 binding-owned `BindingPolicy`、opaque workspace/artifact mount resolver、protected-value resolver、closed secret→env allowlist 与 policy digest；workspace cwd 必须位于 binding root，客户端只能提交 safe refs，重复/未知 mount 或 secret、绝对路径逃逸、policy/digest drift、resource/network 放宽全部 fail closed。子进程 `Env` 使用显式 non-nil allowlist（空 allowlist 不继承 Host 环境），secret 只在启动边界解析，并在任何 projector/event 前同时脱敏 stdout/stderr；sentinel integration 证明批准 secret 被替换两次为 `[REDACTED]`，父进程 env 未继承。资源策略把 request 限制收紧到 binding ceiling，Runtime 同时执行 duration/event/output/concurrency 限制；ProcessRunner 对 stdout+stderr 使用一个累计 byte budget，Linux controller 对 CPU/memory/process 使用 `prlimit`，deny/loopback policy 进入独立 network namespace，allowlisted network 未实现时 fail closed。`exec.CommandContext` 进程组 cancel/cleanup 的真实 helper fixture 启动子进程，policy deadline 后 receipt=`cancelled` 且 child PID 消失，无 orphan；combined output overflow 在 projector 前失败。验证：policy/mount/network/output/secret/timeout component tests PASS，`CGO_ENABLED=1 go test -race ./service/internal/clihost -count=1` PASS，Host `go vet` PASS，完整 `CGO_ENABLED=0 go test ./service/... -count=1` PASS，strict OpenSpec 与 diff check PASS。
- [x] 4.5 Owner: implementer；Lane: H/B；Dependencies: 4.2-4.4, 4.1；Paths: Host cancel/lookup/reconcile adapter；Scope: 实现cancel acknowledgement、process exit race、dispatch/cancel unknown accept、original request lookup和no-replay reconcile；Acceptance: cancel_requested不伪造cancelled，reconcile不spawn第二进程；Validation: `CGO_ENABLED=1 go test -race ./service/... -run 'CliHost.*Cancel|CliHost.*Reconcile' -count=1`；Expected: crash/timeout/race矩阵收敛；Failure re-check: timeout不是worker已停止证据，检查process ownership。
  - **Evidence (2026-08-23):** Host Runtime 新增 private durable receipt journal：production server 强制绝对私有 `receipt-directory`，以 0700 directory/0600 atomic+fsync journal 持久化原 request digest、revisioned receipt、已脱敏 frames 与 cancel replay；损坏/越权权限 journal 启动即 fail closed。dispatch 在启动进程前先持久化 accepted，running/output/cancel_requested/terminal 逐次持久化；provider restart 会把遗留非终态 receipt 收敛为 terminal `unknown`，lookup/exact dispatch replay 仍返回同一原 request，runner call=0，不会 spawn 第二进程。cancel 使用 deterministic request digest + idempotency key 持久化原 ack；终态后 exact replay 仍返回原 `cancel_requested` acknowledgement，changed request 同 key 冲突。Runtime 锁内串行化 process-exit/cancel race：先退出返回 `already_terminal` 原 truth，先 cancel 只记录 `cancel_requested`，直到 process runner 实际退出才写 `cancelled`；Linux child 增加 `Pdeathsig=SIGKILL`。Workbench adapter 对 dispatch/cancel transport unknown 或 retryable revision race 只 lookup original request/digest，不重发 dispatch/cancel；reconcile 可收敛 succeeded/failed/cancelled，terminal-before-cancel 保留 owner receipt 且不伪造成 cancelled。验证：指定 `CGO_ENABLED=1 go test -race ./service/... -run 'CliHost.*Cancel|CliHost.*Reconcile' -count=1` PASS；完整 Host/adapter race PASS；完整 `CGO_ENABLED=0 go test ./service/... -count=1` PASS；Host `go vet`、strict OpenSpec 与 diff check PASS。
- [x] 4.6 Owner: implementer；Lane: H/V；Dependencies: 0.2, 4.1-4.5, 5.4；Paths: canary bindings/config/tests only；Scope: 接入2–3个现有WorkBench structured CLI canary，默认capability flag关闭；Acceptance: binding/executable/output digest pin、rollback、kill switch、non-production first；Validation: canary component/system evidence command；Expected:真实argv/event/result/receipt；Failure re-check: human-only output或copy fallback不得晋级。 **Evidence (2026-08-23):** 新增 default-off `CanaryConfig` 与 sidecar 启动接线，闭合启用 `workbench-config-check`、`workbench-diagnostics runtime validate`、`workbench-readiness` 三个 non-streaming `--json` 只读 binding；仅允许 `integration|canary`，无 dynamic argument/protected env，单进程/单并发、15s/1MiB/64-event、512MiB、loopback-only ceiling。每个 installed regular executable 的 SHA-256 同时作为 revision 与 executable pin 纳入 binding/catalog digest，readiness 和每次 dispatch 前复核内容，symlink、替换、显式 catalog mismatch 均 fail closed；output digest 绑定 command ID、public output-contract version、protocol/spec version。`WORKBENCH_CLI_HOST_CANARY_ENABLED=false` 为默认 kill switch，SIGTERM drain + durable receipt 保留 lookup/reconcile；`details/host-canary-runbook.md` 记录非生产启用、自动/显式 digest、回滚与真实 binary 命令。组件测试覆盖 disabled/production/symlink/digest drift；实际 CGO0 构建三个 binary 后，environment-gated system test 逐个经 `ProcessRunner`+`Runtime` dispatch，验证真实 fixed argv、typed frames、per-frame digest、terminal cursor/resultRef 与 terminal receipt，三 canary PASS。focused race/vet、完整 `CGO_ENABLED=0 go test ./service/... -count=1`、strict OpenSpec 与 diff check PASS。

## 5. Structured Output、Artifacts 与 CLI Contract 收敛

- [x] 5.1 Owner: implementer；Lane: B/H；Dependencies: 1.3, 1.4；Paths: CLI output parser/projector packages；Scope: 实现单JSON envelope parser与NDJSON start/sequence/terminal parser，生成canonical result；Acceptance: malformed/duplicate/out-of-order/missing terminal fail closed，不从human/stderr猜status；Validation: table/fuzz/golden tests；Expected: parser稳定错误与bounded diagnostics；Failure re-check:未知event不得映射success。 **Evidence (2026-08-23):** 新增 `service/internal/cli/output` canonical parser：单 JSON envelope 对 duplicate/unknown/尾随/human/control/nested fact/超限输入 fail closed；NDJSON 强制 start、从 1 连续 sequence、唯一且末位 `end|error`，拒绝重复 fact、乱序、缺 terminal、terminal 后事件与未知 event，不读取 stderr 或 human 文本猜 status。facts 排序、data object/depth/size、safe refs、stable error、canonical JSON 与 digest 均封闭。JSON/events golden、负向表测与两组 fuzz target 通过（本轮 fuzz 分别约 389k/120k executions）；focused CGO0/race、完整 `CGO_ENABLED=0 go test ./service/... -count=1` 与 strict OpenSpec PASS。
- [x] 5.2 Owner: implementer；Lane: B/H；Dependencies: 5.1；Paths: output redaction/truncation/artifact writer；Scope: 双层redaction、ANSI/control stripping、line/event/byte limits、live tail和Task Artifact spill；Acceptance: outputTruncated显式，artifact有size/checksum/evidence ref，无Host path；Validation: secret sentinel/oversize/control-sequence tests；Expected: API/log/evidence零泄漏；Failure re-check: redactor failure必须fail closed。 **Evidence (2026-08-23):** `Sanitizer` 强制 Host→Workbench 双层 redactor 全部成功后才剥离 ANSI CSI/OSC/control、修复 UTF-8 并计算有界 line/byte live tail；任一 redactor、limit、writer 或回执 metadata 失败均返回空结果。截断时完整“已双层脱敏”内容写 Artifact，校验 safe ref、kind、size、sha256 与 evidence ref，合同不包含 Host path，并显式返回 `OutputTruncated`。secret sentinel、private path、control、oversize、spill/checksum/evidence 与 fail-closed 矩阵在 CGO0/race focused tests 通过；完整 pure-Go service 与 diff check PASS。
- [x] 5.3 Owner: implementer；Lane: C/B；Dependencies: 5.1；Paths: shared CLI projection/renderer package（按现有语言边界最小复用）；Scope: 统一human、`--agent`、`--json`、`--events`、`--explain`字段、status和stdout/stderr contract；Acceptance:同projection多renderer，agent必含4个header keys，events终止语义完整；Validation: golden tests across representative Go/TS CLIs；Expected:无语义漂移；Failure re-check: 不强制一次性重写所有CLI。 **Evidence (2026-08-23):** 新增 Go `service/internal/cli/render` 与 TypeScript `scripts/lib/cli-output.ts`，二者从同一 typed projection 渲染 summary/agent/json/events/explain；agent 固定前四行 `spec_version/mode/command/status`，events 连续 sequence 且仅一个 `end|error` terminal，JSON 输出回送 canonical parser 自证。共享 `tests/fixtures/cli-output` fixture 与五份 golden 由 Go/Bun 双侧逐字校验，并比较 machine modes 的 command/status/summary/facts/terminal 语义；失败状态、control、NaN、伪 checksum、重复/尾随 data 均拒绝。Focused Go CGO0/race/vet、Bun 4/4 与 standalone strict TypeScript compile PASS；未要求一次重写所有 CLI。
- [x] 5.4 Owner: implementer；Lane: C/H；Dependencies: 5.3, 0.2；Paths: selected `service/cmd/**`, `scripts/production-build.ts` canaries；Scope: 为首批Host canary补齐/对齐`--events`或声明non-streaming`--json`，增加help/golden/stderr tests；Acceptance: protocol stdout只有机器输出，diagnostics走stderr；Validation: focused Go/Bun CLI tests；Expected: canary满足descriptor output contract；Failure re-check: 不以多次执行拼接不同view。 **Evidence (2026-08-23):** `workbench-config-check`、`workbench-readiness`、`workbench-diagnostics` 全部改为消费共享 Go renderer，明确首批 Host binding 为 non-streaming `--json`；readiness 内部 `ready|blocked` 只用于 gate 评估，对外 machine status 统一为 `success|failed`，gates/runs 下沉 `data`。diagnostics 的 Host 候选仅限 validate 只读子命令，generate/capture 虽复用输出但不晋级；action 只发布 ID，不输出 command preview。三 CLI 均拒绝冲突 output flags，machine/explain projection 走 stdout、flag/renderer diagnostics 走 stderr，help 明示 non-streaming，并各有 canonical JSON golden、failure channel 与 parser round-trip tests。`scripts/production-build.ts` 作为 TS 代表接入共享 renderer，保留完整 NDJSON events，evidence path 转 opaque ref，冲突 flag 与 stdout/stderr 有 subprocess tests，但仍为 Host `reject`。Focused Go CGO0/race/vet、Bun **11/11**、standalone strict TS、完整 `CGO_ENABLED=0 go test ./service/... -count=1`、diff check 与 strict OpenSpec 全通过。
- [x] 5.5 Owner: implementer；Lane: B/V；Dependencies: 5.1-5.4；Paths: result projector + artifact/receipt integration tests；Scope: 验证Summary/Facts/JSON/Events/Explain语义一致、partial/failed/exit disposition和artifact spill；Acceptance:切view无需重跑，Agent只收safe facts；Validation: focused component/integration tests；Expected: canonical digests/terminal status一致；Failure re-check: stderr或exit code冲突按声明contract处理并留evidence。 **Evidence (2026-08-23):** 新增 `cli/result` canonical projector、Host frame assembler 与 durable projection writer contract：Host structured projector 先按 pinned binding revision/digest、output digest、command/protocol 解析真实 process stdout，将 malformed/command drift/exit 冲突收敛为零 facts 的 `output_contract_invalid` canonical failure，并输出 protocol/diagnostic 分离、逐帧 SHA-256、唯一 terminal/resultRef；Workbench 在任何 artifact 写入前校验 frame sequence/cursor/session/content digest、receipt disposition/resultRef、terminal status 与 source canonical digest，再执行双层 redaction、artifact spill 和最终 canonical projection 持久化。TaskService 仅接受 closed kind/safe ref/安全摘要并原子保存 artifact refs，查询层从 reopened canonical Task + external projection 重建 facts/actions/evidence/artifacts/explain/details，Task status/event/receipt 仍只取 repository。组件覆盖五种 view 同源且切换零重跑、Agent 输出无 diagnostics/secret、success+stderr、partial/failed/signal/nonzero、malformed、exit conflict、truncation/checksum/evidence、redactor/store fail-closed、Host digest/terminal drift；focused CGO0/race、`go vet`、完整 `CGO_ENABLED=0 go test ./service/... -count=1` 与 strict OpenSpec PASS。

## 6. Four Transport、SDK 与 BFF

- [x] 6.1 Owner: implementer；Lane: C/B；Dependencies: 1.1-1.5, 2.3, 2.4；Paths: HTTP/gRPC/JSON-RPC CLI transport mappings；Scope: 绑定catalog/get/prepare/get-intent/list-runs/get-result到同一services；Acceptance: transport只auth/decode/map/encode，不拥有command逻辑；Validation: focused transport tests；Expected: schema/error/revision/page parity；Failure re-check: 搜索transport-specific descriptor tables或execution。 **Evidence (2026-08-23):** 新增共享 `clicodec` 与 HTTP/gRPC/JSON-RPC 六方法映射，全部调用同一个 `cliservice.Service`；runtime catalog 只从 sealed Operation registry 投影，未新增 transport descriptor 表或 execution handler。HTTP 严格拒绝未知 query/body，prepare header 幂等键覆盖 body；gRPC/JSON-RPC 保持 closed CLI error、catalog revision/digest、nullable union 和分页游标语义。真实 SQLite + Context Pack + Task projection 的 `TestCLI*` 覆盖三 transport 全六方法，不是 mock-only handler；project-only intent lookup 在单次 tenant/workspace/project 绑定查询中完成。Validation：focused transport/service/runtime tests、focused `-race`、`go vet`、完整 `CGO_ENABLED=0 go test ./service/... -count=1`、strict OpenSpec 与 `git diff --check` 全通过。
- [x] 6.2 Owner: implementer；Lane: C；Dependencies: 1.5, 6.1；Paths: `packages/task-sdk/src/cli-client.ts`, `client.ts`, transport codecs/tests；Scope: 实现 `WorkbenchCliClient` 并组合现有`WorkbenchTaskClient`执行/watch/cancel/retry/reconcile/artifact/receipt；Acceptance: browser不接收local-session/Host token，不创建Pane专用SSE；Validation: `bun test packages/task-sdk && bun run typecheck && bun run test:contract`；Expected:client/codec/unknown union tests通过；Failure re-check: 不复制Task state machine到SDK。 **Evidence (2026-08-23):** `WorkbenchCliClient` 已公开六个严格校验的 catalog/descriptor/prepare/intent/runs/result 方法，并通过同一 `WorkbenchTaskClient` 委托 submit/get/list-events/watch/cancel/retry/reconcile/artifact/receipt；`watchTaskEvents` 是现有 Task stream 的兼容别名，无 Pane SSE。HTTP 六路由与 `workbench.cli.v1alpha1` JSON-RPC namespace 已接入 aggregate client；浏览器请求 DTO 只有 safe refs/typed args，不含 local-session、Host endpoint/token、executable、path 或 env。请求与响应均拒绝未知字段、未知 argument union、非法 nullable success/error 组合；CLI HTTP `safeSummary` 保持 stable error code，不降级为 internal。Validation：SDK 378/378、typecheck、contract 381/381、strict OpenSpec 与 diff check 全通过。
- [x] 6.3 Owner: implementer；Lane: C/W；Dependencies: 6.2；Paths: Web BFF/fetch facade/static backend config；Scope: 接入CLI read/prepare endpoints和Task methods，移除browser identity/path/env字段；Acceptance: only static Workbench backend, no owner/Host direct call；Validation: BFF component/security tests；Expected:auth/error/abort/reconnect正常；Failure re-check: browser bundle不得包含Host endpoint/credential。 **Evidence (2026-08-23):** browser facade 只允许当前页面 same-origin BFF，静态 Workbench backend 与 bearer credential 均由 Bun server context 注入；跨 origin、path-bearing origin 与 CLI mutation 被 fail-closed 拒绝，browser Authorization/cookie/dynamic owner URL 均不会转发或回显。CLI six-method client 与 canonical Task watcher 共用 aggregate SDK，abort signal 和 resume cursor 原样传递，不创建 Pane/Host endpoint。Validation：Web Vitest 123 files / 1035 tests、BFF/security Bun 53 tests、focused CLI facade/server tests和 `bun run typecheck` 全通过。
- [x] 6.4 Owner: implementer + test-engineer；Lane: V；Dependencies: 6.1-6.3 stable diff；Paths: conformance/evidence only for verifier；Scope: 同descriptor/intent/result跨SDK/HTTP/gRPC/JSON-RPC parity，执行继续走Task parity；Acceptance:catalog digest/revision/errors/idempotency/event cursor一致；Validation: `CGO_ENABLED=0 go test ./service/test/conformance -run 'CliCommand' -count=1` + SDK contract tests；Expected: four surfaces green；Failure re-check: mock-only transport不计通过。 **Evidence (2026-08-23):** 新增真实 SQLite、Context Pack、Task run/result projection 的 transport parity harness；同一 `cliservice.Service` 同时挂载 authenticated HTTP、bufconn gRPC 与 JSON-RPC，验证 catalog digest/revision/分页内容、descriptor、prepare idempotency replay、intent、run/result、stable error，以及 opaque cursor 在不同 transport 间可恢复且 group-bound cursor fail closed。执行仍由 canonical Task client/parity 覆盖，未建立 CLI execution path。Validation：3 个 `CliCommand` conformance 用例与 SDK CLI 13/13 focused contract tests 全通过。

## 7. CLI Pane、管理与响应式 UI（Slice A）

- [x] 7.1 Owner: implementer；Lane: W；Dependencies: 1.5, 1.6, 6.2；Paths: `apps/web/src/workbench/agent/agent-pane-{registry,catalog,manifest}.ts`, CLI Pane modules；Scope: 注册`cli: agent.cli.v1` closed params/runtime renderer/required capability/620px hint；Acceptance:同identity focus existing、missing contract disabled、limits/split不变；Validation: focused registry/manifest tests + `bun run typecheck`；Expected:unknown params/version fail closed；Failure re-check:无dynamic component/URL/shell字段。 **Evidence (2026-08-23):** `agent.cli.v1` 使用现有 singleton/session identity 与 closed safe-ref params，manifest 声明 `agent.cli.read`、620px desktop hint 和 static first-party renderer；palette 仅在 CLI browser transport 已注册且 runtime descriptor 提供真实 adapter 时启用，否则保留 disabled reason。renderer 不接收 dynamic component/URL/shell/path/env/credential，未知 params/version 继续 fail closed，原 3-pane default、4-pane hard limit 与 split depth 不变。Validation：registry/manifest/layout/availability 4 files、78/78 focused Vitest，`bun run typecheck` 与 i18n compose 全通过。
- [x] 7.2 Owner: implementer；Lane: W；Dependencies: 6.2, 7.1；Paths: CLI Pane query/controller hooks；Scope: 实现catalog/intent/run/result caches、selected run、draft dirty/stale、Task watcher和session/scope cleanup；Acceptance: server truth只在query cache，tab切换不重订阅/重跑，tenant switch清cache；Validation: focused Vitest；Expected:close/reopen/reconnect/stale tests通过；Failure re-check: 不把canonical output或secret ref写localStorage。 **Evidence (2026-08-23):** `CliPaneController` 统一 catalog/intent/run/result query cache、current draft 与 selected-run descriptor；Task watcher 用 canonical taskRef/cursor 恢复，scope/session cleanup 只清内存 query/session state，未写 localStorage。close/reopen、transient cursor recovery、stale draft、25 条分页保持 draft、提交只传 prepared intent ref、run descriptor 不随 draft 漂移等 8 个 controller 用例通过；tab 切换不创建额外 watcher 或重跑。
- [x] 7.3 Owner: implementer；Lane: W；Dependencies: 1.6, 7.2；Paths: CommandCatalogCombobox/TypedArgumentForm/ContextBindingStrip/preview；Scope: 实现schema-driven first-party fields、human labels、search/filter、copy-only server preview；Acceptance: no editable shell prompt/raw refs，field labels/errors/a11y完整；Validation: component/keyboard tests；Expected:valid/invalid/change-command/IME/detach cases通过；Failure re-check:copy preview永不作为Run input。 **Evidence (2026-08-23):** Command form 仅按 closed descriptor 渲染 text/number/bool/enum/reference/list first-party controls，catalog 支持搜索与键盘选择，Context Binding 可 detach，copy preview 仅复制 server-authored safe summary；DOM 中无 editable shell/argv/path/env/raw ref 字段。唯一 input IDs、labels、错误关联、IME/换命令/invalid shape/preview copy 组件用例通过，执行 DTO 只消费 prepared intent ref。
- [x] 7.4 Owner: implementer；Lane: W；Dependencies: 2.5, 7.2, 7.3；Paths: PreflightGateStrip/CliActionBar/dialogs；Scope: 映射descriptor/scope/permission/cost/version/runtime/output gates和Run/Review/Cancel/Retry/Reconcile CTA；Acceptance: shortcut不绕gate，stale intent禁Run，unknown只有reconcile；Validation: state matrix component tests；Expected:全部blocked/attention/ready状态准确；Failure re-check: disabled button必须有reason/recovery。 **Evidence (2026-08-23):** gate/action resolver 集中映射 descriptor、scope、permission、cost、expected-version、runtime、output contract 与 canonical Task state；Run 只提交 immutable prepared intent，confirmation 走显式 dialog，stale/permission/cost/unknown_accept/cancel-pending 均 fail closed。state-matrix tests 验证 blocked/attention/ready、快捷键不绕 gate、unknown_accept 仅 reconcile，以及所有 disabled CTA 的 localized reason/recovery。
- [x] 7.5 Owner: implementer；Lane: W；Dependencies: 5.5, 7.2；Paths: CliRunWorkspace/Summary/Console/Events/Artifacts/Explain/Details；Scope: 实现canonical result视图、cursor/follow-tail、virtualization、bounded raw disclosure和artifact actions；Acceptance:无fabricated percent，outputTruncated可见，raw需permission；Validation: component/performance tests；Expected:stream/partial/fail/truncate/cursor expiry通过；Failure re-check: large arrays不驻留hidden Pane state。 **Evidence (2026-08-23):** Summary/Facts/Events/Artifacts/Explain/Details/Console 全部读取 canonical result projection；events/facts 使用虚拟化窗口并通过 10k 项测试，cursor expiry 显示恢复动作，partial/fail/truncated 明示真实状态且无百分比推测。raw console 默认不取字节，只有 permission + safe artifact ref 时开放；artifact action只消费 bounded safe refs，hidden tab 不复制大数组。
- [x] 7.6 Owner: implementer；Lane: W；Dependencies: 2.4, 7.2, 7.5；Paths: run history popover/rail/drawer；Scope: 实现25条cursor分页、attention排序、selection与draft分离、shared context actions；Acceptance:选择历史不覆盖dirty draft，running Pane关闭后可重开；Validation: component + Playwright history flow；Expected:pagination/focus/context menu通过；Failure re-check: 不为每run创建Pane或独立SSE。 **Evidence (2026-08-23):** history 首批 25 条并按 opaque cursor 追加，selected run 与 dirty draft 两套状态分离，Escape 只关闭 history 并恢复 trigger focus；running Pane 关闭不触发 cancel，重开恢复 run/result/draft。Playwright 实测 25→26 分页、历史选择不覆盖 draft、close/reopen 和 canonical watcher 恢复；每个 run 未创建独立 Pane/SSE。
- [x] 7.7 Owner: implementer；Lane: W；Dependencies: 7.1-7.6；Paths: Pane chrome/layout/Sheet/styles/a11y tests；Scope: 实现desktop bounded width、tablet right Sheet、mobile full-screen Sheet、sticky actions、200% zoom、focus trap/restore、reduced motion；Acceptance:conversation最小宽度、单overlay、无page overflow、Escape不cancel；Validation: fixed viewport Playwright + Axe；Expected:keyboard-only核心路径和zero serious/critical violations；Failure re-check:不新增第二composer或full-page terminal shell。 **Evidence (2026-08-23):** desktop 使用 bounded 620px hint，900px tablet 为 right Sheet，720 CSS px 覆盖 1440px@200% 的 mobile full-screen Sheet；action bar sticky、单 overlay、Escape 分层关闭 history/Sheet 并恢复焦点，reduced-motion 下无额外运动。固定 viewport Playwright 验证 page overflow ≤1px、Escape 不 cancel、conversation 仍可用；CLI 范围 Axe serious/critical 为零。
- [x] 7.8 Owner: implementer；Lane: W；Dependencies: 7.1-7.7, project data workspace action contracts；Paths: project/Kanban/Todo/workflow context actions + CLI open intent；Scope: 从对象选择编译safe refs并打开/聚焦CLI Pane，统一Context Menu/Inspector/Command Palette/Agent action descriptor；Acceptance:对象drag不直接执行，scope/permission/version重新验证；Validation: component/Playwright cross-pane flow；Expected:同action各入口disabled reason一致；Failure re-check:不解析DOM drag text/raw IDs。 **Evidence (2026-08-23):** 新增单一 `compileCliOpenAction`/`CliOpenIntentContext`，Table inspector、Kanban/Todo context action、workflow definition/run 共用 `agent.cli.open` descriptor；只接受 safe object ref + positive version，Pane params 仍只有 runtime/scope/project closed refs，执行前由服务端重验 Context Pack/permission/version。四入口共享 palette authority/disabled reason，组件 18/18 与 registry/layout 67/67 focused tests 通过。跨 Pane Playwright 证明 Project→Kanban→Todo→Workflow 重复动作始终聚焦一个 CLI Pane、不中断 running Task、无 overflow 且 Axe serious/critical 为零；测试同时发现并修复 pane identity 未按 paneType 分域及窄 split Kanban 三列遮挡问题，未解析 drag text/DOM raw ID。

## 8. Agent Command Collaboration（Slice B）

- [x] 8.1 Owner: implementer；Lane: A/C；Dependencies: 1.1-1.3, existing Agent contract；Paths: Agent proto/schema/types/normalizers；Scope: additive定义`CommandSuggestionRef`/safe result refs和CLI open/focus/select presentation intents；Acceptance:只含safe refs/revision/summary，不含command string/raw args/credential/path；Validation: contract/normalizer tests；Expected:unknown intent fail closed且基础output仍显示；Failure re-check:不新增第二Agent event stream。 **Evidence (2026-08-23):** Agent Proto/JSON Schema/Go/SDK 增量定义 `CommandSuggestionRefV1`、`CommandResultRefV1`、closed CLI target 与 `open_cli|focus_cli|select_cli_run` intents；只投影 command/descriptor/intent/context/runtime/scope/project/task/artifact/receipt/evidence safe refs、revision、safe summary 与时间，不含 command string/raw args/credential/path/URL/raw output。Intent 与所有 nested target 改为 exact-field fail closed，非法 CLI intent 只被基础 `AgentOutput` 投影过滤，不影响同一 output 的安全内容；resolver 仅产生 closed CLI Pane request，CLI intent 永不 Follow Pi。验证：`buf generate && buf lint` PASS，根 typecheck PASS，SDK 合同/normalizer **11/11**、Web resolver **20/20**、CGO0 Go Agent package PASS；未新增 Agent event stream。
- [x] 8.2 Owner: implementer；Lane: A/B；Dependencies: 2.3, 2.5, 8.1；Paths: Agent adapter/command suggestion service；Scope: 实现discover/prepare/present/observe，强制显式Context Pack和descriptor agentPolicy；Acceptance:默认prepare-only，Agent不能继承browser auth或构造unknown command；Validation: focused service tests；Expected:expired context/descriptor/permission/unknown command矩阵通过；Failure re-check:Agent markdown不产生可执行button。 **Evidence (2026-08-23):** 新增 `cli/agentcollab` 单一 adapter：`discover` 复用 canonical catalog/cursor/auth 并隐藏 `agentPolicy=never`；`prepare` 强制 explicit Context Pack + current descriptor revision/policy，只调用 immutable intent prepare；`present` 重新读取 canonical intent 并逐项核对 workspace/session/runtime/scope/context/initiator/delegation 后才生成固定 `suggest` 的 open/focus/select intent；`observe` 只从 Task-derived result 投影 safe summary 与 result/artifact/receipt/evidence refs，显式排除 Console/raw-console artifact。adapter 的最小 command interface 不含 submit/execute；所有调用用 value-stripping context 隔离 browser principal/bearer，再注入有 expiry 的 server-authored `system-agent` 与 initiating principal/delegation。真实 SQLite/Context Pack 矩阵覆盖 browser credential 隔离、prepare-only、expired context、stale descriptor、permission revoke、`agentPolicy=never`、unknown command、shell-like markdown、missing context、forged suggestion、terminal observe/unknown_accept；CGO0 CLI+Agent packages、focused race、`go vet`、strict OpenSpec 与 `git diff --check` PASS。
- [x] 8.3 Owner: implementer；Lane: A/B；Dependencies: 2.5, 3.2, proposal authority stable contract；Paths: ProposalAuthority CLI adapter/integration；Scope: Agent-origin bounded-write/dangerous intent创建canonical proposal，accept current revision后exactly-one Task；Acceptance:changed/expired intent/expected version冲突不执行；Validation: proposal/task integration tests；Expected:prepare→review→accept/reject/request changes闭环；Failure re-check:generic Task submit不能绕proposal。 **Evidence (2026-08-23):** 新增 `cli/proposaladapter`：Agent collaboration 复用 isolated system-agent context 创建 proposal；creator 精确加载 Agent-origin immutable intent、执行 current CLI gates（必须为 `proposal` path）、通过原 Operation compiler 生成 sealed target，并把 intent digest 与唯一 owner expected-version 写为 basis。Proposal accept claim 后 `SafetyPort` 装饰现有 ProposalAuthority safety，重新加载 intent、descriptor、Context Pack、resource、owner version并再次编译/逐字节核对 target；changed/expired intent、descriptor drift、owner version conflict 均以 safety deny 结束且不 dispatch。accept 继续经既有 sealed `orbit.proposal.accept` bridge/TaskService；generic `SubmitPreparedCommand` 对 Agent write 保持 permission deny，不能绕 proposal。危险命令 registry policy 调整为允许 `prepare_only`/`never`、明确拒绝 `delegated_read`。真实 SQLite + ProposalAuthority + TaskService integration 证明 prepare→proposal 时零 Task、accept current revision 恰好一个 canonical Task、幂等 accept 返回同 Task、gate 后目标 Operation 仅执行一次；reject/request_changes 均零 Task。CGO0 CLI+ProposalAuthority 全包、focused race、`go vet`、strict OpenSpec 与 `git diff --check` PASS。
- [x] 8.4 Owner: implementer；Lane: A/B；Dependencies: 2.5, existing permission Gate decision, 8.2；Paths: delegated read grant/receipt implementation or approved Gate reuse；Scope: 实现session/scope/command/expiry/resource-bound grant与system-agent actor审计；Acceptance:默认无grant，scope/expiry/revoke即时deny，only read/delegated_read；Validation: permission/concurrency/revoke tests；Expected:zero write auto-execution；Failure re-check:若现有Gate receipt不能表达边界，先更新设计/contract再新增entity。 **Evidence (2026-08-24):** 先在 design 冻结边界：现有 Task `Gate` 不能表达 session/command allowlist/expiry/resource/concurrency/revoke/version，故新增独立短期 `AgentExecutionGrantV1`，未配置 canonical approval verifier 时禁止签发。schema migration `0032_cli_agent_execution_grants`、lifecycle inventory class 与 GORM repository 绑定 approval/receipt、tenant/workspace/project/session/runtime/scope、initiating principal、command IDs/groups、resource ceiling、max concurrency、DB expiry 和 versioned revoke receipt；进一步新增 `AgentExecutionGrantAdmissionV1` 作为数据库原子准入点，对 grant row 加锁并在同一事务重验 current grant/descriptor/resource、统计 active Task + live reservation、为 immutable intent 建唯一短 lease reservation。revoke 共享同一 authority-row 锁；不同 intent 的 `maxConcurrentRuns=1` 并发提交只接受一个，相同 intent 幂等；Task 确定失败 release，成功绑定 Task ref，crash 前 reservation 自动过期。`cli/grant` 仅接受 verifier 确认的 approved facts，任一非 `read + delegated_read + confirmation=none + ready` descriptor 均拒绝；lookup/lock/count/admission 缺失或失败均 fail closed 为 Prepare。真实 SQLite + Operation + TaskService integration 证明 delegated read 创建唯一 `system-agent` Task，intent 保留 initiator/delegation，grant 可追 approval receipt；revoke 后 admission 立即 `delegation_required`；write grant issuance 被拒绝。CGO0 repository/grant/execution 全包 PASS；CGO1 race 对 concurrent distinct intents、post-revoke admission、delegated Task 重复 10 次 PASS；生命周期目录与 migration required objects 覆盖 admission table/indexes。
- [x] 8.5 Owner: implementer；Lane: A/W；Dependencies: 8.1-8.4, 7.4-7.6；Paths: CommandSuggestionCard/CommandResultCard/Agent timeline integration；Scope: 展示origin/basis/scope/safe args/risk和Open/Run/Review/result actions；Acceptance:presentation suggestion需用户激活，Agent不夺焦点/替换Pane/dirty draft；Validation: component/Playwright Agent flows；Expected:user proposal/delegated read/result-as-context通过；Failure re-check:raw output不进入conversation。 **Evidence (2026-08-24):** Agent turn event reducer 增量严格归一 `CommandSuggestionRefV1`/`CommandResultRefV1`，read suggestion 投影为 finding，write/dangerous 只有携带 canonical proposalRef 才投影 proposal；卡片展示 Agent origin、basis/context、project/scope、safe argument summaries、effect/risk、descriptor revision，并分别提供显式 Open/Run、Review 与 result Open/Evidence/Use-as-context。CLI presentation intent 永不自动 Follow，用户点击后 resolver 重验 session/runtime/scope/project/source refs并由 controller canonical `GetIntent` 重读；同 document identity 只聚焦一个 Pane，activation 不覆盖 dirty CLI draft，Agent composer draft 也保持不变。read Run 只提交 immutable intent ref 并由服务端再验；write suggestion 无直接 Run。result-as-context 仅加入 Task safe projection，raw stdout sentinel 不进入 conversation。验证：SDK reducer 9/9、Web resolver 20/20、CLI controller 9/9、Pane manifest 23/23、Agent workspace 90/90、根 typecheck PASS；Chromium Playwright `Agent CLI cards require user activation...` 1/1 PASS，Axe serious/critical 为零并验证 canonical intent read、单 Pane、两类 dirty draft、显式 submit 与 raw sentinel 排除。
- [x] 8.6 Owner: implementer + test-engineer；Lane: A/V；Dependencies: 8.2-8.5 stable diff；Paths: Agent component/e2e/evidence；Scope: 覆盖prepare-only、grant read、write proposal、grant expiry、permission revoke、result observe、unknown_accept建议；Acceptance:每条链有Task/proposal/receipt/audit refs且无ambient credential；Validation: Agent CLI integration/e2e entrypoint；Expected:redacted evidence通过；Failure re-check:reference Agent/fixture结果不冒充production owner。
  - **Evidence (2026-08-24):** `test:cli-agent:integration` 的脱敏证据 `temp/integration-test-runs/20260824015910-0f9d815d-e343-4157-b5c2-e747644541ff` 为 passed/exit 0/redaction true；真实 SQLite + canonical Task/ProposalAuthority/receipt/audit integration 覆盖 prepare-only、delegated-read grant、grant expiry/revoke、write proposal accept、result observe 与 unknown_accept 建议，browser credential 由 server-side Agent authority 隔离。最新 CLI Playwright 证据 `20260824030504-11b334db-bf3c-4744-aaed-d57a02d244d5` 验证用户显式激活、单 Pane、dirty draft 保留、raw sentinel 排除与 Axe serious/critical=0；fixture 只承担 presentation，不冒充 production Owner。

## 9. 安全、审计、可观测性与性能

- [x] 9.1 Owner: implementer；Lane: B/H；Dependencies: 2.5, 3.2, 4.4, 8.4；Paths: CLI audit/policy/logging modules；Scope: 记录actor/initiating principal/grant/scope/command revision/effect/risk/intent digest/decision/Task/Attempt/receipt，分离audit/TraceEvent/log/stdout/artifact；Acceptance: privileged/denied/cancel/reconcile均可审计且不含raw args/secret；Validation: audit/log redaction tests；Expected:correlation fields完整；Failure re-check: UI activity不是audit记录。 **Evidence (2026-08-24):** 新增独立 `workbench.cli.audit.v1` closed audit channel 与 Memory/Slog recorder；字段只允许 actor/initiator/grant、tenant/workspace/project/session/runtime/scope、command+revision、effect/risk、intent digest、decision、proposal/Task/Attempt/receipt/call refs 和 closed reason code，类型中没有 arguments/payload/path/env/stdout/stderr/credential/artifact。Task submit/delegated submit/prepare-only/proposal-required/deny 均审计；Agent write 创建 canonical proposal 时写 proposal audit；CLI Host cancel/reconcile 在 provider 调用前按 Task 的 immutable intent/scope 重新解析审计上下文，intent 过期后仍仅可读取安全 audit projection，descriptor/scope/Task drift 或 audit context 缺失会在 provider 前 fail closed。cancel/reconcile 写 Task+Attempt+receipt+call refs，unknown/reject 使用 closed reason；Task 已接受后 audit sink 失败不会伪造“未接受”。聚焦 CGO0 tests 覆盖 audit JSON 字段、unsafe ref/reason/missing grant、raw input/secret sentinel 排除、expired intent audit lookup、proposal audit、Task submit/deny、Host cancel/reconcile 与 audit-context fail-closed，五个相关 package 全部 PASS。
- [x] 9.2 Owner: implementer；Lane: H/B；Dependencies: 4.2-4.5, 5.2；Paths: Host/adapter diagnostics/metrics/health；Scope: JSON logs、stderr/file sink、rotation、call IDs、latency/errors/retries/queue/concurrency/output bytes/orphan metrics和dependency readiness；Acceptance:stdio/protocol stdout干净，secret/target sanitized，health不等于ready；Validation: startup/call/rotation/health smoke tests；Expected:故障可由trace/task/call refs定位；Failure re-check:不写unbounded log file。 **Evidence (2026-08-24):** CLI Host gRPC server 为每个 unary/stream 生成低熵 `call_clihost:*` ref，并只记录 method、request/session correlation ref、duration、closed outcome/code、retryable、active/queue/output/orphan counters；不记录 token、binding/argv、workspace/project、private path或输出内容。新增 bounded in-process metrics：calls/errors/retryable/replays、queue rejection/depth、active/max concurrency、output bytes/events、restored orphan、fixed-bucket p50/p95，以及 receipt-store/binding-runtime/drain readiness；restart 将失去 process ownership 的 run 计为 orphan 并保持 unknown，不重放。注册标准 gRPC health：进程存活时可 SERVING，而 typed readiness 可独立 degraded；drain 把 CLI service health 置 NOT_SERVING。正常 JSON logs 始终写 stderr、stdout 为空；可选 absolute-private `0600` rotating file sink 只保留一个 bounded backup（64KiB–100MiB，默认 5MiB），不记录 file path。startup/call/rotation/health/metrics/redaction smoke tests与 Host/adapter package CGO0 全测 PASS；聚焦 `-race` PASS。
- [x] 9.3 Owner: security-reviewer；Lane: V；Dependencies: 6.4, 8.6, 9.1, 9.2 stable diff；Paths: read-only review/evidence；Scope: shell/path/env/secret injection、cross-tenant refs、browser identity spoof、Agent grant escalation、proposal bypass、output/log/evidence leakage、supply chain；Acceptance:critical/high为零或阻断promotion；Validation: diff + threat matrix + negative tests；Expected:finding refs/mitigations明确；Failure re-check:quoting函数不等于no-shell guarantee。 **Evidence (2026-08-24):** daily diff threat review覆盖 21 个受保护 Workbench CLI transport surfaces、5 个 loopback bearer Host methods、Host process/receipt background boundary 与 container config。首轮自审计在 `.gstack/security-reports/2026-08-24-011918.json` 记录 HIGH `cbea15...`：grant active-count 与 Task acceptance 非原子，可并发越过 max concurrency/revoke；promotion 当场阻断。修复新增数据库 authority-row lock + `AgentExecutionGrantAdmissionV1` reservation，并以 CGO0 全包、CGO1 race `-count=10` 验证不同 intent 并发只接受一个、post-revoke 不准入、delegated Task 唯一；复审报告 `2026-08-24-013615.json` 为 critical/high/medium=0、resolved=2、persistent/new=0。代码路径确认唯一执行构造仍是固定 digest-pinned executable + argv 的 `exec.CommandContext`，shell executable/control args denylist；mount/env/secret 只由 server-private binding/resolver 提供，browser DTO 无 executable/cwd/env/actor/delegation authority；cross-scope intent/Task/audit query exact match，proposal accept 重验 current intent/descriptor/context/version，Host token constant-time compare，logs/audit/evidence 只含 closed safe refs且 sentinel negative tests PASS。`.gstack/` 已加入 `.gitignore`；无 gitleaks/secretlint 作为非阻断工具缺口记录。strict OpenSpec 与 diff check PASS。
- [x] 9.4 Owner: performance-reviewer + implementer；Lane: V；Dependencies: 2.4, 5.2, 7.5, 7.6 stable diff；Paths: benchmark/browser evidence only unless approved fix；Scope: catalog 1k descriptors、10k events、1MiB output、100 run history、200 watchers、Host concurrency的p50/p95/memory/allocations/backpressure；Acceptance:budgets和环境记录，no N+1/unbounded DOM/buffer/goroutine；Validation: Go benchmarks/pprof + browser performance scenario；Expected:baseline/comparison report；Failure re-check:warm cache/toy data不宣称production SLO。
  - **Evidence (2026-08-24):** `details/performance-baseline.md` 记录环境、预算、命令与 pprof 观察：1,000 descriptor seal median 14.79ms/7.45–8.00MiB/约14,046 alloc；1MiB sanitize median 10.25ms/98–107MB/s/9.47MiB/11 alloc；full-slot 200 次 backpressure p95 12.61–27.10µs；200 simultaneous gRPC watchers p95 9.34–21.19ms；10k events DOM <100，100-run history 有界且 terminal page 无额外 cursor。所有预算通过；报告明确 warm synthetic baseline 不是 production SLO，staging latency 仍归 10.6。
- [x] 9.5 Owner: test-engineer；Lane: V；Dependencies: 4.5, 8.4 stable diff；Paths: evidence only；Scope: race—double Run、cancel vs exit、reconcile vs late receipt、grant revoke vs dispatch、Host shutdown、slow consumer；Acceptance:one Task/process/owner effect、no race/leak/false terminal state；Validation: `CGO_ENABLED=1 go test -race ./service/... -count=1` + focused repeated tests；Expected:zero race/failure；Failure re-check:timeout不证明original goroutine/process停止。
  - **Evidence (2026-08-24):** 全包命令 `CGO_ENABLED=1 go test -race -timeout 30m ./service/... -count=1`（Taskfile `test:race:full`）exit 0；171 packages `ok`，`FAIL`/`DATA RACE` 均为 0。`service/internal/repository` 962.815s 完成，未超时。Focused 重复矩阵 `CGO_ENABLED=1 go test -race -timeout 20m -count=10` 覆盖 grant concurrent distinct intents / post-revoke admission、delegated Task、submit replay、cancel-vs-exit、late receipt reconcile、shutdown drain、200 watchers/backpressure、unknown_accept lookup，exit 0 且无 race/fail。

## 10. Integration Evidence、Fault Tests 与 Rollout

- [x] 10.1 Owner: implementer；Lane: V；Dependencies: 3.4, 5.5, 6.4, 8.6；Paths: `scripts/test-evidence/**`, Taskfile/package scripts；Scope: 增加CLI contract/component/integration/E2E evidence entrypoints和summary/command/stdout/stderr/env/artifacts自动脱敏；Acceptance:失败也写六件套并保留原exit code，temp ignored；Validation:运行一次pass和intentional fail self-test；Expected:`yeisme.integration_test_evidence.v1`且redaction true；Failure re-check:不手写官方summary。
  - **Evidence (2026-08-24):** 新增 package/Taskfile CLI contract、component、Agent integration、Host fault、rollout、E2E 与 evidence-runner entrypoints。official pass bundles：contract `20260824015446-d7c9c8c9-64f9-4c01-bdb6-3d1830fd40af`、Agent integration `20260824015910-0f9d815d-e343-4157-b5c2-e747644541ff`、Host system `20260824020209-65609651-c557-44e9-8ef8-3ae59ec616b3`、rollout `20260824030900-9984b921-05f0-4a03-99ac-177073461f24`、CLI component `20260824030937-2a663e59-4fd5-40a9-a91b-3c07a6202f7a`、latest E2E `20260824030504-11b334db-bf3c-4744-aaed-d57a02d244d5`；均为 schema v1、exit 0、redaction true。`bun run test:cli-evidence-runner` 25/25 覆盖 intentional child failure，证明失败 bundle 仍写 summary/command/stdout/stderr/env/receipt/redaction/digest 并保留原 exit code；`temp/` 已忽略。
- [x] 10.2 Owner: test-engineer；Lane: V；Dependencies: 4.6, 10.1 stable diff；Paths: system/fault evidence only；Scope: Host offline/429/5xx、dispatch ack loss、cancel ack loss、process crash、malformed NDJSON、output flood、secret sentinel、kill switch、restart/reconcile；Acceptance:no duplicate process/owner mutation、no secret leak、truthful partial/unknown/cancel；Validation: CLI Host fault scenario command；Expected:每故障点有receipt/event/evidence；Failure re-check:mock Host不能替代真实process/receipt canary。
  - **Evidence (2026-08-24):** `test:cli-host-fault:system` bundle `temp/integration-test-runs/20260824020209-65609651-c557-44e9-8ef8-3ae59ec616b3` passed/exit 0/redaction true。Task 先以 CGO0 构建 config-check/diagnostics/readiness 三个真实 binary，再经 ProcessRunner/Runtime 执行；同一 fault matrix 覆盖 offline/429/5xx、dispatch/cancel ack loss、crash、malformed NDJSON、output flood、secret sentinel、kill switch、restart/reconcile，保持 one process/no replay、partial/unknown/cancel truthful 和 durable receipt。
- [x] 10.3 Owner: test-engineer；Lane: V；Dependencies: 7.7, 8.5, 10.1 stable diff；Paths: Playwright evidence only；Scope: desktop/tablet/mobile/200% zoom、keyboard、Axe、reduced motion、empty/ready/running/partial/unknown/truncated、Pane limit/replace/focus restore；Acceptance:critical/serious Axe零，固定截图稳定，Escape不cancel；Validation:项目Playwright integration entrypoint；Expected:screenshots/trace/report写入temp evidence；Failure re-check:test engineer不得修改tracked snapshots/source。
  - **Evidence (2026-08-24):** 最新 Playwright bundle `temp/integration-test-runs/20260824030504-11b334db-bf3c-4744-aaed-d57a02d244d5` passed/exit 0/redaction true，含 `agent-cli-collaboration.png` 与 `agent-cli-mobile-200-percent.png`。三条 Chromium journey 覆盖 unknown_accept 只 reconcile、不重试，desktop Agent activation/单 Pane/raw 排除，以及 tablet/mobile/200%-effective/reduced-motion/focus/Escape/no cancel；Axe serious/critical=0、横向 overflow≤1px。视觉复核发现并修复 desktop sticky action bar 覆盖 readiness P2；最终截图为两列可读 gate + 正常流动作区。
- [x] 10.4 Owner: implementer；Lane: C/W；Dependencies: 7.3, 0.1；Paths: `packages/task-sdk/src/eikona-independent-client.ts` 与其他 copy-only fallback consumers；Scope: 将客户端shell拼接限制为server-authored/redacted preview或Owner deep link，证明执行API不消费字符串；Acceptance:existing user-facing copy behavior兼容或有迁移文案，no browser runner；Validation:focused SDK/Web tests + source scan；Expected:preview-only contract明确；Failure re-check:不删除Owner确认的fallback而无替代。
  - **Evidence (2026-08-24):** `EikonaCliFallback` 从 browser-side flag/value 拼接迁移为 allowlisted `commandId + server-authored redactedPreview + copyOnly:true`；移除 POSIX quoting、flag parser 与 `.join(" ")`，Web fixture 仅模拟 Owner 已脱敏 projection。执行 submit codec exact-field 只接受 scope + immutable `intentRef`，negative test 显式拒绝 `redactedPreview`/command string/argv/cwd/env。focused SDK/Web tests、root typecheck 与 source scan 全通过；copy 文案兼容，未新增 browser runner。
- [x] 10.5 Owner: operations + implementer；Lane: V/H；Dependencies: 4.6, 9.2, 10.2；Paths: capability flags/readiness/kill-switch/runbook；Scope: 默认关闭Host execution，独立控制CLI Pane、Operation commands、Agent delegated read、Host canary，支持drain/read-only rollback；Acceptance:关闭Host后Operation-backed Pane仍可用，已发Task继续reconcile；Validation: readiness/flag/rollback component + staging smoke；Expected:capability isolation truthful；Failure re-check:browser env var不得授予capability。
  - **Evidence (2026-08-24):** 用户授权本机 loopback 作为后续部署环境，身份用 local-session mock。`TestRuntimeHostCanaryOffKeepsOperationPaneAndTaskReconcile` 证明 Host canary 默认关、`VITE_*` 不能授予能力、Operation catalog 仍有 ready 命令、已发 Task 的 reconcile 路由仍在。官方 local staging system 证据 `20260824071317-e1273699-336a-4c8f-b13e-b2a07e45b830` passed/exit 0/`redaction_result.passed=true`，environment=`staging`，layer=`system`。component 证据 `20260824052017-6a22ac2a-fd0f-4d65-b576-e7be1385bf3d` 仍有效。
- [x] 10.6 Owner: operations + test-engineer；Lane: V；Dependencies: 9.3-9.5, 10.2, 10.5；Paths: staging evidence only；Scope: internal tenant 24h canary，覆盖query/event/output/Host concurrency/grant expiry/kill switch/drain/rollback；Acceptance:无duplicate/leak/orphan/secret exposure，latency/error/output budgets达标；Validation: staging soak + rollback dry-run entrypoints；Expected:complete promotion evidence；Failure re-check:任一unknown data或auto retry阻断promotion。
  - **Evidence (2026-08-24):** 本机部署 soak 入口 `task cli:local-staging:test` / `task test:cli-local-staging:system`。`TestLocalCLIStagingSoakCoversQueryEventOutputKillSwitchDrainAndRollback` 覆盖 catalog query、Task get、events、receipts、Host-off、drain/shutdown、Operation kill-switch rollback 后仍可 reconcile。grant expiry/revoke 由 `TestDelegatedReadGrantCreatesSystemAgentTaskAndRevocationImmediatelyDenies` 覆盖；Host concurrency/drain 由 `TestCliHostDispatchRejectsDigestConflictConcurrencyAndDrain` 与 `TestCliHostShutdownDrainsAndWaitsForOwnedRun` 覆盖。观察窗口是压缩 TTL，不是墙钟 24h。官方证据 `20260824071317-e1273699-336a-4c8f-b13e-b2a07e45b830` passed/redaction true。

## 11. Final Verification and Closeout

- [x] 11.1 Owner: test-engineer；Lane: V；Dependencies: all implementation tasks stable；Paths: evidence only；Scope: 执行项目声明的strict/buf/pure-Go/race/Bun/build/contract/integration全门；Acceptance: release path`CGO_ENABLED=0`、race、四transport、Web、CLI output和evidence全部通过；Validation: `openspec validate --all --strict && buf lint && CGO_ENABLED=0 go test ./service/... && CGO_ENABLED=1 go test -race ./service/... && bun install && bun run typecheck && bun test && bun run build && bun run test:contract && bun run test:integration`；Expected:zero failure并记录run refs；Failure re-check:区分introduced/pre-existing/environment，修复后重跑完整门。
  - **Evidence (2026-08-24):** 本地声明门全部 exit 0：`openspec validate --all --strict` 37/37；`buf lint`；`CGO_ENABLED=0 go test ./service/... -count=1` 171 ok；`CGO_ENABLED=1 go test -race -timeout 30m ./service/... -count=1` 171 ok / 0 race / 0 fail；`bun install`；`bun run typecheck`；`bun test` 658 pass / 7 environment-gated skip / 0 fail；`bun run build`；`bun run test:contract` 386/386；`bun run test:integration` official bundle `temp/integration-test-runs/20260824032739-8d3d858d-3b23-4f33-a78a-e1c6ca20c6a2` passed/exit 0/`redaction_result.passed=true`。首次全量 `bun test` 因并行 race/CGO0 负载使 lifecycle inventory 5s timeout（introduced load flake）；隔离复跑 2/2 pass（2.99s），安静重跑全量 658/7/0。10.5/10.6 staging/soak 不在本项本地门清单内，仍保持 open。
- [x] 11.2 Owner: reviewer + security-reviewer；Lane: V；Dependencies: 11.1 stable diff；Paths: read-only diff/evidence review；Scope: Task authority、descriptor seal、intent safety、no-shell、permissions/grants/proposals、cancel/reconcile、redaction、Pane regression/a11y、test gaps；Acceptance:P0–P2与critical/high为零；Validation:spec trace matrix + stable diff/evidence inspection；Expected:review report与resolved finding refs；Failure re-check:不评审changing diff，不把unit test当real Host integration。
  - **Evidence (2026-08-24):** 11.1 本地门已绿后对稳定实现做只读复核：`SubmitPreparedCommand` 只经 `TaskService.SubmitTask`；descriptor/kill-switch 同源投影；browser/SDK 只提交 immutable `intentRef`；Host 唯一进程构造为 `exec.CommandContext(ctx, binding.Executable, argv...)`；grant admission 先锁 grant 行再计数；cancel 在锁内区分 `cancel_requested`/`already_terminal`，reconcile/lookup 不重放进程；双层 redaction fail-closed；Pane 有 label/dialog/Escape 分层且 disabled CTA 带 reason。cso report `.gstack/security-reports/2026-08-24-013615.json` critical/high/medium=0。无未解决 P0–P2 / critical / high。staging/soak 仍归 10.5/10.6，不作为本项缺口。
- [x] 11.3 Owner: root integrator；Lane: V；Dependencies: 10.6, 11.1, 11.2；Paths: OpenSpec/tasks/evidence/closeout docs；Scope: 为每个requirement绑定test/environment/evidence/owner，确认ledger、owner-fit、known risks、flags、kill switches和rollback；Acceptance:deliver-now完成且Host retain-next状态诚实后才允许archive；Validation: `openspec status --change workbench-agent-cli-pane-v1 && openspec validate workbench-agent-cli-pane-v1 --strict`；Expected:apply/verify/archive ready；Failure re-check:不因任务多/预算耗尽标完成，任何real Host/soak/redaction缺口保持open。
  - **Evidence (2026-08-24):** 10.5/10.6 已用本机 loopback + local-session mock 身份关闭。主 spec 已同步：`openspec/specs/workbench-agent-cli-pane/spec.md`、`openspec/specs/workbench-cli-command-runtime/spec.md`，并更新 `workbench-agent-pane-composition`。Host 仍为 retain-next、default-off canary。`details/final-verification.md` 记录本地部署边界：environment=`staging` 表示本机部署证据，不是远程 HTTPS tenant。
