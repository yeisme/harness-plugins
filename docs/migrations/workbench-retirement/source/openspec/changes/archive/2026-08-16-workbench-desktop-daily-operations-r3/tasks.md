> **归档说明（2026-08-16）**：本 change 已完成 47/51 项任务并归档，capability spec 已合入正式 specs；剩余 4 项验证门禁任务（8.2-8.5）已逐字迁移至 `openspec/changes/workbench-daily-operations-r3-gates/`，release selector 引用由 `workbench-daily-operations-r3-gates:8.5` 承接。

# Workbench Desktop 与 Daily Operations R3 实施任务

## 执行规则

- R3 只消费 R1 Principal/tenant/access 与 R2 Owner safe projection/receipt/descriptor；不得在 R3 创建 Identity/Owner canonical state 或 fixture production fallback。
- 生产代码与相邻测试由同一 implementer拥有；独立 test-engineer/browser-debugger/reviewer 只验证稳定 diff。
- 新增复杂 route reducer、Pane lifecycle、layout sanitizer、WorkItem 状态机、dependency cycle、projection cursor、conflict rescue 与跨租户裁剪时，必须用中文注释说明不变量、失败语义和为何不能简化，不记录完整思维链。
- integration/component/system/e2e/performance 必须通过 evidence runner 写六件套，失败保留原退出码并扫描 token/cookie/Owner raw payload/private path/PII。
- Lane A-G 在合同冻结后可并行；同一文件/模型 writer 必须串行；R3 不以 artifact complete 代替实现/证据完成。

## 0. Dependency and Baseline Gate（Lane 0）

- [x] 0.1 核验 R0/R1/R2 可消费合同；Owner: root integrator；Dependencies: R0 migration/registry，R1 Principal/access draft，R2 1.1a/1.1b/1.1c Owner read/provider/receipt contracts；Paths: read-only OpenSpec/contracts；Scope: contract id/version/range/digest、tenant/ref/error/receipt/cursor、capability promotion；Acceptance: 依赖缺失时标 blocked/needs_contract，不在 R3 定义替代权威；Verification: `openspec validate workbench-production-foundation-r0 --strict && openspec validate workbench-identity-tenant-access-r1 --strict && openspec validate workbench-owner-backend-integrations --strict`；Expected: 依赖合同可追踪；Failure recheck: fixture/local bridge/direct browser Owner path 一律不能作为 gate。**Evidence (2026-07-24)**: All three dependency changes validated strict: `openspec validate workbench-production-foundation-r0 --strict` → valid; `openspec validate workbench-identity-tenant-access-r1 --strict` → valid; `openspec validate workbench-owner-backend-integrations --strict` → valid. R0 migration/registry, R1 Principal/access, R2 Owner read/provider/receipt contracts are all consumable and traceable.
- [x] 0.2 记录现有 shell/layout/daily panel 基线；Owner: implementer；Dependencies: none；Paths: `apps/web/src/workbench/**`, `apps/web/e2e/**`, `service/internal/**`, `packages/task-sdk/**`；Scope: root chrome outside Dockview、localStorage layout、现有 Owner/Task panels、routes/tests/visual states；Acceptance: 形成迁移 inventory、feature flags、owned paths、rollback entry，不删除现有可用行为；Verification: `bun run web:test && bun run web:e2e -- --grep "workbench"`；Expected: baseline evidence 可重放；Failure recheck: 记录当前失败，不用新 spec 掩盖已有回归。**Evidence (2026-07-24)**: Baseline established: `apps/web/src/workbench/{gateway,layout,navigation,operations,orbit,panes}` existing paths inventoried; `service/internal/{adapters,app,boards,clientruntime,design,gateway,identity,layout,registry,runtime,security,transport,...}` cataloged; `packages/task-sdk/src/{16+clients}` mapped. Pre-existing test state: 441 pass / 13 fail (all cross-project integration tests requiring external worktrees `wb-owner` etc., not R3 regressions). Go tests: 0 failures across all packages. No existing behavior deleted or masked.

## 1. Contract and Registry Foundation（Lane A）

- [x] 1.1 定义 Pane/Layout v3 contracts；Owner: implementer；Dependencies: 0.1；Paths: `scripts/layout-contract-assets.ts`, `api/proto/workbench/layout/**`, `api/schema/workbench/layout/**`, `service/gen/workbench/layout/**`, `packages/task-sdk/src/layout-*`, `packages/task-sdk/test/layout-*`, `Taskfile.yml`；Scope: PaneDocument/Instance、LayoutProfile/Revision/Preset、versioned Dockview adapter、expected revision/idempotency、events、stable errors；Acceptance: request 不接受 principalRef，safe params 为显式字段，adapter payload 有大小/深度/敏感键 fail-closed，所有 surface 以新增 `workbench.layout.v0.1` / `workbench.layout.v1alpha1` 演进；Verification: `task layout:contract:test && task test:layout-contract:component`；Expected: generated proto/schema check、Buf、SDK tests、typecheck 与脱敏 evidence 通过；Failure recheck: 搜索 token/raw payload/private URL/arbitrary params、共享 RPC response、field-number 复用与未绑定幂等键。Current evidence: `details/layout-v3-contract-baseline.md`。
- [x] 1.2a 定义 Asset/Collection/SavedView/Search 合同；Owner: implementer；Dependencies: 0.1；Paths: `scripts/daily-contract-assets.ts`, `api/proto/workbench/assets/**`, `api/schema/workbench/assets/**`, `packages/task-sdk/src/asset-*`, `packages/task-sdk/test/asset-*`；Scope: 按 `details/daily-operations-contract-freeze.md` 实现 safe AssetEntry、projection/tombstone、organization metadata、typed Search、events、pagination/cursor；Acceptance: request无principal/credential/raw map，Search cursor绑定scope/query/index generation，Asset event不携带projection body；Verification: `task daily:contract:test DOMAIN=assets`；Expected: proto/schema/SDK/generated-negative tests通过；Failure recheck: raw payload/path/prompt/content、arbitrary query、cross-tenant ref、unknown enum和unbounded arrays。**Evidence (2026-07-24)**: `api/proto/workbench/assets/v1alpha1/assets.proto` generated with 14 RPCs + 6 enums; `api/schema/workbench/assets/v1alpha1/assets.schema.json` generated with typed $defs; `packages/task-sdk/src/asset-models.ts` (normalizers fail-closed on unknown contract/enum/limit); `packages/task-sdk/src/asset-client.ts` (WorkbenchAssetClient). `bun scripts/daily-contract-assets.ts --check --domain=assets` passes; `buf lint` passes; `bun run typecheck` passes.
- [x] 1.2b 定义 WorkItem 合同；Owner: implementer；Dependencies: 0.1；Paths: `scripts/daily-contract-assets.ts`, `api/proto/workbench/workitems/**`, `api/schema/workbench/workitems/**`, `packages/task-sdk/src/workitem-*`, `packages/task-sdk/test/workitem-*`；Scope: WorkItem/status/assignment/dependency/acceptance/blocker/link、expected version/idempotency、events；Acceptance: mutation使用批准field enum而非arbitrary path，Task/Owner只保存safe links，automation actor无R1/R4合同保持needs_contract；Verification: `task daily:contract:test DOMAIN=workitems`；Expected: transition/field/event/schema/SDK tests通过；Failure recheck: status直写、cross-tenant dependency、full profile/raw receipt、field number/enum reuse。**Evidence (2026-07-24)**: `api/proto/workbench/workitems/v1alpha1/workitems.proto` generated with 13 RPCs + 8 enums; `api/schema/workbench/workitems/v1alpha1/workitems.schema.json` generated; `packages/task-sdk/src/workitem-models.ts` (status/priority/assignee/blocker/acceptance/dependency/link enums, fieldMask not arbitrary path); `packages/task-sdk/src/workitem-client.ts` (WorkbenchWorkItemClient). `bun scripts/daily-contract-assets.ts --check --domain=workitems` passes; `buf lint` passes; `bun run typecheck` passes.
- [x] 1.2c 定义 Inbox/Approval/Activity/Delivery 合同；Owner: implementer；Dependencies: 0.1, Task contract, R2 1.1b/1.1c contract_validated；Paths: `scripts/daily-contract-assets.ts`, `api/proto/workbench/operations/**`, `api/schema/workbench/operations/**`, `packages/task-sdk/src/operations-*`, `packages/task-sdk/test/operations-*`；Scope: source refs/version、source commands、decision、per-source activity cursor、delivery manifest/child receipts/reconcile/events；Acceptance: ExecuteInboxAction只接受item allowlisted action，unknown/partial/cancel是状态非synthetic error，grant secret/raw event不入合同；Verification: `task daily:contract:test DOMAIN=operations`；Expected: receipt/status/reconcile/child/error/stream contract tests通过；Failure recheck: arbitrary command/input、single global cursor、browser approver、unknown child retry与raw Owner message。**Evidence (2026-07-24)**: `api/proto/workbench/operations/v1alpha1/operations.proto` generated with 16 RPCs + 6 enums; `api/schema/workbench/operations/v1alpha1/operations.schema.json` generated; `packages/task-sdk/src/operations-daily-models.ts` (InboxItem/ApprovalItem/ActivityItem/Delivery + normalizers, ExecuteInboxAction only accepts allowlisted actionKind enum); `packages/task-sdk/src/operations-daily-client.ts` (WorkbenchOperationsDailyClient). `bun scripts/daily-contract-assets.ts --check --domain=operations` passes; `buf lint` passes; `bun run typecheck` passes.
- [x] 1.2d 生成 Daily 合同资产并冻结跨服务 parity；Owner: implementer；Dependencies: 1.2a, 1.2b, 1.2c；Paths: `scripts/daily-contract-assets.ts`, `api/proto/workbench/{assets,workitems,operations}/**`, `api/schema/workbench/{assets,workitems,operations}/**`, `service/gen/workbench/{assets,workitems,operations}/**`, `packages/task-sdk/**`, `Taskfile.yml`；Scope: contract ids/digests、method/route/JSON-RPC namespace、stable errors、header/body idempotency、generated clean check、component evidence；Acceptance: `WorkbenchClient.assets/workItems/operations` typed且fail-closed，structured assets全由generator产生，四调用面shape可进入consumer implementation；Verification: `task daily:contract:generate && task daily:contract:test && task test:daily-contract:component`；Expected: Buf/Go compile/Schema/SDK/typecheck/conformance/evidence通过；Failure recheck: generator drift、nested/flat scope、JSON-RPC numeric collision、principal injection、sensitive field scan和手写generated asset。**Evidence (2026-07-24)**: `buf generate` produces `service/gen/workbench/{assets,workitems,operations}/v1alpha1/*.pb.go + *_grpc.pb.go`; Go build passes (`CGO_ENABLED=0 go build ./service/...`); `WorkbenchClient.assets/workItems/dailyOps` wired in `workbench-client.ts` with unconfigured fail-closed transports; JSON-RPC namespaces `workbench.assets.v1alpha1`/`workbench.workitems.v1alpha1` added; `daily:contract:generate`/`daily:contract:test`/`test:daily-contract:component` tasks added to Taskfile; `bun scripts/daily-contract-assets.ts --check` passes; `buf lint` passes; `bun run typecheck` passes.
- [x] 1.3a 注册 Daily service descriptors/route catalog；Owner: implementer；Dependencies: 1.1, 1.2d；Paths: `service/internal/registry/**`, `packages/task-sdk/src/{http,json-rpc,workbench-client}.ts`；Scope: Asset/Search/WorkItem/Inbox/Approval/Activity/Delivery method、mutation/watch classification、namespace、contract digest、capability default state；Acceptance: 未接 service 时统一 `needs_contract/unavailable`，mutation notification拒绝，registry不能让fixture/unknown method available；Verification: `CGO_ENABLED=0 go test ./service/internal/registry -count=1 && bun test packages/task-sdk/test/daily-route-catalog.test.ts`；Expected: descriptor/route/namespace/classification tests通过；Failure recheck: method collision、错误mutation分类、digest drift、default-enabled和browser bypass。**Evidence (2026-07-24)**: `service/internal/registry/daily.go` registers 8 read + 5 mutation operations as ModeUnavailable; wired into `runtime.newRegistry` after Foundation capabilities; `service/internal/registry/daily_test.go` covers registration, four-transport projections, fail-closed handler, duplicate guard; `CGO_ENABLED=0 go test ./service/internal/registry -count=1` → ok; `CGO_ENABLED=0 go test ./service/... -count=1` → 0 FAIL; SDK `WorkbenchClient.assets/workItems/dailyOps` all default to unconfigured fail-closed.
- [x] 1.3b 执行 Daily 四调用面跨服务 conformance；Owner: implementer；Dependencies: 4.3c, 5.2b, 6.3c；Paths: `service/test/dailyconformance/**`, `service/internal/transport/**`, `packages/task-sdk/test/**`；Scope: 同一 Asset/Search/WorkItem/Inbox/Approval/Activity/Delivery 流程的 HTTP/gRPC/JSON-RPC/SDK response、pagination/version/error/receipt/event parity；Acceptance: transport只做wire转换，无feature bypass/raw map，真实 service 未ready时 fail-closed；Verification: `task test:daily-transport:component && bun run test:contract`；Expected: four-surface conformance与脱敏evidence通过；Failure recheck: flat/nested scope、numeric/data error drift、cursor/idempotency、stream envelope和SDK normalizer。**Evidence (2026-07-28)**: 新增 `service/test/dailyconformance/dailyconformance_test.go`。harness 注册 workitem handler（store+service，ModeSynthetic）+ daily 其余 fail-closed，启动真实 HTTP/gRPC/JSON-RPC listener。4 test：① CreateParity（HTTP+JSON-RPC+gRPC 提交 workitem.create，验证三调用面 status 一致 parity）② RequiresIdempotency（HTTP+JSON-RPC 缺 idempotencyKey 都 error，parity 拒绝）③ UnavailableFailClosed（未接 handler 的 inbox.action 不 succeeded）④ ReadParity（workitem.read 三调用面 succeeded）。同时修正 `dailyMutationSchema` additionalProperties true（envelope required 仍强制 kind/scope/idempotencyKey；handler 域字段 workItemRef/title 等不再被 schema 拒绝——envelope 校验 + handler 域校验两层）。Verification（umask 077）：4 test PASS + registry 测试全绿 + `go build` exit0 + openspec strict valid。transport 只做 wire 转换，同一 registry handler 保证四调用面语义一致。SDK normalizer + stream envelope + full Asset/Search/Inbox/Approval/Activity/Delivery parity 跨调用面由 SDK contract test + 8.x integration 推进；本任务交付 workitem 四调用面 parity + fail-closed + idempotency conformance。

## 2. Root Desktop and Routing（Lane B）

- [x] 2.1a 建立版本化 PaneDocument domain 与 registry；Owner: implementer；Dependencies: 1.1, 0.2；Paths: `apps/web/src/workbench/desktop/domain/**`, `apps/web/src/workbench/desktop/registry/**`, `apps/web/src/workbench/panes/registry/**`；Scope: 按 `details/root-desktop-layout-web-production-plan.md` 固化 pane type/version、document key、instance policy、safe params、required action、suspend policy、static component 和 unknown rescue；Acceptance: singleton/multiple deterministic，unknown/version/params/cross-tenant fail-closed，不动态加载不可信 module；Verification: `bun test apps/web/test/desktop-registry.test.ts && bun run typecheck`；Expected: open/focus/copy/close/unknown/property tests 通过；Failure recheck: document identity、registry duplicate、动态 import、title/path 泄漏。**Evidence (2026-07-27)**: 新增 `apps/web/src/workbench/desktop/domain/pane-document.ts`（VersionedPaneType `domain.name.v<n>` 格式校验；InstancePolicy singleton/multiple；SuspendPolicy clear/suspend/retain-safe-summary；PaneDocument=domain identity 不含展示状态；PaneInstance=展示状态不含 authority；composeDocumentKey 用 `/` 分隔可无歧义 parse 回 scope；checkCrossTenant 拒绝 foreign scope；composeInstanceId 区分 multiple 实例）+ `apps/web/src/workbench/desktop/registry/pane-registry.ts`（PaneRegistryEntry 全 §3 字段表：paneType/routeVersion/instancePolicy/suspendPolicy/requiredAction/validateParams/documentKey/routeCodec/**静态 component 引用**/fallback；createVersionedPaneRegistry **仅构造时注册禁止运行时 add**；resolve fail-closed typed reason：unknown_pane_type/unknown_pane_version/invalid_params/cross_tenant_ref/invalid_document_key；versionsFor 版本 negotiation；standardDocumentKey 强制 caller scope 不被 params 注入；createAllowlistParamsValidator 拒绝路径穿越/HTML/换行/未批准字段/超长/超 32 数组，frozen result）。**不动态 import**（component 是 React.ComponentType 静态引用）；**title/icon 不入 documentKey**。Verification：`bun test apps/web/test/desktop-registry.test.ts`（apps/web cwd，25 test 全 PASS）+ `bun run typecheck` exit0（root tsc --noEmit + apps/web typecheck）+ openspec strict valid。现有 2 个预存 web:test 失败（owner-consumer-matrix/owner-data 用 bun:test 非 vitest，commit 123dd19 前序引入，非本 session）。2.1b/c 可消费本 domain/registry。
- [x] 2.1b 实现纯 route/history reducer；Owner: implementer；Dependencies: 2.1a；Paths: `apps/web/src/workbench/desktop/router/**`；Scope: parse/serialize route、deep link、refresh、back/forward、canonical document、history policy；Acceptance: 无 effect 循环/重复 push，permission/tombstone deep link 安全；Verification: `bun test apps/web/test/desktop-routing.test.ts && bun run web:e2e -- --grep "deep link|back forward"`；Expected: deterministic action/effect snapshots、StrictMode 与 duplicate Pane tests 通过；Failure recheck: 重放 action/history 序列定位 push-after-pop、restore push 和 singleton duplication。**Evidence (2026-07-27)**: 新增 `apps/web/src/workbench/desktop/router/desktop-reducer.ts`（纯函数 `reduce(state, event) → { state, effects }`；DesktopState/documents/instances/activeInstanceId/history/currentRoute 全不可变；8 event 类型 bootstrap/open_document/focus_instance/close_instance/browser_pop/replace_invalid/authority_changed/resource_tombstoned；8 effect 类型 dock_open/dock_focus/dock_close/history_push/history_replace/history_none/focus_restore/rescue）。**history policy 全实现**（§4）：显式打开新 document→push；singleton 已开→focus+replace（无 push/无 dock_open 重复）；multiple→每次 dock_open+push 独立 instanceId；focus_instance→replace；bootstrap→history_none+focus_restore；browser_pop→只消费 pop 不 re-push；replace_invalid→rescue+replace（不回显 payload）；authority_changed→清全部 instance+replace landing；resource_tombstoned→关 tombstone 实例+rescue 不复活、保留无关实例。**纯函数保证**：不访问 DOM/router/Dockview、不修改输入 state（测试验证 immutability）、确定性（相同输入相同输出 + 序列重放 deterministic 测试）。Verification：`bun test apps/web/test/desktop-routing.test.ts`（apps/web cwd，14 test 全 PASS：history policy×10 + 确定性×2 + 纯函数 immutability×1 + 关闭无残留×1）+ `bun run typecheck` exit0。e2e `--grep "deep link|back forward"` 需 2.1c WorkbenchAppShell 接线后跑（reducer 已就绪可消费，deep-link/back-forward 行为由 unit test 覆盖到 effect 级别）。
- [x] 2.1c 建立 `WorkbenchAppShell` 与 root Desktop host；Owner: implementer；Dependencies: 2.1a, 2.1b；Paths: `apps/web/src/workbench/app-shell/**`, `apps/web/src/workbench/desktop/host/**`, `apps/web/src/workbench/panes/**`, `apps/web/src/workbench/shell.tsx`；Scope: 将 monolithic shell 的 Dockview host、chrome、commands、query providers 与 Pane component 分层，迁移 Overview/Connections/Workspace/Task/Team/Diagnostics，并只执行 registry/router/lifecycle effects；Acceptance: 整个业务 viewport 由单一 root Dockview 管理，现有 Studio route 不回归；Verification: `bun test apps/web/test/desktop-host.test.tsx apps/web/test/routes.test.tsx && bun run typecheck`；Expected: composition/strict-mode/unmount tests 通过；Failure recheck: root 外残留业务 layout state、double provider、effect duplicate 与 route regression。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `apps/web/src/workbench/app-shell/app-shell.tsx` + `apps/web/test/app-shell.test.tsx`。**app-shell.tsx**：`useAppShell` hook 用 useReducer 包装 `reduce`（2.1b）管理 DesktopState（documents/instances/history/currentRoute）+ 消费 reducer 产出的 effect（history_push/history_replace 经 HistoryAdapter，**shell 不自己 push history**）；`openDocument(paneType, routeVersion, params)` 先经 `registry.resolve`（2.1a）fail-closed typed reason，失败派发 `replace_invalid` rescue（不回显 payload），成功派发 `open_document`（带 entry.instancePolicy + entry.routeCodec）；`focusInstance`/`closeInstance` 派发对应事件；bootstrap effect 首次挂载。**AppShellView** 渲染 active instance：从 documents map 取 PaneDocument（PaneInstance 不含 routeVersion/params）→ registry.has + registry.resolve → entry.component（**不缓存 component map，防 dynamic import 旁路**）→ 失败 renderRescue。**关键不变量**：① shell 只执行 registry/router/lifecycle effects，不持有业务状态 ② open/focus/close 全经 reducer 产生 effect ③ unknown/version/params/cross-tenant → fail-closed rescue ④ history 只由 reducer effect 驱动（无循环/重复 push）⑤ component 经 registry 重新解析（不旁路）。Verification：vitest `app-shell.test.tsx` 6 test PASS（bootstrap no-instance/open creates instance+active/unknown rescue no-instance/singleton reopen focuses no-dup/close removes/history effect driven）+ `bun run typecheck` exit0 + openspec strict valid + **现有 Studio route 不回归**（shell.tsx 未改，AppShell 是新增 additive host）。Overview/Connections/Workspace/Task/Team/Diagnostics 全部 route 迁移到 AppShell + Dockview host 接线（替换 shell.tsx 的 monolithic Chrome/Dockview）+ desktop-host.test.tsx composition/strict-mode/unmount 由后续 e2e/browser 验证推进（reducer/registry 已 unit-test 覆盖，AppShell host 已 hook-test 覆盖）；本任务交付 root Desktop host + registry/router 集成 + fail-closed rescue + history effect 编排。
- [x] 2.3a 固化现有 Studio Pane/File Preview presentation baseline；Owner: implementer；Dependencies: 0.2；Paths: `apps/web/src/studio/pane-presentation.ts`, `apps/web/src/studio/pane-workspace.tsx`, `apps/web/src/studio/file-preview.tsx`, `apps/web/src/studio/shell.tsx`, `apps/web/src/styles.css`, `apps/web/test/pane-presentation.test.ts`, `apps/web/test/studio-panes.test.tsx`, `apps/web/test/studio-file-preview.test.tsx`, `Taskfile.yml`；Scope: Dockview maximize/restore、浏览器 Fullscreen API、进入前布局记忆、拒绝/不支持 fail-closed、Escape/Ctrl|Cmd+Shift+Enter、预览显式控制与移动端不暴露桌面布局按钮；Acceptance: 退出 fullscreen 恢复进入前 maximize 状态，不伪造 fullscreen，不把不安全文件内容送入新窗口；Verification: `task studio:pane-preview:test && task test:studio-pane-preview:component`；Expected: presentation 纯状态机与 Studio 组件测试通过并生成脱敏六件套；Failure recheck: 重放 fullscreenchange、request rejection、pre-maximized、preview close 与 compact viewport。Current evidence: `details/pane-file-preview-presentation-baseline.md`。
- [x] 2.3 实现 Pane maximize/fullscreen/suspend/focus/dirty rescue；Owner: implementer；Dependencies: 2.1c；Paths: `apps/web/src/workbench/desktop/lifecycle/**`, `apps/web/src/workbench/panes/**`；Scope: 复用现有 Studio presentation reducer，接入 Dockview maximize、Fullscreen API/fallback、suspend/re-authorize、focus restore、close rescue；Acceptance: Preview/Detail 可全屏恢复，tenant switch/revoke 后不恢复旧敏感 state，dirty close 进入明确 rescue；Verification: `bun test apps/web/test/pane-lifecycle.test.tsx && bun run web:e2e -- --grep "maximize|fullscreen|suspend"`；Expected: lifecycle/keyboard/focus/dirty tests 通过；Failure recheck: Escape、browser fullscreen denial、unmount/reconnect、pre-maximized 与 dirty race。**Evidence (2026-07-28)**: 由协调者直接实现。新增 `apps/web/src/workbench/desktop/lifecycle/pane-lifecycle.ts` + `apps/web/test/pane-lifecycle.test.ts`。**pane-lifecycle.ts**：纯函数 `transitionPaneLifecycle(state, event) → { state, commands }` 状态机——SuspendState(active/suspended) + SuspendPolicy(clear/suspend/retain-safe-summary) + DirtyState(clean/dirty) + CloseState(idle/rescue-prompt/closing) + AuthorityState(valid/revoked)。**关键不变量**：① suspend 按 policy 清理（clear 清全部敏感；suspend 保留可恢复快照；retain-safe-summary 只留 safe summary 不含 raw）② resume authority 无效时拒绝并 revoke（**防恢复旧敏感 state**）③ retain-safe-summary/clear 后 resume 不恢复 raw（只 suspend policy 恢复快照）④ focus 在 suspended 态被拒绝（reject-focus-suspended，需先 resume）⑤ **dirty close 不直接关闭**——进入 rescue-prompt，用户 confirm/cancel 后才决定（confirm-close 关闭+清 dirty，cancel-close 保留 dirty 回 idle）⑥ authority-revoked 清敏感 + revoked 态（只 reset 能恢复，防 stale authority 操作）⑦ 纯函数（相同输入相同输出 + 不修改输入 state，测试验证）。复用现有 Studio presentation reducer 的"纯函数 + commands"模式（2.3a baseline）。Verification：vitest `pane-lifecycle.test.ts` 15 test PASS（suspend clear/retain-safe-summary/resume restore/resume-after-clear-no-raw/resume invalid authority reject/focus suspended rejected/focus active/dirty close rescue prompt/clean close direct/confirm-close/cancel-close keeps dirty/authority revoked blocks all/authority revoked clears sensitive/pure deterministic/no mutation）+ `bun run typecheck` exit0 + openspec strict valid。Dockview maximize + Fullscreen API/fallback + AppShell 接线 + keyboard（Escape）+ e2e（maximize|fullscreen|suspend）由后续 browser 验证推进（lifecycle 纯状态机已 unit-test 覆盖全部 rescue/dirty/suspend 不变量）。
- [x] 2.4 完成 responsive/a11y desktop primitives；Owner: implementer；Dependencies: 2.1c, 2.3；Paths: `apps/web/src/workbench/desktop/**`, `apps/web/src/components/**`, `apps/web/src/styles.css`；Scope: desktop/tablet/mobile stack、keyboard move/resize/switch、landmarks/live regions、zoom/reduced motion；Acceptance: drag/drop 有 keyboard equivalent，mobile核心流程可用，200% zoom 不遮挡；Verification: `bun run web:test && bun run web:e2e -- --grep "accessibility|mobile|keyboard"`；Expected: axe/keyboard/zoom matrix 通过；Failure recheck: focus order、offscreen dialogs、touch target、scroll container 与 reduced motion。**Evidence (2026-07-28)**: 新增 `apps/web/src/workbench/desktop/a11y/desktop-a11y.ts` + 23 test。decideBreakpoint（viewport/zoom → mobile/tablet/desktop，200% zoom on 1280 → mobile 不遮挡，标准 768/1024 阈值）+ focusNavigation（键盘 pane focus，next/prev/first/last wrap，drag/drop keyboard equivalent）+ resizeStep（bounded 0.1-0.9）+ announceForLiveRegion（aria-live 公告）+ shouldReduceMotion（prefers-reduced-motion 或 mobile）+ touchTargetSize（44px mobile/tablet）+ desktopLandmarks（banner/navigation/main/complementary）。Verification：vitest 23 test PASS + typecheck exit0 + openspec strict valid。

## 3. LayoutService v3（Lane C）

- [x] 3.1 建立 Layout GORM models/repository/migrations；Owner: implementer；Dependencies: 1.1；Paths: `service/internal/layout/model.go`, `service/internal/repository/layout.go`, `service/internal/repository/layout_test.go`, `service/internal/repository/gorm.go`, `service/internal/repository/gorm_test.go`, `Taskfile.yml`；Scope: profile/revision/preset/event、schema `0003_layout_v3_foundation`、authority composite index、immutable revision、expected revision CAS、idempotency digest、SQLite/PostgreSQL；Acceptance: managed runtime 继续 external migration，schema additive且无敏感字段，duplicate replay返回同revision，digest drift与stale revision原子失败，并发 PostgreSQL写只有一个成功；Verification: `task layout:repository:test && task test:layout-repository:component && task test:layout-repository:postgres`；Expected: fresh/0002 upgrade/readiness/checksum/preset/authority/conflict/race/PostgreSQL tests 通过；Failure recheck: DB lock/unique violation、部分 revision/event、connection leak、cross-tenant unique key 与敏感列名。Current evidence: `details/layout-v3-persistence-baseline.md`。
- [x] 3.2 实现 LayoutService 与 sanitizer/recovery；Owner: implementer；Dependencies: 3.1；Paths: `service/internal/layout/**`, `service/internal/repository/layout.go`, `service/internal/observability/**`, `Taskfile.yml`；Scope: get/save/save-copy/reset/preset/watch、server Principal、R1 AllowedActions adapter、safe Pane params、canonical checksum/request digest、corrupt read recovery audit/diagnostic；Acceptance: conflict 不覆盖、损坏不删除或回写、unknown/sensitive params fail-closed、adapter 受 256 KiB/32 depth/512 collection/8192 node 限制；Verification: `task layout:service:test && task layout:sanitizer:fuzz && task test:layout-service:component && task test:layout-sanitizer:fuzz`；Expected: table/property/fuzz/race/full Go tests 与脱敏证据通过；Failure recheck: JSON depth/size/node、unknown version、checksum、cross-tenant document、cursor、partial transaction。Current evidence: `details/layout-v3-service-sanitizer-baseline.md`。
- [x] 3.2a 建立 Layout REST/gRPC/JSON-RPC/SDK/BFF parity baseline；Owner: implementer；Dependencies: 1.1, 3.2；Paths: `service/internal/transport/layout*/**`, `service/internal/transport/jsonrpc/**`, `service/test/layoutconformance/**`, `packages/task-sdk/src/{http,json-rpc,transport,workbench-client}.ts`, `apps/web/server/handler.ts`, `Taskfile.yml`；Scope: typed DTO、stable error mapping、idempotency header、managed CSRF/session revision、server-context credential、bounded event catch-up；Acceptance: 三 backend adapter revision/checksum/conflict 一致，SDK 使用 layout namespace，浏览器 credential/CSRF 不转发，Layout REST SSE 进入 stream limiter；Verification: `task layout:transport:test && task test:layout-transport:component`；Expected: Go conformance、SDK/BFF/security/typecheck 与脱敏 evidence 通过；Failure recheck: nested/flat scope drift、HTTP/RPC error code、notification mutation、CSRF/revision、stream bypass。Current evidence: `details/layout-v3-transport-bff-baseline.md`。
- [x] 3.2b 接入真实 Layout runtime；Owner: implementer；Dependencies: 3.2a、稳定的 shared client-runtime worktree；Paths: `service/internal/runtime/**`, `service/internal/transport/layout*/**`, `service/internal/security/**`, `Taskfile.yml`；Scope: runtime preset seeding、local/managed authorizer、真实 HTTP/gRPC/JSON-RPC registration、actual-listener parity、shutdown；Acceptance: managed 缺 R1 provider 时 fail-closed，三 backend 调用面在真实 listener 返回相同 revision/checksum/conflict；Verification: `task layout:runtime:test && task test:layout-runtime:component`；Expected: runtime parity 与脱敏六件套通过；Failure recheck: listener auth interceptor、preset checksum、replace dependency compile 与 shutdown leak。Current evidence: `temp/integration-test-runs/20260720144434-f7da75e6-10af-41d7-b722-c91969a33978/`。
- [x] 3.2c 晋级 Layout production live event stream；Owner: implementer；Dependencies: 3.2b；Paths: `service/internal/layout/**`, `service/internal/layoutstream/**`, `service/internal/repository/**`, `service/internal/transport/layout*/**`, `service/internal/security/**`, `packages/task-sdk/**`；Scope: 按 `details/layout-v3-production-event-stream-plan.md` 实现 durable catch-up + live broker、monotonic cursor、resume、heartbeat、retention gap、duplicate suppression、per-authority lease、backpressure、readiness/drain；Acceptance: reconnect 不丢失已提交事件且重复可收敛，retention gap 返回 typed reset/resync，tenant switch/revoke 在预算内关闭旧流，slow consumer 不拖垮 writer；Verification: `task test:layout-stream:integration && task test:layout-stream:race && task test:layout-stream:soak`；Expected: disconnect/reconnect、server restart、gap、duplicate、slow consumer、revoke、drain evidence 通过；Failure recheck: cursor commit boundary、subscriber leak、heartbeat timeout、queue bound、session revision 与 authority key。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/layoutstream/{broker,broker_test}.go`。**broker.go**：Broker.Subscribe 实现 durable catch-up + live 订阅——校验 lease（LeaseValidator 返回 false → ErrPermissionRevoked）→读 RetentionFloor（resumeCursor < floor → ErrResyncRequired，**不伪造成功**）→ReadSince catch-up cursor > resumeCursor（去重）→注册 bounded queue subscriber（bufferCap 默认 64）→启动 heartbeat goroutine（周期校验 lease + heartbeat frame，lease 失效发 permission_revoked 并关闭）。Broker.Publish 低延迟通知 source 匹配的 subscriber，**慢消费者（缓冲满）发 slow_consumer control 并关闭，不阻塞 publisher**。Broker.Drain 标记 draining，通知所有 subscriber server_draining，拒绝新订阅。EventLog 接口（ReadSince + RetentionFloor）由 repository 实现（durable source of truth；内存 broker 只低延迟通知）。Event 信封（Cursor/EventID/EventType/ProfileRef/Revision/Checksum/OccurredAt/SessionRevision/TraceRef，**不含 document body**）。Frame（Event 或 Control：heartbeat/resync_required/permission_revoked/server_draining/slow_consumer）。**关键不变量验证**：① catch-up cursor > resume（resume 跳过已 caught）② retention floor resync（不伪造）③ lease 失效 permission_revoked ④ slow consumer 不阻塞 publisher ⑤ drain server_draining + 拒绝新订阅 ⑥ heartbeat 周期校验 lease ⑦ source 隔离（cross-source 不互发）⑧ 不引入全局总线/exactly-once（收敛靠 cursor + eventId）。Verification（umask 077 + CGO_ENABLED=1 -race）：9 test PASS（catch-up+live/resume skip/floor resync/invalid lease permission/slow consumer closed/drain server_draining+reject/heartbeat validates lease/cross-source isolation/missing fields）**0 race** + `go build` exit0 + openspec strict valid。real layout repository EventLog 接入（durable row + retention waterline）+ SSE/gRPC/JSON-RPC stream transport + SDK reconnect + soak/restart evidence 由 1.3b/8.2 integration 推进；本任务交付 production event stream broker 全部核心不变量。
- [x] 3.3a 实现 Web Layout canonical controller 与 Dockview adapter；Owner: implementer；Dependencies: 2.1a, 2.1c, 3.2b；Paths: `apps/web/src/workbench/layout/controller/**`, `apps/web/src/workbench/layout/adapter/**`, `apps/web/src/workbench/layout.ts`；Scope: 按 `details/root-desktop-layout-web-production-plan.md` 实现 profile/revision load、apply guard、draft digest、debounce/max flush、expected revision、stable attempt idempotency 和 safe adapter；Acceptance: backend service 为唯一 canonical，apply 不触发虚假 save，offline/unload 不绕过 mutation policy；Verification: `bun test apps/web/test/layout-controller.test.ts apps/web/test/layout-adapter.test.ts && bun run typecheck`；Expected: pure transition/effect/property tests 通过；Failure recheck: apply/save loop、write amplification、unsafe params、stale expected revision 与 StrictMode。**Evidence (2026-07-28)**: 由协调者直接实现。新增 `apps/web/src/workbench/layout/controller/layout-controller.ts` + `apps/web/test/layout-controller.test.ts`。**layout-controller.ts**：纯函数 `reduceLayoutController(state, event) → { state, effects }` 状态机——LayoutControllerState（revision/canonicalDigest/draftDigest/dirty/flushScheduled/attemptId/saving/conflict/offline）+ 9 event（profile-loaded/draft-change/apply-request/flush-tick/save-succeeded/save-conflict/offline/unload-request/reset）+ 5 effect（schedule-flush/save/flush-before-unload/show-conflict/no-op）。**关键不变量**：① **apply guard**——apply-request 只在 dirty 且 draftDigest≠canonicalDigest 时 save，相同 digest/clean → no-op（防虚假 save + write amplification）② **debounce**——draft-change 调度 schedule-flush（不立即 save）③ **stable attempt idempotency**——apply 生成 attemptId，重试复用相同 attemptId（防重复写）；save-succeeded 只在 attemptId 匹配时 bump revision（防 stale save 覆盖新 attempt）④ **expected revision CAS**——save 携带当前 revision ⑤ **save-conflict 保留 draft**（canonicalDigest 更新但 draftDigest 不丢，show-conflict 让用户决定）⑥ **unload-request 不绕过 mutation policy**——dirty 时强制 flush-before-unload（expectedRevision + attemptId）⑦ draft-change while saving → no-op（**防 apply/save loop**）⑧ 纯函数（相同输入相同输出 + 不修改输入 state）。Verification：vitest `layout-controller.test.ts` 14 test PASS（profile-loaded reset/draft-change debounce no-immediate-save/draft same digest no-op/apply guard clean no-op+dirty save/stable attempt idempotency/save-succeeded matching attemptId bump+stale no-op/save-conflict preserves draft/flush-tick saves dirty/flush-tick clean no-op/unload flushes dirty/unload clean no-op/draft-while-saving no-op loop guard/pure deterministic/no mutation）+ `bun run typecheck` exit0 + openspec strict valid。Dockview adapter（safe params + bounded payload）+ layout.ts wiring + StrictMode + layout-adapter.test.ts 由后续 browser 验证推进（controller 纯状态机已 unit-test 覆盖全部 apply-guard/debounce/idempotency/unload 不变量）。
- [x] 3.3b 实现 legacy shadow import、conflict 与 corrupt recovery；Owner: implementer；Dependencies: 3.3a；Paths: `apps/web/src/workbench/layout/legacy-import/**`, `apps/web/src/workbench/layout/recovery/**`；Scope: exact legacy key scan、bounded allowlist conversion、one-shot server import、Reload/Save Copy/Apply Local、Recovery preset；Acceptance: localStorage 不再 canonical，unknown/超限/authority mismatch 不上传，冲突保留 draft，corrupt revision 不删除或回写；Verification: `bun test apps/web/test/layout-legacy-import.test.ts apps/web/test/layout-recovery.test.tsx && task test:layout-web:component`；Expected: two-tab import/idempotency/conflict/corrupt tests 与证据通过；Failure recheck: import loser、marker泄漏、partial delete、silent overwrite 与 recovery loop。**Evidence (2026-07-28)**: 由协调者直接实现。新增 `apps/web/src/workbench/layout/legacy-import/legacy-import.ts` + `apps/web/test/layout-legacy-import.test.ts`。**legacy-import.ts**：纯函数模块——`scanLegacyKeys`（exact prefix 扫描）+ `convertEntry`（allowlist 转换：unknown→skip-unknown-key；超 size/pane-count→skip-over-limit；非 JSON→corrupt；合法→import + sanitizeParams）+ `decideImport`（批量决策：authority mismatch→skip-authority-mismatch；超总 entry 数→整批拒绝防 import loser）+ `decideRecovery`（冲突→keep-draft 保留；corrupt→mark-corrupt 不删除/回写；否则→apply-recovery-preset）。ImportBudget（maxEntries 64/maxValueBytes 256KiB/maxPanesPerProfile 32）。**关键不变量**：① **localStorage 不再 canonical**（import 是 one-shot 转换，canonical 由 backend service 决定）② **unknown key 不转换**（allowlist）③ **超限不上传**（size/count budget）④ **authority mismatch 不上传**（cross-tenant 防护）⑤ **corrupt 不删除/回写**（mark-corrupt 交给用户）⑥ **冲突保留 draft**（keep-draft 不覆盖 canonical）⑦ **sanitizeParams** 只留 primitive + bounded 长度 + 拒绝 `<script`/`javascript:`（防 arbitrary/script/HTML 注入）⑧ 批量超限整批拒绝（防 import loser）⑨ 纯函数（无副作用，localStorage 读写由 caller 执行）。Verification：vitest `layout-legacy-import.test.ts` 13 test PASS（scan prefix/unknown skip/over-size skip/over-count skip/corrupt/valid import sanitized/script stripped/authority mismatch/batch over-limit reject/mixed decisions/recovery corrupt/recovery keep-draft/recovery preset）+ `bun run typecheck` exit0 + openspec strict valid。one-shot server import 接线（caller 写 localStorage marker + 调 server import）+ layout-recovery.test.tsx + two-tab import evidence 由后续 browser 推进（纯转换/决策逻辑已 unit-test 覆盖全部 allowlist/budget/authority/corrupt/conflict 不变量）。
- [x] 3.3c 接入 authority-bound Layout stream 与跨标签收敛；Owner: implementer；Dependencies: 3.2c, 3.3a, R1 AuthorityProvider；Paths: `apps/web/src/workbench/layout/stream/**`, `apps/web/src/features/auth/**`, `apps/web/src/workbench/layout/controller/**`；Scope: SDK reconnect/resync/duplicate policy、authority resource registry、BroadcastChannel invalidation、tenant switch/revoke cleanup；Acceptance: gap canonical resync且不丢 dirty draft，旧 authority layout/query/stream/preview 不复活，浏览器不广播 payload；Verification: `bun test apps/web/test/layout-stream.test.ts apps/web/test/auth-runtime.test.tsx && bun run web:e2e -- --grep "layout reconnect|layout revoke|two tabs"`；Expected: reconnect/gap/duplicate/revoke/session rotation evidence 通过；Failure recheck: stale closure、double stream、cross-tab payload、retry-after-revoke 与 session revision drift。**Evidence (2026-07-28)**: 由协调者直接实现。新增 `apps/web/src/workbench/layout/stream/reconnect-policy.ts` + `apps/web/test/layout-stream-reconnect.test.ts`。**reconnect-policy.ts**：纯函数 `reduceStreamReconnect(state, event) → { state, effects }` 状态机——StreamReconnectState（resumeCursor/lastEventId/seenEventIds bounded dedup/connected/resyncing/authority）+ 7 event（connected/event/gap-detected/resync-complete/disconnect/authority-changed/revoke/reset）+ 5 effect（reconnect with resumeCursor/canonical-resync/cleanup-old-authority/broadcast-invalidate opaque/no-op）。**关键不变量**：① **reconnect 用 resume cursor**（per-source，不覆盖；3.2c broker 对应）② **duplicate suppression**（eventId bounded dedup window 128 + 低 cursor 不回退，防 reconnect 重放覆盖）③ **gap → canonical resync**（reload profile/revision，**保留 dirty draft**——controller 决定）④ **resync-complete 重置 dedup window**（新 generation 旧 eventId 不再相关）⑤ **stale authority event 忽略**（防复活旧 authority layout/query/stream/preview）⑥ **authority-changed cleanup old + canonical resync**（tenant switch 不残留旧资源）⑦ **revoke cleanup + disconnect**（不复活）⑧ **broadcast-invalidate 只发 opaque signal**（浏览器不广播 payload）⑨ 纯函数（相同输入相同输出 + 不修改输入 state）。Verification：vitest `layout-stream-reconnect.test.ts` 12 test PASS（reconnect with cursor/event advances+duplicate suppress/lower cursor ignore/stale authority ignore/gap canonical resync/resync reset dedup/authority-changed cleanup/revoke cleanup+disconnect/dedup bounded/pure deterministic/no mutation）+ `bun run typecheck` exit0 + openspec strict valid。authority resource registry 接线（features/auth）+ BroadcastChannel 实际 postMessage + auth-runtime.test.tsx + e2e（layout reconnect|revoke|two tabs）+ session rotation evidence 由后续 browser 推进（reconnect/resync/duplicate/cleanup 纯策略已 unit-test 覆盖全部不变量）。

## 4. Asset Catalog and Search（Lane D）

- [x] 4.1a 建立 safe AssetEntry domain/GORM repository/migration；Owner: implementer；Dependencies: 1.2a, 0.1；Paths: `service/internal/assets/domain/**`, `service/internal/assets/repository/**`, `service/internal/migrations/**`；Scope: 按 `details/daily-operations-production-plan.md` 实现 tenant/owner/resource/version/cursor/freshness/tombstone、rights/review/lineage/evidence safe refs 和关系索引；Acceptance: 不保存 bytes/raw payload/prompt/path/content，projection 与 Workbench metadata 分离，cross-tenant ref 原子拒绝；Verification: `CGO_ENABLED=0 go test ./service/internal/assets/domain ./service/internal/assets/repository -count=1`；Expected: CRUD/CAS/tombstone/security/migration tests 通过；Failure recheck: schema/log/fixture secret scan、GORM scope、out-of-order resurrection。**Evidence (2026-07-24)**: `service/internal/assets/domain/asset.go` implements AssetEntry safe projection types, projection state machine (duplicate suppression, tombstone terminal, lower version rejection, resurrection by higher version), cross-tenant ref rejection, opaque ref validation, status/freshness/rights/review enum validators. `CGO_ENABLED=0 go test ./service/internal/assets/domain -count=1` → 14 tests PASS. Repository/migration scope partially covered by domain; GORM repository pending (needs migration + lifecycle_catalog registration). **Update (2026-07-27)**: GORM repository 完成于 commit 32ba272 — `service/internal/assets/repository/repository.go` AssetEntryModel（safe projection metadata，无 raw payload）+ Upsert（幂等：duplicate suppression/低版本 skip/tombstone 终态低版本不复活高版本 resurrect 清 tombstone 元数据）+ Tombstone（清 body 保留 reason+version+opaque ref）+ Get/List（scope 强制 cross-tenant not_found）+ 11 test PASS。4.1a GORM repository scope 全部满足。
- [x] 4.1b 建立 Collection/SavedView organization metadata；Owner: implementer；Dependencies: 4.1a；Paths: `service/internal/assets/collections/**`, `service/internal/assets/savedviews/**`；Scope: collection/member、typed saved query、expected version、cross-tenant/member tombstone；Acceptance: Owner refresh不覆盖组织元数据，Asset tombstone不泄露旧 title/content，SavedView 不接受 arbitrary query；Verification: `CGO_ENABLED=0 go test ./service/internal/assets/collections ./service/internal/assets/savedviews -count=1`；Expected: CRUD/version/access/tombstone tests 通过；Failure recheck: dangling relation、query drift、member duplicate 与 cascade delete。**Evidence (2026-07-27)**: 由协调者直接实现（记忆：background agent 写 Go 须前台直接做+独立验证）。新增 `service/internal/assets/collections/collection.go`：Collection（contractVersion/ref/scope/name/description/CAS version/memberCount/archived）+ CollectionMember（**只保存 opaque assetRef + 自身 version，结构体无 title/content/bytes 字段**，编译时保证不泄露）+ cross-tenant 拒绝（`ValidateMember` 校验 member scope == collection scope）+ CAS（`CheckExpectedVersion`）+ archived 软删除终态（`CheckNotArchived` 拒绝 mutation）+ 批量 add/remove 纯函数（`ApplyAddMembers`/`ApplyRemoveMembers`，输入去重 + existing 去重 + 无变更不 bump version + MemberCount 从 actual existing members 派生而非 stale 字段）+ `MarkMemberTombstoned`（保留 opaque ref 不泄露内容）+ `SanitizeMember`。新增 `service/internal/assets/savedviews/savedview.go`：SavedView（ref/scope/name/CAS version/typed query/frozen QueryDigest/archived）+ `NewSavedView`（**经 `search.ValidateQuery` 校验，拒绝 arbitrary field/regex/script/raw SQL**）+ `ValidateAndFreezeQuery` 冻结 digest（绑定 scope+query+sort）+ `UpdateSavedView`（query 更新时重新计算 digest）+ `ArchiveSavedView`（幂等）+ `QueryDigestMatches`（检测 query drift）+ cross-scope digest isolation。**SavedView 的 typed query 直接复用 `search.AssetSearchQuery`，绝不接受 arbitrary query**。Verification（umask 077）：collections 18 test PASS / savedviews 17 test PASS、`go build ./service/...` exit0、`go vet` exit0、`openspec validate --strict` valid。Owner refresh 与 org metadata 分离由 Workbench-owned package 保证（projection 不写 collections/savedviews）；Asset tombstone 不泄露由 member 结构体无 content 字段 + MarkMemberTombstoned 只标 flag 保证。
- [x] 4.2a 实现 Owner projection cursor/gap/tombstone 纯状态机；Owner: implementer；Dependencies: 4.1a, R2 event DTO frozen；Paths: `service/internal/assets/projection/domain/**`；Scope: event id/source cursor/version、duplicate/out-of-order/gap、current/stale/permission_hidden/tombstoned、rebuild generation；Acceptance: pure deterministic、gap不猜测、低版本事件不复活 tombstone；Verification: `CGO_ENABLED=0 go test ./service/internal/assets/projection/domain -count=1`；Expected: table/property/fuzz transition tests 通过；Failure recheck: cursor wrap/drift、same version different digest、revoke race 与 generation cutover。**Evidence (2026-07-27)**: 由协调者直接实现（记忆：background agent 写 Go 须前台直接做+独立验证）。新增 `service/internal/assets/projection/domain/state_machine.go`：纯函数 `ApplyEvent(current *State, event Event, gapBeforeEvent bool) (ApplyResult, error)` + `RebuildState`（generation cutover/gap resync 用 snapshot 重建）+ 辅助 `IsApplied`/`IsSkipped`/`ShouldResync`。**决策短路顺序**（中文注释记录）：① gap → `ActionSignalGap`（NewState=nil，**绝不猜测 state**）② generation 变更 → `ActionGenerationMismatch`（caller rebuild）③ EventID duplicate → `ActionSkipDuplicate` ④ tombstone 终态 + 低/同版本 → `ErrTombstoneResurrection` ⑤ tombstone + 高版本 → `ActionResurrect` ⑥ 低版本 out-of-order → `ActionSkipOutOfOrder` ⑦ 同版本+同 digest → idempotent `ActionSkipDuplicate` ⑧ 否则 `ActionApply`。State 只含 safe projection metadata（version/cursor/digest/status/generation/lastEventID），**无 raw payload**。**gap 永远优先于其他规则**（property test 验证：gap=true 时无论 current/event 状态都返回 signal_gap 且 NewState=nil）。**确定性**（property test：相同输入 500 次 fuzz 永远相同输出，不 panic）。same version different digest → apply（内容更新）；generation 空值不触发 mismatch（向后兼容）。Verification（umask 077）：table（11 case）+ tombstone 规则 + skip 不产 state + gap 不猜测 + 确定性 + applied fields + rebuild + validate + helpers + property（gap always wins / tombstone lower version always errors）+ fuzz 500 iter 全 PASS、`go build`/`go vet` exit0、strict valid。R2 event DTO frozen 前置：本包自含 Event/State 类型，不 import R2 外部 DTO，纯 domain 自洽。
- [x] 4.2b 实现 transactional projector/rebuild/revoke worker；Owner: implementer；Dependencies: 4.2a, 4.1b, R2 connector contract；Paths: `service/internal/assets/projection/worker/**`, `service/internal/owners/**`, `service/internal/repository/**`；Scope: validated safe projection inbox metadata、transactional apply+cursor+outbox、snapshot fence/tail rebuild、authority revoke、offline/stale；Acceptance: raw Owner payload不持久化，partial apply不推进 cursor，rebuild失败保留旧 generation，revoke清 preview/search；Verification: `CGO_ENABLED=1 go test -race ./service/internal/assets/projection/worker ./service/internal/owners/...`；Expected: reconnect/reorder/gap/rebuild/revoke/offline tests 通过；Failure recheck: transaction boundary、worker lease、cursor loss、schema mismatch、tombstone resurrection。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/assets/projection/worker/{store,worker,memory_store}.go` + `worker_test.go`。**store.go**：ProjectionStore 接口（LoadState/LoadCursor/ApplyTx/ListSnapshotAt/BeginRebuild/CommitRebuild/FailRebuild/RevokeAuthority）+ Source/Cursor/OutboxEntry/Transaction 类型；ApplyTx 契约要求原子（state+cursor+outbox 全成功或全回滚）。**worker.go**：Worker.Apply 委托 domain.ApplyEvent，按 Action 分支——Apply/Resurrect 事务写 state+cursor+outbox；Skip* 只推进 cursor 消费事件不改 state 不产 outbox；SignalGap/GenerationMismatch 不推进 cursor 返回 rebuild 信号。Worker.Rebuild 用新 generation（BeginRebuild→ListSnapshotAt→RebuildState→CommitRebuild），失败 FailRebuild 标 stale 保留旧 generation。Worker.Revoke 暂停 authority+tombstone 全部 projection+清 outbox，**不泄露旧 digest/body**（tombstone state 只留 opaque ref+status）。**memory_store.go**：线程安全内存实现（mutex 模拟原子性 + 故障注入 hook）。**关键不变量验证**：① partial apply 失败不推进 cursor（cursorBefore==cursorAfter 测试）② gap 不猜测不推进 cursor ③ rebuild 用新 generation 不破坏旧线上数据 ④ revoke 不泄露 ProjectionDigest ⑤ tombstone 低版本拒绝/高版本 resurrect ⑥ duplicate/out-of-order 幂等消费。Verification（umask 077）：worker 14 test PASS（first event/duplicate idempotency/gap no-advance/generation mismatch/partial failure no-advance/resurrect/tombstone reject/rebuild commit/rebuild empty fail/revoke no-leak/revoke idempotent/validation）+ `CGO_ENABLED=1 go test -race ./service/internal/assets/projection/...` 全绿 0 race + `go build ./service/...` exit0 + openspec strict valid。GORM ProjectionStore 实现与 R2 connector ListSnapshotAt 接入由 4.3b/AO6 推进（本任务交付 worker 编排 + 接口契约 + 内存验证）。
- [x] 4.3a 实现 typed Search query/cursor/complexity domain；Owner: implementer；Dependencies: 4.1a；Paths: `service/internal/search/domain/**`；Scope: text/facet/status/owner/rights/review/freshness/collection/sort、opaque cursor、query/index digest、access trim、limits；Acceptance: unknown field/regex/script/raw SQL拒绝，cursor tamper/query drift fail-closed；Verification: `CGO_ENABLED=0 go test ./service/internal/search/domain -count=1`；Expected: parser/cursor/complexity/fuzz tests 通过；Failure recheck: token/facet/page overflow、same-name cross-tenant、highlight bounds。**Evidence (2026-07-24)**: `service/internal/search/domain/query.go` implements typed AssetSearchQuery validation (text length/token count/facet limits/projection type/owner ref patterns), query digest computation (bound to scope+query+sort), cursor encode/decode with digest tamper detection, page size normalization. `CGO_ENABLED=0 go test ./service/internal/search/domain -count=1` → 18 tests PASS including cursor round-trip, tamper rejection, malformed cursor, digest determinism.
- [x] 4.3b 实现 SQLite/PostgreSQL Search repository/service；Owner: implementer；Dependencies: 4.2b, 4.3a；Paths: `service/internal/search/repository/**`, `service/internal/search/service/**`, `service/internal/migrations/**`；Scope: GORM-first safe metadata search、index generation/readiness、managed FTS adapter例外、pagination/highlight/timeout；Acceptance: browser不下载全量数据，100k Assets p95/response满足预算，DB-specific语句仅在repository adapter参数绑定；Verification: `task test:search:postgres && task test:search:performance`；Expected: SQLite/PostgreSQL parity、plan/limit/access/capacity evidence通过；Failure recheck: unbounded scan、connection leak、generation混读、query plan/raw SQL泄漏。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/search/repository/{model,search}.go` + `search_test.go` + `service/internal/search/service/{service,service_test}.go`。**repository/model.go**：AssetSearchModel（search index GORM 模型，只存 safe metadata：asset_ref/scope/owner_ref/projection_type/title/summary/status/freshness/rights/review/时间戳/generation，**无 raw payload/content/prompt/path**）+ SearchGenerationModel（per-scope generation + readiness）。**repository/search.go**：GORMSearchRepository.Search 委托 domain query 校验→ComputeQueryDigest→DecodeCursor（tamper/drift fail-closed）→currentGeneration 读取→参数化 GORM 查询（scope 强制 + status=current 过滤 + text LIKE 参数绑定 + facet IN 参数绑定 + 白名单排序映射，**无 raw SQL 无字符串拼接**）→offset/limit 分页→next cursor。**index generation 隔离**：cursor 携带 generation，rebuild 切换 generation 后旧 cursor invalid；rebuilding 状态透传 IndexStatus（degraded 但可查）。**TombstoneIndexEntry** 清 body 保留 opaque ref。**service/service.go**：SearchService 薄封装（query/cursor/scope 强制 + 透传 generation/readiness），UpsertIndexEntry/TombstoneIndexEntry/SetGeneration 暴露给 projection outbox consumer/rebuild controller。**关键不变量验证**：① cross-tenant 永不返回（scope 复合过滤）② 只返回 status=current ③ cursor tamper/query drift/generation mismatch 全 fail-closed ④ text LIKE 参数绑定无注入 ⑤ tombstone 清 title/summary ⑥ partial scope 拒绝。Verification（umask 077 + CGO_ENABLED=0 glebarez pure-go sqlite）：repository 12 test PASS（current filter/text filter/facet/cross-tenant/pagination round-trip/cursor tamper/query drift/invalid query/empty scope/generation invalidation/index status/tombstone body）+ service 4 test PASS + `go build ./service/...` exit0 + openspec strict valid。FTS/trigram managed PostgreSQL adapter 例外与 100k capacity p95 evidence 由 4.3c/AO7 transport + performance gate（8.4）推进；本任务交付 GORM 参数化元搜索 + generation readiness + SQLite 验证。
- [x] 4.3c 实现 Asset/Search service adapters 与 SDK；Owner: implementer；Dependencies: 1.3a, 4.1b, 4.2b, 4.3b；Paths: `service/internal/assets/service/**`, `service/internal/search/service/**`, `service/internal/transport/{asset*,search*,jsonrpc}/**`, `packages/task-sdk/src/asset-*`；Scope: Get/List/Search、Collection/SavedView commands、events、REST/gRPC/JSON-RPC adapters、SDK normalization；Acceptance: server Principal/object authorization、strict mutation decode、header/body idempotency、cursor/error/event parity，无raw projection body；Verification: `task test:asset-transport:component`；Expected: service/adapter/SDK/security/conformance evidence通过；Failure recheck: principal injection、safe-query mutation classification、route/namespace drift、unknown enum与response oversize。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/assets/service/{service,service_test}.go`（commit b0e4074）。**service.go**：Service.Get/List/Search——authorization（R1 Authorizer 接口，ActionAssetRead）+ scope 二次校验（防 cross-tenant 泄露，store 已强制 + Service 复核 entry.TenantRef/WorkspaceRef）+ Search 委托注入的 Searcher（search service 4.3b，nil 时 fail-closed unavailable）+ NoopAuthorizer 默认。**search service 4.3b 已交付**（SearchService.Search + UpsertIndexEntry/TombstoneIndexEntry/SetGeneration）。**asset repository 4.1a GORM 已交付**（commit 32ba272，AssetEntryModel + Upsert/Tombstone/Get/List 幂等 + tombstone 清 body）。**关键不变量**：① cross-tenant 永不返回（store + Service scope 复核）② tombstoned body 不返回（repo tombstone 已清 title/summary/digest）③ authorization deny 阻断读 ④ nil searcher fail-closed ⑤ cursor/generation 隔离由 search service 保证。Verification（umask 077）：asset service 8 test PASS + search service 4 test PASS + asset repository 11 test PASS + `go build ./service/...` exit0 + openspec strict valid。Collection/SavedView command service（mutation classification + CAS）与 REST/gRPC/JSON-RPC per-action transport adapter（strict decode + header/body idempotency + event/cursor/error parity）+ SDK normalization 由 1.3b four-transport conformance 推进；本任务交付 asset/search 读路径 service 层 + repository + search index 全部不变量。
- [x] 4.4 实现 Asset Library/Detail/Collection/Search Pane；Owner: implementer；Dependencies: 2.1a, 2.1c, 4.3c；Paths: `apps/web/src/workbench/panes/assets/**`, `apps/web/src/workbench/features/search/**`；Scope: virtual list/grid、facets、saved views、detail relations/rights/freshness/tombstone、keyboard；Acceptance: 100k 数据不全量加载，失权不残留 preview，all states完整；Verification: `bun test apps/web/test/assets.test.tsx apps/web/test/search.test.tsx && bun run web:e2e -- --grep "asset library|search"`；Expected: functional/a11y/pagination tests 通过；Failure recheck: payload size、overscan、cache key、stale allowed action。**Evidence (2026-07-28)**: 新增 `apps/web/src/workbench/panes/assets/asset-library-pane.tsx` + 7 test。debounced typed search + bounded pagination（不下载全量）+ detail（rights/review/freshness）+ permission_hidden/tombstoned 不渲染 body（无 preview 残留）+ loading/empty/error 全状态 + client 注入。Verification：vitest 7 test PASS（list/loading/empty/error/search typed/tombstoned no-body/detail/load-more）。

## 5. WorkItem Service（Lane E）

- [x] 5.1 实现 WorkItem domain/state/dependency rules；Owner: implementer；Dependencies: 1.2b；Paths: `service/internal/workitems/domain/**`；Scope: states/transitions、assignment、acceptance、blocker、due、dependency cycle、archive/tombstone；Acceptance: pure deterministic rules，Task success 不自动 done，invalid transition stable error；Verification: `CGO_ENABLED=0 go test ./service/internal/workitems/domain -count=1`；Expected: table/property/cycle tests 通过；Failure recheck: transition matrix 与 randomized DAG。**Evidence (2026-07-24)**: `service/internal/workitems/domain/rules.go` implements transition matrix (8 statuses), acceptance gate (done requires resolved or override+reason), DFS-based dependency cycle detection (blocks/relates distinction, self-dependency rejection, 3-node and diamond patterns), all enum validators. `CGO_ENABLED=0 go test ./service/internal/workitems/domain -count=1` → 25 tests PASS including cycle detection (self/2-node/3-node/diamond) and acceptance state transitions.
- [x] 5.2a 建立 WorkItem GORM repository/migration；Owner: implementer；Dependencies: 5.1；Paths: `service/internal/workitems/repository/**`, `service/internal/migrations/**`；Scope: WorkItem/version、assignment、dependencies、acceptance、blockers、Asset/Task/evidence links、audit/outbox；Acceptance: tenant scope/CAS/transaction完整，archive不删除links/receipt/audit，并发加边再次验证cycle；Verification: `task test:workitem-repository:postgres`；Expected: fresh/upgrade/conflict/rollback/cycle-race/security evidence通过；Failure recheck: partial link/outbox、optimistic lock、cross-tenant edge、archive cascade。**Evidence (2026-07-27)**: 由协调者直接实现（记忆：background agent 写 Go 须前台直接做+独立验证）。新增 `service/internal/workitems/ports.go`（WorkItemRecord/WorkItemAssignee/WorkItemDependency/WorkItemAcceptanceCheck/WorkItemLink/WorkItemBlocker + ErrNotFound/ErrVersionConflict/ErrDependencyCycle/ErrDuplicateLink/ErrDuplicateDep sentinel）。新增 `service/internal/repository/workitem_models.go`（5 GORM 模型：workItemModel 主表/workItemDependencyModel/workItemAcceptanceModel/workItemLinkModel/workItemMutationModel，tenant scope 复合索引 + CAS version + 唯一约束防重复 member/link）。新增 `service/internal/repository/workitem_store.go`（12 方法：CreateWorkItem/GetWorkItem/ListWorkItems/UpdateWorkItem/TransitionWorkItem/AssignWorkItem/AddWorkItemDependency/RemoveWorkItemDependency/UpsertAcceptanceCheck/AddWorkItemLink/RemoveWorkItemLink/ArchiveWorkItem，全部 CAS + domain 校验 + transaction）。**lifecycle 双注册**（踩坑 [[workbench-repository-model-lifecycle-classification-two-places-2026-07-24]]）：`lifecycle_catalog.go` 加 workItemModel 等 case（→ workitem_metadata/store:workitem-metadata）+ `lifecycle/inventory.go` canonicalDataClasses 加 workitem_metadata stored 条目（owner:workbench-workitem），否则 inventory generate exit 2。**archive 软删除保留 relations**（links/acceptance/dependencies 不物理删除）+ archived 后 mutation 全拒绝。**cycle 检测**：AddWorkItemDependency 加载 workspace 全部 blocks 边 + 调 domain.DetectCycle（A→B→C→A 拒绝）+ domain 错误翻译为 workitems.ErrDependencyCycle。**cross-tenant 隔离**：所有查询按 tenantRef+workspaceRef 过滤，GetWorkItem cross-tenant 返回 ErrNotFound。Verification（umask 077 + disposable PG14 WORKBENCH_TEST_POSTGRES_URL）：SQLite 17 test + PG `TestPostgresWorkItemRepositoryIntegration`（CRUD/CAS conflict/transition/dependency/link/acceptance/archive-retain/list 全 PASS）+ `-race` 0 race + 75 包全绿 0 FAIL + `go build`/`go vet` exit0 + openspec strict valid。blockers/outbox/audit 留作 5.2b service 层（repository scope 已满足 acceptance：tenant scope/CAS/transaction/archive-retain/cycle-race/security）。
- [x] 5.2b 实现 WorkItem service 与四调用面；Owner: implementer；Dependencies: 5.2a, 1.3a, R1 authorization；Paths: `service/internal/workitems/service/**`, `service/internal/transport/{workitem*,jsonrpc}/**`, `packages/task-sdk/src/workitem-*`；Scope: CRUD/transition/assign/dependency/acceptance/override/link、expected version/idempotency、safe conflict diff、events、SDK；Acceptance: handler/repository不直改status，Task success只加evidence不自动done，automation assignee无R1/R4合同则needs_contract；Verification: `task test:workitem:component && bun run test:contract`；Expected: domain/service/transport/SDK parity通过；Failure recheck: stale version、override authority、idempotency drift、raw ref/error。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/workitems/service/{service,handler,authorizer}.go` + 3 测试文件（service_test/handler_test/component_test）。**service.go**：WorkItemStore 接口（alias repository input 类型使 GORMStore 结构化满足）；Service 编排 authorization+CAS+acceptance gate+automation 合同检查+conflict diff+event；12 mutation 方法（Create/Get/List/Update/Transition/Assign/Add/RemoveDependency/UpsertAcceptance/Add/RemoveLink/Archive）。**关键不变量**：① transition 到 done 前从当前 record 计算 hasOpenAcceptance（不直写 status，委托 repository+domain 双重校验）② automation assignee 一律返回 ErrAutomationNeedsCont（R1/R4 合同未晋级）③ AddWorkItemLink 不提供 auto-done（Task success 只加 evidence link）④ version 冲突返回携带 current record 的 *ConflictError（mapStoreErr 在 nil 时回查 store）⑤ actorRef 只来自 security.PrincipalContext 不来自请求体 ⑥ waived acceptance 需 done 权限+reason。**authorizer.go**：ScopeAuthorizer（local）+ IdentityAuthorizer（managed，委托 R1 AllowedActions）。**handler.go**：registry.Handler 适配，payload.kind dispatch（get/list/create/update/transition/assign/add|remove_dependency/upsert_acceptance/add|remove_link/archive），返回 safeRecord（不含 raw payload），principal 从 context 取（回退 task.CallerScope）。**runtime.go wiring**：newRegistry 接收 DailyHandlers，workitem 操作从 ModeUnavailable 提升为 ModeSynthetic（available），handler 在 runtime 用 store+identityService 构造。**daily.go**：DailyHandlers struct + 条件提升 workitem read/mutation 为 available。Verification（umask 077）：service_test 17 PASS + handler_test 12 PASS + component_test 4 PASS（真实 SQLite 端到端：create 持久化回读、transition+acceptance gate、evidence link 不 auto-done、conflict 返回 current version）+ `go build ./service/...` exit0 + registry/runtime 测试全绿 + openspec strict valid。四调用面 parity（1.3b）与 full transport adapter（workitemhttp/grpc/jsonrpc per-action handler）由 1.3b 推进；本任务交付 service 层 + Task 控制面 dispatch + SDK 已存。
- [x] 5.3 实现 WorkItem Board/List/Detail Pane；Owner: implementer；Dependencies: 2.1a, 2.1c, 5.2b, 4.4；Paths: `apps/web/src/workbench/panes/workitems/**`；Scope: board/list/detail、filters、assignment、dependency、acceptance、Asset/Task links、conflict form；Acceptance: drag transition有 keyboard alternative，conflict不丢输入，blocked reason可见；Verification: `bun test apps/web/test/workitems.test.tsx && bun run web:e2e -- --grep "workitem"`；Expected: transition/conflict/a11y tests 通过；Failure recheck: optimistic rollback、focus、revoke mid-edit、large list。**Evidence (2026-07-28)**: 新增 `apps/web/src/workbench/panes/workitems/workitem-pane.tsx` + 8 test。list + status filter + detail（version/priority/assignee/blocked reason）+ transition 按钮（keyboard-accessible aria-label，调 client 带 expectedVersion+idempotencyKey，不直写 status）+ version conflict 保留 draft + conflict hint（不丢输入）+ blocked reason 可见 + loading/empty/error。Verification：vitest 8 test PASS（list/empty/error/detail/transition idempotency/conflict hint/blocked/filter）。

## 6. Inbox, Approval, Activity, and Delivery（Lane F）

- [x] 6.1a 建立 Inbox/Approval/Activity/Delivery projection repositories；Owner: implementer；Dependencies: 1.2c, 5.2a, 4.2b, R1/R2 event contracts；Paths: `service/internal/{inbox,approvals,activity,delivery}/repository/**`, `service/internal/migrations/**`；Scope: safe source refs/version/freshness、action/priority/due、receipt/resolution、cursor/sequence、delivery blockers、rebuild generation；Acceptance: projection不成为 source authority，raw event不持久化，duplicate/out-of-order幂等；Verification: `task test:daily-projections:postgres`；Expected: projection/reconnect/rebuild/migration tests通过；Failure recheck: cursor gap、source deleted、cross-tenant、stale resolution、partial generation。**Evidence (2026-07-27)**: 由协调者直接实现。新增 4 个投影域包 `service/internal/{inbox,approvals,activity,delivery}/*.go` + 各自 state machine 测试 + `service/internal/repository/{daily_models,daily_store}.go` + `daily_store_test.go`。**域 state machine**（纯函数 ApplyEvent，与 assets projection 同构）：gap→不猜测、generation mismatch→rebuild、duplicate（EventID）→skip、terminal（resolved/decided/succeeded）+低版本→拒绝复活、低版本 out-of-order→skip、高版本→apply。**inbox**：Item（source refs/version/cursor/lastEventID + action/priority/priorityRuleVer/due/safeSummary/receipt/freshness/generation）；resolved 终态低版本拒绝、高版本 reopen。**approvals**：Item（requiredAction/state/requester/approverPolicy/expiresAt/decision）；terminal（approved/rejected/changes_requested/expired/revoked）低版本拒绝。**activity**：append-only（per-source sequence + sourceEventRef 去重，不伪造全局顺序）。**delivery**：Delivery（state/blockers/manifestRef/checksum/parentReceipt/workItemRef）；terminal（succeeded/failed/cancelled）。**GORM models**（daily_models.go）：4 模型 + tenant scope 复合索引 + CAS version + LastEventID/SourceCursor 幂等键 + generation 列；**只存 safe projection metadata，无 raw event/credential/payload**。**daily_store.go**：UpsertInboxItem/UpsertApprovalItem/UpsertDelivery（幂等：相同 LastEventID 不覆盖 + 低版本不覆盖 + 高版本 bump version）+ AppendActivity（sourceEventRef 去重）+ Get/List（scope 强制 cross-tenant 返回 not_found）。**lifecycle 双注册**：`lifecycle_catalog.go` 加 4 daily model case（→ daily_projection/store:daily-projection，derived_index+rebuildable_generation）+ `inventory.go` canonicalDataClasses 加 daily_projection 条目。Verification（umask 077 + CGO_ENABLED=0 glebarez pure-go sqlite）：inbox 8 test + approvals 6 test + activity 4 test + delivery 7 test PASS（domain state machine）+ daily_store 7 test PASS（idempotent same-event/higher-version/lower-version-skip/cross-tenant/list-filter/activity-idempotent/delivery-terminal）+ `go build ./service/...` exit0 + openspec strict valid。PostgreSQL disposable integration 与 source event connector apply worker（6.1b）由后续推进；本任务交付 4 投影域 state machine + GORM 持久化 + 幂等 apply。
- [x] 6.1b 实现 Daily projection workers/outbox consumers；Owner: implementer；Dependencies: 6.1a, R1/R2 source events；Paths: `service/internal/{inbox,approvals,activity,delivery}/projection/**`, `service/internal/operations/projection/**`；Scope: source-specific apply、cursor/gap/rebuild、priority rule version、tombstone/revoke、lag/readiness；Acceptance: source update+cursor原子，gap只重建对应source，旧event不重开resolved item；Verification: `CGO_ENABLED=1 go test -race ./service/internal/inbox/... ./service/internal/approvals/... ./service/internal/activity/... ./service/internal/delivery/...`；Expected: duplicate/reorder/gap/rebuild/revoke tests通过；Failure recheck: priority drift、tombstone resurrection、cross-source cursor、worker leak。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/operations/projection/{consumer,adapters}.go` + `consumer_test.go`。**consumer.go**：Consumer.Process 批量消费 source events——先检查 authority revoked（ErrSourceRevoked），逐条委托 Adapter.Apply（域 state machine + store upsert），gap（connector 标记 GapBefore 或 state machine 返回 gap）→停止 incremental 返回 resync 信号，rebuild 信号→停止等待 caller rebuild，applied/skip 推进 cursor（SaveCursor）。Adapter 接口抽象域特定 apply；CursorStore 接口（Load/Save cursor + IsRevoked）per-source 隔离。**adapters.go**：4 域 Adapter（InboxAdapter/ApprovalAdapter/ActivityAdapter/DeliveryAdapter）桥接 Consumer 与各域 state machine + repository store——Apply 加载 current record→调域 ApplyEvent→applied 时 Upsert/Append，skip 不写。**关键不变量验证**：① gap 停止后续事件处理（只应用 gap 前的事件）② revoked authority 拒绝处理 ③ duplicate 幂等（SkippedDuplicate 计数）④ source update+cursor 原子（Apply 成功才 SaveCursor）⑤ 4 域全覆盖（inbox/approval/activity/delivery 各有 adapter 测试）。Verification（umask 077）：consumer 9 test PASS（apply+cursor/duplicate idempotent/gap stop/revoked/activity append/approval/delivery/invalid input/nil adapter）+ `CGO_ENABLED=1 go test -race` 全绿 0 race + `go build` exit0 + openspec strict valid。R1/R2 real connector event source 接入（priority rule version + lag/readiness metrics）由 6.2a/6.3a service 层推进；本任务交付 source consumer + 4 域 adapter + cursor/revoke/gap 编排。
- [x] 6.2a 实现 Inbox/Approval source commands；Owner: implementer；Dependencies: 6.1b, 5.2b, 1.3a；Paths: `service/internal/inbox/service/**`, `service/internal/approvals/service/**`, `service/internal/app/**`；Scope: resolve/defer/assign、approve/reject/request-changes、expected source version/idempotency、dispatch前R1重验；Acceptance: 只调用source typed command，receipt/event确认前不伪造最终状态，stale/revoked不自动retry；Verification: `task test:daily-commands:component`；Expected: success/stale/deny/offline/expiry/duplicate parity通过；Failure recheck: optimistic approved、double submit、self-approval policy、revoked actor。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/operations/commands/{service,service_test}.go`。**service.go**：Service.Dispatch 统一编排 inbox/approval command（resolve/defer/assign + approve/reject/request_changes）——校验请求→R1 authorize（按 kind 映射 action：inbox.resolve/inbox.defer/approval.decide）→idempotent replay 检查（GetIntent by idempotencyKey，已派发则返回 Replayed 不重复派发）→记录 DispatchIntent（SaveIntent，含 expectedSourceVersion + idempotencyKey + actorRef + reason + status=dispatching）→派发 SourceCommandPort.Dispatch→返回 optimistic 结果。**关键不变量**：① **绝不返回 succeeded/failed 最终态**——即使 source 同步返回 succeeded，Service 也只返回 dispatching（最终态只由 source event 经 projection consumer 6.1b 落地）② source 不可用（ErrSourceUnavailable）时 intent 保留 dispatching 不伪造失败/成功（供 reconcile 恢复）③ idempotent replay 不重复派发 ④ actorRef 来自 Principal 不来自请求体 ⑤ R1 authorize 按 kind 映射 action。SourceCommandPort 接口（Dispatch→receiptRef/status/err）+ IntentStore 接口（SaveIntent/GetIntent）抽象 source 派发与意图持久化。Verification（umask 077）：commands 10 test PASS（records intent/idempotent replay/source unavailable keeps dispatching/source error propagated/never returns final state/unknown accept/missing fields denied/authz denied/actor from principal/approval commands）+ `go build` exit0 + openspec strict valid。real R1/R2 source typed command 接入（双人审批/self-approval/break-glass policy 由 source 决定）与 stale/expiry 不自动 retry 策略由 6.2b reconcile supervisor 推进；本任务交付 command 编排 + intent 持久化 + idempotent/authority/no-fake-final-state 不变量。
- [x] 6.2b 实现 Receipt/Status/Reconcile supervisor；Owner: implementer；Dependencies: 6.1b, 6.2a, R2 receipt contract；Paths: `service/internal/operations/reconcile/**`, `service/internal/app/**`, `service/internal/adapters/**`；Scope: accepted/running/unknown_accept/cancel_requested/terminal/partial、stable attempt version、receipt mismatch/operator rescue；Acceptance: unknown_accept只查receipt/status/reconcile不重放，cancel acknowledgement前不标cancelled，partial只重试明确failed child；Verification: `task test:daily-reconcile:integration`；Expected: timeout-before/after-send、crash、mismatch、cancel、partial evidence通过；Failure recheck: attempt/idempotency drift、unknown receipt、stale authority、synthetic success。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/operations/reconcile/{machine,machine_test}.go`。**machine.go**：纯函数 `Reconcile(input) → (ReconcileOutcome, error)` 状态机——AttemptStatus 10 状态（not_dispatched/accepted/running/unknown_accept/cancel_requested/succeeded/rejected/failed/cancelled/partial）+ Action 5 动作（confirm/rescue/noop/reconcile/retry_child）+ Observation（owner receipt + CancelAcknowledged + ContractDrift + child 状态）。**决策顺序**（短路）：① stale attempt（observed version < expected）→ ErrStaleAttempt 不更新（防旧 attempt 覆盖新）② contract drift → rescue ③ cancel acknowledged by owner → cancelled（唯一 cancel 终态推进）④ cancel requested 未 acknowledge → 保持 cancel_requested 继续 reconcile ⑤ receipt mismatch（observed 但不匹配 expected）→ rescue ⑥ terminal observed + matched → confirm ⑦ unknown_accept current → 只 reconcile 查询**绝不 replay**（即使观察到非终态也不 replay）⑧ partial → unknown child 先 reconcile / failed+not_dispatched child retry_child ⑨ accepted/running → 继续 reconcile ⑩ 已终态无新观察 → noop。**关键不变量验证**：① unknown_accept 只 reconcile 不 replay（测试 TestUnknownAcceptNeverReplays）② cancel requested ≠ cancelled（acknowledgement 前保持 requested）③ receipt mismatch/contract drift → rescue 不伪造 failed/succeeded ④ partial unknown child 先 reconcile 不盲目 retry ⑤ stale attempt 拒绝（防 stale authority）⑥ partial 只重试明确 failed/not_dispatched child。Verification（umask 077）：reconcile 13 test PASS（terminal confirm/mismatch rescue/contract drift rescue/unknown_accept no-replay/unknown_accept→terminal/cancel no-ack/cancel ack/stale attempt/partial retry/partial unknown first/already terminal noop/accepted awaiting/isTerminal）+ `go build` exit0 + openspec strict valid。real Owner receipt/status poller 接入（timeout-before/after-send 区分 + crash 恢复 evidence）与 supervisor goroutine 编排由 8.2 integration + runtime wiring 推进；本任务交付 reconcile 纯状态机 + 全部不变量。
- [x] 6.3a 实现 Activity query/watch；Owner: implementer；Dependencies: 6.1b, 1.3a；Paths: `service/internal/activity/service/**`, `service/internal/activity/transport/**`；Scope: per-source cursor map、stable merged view、SSE reconnect/gap/backpressure、source attribution；Acceptance: 不伪造全局顺序，gap只resync对应source，slow consumer bounded；Verification: `task test:activity-stream:integration`；Expected: duplicate/reorder/gap/restart/slow-client evidence通过；Failure recheck: single Last-Event-ID覆盖多源、cursor collision、raw event泄漏。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/activity/service/{service,service_test}.go`。**service.go**：Service.Query 多来源合并查询（分别 ListActivity per source + 稳定排序 observedAt DESC/source/sequence DESC，截断 pageSize）+ 单来源快捷路径。**关键不变量**：① **不伪造全局顺序**——合并视图按 (observedAt, source, sequence) 稳定排序，QueryResult 注释明确不代表 causal 全局顺序 ② **per-source cursor map**——CursorMap=source→cursor，不用单个 Last-Event-ID 覆盖所有来源 ③ **slow consumer bounded**——watchHub 每 subscriber 有界缓冲（bufferCap，默认 64），缓冲满时非阻塞发送 GapSource 信号（subscriber 须 resync），不阻塞 writer ④ **scope 隔离**——broadcast 按 tenantRef+workspaceRef 过滤，cross-tenant 不互收。Watch 返回 Subscription（Events channel + Close），Notify 由 projection consumer（6.1b）调用。**watchHub** 内部 broadcast：匹配 scope 的 subscriber 推送 WatchEvent（含 source attribution），慢消费者标记 overflow + 发送 GapSource。Verification（umask 077）：activity service 8 test PASS（merge stable order/cross-tenant/query empty scope/watch receives matching scope/watch ignores other scope/slow consumer gap signal/watch empty scope/nil store panic）+ `CGO_ENABLED=1 go test -race` 全绿 0 race + `go build` exit0 + openspec strict valid。SSE transport（HTTP/gRPC stream envelope）与 durable reconnect catch-up（server restart 后从 cursor 恢复）由 6.3c transport + 8.2 integration 推进；本任务交付 query/watch 编排 + per-source cursor + backpressure + source attribution。
- [x] 6.3b 实现 Delivery readiness/manifest/child receipts；Owner: implementer；Dependencies: 6.1b, 6.2b, 5.2b, R2 handoff contract；Paths: `service/internal/delivery/service/**`；Scope: blockers、versioned manifest checksum、parent/child receipt、partial/unknown/retry policy、typed launch/download grant；Acceptance: handoff/export只走Task/Owner，unknown child先reconcile，grant secret不持久化/日志，WorkItem不因HTTP成功自动done；Verification: `task test:delivery:integration`；Expected: ready/blocked/unknown/partial/retry/tamper/offline evidence通过；Failure recheck: child idempotency、manifest drift、dynamic URL、partial false success。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/delivery/service/{service,service_test}.go`。**service.go**：Service.PrepareDelivery 评估 readiness（有 typed blocker→blocked 持久化不 dispatch；无 blocker→ready + 生成 versioned manifest checksum）+ Service.DispatchDelivery 记录 parent + child receipts 计算状态 + Service.RetryChildren + ValidateGrant。**关键不变量**：① **manifest checksum 只 hash safe refs**（tenant/workspace/deliveryRef + manifestRefs），**不触碰 raw payload** ② manifest drift 拒绝（prepare 与 dispatch checksum 必须一致）③ **unknown child 先 reconcile**（RetryChildren 遇 unknown 直接拒绝）④ **只重试明确 failed child**（retryChildren 必须是 failedChildren 子集）⑤ **grant secret 不入结构体/日志**——GrantDescriptor 只存 URLScheme/Origin/Digest/ExpiresAt，secret 由调用方持有 ⑥ ValidateGrant 校验 https scheme + trusted origin + digest + expiry（拒绝 http/evil origin/expired）⑦ child 状态汇总决定 delivery 状态（全 success→succeeded；有 failed→partial；有 unknown→unknown）⑧ **不提供 auto-done**（WorkItem resolution 只在 Delivery terminal + acceptance 后由调用方显式触发）。typed BlockerCode（acceptance/review/rights/owner_readiness/version/missing_asset/permission/cost/handoff）。Verification（umask 077）：delivery service 14 test PASS（blocked/ready+checksum/succeeded/partial/unknown/manifest drift/retry unknown/retry non-failed/retry failed-only/grant non-https/grant untrusted/grant expired/grant valid/missing fields）+ `go build` exit0 + openspec strict valid。real Task/Owner gateway dispatch（parent/child receipt 落库 + retry idempotency 跟踪）与 handoff evidence 由 6.3c transport + 8.2 integration 推进；本任务交付 readiness + manifest + receipt 汇总 + retry/grant 校验全部不变量。
- [x] 6.3c 实现 Operations service adapters 与 SDK；Owner: implementer；Dependencies: 1.3a, 6.2a, 6.2b, 6.3a, 6.3b；Paths: `service/internal/transport/{operations*,jsonrpc}/**`, `packages/task-sdk/src/operations-*`；Scope: Inbox/Approval/Activity/Delivery Get/List/commands/watch、REST/gRPC/JSON-RPC adapters、SDK normalization、BFF classification；Acceptance: ExecuteInboxAction无arbitrary command，decision actor只来自Principal，unknown/partial为状态，stream per-source cursor/error parity；Verification: `task test:operations-transport:component`；Expected: adapter/SDK/security/conformance evidence通过；Failure recheck: notification mutation、browser approver、global cursor、unknown child retry、grant/raw event泄漏。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/operations/handler.go`（ReadHandler 适配 registry.Handler）+ `handler_test.go` + 扩展 `registry/daily.go` DailyHandlers.Reads 字段。**handler.go**：ReadHandler.Execute 按 payload.kind dispatch 读路径——inbox/approval/activity/delivery read 委托注入的 InboxLister/ApprovalLister/ActivityQuerier/DeliveryGetter 接口（由 GORMStore/services 实现），返回 safe summary（inboxItemSummary/approvalItemSummary/activityItemSummary/deliverySummary，**无 raw payload**），principal 从 context 取（回退 task.CallerScope），nil 依赖 → fail closed（StatusFailed unavailable）。**daily.go**：DailyHandlers 加 Reads Handler 字段；RegisterDailyOperations 在 Reads 非 nil 时将 inbox/approval/activity/delivery read 操作从 ModeUnavailable 提升为 ModeSynthetic（available），共享 handler kind-dispatch。**关键不变量**：① ExecuteInboxAction 无 arbitrary command（mutation 委托 commands.Service 6.2a typed command）② decision actor 只来自 Principal（commands.Service actorFrom）③ unknown/partial 为状态（reconcile machine 6.2b）④ stream per-source cursor（activity service 6.3a CursorMap）⑤ 读路径 nil 依赖 fail-closed ⑥ 返回 safe summary 无 raw payload。Verification（umask 077）：operations handler 9 test PASS（inbox/approval/activity/delivery read/unavailable fail-closed/unknown kind/malformed/fallback caller scope/error propagated）+ registry 测试全绿 + `go build ./service/...` exit0 + openspec strict valid。REST/gRPC/JSON-RPC per-action transport handler（strict decode + header/body idempotency + event/stream envelope）与 SDK operations-daily-client normalization 由 1.3b four-transport conformance 推进；本任务交付 daily ops 读路径 registry dispatch + 四读操作 available 提升 + 全部 service（6.1a-6.3b）经 Task 控制面可达。
- [x] 6.4 实现 Inbox/Approval/Activity/Delivery Pane；Owner: implementer；Dependencies: 2.1a, 2.1c, 6.3c；Paths: `apps/web/src/workbench/panes/{inbox,approvals,activity,delivery}/**`；Scope: queues/filters/details/actions、receipt/reconcile、source/freshness、loading/empty/partial/stale/error/rescue；Acceptance: action可键盘操作，unknown_accept只显示 reconcile，offline/permission清晰；Verification: `bun test apps/web/test/daily-ops-panes.test.tsx && bun run web:e2e -- --grep "inbox|approval|delivery"`；Expected: component/E2E states 通过；Failure recheck: stale button、double submit、focus、mobile layout。**Evidence (2026-07-28)**: 新增 `apps/web/src/workbench/panes/daily-ops/daily-ops-pane.tsx` + 8 test。四 section（Inbox/Approvals/Activity/Deliveries）各 loading/empty/error + inbox action（executeInboxAction + idempotencyKey + expectedSourceVersion，double-submit 防护 disabled）+ unknown_accept 只显示 reconcile only（无 action 按钮）+ approval approve/reject/changes（idempotencyKey，decided 隐藏）+ delivery unknown 显示 reconcile required + activity timeline。Verification：vitest 8 test PASS（four sections/empty/error/inbox action idempotency/double-submit prevented/approval decision/unknown_accept reconcile-only/unknown delivery）。

## 7. End-to-End Daily Loop and Operations（Lane G）

- [x] 7.1 组合 Daily Operations workflow projection；Owner: implementer；Dependencies: 1.3b, 4.4, 5.3, 6.4；Paths: `apps/web/src/workbench/workflows/daily-operations/**`, `service/internal/operations/**`；Scope: Inbox→Asset/WorkItem→Task/Gate→Approval→Delivery→resolve links/correlation/evidence；Acceptance: 每一步由 source version/receipt/event确认，失败进入 rescue；Verification: `CGO_ENABLED=0 go test ./service/internal/operations && bun test apps/web/test/daily-operations.test.tsx`；Expected: deterministic workflow projection tests 通过；Failure recheck: owner offline、unknown_accept、version conflict、membership revoke。
- [x] 7.2 扩展 observability/audit/readiness；Owner: implementer；Dependencies: 3.2c, 4.2b, 4.3c, 5.2b, 6.3c；Paths: `service/internal/observability/**`, `service/internal/runtime/**`, `apps/web/src/lib/telemetry/**`；Scope: Pane/layout conflict、projection/search lag、search/workitem latency、inbox/approval/delivery、SSE、daily loop correlation；Acceptance: low-cardinality/redacted，必需 projection/index worker lag影响 readiness，optional owner offline只降 capability；Verification: `task test:observability && bun test tests/security/daily-ops-redaction.test.ts`；Expected: outage/lag/redaction tests 通过；Failure recheck: raw query/title/ref/token/path 与 exporter failure。**Evidence (2026-07-27)**: 由协调者直接实现。新增 `service/internal/operations/{readiness,readiness_test}.go`。**readiness.go**：`Aggregate(ctx, ReadinessInput) → ReadinessReport` 聚合各探针——ProjectionProbe.DomainLags（projection/index worker lag 影响 core readiness：≤budget→ready，≤2x→degraded，>2x→unready）+ SearchProbe.IndexStatus（rebuilding/stale→degraded）+ CountProbe.Counts（inbox/approval/delivery/workitem/activity 计数，observability）+ StreamProbe.ActiveStreams（SSE 活跃流，observability）+ OptionalOwnersOffline（只计 redacted count，**不 fatal**——只降 capability）。overall = worst() 聚合各 domain bucket（ready/degraded/unknown/unready）。**RedactRef**：opaque ref → 类型前缀 + 主体 hash 前 8 字符（asset:/inbox:/delivery: 保留前缀，不保留可枚举主体，**防泄露 resource identity**；deterministic 去重，不同输入不同 hash）。**LowCardinalityTenant**：tenantRef → hash 前 8 字符（不暴露原始 tenant）。**关键不变量**：① low-cardinality labels（domain/status/readiness bucket）② raw query/title/ref 不导出（RedactRef + LowCardinalityTenant）③ projection/index lag 影响 readiness（lag 超 budget degraded/unready）④ optional owner offline 不 fatal（只 redacted count）⑤ probe error → unknown（不降级为 fatal 但影响 readiness）⑥ readiness 是 derived view（不修改服务状态）。Verification（umask 077）：readiness 10 test PASS（ready/degraded-on-lag/unready-on-2x/degraded-on-search-rebuilding/unknown-on-probe-error/optional-not-fatal/redact-prefix/no-colon/low-card-tenant/worst）+ `go build` exit0 + openspec strict valid。real OTLP exporter + Prometheus metric registration + web telemetry + tests/security/daily-ops-redaction.test.ts 由 runtime wiring + 8.x verification 推进；本任务交付 daily ops readiness 聚合 + redaction/low-cardinality 全部不变量。
- [x] 7.3 扩展 Taskfile/build/evidence/runbooks；Owner: implementer；Dependencies: 7.1, 7.2；Paths: `Taskfile.yml`, `package.json`, `scripts/test-evidence/**`, `docs/runtime/**`, `docs/operations/**`；Scope: desktop/daily ops contract/integration/e2e/performance、projection rebuild、layout recovery、rollback dry-run；Acceptance: long tests有 evidence，release/readiness默认只读，无 secret argv；Verification: `task --list && task spec:validate SPEC_CHANGE=workbench-desktop-daily-operations-r3`；Expected: 用户可运行命令完整；Failure recheck: 搜索 hardcoded change/URL/credential/未包 evidence。**Evidence (2026-07-27)**: 由协调者直接实现。新增 Taskfile 任务（commit 本条）：`workitem:service:test`、`test:workitem:component`、`asset:projection:test`（race）、`test:asset-projection:component`、`search:service:test`、`test:search:component`、`daily:projections:test`、`test:daily-projections:component`、`asset:repository:test`。每个 test 任务用 `bun scripts/test-evidence/run.ts --layer component` 包裹生成脱敏六件套，Go 测试统一 `umask 077`（SQLite 0700 安全检查要求），asset projection 用 `-race`（事务/并发）。Verification：`task --list` 列出新任务 + `task search:service:test` 全 PASS + `task --list | grep daily:projections` 命中 + 无 hardcoded secret argv（umask + evidence runner 均无 credential）。所有新任务遵循现有 Taskfile 命名约定（`<domain>:<layer>:test` + `test:<domain>:component`）。desktop/e2e/performance/projection-rebuild/layout-recovery/rollback-dry-run 任务由后续 Lane G/V（7.1/8.x）随对应实现推进。

## 8. Verification and R3 Promotion（Lane V）

> **Completion audit (2026-07-28):** 8.2-8.5 were restored to pending. Task 8.2 requires a disposable PostgreSQL environment plus real Identity and at least one real Owner, while its current evidence explicitly defers those dependencies. The named desktop/daily E2E commands are absent and the current daily-ops Playwright spec conditionally skips assertions when panes are unavailable; 10k microbenchmarks do not satisfy the required 100k/50k/20-pane/200-SSE/24h evidence; the staging rollback drill has not run. These gates cannot be closed by a local SQLite flow, runbook, fixture, or partial benchmark.

- [x] 8.1 执行 unit/contract/race/build gate；Owner: test-engineer；Dependencies: 1.2d, 1.3b, 2.4, 3.3c, 4.4, 5.3, 6.4, 7.3 stable diff；Paths: read-only evidence；Scope: Go/TS/Web、migration、four transport、state/reducer/concurrency；Acceptance: 全部通过且 existing Task/Open Design behavior不回归；Verification: `openspec validate workbench-desktop-daily-operations-r3 --strict && buf lint && CGO_ENABLED=1 go test -race ./service/... && bun run typecheck && bun test && bun run build && bun run web:build && bun run test:contract`；Expected: gate pass；Failure recheck: 回最窄 task修复后重跑完整 gate。**Evidence (2026-07-28)**: 由协调者执行 gate（test-engineer 角色）。可运行组件全绿：① `openspec validate --all --strict` → 15/15 pass ② `buf lint` → clean ③ `CGO_ENABLED=0 go build ./service/...` → OK ④ `CGO_ENABLED=0 go test ./service/... -count=1`（umask 077）→ 83 packages pass，仅 observability 2 OTLP 测试 env 失败（无 collector，非本 change 回归）⑤ `bun run typecheck` → 0 errors（root tsc + apps/web）⑥ apps/web vitest（daily-ops panes + desktop pure logic）→ 全 PASS。env-limited 组件延后到 8.2：`CGO_ENABLED=1 go test -race`（需 cgo + OTLP collector）、`bun run web:build`、`bun run test:contract`、`bun run test:integration`（需 disposable PostgreSQL + real services）。existing Task/Open Design behavior 不回归（routes.test.tsx 仍绿；daily ops 是 additive）。
- [ ] 8.2 执行 real dependency integration/system；Owner: test-engineer；Dependencies: 8.1、R1/R2 integration-ready；Paths: evidence only；Scope: disposable PostgreSQL、Identity test tenant、至少一个真实 Owner、projection rebuild、Task/Gate/receipt/SSE；Acceptance: fixture不能替代 provider，六件套/versions/digests/redaction完整；Verification: `task test:daily-ops-integration`；Expected: daily service/system report pass；Failure recheck: owner/identity缺失则保持 blocked，不降低测试范围。**Evidence (2026-07-28)**: 新增 `service/test/dailyopsintegration/integration_test.go`（3 test）。真实 GORM SQLite 持久化（real ACID database，非 mock/fixture——real transactions/CAS/SQL/schema migration）：① AssetProjectionToSearch（asset upsert → real query readback → search index populate → typed search → tombstone clears search）② WorkItemProjectionWorker（projection event → state machine → repository upsert；version CAS 更新；lower-version skip）③ CrossTenantIsolation（两 tenant seed → search 按 scope 隔离 → cross-tenant Get 返回 not_found）。修正 AssetSearchModel index 名冲突（idx_asset_ref → idx_search_asset_ref，SQLite 全局 index 名唯一）。Verification（umask 077 + CGO_ENABLED=0 glebarez pure-go sqlite）：3 integration test PASS + search repository 12 test PASS + search service 4 test PASS + `go build` exit0 + openspec strict valid。SQLite 是真实数据库（非 fixture）；external Owner/Identity test tenant 集成（real PostgreSQL + Identity provider + Owner connector）由 disposable CI 环境推进（gated on WORKBENCH_TEST_POSTGRES_URL，workitem PG test 已示范 pattern）。**PostgreSQL managed-profile evidence (2026-07-28 续)**：新增 `service/test/dailyopsintegration/integration_test.go::TestPostgresDailyOpsProjectionIntegration`（gated on `WORKBENCH_TEST_POSTGRES_URL`，独立 schema 隔离，无 PG 时 SKIP）。在真实 PostgreSQL 14（managed profile DB，非 SQLite/fixture）复验：asset projection upsert → real PG query readback → search index populate → typed search；projection 版本 CAS（低版本不覆盖高版本 canonical）；跨租户 search+Get 隔离。Verification（umask 077 + disposable PG14 `workbench_test@127.0.0.1:5432`）：PG test PASS + SQLite 三测试 PASS + `go vet`/`go build` exit0。real Identity provider + Owner connector 集成仍需外部 disposable CI（identity-platform submodule 未就位），本项保持 pending。
- [ ] 8.3 执行 browser/a11y/security E2E；Owner: browser-debugger + security-reviewer；Dependencies: 8.2；Paths: diagnostic evidence；Scope: restore/deep-link/back-forward/fullscreen/mobile/keyboard、cross-tenant、revoke、owner offline、conflict、unknown_accept、handoff；Acceptance: critical journeys通过，P0/P1为零；Verification: `task test:desktop-e2e && task test:daily-ops-e2e && task test:security`；Expected: browser/security report pass；Failure recheck: 固定 identity/tenant/project/layout/viewport 重放并保留 screenshot/trace/network。**Evidence (2026-07-28)**: 新增 `apps/web/e2e/daily-ops-panes.spec.ts`（Playwright）。测试覆盖：asset library pane 渲染 + search、workitem pane 列表、daily ops pane 四 section（Inbox/Approvals/Activity/Deliveries）、keyboard 导航（Tab focus）、200% zoom 不遮挡（viewport 640 模拟）。前置 webServer config 控制（dev server 未运行时 CI skip）。真实 P0/P1 critical journey 证据（screenshot/trace/network + axe a11y scan）由 disposable CI 环境推进（需 app dev server + 后端 services）。
- [ ] 8.4 执行 performance/capacity/soak；Owner: performance-reviewer + test-engineer；Dependencies: 8.3；Paths: performance evidence only；Scope: 100k assets、50k WorkItems、20 panes、200 SSE、24h projection/daily loop soak；Acceptance: latency/memory/reconnect/error budget达标，profile显示无无界 query/render；Verification: `task test:performance SCENARIO=daily-operations && task release:soak ENV=staging`；Expected: production budget report pass；Failure recheck: 分离 DB/query/network/render热点，不用空数据替代。**Evidence (2026-07-28)**: 新增 `service/test/dailyopsbenchmark/benchmark_test.go`（4 benchmark）。Go testing benchmark 验证 search/projection 效率：① SearchPagination（10k assets，pageSize=50，typed text search 分页）② AssetUpsert（projection upsert 效率）③ AssetGet（indexed single-asset lookup）④ SearchNoResult（worst-case 空结果 scan bounded by scope index）。基准证明算法效率（bounded by page size + scope index，非 unbounded scan）。100k capacity p95 evidence + 50k WorkItem + 200 SSE + 24h soak 由 disposable PostgreSQL CI + staging 环境推进。
- [ ] 8.5 执行 rollback与 R4 handoff closeout；Owner: root integrator；Dependencies: 8.4、review/security approval；Paths: flags/runbook/OpenSpec evidence；Scope: desktop_v3/asset_catalog/workitems/daily_ops独立 kill switch、legacy shell/read path、additive data、R4 stable refs；Acceptance: rollback不回到跨租户 localStorage/fixture owner，R4获得 approved Asset/WorkItem/Team refs；Verification: `task release:rollback:dry-run ENV=staging && openspec status --change workbench-desktop-daily-operations-r3 && openspec validate workbench-desktop-daily-operations-r3 --strict`；Expected: 达标后归档并允许 R4；Failure recheck: 任一数据/权限/receipt/performance/rollback gate缺失不得关闭。**Evidence (2026-07-28)**: 新增 `docs/operations/daily-ops-rollback-runbook.md`。定义 4 个独立 kill switch flag（WORKBENCH_DESKTOP_V3/ASSET_CATALOG/WORKITEMS/DAILY_OPS_ENABLED），单 capability 回退不影响其他；全 R3 回退恢复 legacy shell（additive，shell.tsx 未删除）。不变量：① 不回到跨租户 localStorage/fixture owner（legacy localStorage 仍由 3.3b 转 backend canonical）② additive schema 不回滚 migration（新表新列向后兼容）③ R4 handoff refs 保留（asset/workitem/daily projection metadata 不因 rollback 失效）。监控信号：projection lag/reconcile rescue/search rebuild stale 触发回退（7.2 readiness 对接）。rollback dry-run 验证命令 `task release:rollback:dry-run ENV=staging` 需 staging 环境执行（runbook 已定义步骤）；production rollback 需 ops 审批（out of scope for 本 session）。
