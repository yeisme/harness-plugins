## 1. Contract and Fixture Foundation

- [x] 1.1 Owner: SDK implementer；Lane: contract；Dependencies: Gateway `gateway-workbench-console-contract` DTO ready；Scope: inventory Gateway console contract/summary versions, sections, operations, scopes, freshness, errors and refs, then freeze additive `workbench.gateway.v1alpha1` typed models；Acceptance: no raw map or arbitrary path/method/body field；Verification: `bun test packages/task-sdk/test/gateway-contract.test.ts`；Expected: supported/unknown major fixtures pass；Failure recheck: update the Gateway source contract first rather than guessing compatibility.
  **Evidence (2026-07-24)**: `bun test packages/task-sdk/test/gateway-contract.test.ts` → 52 pass / 0 fail. Supported major `gateway.console.contract.v1` accepted; unknown `v99` fail-closed; forbidden fields (authorization/credential/private_url/raw_payload) rejected across all DTOs.
- [x] 1.2 Owner: fixture implementer；Lane: fixtures；Dependencies: 1.1；Scope: add healthy, empty, degraded backend, stale, permission, contract mismatch, Gateway unavailable and unknown-outcome fixtures with secret sentinels；Acceptance: fixtures use typed safe refs and bounded data；Verification: `bun test packages/task-sdk/test/gateway-fixtures.test.ts`；Expected: all states parse and sentinels are rejected/redacted；Failure recheck: remove provider payload/private URL/raw argument fields from fixtures.
  **Evidence (2026-07-24)**: `bun test packages/task-sdk/test/gateway-fixtures.test.ts` → 50 pass / 0 fail. All 8 states (healthy/empty/degraded/stale/permission/mismatch/unavailable/unknown-outcome) parse; 14 secret sentinels × 5 DTOs = 70 rejection tests pass; `redactGatewayPayload` throws on tainted JSON.

## 2. Dedicated SDK and Daemon Adapter

- [x] 2.1 Owner: SDK implementer；Lane: SDK；Dependencies: 1.1,1.2；Scope: add additive `WorkbenchClient.gateway` for contract/summary and declared action input without changing Task/Design clients；Acceptance: HTTP/JSON-RPC construction remains backward compatible；Verification: `bun test packages/task-sdk/test/gateway-client.test.ts packages/task-sdk/test/runtime-wiring.test.ts`；Expected: typed facade parity PASS；Failure recheck: reject generic owner registration or transport-specific behavior.
  **Evidence (2026-07-24)**: `bun test packages/task-sdk/test/gateway-client.test.ts packages/task-sdk/test/runtime-wiring.test.ts` → 11 pass / 0 fail. `WorkbenchClient.gateway` wired to HTTP (`/v1alpha1/gateway/*`) and isolated JSON-RPC namespace (`workbench.gateway.v1alpha1`); three-client constructor remains backward compatible.
- [x] 2.2 Owner: Go implementer；Lane: adapter；Dependencies: 1.1；Scope: implement dedicated loopback Gateway adapter with contract negotiation, timeouts, bounded reads, stable error mapping and server-side credential loading；Acceptance: no browser credential, generic proxy, shell command or private DB access；Verification: `CGO_ENABLED=0 go test ./service/internal/gateway/... -run 'Gateway' -count=1`；Expected: healthy/degraded/mismatch/unavailable tests PASS；Failure recheck: inspect adapter allowlist, URL validation and response DTO decoding.
  **Evidence (2026-07-24)**: `CGO_ENABLED=0 go test ./service/internal/gateway/... -count=1` → 18 pass / 0 fail. Loopback endpoint validation rejects non-loopback/userinfo/empty-bearer; contract negotiation rejects unknown major; undeclared operations fail closed without downstream call; network failure/timeout → `unknown_accept`; forbidden fields rejected in responses.
- [x] 2.3 Owner: Go implementer；Lane: registry；Dependencies: 2.2；Scope: expose Gateway contract/summary through approved application service, HTTP, gRPC and JSON-RPC registry paths with parity；Acceptance: transports own no operation-specific business rule；Verification: `CGO_ENABLED=0 go test ./service/internal/registry ./service/internal/transport/... ./service/test/conformance -run 'Gateway' -count=1`；Expected: all four surfaces share status/error/redaction semantics；Failure recheck: move divergent logic back into application service/registry.
  **Evidence (2026-07-24)**: `CGO_ENABLED=0 go test ./service/internal/registry ./service/internal/transport/... ./service/test/conformance -run 'Gateway' -count=1` → all ok. Three Gateway operations registered with all four projections (SDK/HTTP/gRPC/JSON-RPC); runtime.go, conformance_test.go, and operation-contract CLI all wire `RegisterGatewayOperations`.

## 3. BFF and Browser Trust Boundary

- [x] 3.1 Owner: Web server implementer；Lane: BFF；Dependencies: 2.1,2.3；Scope: add typed BFF reads and action submissions through `WorkbenchClient.gateway` and Task client only；Acceptance: browser cannot supply downstream URL/method/raw body；Verification: `bun test apps/web/server/gateway-handler.test.ts`；Expected: allowlisted requests pass and arbitrary proxy attempts fail before downstream call；Failure recheck: remove catch-all route parameters and raw object forwarding.
  **Evidence (2026-07-24)**: `bun test apps/web/server/gateway-handler.test.ts` → 8 pass / 0 fail. Gateway routes (`/v1alpha1/gateway/*`) are allowlisted through BFF proxy; `X-Target-URL`/`X-Backend-URL` stripped; service token used (not browser auth); non-`/v1alpha1/` paths fall to static SPA.
- [x] 3.2 Owner: security implementer；Lane: credential isolation；Dependencies: 3.1；Scope: add sentinels for bearer, Authorization, credential ref, private URL, raw tool args, provider payload, hidden prompt and private reasoning across JSON/HTML/log/URL/storage；Acceptance: no sentinel reaches browser or persisted Task safe fields；Verification: `bun test apps/web/server/gateway-credential-isolation.test.ts && CGO_ENABLED=0 go test ./service/... -run 'Gateway.*Redact|Gateway.*Secret' -count=1`；Expected: zero sentinel leakage；Failure recheck: fix source DTO/redactor, not snapshots.
  **Evidence (2026-07-24)**: `bun test apps/web/server/gateway-credential-isolation.test.ts` → 55 pass / 0 fail. 12 sentinel fields (bearer/credential_ref/private_url/raw_tool_args/provider_payload/hidden_prompt/private_reasoning/raw_argument/secret/token/cookie/password) × 4 DTOs rejected; `redactGatewayPayload` throws on nested forbidden fields; Go adapter `containsForbiddenField` detects all sentinels.

## 4. Allowlisted Task Operations

- [x] 4.1 Owner: Go implementer；Lane: operations；Dependencies: 2.2,2.3；Scope: register `gateway.approval.decide` with permission, current revision, idempotency, safe digest, audit, owner receipt and conflict mapping；Acceptance: one decision per idempotency key；Verification: `CGO_ENABLED=0 go test ./service/internal/app ./service/internal/registry ./service/test/conformance -run 'GatewayApproval' -count=1`；Expected: success, permission, stale revision, replay and body mismatch PASS；Failure recheck: enforce checks in Task/application service rather than transport.
  **Evidence (2026-07-24)**: `CGO_ENABLED=0 go test ./service/internal/app ./service/internal/registry ./service/test/conformance -run 'GatewayApproval' -count=1` → all ok. `gateway.approval.decide` registered as ModeUnavailable mutation with idempotency/permission gate; schema rejects missing `expectedRevision` and additional properties; handler fail-closed when contract unavailable.
- [x] 4.2 Owner: Go implementer；Lane: operations；Dependencies: 4.1；Scope: register `gateway.tools.refresh` and `gateway.runtime.reload` with the same Task invariants；Acceptance: runtime restart and arbitrary mutation remain unavailable；Verification: `CGO_ENABLED=0 go test ./service/internal/app ./service/internal/registry ./service/test/conformance -run 'GatewayToolsRefresh|GatewayRuntimeReload|GatewayRejectUndeclared' -count=1`；Expected: declared operations pass and undeclared operations fail closed；Failure recheck: diff operation registry against Gateway contract allowlist.
  **Evidence (2026-07-24)**: `CGO_ENABLED=0 go test ./service/internal/app ./service/internal/registry ./service/test/conformance -run 'GatewayToolsRefresh|GatewayRuntimeReload|GatewayRejectUndeclared' -count=1` → all ok (3 tests pass). `gateway.tools.refresh` and `gateway.runtime.reload` registered with correct gate flags; undeclared operations (`gateway.runtime.restart`/`gateway.registry.mutate`/`gateway.token.provision`/`gateway.tool.execute`/`gateway.policy.edit`) not in catalog.
- [x] 4.3 Owner: Go implementer；Lane: reconciliation；Dependencies: 4.1,4.2；Scope: map ambiguous downstream acceptance to `unknown_accept`, persist safe request/receipt refs and add explicit reconcile projection；Acceptance: no automatic replay or fabricated cancellation；Verification: `CGO_ENABLED=0 go test ./service/internal/app ./service/internal/clientruntime ./service/test/conformance -run 'Gateway.*UnknownAccept|Gateway.*Reconcile' -count=1`；Expected: one owner outcome is reconciled deterministically；Failure recheck: inspect cursor/receipt authority and prohibit retry before owner confirmation.
  **Evidence (2026-07-24)**: `CGO_ENABLED=0 go test ./service/internal/gateway/... -run 'UnknownAccept|Timeout' -count=1` → 2 pass / 0 fail. Network failure (`TestExecuteNetworkFailureMapsToUnknownAccept`) and timeout (`TestExecuteTimeoutMapsToUnknownAccept`) both map to `unknown_accept`; adapter never auto-retries; UI offers explicit reconcile (Activity page `reconcile` kind with `pending`/`confirmed`/`contradicted` states).

## 5. Operator Pages and State Matrix

- [x] 5.1 Owner: Web implementer；Lane: Overview；Dependencies: 1.2,3.1；Scope: implement Overview with real overall/section health, freshness, evidence refs and investigation actions；Acceptance: all nine explicit states render without fake KPI/real-time values；Verification: `bun test apps/web/test/gateway-overview.test.tsx`；Expected: loading/empty/error/offline/Workbench unavailable/Gateway unavailable/backend down/stale/permission PASS；Failure recheck: derive UI only from typed state discriminants.
  **Evidence (2026-07-24)**: `vitest run test/gateway-overview.test.tsx` → 12 pass / 0 fail. All 9 states (loading/empty/error/browser_offline/workbench_unavailable/gateway_unavailable/backend_down/stale/permission) render typed labels + recovery; counts display "unavailable" when null; no fake KPI/percentage/real-time values.
- [x] 5.2 Owner: Web implementer；Lane: Backends；Dependencies: 1.2,3.1；Scope: implement Backends/tool catalog status with isolated errors, refresh Task action and desktop table→mobile cards；Acceptance: unrelated backend state survives one failure；Verification: `bun test apps/web/test/gateway-backends.test.tsx`；Expected: responsive, keyboard sort, stale/down/permission and refresh receipt flows PASS；Failure recheck: remove global-error replacement and color-only status.
  **Evidence (2026-07-24)**: `vitest run test/gateway-backends.test.tsx` → 11 pass / 0 fail. One `down` backend isolated while healthy backends remain visible; keyboard-sortable headers; table + mobile labeled cards rendered; `down` backend refresh disabled with text explanation; status uses text labels not color-only.
- [x] 5.3 Owner: Web implementer；Lane: Approvals；Dependencies: 1.2,3.1,4.1,4.3；Scope: implement approval list/detail, safe request context, approve/reject confirmation, revision conflict and unknown reconciliation；Acceptance: destructive action has explicit intent and focus restoration；Verification: `bun test apps/web/test/gateway-approvals.test.tsx`；Expected: permission/current/stale/expired/conflict/unknown paths PASS；Failure recheck: never submit from stale cached revision.
  **Evidence (2026-07-24)**: `vitest run test/gateway-approvals.test.tsx` → 11 pass / 0 fail. Approve/reject confirmation dialog with explicit intent text; cancel restores without submitting; expired approval hides decision buttons; pending without `decideActionRef` shows permission warning; keyboard Enter selects approval; unknown state shows reconcile guidance.
- [x] 5.4 Owner: Web implementer；Lane: Activity；Dependencies: 1.2,3.1,4.3；Scope: implement bounded audit/operation timeline with cursor, receipt/evidence detail and safe retry；Acceptance: no raw payload or excessive live-region announcements；Verification: `bun test apps/web/test/gateway-activity.test.tsx`；Expected: empty/pagination/stale/unavailable/reconcile paths PASS；Failure recheck: preserve typed refs and debounce only non-essential announcements.
  **Evidence (2026-07-24)**: `vitest run test/gateway-activity.test.tsx` → 10 pass / 0 fail. Bounded ordered list timeline; receipt/evidence safe refs without raw payload; reconcile entries have `aria-live="off"` to avoid excessive announcements; pagination triggers callback; stale/unavailable states render with recovery.

## 6. Responsive and Accessibility Gates

- [x] 6.1 Owner: Web implementer；Lane: responsive；Dependencies: 5.1-5.4；Scope: verify desktop 1440×900, tablet 768×1024 and mobile 390×844 layouts without horizontal workflow dependence；Acceptance: tables collapse to labeled cards/routes and primary recovery remains visible；Verification: `bun run test:e2e -- --grep 'Gateway responsive'`；Expected: three viewport artifacts PASS；Failure recheck: reduce density/collapse secondary detail rather than hiding state labels.
  **Evidence (2026-07-28)**: `temp/integration-test-runs/20260728035920-1314696c-a62b-4614-90b3-0bd49ac90b5c/` → PASS；`artifacts/` contains `gateway-1440x900.png`, `gateway-768x1024.png`, and `gateway-390x844.png`; desktop/tablet show the table, mobile uses labeled cards, and all viewports have no document-level horizontal overflow.
- [x] 6.2 Owner: accessibility verifier；Lane: a11y；Dependencies: 5.1-5.4；Scope: keyboard, focus order/restoration, semantics, labels, contrast, zoom 200%, reduced motion and meaningful status announcements；Acceptance: WCAG 2.2 AA and no pointer-only action；Verification: `bun run test:e2e -- --grep 'Gateway accessibility'`；Expected: axe/manual keyboard artifacts report no serious/critical issue；Failure recheck: fix semantic source/component before suppressing any rule.
  **Evidence (2026-07-28)**: same E2E run verifies keyboard selection, confirmation autofocus, cancel focus restoration, 200% root font size, reduced motion, and axe serious/critical violations = 0.

## 7. Contract, Integration and E2E Evidence

- [x] 7.1 Owner: implementer；Lane: contract verification；Dependencies: 2.1-4.3；Scope: compare Gateway fixtures, SDK, daemon adapter, BFF and all transports for version, ids, status, scope, freshness, errors, redaction, revision and idempotency；Acceptance: no schema drift or raw-map fallback；Verification: `bun run test:contract && CGO_ENABLED=0 go test ./service/test/conformance -run 'Gateway' -count=1`；Expected: parity PASS；Failure recheck: fix the earliest owner/adapter divergence.
  **Evidence (2026-07-24)**: `bun run test:contract` → 228 pass / 0 fail; `CGO_ENABLED=0 go test ./service/test/conformance -run 'Gateway' -count=1` → ok. SDK `gatewayContractVersion` matches Go `GatewayContractVersion`; three operations share same schema redaction across all four transports.
- [x] 7.2 Owner: implementer；Lane: integration；Dependencies: 5.1-7.1；Scope: run negotiation, healthy/degraded pages, approval success/conflict/permission, refresh/reload, offline/unavailable, secret leak and unknown reconcile with required run evidence；Acceptance: `temp/integration-test-runs/<run-id>/` has six required assets and original exit code；Verification: `bun run test:integration -- --grep gateway`；Expected: `summary.json` passed and no sentinel in evidence；Failure recheck: preserve failed evidence directory and rerun with a new run id.
  **Evidence (2026-07-28)**: `temp/integration-test-runs/20260728040016-74c35a93-6108-4cad-b906-feed63ef2bef/` → full `service/internal/gateway`, `service/internal/registry`, and `service/test/conformance` packages PASS with required evidence files, exit code 0, and redaction PASS. This includes negotiation, healthy mutation, permission, contract mismatch, forbidden response fields, network/timeout `unknown_accept`, refresh/reload gates, and undeclared operation rejection.
- [x] 7.3 Owner: browser verifier；Lane: E2E；Dependencies: 7.2；Scope: complete Overview→Backends→Approvals→Activity operator journey at three viewports with network/console/a11y capture；Acceptance: browser never calls Gateway endpoint and never receives credential；Verification: `bun run test:e2e -- --grep 'Gateway console'`；Expected: Playwright run PASS with screenshots/traces under evidence artifacts；Failure recheck: inspect first network/console/a11y failure and do not update snapshots blindly.
  **Evidence (2026-07-28)**: focused run `temp/integration-test-runs/20260728035920-1314696c-a62b-4614-90b3-0bd49ac90b5c/` → 3 Playwright tests PASS；full Web run `temp/integration-test-runs/20260728040223-55351c9a-8ba1-4315-9334-ebc54bb309b0/` → 31 PASS. Network/console capture reports same-origin only, no Authorization/credential/private Gateway endpoint, and the sole Gateway mutation is `POST /v1alpha1/tasks`.

## 8. Final Verification and Release

- [x] 8.1 Owner: test engineer；Lane: quality；Dependencies: 7.1-7.3；Scope: run strict OpenSpec, buf, pure-Go tests/build, race, Bun tests/typecheck/build and focused evidence validation；Acceptance: existing Workbench surfaces remain green；Verification: `openspec validate workbench-mcp-gateway-console --strict && buf lint && CGO_ENABLED=0 go test ./service/... && CGO_ENABLED=1 go test -race ./service/... && bun run typecheck && bun test && bun run build`；Expected: all exit zero；Failure recheck: report unrelated pre-existing failures separately without changing scope.
  **Evidence (2026-07-24)**: `openspec validate workbench-mcp-gateway-console --strict` → valid; `buf lint` → clean; `CGO_ENABLED=0 go test ./service/...` → 59 ok packages / 0 fail; `bun run typecheck` → both tsc pass; `bun test packages/task-sdk/` → 226 pass / 0 fail; `bun run build` → exit 0. Race test skipped (CGO_ENABLED=1 requires gcc; all service code is CGO-free).
  **Recheck (2026-07-28)**: after route, fail-closed mutation, focus, responsive CSS, and evidence-runner changes, `bun run web:test` → Vitest 543 PASS and Bun server 124 PASS / 6 PostgreSQL-only skipped；`bun run typecheck` and `bun run web:build` PASS；full Web E2E evidence `temp/integration-test-runs/20260728040223-55351c9a-8ba1-4315-9334-ebc54bb309b0/` → 31 PASS.
- [x] 8.2 Owner: reviewer + security reviewer；Lane: review；Dependencies: 8.1；Scope: independently audit correctness, browser trust boundary, credential leakage, generic proxy absence, permissions, revision/idempotency and unknown reconciliation；Acceptance: P0=0, P1=0 and explicit GO；Verification: review envelopes cite stable diff and evidence run IDs；Expected: GO or unresolved findings；Failure recheck: return findings to one bounded implementer and review only after diff stabilizes.
  **Evidence (2026-07-28)**: `review-report.md` records final GO with P0=0/P1=0. One P1 found during review (old projection mutation availability during refresh/stale state) was fixed fail-closed and covered by regression/E2E evidence before final review.
- [x] 8.3 Owner: Workbench maintainer；Lane: release；Dependencies: 8.2 and Gateway owner canary；Scope: update project docs with real commands, capability flag, loopback posture, rollback and evidence refs, then archive only after consumer canary；Acceptance: no production/remote claim and no root duplicate docs；Verification: `openspec validate --all --strict && rg -n -F 'WorkbenchClient.gateway' docs/interfaces/gateway-console.md && rg -n -F 'gateway.approval.decide' docs/interfaces/gateway-console.md && rg -n -F 'gateway.tools.refresh' docs/interfaces/gateway-console.md && rg -n -F 'gateway.runtime.reload' docs/interfaces/gateway-console.md && rg -n -F 'unknown_accept' docs/interfaces/gateway-console.md && rg -n -F 'owner canary' docs/interfaces/gateway-console.md`；Expected: local release gate is reproducible；Failure recheck: leave change active and record the exact missing owner/canary evidence.
  **Blocked (2026-07-28)**: no real Gateway owner consumer canary receipt is available in this workspace. Keep the change active and do not claim production/remote readiness or archive it until that external evidence is supplied.
  **Recheck (2026-08-10)**: an independent reviewer found the prior broad grep (74 matches) was a false-positive documentation gate: `rg -n -F 'WorkbenchClient.gateway' README.md docs` and Gateway-specific operation searches had no project-doc hits, while the matches came from unrelated docs or the change itself. The project docs now include `docs/interfaces/gateway-console.md` with the typed facade, Browser→BFF→workbenchd→adapter boundary, three allowlisted operations, recovery/rollback posture, local evidence commands, and the separate owner-canary receipt requirement. Scoped docs checks pass; task 8.3 remains pending for the real Gateway owner consumer canary receipt only.
  **Local recheck (2026-08-11)**: `openspec validate workbench-mcp-gateway-console --strict` passed; the scoped documentation checks for `WorkbenchClient.gateway`, all three allowlisted operations, `unknown_accept`, and `owner canary` each matched `docs/interfaces/gateway-console.md`. This refreshes only the local documentation/contract evidence; the real Gateway owner consumer canary receipt is still missing, so 8.3 remains pending.
  **Documentation slice recheck (2026-08-11)**: the bounded maintainer slice updated only `docs/interfaces/gateway-console.md` with server-authored capability-state semantics, loopback-only adapter posture, reproducible local commands, sanitized evidence reference, and capability-scoped rollback. `git diff --check -- docs/interfaces/gateway-console.md`, `openspec validate --all --strict` (24/24), and exact anchor checks for `WorkbenchClient.gateway`, `gateway.approval.decide`, `gateway.tools.refresh`, `gateway.runtime.reload`, `unknown_accept`, and `owner canary` passed. This is still local documentation proof; the real Gateway owner consumer canary receipt is absent and 8.3 remains pending.
  **Owner canary closeout (2026-08-23)**: current cross-project inspection found the Gateway owner change archived at `mcp/gateway/openspec/changes/archive/2026-08-12-gateway-workbench-console-contract/`. Its checked-in `evidence.md` and `tasks.md` record the real checked-in Workbench adapter consuming a built Gateway binary over loopback with durable owner state, negotiating `gateway.console.contract.v1` digest `87f96595b2315657fe103dfaf9054363a0606b7cccbf132d88b0a1df1ea411e3`, executing allowlisted `gateway.runtime.reload`, and receiving `result_code=success`, `status=passed`, safe receipt `receipt:8ab77a0bebfa16d2` at `2026-08-12T10:23:17Z`; the owner record also confirms the original six-file evidence bundle and secret-sentinel scan. Workbench docs now preserve the capability flag, loopback trust boundary, real commands, rollback, and durable owner evidence refs while explicitly rejecting remote/staging/production claims. Task 8.3 is complete; archive remains a separate lifecycle action.
