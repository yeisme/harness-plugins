> **归档说明（2026-08-16）**：本 change 已完成 47/50 项任务并归档，capability spec 已合入正式 specs；剩余 3 项晋级门禁任务（3.1、4.2b、4.4b）已逐字迁移至 `openspec/changes/workbench-client-runtime-promotion-gates/`。

## 0. Lease, compatibility and baseline

- [x] 0.1 Owner: Workbench Maintainer；Lane: lease；Dependencies: none；Scope: 等待 `complete-open-design-studio-workflow`、`workbench-owner-backend-integrations` 与 foundation 的重叠写入稳定，租用 runtime 新路径和 registry/runtime wiring；Acceptance: 不覆盖现有未提交用户改动；Verification: `git status --short && git diff --name-only`；Expected: path lease 明确；Evidence: 2026-07-19 runtime proto/schema/transport/component 使用新增路径，混合 SDK/runtime/Web 文件只做 additive hunk，未清理或覆盖现有 Owner/Open Design 改动；Failure re-check: 重叠时只实现独立新文件或暂停 wiring。
- [x] 0.2 Owner: Compatibility Reviewer；Lane: compatibility；Dependencies: 0.1；Scope: 冻结 `WorkbenchClient` constructor/factory、SDK exports、protobuf/JSON schema 与 transport additions 为 experimental additive；Acceptance: 现有 consumer 不需立即改 constructor；Verification: consumer search + existing SDK tests；Expected: migration/rollback 明确；Evidence: 2026-07-19 三参数 constructor 兼容测试与 29 个 SDK tests 通过；Failure re-check: 若必须 breaking，拆独立 migration change。
- [x] 0.3 Owner: Test Engineer；Lane: baseline；Dependencies: 0.1；Scope: 运行 OpenSpec、Go、SDK、contract/integration baseline；Acceptance: 记录既有失败；Verification: `openspec validate --all --strict && CGO_ENABLED=0 go test ./service/... && bun run typecheck && bun test && bun run test:contract`；Expected: baseline exit 0 或既有失败独立记录；Evidence: 2026-07-19 baseline 全部 exit 0；Failure re-check: 不把既有失败归因本 change。

## 1. Red contract tests

- [x] 1.1a Owner: Implementer；Lane: sdk-independent-module；Dependencies: root contract；Scope: 测试先行新增独立 `runtime-client.ts` / `runtime-models.ts`，覆盖 descriptor/readiness transport methods、unknown state/mode fail-closed、safe field projection 与 secret-shaped field drop；Acceptance: 不修改当前被占用的 `WorkbenchClient`、index、HTTP/JSON-RPC wiring；Verification: `bun test packages/task-sdk/test/runtime-client.test.ts && bun run typecheck && bun test packages/task-sdk`；Expected: 8 个 runtime tests、25 个 SDK tests 与 typecheck 全通过；Evidence: 2026-07-19 exit 0；Failure re-check: unknown enum不得映射为 ready或错误mode，transport extra fields不得穿过 projection。
- [x] 1.1b Owner: Implementer；Lane: sdk-wiring-red；Dependencies: 0.1, 0.2, 1.1a；Scope: 取得 path lease 后先添加 `WorkbenchClient.runtime` factory compatibility、package export 与 HTTP/JSON-RPC route tests；Acceptance: RED 指向缺失 wiring，现有 constructor/factory consumer 不破坏；Verification: `bun test packages/task-sdk --test-name-pattern runtime`；Expected: behavior RED；Evidence: 2026-07-19 RED 分别命中缺失 package export 与 `WorkbenchClient.runtime`；Failure re-check: 当前 index/workbench-client/http/json-rpc 改动未稳定时不得写入。
- [x] 1.2a Owner: Implementer；Lane: service-independent-core；Dependencies: root contract；Scope: 严格 TDD 新增独立 `service/internal/clientruntime`，实现 versioned descriptor/readiness、broker unavailable、descriptor validation、unknown mode/owner、ambiguous adapter 与 defensive copy；Acceptance: 不修改被占用的 registry/runtime/transport wiring，不做 discovery/process/network/credential；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime -count=1 && go test -race ./service/internal/clientruntime -count=1 && CGO_ENABLED=0 go test ./service/... -count=1`；Expected: package普通/race与全 service tests通过；Evidence: 2026-07-19 exit 0；Failure re-check: 无效 descriptor必须拒绝，多个匹配 adapter必须显式选择。
- [x] 1.2a2 Owner: Implementer；Lane: service-independent-broker-source；Dependencies: 1.2a, broker public Go SDK；Scope: 在独立 `service/internal/clientruntime` 新增authenticated broker descriptor/readiness source与公共SDK constructor，不修改registry/transport wiring；Acceptance: 固定 `yeisme.workbench` facade projection，adapter disabled优先于聚合owner unavailable，底层错误不泄露，返回值defensive；Verification: `cd service && go test ./internal/clientruntime -run '^TestBrokerSource' -count=1 && go test -race ./internal/clientruntime -count=1 && CGO_ENABLED=0 go test ./... -count=1`；Expected: focused/race/全service green；Evidence: 2026-07-19严格TDD missing source RED后GREEN；Failure re-check: sibling `replace`仅为monorepo canary，发布前必须切tagged dependency，主wiring路径未获lease前不得接入。
- [x] 1.2a3 Owner: Implementer；Lane: service-independent-operation-source；Dependencies: 1.2a2, broker typed operation SDK；Scope: 在同一独立BrokerSource复用shared typed `RunRequest`/`RunSnapshot`，新增start/cancel/reconcile并把broker sentinel收敛为Workbench稳定错误；Acceptance: 不手拼HTTP/JSON/token header，不新增raw prompt/key/provider payload字段，disabled/conflict/not-found/denied fail closed；Verification: `cd service && go test ./internal/clientruntime -run '^TestBrokerSource' -count=1 && go test -race ./internal/clientruntime -count=1 && CGO_ENABLED=0 go test ./... -count=1`；Expected: green；Evidence: 2026-07-19 focused/race/全service exit 0；Failure re-check: owner binding与主registry wiring未完成时capability保持gated。
- [x] 1.2b Owner: Implementer；Lane: service-wiring-red；Dependencies: 0.1, 0.3, 1.2a；Scope: 取得 path lease 后先添加 schema、HTTP/gRPC/JSON-RPC parity 与 broker unavailable transport tests；Acceptance: RED 指向缺失 service projection；Verification: `CGO_ENABLED=0 go test ./service/... -run 'Runtime|Parity' -count=1`；Expected: behavior RED；Evidence: 2026-07-19 HTTP 先返回 404、gRPC 再返回 unknown service，均由真实 loopback integration test 捕获；Failure re-check: 当前 runtime 变更未稳定时不得写入，禁止 transport-specific fixture 绕过共享 Source。
- [x] 1.3 Owner: Implementer；Lane: security-red；Dependencies: 0.3；Scope: 添加 approval tamper/replay、runtime revision、credential confused deputy、unknown_accept、cursor、sentinel redaction tests；Acceptance: tests 实际运行且失败指向缺失控制；Verification: `CGO_ENABLED=0 go test ./service/... -run '^TestRuntimeSecurity_' -count=1`；Expected: security RED；Evidence: 2026-07-20各TDD slice实际观察到missing handler/registration compile RED、caller-forged approval schema RED、stale/cross-task gate与resource escalation行为RED、connection transport误入unknown_accept行为RED、缺lifecycle persistence/caller-claim控制行为RED；event-session任务另记录cursor repair、rejected taxonomy与unsafe projection行为RED；随后统一selector在app/clientruntime/runtime实际匹配15项`TestRuntimeSecurity_`测试并exit 0，credential/process/provider/approval字段零broker调用、unknown不自动retry、cursor/sentinel redaction由event source/session与evidence scan覆盖；Failure re-check: 无 test match 或语法失败不算红灯。

## 2. Phase A descriptor/readiness

- [x] 2.1 Owner: Implementer；Lane: sdk；Dependencies: 1.1 RED；Scope: 新增 `runtime-models.ts`、`runtime-client.ts` 和 `WorkbenchClient.runtime` additive factory；Acceptance: old factory source compatibility、unknown fail closed；Verification: `bun run typecheck && bun test packages/task-sdk --test-name-pattern runtime`；Expected: SDK tests green；Evidence: 2026-07-19 typecheck 与 29 个 SDK tests 通过，错误 contract/state/mode 均 unavailable；Failure re-check: 检查 export/constructor compatibility。
- [x] 2.2 Owner: Implementer；Lane: service-core；Dependencies: 1.2 RED；Scope: 新增 Go clientruntime descriptor/readiness service、stable enums/errors、shared broker source 和 unavailable fallback；Acceptance: 无 discovery/shell/credential resolution；Verification: `CGO_ENABLED=0 go test ./service/... -run 'Runtime|Parity' -count=1`；Expected: core tests green；Evidence: 2026-07-19 Broker 优先、静态 unavailable 回退、semantic error 不被掩盖，Go full/race 通过；Failure re-check: 检查 private path/stdout/provider payload。
- [x] 2.2a Owner: Workbench Implementer；Lane: shared-module-adoption；Dependencies: 2.2, shared broker Go `ProjectModule`；Scope: 将 broker-backed descriptor/readiness 通过共享 Local CLI/BYOK module projector 转换为现有 Workbench DTO，保留四 transport/schema shape；Acceptance: global readiness只降级，显式disabled优先，selected owner不能被同mode其他ready owner提升，unknown/malformed/duplicate operation fail closed，所有mutation capability仍受gate；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime -run '^TestBrokerSource' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/clientruntime -run '^TestBrokerSource' -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=1 go test -race ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/...`；Expected: focused/full/race/build/vet green且browser contract不变；Evidence: 2026-07-20 degraded闭合、disabled优先、cross-owner anti-elevation测试与full/race/build/vet全部通过；Compatibility: pre-v1 additive dependency + security narrowing，rollback为原Workbench手写readiness mapper；Failure re-check: 任一degraded/disabled/unknown状态可启用operation或底层错误泄漏即回滚。
- [x] 2.3 Owner: Implementer；Lane: service-transport；Dependencies: 2.2；Scope: 以统一 `clientruntime.Source` 投影 protobuf/schema/HTTP/gRPC/JSON-RPC，未来 mutation 才进入 Operation registry；Acceptance: 四 transport 语义 parity，handler 仅做协议转换且不持有独立 runtime 状态/gate；Verification: `buf lint && CGO_ENABLED=0 go test ./service/test/conformance ./service/internal/runtime -count=1 && bun run test:contract`；Expected: parity green；Evidence: 2026-07-19 HTTP/JSON-RPC/gRPC loopback parity、protobuf lint/generate、runtime JSON schema 与 31 个 contract tests 通过；Failure re-check: 从共享 Source/protobuf/schema 修复，不在 transport 增加业务分支。
- [x] 2.4 Owner: Implementer；Lane: web；Dependencies: 2.1, 2.3；Scope: 在 Connections 添加 runtime panel、refresh、owner/adapter/capability status、disabled BYOK 与 safe remediation；Acceptance: browser只用 SDK，无 raw key/session token/direct broker；Verification: `bun run web:test && bun run web:build && bun run web:e2e`；Expected: web tests/build/e2e green；Evidence: 2026-07-19 75 个 Web tests、2 个 BFF tests、Vite build 与 Playwright 全量 e2e 通过，runtime panel axe/screenshot通过；Failure re-check: 检查 direct fetch、独立 store 和 command execution。
- [x] 2.4a Owner: Workbench Product/Implementer；Lane: unified-local-capability-center；Dependencies: 2.4, shared browser-safe module；Scope: 将 Connections runtime panel 收敛为 Workbench 自有的“Local CLI & BYOK”单一能力中心，按 adapter/mode 展示 readiness、diagnostic、完整 lifecycle capability 与安全 remediation；Acceptance: 不新增 raw key/CLI command/provider 输入，不把默认关闭的 generate 描述为 production-ready，未知 capability 使用稳定不可用标签；Verification: `bun run web:test && bun run typecheck && bun run web:build && bun run web:e2e`；Expected: 默认 build 中 connection/readiness可见、generate action隐藏，显式Web canary开启后仍通过既有Workbench gate提交；Evidence: 2026-07-20 focused 8项（含unknown capability fail-closed）、Web 103项+BFF 48项（5 skip）、typecheck、Vite build与Playwright 8项全部通过；e2e evidence run `20260720145320-12ec3d03-d808-4cfd-9a75-a1d74829800f` passed/exit 0；Failure re-check: 任一浏览器字段携带credential/provider/process信息或disabled action被误渲染即回滚本UI增量。

## 3. Phase B broker-backed operations

- [ ] 3.1 Owner: Broker/Owner Implementers；Lane: dependency；Dependencies: shared broker + owner adapters；Scope: 发布 fixed registry、peer/facade ACL、descriptor/run/events/cancel/reconcile 与 resource limits；Acceptance: arbitrary executable/argv/cwd/env/PATH/symlink/replay 被拒绝，broker无 credential Resolve；Verification: broker `TestSecurity_`、race、build、integration evidence；Expected: dependencies pass；Current status: 2026-07-22 Workbench 仍通过 Go `replace` 与 TypeScript `file:` 消费本地 broker；`backend-server/client-runtime` 仅可识别为 commit `2a92ee5`，其 TypeScript package 仍为 private `0.1.0-dev`，Aigora 工作树为 `9acdf9d-dirty`，credentialctl 为 `v0.1.0-2-g143e1c6`。这些状态不能作为 tagged release 或独立 owner promotion 证据，因此本项保持 pending；Failure re-check: 任一失败或依赖未tag时保持 Workbench generate 默认关闭。
- [x] 3.1a Owner: Broker/Aigora Implementers；Lane: fixed-aigora-lifecycle-dependency；Dependencies: broker typed owner binding、Aigora owner lifecycle core；Scope: 为Workbench generate前置门提供真实broker process→Aigora generate/cancel/reconcile/unknown binding evidence；Acceptance: broker只传safe refs，固定`aigora/aigora.text`，unknown无owner run id时仍non-terminal且不redispatch；Verification: broker full/race/build/vet、Aigora focused/race/full/build/vet与Aigora system evidence runner；Expected: 只解锁Workbench独立generate wiring，不等同于events/security/promotion；Evidence: 2026-07-20 broker默认关闭的Aigora fixed resolver与独立owner/adapter/security flags完成；system run `clientruntime-broker-aigora-20260720T120000Z`使用真实broker binary和Aigora `NewOwnerLocalServer`/`RuntimeService`覆盖generate/cancel/reconcile/unknown-no-retry，passed/exit 0/redaction clean；同时修复broker把合法空`owner_run_id`的`unknown_accept`误判502；Failure re-check: 3.1 broad events/resource/security、credential tag、Aigora production executor仍pending，SDK/Web和product promotion继续disabled。
- [x] 3.2a Owner: Workbench Implementer；Lane: independent-operation-handler；Dependencies: 1.2a3, TaskService admission contract, independent path；Scope: 在`service/internal/clientruntime`实现默认未注册的fixed runtime handler与exact registry schema，registration固定operation/owner/adapter/action，trusted Task提供project/user/revision/idempotency，input仅允许safe ref、approval expiry与扁平resource limits；Acceptance: permission/cost仍由TaskService gate，unknown/process/provider/credential字段、expired approval、partial/ceiling escalation limits与wrong registration在broker前拒绝；accepted/running/terminal/rejected/unknown/cancel状态稳定映射，transport ambiguity只进入unknown/reconcile不retry；Verification: `cd service && CGO_ENABLED=0 go test ./internal/clientruntime -run '^TestRuntimeSecurity_Operation' -count=1 && CGO_ENABLED=1 go test -race ./internal/clientruntime -count=1 && go vet ./internal/clientruntime`；Expected: handler/schema core green但Operation catalog不变；Evidence: 2026-07-20 trusted scope construction、zero-call injection/resource negative tests、schema/decoder parity、action/state/error mapping与race/vet通过；Failure re-check: TaskService async accepted event/receipt、registry registration、transport/UI和双owner evidence仍由3.2/3.3/3.4负责。
- [x] 3.2b Owner: Workbench Implementer；Lane: task-async-projection；Dependencies: 3.2a, existing TaskService；Scope: 将owner dispatch attempt与owner生命周期事件分离，按running/unknown_accept/cancel_requested/terminal固定投影事件并保留safe execution receipt ref；Acceptance: running不得发`task.completed`，unknown attempt保持unknown且不自动retry，只有terminal发completed；Verification: `CGO_ENABLED=0 go test ./service/internal/app -run '^TestTaskServiceProjectsOwnerExecutionResults$' -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=1 go test -race ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/...`；Expected: full/race/build/vet green；Evidence: 2026-07-20 running/unknown/cancel-requested/terminal table tests与全service验证通过；Failure re-check: registry wiring、四transport和双owner system evidence仍由3.2/3.3/3.4负责。
- [x] 3.2c Owner: Workbench Implementer；Lane: default-off-connection-admission；Dependencies: 3.2a, broker typed `TestConnection`；Scope: 严格TDD扩展独立broker source与fixed OperationHandler，使`connection_test`使用公共Go SDK专用方法并复用trusted Task scope、approval expiry、revision、idempotency与resource ceiling；Acceptance: handler默认不注册到Operation catalog或四transport，caller不能提交host/model/key/provider字段，connection terminal/rejected/unknown使用既有safe lifecycle projection且不自动retry；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime -run 'ConnectionTest|BrokerSourceOperations' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/clientruntime -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/... && git diff --check`；Expected: default-off admission component green，production catalog/transport/UI不变；Evidence: 2026-07-20 missing source method/action产生预期compile RED，随后focused、package race、全service、pure-Go build、vet与diff check均exit 0；Failure re-check: Aigora真实owner connection service、四transport conformance或promotion evidence缺失时保持未注册并继续gated。
- [x] 3.2d Owner: Workbench Implementer；Lane: connection-test-production-wiring；Dependencies: 3.2c, existing TaskService gate authority；Scope: 移除caller-supplied approval ref/expiry，新增只读gate authority验证并派生broker approval scope；在独立默认关闭flag下固定注册`workbench.runtime.aigora.connection_test`，将broker instance revision投影到readiness，并让Web仅在operation catalog、capability、revision三者齐备时通过普通Task API提交后进入现有gate review；Acceptance: caller不能提交approval/host/provider/model/key/process，未批准/跨task/过期gate、revision缺失或flag关闭均在broker前fail closed，HTTP/gRPC/JSON-RPC/SDK通过既有Operation自动保持parity，关闭flag即可回滚；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime ./service/internal/runtime -run 'RuntimeSecurity_|ConnectionTest' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/clientruntime ./service/internal/runtime -count=1 && bun test packages/task-sdk apps/web/test/routes.test.tsx && bun run typecheck && bun run test:contract && openspec validate workbench-client-runtime-adapter --strict --no-interactive`；Expected: opt-in connection test vertical slice green，run/event/cancel/reconcile仍disabled；Evidence: 2026-07-20严格TDD先后命中缺失gate authority、revision projection、fixed registration与Web action；实现后pure-Go全service、build、vet、focused race、38项SDK、40项contract、76项Web tests、Web build、typecheck与strict OpenSpec均exit 0。真实GORM纵向测试证明permission→cost→dispatch期间broker零/零/一次调用，broker approval ref来自持久化permission gate，caller approval字段在schema阶段拒绝；integration run `20260720100216-e67d9ad6-9bfb-4c60-8cee-a693c6a1254b` 与e2e run `20260720100217-661fa0ae-2817-46ba-9b51-5101c364a7c9` passed/exit 0、六件套完整、sentinel扫描无命中。默认flag关闭，关闭命令与gate流程已写入`docs/runtime/local-control-plane.md`；Failure re-check: broker或owner返回gated时展示安全terminal/unknown结果，不绕过gate、不自动retry、不提升其他operation。
- [x] 3.2e Owner: Workbench Implementer；Lane: durable-safe-runtime-input；Dependencies: 3.2d, existing Task replay-input column；Scope: 为registry增加默认false的`PersistSafeInput`内部授权位，仅固定connection-test启用；TaskService持久化并重放schema验证后的canonical safe input，使待审批runtime task可在Workbench重启后继续dispatch，并为后续cancel/reconcile保留可信input ref；Acceptance: 普通owner operation仍不持久化input，caller approval/provider/credential/process/unsafe ref在持久化前拒绝，replay必须重新通过schema与digest，flag关闭或operation缺失时不自动dispatch；Verification: `CGO_ENABLED=0 go test ./service/internal/app ./service/internal/runtime -run 'RuntimeSecurity_|Restart|Replay' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/app ./service/internal/runtime -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/... && openspec validate workbench-client-runtime-adapter --strict --no-interactive`；Expected: connection task restart replay green且其他owner input仍volatile；Evidence: 2026-07-20 registry seal仅允许owner mutation、固定`inputRef`与有界resource limit字段，unsafe ref/secret-shaped字段/额外字段在Task持久化前拒绝；TaskService使用现有`ReplayInput`保存canonical safe input，重启后重新校验schema与request digest再dispatch，普通owner restart测试继续fail closed；全service test、pure-Go build、vet与focused race均exit 0；integration run `20260720101816-50a2d312-121a-4378-a64e-d3856c4af42c`、e2e run `20260720101816-dc8d83e7-a854-4b42-bb8d-7416f5091305` passed/exit 0、六件套完整且敏感扫描无命中；Failure re-check: raw payload、secret-shaped field、schema drift或digest mismatch可被重放即回滚并保持cancel/reconcile disabled。
- [x] 3.2f Owner: Workbench Implementer；Lane: connection-test-uncertainty；Dependencies: 3.2d, broker connection-test contract；Scope: 将没有reconcile endpoint的一次性connection-test transport failure映射为固定terminal unavailable/failed，而不是不可恢复的`unknown_accept`；Acceptance: connection test不自动retry、不声称success，用户后续测试必须提交新的显式Task/idempotency scope；start/cancel/reconcile的transport ambiguity仍保持`unknown_accept`并只允许reconcile；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime -run '^TestRuntimeSecurity_OperationHandlerMapsLifecycleWithoutAutomaticRetry$' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/clientruntime -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/... && openspec validate workbench-client-runtime-adapter --strict --no-interactive`；Expected: connection-test failure terminal且持久run ambiguity语义不变；Evidence: 2026-07-20 TDD RED证明connection-test transport error原先错误进入不可reconcile的`unknown_accept`，随后仅对`OperationConnectionTest`映射固定`runtime connection test is unavailable` terminal failure；同表start transport ambiguity继续为`unknown_accept`、无自动retry；全service tests、pure-Go build、vet、clientruntime/runtime race与strict Spec均exit 0；integration run `20260720102707-188e7345-bf9e-4e92-ac67-8b58d18b8a16`、e2e run `20260720102707-a4464703-b40c-4072-b092-a135f7efa001` passed/exit 0、六件套完整且敏感扫描无命中；Failure re-check: connection-test仍可进入unknown_accept或start ambiguity被误降级为failed即回滚。
- [x] 3.2g Owner: Workbench Implementer；Lane: lifecycle-control-authority；Dependencies: 3.2e, 3.2f, broker cancel/reconcile contract；Scope: 为fixed persistent start handler增加内部optional cancel/reconcile lifecycle interface；approval由已批准same-task gates与先持久化的fresh control attempt共同派生，attempt ID作为短期broker approval ref；connection-test不实现该能力；Acceptance: wrong task/kind/status、unsafe/expired/unpersisted attempt、缺safe replay/digest或caller observed status/receipt在broker前拒绝，不刷新旧gate、不自动retry；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime ./service/internal/app -run '^TestRuntimeSecurity_(Lifecycle|OwnerLifecycle)' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/clientruntime ./service/internal/app -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/... && openspec validate workbench-client-runtime-adapter --strict --no-interactive`；Expected: lifecycle handler与TaskService先持久化后side-effect测试green，公开transport shape暂不改变；Evidence: 2026-07-20 TDD先证明handler缺Cancel/Reconcile、TaskService未调用owner lifecycle且owner reconcile只接受caller claim；随后registry新增internal `SupportsCancel/SupportsReconcile` seal/digest授权，只有persisted-safe owner mutation且handler实现lifecycle interface才可开启，connection-test两项保持false；OperationHandler以same-task approved gates证明基础授权，并从GORM可读的fresh started control attempt派生broker approval ref/expiry，wrong task/kind/status、unsafe/expired/unpersisted attempt均零broker调用；TaskService通过optimistic task version mutation先持久化Attempt再调用handler，owner reconcile拒绝caller status/receipt并只接受typed handler结果；全service tests、pure-Go build、vet、registry/app/clientruntime/runtime race与strict Spec均exit 0；integration run `20260720103729-096d6295-5cb9-48e2-addc-cbfbc31684d4`、e2e run `20260720103730-a4063f08-3082-4fbe-8ff5-e4b2a48ac7e9` passed/exit 0、六件套完整且敏感扫描无命中；Failure re-check: side effect早于attempt persistence、过期dispatch approval被复用或connection-test获得cancel/reconcile即回滚。
- [x] 3.2h Owner: Workbench Implementer；Lane: fixed-aigora-generate-registration；Dependencies: 3.2g, broker start/event/cancel/reconcile SDK；Scope: 新增固定`workbench.runtime.aigora.generate`→`aigora/aigora.text/start` registration，显式声明PersistSafeInput/events/cancel/reconcile与四projection，但不接workbenchd主registry/flag/Web；Acceptance: connection-test registration不提升generate，owner/adapter/action不可由caller选择，缺真实owner/system evidence时catalog不变；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime ./service/internal/runtime -run '^TestRuntimeSecurity_(AigoraGenerate|ConnectionTestRegistration)' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/clientruntime ./service/internal/runtime -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/... && openspec validate workbench-client-runtime-adapter --strict --no-interactive`；Expected: independent generate registration green且production catalog仍只有显式connection-test canary；Evidence: 2026-07-20 compile RED命中缺失`RegisterAigoraGenerate`/operation constant，随后新增固定Aigora owner/adapter/start registration，启用PersistSafeInput/events/cancel/reconcile、permission/cost/idempotency与四projection；handler执行测试证明broker只收到`aigora/aigora.text`与start action；runtime registry测试证明connection-test flag开启时generate仍不进入catalog；全service tests、pure-Go build、vet、registry/app/clientruntime/runtime race与strict Spec均exit 0；Failure re-check: generate随connection flag出现、lifecycle flags缺失或caller可选择binding即回滚。
- [x] 3.2i Owner: Workbench Implementer；Lane: default-off-generate-runtime-wiring；Dependencies: 3.1a, 3.2h；Scope: 以独立`WORKBENCH_RUNTIME_GENERATE_ENABLED`/`--client-runtime-generate-enabled`接入workbenchd主registry，不改变connection-test flag或Web可见性；Acceptance: 默认false，runtime配置不完整时fail closed，generate-only与connection-only registry互不提升，缺broker operations/gate authority不能seal；Verification: `CGO_ENABLED=0 go test ./service/internal/runtime -run 'AigoraGenerate|ConnectionTestRegistration' -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/... && CGO_ENABLED=1 go test -race ./service/internal/registry ./service/internal/app ./service/internal/clientruntime ./service/internal/runtime -count=1`；Expected: service-side generate catalog可显式启用，Web/automatic event-pump仍pending；Evidence: 2026-07-20 missing config field/newRegistry signature compile RED后GREEN；新增独立Config/env/CLI flag、完整runtime配置校验与`RegisterAigoraGenerate`主registry调用；focused、全service、pure-Go build、vet与四package race全部exit 0；integration `20260720112624-f0184254-79dc-401e-bfd0-c2c96e1b869b`与e2e `20260720112625-299f8fab-169a-44e2-88f6-68c28bac1bb9` passed/exit 0、六件套完整且敏感扫描无命中；Failure re-check: connection flag提升generate、默认启用、incomplete config启动或无依赖仍注册即回滚。
- [x] 3.2j Owner: Workbench + Aigora Implementer；Lane: real-connection-system；Dependencies: 3.2d, Aigora connection-only owner CLI, credentialctl readiness/grant；Scope: 从Workbench `TaskService`启动真实credentialctl binary、Aigora owner CLI与client-runtime broker，提交固定connection task并依次批准permission/cost gate；Acceptance: Task最终succeeded，provider只收到一次probe，浏览器/Workbench/broker不读取credential，secret/provider body/URL/private path不进入process output或evidence；Verification: `CGO_ENABLED=0 go test ./service/internal/runtime -run '^TestRuntimeSecurity_WorkbenchConnectionTaskUsesRealAigoraOwnerAndCredentialctl$' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/runtime -run '^TestRuntimeSecurity_WorkbenchConnectionTaskUsesRealAigoraOwnerAndCredentialctl$' -count=1`；Expected: Workbench first-support connection vertical slice green；Evidence: system run `20260720130339-385a7ae7-8e13-4cd2-91df-937a2ece2725` passed/exit 0，六件套完整，sentinel/Authorization/provider payload/private path扫描无命中；Failure re-check: 本证据仅晋级connection_test，不得提升generate/events/cancel/reconcile。
- [x] 3.2 Owner: Workbench Implementer；Lane: admission；Dependencies: 1.3 RED, 3.1a fixed lifecycle evidence；Scope: 实现 connection test/start/cancel/reconcile inputs 与 unified admission；Acceptance: scope/revision/idempotency/cost/version fail closed，unknown只 reconcile；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime ./service/internal/app ./service/internal/registry ./service/internal/repository ./service/internal/runtime -run 'TestRuntimeSecurity_|TestRuntimeCursor|TestTaskServiceProjectsOwnerExecutionResults' -count=1`；Expected: security tests green；Evidence: 2026-07-22 当前工作树五个 Go package exit 0，覆盖 fixed registration、persisted gate/attempt、unknown reconcile、event checkpoint、restart recovery 与真实 connection/generate system paths；本项只表示 Workbench admission 实现完成，不解除 3.1、4.2b 或 4.4b promotion gate；Failure re-check: 检查 side effect 是否发生在 gate 前。
- [x] 3.2k Owner: Workbench + Aigora + Broker Implementer；Lane: real-generate-lifecycle-system；Dependencies: 3.2j, Aigora canonical generate worker, broker owner reconcile pump；Scope: 从真实Workbench Task permission/cost gates提交`input:vault:sha256:<digest>`，经broker/Aigora/credentialctl执行provider并把accepted/running/terminal cursor投影回Task，同时验证queued cancel；Acceptance:raw prompt/key/provider host不经过Workbench/broker，provider成功路径一次调用，terminal含owner usage/cost refs，queued cancel不增加provider调用，幂等重放不redispatch；Verification: `CGO_ENABLED=0 go test ./service/internal/runtime -run '^TestRuntimeSecurity_WorkbenchGenerateTaskRunsCanonicalAigoraWorkerEventsAndCancel$' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/runtime -run '^TestRuntimeSecurity_WorkbenchGenerateTaskRunsCanonicalAigoraWorkerEventsAndCancel$' -count=1`；Expected: green；Evidence: focused/race通过；system run `20260720135956-51a634bd-953c-4af7-b1d2-831955ab532d` passed/exit 0、六件套与敏感扫描通过；Failure re-check: generate仍默认关闭，独立security/tag/promotion缺失时不标记3.4或4.4b完成。
- [x] 3.3a Owner: Workbench Implementer；Lane: independent-event-source；Dependencies: shared broker public Go event SDK, independent path；Scope: 在`service/internal/clientruntime`接typed `WatchEvents`/`EventIterator`，只投影allowlisted event/run/cursor/ref/progress并映射cursor repair errors；Acceptance: client缺event capability时gated，unknown type fail closed，safe JSON无token/raw SSE/stdout/stderr/prompt/provider/credential/private path，context/EOF保持可控；Verification: `cd service && CGO_ENABLED=0 go test ./internal/clientruntime -count=1 && CGO_ENABLED=1 go test -race ./internal/clientruntime -count=1 && go vet ./internal/clientruntime`；Expected: independent source green但registry/transport/Web仍disabled；Evidence: 2026-07-20 typed safe projection、cursor expired/invalid、unknown type、missing capability与close tests通过；Failure re-check: 本任务不等于3.3四transport cursor persistence/dedupe、3.4 SDK/Web promotion或双owner system evidence。
- [x] 3.3b Owner: Workbench Implementer；Lane: independent-event-session；Dependencies: 3.3a, independent path；Scope: 在`service/internal/clientruntime`增加有界event session，以last cursor重连、去重resume replay、映射cursor expiry/invalid为`reconcile_required`，并投影safe receipt/evidence ref与accepted/running/terminal/rejected/unknown_accept/cancel_requested outcome taxonomy；Acceptance: 只重连typed event stream、不重发owner operation，最多3次配置上限，repair失败后session不可继续，unsafe code/ref/run/cursor/progress固定fail closed且不回显；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime -run '^TestEventSession' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/clientruntime -count=1 && go vet ./service/internal/clientruntime && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/...`；Expected: independent session与service/conformance green，registry/transport/Web仍disabled；Evidence: 2026-07-20 missing session/outcome/reconcile symbols产生compile RED，rejected taxonomy、repair后复用与iterator中途repair分别产生行为RED；随后6项session测试、package race/vet、全service tests/conformance及pure-Go build通过；initial stream关闭后按last cursor仅重连一次并跳过duplicate，握手或iterator cursor failure均同时满足原错误与`ErrRuntimeReconcileRequired`并终止session，Authorization/private path输入收敛为固定unavailable；Failure re-check: 本任务不替代pending 3.2 admission、3.3四transport cursor persistence或3.4 SDK/Web promotion。
- [x] 3.3c Owner: Workbench Implementer；Lane: runtime-event-checkpoint；Dependencies: 3.2h, 3.3b, existing GORM Task store；Scope: 为Task/GORM增加internal runtime run-ref/cursor/sequence checkpoint，并实现typed owner event→Task状态/event/receipt/artifact的atomic projector；Acceptance: 首次run ref只来自event-capable handler safe receipt，event同run且sequence严格连续，exact replay幂等，gap/drift/unsafe ref/unknown outcome fail closed；不改变公开Task transport shape、不保存raw event；Verification: `CGO_ENABLED=0 go test ./service/internal/repository ./service/internal/app -run '^TestRuntime(Cursor|Security_OwnerEvent)' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/repository ./service/internal/app -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/... && bun run test:contract && openspec validate workbench-client-runtime-adapter --strict --no-interactive`；Expected: checkpoint roundtrip、dedupe、gap/reconcile与safe projection green；Evidence: 2026-07-20 compile RED命中缺失Task checkpoint字段、typed OwnerEvent与ProjectOwnerEvent；随后GORM schema升级为`0002_runtime_event_checkpoint`并增加0001→0002 migration/ready测试，Task内部持久化run-ref/cursor/sequence但现有transport encoder不投影；event-capable dispatch只绑定validated `run_` receipt；ProjectOwnerEvent以optimistic task version原子推进checkpoint/event/receipt/artifact，exact replay不增version或receipt，run drift、same-sequence cursor conflict、gap、unsafe ref、unknown outcome均fail closed；terminal evidence只进入Artifact safe ref；全service tests、pure-Go build、vet、repository/registry/app/clientruntime/runtime race、42项SDK/contract与strict Spec均exit 0；integration run `20260720105108-2f9b665e-3927-4810-9506-292fabef4401`、e2e run `20260720105108-b12e5341-e112-4eaf-945e-18a73ec8a605` passed/exit 0、六件套完整且敏感扫描无命中；Failure re-check: cursor未与task version原子提交、duplicate产生二次receipt、公开Task shape漂移或raw payload落盘即回滚。
- [x] 3.3d Owner: Workbench Implementer；Lane: typed-task-event-pump；Dependencies: 3.3b, 3.3c；Scope: 在clientruntime增加有界EventSession→OwnerEventSink bridge，从Task持久化run-ref/cursor/version恢复并逐条调用TaskService projector；Acceptance: 每次成功使用返回version继续，terminal/rejected/EOF停止，cursor repair/sink conflict/gap/max-events停止且不retry owner；bridge不注册route/goroutine/Web；Verification: `CGO_ENABLED=0 go test ./service/internal/clientruntime -run '^TestTaskEventPump' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/clientruntime -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/... && bun run test:contract && openspec validate workbench-client-runtime-adapter --strict --no-interactive`；Expected: resume/version/terminal/reconcile/bound tests green；Evidence: 2026-07-20 missing PumpTaskEvents/TaskEventPumpConfig compile RED后新增无goroutine的bounded bridge；测试证明从持久cursor恢复、sink expected version按7→8推进、terminal停止，cursor repair同时保留reconcile error、sink version conflict透传、max-events在读取下一event前停止且不busy-loop；runtime compile assertion证明TaskService实现OwnerEventSink；bridge只调用WatchEvents/ProjectOwnerEvent、不触发Start/Cancel/Reconcile；全service tests、pure-Go build、vet、repository/registry/app/clientruntime/runtime race、42项SDK/contract与strict Spec均exit 0；Failure re-check: pump使用stale version、吞掉repair错误、无限loop或触发owner operation即回滚。
- [x] 3.3e Owner: Workbench Implementer；Lane: runtime-owned-event-supervisor；Dependencies: 3.2i, 3.3d；Scope: TaskService仅在event-capable running task及safe run ref已持久化后通知runtime-owned supervisor，由supervisor按task+run去重启动一个bounded pump并在Shutdown取消等待；Acceptance: observer不与TaskService mutation锁死锁，不在持久化前启动，不重复watch，不自动retry owner，runtime关闭后无脱离goroutine；Verification: `CGO_ENABLED=0 go test ./service/internal/app ./service/internal/clientruntime ./service/internal/runtime -run 'ObservesOwnerRun|TaskEventSupervisor|AigoraGenerate' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/registry ./service/internal/app ./service/internal/clientruntime ./service/internal/runtime -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/... && bun run test:integration && bun run test:e2e && bun run test:contract`；Expected: generate flag拥有bounded event lifecycle且Web仍未promotion；Evidence: 2026-07-20 missing supervisor/observer compile RED后首次实现暴露同锁observer自死锁，timeout stack定位后拆分独立RWMutex并保持post-mutation通知；single-watch、terminal projection、runtime ownership与Shutdown测试通过；full service/build/vet、四package race、42项contract通过；integration `20260720113252-1eee35c6-9154-4c40-af0b-076feacc9863`、e2e `20260720113253-f19d47e0-aa2f-4a95-8dcf-063cfad75ffe` passed/exit 0；Failure re-check: observer在mutation前触发、duplicate watch、shutdown泄漏、owner retry或死锁即回滚。
- [x] 3.3f Owner: Workbench Implementer；Lane: restart-event-recovery；Dependencies: 3.3e；Scope: workbenchd启动时通过GORM只加载fixed generate下仍为running且具有safe run ref的任务，从持久化cursor/sequence/version恢复bounded event pump；Acceptance: 最多64个恢复任务，超过上限启动fail closed，terminal/no-run/other-operation不恢复，后续构造失败时supervisor也必须Shutdown；Verification: `CGO_ENABLED=0 go test ./service/internal/repository ./service/internal/clientruntime ./service/internal/runtime -run 'ListsOnlyRecoverable|RecoversOnlyRunning|AigoraGenerateFlagOwns' -count=1 && CGO_ENABLED=1 go test -race ./service/internal/repository ./service/internal/clientruntime ./service/internal/runtime -count=1 && CGO_ENABLED=0 go test ./service/... -count=1 && CGO_ENABLED=0 go build ./service/... && go vet ./service/...`；Expected: 重启后从last cursor续接且不重发owner operation；Evidence: 2026-07-20 GORM targeted recovery query、cursor_7→sequence 8恢复测试、64-task fail-closed边界与constructor cleanup已实现；full Go tests/build/vet及关键包race exit 0；Failure re-check: unbounded goroutine、从空run ref恢复、cursor归零或构造失败泄漏即回滚。
- [x] 3.3 Owner: Workbench Implementer；Lane: events-receipts；Dependencies: 3.2；Scope: 实现 cursor/dedupe/reconnect、redacted receipt/evidence refs 与 process outcome taxonomy；Acceptance: 无 raw stdout/stderr/provider payload；Verification: service event/receipt tests + conformance；Expected: green；Evidence: 3.3a–3.3f已覆盖typed broker source、bounded reconnect、atomic GORM checkpoint、dedupe/gap/reconcile、safe Receipt/Artifact、runtime supervisor与restart recovery；2026-07-20 full Go tests/build/vet、五包race、45项SDK/conformance通过，integration `20260720114734-c6a07431-a0bf-4ec1-87bc-fea3b90242a5`、SDK e2e `20260720114735-53936429-0e58-41cc-9c37-f9bf77a4190a` 均passed/exit 0且敏感扫描无命中；Failure re-check: redactor/parser failure必须 fail closed。
- [x] 3.4a Owner: Workbench Implementer；Lane: typed-sdk-hidden-web-canary；Dependencies: 3.2i, 3.3f；Scope: 在`WorkbenchClient.runtime`增加typed generate/cancel/reconcile/events facade并复用既有Task transport；Web只增加默认隐藏的generate canary；Acceptance: SDK运行时白名单仅保留inputRef与成组bounded limits，Web要求显式`VITE_WORKBENCH_RUNTIME_GENERATE_CANARY_ENABLED=true`、safe revision、fixed Aigora descriptor/readiness四项lifecycle capability及owner mutation/events/permission/cost registry同时满足；任何缺失均不渲染；Verification: `bun run typecheck && bun run test:contract && bun run web:test && bun run web:build && bun run test:integration && bun run test:e2e && bun run web:e2e`；Expected: typed API可用但Phase B仍未晋级；Evidence: 2026-07-20 SDK 45项contract、Web 84项组件+BFF 39项、typecheck/build通过；integration `20260720114734-c6a07431-a0bf-4ec1-87bc-fea3b90242a5`、SDK e2e `20260720114735-53936429-0e58-41cc-9c37-f9bf77a4190a`、browser e2e `20260720114734-7d6cabf6-34bf-44fc-90c8-45ec49a7bb0b` 均passed/exit 0、六件套完整且敏感扫描无命中；Failure re-check: 默认出现、connection flag误提升、缺cancel/reconcile仍显示、浏览器发送provider/model/credential/host或新建平行协议即回滚。
- [x] 3.4b Owner: Workbench Implementer；Lane: shared-browser-module-adoption；Dependencies: 3.4a, shared `client-module` canary；Scope: 让 Workbench SDK 的 connection-test/generate availability 与 runtime input normalization 消费 `@yeisme/client-runtime/client-module`，同时保留 Workbench registry/permission/cost/fixed-binding 门；Acceptance: descriptor/readiness capability 取交集，unsafe revision、unknown capability、partial/degraded/disabled、partial limits 均在 transport 前 fail closed，Web bundle 不引入 broker Node/token implementation；Verification: `bun test packages/task-sdk/test/runtime-client.test.ts && bun test packages/task-sdk && bun run typecheck && bun run test:contract && bun run web:test && bun run web:build && bun run test:integration && bun run test:e2e && bun run web:e2e`；Expected: 共享门生效且现有 Workbench UI/transport contract 不变；Evidence: 2026-07-20 TDD RED 证明旧手写 gate 会接受 `../../private/token` revision，切换共享 projector 后 broker SDK 19项、Workbench focused 15项、SDK 44项、contract 46项、typecheck、Web 96项+BFF 40项（4 skip）与 Vite build 全部通过；integration `20260720122212-851ed134-77ed-4059-bb75-8bf01017b801`、SDK/security e2e `20260720122211-7d716d82-4dbc-404a-bf07-9c23b3ad3980`、browser e2e `20260720122213-ea77b094-3e15-4907-a992-dd28f9479ee1` 均 passed/exit 0、六件套完整且敏感扫描无命中，browser dist Node/token 定向扫描无命中；Compatibility: `0.1.0-dev` sibling file dependency 仅为 monorepo canary，正式发布前切 tagged dependency；Failure re-check: shared module 缺失、bundle 引入 Node/token custody 或 Workbench product gate 被弱化即回滚。
- [x] 3.4c Owner: Workbench Product/Implementer；Lane: connection-first-support-promotion；Dependencies: 3.2j, 3.4b；Scope: 将Connections中的Aigora connection-test定义为独立first-support capability，继续由service flag、descriptor/readiness revision与Operation registry共同控制；Acceptance:默认build不显示按钮，显式启用且真实owner ready时可通过普通Task/gate路径执行；关闭flag立即从catalog/UI移除，terminal history保留且不重放；Verification: `bun run web:test && CGO_ENABLED=0 go test ./service/internal/runtime -run 'ConnectionTaskUsesReal|ConnectionTest' -count=1 && openspec validate workbench-client-runtime-adapter --strict --no-interactive`；Expected: connection_test可单独晋级，generate仍为隐藏canary；Evidence: 2026-07-20 Web 103项+BFF 48项（5 skip）通过，真实system run `20260720130339-385a7ae7-8e13-4cd2-91df-937a2ece2725` passed/exit 0；Failure re-check: owner evidence过期、flag关闭、revision/capability/registry任一缺失即恢复隐藏。
- [x] 3.4 Owner: Workbench Implementer；Lane: SDK-Web gated surface；Dependencies: 3.3；Scope: 提供 typed mutation/stream methods、connection-test first-support action与默认隐藏的generate canary；Acceptance: 只渲染 registry/owner allowed action，generate必须同时经过service flag与独立Web canary flag，任一缺失立即隐藏；Verification: `bun test packages/task-sdk/test/runtime-client.test.ts packages/task-sdk/test/runtime-wiring.test.ts packages/task-sdk/test/json-rpc.test.ts && cd apps/web && bunx vitest run test/routes.test.tsx`；Expected: only proven capabilities enabled；Evidence: 2026-07-22 SDK/JSON-RPC 24项与Web route 8项exit 0，默认generate flag关闭、connection flag不提升generate、unsafe input/revision与不完整lifecycle capability均在transport前fail closed；本项不等于Phase B产品晋级，4.4b仍保持pending；Failure re-check: 缺 evidence、默认出现generate或browser发送provider/credential字段时恢复disabled。

## 4. Verification, review and evidence

- [x] 4.1 Owner: Test Engineer；Lane: verify-phase-a；Dependencies: 2.4；Scope: 完整 OpenSpec/buf/Go/race/SDK/Web/build；Acceptance: 所有既有 surface 无回归；Verification: `openspec validate --all --strict && buf lint && CGO_ENABLED=0 go test ./service/... && CGO_ENABLED=1 go test -race ./service/... && bun run typecheck && bun test && bun run build && bun run test:contract && bun run web:test && bun run web:build`；Expected: exit 0；Evidence: 2026-07-19 全部 exit 0；Failure re-check: 修复首个确定失败后完整重跑。
- [x] 4.1b Owner: Test Engineer；Lane: verify-phase-b-stable-diff；Dependencies: 3.2k, 3.3f, 3.4b；Scope: 在 Workbench、broker 与 Aigora generate 实现稳定后，复跑 Workbench service/SDK/Web、broker lifecycle/event pump、Aigora canonical worker 的 focused/full/race/pure-Go build/vet 与 strict OpenSpec；Acceptance: 所有命令真实匹配测试且 exit 0，默认 build 仍不启用 generate；Verification: `CGO_ENABLED=0 go test ./service/... -count=1`、`CGO_ENABLED=1 go test -race ./service/internal/clientruntime ./service/internal/app ./service/internal/registry ./service/internal/repository ./service/internal/runtime -run 'RuntimeSecurity_|RuntimeCursor|TaskEventSupervisor|TaskServiceProjectsOwnerExecutionResults' -count=1`、`CGO_ENABLED=0 go build ./service/cmd/workbenchd`、focused `go vet`、`bun test packages/task-sdk && bun run test:contract`、`bun run typecheck && bun run web:test && bun run web:build && bun run web:e2e`；Expected: stable diff green；Evidence: 2026-07-22 当前 Workbench 工作树 service full、五包focused race、pure-Go workbenchd build、focused vet、SDK 118项、contract、typecheck、Web 310项+BFF 54项（5 skip）、Vite build、Playwright与strict OpenSpec均exit 0；generate system race 连续5次通过，supervisor shutdown覆盖driver包装的context cancellation；默认generate service/Web flags仍关闭。该本地复验不声称 broker/Aigora tagged release 或独立安全评审已完成；Failure re-check: 任一零用例、flaky、默认启用或依赖未固定即恢复pending。
- [x] 4.2 Owner: Security Reviewer；Lane: security-phase-a；Dependencies: 4.1 stable diff；Scope: 审查 Phase A browser/service/broker trust boundary、unauthorized access、unknown mode/owner、secret/private path projection、arbitrary shell/path/env 与 fail-closed fallback；Acceptance: 无 P0/P1；Verification: runtime negative tests + SDK normalization tests + stable diff；Expected: pass；Evidence: 2026-07-19 unauthorized HTTP、invalid HTTP/gRPC mode、unknown owner、unsupported contract/state/mode、secret-shaped field与private path扫描通过；Failure re-check: 修复后独立复验。
- [ ] 4.2b Owner: Security Reviewer；Lane: security-phase-b-generate；Dependencies: 4.1b stable diff, Aigora 5.3；Scope: 独立审查 Workbench Task gate → broker stored admission/reconcile pump → Aigora canonical plan/worker → credentialctl/provider 边界，覆盖 approval replay、revision/credential drift、同一 run 多订阅者、cancel/restart replay、unknown 不重发、SSRF/redirect/DNS/private-IP、secret/provider payload/private path 与轮询资源上限；Acceptance: 无 P0/P1，瞬时 reconcile 错误不得投影伪终态，cancel 重启重放保持 terminal cancelled，broker/Workbench 永不获得 credential Resolve 权限；Verification: 三项目全部 `TestRuntimeSecurity_`、broker reconcile event/race tests、Aigora safe transport/grant/worker tests及稳定 diff审查；Expected: pass before generate promotion；Current evidence: 实现者自检已确认 exact admitted request 重用、terminal shortcut、EventHub dedupe、cancel replay 与 process-output敏感扫描通过，但不得替代独立复核；Failure re-check: 任一风险缺负向测试或出现 P0/P1 时关闭 generate flag并回到 owning change修复。
- [x] 4.3 Owner: Integration Test Engineer；Lane: evidence-phase-a；Dependencies: 4.2；Scope: 运行 integration/system/browser e2e 六件套 evidence 与 sentinel scan；Acceptance: `summary.json`、`command.txt`、stdout/stderr/env/artifacts 完整且脱敏；Verification: `bun run test:integration && bun run test:e2e && bun run web:e2e`，随后扫描 `temp/integration-test-runs`；Expected: exit 0、sentinel无命中；Evidence: 2026-07-22 runs `20260722143242-0d13b4a8-028c-4a6d-b27e-9a30e887bd11`、`20260722143311-d743bb8f-1782-4d97-8f39-432d1d5b4333`、`20260722143357-d82db871-7dd6-4e5e-ad5b-6daafa7d7297` 均 passed/exit 0，六件套完整，runner 9项测试与敏感模式扫描无命中；Failure re-check: evidence 缺失/泄漏/错误 exit code 即阻断 promotion。
- [x] 4.3b Owner: Integration Test Engineer；Lane: evidence-phase-b-generate；Dependencies: 3.2k；Scope: 从真实 Workbench Task permission/cost gate，经 broker/Aigora/credentialctl/provider 验证 accepted→terminal event cursor、usage/cost refs、幂等与 queued cancel，并写入 system 六件套 evidence；Acceptance: provider 成功路径恰好一次调用，queued cancel 零新增调用，secret、Authorization、provider body/URL、raw prompt与private path不落盘；Verification: `bun scripts/test-evidence/run.ts --layer system --project client/yeisme-workbench -- env CGO_ENABLED=0 go test ./service/internal/runtime -run '^TestRuntimeSecurity_Workbench(ConnectionTaskUsesRealAigoraOwnerAndCredentialctl|GenerateTaskRunsCanonicalAigoraWorkerEventsAndCancel)$' -count=1` 后扫描该 run；Expected: passed/redacted；Evidence: 2026-07-22 run `20260722143327-f5b8651e-1ae7-4d29-b59c-51a824ff255c` passed/exit 0，六件套与敏感模式扫描通过；此证据重跑当前本地依赖链，但不替代4.2b、tagged dependency或4.4b promotion；Failure re-check: 此证据不替代4.2b、credentialctl tag或4.4b promotion。
- [x] 4.4a Owner: Product/Compatibility Review；Lane: promotion-phase-a；Dependencies: 4.3；Scope: 验收 descriptor/readiness、Broker 自动接入与 unavailable rollback；Acceptance: Phase A 作为 first-support，只发布 read-only capability；Verification: 引用 4.3 run IDs、runtime screenshot 与真实命令；Expected: descriptor/readiness 晋级，mutation 保持 disabled；Evidence: 2026-07-19 Phase A service/SDK/Web/evidence完成，run/event/cancel/reconcile未注册到客户端 transport/UI；Failure re-check: 不以 OpenSpec valid 替代真实测试。
