> **归档说明（2026-08-16）**：本 change 已交付只读体验基线等 17 项任务并归档；剩余 20 项未完成任务与完整 spec delta 当时迁移到后续 change。该后续 change 已于 2026-08-26 按用户决定取消并删除；归档继续只保留历史审计，未交付能力从未合入正式 specs。

## 1. Owner 合同与晋级门槛

- [ ] 1.1 Owner: Open Design；Lane: `owner-contract`；Dependencies: none；Paths: Open Design owner 的 `openspec/**`；Scope: 建立版本化 capability、prompt、candidate、review、handoff、event、receipt、cancel 与 reconcile 合同，并明确 opaque ref、expected version、idempotency 和 redaction；Verification: 在 owner 仓库运行 `openspec validate --all --strict`；Expected: Workbench 可消费机器可校验合同，不依赖 CLI human output；Failure recheck: owner 未接受前 Workbench 保持 `needs_contract`，不得宣布完整功能。
- [ ] 1.2 Owner: Workbench Architecture；Lane: `contract-mapping`；Dependencies: 1.1；Paths: `openspec/changes/complete-open-design-studio-workflow/**`, `docs/product/**`；Scope: 将 owner 方法逐项映射到 Workbench read projection 与六个 Operation，记录 unsupported cancel/preview 格式；Verification: `openspec validate complete-open-design-studio-workflow --strict`；Expected: 每个主路径动作有唯一 owner 合同和错误语义；Failure recheck: 删除无法追溯到批准 owner endpoint 的 speculative action。
- [x] 1.3 Owner: Workbench Security；Lane: `threat-model`；Dependencies: 1.1；Paths: `docs/security/**`, 本 change `design.md`；Scope: 评审 raw prompt 瞬时传输、preview sandbox、SSRF、opaque ref、token、日志、evidence 与 unknown accept；Verification: `git diff --check` 加安全评审记录；Expected: blocker 在实现前归属到明确 lane；Failure recheck: 任一 secret/private path 持久化风险阻断下一阶段。
- [x] 1.4 Owner: Workbench Architecture；Lane: `owner-handoff-draft`；Dependencies: none；Paths: `docs/implementation/open-design-owner-contract-handoff.md`, 本 change `design.md`；Scope: 形成待 Owner 批准的 capability、read method、六 Operation、event、receipt、reconcile、错误、preview 与验收语义交接稿；Verification: `openspec validate complete-open-design-studio-workflow --strict && git diff --check`；Expected: Owner 可直接据此建立自身 OpenSpec，且文档不伪装为当前能力；Failure recheck: endpoint binding 未批准前只使用稳定语义 method id，Workbench 保持 fail-closed。

  当前证据：已完成 `docs/implementation/open-design-owner-contract-handoff.md`，并在 `design.md` 建立 Owner method id 到 Workbench projection/Operation 的唯一映射。`1.2` 仍保持未完成，直到 Owner `1.1` 批准具体合同版本与 transport binding。

  Legacy 评估：公开 source map 已确认 finalize 为同步长请求且客户端 abort 不保证 Owner 取消，handoff 为 conversation transcript synthesis，通用 run cancel 未发布 Design 领域 receipt/reconcile 绑定；这些接口不能替代批准合同，也不能用于提前勾选 `1.2`、`3.2` 或 `4.2`。
- [x] 1.5 Owner: Workbench Contract Canary；Lane: `owner-contract-canary`；Dependencies: 1.4；Paths: `scripts/owner-contract-canary.ts`, `tests/owner-contract-canary.test.ts`, `Taskfile.yml`；Scope: 提供只读 machine-readable contract canary，校验必需 read/Operation、version、payload、event cursor、receipt/reconcile、preview 与 stable errors，并拒绝 credentialed URL、query/fragment、redirect、oversize 和原始错误回显；Verification: `task owner:contract:test`；Expected: canary 无 mutation，只输出安全版本和计数；Failure recheck: 实际 Owner 验收必须显式提供 `OPEN_DESIGN_CONTRACT_URL` 并通过 evidence target。

  当前证据：`task owner:contract:test` 通过 7/7，覆盖 SemVer、必需 Operation/reconcile、credentialed URL、query/fragment、redirect、Content-Type、流式 1 MiB 越界立即取消和原始值不回显；`temp/integration-test-runs/20260719193630-a11a5ed9-107c-4c3c-9112-177834bc433a/summary.json` 记录对当前 Owner `/api/version` 的预期失败 canary，安全错误为 `contractVersion is required`，证明应用版本不会被误判为生产合同；evidence 未记录 Owner URL、Authorization、token 或私有路径。

## 2. 共享合同与 SDK

- [x] 2.1 Owner: Workbench Contracts；Lane: `contracts`；Dependencies: 1.2；Paths: `proto/**`, `schemas/**`；Scope: additive 定义 workflow、reference、prompt version、candidate、comparison、review evidence/status、handoff draft/validation projection 与 capability 字段；Verification: `buf lint`；Expected: 字段使用 opaque refs、safe summaries 和版本信息，无 raw owner payload；Failure recheck: 不用通用 `map<string, any>` 压平领域类型。

  当前证据：在保留 `promptGenerate`、`reviewWrite`、`handoffExport` 等旧字段的同时，Proto 以新字段号 11–16 additive 增加六个 Operation capability；Go REST/JSON-RPC/gRPC 显式投影，TypeScript SDK 与 JSON Schema 将新字段保持可选以兼容旧 consumer。局部 Go transport/schema tests、31/31 contract tests、typecheck、build、76/76 Web tests 和 Web build 通过；重启后的真实预览 backend 对六项均返回 `needs_contract`，证明字段已运行且没有由旧粗粒度字段误放行。
- [x] 2.2 Owner: Workbench Contracts；Lane: `contracts`；Dependencies: 2.1；Paths: `proto/**`, `schemas/**`, `service/internal/registry/**`；Scope: 注册 `design.prompt.save`、`design.candidate.generate`、`design.candidate.cancel`、`design.review.decide`、`design.handoff.prepare`、`design.handoff.export` 的 request/result/error/gate schema；Verification: `buf lint && bun run test:contract`；Expected: 六个 Operation 可自动投影到四种 transport；Failure recheck: transport 层不得出现 operation-specific handler。
- [x] 2.3 Owner: Workbench SDK；Lane: `sdk`；Dependencies: 2.1；Paths: `packages/task-sdk/src/design-models.ts`, `packages/task-sdk/src/design-client.ts`, `packages/task-sdk/src/workbench-client.ts`, `packages/task-sdk/src/index.ts`；Scope: 实现 typed read clients、workflow models、projection pagination/freshness 和向后兼容导出；Verification: `bun test packages/task-sdk && bun run typecheck`；Expected: 旧 `WorkbenchTaskClient` 和现有 Design 方法保持可用；Failure recheck: 不在 SDK 复制服务端状态机。
- [x] 2.4 Owner: Workbench Conformance；Lane: `contract-tests`；Dependencies: 2.2, 2.3；Paths: `tests/conformance/**`, `packages/task-sdk/src/testing.ts`；Scope: 添加 REST、gRPC、JSON-RPC、TypeScript SDK parity，覆盖 success、validation、permission、cost、conflict、partial、unknown accept；Verification: `bun run test:contract`；Expected: 四调用面共享 task/error/event/receipt 语义；Failure recheck: 任一调用面差异回到 registry/schema 修复。

## 3. Design Read Projection

- [x] 3.1 Owner: Workbench Design Service；Lane: `read-service`；Dependencies: 2.1；Paths: `service/internal/design/**`；Scope: 实现 `GetDesignWorkflow`、reference、prompt version、candidate、comparison、review 与 handoff read projection，统一 pagination、freshness、size 和 redaction；Verification: `CGO_ENABLED=0 go test ./service/internal/design/...`；Expected: owner 离线时 fail-closed，Task 控制面继续可用；Failure recheck: 不缓存 raw body 或私有路径。
- [ ] 3.2 Owner: Workbench Owner Adapter；Lane: `read-adapter`；Dependencies: 1.1, 3.1；Paths: `service/internal/adapters/**`, `service/internal/design/**`；Scope: 实现 Open Design version negotiation、typed reads、timeout、redirect/URL allowlist 与 opaque ref re-resolution；Verification: `CGO_ENABLED=0 go test ./service/internal/adapters/... ./service/internal/design/...`；Expected: `available`、`degraded`、`offline`、`needs_contract`、`contract_mismatch` 状态真实；Failure recheck: 禁止动态 endpoint、CLI 解析和目录读取。
- [x] 3.3 Owner: Workbench Preview Gateway；Lane: `preview-security`；Dependencies: 3.2；Paths: `service/internal/adapters/**`, `apps/web/server/**`；Scope: 实现 raster preview 同源代理、MIME/size/cache/CSP/download 边界，并为未来 HTML preview 提供默认关闭 capability；Verification: `bun test apps/web/server && CGO_ENABLED=0 go test ./service/...`；Expected: preview 无凭据且不可访问父页面/session；Failure recheck: HTML 未通过安全评审时保持 unsupported。
- [ ] 3.4 Owner: Workbench Read Integration；Lane: `read-integration`；Dependencies: 3.1, 3.2, 3.3；Paths: `service/test/conformance/**`, `scripts/test-evidence/**`；Scope: 用结构化 owner fake server 覆盖 pagination、stale version、offline、contract mismatch、oversize、unsafe MIME 和 redaction；Verification: `bun run test:integration`；Expected: 每次运行写入标准脱敏 evidence；Failure recheck: fixture 不得返回 owner 私有字段后再由 UI 隐藏。
- [ ] 3.5 Owner: Workbench Owner Canary；Lane: `owner-read-canary`；Dependencies: 1.2, 3.2, 3.4；Paths: `scripts/test-evidence/**`, `docs/runtime/**`；Scope: 对显式配置的测试 Owner 执行 capability negotiation 与只读 projection canary，禁止 mutation 探测，并写入脱敏 evidence；Verification: `task test:owner-read-canary OPEN_DESIGN_URL=<test-owner-url>`；Expected: 合同版本、method/schema、project scope、opaque ref 与 redaction 均通过；Failure recheck: Owner URL 未显式提供时跳过而非回退默认生产地址，任何私有路径进入 evidence 立即失败。

## 4. Mutation、Task 与恢复

- [x] 4.1 Owner: Workbench Task Registry；Lane: `mutation-core`；Dependencies: 2.2；Paths: `service/internal/registry/**`, `service/internal/core/**`；Scope: 将六个 Design Operation 接入现有 permission、cost、expected-version、idempotency、attempt、event 与 receipt 流程；Verification: `CGO_ENABLED=0 go test ./service/internal/registry/... ./service/internal/core/...`；Expected: Design mutation 无独立状态机；Failure recheck: 任一正文进入 Task persistence/event 必须阻断。
- [ ] 4.2 Owner: Workbench Mutation Adapter；Lane: `mutation-adapter`；Dependencies: 1.1, 4.1；Paths: `service/internal/adapters/**`；Scope: 实现 prompt save、generate、cancel request、review decide、handoff prepare/export 的 typed invoke、non-persisted body、item receipt 与 owner error mapping；Verification: `CGO_ENABLED=0 go test ./service/internal/adapters/...`；Expected: prompt body 只在瞬时 invoke 内存在；Failure recheck: cancel unsupported 时不得确认 `cancelled`。
- [ ] 4.3 Owner: Workbench Reconcile；Lane: `reconcile`；Dependencies: 4.2；Paths: `service/internal/core/**`, `service/internal/adapters/**`；Scope: 实现 unknown accept、receipt/status reconcile、partial child retry、SSE cursor 恢复和 projection invalidation；Verification: `CGO_ENABLED=0 go test ./service/... -run 'UnknownAccept|Reconcile|Partial|Cursor'`；Expected: 未确认副作用不自动重放；Failure recheck: timeout 不得被映射为普通 failed。
- [x] 4.4 Owner: Workbench Persistence；Lane: `persistence-audit`；Dependencies: 4.1, 4.3；Paths: `service/internal/persistence/**`, `service/internal/core/**`；Scope: 验证现有 GORM schema 足以保存 task metadata/safe refs/receipts，仅在必要时做 additive migration，并添加敏感字段负向测试；Verification: `CGO_ENABLED=0 go test ./service/internal/persistence/... ./service/internal/core/...`；Expected: 无设计正文、artifact blob 或 credential 表；Failure recheck: 新增持久化字段必须回到设计评审。
- [ ] 4.5 Owner: Workbench Mutation Integration；Lane: `mutation-integration`；Dependencies: 4.2, 4.3, 4.4；Paths: `service/test/conformance/**`, `tests/conformance/**`；Scope: 覆盖六 Operation 的 happy path、permission/cost reject、version conflict、partial export、unknown accept、cancel unsupported 与 reconcile；Verification: `bun run test:contract && bun run test:integration`；Expected: 原退出码和脱敏 evidence 完整；Failure recheck: 不以跳过失败 Operation 或 fixture success 宣称通过。

## 5. Web 工作流基础

- [x] 5.1 Owner: Workbench Web State；Lane: `web-state`；Dependencies: 2.3, 3.1, 4.1；Paths: `apps/web/src/studio/**`；Scope: 建立 phase + overlay projection、React Query keys、Task event cursor、selection、dirty prompt、capability gate 与下一步 selector；Verification: `bun run web:test`；Expected: refresh/offline 不把业务 phase 误写为 failed；Failure recheck: 复杂状态转换必须有中文设计注释和单元测试。
- [x] 5.2 Owner: Workbench Web Shell；Lane: `web-shell`；Dependencies: 5.1；Paths: `apps/web/src/studio/pane-workspace.tsx`, `apps/web/src/studio/layout.ts`, `apps/web/src/studio/shell.tsx`；Scope: 把 Project、Prompt、Candidate、Review、Handoff、Activity/Evidence 注册为可停靠 panel，保持 maximize/restore/float/reset 与 project/device scoped recovery；Verification: `bun run web:test`；Expected: selection、workflow 和 dirty state 在布局操作中保持；Failure recheck: 损坏 schema 回退 preset，不删除用户项目状态。
- [x] 5.3 Owner: Workbench Command Surface；Lane: `web-command`；Dependencies: 5.1；Paths: `apps/web/src/**`；Scope: 让命令面板与快捷键按 capability/gate 发现动作，执行前展示 permission、cost、version 条件；Verification: `bun run web:test`；Expected: 不可执行动作有真实原因和下一步；Failure recheck: 禁止隐藏 gate 后直接调用 owner。
- [x] 5.4 Owner: Workbench Preview Lifecycle；Lane: `preview-lifecycle`；Dependencies: 5.2；Paths: `Taskfile.yml`, `scripts/preview-lifecycle.ts`, `docs/runtime/local-web-workbench.md`；Scope: 提供前台 preview 与受管后台 up/status/logs/smoke/down，后台使用独立 process group 和 PID 文件且不得扫描/停止非受管进程；Verification: `task preview:up && task preview:status && task preview:smoke && task preview:down && task preview:up`；Expected: down 后 backend/Web 端口关闭，最终 preview 恢复健康；Failure recheck: PID 缺失或损坏时必须拒绝发信号并保留现有预览。

  当前证据：已验证无 PID 文件时安全 no-op、损坏 PID 返回非零且现有预览保持健康；真实迁移后完成 up→status→smoke→down→两个端口不可达→up→status→smoke 全循环。当前受管 preview 已重新启动，Studio 保持 `http://127.0.0.1:4173/studio/open-design-studio-experience` 可用。

## 6. Prompt、Candidate 与 Review UI

- [ ] 6.1 Owner: Workbench Prompt UI；Lane: `web-prompt`；Dependencies: 5.1, 4.5；Paths: `apps/web/src/studio/**`, `apps/web/test/**`；Scope: 实现 editor、save、history、diff、lineage、dirty warning、conflict reload 和 generate input selection；Verification: `bun run web:test`；Expected: prompt body 不进 localStorage、URL、日志或测试快照；Failure recheck: offline 时不得显示已保存。

  当前证据（未完成）：已实现只驻留 React 内存的 textarea、确定性行级 diff、Owner 版本历史只读投影、`beforeunload` 脏状态保护、源文件切换确认、reload，以及仅在 capability、Operation 和 owner version 均满足时调用通用 TaskService 的 save submission。提交后界面明确保持“未保存”，直到 Owner 安全投影确认；无合同、离线或 registry 缺失时 fail-closed。`apps/web/test/prompt-editor.test.tsx` 覆盖正文不进入 localStorage、diff、离开保护、gate 与非乐观提交；`apps/web/e2e/workbench.spec.ts` 覆盖真实浏览器编辑路径。Owner 写合同、version conflict 回读和 generate input selection 尚未完成，因此 6.1 保持未勾选。
- [ ] 6.2 Owner: Workbench Candidate UI；Lane: `web-candidate`；Dependencies: 3.4, 4.5, 5.2；Paths: `apps/web/src/studio/**`, `apps/web/test/**`；Scope: 实现 generate/cancel request、1–4 compare、差异维度、lineage、partial retry、安全 preview 与 Inspector；Verification: `bun run web:test && bun run web:build`；Expected: 未选 preview 不全量预取，成功 item 不被重复重试；Failure recheck: preview 失败显示 fallback 和 safe diagnostics。

  当前证据（未完成）：已实现 24 项分页与显式 load-more、基于 Owner prompt version/reference/skill/design system/scope 的生成表单、通用 TaskService 非乐观提交、运行任务 cancel request、partial child retry、1–4 safe comparison、候选 lineage、Task 关联与 preview contract 缺失 fallback。取消和 retry 只刷新 Task 投影，不提前刷新或伪造 Owner 候选结果。`apps/web/test/candidate-generation.test.tsx` 与 `apps/web/test/studio.test.tsx` 覆盖 gate、完整输入、分页、cancel、partial retry 和非乐观状态；浏览器 E2E 验证同源 BFF、无 Authorization、HTTP input 与 Axe。Owner mutation/read 合同和候选 preview bytes 尚未晋级，因此 6.2 保持未勾选。
- [ ] 6.3 Owner: Workbench Review UI；Lane: `web-review`；Dependencies: 4.5, 5.2；Paths: `apps/web/src/studio/**`, `apps/web/test/**`；Scope: 实现 evidence 分类、风险筛选、短理由、accept/reject/request_revision、expected-version 和 decision receipt；Verification: `bun run web:test`；Expected: conflict 强制 reload/recompare，理由不包含完整内部推理；Failure recheck: 不得 optimistic 显示 accepted 后忽略 owner result。

  当前证据（未完成）：已在现有 evidence 分类与风险筛选基础上实现证据多选、8–280 字符短理由，以及 `request_revision`、`reject`、`accept` 三种决策；提交统一调用 TaskService `design.review.decide`，携带 candidate ref、evidence refs、`expectedOwnerVersion` 和 idempotency key。界面仅显示 Task submission/receipt，并明确等待 Owner projection，不乐观改写 review 状态；Owner 确认后仅从只读投影显示 `decisionRef`。遇到 `task_version_conflict` 时锁定全部决策按钮，要求 reload Owner review/evidence 并重新比较后才能重试；短高度 Dockview pane 改为整体滚动，避免决策区遮挡。`apps/web/test/review-decision.test.tsx`、`apps/web/test/studio.test.tsx` 与 `apps/web/e2e/workbench.spec.ts` 覆盖 gate、请求体、非乐观状态、Owner receipt projection 和 expected-version 冲突恢复。Owner mutation/read 合同尚未晋级，真实 decision receipt 仍无法端到端确认，因此 6.3 保持未勾选。
- [ ] 6.4 Owner: Workbench Workflow Integration；Lane: `web-workflow-integration`；Dependencies: 6.1, 6.2, 6.3；Paths: `apps/web/test/**`, `apps/web/src/studio/**`；Scope: 覆盖 prompt save→generate→compare→request revision→new version→regenerate→accept；Verification: `bun run web:test`；Expected: lineage 与当前下一步在每个阶段一致；Failure recheck: 使用 MSW 合同响应，不在组件内伪造成功。

  当前证据（未完成）：Candidate safe projection 已 additive 增加可选 `supersedesCandidateRef` 与 `revisionDecisionRef`，使用 Proto 新字段号 14/15、JSON Schema 可选属性、Go/TypeScript 可选字段和 gRPC 显式映射；旧 consumer 可忽略，回滚可停止发送并删除 UI 展示，不改变既有字段语义。Candidate Compare 仅在选择后显示被替代 candidate 与 revision decision safe refs。状态化 Playwright 场景完整执行 prompt v2 保存→candidate 1 生成→request revision→prompt v3 保存→candidate 2 再生成→lineage 核对→accept→handoff prepare→export，并精确断言八个 TaskService Operation 顺序与 `v1` 至 `v8` expected owner version；每次 mutation 后先验证 UI 保持 pending，再通过 reload Owner projection 推进。`buf lint`、`bun run test:contract`、Design Go transport tests、76/76 Web tests、typecheck、build 与 25/25 Playwright 均通过。该场景仍使用批准 shape 的浏览器路由夹具，真实 Owner 写合同未晋级，因此 6.4 保持未勾选。

## 7. Handoff、Activity 与响应式体验

- [ ] 7.1 Owner: Workbench Handoff UI；Lane: `web-handoff`；Dependencies: 4.5, 5.2, 6.3；Paths: `apps/web/src/studio/**`, `apps/web/test/**`；Scope: 实现 manifest prepare/preview、强制项、blocker、target、export、item receipt 和 partial child retry；Verification: `bun run web:test && bun run web:build`；Expected: blocker 阻止 export，成功 item receipt 被保留；Failure recheck: UI 不可删除 contract gaps 和 verification checklist。

  当前证据（未完成）：已实现 target owner 输入与 prepared manifest 锁定、`design.handoff.prepare` 非乐观提交、Owner manifest/version 投影、required item 强制选中、optional item 显式选择、blocker/warning 展示、ready validation gate、`design.handoff.export` 非乐观提交、逐项 Owner `receiptRef` 展示、partial export Task 的显式 child retry，以及 expected-version 冲突后的 mutation 锁定与 Owner manifest/validation reload。prepare 使用 workflow owner version 和 selected candidate；export 使用 manifest owner version、handoff ref、锁定 target 与所选 item refs。成功 Task 只刷新 Task projection，Owner manifest/delivery 状态等待 projection 或 Task event 后再更新，不伪造 delivered；partial retry 保留成功 item receipt。`apps/web/test/handoff-workflow.test.tsx`、`apps/web/test/studio.test.tsx` 与 `apps/web/e2e/workbench.spec.ts` 覆盖 required blocker、target、prepare/export 请求体、receipt、非乐观状态、partial retry、冲突恢复、BFF-only network 和 Axe。Owner mutation/read 合同尚未晋级，真实 manifest/export receipt 仍无法端到端确认，因此 7.1 保持未勾选。
- [x] 7.2 Owner: Workbench Activity UI；Lane: `web-activity`；Dependencies: 4.3, 5.2；Paths: `apps/web/src/studio/**`, `apps/web/test/**`；Scope: 实现 Task/event/receipt timeline、filter、safe ref copy、SSE reconnect、unknown accept reconcile 和 connector diagnostics；Verification: `bun run web:test`；Expected: diagnostics 不含 raw payload、token、prompt 或私有路径；Failure recheck: 网络离线和业务失败必须可区分。
- [ ] 7.3 Owner: Workbench Responsive/A11y；Lane: `web-accessibility`；Dependencies: 6.4, 7.1, 7.2；Paths: `apps/web/src/**`, `apps/web/e2e/**`；Scope: 完成键盘主路径、focus/live region、Tooltip locale、reduced motion、移动端 Tab/Sheet 和 preview maximize；Verification: `bun run web:test && bun run web:e2e`；Expected: 430×932 到 2560×1440 均可完成工作流；Failure recheck: 移动端不开放自由 docking。

  当前证据（未完成）：2026-07-19 已验证 430×932、1024×768、1440×900、2560×1440 的固定标签、键盘切换、reduced motion、preview maximize/restore 与 Axe critical/serious 清零；430×932 真实 Handoff 场景还完成 target 输入、prepare、prepared target 锁定、required/optional item、export、partial retry，并确认移动端不显示 Float/Maximize 自由 docking。Dockview 7.0.2 浮窗 `dialog` 错配 `aria-level` 已局部修复。完整真实 Owner 主路径仍依赖 6.1–6.4 与 7.1 晋级，因此保持未勾选。

## 8. 系统验证与发布

- [ ] 8.1 Owner: Workbench E2E；Lane: `browser-e2e`；Dependencies: 6.4, 7.1, 7.2, 7.3；Paths: `apps/web/e2e/**`, `scripts/test-evidence/**`；Scope: 覆盖 save→generate→compare→review→revise/accept→handoff→export、reload、offline、partial、conflict、unknown accept 与 layout recovery；Verification: `bun run web:e2e`；Expected: 浏览器 network 只到 BFF，失败保留 screenshot/trace/evidence；Failure recheck: 不以 mock 绕过 TaskService 主路径。

  当前证据（未完成）：`temp/integration-test-runs/20260719191819-f9c5b5ea-415a-44a2-a824-06fbbfa9bcb5/summary.json` 记录 25/25 Playwright 场景通过；状态化场景验证 prompt save→generate→request revision→new prompt version→regenerate→revision lineage→accept→handoff prepare→export 的完整恢复链及八个 expected-version，并继续覆盖 430×932 Handoff partial retry、Review conflict recovery、Candidate generation、Prompt 内存编辑/diff/脏状态保护、opaque SSE 文件刷新、raster preview 版本失效、浏览器无 Authorization、pane maximize/restore 与 Axe。该证据使用浏览器路由夹具，不能替代受 Owner 写合同阻断的真实 TaskService save→export 主路径，故 8.1 保持未勾选。
- [ ] 8.2 Owner: Workbench Security Review；Lane: `security-review`；Dependencies: 3.3, 4.5, 8.1；Paths: read-only whole diff；Scope: 审查 token、SSRF、redirect、opaque ref、preview sandbox、prompt persistence、event/log/evidence redaction 和 unknown accept；Verification: `bun run test:integration && bun run web:e2e` 加安全报告；Expected: blocker 与 high risk 清零；Failure recheck: 问题回到对应 owner lane，不通过文档豁免。
- [ ] 8.3 Owner: Workbench Performance；Lane: `performance`；Dependencies: 8.1；Paths: `apps/web/**`, `service/**`, diagnostic artifacts only；Scope: 测量 warm shell、panel switch、projection p95、preview concurrency、SSE reconnect 和内存增长；Verification: 使用仓库现有浏览器/Go profiling 命令并保存脱敏结果；Expected: 满足设计目标或记录已接受预算；Failure recheck: 不以主观“流畅”替代数据。
- [ ] 8.4 Owner: Workbench Docs；Lane: `docs`；Dependencies: 8.1, 8.2；Paths: `README.md`, `docs/product/**`, `docs/runtime/**`；Scope: 更新完整工作流、owner 配置、真实启动命令、状态恢复、限制与故障排查，移除主路径 `needs_contract` 文案；Verification: `openspec validate complete-open-design-studio-workflow --strict && git diff --check`；Expected: 文档只宣称已通过功能；Failure recheck: optional/unsupported capability 保持明确标记。
- [ ] 8.6 Owner: Workbench Release Canary；Lane: `owner-mutation-canary`；Dependencies: 3.5, 4.5, 8.1, 8.2；Paths: `scripts/test-evidence/**`, `docs/runtime/**`；Scope: 经用户显式指定的专用测试项目执行真实 prompt save→candidate generate→review→handoff prepare/export，并验证 Task/event/receipt/reconcile 与 Owner projection；Verification: `task test:owner-workflow-canary OPEN_DESIGN_URL=<test-owner-url> OPEN_DESIGN_PROJECT=<test-project-ref>`；Expected: 六 Operation 均通过真实合同且 evidence 无正文、token、私有路径；Failure recheck: 禁止自动选择普通用户项目，失败或 unknown accept 必须先 reconcile，不得自动重放。
- [x] 8.7 Owner: Workbench Readiness Gate；Lane: `readiness-gate`；Dependencies: 2.1；Paths: `scripts/workflow-readiness.ts`, `tests/workflow-readiness.test.ts`, `Taskfile.yml`；Scope: 机器检查除 Final Gate 自身外的 OpenSpec task 与六个 granular Operation capability，旧四项粗粒度 capability 不得推断通过；Verification: `task workflow:readiness:test`；Expected: 缺失字段 fail-closed 为 `needs_contract`，未知状态归一为 `unavailable`，输出不含 token、URL 或原始 Owner payload；Failure recheck: readiness 只能在真实运行时六项均为 `available` 时通过。

  当前证据：`task workflow:readiness:test` 通过 4/4；当前真实本地 backend 的预期阻塞运行报告 16 个完成前置 task、19 个 pending task 和六项 `needs_contract`，且逐项列出安全 task owner 与 capability key，不输出 token、URL 或 Owner payload。Final Gate 已改为依赖 8.6 Release Canary，并将 `task workflow:readiness` 纳入最终命令。
- [ ] 8.5 Owner: Workbench Final Gate；Lane: `final-gate`；Dependencies: 2.4, 3.4, 4.5, 8.1, 8.2, 8.3, 8.4, 8.6；Paths: whole subproject；Scope: 执行完整发布门禁、机器 readiness 检查并核对主路径六 Operation capability；Verification: `openspec validate --all --strict && buf lint && CGO_ENABLED=0 go test ./service/... && CGO_ENABLED=1 go test -race ./service/... && bun run typecheck && bun test && bun run build && bun run test:contract && bun run test:integration && bun run web:build && bun run web:test && bun run web:e2e && task workflow:readiness`；Expected: 全部通过、evidence 脱敏、除 Final Gate 自身外无 pending task、主路径无 `needs_contract`；Failure recheck: 任一失败不得以 demo、fixture-only 或跳过 owner 方式宣布 production complete。
