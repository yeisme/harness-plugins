## 1. 合同冻结与现状审计

- [x] 1.1 Owner: root/architect；Dependencies: none；Paths: 本 change、`docs/interfaces/agent-pi-workspace.md`；冻结 `ProposalRecordV1`、`ProposalDecisionAttemptV1`、closed decision/status/error enum、safe ref/文本上限、revision/idempotency/expiry 语义和四 transport method identity；Acceptance: proto/schema/service/SDK/UI 只有一套字段与状态命名；Verification: `openspec validate workbench-agent-proposal-authority-v1 --strict`；Expected: strict validation pass；Failure recheck: 若同一状态在 Agent/Mission Brief/Task 中含义不同，先写 compatibility mapping，不进入实现。 Evidence (2026-08-21): proposal.proto(WorkbenchAgentProposalService 4 RPC)+proposal.schema.json+domain closed enums 冻结单一命名；buf lint/generate clean，SDK contract 363/363。
- [x] 1.2 Owner: debugger/read-only；Dependencies: 1.1；Paths: `packages/task-sdk/src/agent-*`, `apps/web/src/workbench/agent/**`, `service/internal/agentruntime/**`, `service/internal/missionbrief/service/accept.go`, `service/internal/core/**`；记录当前浏览器 `basisRefs`/expected version 直接提交 Task 的完整路径与缺失重载点；Acceptance: 每个不可信输入、权威来源、dispatch boundary 和 `unknown_accept` 分支均有文件/符号证据；Verification: targeted `rg` + focused tests inventory；Expected: audit 可直接指导 4.1–6.4；Failure recheck: 不以 UI disabled 推断后端安全。 Evidence (2026-08-21): 审计结论落码——旧浏览器路径 apps/web agent-conversation-workspace.tsx acceptProposal（basisRefs 直组）已改 typed decideProposal；SDK acceptProposal 标记 @deprecated；registry orbitProposalAcceptSchema 冻结 proposalDecisionRef-only（additionalProperties:false），legacy payload 在 ValidateInput 即拒（bridge 集成测试覆盖）。
- [x] 1.3 Owner: architect；Dependencies: 1.1, 1.2；Paths: `service/internal/proposalauthority/**`, `service/internal/missionbrief/**` design notes；冻结共享 authority kernel 与 Mission Brief adapter 边界；Acceptance: 同一 Owner mutation 不会被两个独立 proposal repository/accept service 重复 dispatch；Verification: design review + strict spec；Expected: one claim/dispatch/reconcile primitive；Failure recheck: 若迁移 Mission Brief 会改变其规范行为，拆出独立 compatibility change。 Evidence (2026-08-21): kernel 边界按 design §1 实现——proposalauthority(service+ports) 为共享 claim/dispatch/reconcile 原语；Mission Brief AcceptService 未迁移（3.6 独立切片），无双重 dispatch：两包各自调用 orbit.proposal.accept 且 authority 只有一处 sealed handler。
- [x] 1.4 Owner: root/security；Dependencies: 1.1；Paths: proto/schema/domain test fixtures；建立敏感 sentinel、跨 tenant/workspace/project/session/principal、stale descriptor/source、idempotency collision 与 dispatch-crash 威胁矩阵；Acceptance: 每个规范安全场景映射到至少一个 deterministic test；Verification: matrix review；Expected: 无 raw prompt/provider/tool/credential/private path/signed URL storage path；Failure recheck: 未能证明 redact 的字段保持禁止。 Evidence (2026-08-21): sentinel/跨租户/幂等碰撞矩阵以 deterministic tests 落地——cross-scope not_found（store_test/service_test）、unsafe ref 与 closed-enum fail closed（domain+SDK normalizer 测试）、unknown-field strict decode（HTTP handler 测试）、dispatch-crash durable replay→unknown（store_test）、idempotency conflict（store_test）。完整威胁矩阵文档随 7.3 安全 evidence 收敛。

## 2. Proposal domain、schema 与持久化

- [x] 2.1 Owner: implementer；Dependencies: 1.1；Paths: `api/proto/workbench/agent/v1alpha1/**`, `api/schema/workbench/agent/**`；新增 additive proposal projection、decision request/result、reconcile request/result、closed enums 与 typed conflicts；Acceptance: existing field numbers 不复用，`additionalProperties: false` 保持，unsafe arbitrary map 不进入合同；Verification: `buf lint && buf generate && bun run test:contract`；Expected: generated drift clean；Failure recheck: unknown enum/field fail closed 且不回显原值。 Evidence (2026-08-21): api/proto/workbench/agent/v1alpha1/proposal.proto（additive，existing field numbers 未复用）+ api/schema/workbench/agent/v1alpha1/proposal.schema.json（additionalProperties:false，无 arbitrary map）；buf lint+generate 通过，gen 代码入库。
- [x] 2.2 Owner: implementer；Dependencies: 1.1, 2.1；Paths: `service/internal/proposalauthority/domain/**`；实现 Proposal、DecisionAttempt、state transition、safe validation、digest 与 attention/event mapping；Acceptance: closed state machine 与 design 一致，accepted 不等于 Task succeeded；Verification: `CGO_ENABLED=0 go test ./service/internal/proposalauthority/domain/...`；Expected: transition/property tests pass；Failure recheck: 为复杂状态、错误和边界检查添加中文注释。 Evidence (2026-08-21): domain/proposal.go——closed 状态机（含 deciding→open pre-dispatch 回退、decision_unknown→open reconcile 重开）、DecisionAttempt、bounded Limits、RequestDigest/ProposalDigest；全部经 repository/service/bridge 测试。
- [x] 2.3 Owner: implementer；Dependencies: 2.2；Paths: GORM models/migrations/repository；新增 forward-only additive tables/indexes/unique constraints，覆盖 proposal revision、principal-scoped idempotency、single active acceptance、decision/receipt refs 与 high-water；Acceptance: pure-Go SQLite 与 managed PostgreSQL schema 均可迁移，rollback 不删除数据；Verification: `CGO_ENABLED=0 go test ./service/internal/migrations/... ./service/internal/proposalauthority/repository/...`；Expected: migration/checksum/concurrency pass；Failure recheck: 不以进程内 mutex 替代 DB 唯一约束。 Evidence (2026-08-21): repository/models.go 5 张 additive 表（workbench_agent_proposals/basis_refs/decisions/events/event_streams）；migration 0028（AutoMigrate，无破坏性回退）；lifecycle_catalog+inventory 双注册；schema version bump 0028 并更新 4 个 pin 测试，migration/lissionbrief/board schema 测试全绿。
- [x] 2.4 Owner: implementer；Dependencies: 2.3；Paths: repository；实现 register/get/claim/complete/reconcile/expire/supersede/idempotent replay 与 cursor-safe event append；Acceptance: crash-safe dispatch intent 先于 external call 持久化，stale revision 不覆盖新状态；Verification: focused repository + `CGO_ENABLED=1 go test -race`；Expected: race/idempotency/restart pass；Failure recheck: 任何 dispatch 后丢记录路径必须转 `decision_unknown`。 Evidence (2026-08-21): store.go——BeginDecision（幂等/revision/decision_in_progress CAS+FOR UPDATE）、CompleteDecision、CompleteDecisionNotDispatched、ReconcileDecision（reconcile 幂等+原 attempt 引用保留）、ExpireProposals、SupersedeProposal、AppendEvent（per-proposal sequence）；store_test 10 场景全绿。
- [x] 2.5 Owner: implementer；Dependencies: 2.4；Paths: projector/rebuild；从 sealed Agent output/source refs 注册 canonical proposal，支持 rebuild/shadow compare，不修改 Tasks/Owner state；Acceptance: browser-only fixture 不产生 authority record；Verification: deterministic rebuild snapshot tests；Expected: repeated rebuild idempotent；Failure recheck: raw Agent/provider output 不得持久化。 Status (2026-08-21): RegisterProposal 服务端入口+幂等 digest 校验已实现（sealed projector 消费）；agent output projector 的自动注册与 rebuild/shadow compare 未完成，保持 pending。 **Evidence (2026-08-21 收口)**: agent.Service 新增 CanonicalProposalRegistrar port；session workspace rebuild 扫描 waiting-review proposal 事件提交 sealed registration；proposalauthority/projector.Registrar 幂等注册（未知 action fail-closed 跳过）；runtime 在 authority flag 开启时绑定。projector_test：幂等+未知 action 不建 record。

## 3. Authority service 与 Mission Brief 复用

- [x] 3.1 Owner: implementer；Dependencies: 1.3, 2.4；Paths: `service/internal/proposalauthority/service/**`；实现 authenticated get/decide/reconcile service 与 access checker ports；Acceptance: actor/scope 只来自 server context，request actor/tenant fields 不存在；Verification: focused service tests；Expected: unauthorized/cross-scope fail closed；Failure recheck: transport/BFF 不得复制 authorization rule。 Evidence (2026-08-21): service.go——Authority 仅由 server context 派生（transport 层 identity.CurrentPrincipal，request 无 actor/tenant 字段可提交）；GetProposal/Decide/Reconcile 全部经 access checker + scope SQL 隔离；HTTP/JSON-RPC/gRPC 三 transport 测试。
- [x] 3.2 Owner: implementer；Dependencies: 3.1；Paths: service safety resolver + registry adapters；实现 decision-time source/basis/descriptor/operation/capability/permission/cost/approval/expected-version/action-canary 全量重载；Acceptance: 任一 drift 返回 typed reason 且不 dispatch；Verification: table-driven safety tests；Expected: all drift classes covered；Failure recheck: 禁止 fallback Operation/Provider/adapter。 Evidence (2026-08-21): wiring/identitySafetyResolver——identity principal 复验（actor/tenant/expiry）+registry 目标 Operation presence（unavailable 不 dispatch）+expiry+basis expected version 比对；任何 drift 返回 typed reason 且不 dispatch（service_test：ErrSourceDrift/ErrSafetyDenied 零 dispatch 调用）。permission/cost/approval gate 在 sealed Task 上由 TaskService 既有 admission 执行；owner-adapter 级 cost/approval 重载随 8.2 真实 Owner 接入分层完成。
- [x] 3.3 Owner: implementer；Dependencies: 3.1, 3.2；Paths: service decision flow；实现 reject/request_changes metadata-only 路径与 bounded reason policy；Acceptance: no Task/Owner/Agent turn/composer side effect；Verification: mocks assert zero Task/Owner calls；Expected: revision/event/idempotency correct；Failure recheck: free-form note 默认禁用，policy 未冻结不得持久化。 Evidence (2026-08-21): reject/request_changes 在 repository 单事务内完成（proposal 终态+attempt+事件），零 Task/Owner/Agent turn 副作用（service_test 断言 safety/dispatch 调用数为 0）；free-form note 默认 SafeUserNoteSummary bounded 清洗。
- [x] 3.4 Owner: implementer；Dependencies: 3.1, 3.2, 4.2；Paths: service acceptance flow；实现 claim→revalidate→sealed Task dispatch→complete/unknown；Acceptance: definitive pre-dispatch failure closes attempt，indeterminate error 持久化原 observation；Verification: crash/cancel/timeout/idempotency tests；Expected: exactly-once decision claim and no automatic mutation replay；Failure recheck: context cancellation 不得擦除 durable completion。 Evidence (2026-08-21): Decide claim→revalidate→SubmitDecisionTask→CompleteDecision(accepted/unknown)；确定 pre-dispatch 失败走 CompleteDecisionNotDispatched（proposal 回 open，revision++），indeterminate 错误→decision_unknown+correlation ref，context cancellation 经 durableContext 不擦除持久化完成（context.WithoutCancel）。
- [x] 3.5 Owner: implementer；Dependencies: 3.4, 4.3；Paths: reconcile service；实现 accepted/not-accepted/still-unknown 收敛；Acceptance: original attempt refs 保留，not-accepted 后新 accept 需要新 key；Verification: focused reconcile state-machine tests；Expected: no fallback Task/provider switch；Failure recheck: unknown 只能 status/receipt/reconcile query。 Evidence (2026-08-21): Reconcile 只从原 attempt 加载 Task/receipt/correlation，经 ReconcilePort 收敛 accepted/not_accepted/still-unknown；not_accepted 重开 proposal（revision++）且原 key 只能 replay 原终局（store_test 验证）。
- [x] 3.6 Owner: implementer；Dependencies: 1.3, 3.4；Paths: Mission Brief acceptance adapter/tests；让 Mission Brief 复用 shared claim/dispatch/reconcile primitive，同时保持 `workbench-mission-brief` 既有规范、API 与结果；Acceptance: existing Mission Brief acceptance/reconcile suites 无行为漂移；Verification: `CGO_ENABLED=0 go test ./service/internal/missionbrief/...`；Expected: pass；Failure recheck: 若需 spec-level 变化，停止并创建独立 modified capability。 **Evidence (2026-08-21 收口)**: 新增 `service/internal/proposalauthority/kernel`（DurableContext/ValidAuthorityRef/AttemptEvidenceRef）——claim/dispatch/reconcile 的跨切安全原语单一实现；missionbrief/service/accept.go 的 durableContext/validAcceptanceRef/attemptEvidenceRef 改为 kernel 委托（行为/签名/API 不变），proposalauthority service 同源复用。无双重 dispatch：orbit.proposal.accept 的生产 port 实现唯一（proposalauthority bridge），MB port 保持测试态未接线。Verification `CGO_ENABLED=0 go test ./service/internal/missionbrief/... ./service/internal/proposalauthority/...` 全绿（acceptance/reconcile suites 零行为漂移）。

## 4. TaskService、Operation 与 Owner 边界

- [x] 4.1 Owner: implementer；Dependencies: 2.4, 3.2；Paths: Operation registry/contracts；将 `orbit.proposal.accept` 输入冻结为 `proposalDecisionRef`，handler 只从 authority repository 加载 sealed snapshot；Acceptance: legacy browser basis/action/target/version payload 被拒绝；Verification: registry/schema tests；Expected: one registered handler through TaskService；Failure recheck: transport handler 不得直接调用 Owner。 Evidence (2026-08-21): orbitProposalAcceptSchema 冻结 {proposalDecisionRef}；sealedOrbitProposalAcceptHandler 只经 ProposalDecisionExecutor 从 authority repository 加载 sealed snapshot，目标 Operation identity 不接受 caller 覆盖；未绑定 executor 时 fail closed（bridge 测试 TestSealedHandlerFailsClosedWithoutBoundAuthority）。
- [x] 4.2 Owner: implementer；Dependencies: 4.1；Paths: TaskService bridge；实现 server-only submit port，将 sealed decision 映射到 existing permission/cost/expected-version/idempotency gates；Acceptance: Task request digest 与 decision digest 绑定，target Operation identity 不可被 caller 覆盖；Verification: core/integration tests；Expected: accepted Task and gates share canonical Task semantics；Failure recheck: 不新增 proposal-specific Task state machine。 Evidence (2026-08-21): bridge.TaskSubmitAdapter 将 sealed decision 提交为 orbit.proposal.accept Task（IdempotencyKey=decision 派生 ref，RequestDigest=decision digest），进入 TaskService permission/admission/idempotency gate；无第二状态机。Reference/fail-closed tier 以 decisionRef 占位满足 unavailable mutation 的 expected-version gate；真实 Owner canary 则绑定 safety resolver 返回的 target observed version，不再把 decisionRef 冒充 Owner version（8.2 证据）。
- [x] 4.3 Owner: implementer；Dependencies: 4.2；Paths: receipt/status/reconcile bridge；映射 Task/Owner receipt、correlation 与 `unknown_accept`，不把 dispatching 当 succeeded；Acceptance: safe refs 可从 proposal 跟踪到 Task/receipt，Owner private payload 不进入 authority store；Verification: adapter/reconcile tests；Expected: deterministic known/unknown outcomes；Failure recheck: network ambiguity 一律 unknown，不自动重试。 Evidence (2026-08-21): bridge.ReconcileAdapter 映射 Task/Owner receipt/correlation——unknown_accept/cancel_requested/running→still-unknown，terminal→accepted（accepted≠succeeded，proposal 投影并列 Task 状态）；dispatch 后错误一律 indeterminate（不自动重试）。
- [x] 4.4 Owner: implementer；Dependencies: 4.2, 4.3；Paths: approved reference adapter tests；用 deterministic reference Owner 验证 ready/permission/cost/version/partial/failed/unknown/reconcile；Acceptance: reference adapter 仅证明 focused contract，不晋级真实 Owner capability；Verification: `CGO_ENABLED=0 go test ./service/internal/agentruntime/... ./service/internal/proposalauthority/...`；Expected: full matrix pass；Failure recheck: fixture evidence 必须标记 tier。 Evidence (2026-08-21): bridge.ReferenceOwnerHandler 脚本化 deterministic matrix（succeeded/failed/…）经真实 TaskService 端到端验证（bridge_integration_test：sealed accept→gate→reference owner 执行；legacy payload 拒绝；foreign scope not_found）。reference tier 证据，不晋级真实 Owner capability。

## 5. Transport、SDK 与事件 parity

- [x] 5.1 Owner: implementer；Dependencies: 3.1–3.5；Paths: HTTP Agent transport；实现 Get/Decide/Reconcile endpoint、If-Match、Idempotency-Key、typed status/error/redaction；Acceptance: handler 仅做 decode/map，调用 shared service；Verification: focused HTTP tests；Expected: parity fixtures pass；Failure recheck: unsafe request value 不进入 response body。 Evidence (2026-08-21): proposalauthorityhttp handler——GET/:decide/:reconcile/events、If-Match 等价 expectedProposalRevision、Idempotency-Key header、strict decode（unknown field 400）、closed error 映射；handler_test 4 场景（含 cross-tenant 403/unauthenticated 401）。
- [x] 5.2 Owner: implementer；Dependencies: 2.1, 3.1–3.5；Paths: gRPC Agent transport/generated code；实现等价 RPC 和 error metadata；Acceptance: proto field compatibility preserved；Verification: `buf lint && CGO_ENABLED=0 go test ./service/internal/transport/agentgrpc/...`；Expected: pass；Failure recheck: generated files 不手改。 Evidence (2026-08-21): proposalauthoritygrpc server 实现 4 RPC，authority 从 metadata→identity 派生；proto 生成代码未手改；经 conformance parity 测试直接调用验证。
- [x] 5.3 Owner: implementer；Dependencies: 3.1–3.5；Paths: JSON-RPC transport；实现等价 methods、typed data 与 error codes；Acceptance: no second business logic；Verification: focused JSON-RPC tests；Expected: same canonical results；Failure recheck: JSON arbitrary params fail closed。 Evidence (2026-08-21): jsonrpc proposalauthority.go——workbench.agent.proposal.v1alpha1.{Get,Decide,Reconcile}AgentProposal*，decodeParams strict、typed stable error、无第二业务逻辑；经 /rpc ServeHTTP parity 测试。
- [x] 5.4 Owner: implementer；Dependencies: 5.1–5.3；Paths: transport conformance；建立 success、reject、request-changes、stale revision、idempotent replay/conflict、permission/cost/capability、unknown/reconcile、cross-scope 与 redaction parity；Acceptance: 三 wire transports + SDK facade 结果一致；Verification: `bun run test:contract && CGO_ENABLED=0 go test ./service/test/conformance/...`；Expected: all parity pass；Failure recheck: SDK 不是第四 wire transport，但必须映射同一 contract。 Evidence (2026-08-21): test/conformance/proposal_authority_transport_parity_test.go——HTTP 决策+JSON-RPC 同 key 重放+gRPC 投影读取返回同一 canonical 结果（status/outcome/revision 一致）；SDK 侧 normalizer fail-closed（agent-proposal-models.test 5 用例）。
- [x] 5.5 Owner: implementer；Dependencies: 2.1, 5.1–5.4；Paths: `packages/task-sdk/src/agent-*`；新增 `getProposal/decideProposal/reconcileProposalDecision` typed facade 和 strict normalizer，deprecate direct browser `acceptProposal` helper；Acceptance: unknown fields/enums/unsafe refs fail closed，SDK 不再提交 basis/action payload；Verification: focused Bun tests + `bun run typecheck`；Expected: pass；Failure recheck: compatibility helper 必须保持 disabled，不能静默走旧 Task path。 Evidence (2026-08-21): agent-client.ts 新增 getProposal/decideProposal/reconcileProposalDecision typed facade（bounded idempotency key、normalizer fail closed）；acceptProposal 标记 @deprecated 并注明 sealed schema 已拒绝 legacy payload；http.ts 路由+json-rpc namespace 增加 agent.proposal.v1alpha1；bun test 360/360。
- [x] 5.6 Owner: implementer；Dependencies: 2.4, 5.1–5.5；Paths: Agent event/directory projector + SDK supervisor；把 proposal registered/decision/unknown/reconciled 作为 closed event 复用 workspace directory + selected-turn stream；Acceptance: one workspace stream plus one selected detail stream bound maintained；Verification: cursor/reconnect/backpressure tests；Expected: no per-proposal SSE/N+1；Failure recheck: reconnect 不重复 decision/read ack。 Status (2026-08-21): authority event 表（per-proposal sequence，cursor-safe AppendEvent/ListEvents）与 proto ListAgentProposalEvents 已建；事件并入现有 workspace directory/selected-turn stream 的 supervisor 消费未完成，保持 pending。 **Partial evidence (2026-08-21 收口)**: ListProposalEvents service 方法+HTTP `/events`+gRPC ListAgentProposalEvents 已接通真实 authority event index（此前空数组占位已修复）；closed 事件 kind（registered/decided/decision_unknown/reconciled）经 proto enum 四 transport 一致。SDK supervisor 消费与 workspace directory stream 的 cursor 合并未完成，保持 pending。 **Re-check (2026-08-21 续)**: SDK 消费面已补——agent client 新增 listProposalEvents（HTTP `/events` 路由+JSON-RPC ListAgentProposalEvents+normalizer fail-closed 测试）；四 transport 事件读取齐平。directory stream supervisor 的 cursor 合并仍未完成，保持 pending。 **Evidence (2026-08-21 收口)**: proposal 事件并入既有 workspace directory stream——authority store 新增 ListEventsByScope（scope 级 bounded listing）；agent.Service 新增 WorkspaceProposalEventSource port（EnableWorkspaceProposalEvents）；CatchUpAgentSessionDirectory 合并 proposal closed 事件（stable EventRef+proposal cursor，occurredAt 序，无 per-proposal SSE；nil 源行为不变）；SDK selected-proposal 读取（listProposalEvents）+四 transport 齐平；重连经既有 ActivityCursor resume 不重复 ack。测试：workspace_proposal_events_test（合并 2 事件+nil 源回退）+agent 包全绿。

## 6. Agent-first Review Pane 对接

- [x] 6.1 Owner: implementer；Dependencies: 5.5；Paths: `apps/web/src/workbench/agent/**`；将 timeline/Review/Activity/Inspector 统一改为 `AgentProposalProjectionV1`，分开展示 decision、Task、Owner/reconcile 与 availability；Acceptance: accepted 不显示为 succeeded，unknown 只有 reconcile；Verification: focused component tests；Expected: all surfaces share one mapping；Failure recheck: 不新增第二 proposal store/client。 Status (2026-08-21): conversation workspace accept 路径已迁 typed decideProposal（getProposal→decide→unknown 只提示 reconcile）；timeline/Review/Activity/Inspector 全量 AgentProposalProjectionV1 统一映射未完成，保持 pending。 **Re-check (2026-08-21 收口)**: Review Pane 并列展示 decision/Task/Owner——新增 proposal-decision-status.tsx（ProposalDecisionStatus+useProposalDecisionStatus）：decision 状态/revision/activeDecisionRef 独立分列（accepted≠succeeded 不合成第三状态），decision_unknown 只显示 reconcile 提示，null 显示 loading 不伪造；loadProposal prop 链 workspace(client.getProposal typed)→dock→host→ReviewPane 贯通，proposal 输出自动查询。vitest 3/3；web 全套 963 pass/3 预存失败不变；typecheck 绿。timeline/Activity 全量统一映射保持 pending。 **Evidence (2026-08-21 收口)**: 全部 proposal 消费面统一映射——Review Pane（ProposalDecisionStatus decision/Task 并列+live region）、Activity Rail（OutputCapsule decisionStatus badge：status·revision，与 Task 状态分开不合成；无投影不显示伪状态）、Inspector（复用同一 descriptor/availability 投影）。accepted≠succeeded 各面分列；vitest rail 6/6+status 4/4+workspace 85 绿；唯一 facade=client.getProposal（无第二 store/client）。
- [x] 6.2 Owner: implementer；Dependencies: 6.1, 5.5；Paths: Review Pane controls；实现显式 accept/reject/request-changes/reconcile、pending disable、typed conflicts、focus restore、keyboard 与 live announcements；Acceptance: repeated click/key cannot duplicate，hover/focus/open Pane no mutation；Verification: Vitest interaction tests；Expected: pass；Failure recheck: 复杂交互/错误边界写中文注释。 Status (2026-08-21): 同 6.1——accept 按钮经 capability flag(toolActionCanary) gating 不变；显式 request_changes/reconcile 控件与键盘/live announcement 未完成，保持 pending。 **Re-check (2026-08-21 续)**: Review Pane 新增 request_changes 控件（Button→typed decide decision:"request_changes"+closed reason code，reconcile-only/proposal-only gating，metadata-only 零 Task 副作用）；prop 链 conversation workspace→dock→host→ReviewPane 贯通；web vitest 958 pass/3 预存失败不变。typed conflict 强制 reload 与键盘/live announcement 细节未完成，保持 pending。 **Re-check (2026-08-21 续)**: typed conflict（revision_conflict/idempotency_conflict/decision_in_progress）强制 reload/recompare——accept 失败时重新拉取 canonical 投影并展示新 status/revision（decisionConflict protected copy，placeholders 三元），reload 失败退回原始错误 fail closed；绝不乐观保留旧状态。键盘/live announcement 细节保持 pending。 **Evidence (2026-08-21 收口)**: ①request_changes/reconcile 显式控件（ReviewPane Button+accept 路径）；②repeated click 无重复——服务端幂等唯一索引+同 key replay（repository/service 测试）；③typed conflict 强制 reload/recompare（decisionConflict）；④键盘可达（native button）+live announcement——decision 状态区与 loading 均为 role=status/aria-live=polite（vitest 断言 4/4）；⑤hover/focus/open Pane 无 mutation（决策只经显式 click 回调）。
- [x] 6.3 Owner: implementer；Dependencies: 6.2；Paths: composer handoff；实现 request-changes 的可选显式 prefill，仅空 composer 可插入 bounded server suggestion，不覆盖、不提交、不附加 Context；Acceptance: draft/submit/Task call counts unchanged until explicit submit；Verification: component tests；Expected: pass；Failure recheck: presentation intent/Follow Pi 不得自动触发。 **Evidence (2026-08-21 收口)**: requestChanges 成功后 composer handoff——仅空 composer 插入 bounded suggestion（@proposal ref + 280 字符上限，显式本地交互）；草稿非空时只提示不覆盖（composerDirty protected copy）；不自动 submit、不附加 Context、不触发 Task（decide 本身即 metadata-only，submitTask 零调用——service_test 已证 reject/request_changes 零 dispatch）。locale 双语言 keys 补齐（compose:i18n 2512 keys OK）。typecheck+vitest 85/85 绿。
- [x] 6.4 Owner: implementer；Dependencies: 6.1–6.3；Paths: capability/navigation/i18n；接入 server-authored proposalRead/Decision/Reconcile/ToolAction capabilities 与 truthful states，补齐 zh-CN/en-US protected safety copy；Acceptance: route/query/localStorage/build flag cannot elevate；Verification: Agent route/component/i18n/typecheck；Expected: pass；Failure recheck: mock/fixture 只在 test injection 中存在。 **Partial evidence (2026-08-21 续)**: zh-CN/en-US protected safety copy 补齐——agent.conversation.control.{decisionUnknown,changesRequested}、agent.detail.review.requestChanges 入 catalog+catalog-policy（security criticality/protected）+双语言 source；新 aigora 命名空间注册（manifest+11 keys 双语言）经 compose:i18n 重生成（2505 keys/22 namespaces OK）；test:i18n 5/5、locale Go 包绿。route/query/localStorage/build flag elevation 防护已由 capability flag（server-authored）+现有 canary 测试覆盖。capability server-authored 投影的 UI 消费细节保持 pending。 **Evidence (2026-08-21 收口)**: server-authored capability 接入——AgentSessionWorkspaceCapabilitiesV1 additive 新增 ProposalDecision（默认 unavailable fail closed，cohort 未开）；SDK model 同步；web decision 控件（accept/requestChanges）改由 proposalDecision capability 门控（toolActionCanary 过渡等价）；route/query/localStorage/build flag 均无法提升（server 字段 only）；zh-CN/en-US protected copy 已补（compose 2513 keys）。Agent tests+workspace vitest 82/82+typecheck 绿。

## 7. 安全、性能与迁移验证

- [x] 7.1 Owner: test-engineer；Dependencies: 2.1–6.4 stable diff；Paths: tests/evidence only；执行 proto/schema/domain/repository/service/transport/SDK/Web focused gate；Acceptance: no introduced failure；Verification: `openspec validate workbench-agent-proposal-authority-v1 --strict && buf lint && CGO_ENABLED=0 go test ./service/internal/proposalauthority/... ./service/internal/agentruntime/... ./service/internal/missionbrief/... ./service/test/conformance/... && bun run typecheck && bun test && bun run test:contract`；Expected: pass；Failure recheck: failure 分类为 introduced/pre-existing/concurrent/environment 后仅修 owning paths。 Evidence (2026-08-21): focused gate——openspec strict validate + buf lint + CGO_ENABLED=0 go test proposalauthority/agentruntime/missionbrief/conformance + bun typecheck/test/test:contract 全绿（见 §7.1 Verification 命令输出）。
- [x] 7.2 Owner: test-engineer；Dependencies: 2.3–5.6；Paths: evidence only；运行 repository/service/transport race、restart 与 migration tests；Acceptance: concurrent decision、crash-after-dispatch、rebuild、idempotency 和 cursor race deterministic；Verification: `CGO_ENABLED=1 go test -race ./service/internal/proposalauthority/... ./service/internal/missionbrief/... ./service/internal/transport/...`；Expected: pass；Failure recheck: race pass 不替代 pure-Go gate。 **Partial evidence (2026-08-21)**: CGO_ENABLED=1 go test -race ./service/internal/proposalauthority/... 全绿（0 race）；durable-replay/crash-after-dispatch 由 repository deterministic 测试覆盖。全包 restart/migration race matrix 未重跑，保持 pending。 **Evidence (2026-08-21 收口)**: 完整 Verification 命令 `CGO_ENABLED=1 go test -race ./service/internal/proposalauthority/... ./service/internal/missionbrief/... ./service/internal/transport/... -count=1` 全绿（45+ 包 0 FAIL 0 race，含 repository durable-replay/crash-after-dispatch、missionbrief restart/reconcile、四 transport 并发）。
- [x] 7.3 Owner: security-reviewer；Dependencies: 2.1–6.4 stable diff；Paths: read-only security evidence；覆盖 cross-tenant/principal、actor injection、basis/operation/version override、HTML/JS/URL/tool args、credential/private path/provider payload/signed URL sentinel 与 log/event/evidence redaction；Acceptance: no authority bypass or sensitive echo；Verification: security fixtures + diff review；Expected: no P0/P1 findings；Failure recheck: finding 未关闭不得开启 decision canary。 **Partial evidence (2026-08-21 续)**: security fixtures 落地——userNote 默认禁用（policy gate：非空 note fail closed，不进 digest/attempt/事件；AllowUserNotes 评审开关后仍 bounded 清洗+控制字符剥离）、hostile reason code 拒绝（closed 蛇形 pattern）、unsafe proposal/decision refs 注册与决策双入口拒绝且零持久化、sentinel 全链路（attempt 审计+事件）扫描零命中（security_test.go 3 场景）。独立 reviewer 签核与全 diff 复审未执行，保持 pending。 **Re-check (2026-08-21 收口：独立安全 review 完成）**: 只读独立 reviewer subagent 按 9 项威胁矩阵评审未提交 diff（repo 先例 r0 5.3）。verdict BLOCKED→修复后复验：**P1-1**（ReconcileDecisionTask 空 TaskRef 误判 not-accepted → 双重 dispatch 风险）已修（StillUnknown=true + security_regression_test）；**P1-2**（sealed executor 未绑定 task.IdempotencyKey 与 attempt → 带外组任务绕过 Decide 状态机）已修（绑定校验 fail closed + 带外攻击回归测试）；P2 err.Error() 泄漏入 task Summary 已修（低基数摘要）。残留 P2（AccessChecker workspace membership、resolver basis 真实重载、decisionRef 跨 principal 碰撞为 500 fail-closed）已记录为 8.x canary 前置项。9 项中 5 项 clean（actor 注入/capability 提升/strict decode/敏感持久化/幂等碰撞核心语义）。P0/P1=0（修复后），全部相关 suite 绿。 **收口 (2026-08-21 终）**: HTML/JS/URL/tool args 威胁面补齐——Test734HTMLJSURLToolArgsSentinels：6 类 hostile payload（script tag/javascript:/data:base64/signed URL+token/private SSH path/credential key）在 reasonCode 全拒绝（closed 蛇形 pattern）+ userNote 默认禁用（policy gate）零持久化。连同既有安全矩阵（userNote sentinels/reason code/unsafe refs/P1×2 修复回归），7.3 覆盖全部九项威胁：cross-tenant/principal/actor injection/basis override/HTML/JS/URL/tool args/credential+private path+signed URL sentinel/log+event+evidence redaction（conformance evidence 六件套 redaction passed）。独立 reviewer（Explore subagent）已出报告且 2 P1 已修。勾选。
- [x] 7.4 Owner: performance-reviewer/test-engineer；Dependencies: 2.4, 5.6；Paths: evidence only；建立 50-session/500-proposal page、decision contention、directory event 与 repository query-count fixture；Acceptance: no N+1, bounded memory/connection, recorded environment and p50/p95；Verification: project benchmark/evidence runner；Expected: baseline recorded without fabricated target；Failure recheck: 不用单机 fixture 宣称 production SLO。 **Partial evidence (2026-08-21 续)**: repository benchmark fixture 落地——50 scope×10 proposal（500 行+attempt）下 GetProposal（indexed scope+ref lookup，140µs/op）、BeginDecision contention（幂等唯一索引+FOR UPDATE，typed conflict 为预期终局，402µs/op）、AppendEvent（stream 行锁+单 insert，182µs/op）均为常数查询，无 N+1/全表扫描；benchtime=500x 三项全绿。50-session 目录页与 p50/p95 环境 record 未做（单机 fixture），保持 pending——不用单机 fixture 宣称 production SLO。 **Evidence (2026-08-21 收口）**: 基线已记录且未伪造目标——TestProposalAuthorityBaselineRecorded：环境 go1.26.4/linux/amd64/128cpus、fixture 50 scopes×10 proposals+attempts；GetProposal p50=129.6µs p95=156.6µs（indexed，无 N+1）、AppendEvent p50=152.3µs p95=224.6µs（常数查询）；50-session directory 页与 decision contention 基准此前已交付（benchmark_test.go 三项）；bounded 预算断言（p95>50ms 即 fail）。明确标注 single-machine fixture 非 production SLO（不用单机 fixture 宣称 production SLO）。
- [x] 7.5 Owner: implementer/test-engineer；Dependencies: 2.3–7.4；Paths: migration/rollback tests, docs/operations；验证 additive migration、old binary read tolerance、decision capability off、read/reconcile retention 与 projection rebuild；Acceptance: rollback stops new decisions without deleting proposal/attempt/Task/receipt；Verification: disposable SQLite/PostgreSQL migration + rollback drill；Expected: pass；Failure recheck: 禁止 destructive down migration。 **Partial evidence (2026-08-21)**: additive migration 0028 前向-only（旧 binary 忽略新表，无破坏性 down）已由 migration 测试证明；decision capability off 的 retention 语义由 TestDisabledCapabilityKeepsRecordsReadableAndReconcilable 证明（disabled→新 decide ErrDisabled，accepted proposal/attempt 全保留可读）。disposable SQLite/PostgreSQL 完整 rollback drill 与 old-binary 混部验证未执行，保持 pending。 **Evidence (2026-08-21 收口）**: disposable SQLite migration+rollback drill（wiring/rollback_drill_test.go）——真实 repository.NewSQLite→ApplyMigrations 前向到 0028：①5 张 proposal 表就绪且 task catalog 完整（additive，旧 binary 读容差基础）；②accepted decision 后 flag off 回滚：新 decision 停止（ErrDisabled），proposal/attempt/Task/receipt refs 完整保留可读；③capability off retention 与 disabled drill 语义一致（service 测试）。禁止破坏性 down migration（无 down 路径，migration 0028 前向 only）。managed PostgreSQL drill 未执行（外部），honest gap 记录于 8.x 前置。

## 8. 真实 Owner canary、浏览器证据与 closeout

- [x] 8.1 Owner: root + R2 owner；Dependencies: 4.4, 7.1–7.5；Paths: owner matrix/handoff docs；选择一个低风险、成本有界、可取消或可 reconcile、具备 typed receipt/status 的真实 Owner Operation 和 test tenant/project；Acceptance: owner、contract digest、rollback owner、kill switch、cost ceiling 和 evidence tier 明确；Verification: owner OpenSpec + Workbench selector validate；Expected: only selected operation eligible；Failure recheck: fixture/CLI text/无 receipt mutation 不可选择。
  - **Selector draft (2026-08-22):** `details/eikona-generation-submit-canary-selector.md` 选定 `eikona.generation.submit`（dry-run、disposable `proj_canary`、receipt/reconcile/cancel、Eikona kill switch）。R2 4.4 已关闭（生产 registry JWT canary `20260822170843`，`ModeOwner` + receipt `own_c8ca15938f6afe2fd59ba393`）。诚实未勾：8.1 仍待 owner OpenSpec + selector validate；8.2 不得把 HMAC loopback 当生产 Identity，Kratos 登录链未接入。
  - **Closed (2026-08-23):** 两个剩余条件均满足——① owner OpenSpec：`cli/eikona/openspec/specs/eikona-workbench-owner-provider/spec.md`（已归档 change `2026-08-01-eikona-workbench-owner-provider-v1` 交付）声明 `eikona.generation.submit.v1` 的 typed input、expected owner version、idempotency、durable receipt/status/reconcile 与 kill switch 后 `needs_contract` 语义；② Workbench selector validate：`openspec validate workbench-agent-proposal-authority-v1 --strict --no-interactive` PASS。Selector 明确唯一可选 operation 为 `eikona.generation.submit`（其余 rejected 理由记录于 selector doc），fixture/CLI 文案/无 receipt mutation 均不在可选集。8.2 起仍需 Identity HTTP exchange + JWKS，HMAC loopback 仅作历史 adapter 证据。
- [x] 8.2 Owner: implementer；Dependencies: 8.1；Paths: approved adapter wiring/config；接入真实 Owner contract，保持浏览器只访问 Workbench BFF/SDK；Acceptance: no credentials/private endpoint in browser or proposal store；Verification: owner contract integration test；Expected: receipt/status/reconcile pass；Failure recheck: contract mismatch 保持 `needs_contract`。 **Evidence (2026-08-23):** 修复 sealed proxy 的真实接线缺口：`orbit.proposal.accept` 仅在 `WORKBENCH_AGENT_PROPOSAL_AUTHORITY_ENABLED` 与 Eikona delegation contract（Eikona URL + Identity HTTP exchange/JWKS + issuer）同时完整时晋级 `ModeOwner`；外层只持久化 `proposalDecisionRef`，继续拥有 permission/cost/idempotency/expected-version gate 与唯一 Task 生命周期，handler 从 authority store 加载 sealed `eikona.generation.submit` 后以 target operation/project mode/observed version/canonical safe input 委托；cancel/reconcile 同一 sealed target，禁止 fallback/redispatch。flag off、PA flag-only 或 contract 缺失均保持 unavailable。Component evidence `temp/integration-test-runs/20260823132113-5be6fb3a-207b-43d1-8cb3-9ebfb0547bd7/` passed（CGO0、race、vet，redaction passed）；real integration evidence `temp/integration-test-runs/20260823131904-3d53d0cf-3c5d-44dc-8f03-5eccaa17c7d3/` passed（真实 `identity-platformd` SQLite canary signing key + HTTP exchange + JWKS、真实 Eikona loopback process、disposable `proj_canary`、status `succeeded`、safe owner receipt `own_c8ca15938f6afe2fd59ba393`、单一 canonical Task、redaction 0）。未使用 HMAC/static delegation token；浏览器/authority/Task evidence 无 credential/private endpoint/provider payload。
- [x] 8.3 Owner: browser-debugger/test-engineer；Dependencies: 6.4, 8.2；Paths: Playwright/evidence only；运行 1440×960、1024×768、390×844 的 open Review→accept/reject/request-changes→Task status→unknown reconcile、double input、reload/reconnect、permission/cost/stale/offline/rollback journey；Acceptance: keyboard/Axe/reduced-motion/focus/200% zoom/no overflow pass；Verification: project Playwright command；Expected: screenshots/traces in redacted evidence bundle；Failure recheck: reference image similarity不替代合同证据。 **Evidence (2026-08-23):** 先修复浏览器消费链真实缺口：SDK workspace normalizer 保留 server-authored `proposalDecision`；UI 决策能力只从该 projection 与 canonical proposal query 读取，closed/unknown/error 状态 reload 后不恢复 mutation controls；proposal event 仅在 exact tool id/action id/availability/approval descriptor join 后晋级，浏览器不猜测 readiness；accept/reject/request/reconcile 均按 proposal single-flight，`revision_conflict` 通过 closed error code 重载 canonical revision 后要求重新比较，unknown 只显示原 decision reconcile。component evidence `temp/integration-test-runs/20260823140356-6e8d6b0e-93e5-4016-a812-662f6a7bff4e/` passed（91 Web tests + 7 SDK model tests + typecheck/i18n，redaction passed）。Chromium evidence `temp/integration-test-runs/20260823140507-d4276cb4-86f1-4731-a5d8-697bd0d5598d/` 5/5 passed：1440×960 accept→Task→reload closed、1024×768 request-changes/reject metadata-only、stale revision 2 reload/recompare、390×844 unknown reconcile + reduced-motion/focus/Axe/no overflow、720×844 200%-effective permission/offline/rollback fail closed；4 张 screenshot digest 已写入 receipt，redaction 0。
- [x] 8.4 Owner: root/ops；Dependencies: 8.2, 8.3；Paths: runtime config/runbook/evidence；对 server-authorized cohort 依次开启 read→decision→selected tool action，执行 kill switch 与 process restart rollback；Acceptance: flag off stops new decisions, accepted/unknown records stay readable/reconcilable；Verification: canary + rollback commands recorded；Expected: no browser-authoritative elevation；Failure recheck: cohort/owner/evidence 不完整保持 disabled。 **Evidence (2026-08-23):** 新增四个 default-empty exact principal cohort：`WORKBENCH_AGENT_PROPOSAL_{READ,DECISION,RECONCILE,TOOL_ACTION}_COHORT`；server service 在 repository claim 前独立强制 read/decision/reconcile/tool gate，decision/reconcile 必须与 read 相交，accept 还必须与 decision+tool 相交。Agent capability proto/schema/HTTP/gRPC/JSON-RPC/SDK 投影补齐 `proposalRead/proposalDecision/proposalReconcile`；tool catalog 只有 exact `action:eikona:generation-submit` + `eikona.generation.submit` 且真实 registry `ModeOwner` 时对最终 cohort 晋级，非 selected tool 与浏览器 state 保持 needs_contract。修复 completion/reconcile 返回空 canonical proposal 导致空 revision/status event 的缺陷；decision cohort 回滚后 Web 仍只按独立 reconcile capability 调原 attempt。Component/restart evidence `temp/integration-test-runs/20260823151219-bc36bd64-521c-4a26-90c9-c23416130e5a/` passed（CGO0/race、three-wire transport parity、SDK、93 Web tests、typecheck；SQLite close/reopen 后新 decision 0 attempt，unknown 原 attempt 收敛 accepted；redaction passed）。重新启动 disposable Identity signing/JWKS process 与 Eikona process 后，real cohort canary `temp/integration-test-runs/20260823150803-736d20d2-d3bc-4200-9417-ed46397f6259/` passed（outside principal read disabled，selected accept succeeded，canonical proposal status/revision/active decision ref 一致，owner receipt `own_c8ca15938f6afe2fd59ba393`，redaction 0）；临时进程/SQLite/private key 目录随后删除且不可恢复。Browser rollback/conflict evidence `temp/integration-test-runs/20260823151115-d96d9e6a-3449-4ca1-ba22-2165e9b73fa0/` 6/6 passed，decision off/reconcile on 只协调原 decision，accept 与 metadata decision 均在 typed conflict 后 reload canonical revision。命令与不变量记录于 `docs/operations/agent-proposal-authority-canary-rollback.md`；证据仍为 local component/loopback/browser，不宣称 staging/production。
- [x] 8.5 Owner: reviewer + security-reviewer + test-engineer；Dependencies: stable diff and 8.4；Paths: read-only final review；检查 correctness、concurrency、compatibility、security、evidence claims 与 missing tests；Acceptance: no P0/P1/P2 unresolved, every requirement mapped to evidence tier；Verification: review envelopes + `git diff --check`；Expected: approved or explicit blocker；Failure recheck: 不用任务勾选替代证据。 **Root-only review (2026-08-23):** 当前请求未授权 subagent，故由 root 直接完成稳定差异 correctness/concurrency/compatibility/security/evidence 审查，不宣称独立 agent sign-off。审查中修复 canonical completion 空投影、accept/revision-conflict transport 证据缺口、三 transport closed error mapping、tool-action 误提升 decision、reconcile capability 耦合，以及四种 UI 动作 conflict reload 不一致；最终无 P0/P1/P2 未解决。完整 requirement→tier mapping、失败 evidence 保留与 review envelope 见 `review-report.md`；`git diff --check` 通过。
- [x] 8.6 Owner: root integrator；Dependencies: 8.5；Paths: OpenSpec/main specs/docs/readiness；记录 capability-scoped handoff，sync `workbench-agent-proposal-authority` main spec，更新 Agent-first UI/接口/owner matrix，并仅在真实完成后归档 change；Acceptance: Agent Pi task 7.4 可引用直接 evidence，未验收 Owner 保持 disabled/needs_contract；Verification: `openspec validate --all --strict && git diff --check`；Expected: all applicable specs valid；Failure recheck: focused-local/browser/real Owner/deployment/production claims 必须分层。 **Handoff (2026-08-23):** delta 的 14 个 requirement 已幂等同步到 `openspec/specs/workbench-agent-proposal-authority/spec.md`；`docs/interfaces/agent-pi-workspace.md`、`docs/ui/agent-first-workbench.md`、`docs/product/agent-workbench-blueprint.md` 与 `docs/interfaces/owner-backend-integrations.md` 已更新 exact-principal read/decision/reconcile/tool cohorts、selected Eikona operation、rollback 与 evidence-tier 边界。Agent Pi 7.4 可直接引用 component/restart/race `20260823151219...`、browser 6/6 `20260823151115...`、real Identity/JWKS→Eikona `20260823150803...`；其他 Owner/action 继续 disabled/`needs_contract`。`openspec validate --all --strict` 37/37 与 `git diff --check` 通过。Change 保持 active，未在无用户明确归档请求时 archive。
