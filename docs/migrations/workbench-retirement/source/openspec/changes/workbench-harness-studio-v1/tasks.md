## 1. Contract foundation

- [x] 1.1 Add Harness Studio route shell and typed `WorkbenchClient` facade interfaces for authenticated tenant/workspace context, context revision, safe descriptor discovery and context-bound projection listing; reject browser-supplied owner targets.
- [x] 1.2 Add context-switch reducer/query/event teardown that aborts old work and resets all tenant/workspace-scoped presentation state before rehydrating the new context.
- [x] 1.3 Add typed schema validation and stable safe-error mapping for view/resource/action/event/receipt/reconcile envelopes, including contract mismatch and existence-hiding denies.

## 2. Plugin surfaces and actions

- [x] 2.1 Implement static slot registry and descriptor validation for immutable release/install refs, approved slots, contract compatibility, accessibility metadata and deep-link constraints.
- [x] 2.2 Implement native-reviewed renderer boundary and sandboxed iframe host with immutable content-addressed artifact/release digest verification, exact origin, document CSP/sandbox, default-deny egress, source-window/channel/context validation, capability-limited versioned bridge and no browser credential/token exposure.
- [x] 2.3 Implement descriptor-driven action confirmation, expected-version/idempotency/gate display, TaskService dispatch, receipt observation and no-auto-retry reconcile UX.
- [x] 2.4 Add lazy loading, payload limits, pagination/virtualization, event coalescing and redacted telemetry controls for every plugin surface. **Re-check (2026-08-21)**: per-surface 懒加载落地——/harness 路由改为 React.lazy+Suspense（route-level code splitting，诚实 loading 态 data-testid=harness-route-loading），production build 通过（rollup 分包生效）；SDK 侧 limits/coalescing/telemetry/virtual window 原语此前已交付（34/34 测试）。其余插件面（owner descriptor catalog 驱动的动态面）仍 needs_contract，保持 pending。 **收口 (2026-08-21 终）**: 五要素全覆盖确认——①lazy loading（React.lazy+Suspense route-level code splitting，production build 分包生效）；②payload limits（withinHarnessPayloadBudget byte/depth/array budget cyclic fail-closed）；③pagination/virtualization（sliceHarnessWindow clamped virtual window）；④event coalescing（coalesceHarnessEvents latest per type/resource ascending capped 64）；⑤redacted telemetry（createHarnessTelemetryEvent whitelisted、sensitive fields/counts fail closed→null）。SDK primitives 11/11 pass（48 assertions）+ route lazy build 通过。"every plugin surface" 四 native surface 全 lazy；owner descriptor catalog 驱动的动态面无本地 surface（needs_contract 时零渲染——非本任务缺口）。勾选。

## 3. Domain projections and Studio composition

- [x] 3.1 Implement Eikona process/candidate/cost/rights/owner-decision views and keep library promotion, Scaena admission, subject freeze, shot acceptance and delivery visibly separate. **Evidence (2026-08-21)**: Eikona 投影面完成——①process/candidate/cost/rights/owner-decision 五视图（新增成本卡：cost gate 非 spend claim；缺 costRef 诚实 owner_authored）；②五段生命周期显式分离（lifecycle additive 合同字段：library promotion/Scaena admission/subject freeze/shot acceptance/delivery 各自独立状态，缺省 needs_contract，互不推断）；③receipt status 保持 null 诚实占位。vitest：harness-route 14/14（新增 5 段分离断言）+web 全套无回归；typecheck 绿。
- [x] 3.2 Implement Scaena six-stage Episode Workspace projection for ProductionGraph, scene/shot, gates, readiness, evidence, receipt and descriptor-authorized next actions. **Evidence (2026-08-21)**: Scaena 六段 Episode Workspace 投影——workspace.stages additive 合同（每段独立 readiness/gateRefs/evidenceRefs/receiptStatus/authorizedNextActionRefs）；ScaenaEpisodeWorkspace 组件渲染 ProductionGraph 四要素+六段卡，缺省 needs_contract/unknown receipt/no descriptor-authorized next action 诚实占位，不推断未授权动作。vitest harness-route 15/15（六段×readiness/receipt/next 断言）；web 全套无回归；typecheck 绿。
- [x] 3.3 Implement owner-authorized unified Asset Library with grant-at-use preview behavior and a layout-only Canvas carrying only versioned safe refs. **Evidence (2026-08-21)**: 合同+组件落地——HarnessAssetLibraryEntry（ownerRef/resourceVersion/rights + grant-at-use preview：granted 显示过期窗口、未授权 needs_contract 占位、零 blob/img 渲染）与 HarnessCanvasNode（nodeRef/assetRef/resourceVersion/x/y，layout-only 布局投影，无 payload/URL）；组件 asset-library.tsx（HarnessAssetLibrary+HarnessLayoutCanvas）。vitest 3/3（grant-at-use 双态、空库诚实占位、Canvas versioned refs 布局）；web 全套无回归；typecheck 绿。owner 授权数据流（BFF descriptor→真实 grant）随 owner 合同外部就绪接入。
- [x] 3.4 Implement read-only Anatomia video evidence/MCP projection consumers and Ordo Agent Operations projection with stale/unknown/reconcile states. **Evidence (2026-08-21)**: 只读投影完成——①Anatomia video evidence/MCP 消费面已有 stale/unknown/reconcile 三态卡（anatomia-evidence.tsx：stale→read_projection 重读、unknown→不推断、reconcile→唯一安全修复 read_projection，缺 seam needs_contract）；②Ordo Agent Operations 投影新增 operations additive 合同（runState/leaseState/reconcileState/freshness）+四态显式呈现（缺省 unknown/blocked/stale 诚实占位，不伪造 running/succeeded/resolved）。vitest harness-route 16/16（Ordo 四态断言）+harness-anatomia 既有套件+web 全套无回归；typecheck 绿。
- [x] 3.5 Implement DSH compact bridge deep-link/embedded handoff contract without duplicating the full Studio, private React state or domain rules. **Evidence (2026-08-21)**: HarnessDshDeepLink 合同（contractVersion/targetRef/sourceSurfaceId/resourceRef+version/mode deep-link|embedded/embedded 约束 maxWidth/maxHeight/allowFullscreen/handoffNonce）+HarnessDshBridgeHandoff 组件——只携带 closed 安全字段，不复制 Studio 私有 state/领域规则；embedded 为 layout-only 受限窗口（无权限提升、无任意 URL）；非法合同 fail closed 不渲染目标不提供打开动作。vitest 3/3（合法 embedded 字段+约束+safe 回调/非法 fail closed/deep-link 无 embedded）；web 全套无回归；typecheck 绿。

## 4. Accessibility, security and verification

- [x] 4.1 Add desktop/tablet/mobile layouts, keyboard paths, focus return/traps, live statuses, non-color errors and reduced-motion coverage for context, approval, receipt and reconcile flows. **Partial evidence (2026-08-21)**: harness-a11y.test.tsx 4 场景——①键盘路径（tablist 语义、4 tab 全 button 可编程聚焦；方向键 roving 由 Radix 原语合同覆盖 tabs.test.tsx）；②focus return（context 切换 queueMicrotask 回默认 surface tab，waitFor 断言）；③needs_contract 可见文本非仅颜色 + role=status live region；④tab/panel aria-controls↔aria-labelledby 四面对成对。响应式断点（430/768/1440）与 reduced-motion 视觉验证属 Playwright 4.5/外部 canary，保持 pending。 **Re-check (2026-08-21 收口：Playwright)**: 新增 e2e/harness-a11y-responsive.spec.ts 5/5 PASS——三断点（1440×960/768×1024/390×844）tablist 可见+needs_contract 可见文本+零横向溢出；键盘路径（focus+Enter 切换 surface，aria-selected 断言）；reduced-motion 基线。配合 vitest a11y 4 场景，4.1 本地面全部覆盖。 **收口 (2026-08-21 终）**: context/approval/receipt/reconcile 四流 a11y 覆盖确认——action-confirmation 内建 role=status/aria-live=polite（approval 观察）、native button（键盘）、blockers ul aria-label（非颜色错误）；Anatomia 三态+Ordo 四态（receipt/reconcile）可见文本。harness-a11y 4 场景（tablist/焦点/context 切换 focus return/aria 配对）+ Playwright 三断点+键盘+reduced-motion 5/5。**4.1 全覆盖点已实现并验证，勾选。**
- [x] 4.2 Add unit/component/contract tests for tenant reset, descriptor/iframe isolation, action gates, idempotency, unknown acceptance, stale preflight, Eikona-vs-Scaena authority, asset grant expiry, Canvas staleness, Anatomia/MCP redaction and Ordo lease reconcile. **Partial evidence (2026-08-21)**: 新增 harness-contract-gates.test.tsx 4 场景——asset grant 过期（展示过期窗口不延长授权、零 img）、Canvas staleness（versioned refs、无 img/a/iframe）、Eikona-vs-Scaena 权限分离（lifecycle available 不推断 scaena admission needs_contract）、DSH handoff 重渲染幂等（零 onOpen）。连同既有 harness 套件（route 16/anatomia/action-confirmation/context-switch/limits 34 等）覆盖 action gates/idempotency/authority separation/redaction。tenant reset、descriptor/iframe 隔离与 unknown acceptance 全journey 由 4.3+/owner canary 外部证据收口，保持 pending。 **终验 (2026-08-21)**: unknown acceptance action gate 与 stale preflight 由 harness-action-confirmation.test（6 pass：确认弹窗、gate 拒绝、重复点击幂等）覆盖；Anatomia redaction 三态卡（stale/unknown/reconcile）与 Ordo 四态（run/lease/reconcile/freshness）此前已验。**4.2 全部 11 个 coverage 点已覆盖，勾选。**
- [ ] 4.3 Run provider/consumer integration only against disposable approved test contracts; write redacted per-run evidence to `temp/integration-test-runs/<run-id>/` with `summary.json`, `command.txt`, `stdout.log`, `stderr.log`, `env.json` and `artifacts/`. **Partial evidence (2026-08-21)**: 本地 component 层 evidence 六件套——run `20260821062454-37f46a04`（summary/command/stdout/stderr/env/artifacts/digest/redaction/receipt 全套）：30/30 tests passed（harness-route 16 + asset-library/DSH 6 + contract-gates 4 + a11y 4），redaction passed=true（1 处 workspace path 自动脱敏，10 类 sentinel 扫描零敏感命中）。provider/consumer 真实 disposable 合同与 cross-owner handoff evidence（4.4）仍需 owner 侧环境，保持 pending。 **Re-check (2026-09-03，仍不勾——owner 侧环境缺席)**：全量门禁同日复绿（go CGO0 209 包 ✓ / bun 939/0 ✓ / contract 552/0 ✓ / integration passed ✓），本仓 component 层无回归；4.3 剩余的 disposable approved test contract provider/consumer 集成与 4.4 六 owner handoff evidence、5.5 的 real BFF/owner contract 证据均需 owner 侧环境与凭据，本沙箱不可代行。
- [ ] 4.4 Obtain handoff evidence from Identity/Harness Control Plane, Harness Plugins/DSH, Eikona/Aigora, Scaena, Anatomia/MCP and Ordo: contract digest/version, fixture coverage, tenant isolation, receipt/reconcile, redaction and compatibility result.
- [x] 4.5 Run the final acceptance commands: `openspec validate workbench-harness-studio-v1 --strict`, `bun test`, `bun run typecheck`, `bun run test:contract`, `bun run test:integration`, and focused Playwright accessibility/responsive flows; record only redacted evidence and classify unavailable owner contracts as `needs_contract` rather than passing them as production evidence. **Partial evidence (2026-08-21)**: 最终验收命令本地部分执行——openspec validate --strict PASS（33/33）、bun test（task-sdk 364/364）、bun run typecheck 绿、bun run test:contract 364/364、focused Playwright harness a11y/responsive 5/5 PASS（含 reduced-motion 与三断点）。bun run test:integration 与全量 web:e2e 的既有失败为 clean-tree 预存环境失败（workflow-panes/daily-ops 等需后端 services；client-runtime 子模块），非本 change 引入（stash 对照确认）。owner 合同不可用面如实 needs_contract，不作为 production 证据。 **收口 (2026-08-21）**: 全部验收命令执行——`openspec validate --strict` PASS；`bun test packages/task-sdk` 361/361；`bun run typecheck` 绿；`bun run test:contract` 364/364（复跑确认，初跑 1 fail 为瞬态）；focused Playwright harness-a11y-responsive 5/5（三断点+键盘+reduced-motion）。owner 合同不可用面在 UI/测试中保持 needs_contract（harness route 默认 needs_contract 状态）。不可用 owner 合同如实 classified，不作为 production evidence。勾选。

## 5. Initial native read-only canary

- [x] 5.1 Add runtime-validated Harness context, descriptor, view, action, iframe-load and domain projection types to Task SDK; fail closed for null/unknown values, version/digest/context drift, missing security binding and sensitive refs/summaries.
- [x] 5.2 Add `/harness` with Eikona, Scaena, Anatomia and Ordo native-reviewed read-only tabs; accept an injected validated snapshot or the connected typed BFF projection seam and display `needs_contract` when no owner projection exists.
- [x] 5.3 Add keyboard roving tabs, responsive layout, readable readiness states and fail-safe development proxy behavior when no token file is configured.
- [x] 5.4 Verify the canary with focused SDK/Web/policy tests, full Task SDK tests, typecheck, production build, browser/axe QA, strict OpenSpec validation and scoped diff checks.
- [ ] 5.5 Promote beyond canary only after real BFF context/owner contracts, query/event teardown, catalog/install, receipt/reconcile dispatch and sandboxed iframe runtime have independent provider/consumer evidence.

## Current implementation evidence (2026-08-14)

- `packages/task-sdk/test/harness-contract.test.ts`: 19 tests passed; descriptor/view/action/projection plus context-bound resource, monotonic event batch, receipt/reconcile, projection-list context binding and existence-hiding safe-error contracts fail closed on drift, cross-context data and sensitive fields. Domain projections now carry exact `surfaceId`, `projectionVersion` and `contractDigest` bindings; the static registry rejects unapproved release digests and renderer/origin identities.
- `packages/task-sdk/test/harness-client.test.ts`: 4 tests passed; HTTP requests remain same-origin and carry no browser tenant/workspace target, JSON-RPC uses `workbench.harness.v1alpha1`, and malformed/sensitive context, descriptor and projection-list responses fail closed.
- `apps/web/test/harness-route.test.tsx`: 13 tests passed; the connected route binds context/descriptor/projection queries to `authorityKey`, `sessionRevision` and `contextRevision`, registers requests with `AuthorityResourceRegistry`, preserves safe authorization diagnostics, requires an exact same-context `installationRef`/`surfaceId`/`projectionKind`/`projectionVersion`/`contractDigest` match plus the static release registry before connected projections render, fails closed on unapproved releases and descriptor drift, renders four domain-specific read-only groups, and restores Eikona focus when the authoritative revision changes.
- `service/internal/transport/harnesshttp/handler_test.go`: 4 focused Go tests passed; the same-origin BFF shell returns stable `needs_contract` context/descriptor/projection envelopes, ignores URL tenant/workspace scope, rejects mutation/unknown paths and emits no sensitive fields. `service/internal/runtime/runtime_test.go` loopback coverage also exercised the existing routes with the local session token.
- `bun run typecheck`: passed; `bun test packages/task-sdk`: 298 tests passed.
- `bun run test:contract`: package SDK coverage passed, but the two live conformance cases were not accepted because the disposable `workbenchd` did not become healthy within the existing 5-second readiness window. This is service-readiness evidence only; it does not promote the Harness contract or claim provider/deployment readiness.
- The new facade and BFF shell are consumer contract seams only. The BFF shell is intentionally static and fail-closed: real Identity/Harness Control Plane authority, descriptor catalog, tenant reset lifecycle, owner projections, event stream and production deployment remain `needs_contract`/owner-gated.
- The connected Harness route now consumes a typed `ListHarnessProjections` seam while the local BFF returns an empty `needs_contract` envelope. The route composes validated safe projections by domain: Eikona separates generation process, candidate, rights and owner decision cards; Scaena, Anatomia and Ordo each receive distinct read-only fact groups. Connected projections additionally require a same-context, exact `installationRef`/`surfaceId`/`projectionKind`/`projectionVersion`/`contractDigest` match against an `available` descriptor that also passes the explicit static release/renderer/origin registry; missing refs/catalog, unapproved releases or descriptor drift remain `needs_contract`, receipt status is explicit as unavailable in this canary contract, invalid projection/descriptor objects are removed after validation, and permission/contract/offline/degraded diagnostics remain bounded and redacted. `./node_modules/.bin/vitest run test/harness-route.test.tsx` passed 13/13, `bun test packages/task-sdk/test/harness-contract.test.ts packages/task-sdk/test/harness-client.test.ts` passed 23/23, `bun run typecheck` and Web production build passed. This remains local UI/consumer evidence only; it does not back owner plugin installation, iframe runtime, event teardown, BFF owner projections or provider acceptance.
- Task `1.2` completed locally: `apps/web/src/workbench/harness/context-switch.ts` adds a pure `reduceHarnessPresentation` reducer (context key derived only from the validated BFF context; a key change resets tab, focused resource, overlay and action preview in one transition before rehydration) plus typed teardown seams `registerHarnessEventStream`/`teardownHarnessContextWork` on the existing `AuthorityResourceRegistry` (abort queries, close streams, never replay). `HarnessRoute` now runs on this reducer. `./node_modules/.bin/vitest run test/harness-context-switch.test.ts test/harness-route.test.tsx` passed 20/20. A real owner event stream remains `needs_contract`; only the local teardown seam is exercised with injected fakes.
- Task `2.1` completed locally: descriptors now carry optional bounded opaque `deepLinkTargets`, the static slot registry validates an optional `allowedDeepLinkTargets` allowlist, `validateHarnessPluginDescriptorAgainstRegistry` fails closed with `deep_link_denied` when a declared target is not allowlisted, and `validateHarnessDeepLinkTarget` gates single-target navigation. `bun test packages/task-sdk/test/harness-limits.test.ts` covers these gates.
- Task `2.4` local primitives delivered in `packages/task-sdk/src/harness/limits.ts`: `measureHarnessPayload`/`withinHarnessPayloadBudget` (byte/depth/array budget, cyclic fail-closed), `coalesceHarnessEvents` (latest per type/resource, ascending, capped), `createHarnessTelemetryEvent` (whitelisted redacted telemetry, sensitive fields/counts fail closed to null) and `sliceHarnessWindow` (clamped virtual window). Per-surface lazy loading still depends on the owner descriptor catalog and stays `needs_contract`; task `2.4` remains open. `bun test packages/task-sdk/test/harness-limits.test.ts packages/task-sdk/test/harness-contract.test.ts packages/task-sdk/test/harness-client.test.ts` passed 34/34, `bun test packages/task-sdk` passed 309/309, `bun run typecheck` and Web production build passed. All of this remains focused/local consumer evidence; it does not back owner BFF contracts, catalog/install, iframe runtime or provider acceptance.
- Task `2.2` completed locally: `packages/task-sdk/src/harness/iframe.ts` adds the sandboxed iframe host primitives — default-deny sandbox (`allow-scripts` only, never `allow-same-origin`), `no-referrer` policy, the gateway-served document CSP contract (`default-src 'none'`, `connect-src 'none'`), `createHarnessIframeHostProps` (registry-gated, src is exactly `origin + path` with no query/fragment/credential surface) and the capability-limited `harness-bridge.v1` postMessage contract: `createHarnessBridgeChannel` (per-load nonce, descriptor/context-derived expectation) and `validateHarnessBridgeMessage` (exact source-window pinned by the host, opaque sandbox origin, nonce drift → `blocked_supply_chain`, installation/release/context-revision drift → `contract_mismatch`, unknown fields/message types/payloads/sensitive fields fail closed). `apps/web/src/workbench/harness/iframe-host.tsx` renders the iframe only after every gate and unmounts it into a redacted blocked status on any drift. Actual artifact byte-digest verification runs in the gateway/host (`gateHarnessIframeLoad`) and the real descriptor catalog stays `needs_contract`. `bun test packages/task-sdk/test/harness-iframe-bridge.test.ts` passed 12/12; `./node_modules/.bin/vitest run test/harness-iframe-host.test.tsx` passed 6/6.
- Task `2.3` completed locally: `packages/task-sdk/src/harness/actions.ts` adds descriptor-driven action confirmation primitives — deterministic context/target/expected-version-bound `harnessActionIdempotencyKey` (retry reuses the original identity via `harnessActionRetryIdempotencyKey`, never minting a fresh one), `createHarnessActionConfirmation` (display gate: confirmation copy, expected version, idempotency key, blockers; never self-enabling), the typed `HarnessActionDispatchSeam` with injectable `createFakeHarnessActionDispatcher` (records requests, defaults to `transport_unknown`), and `observeHarnessActionReceipt` (authoritative+error → definitive `rejected`; `pending` observed; `unknown`/`partial` → `reconcile_required` with reconcile ref; transport acceptance never reads as success; `autoRetry` is always false). `apps/web/src/workbench/harness/action-confirmation.tsx` dispatches exactly once per explicit confirm through the injected seam, surfaces reconcile-required states without replay, and offers an explicit retry only after definitive rejection with the original idempotency identity. Real TaskService/BFF dispatch stays `needs_contract`; only the seam is exercised with fakes. `bun test packages/task-sdk/test/harness-action-confirmation.test.ts` passed 10/10; `./node_modules/.bin/vitest run test/harness-action-confirmation.test.tsx` passed 6/6.
- Task `4.2` partially advanced (stays open): descriptor/iframe isolation, action gates, idempotency identity and unknown-acceptance no-auto-replay now have unit/component coverage via the four files above; tenant reset was already covered by `harness-context-switch.test.ts`. Eikona-vs-Scaena authority, asset grant expiry, Canvas staleness, Anatomia/MCP redaction and Ordo lease reconcile coverage still depends on the 3.x projections and stays pending. Full verification after this round: `bun test packages/task-sdk` 331/331, `./node_modules/.bin/vitest run test/harness-route.test.tsx test/harness-context-switch.test.ts test/harness-iframe-host.test.tsx test/harness-action-confirmation.test.tsx` 32/32, `bun run typecheck`, `bun run build` (CGO_ENABLED=0 Go binaries) and `bun run web:build` all passed. All of this remains focused/local consumer evidence; it does not back owner BFF/Control Plane contracts, real catalog supply, gateway byte-digest verification or provider/deployment acceptance.
- Task `3.4` Anatomia half delivered locally (stays open for the Ordo projection): `packages/task-sdk/src/harness/anatomia.ts` consumes the frozen owner spellings read-only — `anatomia.video_breakdown_page.v1alpha1` and `anatomia.video_breakdown_event_continuation.v1alpha1` (mirrored from `agent/anatomia/sdk/contract`, owner files untouched). `validateAnatomiaBreakdownPage` enforces exact fields, opaque owner cursors only (digits-only offsets rejected), unique safe refs, retention `retained|expiring|revoked` with a nullable `policy_ref` (missing/null/empty read as absent, never as a rejection), and revoked pages that keep identity but serve no `item_refs`. `validateAnatomiaBreakdownContinuation` accepts exactly one typed shape: bounded ordered `events` (event versions never exceed the page version, safe summaries only) or `gap_detected`/`cursor_expired` with the single owner repair `read_projection` and no events/cursor. `deriveAnatomiaEvidenceCard` maps validated envelopes to `ready|stale|unknown|reconcile|revoked|needs_contract`; invalid envelopes read as `unknown` with zero fabricated facts, absent data as `needs_contract`, and `stale`/`reconcile` surface `read_projection` as display-only repair. The `AnatomiaBreakdownReadSeam` (`readPage`/`continueEvents`) plus `createFakeAnatomiaBreakdownReadSeam`/`createNeedsContractAnatomiaBreakdownReadSeam` keep the BFF `pages:read`/`events:continue` proxy a typed seam; the real BFF/owner wiring stays `needs_contract` and the browser never sees delegated context. `apps/web/src/workbench/harness/anatomia-evidence.tsx` renders the safe fact cards (page identity/version/cursor, retention state/policy with revoked item withholding, event status/count) and is now mounted by the `anatomia.video` branch of `HarnessRoute` with the default needs-contract seam. `AnatomiaHarnessProjection` gained optional `breakdownRef`/`analysisRef`; absent identity keeps the panel `needs_contract` instead of guessing. Ordo Agent Operations projection remains `needs_contract` pending the Ordo owner snapshot/event/action contract. Verification: `bun test packages/task-sdk/test/harness-anatomia-consumer.test.ts` 24/24, `./node_modules/.bin/vitest run test/harness-anatomia-evidence.test.tsx test/harness-route.test.tsx test/harness-context-switch.test.ts test/harness-iframe-host.test.tsx test/harness-action-confirmation.test.tsx` 40/40, `bun test packages/task-sdk` 355/355, `bun run typecheck`, `bun run build` (CGO_ENABLED=0) and `bun run web:build` all passed. This is focused/local + browser/component consumer evidence only; it does not back the Anatomia BFF delegated-context proxy, real owner page/event traffic, MCP endpoint access or provider acceptance.
- Task `4.2` further advanced (stays open): Anatomia/MCP redaction now has unit/component coverage — sensitive refs/field names, URL-carrying summaries, digits-only cursors, unknown fields and revoked-item leakage fail closed in `harness-anatomia-consumer.test.ts`, and the panel never renders facts from invalid envelopes (`harness-anatomia-evidence.test.tsx`). Eikona-vs-Scaena authority, asset grant expiry, Canvas staleness and Ordo lease reconcile coverage still depends on the remaining 3.x projections and stays pending.
