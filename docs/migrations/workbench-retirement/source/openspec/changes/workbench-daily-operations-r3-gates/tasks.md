# Workbench Daily Operations R3 验证门禁实施任务

> 本任务清单逐字承接已归档 change `workbench-desktop-daily-operations-r3` 的 4 项未完成任务（2026-08-16 迁移），任务编号、Owner、Lane、依赖与验收口径保持原文。

## 8. Verification and R3 Promotion（Lane V）

- [x] 8.2 执行 real dependency integration/system；Owner: test-engineer；Dependencies: 8.1、R1/R2 integration-ready；Paths: evidence only；Scope: disposable PostgreSQL、Identity test tenant、至少一个真实 Owner、projection rebuild、Task/Gate/receipt/SSE；Acceptance: fixture不能替代 provider，六件套/versions/digests/redaction完整；Verification: `task test:daily-ops-integration`；Expected: daily service/system report pass；Failure recheck: owner/identity缺失则保持 blocked，不降低测试范围。**Evidence (2026-07-28)**: 新增 `service/test/dailyopsintegration/integration_test.go`（3 test）。真实 GORM SQLite 持久化（real ACID database，非 mock/fixture——real transactions/CAS/SQL/schema migration）：① AssetProjectionToSearch（asset upsert → real query readback → search index populate → typed search → tombstone clears search）② WorkItemProjectionWorker（projection event → state machine → repository upsert；version CAS 更新；lower-version skip）③ CrossTenantIsolation（两 tenant seed → search 按 scope 隔离 → cross-tenant Get 返回 not_found）。修正 AssetSearchModel index 名冲突（idx_asset_ref → idx_search_asset_ref，SQLite 全局 index 名唯一）。Verification（umask 077 + CGO_ENABLED=0 glebarez pure-go sqlite）：3 integration test PASS + search repository 12 test PASS + search service 4 test PASS + `go build` exit0 + openspec strict valid。SQLite 是真实数据库（非 fixture）；external Owner/Identity test tenant 集成（real PostgreSQL + Identity provider + Owner connector）由 disposable CI 环境推进（gated on WORKBENCH_TEST_POSTGRES_URL，workitem PG test 已示范 pattern）。**PostgreSQL managed-profile evidence (2026-07-28 续)**：新增 `service/test/dailyopsintegration/integration_test.go::TestPostgresDailyOpsProjectionIntegration`（gated on `WORKBENCH_TEST_POSTGRES_URL`，独立 schema 隔离，无 PG 时 SKIP）。在真实 PostgreSQL 14（managed profile DB，非 SQLite/fixture）复验：asset projection upsert → real PG query readback → search index populate → typed search；projection 版本 CAS（低版本不覆盖高版本 canonical）；跨租户 search+Get 隔离。Verification（umask 077 + disposable PG14 `workbench_test@127.0.0.1:5432`）：PG test PASS + SQLite 三测试 PASS + `go vet`/`go build` exit0。real Identity provider + Owner connector 集成仍需外部 disposable CI（identity-platform submodule 未就位），本项保持 pending。 **Re-check (2026-08-21 PG 解锁）**: `go test ./test/dailyopsintegration/ -count=1 -v` **4/4 PASS 含 PG 用例**（PostgreSQL managed-profile 用例在本地 PG14 disposable 全过，WORKBENCH_TEST_POSTGRES_URL 注入后不再 SKIP）；evidence run `20260821131837` passed/exit 0。real Identity provider + Owner connector 集成仍需外部 disposable CI——保持 pending（R1 依赖未变）。 **Re-check (2026-08-28 真实栈勘察)**: 新增 `service/test/dailyopsintegration/real_stack_test.go::TestRealIdentityOwnerDailyOpsSystem`（`-tags=integration`，7 个 WORKBENCH_TEST_* env 门控）+ Taskfile `daily-ops-integration:real-stack:scenario` / `test:daily-ops-integration:real-stack`。真实 loopback 栈（identity-platformd（/tmp/r2canary 08-22 预构建二进制）+ eikona canary（JWKS/issuer/proj_canary）+ disposable PG14 + 测试内自签 TLS 同源 JWKS 反代 + 临铸 5min PrincipalContext + 真实 workbench-migrate/workbenchd 进程）已验证通过的链路：managed profile 启动校验、真实 PG 外部 migration（0037 全套）、TLS 同源 JWKS 验签（identity validator 强制 HTTPS 同源约束经 WORKBENCH_IDENTITY_JWKS_CA_FILE + WORKBENCH_PROFILE=managed env 层生效——LoadConfig 在 flag.Parse 前运行，-profile flag 单独不足）、真实 JWT 认证 catalog 且 eikona.generation.submit 真实晋级 ModeOwner。**mutation 授权链被结构性阻塞（如实保持 pending）**：workbenchd runtime `identity.NewService(profile, auth, nil)`（runtime.go:796）——identity Provider 合同 needs_contract（workbench 侧无任何 HTTP Provider 实现，identity-platform API 亦无 AllowedActions 端点，属 R1 未完成设计面）；operator-mounted disposable-staging allowlist 路径所需的 task.submit scope principal 只能由 identity-staging-principal 签发，而该工具钉死 staging compose 拓扑（issuer identity-tls:8443 + postgres:5432/identity_disposable_staging verify-full + /run/workbench-secrets + stagingcontrol DSN 校验）。测试在 submit 503 identity_unavailable 时以精确自诊断失败（不绕过、不降级）；R1 provider 合同接线或 staging compose 环境就绪后本 gate 转绿。本次勘察结论与「Dependencies: R1/R2 integration-ready」一致：R1 消费端授权未就绪，8.2 不可关闭。
  - **Closeout (2026-08-30 R1 provider 链解锁)**：workbench-identity-provider-contract-v1 slice 2/3 落地——identity-platform 实现 `/v1alpha1/identity/provider/*` 五端点（session/membership fence + scope 授权）；workbenchd 以 `WORKBENCH_IDENTITY_PROVIDER_URL` 接入真实 provider；结构性根因修复（workitems `_` 占位符折叠、Task 0041 权威 session/scopes/auth-window 迁移与执行期 principal 恢复）；`TestRealIdentityOwnerDailyOpsSystem` 全链 PASS（真实 PG + 真实 Identity issuer + eikona canary + workitem 真实持久化 + owner delegation receipt + SSE/receipts/脱敏）；system evidence run `20260830090100-39c102fc-4481-42b9-b14b-d835a4b29ce2` passed/exit 0/redaction passed（六件套）。
  **机制解锁 + PG 矩阵刷新 (2026-08-22)**：(1) 交付此前缺失的验证目标 `daily-ops-integration:test` / `task test:daily-ops-integration`（Taskfile，evidence-harness 包装）；(2) 真实数据库矩阵 4/4 全过（asset projection→search、workitem projection worker CAS、cross-tenant isolation、PostgreSQL managed-profile 投影）——本地 disposable PostgreSQL 14，evidence `temp/integration-test-runs/20260822175329-eec68df3-ca2d-422b-b4e8-b02c5cd7856c/`（passed、exit 0、redactions 0）。(3) 旧阻塞前提更新：R1 0.1 已交付 `backend-server/identity-platform`（2026-08-22，JWT PrincipalContext 全链证据在案），R2 3.6/4.4 已有真实 Eikona read/mutation adapter——8.2 的"real Identity provider + Owner connector"装配材料已就位，剩余工作是把两者拉进本 gate 的 harness（Kratos 进程 + Eikona loopback + Task/Gate/receipt/SSE 断言），保持 pending。
- [x] 8.3 执行 browser/a11y/security E2E；Owner: browser-debugger + security-reviewer；Dependencies: 8.2；Paths: diagnostic evidence；Scope: restore/deep-link/back-forward/fullscreen/mobile/keyboard、cross-tenant、revoke、owner offline、conflict、unknown_accept、handoff；Acceptance: critical journeys通过，P0/P1为零；Verification: `task test:desktop-e2e && task test:daily-ops-e2e && task test:security`；Expected: browser/security report pass；Failure recheck: 固定 identity/tenant/project/layout/viewport 重放并保留 screenshot/trace/network。**Evidence (2026-07-28)**: 新增 `apps/web/e2e/daily-ops-panes.spec.ts`（Playwright）。测试覆盖：asset library pane 渲染 + search、workitem pane 列表、daily ops pane 四 section（Inbox/Approvals/Activity/Deliveries）、keyboard 导航（Tab focus）、200% zoom 不遮挡（viewport 640 模拟）。前置 webServer config 控制（dev server 未运行时 CI skip）。真实 P0/P1 critical journey 证据（screenshot/trace/network + axe a11y scan）由 disposable CI 环境推进（需 app dev server + 后端 services）。
  - **Increment（2026-09-01，真实栈浏览器旅程扩展）**：`e2e/daily-ops-real-backend.spec.ts` 1→5 journey——原真实栈诚实投影+axe 0 critical 之上新增 deep-link/back/forward 历史导航、fullscreen 切换（graceful 拒绝可接受、不崩溃）、mobile viewport（390×844 无遮挡/错误态）、纯键盘 Tab 序列（role-aware 交互目标 + Escape 不破）。旅程改用注册路由 `/agent`（裸 `/workspace` 现为诚实 Route unavailable——路由面经 spatial v3 演进为 `/workspace/:ownerId/:projectRef` 参数形态）。`temp/daily-ops-real-backend-e2e.sh` 全链 5 passed（真实 identity-platformd+TLS JWKS 代理+managed workbenchd+disposable PG）；`task test:daily-ops-e2e` evidence `20260901021916-8a9bc8a5-4453-4e03-9f5e-3d774170f35e` passed。8.3 覆盖面推进后仍 pending：cross-tenant/revoke/owner offline/conflict 已落地（见下），unknown_accept（审批未知接受不重放）与 handoff 旅程仍缺审批/handoff 领域面。
  - **Increment 二（2026-09-01，mutation 旅程 J1-J6 + offline）**：真实栈脚本三阶段——Phase 2 真实 API mutation：J1 主体 A session 投影 200；J2 真实 API 创建 workitem（非 SQL 种子）；J3 cross-tenant 隔离（主体 B ten_other list 不可见 A 的创建，无泄漏）；J4 revoke 传播（identity-platform 撤销 sid_dailyops_b → workbenchd Session 投影立即 403）；J5 幂等重放（同 Idempotency-Key 顺序重复 update 由 replay 缓存吸收，replayed=true、无二次 mutation）；J6 版本冲突（stale expectedVersion 不同 key → 409；并发同 key 诚实冲突同路径）。Phase 3 owner offline：identity-platformd+TLS 代理停机 → 浏览器诚实降级（壳渲染不白屏、无伪造数据行）。验证门：`task test:security` exit 0、`task cli:e2e:test` 3 passed（desktop/tablet/mobile/200%/keyboard/Axe/recovery/Pane 面）、`task test:daily-ops-e2e` passed。注：workitems List 需 tenant_ref+workspace_ref 双参（validateScope）；单条 create 无幂等去重（同 ref 重放 500——batch 才有 idempotency record，如实记录为产品观察，未在本次修复）。
  - **Evidence (2026-09-01 第四波：缺口①②收口，8.3 关闭)**：上一波留下的两个诚实缺口本波解决——**缺口①（控制面 Task 投影接线）**：6.1b projection consumer 已在 workbenchd runtime 真实挂载。实现：`service/internal/operations/projection/controlplane/`（`Projector`：控制面 Task/Gate 变更 → daily 投影 source events，经既有 `operations/projection.Consumer` + 域 adapter apply；`ObservingStore`：包装 TaskService 的 repository.Store，CreateSubmission/ApplyMutation/CreateRetry 提交成功后同步通知投影——投影是派生索引，失败只 log 滞后不回滚控制面；`TaskSourceCommandPort`：daily item typed command → 控制面 `ResolveGate`/`ReconcileUnknownAccept`，版本 CAS + 投影 store tenant fence 先于 source 调用；defer/request_changes 等 source 无对应 typed command 的动作 fail-closed 422）；`service/internal/operations/commands/memory_intent_store.go`（进程内 dispatch intent 幂等，重启后由 source CAS 收敛）；`service/internal/operations/projection/payloads.go` + adapters（SourceEvent 域 payload 扩展，修复 6.1b adapter 丢域字段的缺口）；`service/internal/repository/daily_store.go` 加法 `ListApprovalItems`/`CountActivity`；`service/internal/transport/dailyopshttp/`（浏览器直连 HTTP 面：inbox/approvals/activity 读 + decide/execute typed mutation，SDK strict normalizer wire 合同 `workbench.operations.v0.1`；delivery 无 source 接线不注册 route）；runtime.go 挂载（registry daily read 提升 available + `/v1alpha1/operations/daily/` mux + `taskStore` 包装）。前端：SDK `http.ts` daily 路由、`use-agent-pane-availability.ts` dailyOps transport 翻转、agent daily-ops pane expectedVersion 改 source CAS 语义 + tabs 常驻（空态不再藏起审批 affordance）。**新浏览器断言（真实 UI，无 fixture）**：Phase 5b J18-J20 pane approve 旅程——审批经 pane「批准」真实触发（typed command → ResolveGate → 真实 eikona owner dispatch）→ open 审批列表收敛（source 派生 decided）+ 活动分区 succeeded 行可见 + axe P0/P1=0；Phase 4 unknown 窗口旅程重写——Inbox 显示真实 reconcile 行（unknown_accept 语义、含 receipt-safe task ref）且不伪造任何终态（succeeded/failed/cancelled/completed 均断言不在行内）、pane 无错误态；J20 API 复核：approval 投影 approved、open 列表无泄漏、succeeded activity 行、未认证 daily 读 401 fail-closed。**缺口②（test:desktop-e2e）**：Taskfile 加法 `test:desktop-e2e` 别名（fail-closed 路由到既有桌面面套件 `test:daily-ops-e2e` + `test:cli:e2e`，evidence runner 脱敏六件套）。**验证门（本波全绿）**：`sh temp/daily-ops-real-backend-e2e.sh` 全链 exit 0（Phase 1 浏览器 5 passed + Phase 4 窗口 1 passed + Phase 5b pane approve 1 passed + Phase 3 offline 1 passed；J1-J20 API 断言全过）；`task test:desktop-e2e` exit 0（evidence `20260901065954-b0ce4907-e375-41cb-ac11-6845f82a8bdb`/`20260901070039-9320748d-5f71-4cbe-9d25-2a28b3d90321`）；`task test:daily-ops-e2e` evidence `20260901065725-05d7f1d3-3ca2-4a59-9935-8673f5ca47b7` passed；`task test:security` exit 0；`task cli:e2e:test` 3 passed。组件级：`go test ./internal/operations/... ./internal/transport/dailyopshttp/... ./internal/app` 全绿（含 controlplane 真实 SQLite 链路 4 测试：投影+typed approve/幂等重放/stale 冲突/unknown_accept inbox→resolved/无 tenant 跳过/fail-closed ref；dailyopshttp wire 合同+tenant fence 403+unsupported 422+delivery 404）；`bun test packages/task-sdk` 500/500；web vitest 109/109（pane/availability/conversation workspace）。scope 全项复核：restore/deep-link/back-forward/fullscreen/mobile/keyboard（Phase 1）、cross-tenant（J3/J14/J20 unauth 401 + 组件 403）、revoke（J4/J17）、owner offline（Phase 3）、conflict（J5/J6/J9/J10）、unknown_accept（J7-J12+重写窗口旅程）、handoff（J13-J17+J18-J20）——全部真实栈证据，P0/P1=0（axe critical/serious 为零）。**如实记账（不阻塞本门、属后续 connector 范围）**：①本切片 daily 投影只索引控制面 source（task_gate→approval、unknown_accept→inbox reconcile、task events→activity）；inbox 的 failed/partial task、WorkItem blocker、Owner review、Delivery blocker 索引与 delivery 读路径/defer 投影本地化仍未接线（SDK 调用 404/422 诚实呈现，capability spec 完整聚合待后续 change）；②投影 cursor 为进程内（同步消费合同下足够；tail 型续读 consumer 需 durable cursor）；③staging 环境全矩阵重放仍属 staging 门（8.4/8.5）。
  - **Partial evidence（2026-09-01 第三波：unknown_accept + handoff + 自起 disposable 栈）**：`temp/daily-ops-real-backend-e2e.sh` 全栈自起化并补齐两个缺口旅程（证据 `temp/daily-ops-real-backend-e2e-evidence-20260901/`，脱敏无凭据/无本地绝对路径）。栈：私有 disposable PostgreSQL cluster（initdb/pg_ctl 自起可停，127.0.0.1:54330，替代共享本地 PG）+ 真实 identity-platformd（19401）+ TLS 同源 JWKS 反代（19443，新增 `temp/daily-ops-e2e-helpers/tlsproxy`）+ 真实 eikona canary owner（18146）+ owner mutation 故障注入代理（18147，`temp/daily-ops-e2e-helpers/faultproxy`：owner 确实接受后响应单次黑洞；workbenchd WORKBENCH_EIKONA_URL 指向它）+ 真实 workbenchd（managed，18487/18488）；端口全避开默认值，结束全停无孤儿。**unknown_accept（J7-J12）**：真实 owner-mode task（eikona.generation.submit，permission+cost 双真实 gate）在响应被黑洞后诚实转 `unknown_accept`（owner 实际已接受、非伪终态）；2s 无自动重发（dispatch attempt 恒 1/status unknown/无伪造 execution receipt）；retry 412（RetryTask 拒绝 unknown_accept）；伪造 caller-observed 对账 412 且任务保持 unknown_accept；权威对账（owner 按 idempotency key 状态查询，透传）确定性转 succeeded + reconciliation receipt（own_…）；浏览器未知窗口旅程（WORKBENCH_UNKNOWN_ACCEPT_WINDOW=1）断言 UI 不伪造 probe 行/终态、pane 诚实空态。**handoff（J13-J17）**：eikona.handoff.prepare 交接面——A 提交停真实 permission gate；跨租户 B（全 scope+自有工程授权）GET/gate resolve 均 404、自有 list 无泄漏；A approve → 真实 owner 接受 → succeeded + execution receipt（own_…）投影可见；同租户已授权成员 C 可见（200）；identity revoke C 后同一交接面读取拒绝（404）。**restore 补强**：deep-link 旅程新增真实整页 reload 断言（session/layout restore 不白屏/错误页/合同缺失）。**conflict 记账**：已由 J5（同 key 幂等重放 replayed=true）+ J6（stale expectedVersion → 409）覆盖，本波不重复。验证门（本次全绿）：脚本全链 exit 0（Phase 1 浏览器 5 passed + Phase 4 窗口 1 passed + Phase 3 offline 1 passed）；`task test:daily-ops-e2e` evidence `20260901060016-ec914f01-f6dd-4bde-83e0-0816d8f24dbf` passed/redaction pass；`task test:security` exit 0；`task cli:e2e:test` 3 passed。**仍缺（保持 pending 的理由）**：① 浏览器 daily-ops Inbox/Approvals pane 对控制面 Task 的投影未接线（workbenchd runtime 未挂 6.1b projection consumer）——unknown_accept/审批在 pane 上只有诚实空态，无 live reconcile/approve affordance 的浏览器旅程（产品接线属 6.1b/R3 服务面，非本验证门可伪造）；② 8.3 Verification 所列 `task test:desktop-e2e` 目标不存在于 Taskfile（以 `task cli:e2e:test` + 真实栈全链替代，如实记录）；③ staging 环境全矩阵重放仍属 staging 门。
  - **Real-backend increment (2026-08-30）**：真实栈 browser journey 全绿并落盘 e2e evidence `20260830112346-a939857f`（passed/exit 0/redaction passed）——`apps/web/e2e/daily-ops-real-backend.spec.ts`（无 fixture route）+ `temp/daily-ops-real-backend-e2e.sh` 编排：真实 identity-platformd（:8499）+ 自签 TLS 同源 JWKS 反代（:19443，PEM CA）+ 真实 workbenchd（managed、disposable PG、provider/delegation URL、真实 PrincipalContext token 文件）+ vite 同源代理 token 注入 + Playwright Chromium 真浏览器 + **@axe-core critical/serious 违规 = 0**；断言诚实空投影（无 backend_needs_contract/伪数据/错误页）。8.3 剩余：restore/deep-link/revoke/owner offline/conflict/handoff 全矩阵 journey 与 staging 环境属 disposable CI/staging 门，保持 pending。
  - **Increment (2026-08-30)**：`task test:daily-ops-e2e` 全绿并落盘 e2e evidence `20260830090907-fed501e3`（5/5：panes 渲染/search/键盘 Tab/200% zoom；passed/exit 0/redaction passed）；`task test:security`（security/runtime/transport Go 包 + local-session bun）通过；desktop 级 `workbench.spec.ts`（gates 无乐观成功/unknown-accept reconcile/mobile/多 viewport）+ `agent-direct-panes.spec.ts` 16/18 过（`identity readiness gates` 修复 loginMethods 重复 reason 文案后过；`CLI entry fail-closed` 为 HEAD 基线即失败的预存回归，与本 change 无关，如实记录待 CLI owner 修复）。真实后端 orchestration 的完整 P0/P1 journey（restore/deep-link/revoke/owner offline/conflict/handoff + axe 扫描）仍需 disposable CI 接 real-stack harness，保持 pending。
- [ ] 8.4 执行 performance/capacity/soak；Owner: performance-reviewer + test-engineer；Dependencies: 8.3；Paths: performance evidence only；Scope: 100k assets、50k WorkItems、20 panes、200 SSE、24h projection/daily loop soak；Acceptance: latency/memory/reconnect/error budget达标，profile显示无无界 query/render；Verification: `task test:performance SCENARIO=daily-operations && task release:soak ENV=staging`；Expected: production budget report pass；Failure recheck: 分离 DB/query/network/render热点，不用空数据替代。**Evidence (2026-07-28)**: 新增 `service/test/dailyopsbenchmark/benchmark_test.go`（4 benchmark）。Go testing benchmark 验证 search/projection 效率：① SearchPagination（10k assets，pageSize=50，typed text search 分页）② AssetUpsert（projection upsert 效率）③ AssetGet（indexed single-asset lookup）④ SearchNoResult（worst-case 空结果 scan bounded by scope index）。基准证明算法效率（bounded by page size + scope index，非 unbounded scan）。100k capacity p95 evidence + 50k WorkItem + 200 SSE + 24h soak 由 disposable PostgreSQL CI + staging 环境推进。 **本地 perf 半场完成 (2026-09-02，soak 半场仍开不勾)**: `test:performance SCENARIO=daily-operations` 场景落地并全绿（evidence `temp/integration-test-runs/20260902074055-73b9ba1c-57a9-4ef9-81ed-308c347d061c` passed/exit 0，disposable PG14 wb_084_perf）——①新 `TestPostgresDailyOpsSearchCapacityGate`（100k asset_search_index：typed facet p95=32.8ms、no-result p95=78.3ms、text LIKE p95=80.0ms，预算 500ms；cursor 深分页 20 页零重复；EXPLAIN(FORMAT JSON) rare-facet 拒 Seq Scan；pool in-use 归零）；②既有 50k WorkItem 门同 run 复跑 window p50=16.8ms/p95=18.9ms（预算 400ms）——**修真 bug**：该门缺 seed 后 ANALYZE（08-31 board 门同根因漏改），共享库写删搅动下 autovacuum 滞后→零统计→p95 一度 3000ms，补 `ANALYZE work_items/workitem_field_values` 后恢复 19ms；③200 SSE watch fanout 门同 run PASS。Taskfile 路由 `daily-operations` fail-closed 建齐。剩余：24h projection/daily loop soak 走 staging `task release:soak ENV=staging`（本波已建 target，缺 staging 输入时按名报缺失 exit 5）——同日复验 fail-closed exit 5（evidence `temp/integration-test-runs/20260902074629-afb69be9-906d-42a3-8b26-ec038fb511cd`，按名缺失五输入；本沙箱无 master1 接入材料，解锁需 operator 预置 fixture/start receipt + handshake）。
  - **Safety remediation (2026-09-02, still open):** the Taskfile no longer self-asserts `WORKBENCH_POSTGRES_TEST_TARGET=disposable`. Both 100k/50k capacity tests now fail before opening a database unless the caller supplies the disposable guard, and remain blocked before migration/seed/ANALYZE until an external provisioner-owned database-side attestation contract exists. This retracts the former local-PG pass as release evidence; it does not close 8.4.
- [ ] 8.5 执行 rollback与 R4 handoff closeout；Owner: root integrator；Dependencies: 8.4、review/security approval；Paths: flags/runbook/OpenSpec evidence；Scope: desktop_v3/asset_catalog/workitems/daily_ops独立 kill switch、legacy shell/read path、additive data、R4 stable refs；Acceptance: rollback不回到跨租户 localStorage/fixture owner，R4获得 approved Asset/WorkItem/Team refs；Verification: `task release:rollback:dry-run ENV=staging && openspec status --change workbench-desktop-daily-operations-r3 && openspec validate workbench-desktop-daily-operations-r3 --strict`；Expected: 达标后归档并允许 R4；Failure recheck: 任一数据/权限/receipt/performance/rollback gate缺失不得关闭。**Evidence (2026-07-28)**: 新增 `docs/operations/daily-ops-rollback-runbook.md`。定义 4 个独立 kill switch flag（WORKBENCH_DESKTOP_V3/ASSET_CATALOG/WORKITEMS/DAILY_OPS_ENABLED），单 capability 回退不影响其他；全 R3 回退恢复 legacy shell（additive，shell.tsx 未删除）。不变量：① 不回到跨租户 localStorage/fixture owner（legacy localStorage 仍由 3.3b 转 backend canonical）② additive schema 不回滚 migration（新表新列向后兼容）③ R4 handoff refs 保留（asset/workitem/daily projection metadata 不因 rollback 失效）。监控信号：projection lag/reconcile rescue/search rebuild stale 触发回退（7.2 readiness 对接）。rollback dry-run 验证命令 `task release:rollback:dry-run ENV=staging` 需 staging 环境执行（runbook 已定义步骤）；production rollback 需 ops 审批（out of scope for 本 session）。
