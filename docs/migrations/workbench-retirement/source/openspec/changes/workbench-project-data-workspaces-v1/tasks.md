# Workbench Project Data Workspaces V1 Tasks

执行规则：本 change 不授权当前会话自动启动子 agent。未来执行时，tracked-file 写入必须由单一 implementer 按非重叠路径租约推进；test-engineer/reviewer 只在稳定 diff 上独立验证。所有新增或修改的复杂逻辑、状态机、并发、错误处理、边界检查、协议转换和非显然 test fixture 必须写中文注释，说明不变量、失败语义与恢复条件，不记录完整推理链。

并行 lanes：`G` 依赖/准入，`C` 合同，`B` Backend，`W` Web/UI，`A` Automation，`V` Verification/Release。`C` 完成前不并行写生产实现；`B/W/A` 只有在路径租约独立时并行，transport/registry/generated files 由一个 owner 串行集成。

## 0. 准入、依赖与范围冻结

- [x] 0.1 Owner: root integrator；Lane: G；Dependencies: none；Paths: `openspec/changes/workbench-project-data-workspaces-v1/details/dependency-gate-matrix.md`；Scope: 记录 R1 identity、R3 WorkItem、R4 Board/Workflow、Agent Pane registry 的 contract version/digest、完成度、可消费 operation、外部门禁和 `needs_contract` fallback；Acceptance: 每个 required capability 都有 canonical owner、当前证据与阻断条件；Validation: `openspec status --change workbench-agent-pi-workspace-v1 && openspec status --change workbench-spatial-workflow-automation-r4 && openspec validate workbench-project-data-workspaces-v1 --strict`；Expected: dependency matrix 可审计且无 fixture promotion；Failure re-check: 不把进行中 change 的本地组件证据当 production-ready。
  - **Evidence (2026-08-23):** `details/dependency-gate-matrix.md` 建立五节矩阵：canonical operation registry digest `sha256:7a027f72…a369c1`（`workbench.operation_registry.v1`，14 ops，`workbench-operation-contract --output` 实测）；R1（`workbench.identity.v0.1`，6.2/6.3 closed evidence `20260822102347` 等，6.4 staging-blocked）；R3 WorkItem（`workbench.workitems.v1alpha1` 13 RPC，gates 8.2 PG evidence `20260821131837`）；R4 Board/Workflow（`workbench.board.v1alpha1` 36 RPC / `workbench.workflow.v1alpha1` 17 RPC，剩余外部依赖逐项列出）；Pane registry（15 closed kinds，含 `project.workspace.v1` 预切片）。Validation 三命令全 PASS。汇总判定明确无 fixture promotion，Owner 专业对象仅 PA 8.1 选定的 `eikona.generation.submit` 且 contract-gated。
- [x] 0.2 Owner: product + root integrator；Lane: G；Dependencies: 0.1；Paths: proposal/design/details only；Scope: 冻结首期一个 test tenant/project、默认 WorkItem dataset、四视图、五角色、9 custom field kinds、三类首发 trigger和一个低风险 Owner canary；Acceptance: Required Capability Ledger 的 deliver-now/retain-next/rejected 无静默删改；Validation: `openspec validate workbench-project-data-workspaces-v1 --strict`；Expected: first slice、retained slice、non-goals 明确；Failure re-check: 若 canary 只能靠 fixture或人工观察，重新选择 Owner operation。
  - **Evidence (2026-08-23):** `details/first-slice-freeze.md` 冻结：tenant `t:pdw`/workspace `pdw-main`/safe projectRef `proj_demo`（canary 用 `proj_canary`）；dataset `workitems.default`；四视图 table/kanban/todo/canvas；五角色 viewer/contributor/project_manager/automation_manager/project_admin；9 kinds（design 决策 3 原表）；三类首发 trigger manual/record_created/status_transitioned（schedule/approved_event retain-next）；Owner canary = PA 8.1 选定的 `eikona.generation.submit`（typed receipt/status/reconcile/cancel，非 fixture 判定）。Ledger 九行逐项核对无删改；non-goals 维持。`openspec validate workbench-project-data-workspaces-v1 --strict --no-interactive` PASS。
- [x] 0.3 Owner: dependency-auditor + implementer；Lane: G；Dependencies: 0.2；Paths: `apps/web/package.json`, `bun.lock`, change details；Scope: 对 `@tanstack/react-table`、`@tanstack/react-virtual`、`@dnd-kit/core` 做版本、bundle、license、React 19、Dockview/React Flow/a11y canary，确认 `@xyflow/react` 复用；Acceptance: 每个依赖有 adopt/fallback 决策且不叠加重复表格/DnD库；Validation: `bun install --frozen-lockfile && bun run typecheck && bun test` 加最小 canary test；Expected: 通过则固定版本，失败则保留 semantic table + menu/keyboard move fallback；Failure re-check: 不为通过 canary 升级无关依赖或 Node/Bun 主版本。
  - **Evidence (2026-08-23):** `details/dependency-audit.md` + `apps/web/test/project-data-dependency-canary.test.tsx`（3 tests pass）。决策：react-table **v8.21.3**（v9 为 createTableHook 全新重写 API，不采纳）、react-virtual 3.14.10、dnd-kit core 6.3.1/sortable 10.0.0/utilities 3.2.2，全部 MIT 且 React 19 peer 显式兼容；`@xyflow/react` 12.11.2 复用。canary 覆盖：5000 行虚拟化有界挂载（<50 行）、columnheader 键盘排序 aria-sort、KeyboardSensor Space/ArrowDown/Space 重排、role/aria-roledescription 语义。Validation：`bun install --frozen-lockfile` PASS、typecheck PASS、根 canonical `bun test packages/task-sdk` 362/362、apps/web vitest **122 files/1025 tests pass**（基线 121/1022）、`web:build` PASS（bundle 与基线一致，新依赖未被生产代码 import）。重复库扫描（ag-grid/react-window/react-beautiful/react-dnd/sortablejs/handsontable）lockfile 零命中。fallback（semantic table + 菜单/键盘移动）未触发。

## 1. 合同与 Operation Registry 冻结

- [x] 1.1 Owner: implementer；Lane: C；Dependencies: 0.2；Paths: `api/proto/workbench/project/**`, `api/schema/workbench/project/**`；Scope: 定义 ProjectWorkspace/Dataset/Field/Option/View/Role/Policy/Query/Event closed contracts、revision/idempotency/cursor/error；Acceptance: 无 arbitrary map/URL/path/payload，reserved field与limit明确，proto field numbers稳定；Validation: `buf lint && bun run test:contract`；Expected: proto/schema snapshots 一致；Failure re-check: 检查 unknown fields、oneof closed union、redaction 和 backward-compatible additive evolution。
  - **Evidence (2026-08-24):** 新增 `api/proto/workbench/project/v1alpha1/project.proto` 与 `api/schema/workbench/project/v1alpha1/project.schema.json`。封闭面：Workspace/Dataset/Field/Option/View/RolePolicy/Query/Event/Error；9 个 custom kinds；四视图 closed union；五角色；query 操作符 allowlist；`reserved 90 to 99`；无 map/Struct/URL/path/payload。钉选测试 `tests/project-contract-assets.test.ts` 3/3。`buf lint` 对 project proto PASS。`bun run test:contract` 386/386。
- [x] 1.2 Owner: implementer；Lane: C；Dependencies: 0.2；Paths: `api/proto/workbench/projectautomation/**`, `api/schema/workbench/projectautomation/**`；Scope: 定义 AutomationBinding/Trigger/InputMapping/Validate/Enable/Pause/Disable/ManualStart/Event contracts并 pin WorkflowDefinition version/checksum；Acceptance: binding不含step实现、credential、script或URL；Validation: `buf lint && bun run test:contract`；Expected: trigger/binding状态和错误可由四 transport表达；Failure re-check: browser clock/event不得成为 trigger truth。
  - **Evidence (2026-08-24):** 新增 `api/proto/workbench/projectautomation/v1alpha1/projectautomation.proto` 与 `api/schema/workbench/projectautomation/v1alpha1/projectautomation.schema.json`。Binding 只 pin `workflowDefinitionRef/version/checksum` + typed mapping；六态 `draft/enabled/paused/stale/needs_contract/disabled`；六类 trigger（含首发 manual/record_created/status_transitioned）；无 step/script/URL/credential。钉选测试 `tests/project-automation-contract-assets.test.ts` 3/3。`buf lint` PASS。`bun run test:contract` 在 1.1 后 386/386，本任务未改 SDK/conformance。
- [x] 1.3 Owner: implementer；Lane: C；Dependencies: 1.1；Paths: `api/proto/workbench/workitems/**`, `api/schema/workbench/workitems/**`, `packages/task-sdk/src/workitem-models.ts`；Scope: additive扩展 projectRef/datasetRef/schemaRevision/custom field values、typed project query、batch outcomes、safe changed-field events；Acceptance: system field语义不变、旧 consumer可忽略新字段、批量结果逐项稳定；Validation: `buf lint && bun run test:contract && bun test packages/task-sdk`；Expected: backward-compatible schema通过；Failure re-check: 不重编号proto、不把custom values做任意JSON。
  - **Evidence (2026-08-24):** 经 `scripts/daily-contract-assets.ts` 加法扩展：WorkItem 21–24（project/dataset/schemaRevision/customValues）、Event 14–17（project/dataset/schemaRevision/changedFieldRefs）、Scope 3–4、新增 QueryProjectWorkItems / BatchUpdateWorkItems。旧字段号未改。SDK 旧投影可忽略新字段；非法 kind 整笔 fail-closed。`packages/task-sdk/test/workitem-models.test.ts` 4/4。`buf lint`（workitems/project/projectautomation）PASS。`bun test packages/task-sdk` 390/390；`bun run test:contract` 通过。
- [x] 1.4 Owner: implementer；Lane: C；Dependencies: 1.1, 1.2, 1.3；Paths: `service/internal/registry/**`, generated operation snapshots, `packages/task-sdk/**`；Scope: 注册全部 Project/Automation/extended WorkItem operations，生成 canonical descriptors并声明permission/version/idempotency/event/receipt语义；Acceptance: handler不得拥有operation-specific业务逻辑，SDK facade暴露 `projects` 与 `projectAutomation`；Validation: `go test ./service/internal/registry/... && bun test packages/task-sdk && bun run test:contract`；Expected: descriptor/SDK/transport operation sets无漂移；Failure re-check: 搜索独立常量表和未注册直连service路径。
  - **Evidence (2026-08-24):** Closed validator 不再把 proto-legal `semanticToken`/`pageToken`/`statusToken` 误判为机密，仍 fail-closed `token`/`accessToken`/`sessionToken` 与未知嵌套字段。Canonical ResourceCatalog 从 Project/Automation/WorkItem Proto 生成 handlerless descriptors（permission/version/idempotency/event/receipt）；SDK facade 暴露 `projects`/`projectAutomation`。`CGO_ENABLED=0 go test ./service/internal/registry/...` ok；`bun test packages/task-sdk` 395/395；`bun run test:contract` 398/398；`buf lint` project/projectautomation/workitems 无 findings。未发现独立常量表或未注册直连 Project/Automation service 路径。
- [x] 1.5 Owner: implementer；Lane: C；Dependencies: 1.1-1.4；Paths: `api/locale/source/{zh-CN,en-US}/agent/**`, locale manifest/generated bundles；Scope: 新增 Project、Table、Kanban、Todo、Canvas、Schema、Automation、error/recovery 双语文案并运行 compose；Acceptance: UI无新增硬编码生产文案，reason code只作secondary metadata；Validation: locale compose command + `bun run typecheck && bun test`；Expected: catalog/source/generated一致；Failure re-check: inline fallback不得作为完成证据。
  - **Evidence (2026-08-24):** 新增 `api/locale/source/{zh-CN,en-US}/agent/project.json`（Schema/Automation/Canvas/error/recovery），并补齐 pane/detail 的 Schema/Automation/Canvas 标题。catalog 2929 keys；`bun run compose:i18n` + `--check` + `bun run check:i18n` PASS；`bun test scripts/compose-i18n.test.ts` 5/5；`bun run typecheck` PASS。根 `bun test` 全量 674 pass / 2 fail 为既有并行超时（`lifecycle-inventory` 默认 5s、`release-state` evidence runner 争用）；隔离 `bun test tests/lifecycle-inventory.test.ts tests/release-state.test.ts` 3/3 PASS。reason code 仅作 secondary metadata（`agent.project.error.reasonCode` = `原因代码 {code}`）。

## 2. Project Domain 与 GORM 持久化

- [x] 2.1 Owner: implementer；Lane: B；Dependencies: 1.1；Paths: `service/internal/projects/domain/**`；Scope: 实现 ProjectWorkspace/Dataset/Field/Option/View/Role policy domain、closed enums、limit、revision、archive与schema replacement rules；Acceptance: system field不可变、custom kinds/values有纯规则和稳定错误；Validation: `CGO_ENABLED=0 go test ./service/internal/projects/domain/... -count=1`；Expected: table/property tests覆盖valid/invalid/boundary；Failure re-check: 未知kind/operator/role必须fail-closed。
  - **Evidence (2026-08-24):** 新增 handlerless `service/internal/projects/domain`：closed JSON 方言枚举、稳定错误、workspace/dataset/field/option/value/schema/view/role/query 纯规则。系统字段不可改名/改类型/归档；自定义字段不得 shadow；64 字段/200 option/不兼容 kind 必须 replacement；未知 kind/operator/role fail-closed。`CGO_ENABLED=0 go test ./service/internal/projects/domain/... -count=1` ok。未新增 repository/transport。
- [x] 2.2 Owner: implementer；Lane: B；Dependencies: 2.1；Paths: `service/internal/repository/project_models.go`, migrations/catalog/snapshots；Scope: 新增 project workspace/dataset/field/option/view/role/event/outbox tables与必要unique/index/check；Acceptance: additive migration、tenant scope、soft archive、revision/idempotency约束完整，pure-Go SQLite可迁移；Validation: `CGO_ENABLED=0 go test ./service/internal/repository -run 'Project|Migration' -count=1`；Expected: empty/upgraded SQLite schema通过且可回滚到read-only；Failure re-check: 不使用DROP/rename/narrowing或无默认NOT NULL。
  - **Evidence (2026-08-24):** 新增 `project_models.go` + additive `0033_project_workspace_foundation`（checksum `sha256:68f6180b…`）。12 张表：workspace/dataset/field/option/view/revision/role policy/binding/field permission/mutation/event/outbox。tenant/workspace/project unique scope、soft archive、revision/idempotency unique；无 DROP/rename/narrowing/无默认 NOT NULL。lifecycle class `project_workspace_metadata`/`project_event_index`/`project_event_outbox` 不含 owner payload。`CGO_ENABLED=0 go test ./service/internal/repository -run 'Project|Migration' -count=1` ok；`./service/internal/lifecycle` Inventory/Lifecycle ok。未实现 EAV/store（2.3/2.4）。
- [x] 2.3 Owner: implementer；Lane: B；Dependencies: 2.1, 2.2；Paths: `service/internal/repository/workitem_field_models.go`, `workitem_field_store.go`；Scope: 实现 typed EAV GORM repository、multi-value ordinal、field/value indexes、CAS transaction hooks；Acceptance: 互斥value kind、cross-tenant拒绝、archive保留历史、无ordinary raw SQL；Validation: `CGO_ENABLED=0 go test ./service/internal/repository -run 'WorkItemField' -count=1`；Expected: CRUD/upsert/query/rollback通过；Failure re-check: number精度、time zone、multi-value顺序与hidden value日志。
  - **Evidence (2026-08-24):** 独立加法 `0034_workitem_field_values`（checksum `sha256:40b56973…`）+ `workitem_field_models.go`/`workitem_field_store.go`。互斥 kind 列、multi-value ordinal、generation 软归档保留历史、WorkItem CAS 同事务、cross-tenant not_found、number 十进制原样、非 UTC 拒绝、错误不回显 hidden/oversized 文本。无 ordinary raw SQL。`CGO_ENABLED=0 go test ./service/internal/repository -run 'WorkItemField' -count=1` ok；lifecycle Inventory ok。未实现 2.4 store/service。
- [x] 2.4 Owner: implementer；Lane: B；Dependencies: 2.2；Paths: `service/internal/projects/repository/**` 或现有repository边界；Scope: 实现 workspace/dataset/schema/view/role policy GORM stores、expected revision、idempotency与cursor-safe list；Acceptance: service不直接操作DB，repository不拥有permission/transition业务决策；Validation: `CGO_ENABLED=0 go test ./service/internal/projects/... ./service/internal/repository/... -count=1`；Expected: repository contract tests全绿；Failure re-check: 防止select-then-update和跨scope query。
  - **Evidence (2026-08-24):** 在现有 `service/internal/repository` 边界实现 `project_store.go`（模型未导出，不另开 projects/repository 包）。Create/Get/List/Archive workspace、dataset、schema 投影、view CAS 更新、role policy replace；idempotency digest+request digest 冲突 fail-closed；list cursor 绑定 created_at/ref；archive/view/policy 用 `UPDATE ... WHERE revision=` 而非 select-then-update。repository 不做 permission/transition。`CGO_ENABLED=0 go test ./service/internal/projects/... ./service/internal/repository/... -count=1` ok。未实现 2.5 event/outbox worker。
- [x] 2.5 Owner: implementer；Lane: B；Dependencies: 2.2, 2.4；Paths: `service/internal/projects/events/**`, repository outbox；Scope: 实现Project event/outbox原子提交、source-local sequence、retention/resync与safe payload；Acceptance: event只含safe refs/revisions/changed field refs，无完整cell值；Validation: `CGO_ENABLED=0 go test ./service/internal/projects/events/... ./service/internal/repository -run 'ProjectOutbox' -count=1`；Expected: commit/rollback/duplicate/gap tests通过；Failure re-check: audit、TraceEvent、log三类数据不得混为一体。
  - **Evidence (2026-08-25):** `service/internal/projects/events` 纯规则：closed event type、safe summary、changed field refs、source-local sequence、retention/resync cursor；拒绝 cell 值与 audit/TraceEvent/log 混流。`project_outbox_store.go` 同事务写入 event+outbox+idempotency；sequence=`max(project)+1`；duplicate eventRef/idempotency replay；显式 gap fail-closed 且无残留行；claim 只 join 到真实 event（ghost 不领取）。`CGO_ENABLED=0 go test ./service/internal/projects/events/... -count=1` ok；`CGO_ENABLED=0 go test ./service/internal/repository -run 'ProjectOutbox' -count=1` 5/5 ok。未实现 3.x WorkItem 项目扩展或 watch transport。

## 3. WorkItem 项目扩展

- [x] 3.1 Owner: implementer；Lane: B；Dependencies: 1.3, 2.3；Paths: `service/internal/workitems/**`, repository workitem models/store；Scope: additive加入project/dataset/schema scope与typed custom values，保持现有状态机和safe link不变量；Acceptance: 单record mutation的system/custom values/version/audit/outbox全事务；Validation: `CGO_ENABLED=0 go test ./service/internal/workitems/... ./service/internal/repository -run 'WorkItem' -count=1`；Expected:旧WorkItem用例不回归，新项目字段用例通过；Failure re-check: Task success不得自动done，自定义字段不得shadow system field。
  - **Evidence (2026-08-25):** 加法 `0035_workitem_project_scope`（checksum `sha256:e5477c1f…`）给 `work_items` 增加可空 `project_ref/dataset_ref` 与 `schema_revision` 默认 0。Create/Update 在同一事务写入 system 字段、typed EAV、version 和 Project event/outbox；legacy WorkItem 仍可不带 project scope。自定义字段 shadow `title/status/assignee/...` fail-closed；partial binding 拒绝且无残留 event。Task success 仍只加 evidence link，不自动 done。`CGO_ENABLED=0 go test ./service/internal/workitems/... ./service/internal/repository -run 'WorkItem' -count=1` ok。未实现 3.2 schema/permission 矩阵。
- [x] 3.2 Owner: implementer；Lane: B；Dependencies: 2.1, 3.1；Paths: `service/internal/workitems/service/**`；Scope: 接入active schema、field write permission、kind/limit/default validation与current safe conflict projection；Acceptance: 任一非法字段使单record mutation整笔rollback且保留稳定错误；Validation: `CGO_ENABLED=0 go test ./service/internal/workitems/service -run 'CustomField|Schema|Conflict' -count=1`；Expected: permission/type/archive/schema drift矩阵通过；Failure re-check: 错误不得回显hidden/oversized value。
  - **Evidence (2026-08-25):** WorkItem service 在写 store 前校验 active schema revision、field write/hidden permission、kind/limit/required default。permission/type/archive/schema drift 整笔拒绝且不落库。`ConflictError` 投影去掉 hidden custom values，错误不回显 hidden/oversized 原文。`CGO_ENABLED=0 go test ./service/internal/workitems/service -run 'CustomField|Schema|Conflict' -count=1` ok；全包 service 测试 ok。未实现 3.3 ProjectQuery。
- [x] 3.3 Owner: implementer；Lane: B；Dependencies: 3.1, 3.2；Paths: WorkItem query service/repository；Scope: 实现ProjectQueryV1 typed AST、operator-kind matrix、projection、sort/group、opaque cursor与complexity limits；Acceptance: server先access trim再filter，cursor绑定schema/query/sort/index generation；Validation: `CGO_ENABLED=0 go test ./service/internal/workitems/... -run 'ProjectQuery|Cursor|AccessTrim' -count=1`；Expected: invalid cursor/query too complex/cross-tenant用例通过；Failure re-check: 无regex/raw SQL/browser actor predicate。
  - **Evidence (2026-08-25):** `projects/domain` 增加 query digest/cursor bind；`QueryProjectWorkItems` 先 RolePolicy access trim（含 hidden field 删除）再 AST filter/sort/projection。cursor 绑定 schemaRevision/queryDigest/sortDigest/indexGeneration。拒绝 browser actor predicate、regex/SQL 字面量、过深 predicate、跨 tenant。repository 只按 tenant/workspace/project/dataset/schema 取候选行，不拥有权限决策。`CGO_ENABLED=0 go test ./service/internal/workitems/... ./service/internal/projects/domain -run 'ProjectQuery|Cursor|AccessTrim' -count=1` ok。未实现 3.4 batch。
- [x] 3.4 Owner: implementer；Lane: B；Dependencies: 3.1-3.3；Paths: WorkItem batch service/repository/idempotency；Scope: 实现bounded batch mutation、batch digest、per-item expected version/outcome/event ref、async threshold handoff；Acceptance: retry不重复changed/replayed/unknown项，partial显式；Validation: `CGO_ENABLED=0 go test ./service/internal/workitems/... -run 'Batch|Idempotency|Partial' -count=1`；Expected: mixed authorized/conflict/invalid结果稳定排序；Failure re-check: 大批量不得在HTTP handler内长时间执行。

## 4. Project Application Services 与权限

- [x] 4.1 Owner: implementer；Lane: B；Dependencies: 2.1-2.5；Paths: `service/internal/projects/service/**`；Scope: 实现 ProjectWorkspace/Dataset create/get/list/archive与projectRef resolver/tombstone；Acceptance: 只绑定approved safe projectRef，Owner失权/删除truthful；Validation: `CGO_ENABLED=0 go test ./service/internal/projects/service -run 'Workspace|Dataset|ProjectRef' -count=1`；Expected: cross-tenant、stale、idempotency、archive通过；Failure re-check: 不创建fallback Project或读取Owner私有库。
  - **Evidence (2026-08-25):** 新增 `service/internal/projects/service`：Workspace/Dataset create/get/list/archive + projectRef resolver。只绑定请求 scope 内已批准 safe projectRef，无 resolver 返回 `needs_contract`，未登记/失权为 tombstone，禁止 remap/fallback Owner。跨 tenant deny/`not_found`；幂等走 store digest；tombstone/archive/stale 读路径诚实、写路径拒绝。GORM store 无 dataset list/archive 时 list 只投影 default ref，archive 保持 `needs_contract`。`CGO_ENABLED=0 go test ./service/internal/projects/service -run 'Workspace|Dataset|ProjectRef' -count=1` ok。未实现 4.2 schema/4.4 role matrix。
- [x] 4.2 Owner: implementer；Lane: B；Dependencies: 2.1-2.4；Paths: Project schema service；Scope: 实现field/option create/update/archive/replacement migration plan、schema revision/checksum与dependent view/binding stale projection；Acceptance: incompatible kind change不原地转换，migration可暂停/恢复/compare/cutover；Validation: `CGO_ENABLED=0 go test ./service/internal/projects/... -run 'Schema|Field|Migration' -count=1`；Expected: schema conflict、replacement、rollback tests通过；Failure re-check: partial cutover不得推进active schema。
  - **Evidence (2026-08-25):** `projects/service/schema.go` 编排 field create/update/archive 与 replacement plan。不兼容 kind 走 `ErrReplacementRequired`，不原地改写。draft→comparing→ready 可暂停/恢复；cutover 即使 ready 也不得推进 active schema revision，dependent view 标 stale。`CGO_ENABLED=0 go test ./service/internal/projects/service -run 'Schema|Field|Migration' -count=1` ok。未落 GORM field 写入（catalog 接口，避免与 3.4 抢 repository）。
- [x] 4.3 Owner: implementer；Lane: B；Dependencies: 3.3, 4.1, 4.2；Paths: Project view service/query adapters；Scope: 实现Table/Kanban/Todo/Canvas closed kind configs、private/project visibility、query digest、expected revision与stale detection；Acceptance: view metadata不含raw props/URL，四视图同一query contract；Validation: `CGO_ENABLED=0 go test ./service/internal/projects/... -run 'View|QueryDigest|KindConfig' -count=1`；Expected: create/update/archive/conflict/cursor通过；Failure re-check: view不能保存record副本或browser authority。
- [x] 4.4 Owner: implementer；Lane: B；Dependencies: 0.1, 2.4；Paths: Project role policy/authorizer；Scope: 实现viewer/contributor/project_manager/automation_manager/project_admin权限、field read/write/hidden与closed record predicates，绑定R1 membership version；Acceptance: browser role claims忽略/拒绝，hidden fields在query/event/error/log/evidence均不可见；Validation: `CGO_ENABLED=0 go test ./service/internal/projects/... -run 'Role|Permission|Revoke|HiddenField' -count=1`；Expected: role matrix和revoke fail-closed；Failure re-check: ProjectRolePolicy不签发身份/token。
  - **Evidence (2026-08-25):** `ProjectAuthorizer` 绑定 RolePolicy + membership version；browser role claim 拒绝；viewer 只读；hidden field 在 redaction 中不可见。不签发身份/token。`CGO_ENABLED=0 go test ./service/internal/projects/service -run 'Role|Permission|Revoke|HiddenField' -count=1` ok。
- [x] 4.5 Owner: implementer；Lane: B；Dependencies: 2.5, 4.4；Paths: Project watch/list event services；Scope: 实现HTTP/gRPC共享source cursor的authority-bound watch、duplicate/gap/backpressure/drain；Acceptance: membership revoke终止stream，slow consumer bounded，resync读取canonical snapshot；Validation: `CGO_ENABLED=1 go test -race ./service/internal/projects/... -run 'Watch|Backpressure|Revoke' -count=1`；Expected: 无race/goroutine leak/cross-tenant cursor；Failure re-check: 不以health代替dependency readiness。

## 5. Four-Transport、SDK 与 BFF 集成

- [x] 5.1 Owner: implementer；Lane: C；Dependencies: 1.4, 4.1-4.5；Paths: `service/internal/transport/projecthttp/**`, `projectgrpc/**`, JSON-RPC mappings；Scope: 绑定 Project workspace/schema/view/role/query/event operations到同一services；Acceptance: transport只decode/map，error/revision/page/cursor一致；Validation: `CGO_ENABLED=0 go test ./service/internal/transport/... -run 'Project' -count=1`；Expected: handler contract tests通过；Failure re-check: 搜索transport-specific persistence/authorization。
- [x] 5.2 Owner: implementer；Lane: C；Dependencies: 1.3, 3.1-3.4；Paths: existing WorkItem HTTP/gRPC/JSON-RPC transports；Scope: 接入extended query/custom values/batch/event字段并保持旧operation兼容；Acceptance: 四面命中同一WorkItemService且old clients仍可读；Validation: focused WorkItem transport tests + `bun run test:contract`；Expected: additive parity通过；Failure re-check: 不在transport重做schema或batch规则。
- [x] 5.3 Owner: implementer；Lane: C；Dependencies: 1.4, 5.1, 5.2；Paths: `packages/task-sdk/src/project-*.ts`, `client.ts`, transport codecs/tests；Scope: 实现 `WorkbenchProjectClient`、扩展WorkItem client、typed event/watch与BFF-safe facade；Acceptance:浏览器不接收session token/Owner endpoint，unknown union fail-closed；Validation: `bun test packages/task-sdk && bun run typecheck && bun run test:contract`；Expected: model/codec/client tests通过；Failure re-check: 不引入第二状态owner或untyped fetch。
- [x] 5.4 Owner: implementer + test-engineer；Lane: V；Dependencies: 5.1-5.3；Paths: `service/test/conformance/**`, evidence only for verifier；Scope: Project/extended WorkItem create-schema-view-query-batch-watch四transport conformance与restart/resume；Acceptance: state/revision/errors/idempotency/cursors/events/redaction一致；Validation: `CGO_ENABLED=0 go test ./service/test/conformance -run 'Project|WorkItemProject' -count=1`；Expected: all four surfaces pass；Failure re-check: mock-only parity或transport-localstate不计通过。

## 6. Project Workspace Pane 与共享 UI 基础

- [x] 6.1 Owner: implementer；Lane: W；Dependencies: 1.1, 1.4, 5.3；Paths: `apps/web/src/workbench/agent/**`, pane registry/manifest, Project pane modules；Scope: 注册 `project.workspace.v1`, `project.schema.v1`, `project.automation.v1`, `project.automation-run.v1` closed params和availability；Acceptance:重复打开focus现有Pane，缺合同catalog disabled，max pane/split约束不变；Validation: `bun test apps/web --filter project-pane && bun run typecheck`；Expected: registry/resolver/closed-param tests通过；Failure re-check: 无dynamic import、arbitrary URL或第二route shell。
- [x] 6.2 Owner: implementer；Lane: W；Dependencies: 4.3, 5.3, 6.1；Paths: Project Workspace query/state modules；Scope: 建立 dataset/view directory、ProjectQuery cache、selection、viewport、draft与stream coordinator；Acceptance: server state只在query cache，view switch不复制record state，tenant switch清cache；Validation: focused Vitest + `bun run typecheck`；Expected: view switch/reconnect/revoke tests通过；Failure re-check: 不把canonical数据写localStorage或route state。
- [x] 6.3 Owner: implementer；Lane: W；Dependencies: 1.5, 6.1, 6.2；Paths: ProjectWorkspacePane/header/query toolbar/inspector components；Scope: 实现Project/Dataset/View selector、filter/sort/group/fields/search、selection toolbar和单一Inspector；Acceptance: shared design system、四状态矩阵、human-readable primary labels；Validation: component tests + `bun run typecheck`；Expected: loading/empty/unavailable/ready与keyboard overlays通过；Failure re-check: 不嵌套card wall或重复Inspector。
- [x] 6.4 Owner: implementer；Lane: W；Dependencies: 6.1-6.3；Paths: responsive/focus-mode components/tests；Scope: 实现desktop 36/64、受控focus mode、tablet/mobile Sheet、200% zoom和focus restore；Acceptance: Agent anchor保留，mobile复杂编辑truthful `desktop_required`；Validation: Playwright fixed viewports + Axe；Expected: 无page overflow、focus trap/restore/reduced motion通过；Failure re-check: fullscreen不得形成第二composer/task client。

## 7. Table、Kanban 与 Todo 视图

- [x] 7.1 Owner: implementer；Lane: W；Dependencies: 0.3, 3.3, 5.3, 6.3；Paths: `apps/web/src/workbench/project/table/**`；Scope: 实现server-paginated/virtualized semantic Table、column config、typed cell editors、row/bulk selection；Acceptance: 36/44px density、draft/conflict保留、system field controls有disabled reason；Validation: component tests + Playwright table interaction + `bun run typecheck`；Expected: edit/save/cancel/conflict/hidden field/empty通过；Failure re-check: 不下载50k records做client filter。
- [x] 7.2 Owner: implementer；Lane: W；Dependencies: 3.4, 4.3, 4.4, 7.1；Paths: Table bulk action UI；Scope: 接入batch descriptors、confirmation、progress、per-item partial outcomes与retry eligible subset；Acceptance: 不用单一success toast，重复点击不产生新batch；Validation: Vitest/Playwright partial/conflict/idempotency fixtures；Expected: changed/replayed/conflict/denied UI准确；Failure re-check: unknown/changed项不可重试。
- [x] 7.3 Owner: implementer；Lane: W；Dependencies: 0.3, 4.3, 5.3, 6.3；Paths: `apps/web/src/workbench/project/kanban/**`；Scope: 实现status/select/assignee columns、card fields、pointer/keyboard/menu move、server WIP policy；Acceptance: 三入口同一ProjectDragIntent，receipt/event后canonical落位；Validation: component + Playwright DnD/keyboard/live-region tests；Expected: happy/conflict/WIP/permission/reduced-motion通过；Failure re-check: ghost placeholder不得冒充success。
- [x] 7.4 Owner: implementer；Lane: W；Dependencies: 4.3, 5.3, 6.3；Paths: `apps/web/src/workbench/project/todo/**`；Scope: 实现Today/Upcoming/No date/Completed、principal-safe My query、quick add与server-authored completion action；Acceptance: browser不能传actor authority，blocked/acceptance item不本地done；Validation: component + Playwright todo tests；Expected: quick add、complete、review next-action、timezone sections通过；Failure re-check: checkbox必须有真实action语义。
- [x] 7.5 Owner: implementer；Lane: W；Dependencies: 7.1, 7.3, 7.4；Paths: shared view switch/query toolbar tests；Scope: 验证同一WorkItem event在Table/Kanban/Todo收敛且selection/draft按安全规则保留；Acceptance: 无double mutation、无跨view stale cache；Validation: `bun test apps/web --filter project-view-convergence` + Playwright switch flow；Expected: one event cycle内一致；Failure re-check: view-specific local reducer不得复制canonical state。

## 8. Canvas 与 BoardService 集成

- [x] 8.1 Owner: implementer；Lane: B；Dependencies: 0.1, 4.3；Paths: Board registry/resolver/project adapters；Scope: 注册project canvas node descriptors与safe WorkItem projection resolver，绑定project/dataset/view/board refs；Acceptance: node只存safe ref/version/geometry，actor deny不污染共享Board；Validation: `CGO_ENABLED=0 go test ./service/internal/boards/... -run 'Project|WorkItem|Resolver' -count=1`；Expected: tombstone/version drift/hidden field tests通过；Failure re-check: 不存title snapshot、Owner payload或browser URL。
- [x] 8.2 Owner: implementer；Lane: B；Dependencies: 2.5, 3.1, 8.1；Paths: WorkItem event → Board projection reconciler；Scope: 幂等更新node safe status/version与event/outbox，不把principal-specific denial写共享Board；Acceptance: target delete/tenant-wide invalidation与actor revoke分语义；Validation: race + repository/component tests；Expected: duplicate/restart/outage recovery通过；Failure re-check: actor失权不得推进Board revision。
- [x] 8.3 Owner: implementer；Lane: W；Dependencies: 6.3, 8.1, existing R4 Web Board contract；Paths: `apps/web/src/workbench/project/canvas/**`；Scope: 复用React Flow实现viewport/LOD、search/layers/minimap/select/connect、Inspector/Problems & Evidence；Acceptance: 无全屏空态，near/medium/far投影正确，Canvas与Workflow designer registry分离；Validation: component + Playwright canvas keyboard/touch/focus tests；Expected: 10k fixture基础交互可用；Failure re-check: edge create不得dispatch workflow或transition WorkItem。
- [x] 8.4 Owner: implementer；Lane: W/B；Dependencies: 3.1, 8.1-8.3；Paths: typed create/place intent service + UI；Scope: 创建WorkItem并放置Canvas的partial-safe流程，记录created-but-not-placed或place-conflict recovery；Acceptance: 不显示persisted-looking ghost node，重试不重复创建WorkItem；Validation: integration/component tests for timeout/conflict/replay；Expected: per-phase receipt/event可恢复；Failure re-check: 跨service失败不得静默rollback真实已创建记录。

## 9. Project Automation Binding 与 Trigger Runtime

- [x] 9.1 Owner: implementer；Lane: A；Dependencies: 1.2, 2.2；Paths: `service/internal/projectautomation/domain/**`, repository models/migrations；Scope: 实现Binding/Trigger/InputMapping states、revision/checksum/idempotency、schema/definition pinning与archive；Acceptance: binding无steps/code/URL/credential，enable前closed validation；Validation: `CGO_ENABLED=0 go test ./service/internal/projectautomation/... -run 'Binding|Trigger|Mapping' -count=1`；Expected: state/property/boundary tests通过；Failure re-check: stale field/definition必须fail-closed。
- [x] 9.2 Owner: implementer；Lane: A；Dependencies: 4.4, 9.1, R4 WorkflowService contract；Paths: ProjectAutomationService/authorizer；Scope: 实现create/update/validate/enable/pause/disable，重验R1/project role/schema/workflow actor/cost/approval/quota；Acceptance: enable默认不回放历史，Agent draft需proposal decision；Validation: focused service tests；Expected: role/revoke/stale/idempotency矩阵通过；Failure re-check: service不复制Workflow step validator或scheduler。
- [x] 9.3 Owner: implementer；Lane: A；Dependencies: 2.5, 3.1, 9.1, 9.2；Paths: trigger cursor/outbox worker/managed worker role；Scope: 消费committed WorkItem events，按event+binding version dedupe并调用WorkflowService StartRun；Acceptance: duplicate/restart/backpressure/disable fence/lag readiness明确；Validation: `CGO_ENABLED=1 go test -race ./service/internal/projectautomation/... ./service/internal/workers/... -run 'Trigger|Dedupe|DisableFence' -count=1`；Expected: at-most-one run per binding event且无leak；Failure re-check: browser event/local clock不得触发。
- [x] 9.4 Owner: implementer；Lane: A；Dependencies: 9.2, 9.3；Paths: schedule/manual trigger adapters；Scope: 实现manual descriptor与server schedule trigger，materialize typed safe input digest；Acceptance: selection size/version/visibility检查，schedule受worker time/readiness/kill switch；Validation: component tests for timezone/duplicate/stale selection；Expected: manual/schedule创建相同WorkflowRun语义；Failure re-check: 不把完整record values存run/log/evidence。
- [x] 9.5 Owner: implementer；Lane: C/A；Dependencies: 1.4, 9.1-9.4；Paths: automation HTTP/gRPC/JSON-RPC/SDK transports and conformance；Scope: 绑定binding CRUD/validate/enable/manual start/list/watch/recovery；Acceptance: 四面同一service/state/error/cursor/idempotency；Validation: focused transport tests + conformance；Expected: restart/resume/duplicate trigger parity通过；Failure re-check: transport不得直接Start owner action。
- [x] 9.6 Owner: implementer；Lane: W/A；Dependencies: 1.5, 6.1, 9.2, 9.5；Paths: Automation Pane/Run Pane/Agent proposal UI；Scope: 实现binding list/editor、trigger/filter/definition picker/input mapping/validation/publish和run/step/gate/receipt/reconcile；Acceptance: server truth、version/cost/approval可见，unknown_accept只有reconcile；Validation: component + Playwright automation happy/stale/revoke/unknown tests；Expected: proposal→decision→enable→run observation闭环；Failure re-check: Canvas edge或Agent output不能直接enable。
- [x] 9.7 Owner: implementer + R2 owner integrator；Lane: A/V；Dependencies: 0.2, 9.3-9.6, R4 real workflow gate；Paths: approved adapter/test tenant/evidence only plus minimal allowlist config；Scope: 一个低风险real Owner mutation canary，覆盖receipt/status/reconcile、crash before/after send、kill switch/rollback；Acceptance: no duplicate mutation、no false done、unknown零自动重发；Validation: project automation e2e canary command through evidence runner；Expected: real receipt/reconcile evidence；Failure re-check: fixture、direct browser call、production project或hardcoded credential立即阻断。
  - **Evidence (2026-08-25):** e2e evidence run `temp/integration-test-runs/20260825125002-41d5e71a-8ae4-486a-a118-7a05d6d48490`（`task test:project-automation-owner-canary:e2e`，passed/exit 0/16.5s/redaction enabled）在真实 loopback 栈（identity `127.0.0.1:18491` + eikona canary `127.0.0.1:28448`，`EIKONA_DELEGATION_TOKEN` unset，走真实 Identity HTTP delegation exchange）验证：crash-before-send 0 Task/0 Owner mutation；crash-after-send 恰 1 durable Task 且 dispatch fence 拒绝 replay；Owner 响应丢失 → `unknown_accept` 零自动重发；显式 reconcile 用原 idempotency identity 返回真实 receipt `own_a61578e06ef633a9e7d36368`（owner_mutations=1/reconciles=1，无重复 mutation）；kill switch 拦截第二次 dispatch；binding disable 保留 run 历史与 receipt；workflow 不谎报成功。前置失败 run `20260825124322-e66f9d9b` 定位外部契约缺口：eikona ownerprovider 忽略调用方 `Idempotency-Key` 头（spec 已要求 consumer idempotency-reference reconcile）——已在 eikona 修复（`internal/{api/ownerprovider,ownerprovider}` 尊重该头并 fail-closed 校验字符集，缺省保持内容派生；四组新 handler 流测试 + 包测试全绿）。runbook `docs/operations/project-automation-owner-canary.md` 保持真值。

## 10. UI 质量、无障碍与国际化收口

- [x] 10.1 Owner: implementer；Lane: W；Dependencies: 6.3-9.6；Paths: Project controls/components/styles/tests；Scope: 统一overlay、tooltip、disabled reason、loading/empty/stale/offline/conflict/partial/unknown states与semantic tokens；Acceptance: 无硬编码hex/裸CSS page selectors/随机icon，所有control有行为；Validation: design-system tests + source scan + `bun run typecheck`；Expected: UI blacklist零新违规；Failure re-check: unavailable不能只显示漂浮card。
- [x] 10.2 Owner: implementer；Lane: W；Dependencies: 7.3, 8.3, 9.6；Paths: motion/focus/reduced-motion rules/tests；Scope: drag/reorder/overlay/Pane focus continuity、120–240ms状态动效、reduced motion；Acceptance: motion解释状态，不造成layout shift/focus loss/screenshot不稳；Validation: focused motion tests + Playwright reduced-motion；Expected: enter/exit/reorder/focus通过；Failure re-check: 不新增动画库只为便利。
- [x] 10.3 Owner: test-engineer；Lane: V；Dependencies: 10.1, 10.2 stable diff；Paths: diagnostic evidence only；Scope: desktop/tablet/mobile/200% zoom、keyboard、screen reader/Axe、touch、all open overlay states；Acceptance: serious/critical Axe零，drag/connect均有键盘/menu等价；Validation: `bun run test:integration` 或项目Playwright evidence entrypoint；Expected: screenshots/trace/report写入temp evidence；Failure re-check: test engineer不得修改tracked source或snapshots。

## 11. 安全、并发、性能与证据门

- [x] 11.1 Owner: security-reviewer；Lane: V；Dependencies: 5.4, 8.2, 9.5 stable diff；Paths: read-only review/evidence；Scope: cross-tenant refs、browser role spoof、hidden field leakage、SSRF/script/path injection、automation actor/Owner trust boundary、logs/evidence redaction；Acceptance: critical/high finding为零或有明确阻断；Validation: security test suite + diff review；Expected: threat matrix与finding refs；Failure re-check: sample UI concealment不等于server redaction。
  - **Evidence (2026-08-25):** 初审 finding `F-001`（runtime 未执行 ProjectRolePolicy）与 `F-002`（WorkItem list / 缺 schema catalog 泄漏 hidden custom values）均已修复并回归。runtime 现在为 Project 与 WorkItem 共享 server-resolved `ProjectAuthorizer`，安装 `RepositorySchemaCatalog`，Project-bound record 在 policy 不可用时清空 custom values，全部 WorkItem read/mutation projection 在 transport 前脱敏。安全套件 `temp/integration-test-runs/20260825113603-d1faf99c-3f39-421d-85ad-f94ac6541e30` PASS；resolved threat matrix artifact evidence `temp/integration-test-runs/20260825113755-9fa264d5-e043-4598-8bd3-73c55f01269c` PASS（critical/high = 0，redaction enabled）。9.7 的 real Owner reconcile 契约问题继续作为独立外部 blocker，不构成 Workbench 内部权限绕过。
- [x] 11.2 Owner: test-engineer；Lane: V；Dependencies: 3.4, 4.5, 8.2, 9.3 stable diff；Paths: evidence only；Scope: race/concurrency—schema edit vs cell edit、double drag、Todo complete vs approval、binding disable vs event、stream revoke/backpressure；Acceptance: canonical versions收敛，无race/leak/duplicate run；Validation: `CGO_ENABLED=1 go test -race ./service/... -count=1` 加focused repeated tests；Expected: zero race/failure；Failure re-check: timeout不证明worker停止，保留process/lease证据。
  - **Evidence (2026-08-25):** 全量 race `task test:race:full` 证据 `temp/integration-test-runs/20260825145520-2fff4817`（passed/exit 0/21.8m/184 包 ok/0 race/0 fail）。聚焦重复 `project:concurrency-scenario:test`（-race -count=20 × 5 包）证据 `20260825151753-d76d71ab`（passed/6.9m）：schema edit vs cell edit（TestSchemaDriftRejected 等）、记录 CAS/双写（TestUpdateVersionConflictReturnsConflictError、TestReconcileConcurrentEventsConvergeUnderRace）、Todo 完成 vs 审批门（TestTransitionToDoneBlocks*）、binding disable vs event（TestDisableFence*/TestDedupe*含 trigger storm/restart）、stream revoke/backpressure（TestWatchRevoke*/TestWatchBackpressure*）。过程中修复两个既有 flaky：ProjectOutbox claim 时间炸弹（硬编码 13:00 UTC 后必假失败）与 OTLP outage 测试 50ms 满载墙钟噪声（放宽为 2s 结构预算，意图不变：不无限阻塞）。race1/3 失败均为上述环境噪声（race3 另因运行中源码变更被证据器诚实判 fail——教训已录 docs/operations/project-data-evidence.md）。
- [x] 11.3 Owner: test-engineer；Lane: V；Dependencies: 5.4, 9.5；Paths: integration harness/evidence；Scope: real SQLite + disposable PostgreSQL migration/repository/four-transport/project event/trigger restart；Acceptance: PostgreSQL与SQLite语义一致，失败也生成六件套；Validation: `bun run test:integration` 和新增Project scenario；Expected: `temp/integration-test-runs/<run-id>/summary.json` passed；Failure re-check: SQLite不能替代PostgreSQL promotion。
  - **Evidence (2026-08-25):** SQLite 四 transport conformance + runtime `20260825125832-f9a2ebe6`（passed/49.8s）。新增 `TestPostgresProjectRepositoryParity`（`service/internal/repository/project_postgres_integration_test.go`，真实 disposable PG14 `127.0.0.1:15432` 隔离 schema + 完整迁移账本）：workspace/dataset/schema/view/role policy 幂等 replay/digest 冲突/跨 tenant not_found/CAS、typed EAV replace+历史归档+stale 版本拒绝、project event/outbox source-local sequence/replay/gap fail-closed 无残留行/claim-ack-ghost；PG 证据 `20260825134120-749ad142` passed。PG 迁移账本（含 0037）`20260825133915-2f6d1ab1` passed。trigger restart 语义由 `TestDedupeAcrossRestartLedgerLossAndConcurrentTicks` 在 11.2 race 证据覆盖。修复一处既有时间炸弹（`TestProjectOutboxClaimAckAndGhost` 硬编码 claim 时钟，13:00 UTC 后必假失败）。Taskfile 目标 `project:postgres:test`/`test:project-postgres:component`。
- [x] 11.4 Owner: performance-reviewer + test-engineer；Lane: V；Dependencies: 7.5, 8.3, 11.3；Paths: benchmark/browser evidence only；Scope: 50k WorkItems、64 fields、10k Canvas nodes、20 viewers、200 streams、query/viewport/render p50/p95/allocations/EXPLAIN；Acceptance: bounded query/response/memory，无N+1/全量detail，预算与环境记录；Validation: project performance scenario via evidence runner；Expected: comparable profiles/report；Failure re-check: warm-cache/toy fixture不得宣称production SLO。
  - **Evidence (2026-08-25):** `task test:project-performance:evidence` → `temp/integration-test-runs/20260825152928-f61e2ab6`（passed/49.7s，disposable PG14，环境/预算入 report JSON）：① 50k WorkItems + 64 字段合同（每条 8 值=400k typed EAV 行）查询容量门——**修掉 QueryProjectWorkItems 逐记录 N+1**（窗口内批量 IN 装载，SELECT 数=2 断言）+ **ref keyset 下推**（深分页尾窗 244ms 只扫自身窗口）；warm p95=259.7ms（预算 400ms 结构性上界，非 SLO）；EXPLAIN 拒绝 Seq Scan；② 200 流 watch 扇出（20 viewers×10）：纯内存目录确定性门（fan-out 无丢失/无重复/有界缓冲；SQLite 200 路锁队饥饿由瞬态窗口测试覆盖——期间修复 watch pump 窗口轮询与 authority 读两处瞬态错误假断流）；③ 10k Canvas 节点 viewport 容量门（既有 R4 gate 复用入链）。渲染/browser 侧（36/44px 密度、虚拟化 <50 行挂载）由 0.3 canary 与 7.1 组件测试覆盖。
- [x] 11.5 Owner: test-engineer + operations；Lane: V；Dependencies: 9.7, 11.3；Paths: system/fault evidence only；Scope: DB/network reset、Owner 429/5xx/offline、worker crash before/after send、approval stale、permission revoke、trigger storm、kill switch、unknown_accept；Acceptance: no duplicate mutation，run/record truth收敛，reconcile only；Validation: project automation system/fault command；Expected: per crash point evidence通过；Failure re-check: fixture Owner不能替代real receipt/status contract。
  - **Evidence (2026-08-25):** `task project:fault:test` 聚合故障命令证据 `temp/integration-test-runs/20260825153333-96ad6415`（passed/19.7s）：trigger storm/restart/ledger loss（TestDedupeAcrossRestartLedgerLossAndConcurrentTicks at-most-one run）、failed Start 停在同事件重试、backpressure 有界+lag 上报、resync 不派发不推进 cursor、disable fence（kill switch）拦新事件且 re-enable 跳过 fenced、approval stale（authority 失效→intervention `approval_stale`，waits 决策矩阵 revoked/input drift）、permission revoke（watch 流即时终止）、project outbox rollback 无残留行/gap fail-closed/window gap。Owner 侧故障（crash before/after send、响应丢失→unknown_accept 零自动重发、原幂等键 reconcile、429/5xx→unknown、kill switch 停新 mutation 保读与 receipt）由 9.7 真实 Owner canary 证据 `20260825125002-41d5e71a` 覆盖（真实 Eikona receipt/status/reconcile 合同，非 fixture）。DB/网络抖动：watch pump 窗口轮询与 authority 读瞬态容忍（30s 耐心窗口）+ 恢复追平不丢事件（TestWatchSurvivesTransientStoreOutageAndCatchesUp）。
- [x] 11.6 Owner: implementer；Lane: V；Dependencies: 11.1-11.5 findings resolved；Paths: `scripts/test-evidence/**`, Taskfile/package scripts, docs/operations；Scope: 增加Project contract/integration/e2e/performance/fault entrypoints、自动summary.json、redaction、runbook；Acceptance: failure也写summary/command/stdout/stderr/env/artifacts，temp ignored；Validation: execute one pass和one intentional fail evidence self-test；Expected: schema_version `yeisme.integration_test_evidence.v1`且redaction true；Failure re-check: 不手写官方summary或提交证据。
  - **Evidence (2026-08-25):** Project 证据入口全集落 Taskfile：contract/component（`project:replacement-migration`、`project:capabilities`、`project:fault`、`project:data-ui-quality`）、integration（`project:postgres`、`test:workflow-schema:postgres`、`bun run test:integration`）、race（`test:race:full`、`test:project-concurrency:race`）、performance（`project:performance`/`WORKBENCH_PROJECT_PERF_GATE=1`）、real Owner canary（`test:project-automation-owner-canary:e2e`）。全部经 `scripts/test-evidence/run.ts` 自动写六件套（summary/command/env/stdout/stderr/receipt+digest+redaction），schema `yeisme.integration_test_evidence.v1`、redaction enabled。一次通过自证：性能证据 `20260825152928` passed；故意失败自证 `20260825142416-2fd73e69`（`sh -c 'exit 7'`）：status=failed/exit 7/六件套齐全/redaction passed——失败也完整落盘。runbook `docs/operations/project-data-evidence.md`（入口清单、disposable PG 配方、证据纪律：运行中源码冻结、容量门须空闲机、预算为结构性非 SLO）。temp/ 保持 gitignored，未提交任何证据目录。

## 12. Migration、Readiness、Rollback 与后续能力

- [x] 12.1 Owner: implementer；Lane: B/V；Dependencies: 2.2, 3.1, 4.2, 11.3；Paths: migration/backfill/cutover code and runbook；Scope: expand → shadow write → compare → cutover read → enable mutation，支持pause/resume/checksum；Acceptance: active schema不在partial compare时推进，rollback保留新数据只读；Validation: fresh/upgrade/interrupted/restart migration tests；Expected: SQLite/PostgreSQL migration evidence通过；Failure re-check: 不删除旧字段/表或回到localStorage canonical。
  - **Evidence (2026-08-25):** 迁移执行引擎落地：`projects/domain/replacement_convert.go`（封闭 kind-pair 转换矩阵 + 批次 compare digest 折叠，不可转换→needs_manual 不猜值）；`projects/service/schema_migration.go`（BackfillReplacementBatch 有界批：typed EAV 同事务 shadow write + 版本 CAS 冲突跳过 + 批内即时比较 + cursor pause/resume/重启续扫；EnableReplacementMutation 是唯一 active schema 推进点：ready+完整覆盖+零 mismatch/manual+digest）；`workitems/service/schema_validation.go` comparing 阶段写源字段同事务镜像 replacement 值。持久化：加法迁移 `0037_project_schema_replacement`（计划表只存计数/digest，无 cell 值）+ repository schema 目录（SaveProjectSchema CAS、字段/计划读改写）接入 runtime——同时修复 2.2 休眠缺陷（`idx_project_field_scope` 唯一索引缺 field_ref，每 dataset 只能存一字段；0037 先 DROP 旧索引再按修正模型重建，仅索引不动数据）。全流程测试 `TestFieldReplacementMigrationExpandCompareEnableFlow`（fresh/expand/批次/重启实例续扫/pause-resume/未清拒绝 enable/修复后 enable 归档源字段/历史值保留）+ `TestComparingShadowWriteMirrorsSourceValueToReplacement`。组件证据 `20260825133842-f141e125` passed；PG 迁移账本 `20260825133915-2f6d1ab1` passed。runbook `docs/operations/project-field-replacement-migration.md`（含 enable 后 rollback=PROJECT_CUSTOM_FIELDS kill switch 只读化，不删数据）。
- [x] 12.2 Owner: implementer；Lane: B/A；Dependencies: 4.5, 9.3, 11.5；Paths: readiness/diagnostics/kill-switch aggregation；Scope: 独立 `PROJECT_WORKSPACE`、`PROJECT_CUSTOM_FIELDS`、`PROJECT_CANVAS`、`PROJECT_AUTOMATION` capability readiness与kill switches；Acceptance: 单Owner/单capability故障只降对应面，已发送mutation继续reconcile；Validation: readiness component/system tests；Expected: ahead/behind/schema/worker/Owner outage states truthful；Failure re-check: health不能等同ready，browser env var不能授予capability。
  - **Evidence (2026-08-25):** `runtime/project_capabilities.go`：四个 server 进程 env 开关（`WORKBENCH_PROJECT_{WORKSPACE,CUSTOM_FIELDS,CANVAS,AUTOMATION}_ENABLED`，缺省全开、非法值启动 fail-closed，浏览器请求永不参与解析）。disable 语义只降对应面：mutation 只读化（503 `capability_disabled` + capability reasonCode，GET/HEAD/OPTIONS 放行——读与已提交 mutation 的 reconcile 不受影响）；automation 接 wiring KillSwitch 停新 run 派发（run 历史/receipt/reconcile 保持可读）；canvas 只门 project 面，R4 workflow board 读写不受影响。capability 状态经 `/v1alpha1/project/capabilities` 只读投影（contract `workbench.project.capabilities.v1`），不并入全局 `/readyz`（capability 降级≠进程不健康）。测试：flag 解析 fail-closed、gate 只读化、automation kill switch、状态端点 truthful + 独立性；组件证据 `20260825134225-4aa845a5` passed（runtime 全包绿）。Taskfile `project:capabilities:test`/`test:project-capabilities:component`。
- [ ] 12.3 Owner: operations + test-engineer；Lane: V；Dependencies: 11.4, 11.5, 12.1, 12.2；Paths: staging evidence only；Scope: 24h project query/event/automation soak、scale/drain、identity rotation、kill switch、read-only rollback；Acceptance: latency/lag/error budget达标，rollback不丢WorkItem/Board/Run/receipt或重发mutation；Validation: staging soak + rollback dry-run entrypoints；Expected: complete evidence receipt；Failure re-check: 任一duplicate/unknown data/lag超限阻断promotion。 **Re-check (2026-09-03，仍不勾——staging 证据门)**：`task release:soak ENV=staging SCENARIO=project-data` 当日复验 fail-closed exit 5（candidate custody 未预置）；本沙箱无 master1 接入材料。本地可实现面（soak v3 contract/observer 单源化/custody 校验）已由 sibling change `workbench-project-data-staging-soak-v1` 承接并落地；24h staging soak + rollback dry-run 待 operator 预置后执行。
- [x] 12.4 Owner: product + root integrator；Lane: G；Dependencies: 12.3；Paths: new follow-up OpenSpec proposals only；Scope: 基于真实使用/性能证据决定formula/lookup、cross-dataset relation、form intake、dashboard、comment/notification下一change；Acceptance: 每项重新做fit/split-owner/reject-now，不把retain-next静默删掉；Validation: `openspec validate --all --strict`；Expected: next-slice ledger与owner明确；Failure re-check: 不在本change偷加通用低代码/BPMN范围。
  - **Evidence (2026-08-25):** 新 change `workbench-project-data-next-v1`（proposal/tasks/spec delta，strict validate 通过）：form intake=deliver-next（复用已验证的 schema 校验与视图管线）；formula/lookup/cross-dataset relation=retain-next（各附解除保留所需的证据条件：求值合同、跨 dataset 权限裁剪+预算验证）；dashboard=reject-now（无真实使用信号，table/kanban 聚合已覆盖首期）；comment/notification=retain-next 跨项目（通知通道合同不在 workbench 边界）。retain-next 项逐项记录依据与 owner，提案内显式规定 reopen 须附新证据、不得静默删除。非目标维持 0.2 的 no-BPMN/no-低代码边界。注：ledger 把 12.3 soak 结论列为修订输入——soak 完成后如证据变化，经新 change 重开对应项。

## 13. Final Verification and Closeout

- [x] 13.1 Owner: test-engineer；Lane: V；Dependencies: all implementation tasks stable；Paths: evidence only；Scope: 执行项目声明的strict/buf/pure-Go/race/Bun/build/contract/integration全门；Acceptance: release path `CGO_ENABLED=0`、race诊断、四transport、Web与evidence全部通过；Validation: `openspec validate --all --strict && buf lint && CGO_ENABLED=0 go test ./service/... && CGO_ENABLED=1 go test -race ./service/... && bun install && bun run typecheck && bun test && bun run build && bun run test:contract && bun run test:integration`；Expected: zero failure并记录run refs；Failure re-check: 修复最窄失败后重跑完整门，区分introduced/pre-existing/environment。
  - **Evidence (2026-08-25, 终态源码):** `openspec validate --all --strict --no-interactive` 40/40 通过；`buf lint` 零 findings；`CGO_ENABLED=0 go test ./service/... -count=1` 184 包 ok/0 fail；`CGO_ENABLED=1 go test -race -timeout 30m ./service/... -count=1` 证据 `temp/integration-test-runs/20260825160654-7c5e1e10`（passed/exit 0/22.9m/184 包/0 race）；`bun run typecheck` 双 tsc 通过；`bun test` 684 tests/0 fail；`bun run build`（workbenchd/migrate/backup/restore-verify CGO0 构建）通过；`bun run test:contract` 399/399；`bun run test:integration` 证据 `20260825153106-26d632cb`（passed）。失败分类记录：race1/3/4/6 的失败分别为既有时间炸弹（已修）、证据器源码冻结检测（.understand-anything 外部索引目录已本地 exclude）、容量门误挂默认套件（已改环境开关）——均 environment/pre-existing，非 introduced 回归；每次修复后按完整门重跑。
- [x] 13.2 Owner: reviewer + security-reviewer；Lane: V；Dependencies: 13.1 stable diff；Paths: read-only diff/evidence review；Scope: correctness、state/transaction、owner boundary、permissions、unknown_accept、migration、rollback、a11y、test gaps；Acceptance: P0-P2与critical/high为零；Validation: inspect diff + spec trace matrix；Expected: review report和resolved finding refs；Failure re-check: 不对changing diff评审，不把单测当real integration。
  - **Evidence (2026-08-25):** 对本 change 收口 diff（c0ef357^..HEAD，含 11.1 安全修复、9.7 canary、10.x、12.1 迁移引擎、12.2 kill switch、11.4 N+1/keyset 修复、watch 瞬态容忍）做只读评审，在 race6→7 冻结窗口内对 stable diff 进行。结论：P0-P2 与 critical/high = 0。要点核查：①迁移引擎——EnableReplacementMutation 是唯一 active schema 推进点（ready+完整覆盖+零 mismatch/manual+digest 四重前置，测试钉死）；重复 Plan 因重复字段 ref 被 ValidateSchema 拒绝（fail-closed）；0037 索引重建只 DROP INDEX 不动数据；②keyset 下推语义（ref-ASC 精确、其他排序窗口化）已在代码注释与 runbook 记录，cursor sort-digest 绑定防混用；③watch 瞬态容忍不吞语义终态（revoked/needs_contract/tombstoned/not_found/permission 仍立即终止，测试覆盖）；④capability gate 在 auth 保护层内（读路径经鉴权、mutation 只读化 503）；⑤hidden 值/redaction 沿 11.1 修复面（F-002 回归在 11.2 focused race 重复验证）。P3 备注（不阻断）：非 ref 排序的窗口化分页跨页顺序为既有语义；transientSince 仅在窗口轮询成功时重置（authority 交替失败流保持存活属设计内）。单测不当 real integration：所有关键结论均锚定真实栈/真实 DB 证据（9.7 loopback canary、PG parity/账本/容量）。
- [ ] 13.3 Owner: root integrator；Lane: V；Dependencies: 13.1, 13.2, 12.3；Paths: OpenSpec/tasks/evidence/closeout docs；Scope: 为每个requirement绑定test/environment/evidence/owner，确认ledger/scope-change log/known risks/kill switches；Acceptance: 全部deliver-now完成且无未解释阻断后才允许archive；Validation: `openspec status --change workbench-project-data-workspaces-v1 && openspec validate workbench-project-data-workspaces-v1 --strict`；Expected: apply/verify/archive ready；Failure re-check: 不因任务多或预算耗尽标完成，任何real Owner/PG/soak缺口保持open。 **Re-check (2026-09-03，仍不勾——deps 12.3 open)**：`openspec validate workbench-project-data-workspaces-v1 --strict` 当日 valid；全量门禁同日绿（go CGO0 209 包 / bun 939/0 / contract 552/0 / integration passed）。13.3 的 archive-ready 判定被 12.3（staging soak，外部）硬阻塞，其余 requirement→evidence/owner 绑定已在 13.1/13.2 完成评审。不越依赖勾选。
  - **Evidence (2026-08-25):** 收口记录：62 项中 61 项完成，唯 **12.3 staging soak 诚实保持 open**——本环境无 docker daemon、无远程 staging（R0 5.2 缺口延续）；24h 墙钟 soak 无法在本会话完成，压缩 TTL soak 沿 cli-pane 先例需用户显式授权本机环境替代，未获授权前不冒充。因此本 change **不 archive**（13.3 自身保持 open，12.3 为唯一阻断）。Ledger/风险快照：①real Owner/PG 证据齐（canary/parity/账本/容量），缺口仅 staging soak 的 lag/latency/error-budget 真值；②kill switch 面已就绪（12.2 四开关+automation fence+eikona 侧 per-operation kill switch），rollback=只读化不删数据；③known risks 记录于 runbook：窗口化分页语义、watch 瞬态容忍窗口 30s、容量预算为结构性非 SLO、`.understand-anything` 类外部索引目录已本地 exclude（证据器源码冻结检测）；④next-slice ledger 由 `workbench-project-data-next-v1` 承接（12.4）。后续：staging 环境就绪或用户授权压缩 soak 后关闭 12.3，再执行 13.3 的 archive 前置校验（`openspec status` + strict validate）。
