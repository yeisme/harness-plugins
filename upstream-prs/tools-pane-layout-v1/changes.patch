--- a/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css
+++ b/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css
@@ -495,3 +495,18 @@
 .scrollBody:has([data-conversation-readonly-view]) { overflow: hidden; scrollbar-gutter: auto; }
 .scrollBody:has([data-conversation-readonly-view]) > :global([data-slot='conversation.session']) > .viewArea { flex: 1 1 0; min-height: 0; overflow: hidden; }
 .scrollBody:has([data-conversation-readonly-view]) > .composerSeat { display: none; }
+
+/* Tools own the full view slot, not the transcript reading-width axis.
+   Keep this inside the conversation root: Workbench Pane splitters are outside. */
+.body:has([data-conversation-readonly-view]) > .widthHandle {
+  display: none;
+  pointer-events: none;
+}
+
+.scrollBody:has([data-conversation-readonly-view]) > :global([data-slot='conversation.session']),
+.scrollBody:has([data-conversation-readonly-view]) .viewArea,
+.scrollBody :global([data-conversation-readonly-view]) {
+  width: 100%;
+  max-width: none;
+  min-width: 0;
+}
