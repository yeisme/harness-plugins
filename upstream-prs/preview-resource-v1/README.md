# PreviewResourceV1

Status: backlog. Owner-issued preview ref, MIME sniff, range/rendition, Abort/release.

Do not apply until a DSH PR staging worktree exists. Plugins keep using the local `@yeisme/dsh-rich-media` preview contract and capability-probe after merge.

```bash
# later:
# bash apply.sh /path/to/deepseek-harness
```
