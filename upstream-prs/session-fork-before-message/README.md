# session.forkBeforeMessage

Status: backlog. Atomic first-round edit/retry fork (`seedLength: 0`).

Conversation rewrite keeps first-round Edit/Retry retain-next until this RPC exists.

```bash
# later:
# bash apply.sh /path/to/deepseek-harness
```
