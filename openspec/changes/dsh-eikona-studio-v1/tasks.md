# 实施与验收任务

由 scripts/openspec-tasks.py 维护状态。

- [ ] 1.1 核对 Eikona consumer contract matrix 与实际 registry/handler/SDK：generation/workflow/batch/asset/lineage/handoff 逐项记录版本或 digest、源码位置、支持/缺失/未验证；遮罩/局部编辑模型支持单独成表；与 cli/eikona 双向链接。 | evidence: 重计划Native Remote @1、bundle注册与controller接线完成，固定ref/digest、strict费用/缺项/未授权结果复验，不dispatch。client47项+bundle15项通过，client typecheck/host build通过。计划UI及真实Native计划调用仍待。
- [ ] 1.2 冻结最小生成台 consumer 合同与 UI Contract；核对现有 host/client/bundle 脚手架复用；项目列表 project scope/分页/freshness 语义冻结。
- [ ] 2.1 实现项目与资产列表 adapter：分页游标、freshness、空/错/禁用诚实态；不从列表顺序推断最新采用。 | evidence: 进行中：修复下一页失败后重试误读当前页，分页组件保留失败目标cursor/history，重试同一只读请求，成功前继续展示旧页；失权或合同失效重置目标到首屏，不继续旧权限游标。2项focused组件测试及client typecheck通过。真实owner/浏览器既有证据保留；完整Typert/profile、项目选择与媒体访问仍待。
- [ ] 2.2 实现动作发现与预览：server-authored descriptor、输入类型、权限、费用状态、expected revision；缺失能力保留禁用入口并显示原因与owner任务。 | evidence: 进行中：实际 owner 准备→adapter→OwnerDirectory→实际 Cordis Gateway.snapshot→公共快照 schema 完整通过，固定ref/digest、无执行动作，正文和owner URL不进入投影；参数摘要改有界文本避免JSON转义膨胀。33项runner测试及Host typecheck通过，证据 temp/integration-test-runs/eikona-discovery-20260909024450Z-1244036/。README补可选selectedPreparation接入；浏览器配置/批准/执行仍待。
- [ ] 2.3 实现草案参数编辑：base revision、有限变更集、摘要与撤销；不原地修改 accepted artifact。 | evidence: draft版本化合同/单Host store已接CreatorStudioGateway生命周期及bundle 3个Remote。真实Cordis+DSH JSON storage-domain经Gateway保存→磁盘文件→dispose→另一会话重开读回原无效输入/未确认checkpoint，回执对账、旧revision冲突、他项目拒绝；3项存储集成（含画布回归）通过 temp/integration-test-runs/project-canvas-storage-20260909063238Z-390689/。Host typecheck和bundle15项通过。8项草稿合同/内存测试已有。客户端codec/controller/表单尚未接线，不算浏览器刷新恢复。 新增共享请求/结果schema、客户端@1 Remote codec及controller scope/id/requestId/revision和迟到检查；3项controller/8项Host测试、Host build/client typecheck通过。页面仍待接线。
- [ ] 2.4 实现确认执行与取消：固定计划、idempotency、预算/权限复核；取消收到 owner 确认才显示 cancelled，unknown 只对账。 | evidence: 批量标准adapter→真实Cordis Gateway→JSON storage/domain→Go HTTP持久恢复通过temp/integration-test-runs/eikona-discovery-20260909133026Z-3345660。unknown保留原key/targetVersion且不存values，换新key重复提交被阻止；销毁重建Host后无计划选择且mutation关闭仍列出原请求；403保留记录，对账completed后清除，一次submit两个项目run。复用现有恢复仓。浏览器ready批次Composer确认与其余取消/partial验收仍待。
- [ ] 2.5 实现候选读取与比较：显式授权正文/媒体范围、内容与控制面摘要分离；复用既有候选组件不复制状态机。 | evidence: 成员Host/Gateway后续已接Native Remote @1、bundle和controller共享schema；参数限定operation/offset/limit，拒绝project/confirmed，复验响应operation与页长度，不dispatch/刷新/自动翻页。client54项、bundle15项与两包typecheck通过，Host build通过。实际Go两页Gateway证据temp/integration-test-runs/eikona-discovery-20260909134456Z-3516928。成员列表/跨页固定身份/真实Native读取及候选审阅UI待。
- [ ] 2.6 实现采纳/写回/交接回执：分别取得 owner receipt、版本与目标 scope；资产按 ArtifactRefV1 回填画布节点。 | evidence: 进行中：原生browser Connection/registry/Gateway源码挂载Remote，WebSocket世代就绪、浏览器直发HTTP且无提交桥。发现并修复bundle遗漏recallOperationIdentity/listOperationRecoveries导致执行前404；保留身份核验。27组件+4adapter+实际owner/browser通过，证据temp/integration-test-runs/eikona-discovery-20260909000124Z-3150273/，14 bundle及Host/bundle typecheck通过。失败诊断证据保留。正式ModuleLoader/profile、原生媒体/持久恢复综合链、写回交接与画布引用仍待。 实际新生成无assessment候选经独立采用请求变accepted/version1，采用/生成回执不同、ref/digest与run数量不变；33项证据 temp/integration-test-runs/eikona-discovery-20260909094542Z-3744866/。Host/HTTP验证，浏览器新候选采用及修改交付仍待。
- [ ] 3.1 注册生成台 Pane：token/locale/键盘等价操作、独立打开与重复绑定 focus 原 Pane、dispose/HMR 无残留。
- [ ] 4.1 依赖第2/3组及5.1–5.4稳定；focused 测试全绿后运行 typecheck/test/build/check:bundles/check:plugins/check:surfaces/test:visual。 | evidence: implementation-baseline.md：共享Composer消费bundle外部require已修复；Director build/smoke/4 declaration及全局check:plugins通过，完整领域实施与全部质量门仍未完成。
- [ ] 4.2 依赖4.1和本领域required owner交付；真实完整验收：真实生成两候选→比较→基于选定结果修改→保留旧版并采用新版→固定版本引用/交接；逐能力标注fixture/real，至少一项拒绝与恢复：默认模型/不支持模型、遮罩过期、费用unknown、批量partial、重复点击、采用与交接分离；零匹配/skip不能通过。
- [ ] 4.3 保存脱敏证据并更新实际 readiness；不把协议测试当真实生成验收。
- [ ] 5.1 依赖1.1；负责领域adapter；对确认缺失动作在cli/eikona创建最小配套OpenSpec/tasks并双向链接，记录operation/交付物/受影响任务；无缺口则引用可验证合同，不重复创建API。 | evidence: 确认生产GenerationPreparation仍缺实现，现有canonical adapter+fixture不代表准备可用。cli/eikona已用openspec CLI建立eikona-dsh-generation-preparation-v1，含proposal/design/spec及10项CLI管理tasks并strict通过；owner consumer matrix与DSH baseline双向关联，写明prompt固定版本/项目/变量/typed inputs/费用批准及CLI/HTTP/SDK交付，影响1.1/1.2/2.2-2.4/5.3/4.2。复用RenderEntryVersion与Generation Intent，不在Host重建准备逻辑；edit/mask/batch要求保留。
- [ ] 5.2 依赖1.2；负责UI设计与本领域内容模块；按docs/design/dsh-eikona-studio.md完成带fixture标识的可丢弃原型，走查所有required页面/主动作/窄Pane与并排交互；原型不启用真实能力。
- [ ] 5.3 依赖2.1–2.6、5.2；负责领域UI；补齐生成配置与固定默认模型、参考图/区域/遮罩、批量候选、比较继续修改、资产lineage与固定引用；复用既有共享组件；focused组件和动作合同覆盖每个可见控件。 | evidence: 用户要求停止，并明确不满意图片渲染对比，视觉未验收；保留现有成员/比较实现，不再推进。停止汇总docs/design/dsh-creative-stop-handoff-2026-09-09.md。fixture截图及技术通过不作为用户体验完成依据，须用户明确恢复后重新评估。
- [ ] 5.4 依赖5.3；负责验证；覆盖默认模型/不支持模型、遮罩过期、费用unknown、批量partial、重复点击、采用与交接分离，双会话/项目隔离、旧版本保留、unknown零重试、禁用不删除；与画布同步按可选消费单独验收，不能阻塞独立Pane。 | evidence: 最后Native跨成员比较技术验证通过temp/integration-test-runs/eikona-discovery-20260909141116Z-3830327；确认后两次媒体读取，切换模式零追加请求，submit为1。用户明确否决当前图片渲染对比效果，视觉及完整专业路径未通过，按用户要求停止。停止记录docs/design/dsh-creative-stop-handoff-2026-09-09.md；未做完成归档。
