# 实施与验收任务

由 scripts/openspec-tasks.py 维护状态。

- [ ] 1.1 核对 Scaena Production API 与 review-package 合同：镜头/资产/声音动作、编排计划、导出、transport 逐项记录版本/digest、支持/缺失/未验证；与 agent/scaena 双向链接。 | evidence: implementation-baseline.md：合同初查完成（owner HEAD 087f7820）：Production四个只读合同+action descriptor+review-package镜头/资产/编排/导出/transport已逐项记录版本与支持状态；声音动作确认缺失（media路由未进catalog，404）。agent/scaena侧无本change反向链接（全文grep仅命中其他DSH change先例），owner侧配套与反向链接须由scaena-owner会话经5.1落地，本仓不代写，故保持未勾。
- [x] 1.2 冻结最小制作台 consumer 合同与 UI Contract；核对脚手架复用。 | evidence: implementation-baseline.md：最小consumer合同冻结（只读四合同/storyboard-packages动作+SSE游标恢复；声音动作与future inventory明确排除，未知critical版本拒绝）；UI Contract在design.md两节冻结（adopted workspace+inspector、State Matrix、Responsive、A11y，符合视觉系统§12字段）；脚手架复用核对CreatorActionComposer/artifact-workspace/SurfaceState与host gateway/validation。本切片无代码，openspec validate --strict 通过。
- [x] 2.1 实现制作项目/镜头/资产列表 adapter：project scope、分页、freshness、诚实态。（done 2026-09-14：`scaena-production-contract.ts` 冻结四读合同 wire（portfolio/cockpit/reviews/evidence-export，contract_id+schema_version literal 钉死，未知 critical 版本→contract_mismatch 拒绝）；`scaena-production-reads.ts` 本地 CLI 消费（与 `/api/v1/production/**` 同一 application 投影），project scope 显式 `--project`、portfolio/reviews 显式 offset 分页游标（坏 cursor 零 owner 调用）、owner_availability 断连→stale、empty/error/未初始化诚实态；`local-scaena-production.ts` wrapper 把 cockpit 六阶段映射进快照 production 投影（缺失阶段按 not_started，不伪造）+ 三个只读 face（readScaenaProduction/readScaenaDelivery/readScaenaPackageEvents）+ Gateway `@Remote` 三重 fence；镜头列表复用 canonical 镜头表（local-scaena-table，wire `scaena.storyboard_table_view.v1alpha1`），场景/镜头资源复用既有 local-scaena-package 快照并经既有 gateway assets face 聚合，零第二镜头表。证据：tests/scaena-production-reads.spec.ts 6 项 + tests/local-scaena-production.spec.ts 3 项；host 482/482 + typecheck 绿）
- [x] 2.2 实现动作发现/预览/确认：descriptor、权限、expected version/digest；stale 拒绝刷新不覆盖。（done 2026-09-14：`scaena-storyboard-actions.ts` `withLocalScaenaStoryboardActions` wrapper——动作门唯一取自 owner 投影 `allowed_actions`（recommend/confirm_recommendation/accept_scene/confirm_creative_contract/complete_episode；stale 场景才发布 scene_revalidate 恢复面），descriptor 按 owner 合同区分 expected package version 与 graph version 并绑定 digest 身份；dispatch 前重读投影复核 descriptor（CAS），owner `SCN_GRAPH_VERSION_CONFLICT` 等 stale 码→reconcile_required（刷新不覆盖、零重试、草稿保留），owner 版本推进→descriptor 消失→不重发；版本保护 mutation 全携 `--expected-version`+`--idempotency-key`+`--confirm`+`--actor` 固定动词数组（recommend 走 owner 无键确定性推导面）；对账走有界（≤32）内存飞行表逐字节幂等回放（owner durable replay 合同），重启后 unknown 不猜输入；投影 wire schema 抽取到 scaena-package-contract.ts 与 local-scaena-package 共享（Go nil slice→null 归一）。证据：tests/scaena-storyboard-actions.spec.ts 7 项（发现门控/版本绑定/scene-accept 参数/stale 不重发/漂移不重发/幂等回放+遗忘 unknown/值校验零调用））
- [x] 2.3 实现候选比较与采纳：复用既有候选通道；owner receipt、版本与目标 scope 分别取得。（done 2026-09-14：`scaena-visual-acceptance.ts`——候选比较复用既有候选/媒体面（canonical 镜头表 pane 选择 handoff、既有候选组件与 execute 回执的 job/candidate 事实），本模块只交付比较之后的 owner 动作：`visual-reviews`（accept/reject/request_change，request_change 只记录决定不批准重生成，仅对进入执行态的 wave 发布）与 `visual-acceptances`（`shot|digest|candidate_index|job` 线格式，digest 为 CAS 输入）；三件事分别取得并交叉校验：receipt=owner 回显 receipt_ref、版本=graph_version（expected graph version dispatch）、scope=scene_ref+selections 镜头集合（场景外镜头拒绝）；回显缺失任一→unknown 不回填。证据：tests/scaena-visual-acceptance.spec.ts 3 项（发现门控/线格式与 scope 校验/决定词表与索引边界））
- [x] 2.4 实现编排预览：固定范围、将执行项与范围外输入阻塞清单；跨领域经 creative-workflow 的 Ordo 通道不重复实现。（done 2026-09-14：`scaena-wave-preview.ts`——`deriveScaenaWavePreview` 从 wave-plan 返回的 GenerationWave（`scaena.storyboard.generation_wave.v1` literal 钉死）+当前包投影推导预览：将执行项=job plan 逐镜头（job_key/候选数/模型），范围外输入阻塞清单=未被本 wave 覆盖且缺采纳/stale/带 blocker 的场景切片逐项列出（SCN_SCENE_BOUNDARY_STALE/SCN_VISUAL_ACCEPTANCE_REQUIRED 等 owner 码），scopeFrozen 常真、绝不静默扩scope；wave_approve 绑定 exact wave ref+plan digest+expected package version（digest/版本漂移→descriptor 消失→reconcile_required），estimate unknown 必须显式 boolean 确认（--confirm-unknown-estimate 仅本 wave）；execute 仅对 approved 且 execution_state=approved 的 wave 构造命令；wave_reconcile 为 owner 恢复面（无幂等键不盲重发）；跨领域编排未实现任何内容，按设计归 dsh-creative-workflow-v1 Ordo 通道。证据：tests/scaena-wave-preview.spec.ts 5 项）
- [x] 2.5 实现导出交付消费：owner 回执与产物 ref 为准；部分成功保留已完成成果；关闭 Pane 不取消。（done 2026-09-14：`scaena-export-delivery.ts`——交付页以 owner evidence-export 投影（run/receipt/review/finding/artifact/package refs+readiness+blocking reasons）与 canonical 包投影 export 门联立，UI toast 不算交付；部分成功保留：readiness 非 ready 时已完成 run/receipt/package/artifact refs 全部保留仅缺口进 blocking reasons，修复仍是独立 owner 动作不在本页合成重试；导出执行/幂等 export-status 对账复用既有 withLocalScaenaPackage（零重复实现）；本模块零运行态——所有事实每次从 owner 重读，Pane 关闭只停止观察不取消（测试断言重开读取与 owner 一致）。证据：tests/scaena-export-delivery.spec.ts 4 项 + wrapper face 透传在 local-scaena-production.spec.ts）
- [x] 2.6 实现 review-package 固定版本消费与 transport 验证。（done 2026-09-14：`scaena-review-package-transport.ts`——固定版本三元组 {packageRef,packageVersion,graphVersion} 经 `verifyScaenaPackagePin` 每次重读校验，任一漂移→unconfirmed+观测值（该包不作为有效版本打开，失败原因显式）；观察面 `ScaenaPackageEventsClient` 消费 `storyboard package watch`：refs-only 事件行（`<seq> <kind>`，载荷即拒）、numeric cursor resume（cursor=N 只回 seq>N、next=max seq，回放违规/身份漂移→unconfirmed）；mutation transport 语义（project scope+Idempotency-Key+冲突不改状态+durable replay）由 §2.2 wrapper 的 CAS 复核、stale 映射与飞行表回放承担并有测试；投影 schema 与既有 local-scaena-package 共享单副本。证据：tests/scaena-review-package-transport.spec.ts 3 项（cursor resume/载荷与违规拒绝/pin 漂移四路））
- [ ] 3.1 注册制作台 Pane：token/locale/键盘等价、独立打开与重复绑定 focus、dispose/HMR 无残留。
- [ ] 4.1 依赖第2/3组及5.1–5.4稳定；focused 测试全绿后运行全门禁。
- [ ] 4.2 依赖4.1和本领域required owner交付；真实完整验收：创建或打开镜头→绑定图像/声音→制作→比较替换候选→保存镜头编排→审阅→合成或导出；逐能力标注fixture/real，至少一项拒绝与恢复：候选digest冲突、绑定失权、partial制作、声音时长不匹配、导出失败、unknown采用；零匹配/skip不能通过。
- [ ] 4.3 保存脱敏证据并更新实际 readiness。
- [ ] 5.1 依赖1.1；负责领域adapter；对确认缺失动作在agent/scaena创建最小配套OpenSpec/tasks并双向链接，记录operation/交付物/受影响任务；无缺口则引用可验证合同，不重复创建API。
- [ ] 5.2 依赖1.2；负责UI设计与本领域内容模块；按docs/design/dsh-scaena-production-studio.md完成带fixture标识的可丢弃原型，走查所有required页面/主动作/窄Pane与并排交互；原型不启用真实能力。
- [ ] 5.3 依赖2.1–2.6、5.2；负责领域UI；补齐项目/剧集/场景/镜头导航、结构编辑、图像/声音绑定、候选替换、镜头顺序/时长与支持的合成/导出；复用既有共享组件；focused组件和动作合同覆盖每个可见控件。
- [ ] 5.4 依赖5.3；负责验证；覆盖候选digest冲突、绑定失权、partial制作、声音时长不匹配、导出失败、unknown采用，双会话/项目隔离、旧版本保留、unknown零重试、禁用不删除；与画布同步按可选消费单独验收，不能阻塞独立Pane。
