# dsh-token-usage-panel-v1

DSH Web token 用量面板：右侧栏为主、overlay 弹窗为降级；Host 维护进程账本，DeepSeek 官方路由可查余额。

- 交付：`packages/host/dsh-token-usage` + `packages/client/ui-token-usage` + `packages/bundle/dsh-token-usage`。
- 状态：spec 就绪，待 apply。
- 完成门不含启动官方 `dsh web`。
