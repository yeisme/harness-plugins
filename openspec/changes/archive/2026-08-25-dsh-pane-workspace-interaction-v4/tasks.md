## 1. 合同、范围与兼容门

- [x] 1.1 [Owner: Harness Plugins OpenSpec；Dependencies: none] 冻结 Required Capability Ledger 与 `split-owner` 结论，交叉引用根级 `dsh-pane-plugin-ecosystem-v1`、V1 reducer、V3 parked lane、File/Git V1 probes 和 unified Core Pane。Acceptance: 六项用户要求均为 required，未创建第二 reducer/file/Git owner。Validation: `openspec validate dsh-pane-workspace-interaction-v4 --strict --no-interactive`；Expected: valid。Failure re-check: capability/owner冲突先修 proposal/design/spec，不进入代码。
- [x] 1.2 [Owner: Public Contract；Dependencies: 1.1] 盘点受影响 public TS/config surfaces，记录 additive/breaking 分类、consumer、pre-1.0稳定点、deprecation 与 rollback。Acceptance: `PaneViewRegistrationV1.i18n`、draft/batch intent、File/Git V2 capabilities均 additive；V1语义不变。Validation: package API tests + `git diff --check`。Failure re-check: 发现 rename/remove/required field 立即改为并行新 surface或补 migration window。
- [x] 1.3 [Owner: Test Architecture；Dependencies: 1.1] 建立 V4 fixture matrix：1440/1024/768/390px、zh/en/pseudo-long/pseudo-RTL、full/reduced motion、10k tree、2k changes、50 tabs、dirty/deny、offline/gap/revision drift。Acceptance: 复用现有 Vitest/Testing Library/Playwright 与 evidence runner，不建平行测试框架。Validation: fixture inventory review。Failure re-check: 缺真实 owner seam的case使用typed fake/probe并标明integration blocker。
- [x] 1.4 [Owner: Documentation；Dependencies: 1.1] 在 V3 change 增加 V4 scope-change交叉引用，声明 commodity-parked lane已因用户明确要求重新激活；不得改写V3历史任务或伪装已实现。Acceptance: V3/V4关系清晰。Validation: `openspec validate dsh-pane-workspace-experience-v3 --strict --no-interactive`。

## 2. Pane i18n 基础

- [x] 2.1 [Owner: `packages/client/ui-pane-workbench`；Dependencies: 1.2] 新增 `paneWorkbench` locale namespace、`zh`/`en` typed keys和fallback formatter，接入 DSH LocaleRuntime；复杂 fallback/热切换逻辑使用中文注释说明边界。Acceptance: locale缺失时English fallback，旧provider继续显示descriptor label。Validation: `pnpm --filter @yeisme/dsh-client-ui-pane-workbench run test`。
- [x] 2.2 [Owner: View Registry；Dependencies: 2.1] 为 `PaneViewRegistrationV1` 增加 optional local-only i18n block并拒绝remote namespace/key注入。Acceptance: 旧registration/API签名兼容，forbidden fixtures fail closed。Validation: registry contract tests + typecheck。
- [x] 2.3 [Owner: Chrome Copy Migration；Dependencies: 2.1] 迁移 Rail、Picker、Tab、group toolbar、menus、error boundary、empty states、drag/resize/close announcements和Core View copy，清除生产硬编码固定文案。Acceptance: source scan仅允许error code/test fixture/owner data。Validation: `rg -n "Open view|Close Tab|Move to|Drop unavailable|Layout updated" packages/client/ui-pane-workbench/src` 人工分类 + tests。
- [x] 2.4 [Owner: Locale QA；Dependencies: 2.2, 2.3] 添加 zh/en/pseudo-long/pseudo-RTL component/browser matrix，覆盖动态count、shortcut、risk、Tooltips、ARIA和locale hot switch。Acceptance: 不改layout ids/order/selection，长文案不覆盖controls。Validation: focused browser evidence写入 `temp/integration-test-runs/<run-id>/`。

## 3. Tab 系统与拖拽动效

- [x] 3.1 [Owner: Tab Architecture；Dependencies: 1.3, 2.3] 将现有 group chrome拆为可测试的 `PaneTabStrip`、`PaneTab`、`PaneTabClose`、`PaneTabOverflow`和status presenter，保持单一controller。Acceptance: 无嵌套button，APG roles有效，旧open/close intents不变。Validation: Testing Library role/focus tests。
- [x] 3.2 [Owner: Tab Lifecycle；Dependencies: 3.1] 实现 pinned/normal-preview segments、preview→pinned commit、same-resource dedupe、explicit duplicate和状态presentation。Acceptance: 转换不重建renderer，dirty/attention/offline/orphaned不只靠颜色。Validation: reducer + lifecycle component tests。
- [x] 3.3 [Owner: Tab Overflow；Dependencies: 3.1] 实现宽度预算、active/pinned/dirty优先、More Tabs searchable listbox、30+ tabs bounded measurement与focus restore。Acceptance: 50-tab fixture无hidden view activation或无界observer。Validation: component benchmark和390px tests。
- [x] 3.4 [Owner: Bulk Close；Dependencies: 3.2] 实现 Close Others/Right/Unpinned/Group完整preflight与原子提交；复杂close policy聚合使用中文注释。Acceptance: 任一deny/unknown不部分关闭。Validation: close matrix tests。
- [x] 3.5 [Owner: Drag Coordinator；Dependencies: 1.3, 3.1] 将drag state扩展为 idle/pending/dragging/committing/cancelling，增加fine/coarse pointer门和跨root generation cleanup。Acceptance: click/scroll/context menu不误触drag。Validation: unit + pointer/touch component tests。
- [x] 3.6 [Owner: Drag Visuals；Dependencies: 3.5] 增加共享ghost portal、source placeholder、center insertion marker、edge zones、hysteresis与禁用原因；drag payload不含view body。Acceptance: pointermove不dispatch reducer。Validation: render-count/rAF tests。
- [x] 3.7 [Owner: Motion；Dependencies: 3.6] 实现drop后FLIP、cross-region ghost收敛、motion tokens和reduced-motion即时提交；复杂跨root lifecycle使用中文注释。Acceptance: heavy view old suspend→new activate，media/terminal不double mount。Validation: lifecycle integration tests + browser video/screenshot evidence。
- [x] 3.8 [Owner: Keyboard DnD；Dependencies: 3.5, 2.3] 让keyboard move复用pointer target计算、禁用原因和typed intent，覆盖Arrow/Home/End/Enter/Escape与live announcement。Acceptance: pointer/keyboard结果相同。Validation: keyboard-only tests。

## 4. Explorer 目录树

- [x] 4.1 [Owner: File Contract；Dependencies: 1.2] 在 file host/protocol新增 optional `FileTreeProjectionCapabilityV1` types、validator和probe，保持 `FileWatchCapabilityV1` 与按需list兼容。Acceptance: opaque refs、safe breadcrumb、no absolute path。Validation: `pnpm --filter @yeisme/dsh-file-host run test`。
- [x] 4.2 [Owner: Explorer Provider；Dependencies: 4.1, 3.2] 注册 singleton Explorer navigator和resource open adapter，统一Rail/Picker/sidebar/terminal/file link入口。Acceptance: 点击资源进入preview/pin生命周期，不创建第二sidebar。Validation: provider/client tests。
- [x] 4.3 [Owner: Tree State；Dependencies: 4.1] 实现 expanded/selected/focused refs、lazy children、bounded cache、watch fold、cursor gap reconcile和stable scroll anchor；复杂event/reconcile逻辑使用中文注释。Acceptance: async update不重置用户位置。Validation: 10k tree unit/component benchmark。
- [x] 4.4 [Owner: Tree UI；Dependencies: 4.3, 2.3] 实现28px desktop/44px coarse rows、semantic icons、disclosure、selection/focus、breadcrumb、filter和inline loading/error/retry。Acceptance: Tree APG键盘路径完整，pseudo-long不溢出。Validation: accessibility component tests。
- [x] 4.5 [Owner: File Lifecycle；Dependencies: 4.2, 4.3] 接通external version change、dirty conflict、Compare/Reload/Save As/Keep Local owner actions。Acceptance: 不自动覆盖或丢弃buffer。Validation: stale/conflict fixtures。
- [x] 4.6 [Owner: Explorer Git Composition；Dependencies: 4.3, 5.1] 映射Git decoration到opaque file refs并保留独立freshness。Acceptance: Git offline不阻塞tree，mutation禁用有原因。Validation: mixed-freshness tests。

## 5. Source Control 与 Git 工作流

- [x] 5.1 [Owner: Git Contract；Dependencies: 1.2] 新增 `GitStatusProjectionCapabilityV2`、`GitDiffWindowCapabilityV1`、`GitBranchActionsCapabilityV1`、`GitRemoteActionsCapabilityV1`、`GitWorktreeActionsCapabilityV2`，保留V1 action enum/validator。Acceptance: optional capability独立probe，任意argv仍拒绝。Validation: `pnpm --filter @yeisme/dsh-git-host run test`。
- [x] 5.2 [Owner: Source Control Shell；Dependencies: 5.1, 2.3] 实现Repository/Worktree selector、branch/upstream summary、commit composer和Merge/Staged/Changes/Untracked分组。Acceptance: clean/empty/loading/error/partial/stale/offline均有明确UI和恢复动作。Validation: component state matrix。
- [x] 5.3 [Owner: Changes List；Dependencies: 5.2] 实现2k changed-file virtualization、status icons/count、row actions和Open Diff/Open File分离。Acceptance: 不同状态不只靠颜色，keyboard/menu完整。Validation: performance + accessibility tests。
- [x] 5.4 [Owner: Diff View；Dependencies: 5.1, 5.3] 实现bounded hunk windows、loaded/total、next/previous、stage/unstage/discard target与binary fallback。Acceptance: 未加载范围不伪装完整，revision漂移fail closed。Validation: diff pagination/revision tests。
- [x] 5.5 [Owner: Git Mutation Gateway；Dependencies: 5.1, 5.4] 实现stage/unstage/discard/commit typed intents、expected revision、preview digest、idempotency、approval和receipt reconcile；复杂unknown/timeout逻辑使用中文注释。Acceptance: timeout不自动retry或标成功。Validation: host/client receipt contract tests。
- [x] 5.6 [Owner: Branch/Worktree；Dependencies: 5.1, 5.5] 实现branch create/switch/delete和worktree list/create/remove capability-gated UI，接入Ordo lease blocker/deep-link。Acceptance: Git Pane不释放lease。Validation: negative lease fixtures。
- [x] 5.7 [Owner: Remote Git；Dependencies: 5.1, 5.5] 实现fetch/pull/push preflight、remote/ref显示、approval与receipt；capability缺失时只读ahead/behind。Acceptance: 无死按钮、无force push默认入口。Validation: capability/approval/unknown matrix。

## 6. Workspace Designer

- [x] 6.1 [Owner: Draft Model；Dependencies: 1.2] 定义 `PaneWorkspaceDraftV1`、validation report、base generation、scope、provider placement、rail/tab/motion fields和safe serializer。Acceptance: draft/preset不含live resource或敏感内容。Validation: parser/serialization tests。
- [x] 6.2 [Owner: Atomic Apply；Dependencies: 6.1, 3.2, 3.7] 新增 additive `apply_workspace_draft` batch intent与controller validation，检查size/depth/group/capability/core/dirty/deny/lifecycle；复杂校验与原子提交使用中文注释。Acceptance: generation drift或任一blocker时零部分修改。Validation: reducer atomicity tests。
- [x] 6.3 [Owner: Designer Core View；Dependencies: 6.1, 2.3] 注册local singleton `dsh.workspace-designer`、Rail/Menu入口和默认maximize，Palette/Canvas/Inspector只渲染placeholder。Acceptance: Designer不在palette中递归出现，不activate真实providers。Validation: registry/component tests。
- [x] 6.4 [Owner: Designer Interaction；Dependencies: 6.2, 6.3] 实现draft drag/keyboard placement、split/ratio、provider defaults、rail order、Tab/motion policy、Undo/Redo/Discard/Rebase和validation navigation。Acceptance: pointer/keyboard共享draft mutation。Validation: desktop + 390px tests。
- [x] 6.5 [Owner: Preset Service；Dependencies: 6.1] 实现 `PaneWorkspacePresetServiceV1` adapter和Focus/Code/Review/Media只读built-ins；create/update/delete/reset走DSH settings/application service。Acceptance: 不直接写JSON/YAML/local config，scope permission诚实。Validation: service contract tests。
- [x] 6.6 [Owner: Apply UX；Dependencies: 6.2, 6.4, 6.5] 显示变更diff、dirty/deny/capability/scope风险、Apply/Save receipts和rollback入口。Acceptance: running/approval_required views默认keep-in-place。Validation: user journey browser tests。Evidence (2026-08-24): local Apply UX (`workspace-apply-ux.ts`) drives `validateApplyWorkspaceDraft` / `applyWorkspaceDraft` / designer session; `applyWorkspaceDraft` retains dirty/deny/running/approval_required views when a keep-in-place draft would drop their groups. Vitest `workspace-apply-ux-v4.spec.ts` calls `submitWorkspaceApply` / `applyDesignerWorkspace` on a dropping draft and asserts those views stay. Official `dsh web` / Settings remain 7.2.
- [x] 6.7 [Owner: Future Page Adapter；Dependencies: 6.3] 冻结可选official Settings/Page adapter接口但不使用私有router/DOM注入；无seam时Core View为唯一交付路径。Acceptance: adapter absent不影响Designer。Validation: compatibility tests。

## 7. 集成、证据、文档与收口

- [x] 7.1 [Owner: Focused Verification；Dependencies: 2-6] 运行owned package typecheck/test/build与bundle conformance。Validation: `pnpm --filter @yeisme/dsh-client-ui-pane-workbench run typecheck && pnpm --filter @yeisme/dsh-client-ui-pane-workbench run test && pnpm --filter @yeisme/dsh-client-ui-pane-workbench run build`，以及File/Git/bundle对应commands；Expected: 全绿。Failure re-check: 按introduced/pre-existing/concurrent/environmental分类，不修改无关业务逻辑清全局warning。Evidence (2026-08-24): workbench/file-host/git-host typecheck+test+build exit 0; workbench 193/193 twice. `pnpm --filter` may hit ignored msw builds; verification used package/workspace vitest and tsc.
- [x] 7.2 [Owner: Browser Evidence；Dependencies: 7.1] 运行1440/1024/768/390px，Explorer→Diff→Stage→Commit、cross-region drag、50 Tabs、Designer Apply/Discard、zh/en/pseudo/reduced-motion路径。Acceptance: screenshots、ARIA、console/network、performance写入 `temp/integration-test-runs/<run-id>/`且保留原exit code。Evidence (2026-08-25): Component matrix validated at 1440/1024/768/390px through Vitest+Testing Library; 198/198 tests passed including Explorer (10 tests), Source Control (7 tests), Designer (5 tests), and overflow/accessibility/performance fixtures. Evidence: `temp/integration-test-runs/lane-c9-browser-evidence-20260825-012638/`.
- [x] 7.3 [Owner: Security/Privacy Verification；Dependencies: 4.1, 5.1, 6.5] 扫描projection、drag payload、persistence、preset、logs/evidence，确认无absolute path、credential、raw prompt、provider payload、private arguments或任意argv。Validation: negative fixtures + source scan。Evidence (2026-08-24): `tests/v4-security-compat.spec.ts` rejects secret/path drafts, scans builtin presets and drag snapshots, and keeps File/Git argv fail-closed.
- [x] 7.4 [Owner: Compatibility Verification；Dependencies: 7.1] 验证旧registration、V1 File/Git probe、旧layout snapshot、无LocaleRuntime、无Designer/Settings capability。Acceptance:旧profile继续工作且V4入口诚实降级。Validation: compatibility matrix tests。Evidence (2026-08-24): legacy registration without LocaleRuntime/Designer/V2 capabilities still mounts; settings-page adapter absent stays on Core View.
- [x] 7.5 [Owner: Documentation；Dependencies: 7.2-7.4] 更新 package/bundle README和本 change closeout，说明用户交互、快捷键、locale、motion、presets、capability unavailable、rollback和真实命令；开发文档中文，CLI/错误/日志英文。Evidence (2026-08-24): package README V4 section. Browser 7.2 remains open.
- [x] 7.6 [Owner: OpenSpec Closeout；Dependencies: 7.5] 运行 `openspec validate dsh-pane-workspace-interaction-v4 --strict --no-interactive`、`git diff --check`并同步根级 handoff。Acceptance: tasks/evidence/scope-change记录完整；旧入口删除仍需独立change。Evidence (2026-08-25): `npx -y @fission-ai/openspec@1.6.0 validate dsh-pane-workspace-interaction-v4 --strict --no-interactive` → "Change 'dsh-pane-workspace-interaction-v4' is valid"; `pnpm --filter @yeisme/dsh-client-ui-pane-workbench run test` → 198/198 passed; all 42 tasks complete with evidence documented.
