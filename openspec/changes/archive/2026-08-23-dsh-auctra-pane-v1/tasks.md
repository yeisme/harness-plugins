## 1. Owner contract dependency

- [x] 1.1 [Owner: Auctra；Scope: `cli/auctra/internal/app/pane.go`；Dependencies: none] 核对 project/unit/review 的脱敏 snapshot、负向状态与 owner action descriptor。验收：无正文全量、raw prompt、凭据；timeout 不自动 accept。验证：`go test ./internal/app -run Pane -count=1`（在 `cli/auctra` 执行）。

## 2. DSH 插件实施

- [x] 2.1 [Owner: Harness Plugins；Scope: `packages/client/ui-pane-domain/`；Dependencies: 1.1] 注册 `workspace.auctra`，复用统一 Domain Pane view、snapshot normalization、action admission 与 ArtifactIntent builder。验证：`pnpm --filter @yeisme/dsh-client-ui-pane-domain run test`。
- [x] 2.2 [Owner: Harness Plugins；Scope: `packages/bundle/pane-domain/`；Dependencies: 2.1] 将 Auctra Pane 纳入可安装 `@yeisme/dsh-pane-domain` bundle。验证：`pnpm --filter @yeisme/dsh-pane-domain run test`。
- [x] 2.3 [Owner: Harness Plugins；Scope: Auctra Host bridge；Dependencies: 1.1, 2.2] 挂载正式 `domain.auctra` owner source，消费 snapshot + push event，处理 stale revision、gap、offline，禁止 timer polling。验证：src/auctra.ts `auctraSnapshotRead`/`auctraNegativeRead` 与 cli/auctra/internal/app/pane.go 逐字对齐（reviews 优先投影、structure 回退、paneStatus/paneUnsafe/compactTitle 同源；stale_revision→stale、permission_denied、offline、timeout→unknown）；auctra.spec.ts 12 用例 + auctra-pane-integration.spec.ts 全绿——shared DomainOwnerSourceBridge 消费 snapshot + push（live push、duplicate 引用稳定、sequence gap 保安全投影、context 世代切换→reconcile、offline→重连恰好重读一次）；禁轮询由共享 bridge 的 owner-source.spec.ts fake-timer 用例锁定，auctra 模块自身无 timer；bundle pane-domain 冒烟新增 lib/host.js 经 cordis 实测 mount domain.auctra（5/5 绿）。
- [x] 2.4 [Owner: Harness Plugins；Scope: Auctra action gateway；Dependencies: 2.3] 接通 create candidate、review accept/partial、version/export 与 owner receipt；unknown/timeout 不提升 canonical text。验证：`createAuctraActionChannel` 只发布 owner PaneGatedActions 的 candidate.create/review.accept/review.partial（+export-ready 时的 export.unit，发布条件完全来自 owner pulse.exportReady，不发明导出事实）；candidate.create 只声明 unit_ref 字段、candidate 正文永不经 Pane；descriptor 带 owner 版本门（targetVersion=expectedRevision）；结论只认 owner receipt——stale receipt/unknown receipt/timeout 全部 reconcile_required，且断言 owner 投影中 review 仍为 pending（canonical 未被提升）；export.unit receipt 携带 outputArtifacts（auctraUnitArtifact 只带 owner/ref/version，mediaType text/plain）。

## 3. 验证

- [x] 3.1 [Owner: Harness Plugins；Scope: component/integration evidence；Dependencies: 2.3, 2.4] 覆盖 candidate/canonical diff、review queue、permission_denied、offline 与 scene handoff；证据写入 `temp/integration-test-runs/<run-id>/`。验证：tests/auctra-pane-integration.spec.ts 全绿（host mount + 真实 apply() 视图 + live push 入队 + gap/stale revision/offline/重连恰好一次 + candidate→accept/partial→export receipt 链 + no-canonical-promotion + scene→Eikona handoff 只带 owner/ref/version + teardown）；证据在 temp/integration-test-runs/dsh-auctra-pane-v1-20260823T083251Z-36724/（summary.json 17 场景/artifacts/脱敏 stdout/stderr/command/env）。
- [x] 3.2 [Owner: Harness Plugins；Scope: final gates；Dependencies: 3.1] 运行 `pnpm --filter @yeisme/dsh-client-ui-pane-domain run typecheck && pnpm --filter @yeisme/dsh-client-ui-pane-domain run test && pnpm --filter @yeisme/dsh-client-ui-pane-domain run build && openspec validate dsh-auctra-pane-v1 --strict --no-interactive`。验证（2026-08-23，全部 exit 0）：typecheck 无错误、test 104/104 绿（含并行 anatomia 用例）、build 成功、openspec validate 输出 valid；另 pane-domain bundle 冒烟 5/5 绿。
