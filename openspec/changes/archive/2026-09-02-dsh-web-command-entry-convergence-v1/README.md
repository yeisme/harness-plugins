# dsh-web-command-entry-convergence-v1

命令入口收敛：31 bundle 的散落入口 additive 注册进 command-first 的统一 slash+Palette 目录；旧入口保留 probe-first fallback。对应 roadmap G20；硬门依赖 = `dsh-web-command-first-interaction-v1` 冻结/归档（并行会话所有，本 change 不碰其文件）。
