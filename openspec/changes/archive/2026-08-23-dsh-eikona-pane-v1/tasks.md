## 1. Owner contract dependency

- [x] 1.1 [Owner: Eikona；Scope: `cli/eikona/internal/workspaceprojection/pane.go`；Dependencies: none] 核对 gallery/run/artifact/review 的脱敏 snapshot、负向状态与 owner action descriptor。验收：无 path、token、raw prompt、provider payload；默认模型为 `openai/gpt-5.4-image-2`。验证：`go test ./internal/workspaceprojection -run Pane -count=1`（在 `cli/eikona` 执行）。

## 2. DSH 插件实施

- [x] 2.1 [Owner: Harness Plugins；Scope: `packages/client/ui-pane-domain/`；Dependencies: 1.1] 注册 `workspace.eikona`，复用统一 Domain Pane view、snapshot normalization、action admission、ArtifactIntent builder 与 Eikona default model normalization。验证：`pnpm --filter @yeisme/dsh-client-ui-pane-domain run test`。
- [x] 2.2 [Owner: Harness Plugins；Scope: `packages/bundle/pane-domain/`；Dependencies: 2.1] 将 Eikona Pane 纳入可安装 `@yeisme/dsh-pane-domain` bundle。验证：`pnpm --filter @yeisme/dsh-pane-domain run test`。
- [x] 2.3 [Owner: Harness Plugins；Scope: Eikona Host bridge；Dependencies: 1.1, 2.2] 挂载正式 `domain.eikona` owner source，消费 snapshot + push event，处理 duplicate/gap/expired cursor/offline，禁止 timer polling。验证：src/eikona.ts `eikonaSnapshotRead`/`eikonaNegativeRead` 与 cli/eikona/internal/workspaceprojection/pane.go 逐字对齐（gallery 卡片摊平 entities、value 内嵌 canonical model_ref、unsafeRef/paneStatus/paneFreshness 同源；gap/expired_cursor→reconcile_required、permission_denied、contract_mismatch、offline 各归其位）；eikona.spec.ts 11 用例 + eikona-pane-integration.spec.ts 全绿——shared DomainOwnerSourceBridge 消费 snapshot + push（duplicate 引用稳定、sequence gap 保安全投影、expired cursor 负向、offline→重连恰好重读一次）；禁轮询由共享 bridge 的 owner-source.spec.ts fake-timer 用例锁定，eikona 模块自身无 timer；bundle pane-domain 冒烟新增 lib/host.js 经 cordis 实测 mount domain.eikona（5/5 绿）。
- [x] 2.4 [Owner: Harness Plugins；Scope: Eikona action gateway；Dependencies: 2.3] 接通 generate preview、accept/reject/export 与 owner receipt；unknown/timeout 进入 reconcile，不乐观成功。验证：`createEikonaActionChannel` 只发布 owner PaneGatedActions 的 generate.preview/review.accept/review.reject；generate 只接受 owner-held prompt 的 opaque ref（prompt_ref）与 canonical 模型选择（select 只含 openai/gpt-5.4-image-2），raw prompt 永不经 Pane；`normalizeEikonaModelRef` 仅在入射点归一化 legacy 别名（gpt-image-2/gpt-5.4-image-2→canonical），其余值原样交 owner 校验；unknown receipt/timeout → reconcile_required（requirePreviewFields cost 缺失 → approval_required 可见失败）；export/handoff 走 eikonaCardArtifact + ArtifactIntentV1（只带 owner/ref/version + idempotencyKey），eikona canonical 状态不变。

## 3. 验证

- [x] 3.1 [Owner: Harness Plugins；Scope: component/integration evidence；Dependencies: 2.3, 2.4] 覆盖 gallery/compare/generate/review/export/handoff、permission_denied、offline 与大 gallery bounded activation；证据写入 `temp/integration-test-runs/<run-id>/`。验证：tests/eikona-pane-integration.spec.ts 全绿（host mount + 真实 apply() 视图 + compare ArtifactIntent + generate→accept/reject receipt 链 + unknown/timeout 不乐观成功 + handoff→Anatomia + 大 gallery：300 卡投影 DOM 仅渲染 80 行、aria-rowcount=300、data-virtualized 有界激活 + permission_denied/contract_mismatch/offline + teardown）；证据在 temp/integration-test-runs/dsh-eikona-pane-v1-20260823T083251Z-36723/（summary.json 18 场景/artifacts/脱敏 stdout/stderr/command/env）。
- [x] 3.2 [Owner: Harness Plugins；Scope: final gates；Dependencies: 3.1] 运行 `pnpm --filter @yeisme/dsh-client-ui-pane-domain run typecheck && pnpm --filter @yeisme/dsh-client-ui-pane-domain run test && pnpm --filter @yeisme/dsh-client-ui-pane-domain run build && openspec validate dsh-eikona-pane-v1 --strict --no-interactive`。验证（2026-08-23，全部 exit 0）：typecheck 无错误、test 104/104 绿（含并行 anatomia 用例）、build 成功、openspec validate 输出 valid；另 pane-domain bundle 冒烟 5/5 绿。
