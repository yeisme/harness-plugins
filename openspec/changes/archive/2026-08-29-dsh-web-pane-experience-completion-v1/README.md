# dsh-web-pane-experience-completion-v1

让做剧与 Workbench 在 dsh web 达到安装即可用的完整 Pane 交互体验。

- 交付：Experience Tier 模型与能力矩阵投影；Tier 0 overlay 宿主完整单 region 交互（共享 tab 系统/区域内拖拽/Quick Pick/键盘路径）；AI Drama Director client 真接线（registerView + 命令面三级 probe 链 + preset 塌缩）；drama ↔ Workbench 场景回路（handoff 消费门 + 双轨 artifact handoff）。
- 不交付：AppFrame 几何/split/dock 的插件侧实现、xterm 真终端、官方 seam 合入、`dsh-ai-drama-director-pack-v1` 的 metadata CLI 缺口。
- 完成门不含官方 `dsh web`；seam 缺失路径一律 fail-closed + 标准 reason。
