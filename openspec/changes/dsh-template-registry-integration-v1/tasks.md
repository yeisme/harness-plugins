# 实施与验收任务

由 scripts/openspec-tasks.py 维护状态。

- [x] 1.1 核对 template-registry 本地 stdio MCP 工具面、promptrepo 只读 catalog adapter 与现有 `dsh-tool-hub`/catalog 包的可复用性；交付真实 seam/缺口列表；不重复已有 pane 能力。 | evidence: seam 清单文档化，含 MCP tools/list 实测摘要（脱敏）（done 2026-09-14：implementation-baseline.md 落地——真实 stdio 握手（real 级）实测 32 工具/信封/browse 记录/inspect 合同/session 视图/doctor facts，compiler `template-registry.prompt-compiler.v0.2`；promptrepo Go SDK + catalog.json 降级面、tool-hub/radar 传输/sessionProjections/scene-store 五项复用核对、七条缺口、五类既有 pane 不重复结论）
- [x] 1.2 冻结集成合同：safe projection zod schema、编译会话状态机、storage domain、UI Contract；复用 host/client/bundle 脚手架；验收 session 隔离与兼容。 | evidence: design.md 合同定稿；脚手架 typecheck 通过（done 2026-09-14：design.md 增「编译会话状态机（1.2 定稿）」与「集成合同冻结（1.2）」两节——readiness 折叠/转移表/CAS/decision_ref、`yeisme_template_registry_v1`+snake_case `compile_sessions`+按 DSH 会话隔离键、9 工具 MCP 消费集、兼容表；`packages/host/template-registry/src/index.ts` 零新依赖演进（已提交导出全部保形，provider_calls 改带默认 0），typecheck+test 18 用例通过、client typecheck 通过）
- [x] 2.1 host：实现 MCP 连接管理（主通道）与 catalog adapter 降级路径，capability probe 与断开检测；依赖 1.1；focused 单测覆盖连接成功/断开/降级；失败重检 transport 与超时。 | evidence: host 单测通过，三态覆盖（done 2026-09-14：`transport.ts` 固定 argv `mcp serve` stdio MCP（radar 传输模式：注入 spawn 工厂/无 shell/JSON-RPC id 复用/常量断开文案 `template_registry_disconnected`/掉线惰性重建）+ `service.ts` 连接管理器三态 connected/degraded/offline——capability probe=initialize 身份校验+tools/list ⊇ 九工具集（超时 abort 整连接）+断开检测（传输失败→transport_lost）+catalog.json 只读降级（`catalog.ts`，fail-closed 权限+快照 digest 陈旧标注）+probe() 复检恢复；单测 9+9 用例覆盖连接成功/中途断开重连/降级回退/恢复/超时/离线零捏造，真实二进制 smoke 见 2.3 证据）
- [x] 2.2 host：目录查询、inspect 与 preview typed RPC，rights fail-closed；依赖 2.1；验证 preview 禁用时返回原因码；失败重检 rights 判定。 | evidence: RPC 单测通过，fail-closed 用例存在（done 2026-09-14：`rpc.ts` 信封解析（content[0].text→RegistryEnvelopeSchema+raw data）+list（BrowseRequest 有界子集）/search（严格 schema 仅 query+locale，多传字段透传 owner INPUT_INVALID）/inspect（合同+i18n inputs 折叠）；rights 双层 fail-closed 派生 `deriveTemplateRights`（deny/blocked 权限与 blocked/prohibited 及未知 rightsLevel 一律无权限，preview/export 各需自身合同权限）；preview 为 inspect 派生受限 DTO（无独立预览工具），禁用原因码 not_found/permission_denied/rights_denied/degraded/contract_unavailable；12 用例含实测采样信封）
- [x] 2.3 host：编译会话 RPC（填字段/确认/编译/导出）与 storage domain 持久化；依赖 2.2；验证 `provider_calls=0`、缺字段拒绝、未确认禁编译；失败重检 contract 校验转发。 | evidence: 编译演练 evidence，`provider_calls=0`（done 2026-09-14：`sessions.ts` 全生命周期 RPC——update 字段为实测 wire 形状 {value,kind,confirmed}、confirm 必带 host 侧 decision_ref（空值上线前拒绝，绝不自批）、compile 前置 canCompile 武装门+后置 provider_calls===0 断言（非零 fail-closed 弃包）、export 只回有界回执（owner 相对 outputRef）；REVISION_CONFLICT 重读最新 revision 一次（不覆盖不重试写）；`store.ts` 绑定 `yeisme_template_registry_v1`（scene-store 模式：单 open、尾串行、行 schema put/get 双向校验、templateRegistrySessionKey 按 DSH 会话隔离）；11 单测+真实二进制端到端 smoke evidence `temp/integration-test-runs/template-registry-wave2-real-binary-2026-09-14T19-35-55-155Z-1559632/run.json`（real 级：probe connected v0.2→browse/inspect/preview→会话旅程 provider_calls=0→导出回执→not_confirmed/stale_digest 门→存储往返））
- [x] 2.4 host：session projection 注册与 stale 检测（digest 变化禁导出）；依赖 2.3；验证投影非第二份状态（投影由工具日志折叠生成）；失败重检 digest 比对。 | evidence: projection 单测通过（done 2026-09-14：`projection.ts` `templateRegistryCompile` 投影单元按 dsh-context sessionProjections 合同结构镜像（key/stateSchema/init/apply/wire{viewSchema,view}/stateVersion=1，零新依赖）——apply 折叠会话事件日志中的 template_registry_* 工具流量（tool/call 按名+参武装、tool/result 按 callId 配对解析信封，非第二份状态），Object.is 变更门控、plain JSON、有界（会话环 8/digest 图 128/挂起 32）；stale 检测=inspect 折叠记录每 exact ref 最新 digest，export gate 对 digest 漂移禁导出（digest_stale/already_exported/not_compiled/unknown_session）；13 单测覆盖全旅程折叠/门控/漂移禁导出）
- [x] 3.1 client：模板目录 pane（导航/列表/详情、过滤、状态矩阵全态）；依赖 2.2；验证 UI Contract 与 `check:surfaces`；失败重检状态矩阵空/错/禁用态。 | evidence: 组件测试 + `pnpm run check:surfaces` 通过（done 2026-09-14：`packages/client/ui-template-registry` 重写为真实 panes——navigator Surface（左 tag/capability facet 导航 + 结果 listbox + inspect 详情抽屉），过滤 query/tag/capability 双侧折叠（pane 侧即时空态），状态矩阵全态落地：loading 骨架（vk-skeleton）/ offline 错误+重试说明（probe() 显式恢复）/ 降级 catalog 快照 partial 徽标+刷新 / 无匹配空态+清除过滤 / owner 错误码 verbatim / rights fail-closed 预览禁用+稳定原因码（zh/en）；键盘路径过滤框→列表→详情（ArrowUp/Down/Home/End roving tabindex，首个 option 兜底可聚焦），details 抽屉焦点回 summary，coarse pointer ≥44px；surface catalog 升 adopted（check:surfaces 29 client 通过）；45 用例（4 spec）全绿）
- [x] 3.2 client：引导编译 pane（contract 表单、确认门、结果卡、导出）；依赖 2.4；验证键盘路径与 reduced-motion；可与 3.1 并行。 | evidence: 组件测试通过，无障碍路径覆盖（done 2026-09-14：workspace Surface 编译 pane——合同表单以 host inspect contract 为唯一事实源（i18n labels/descriptions 中英、required/min/max 长度、textarea/Input 于 ys-field、待补字段逐条列表），确认门显式 decision_ref `dsh.template-registry.confirm.v1:<session>:<revision>` 仅在用户点击时提交（门内可见、绝不自动），编译按钮 armed=confirmed+必填齐全+canCompile（host 侧 re-guard），结果卡含 exact ref/compile id/digest + provider_calls=0 声明且完成焦点移结果标题（tabIndex=-1 目标），导出受 digest-stale 门禁（checkFreshness 漂移→stale 徽标+禁用+重新固定 reset 路径，host exportSession 双重 guard），回执只显 owner 相对 outputRef；键盘全程原生控件 DOM 序，reduced-motion scoped kill switch；revision_conflict 折叠 latest 会话不覆盖不重试）
- [x] 3.3 bundle：可安装 patch 层与入口注册（probe 未过时禁用+原因）；依赖 3.1、3.2；验证 `check:bundles` 无 `@yeisme/*` require 红灯；失败重检 bundle 自包含。 | evidence: `pnpm run check:bundles` 通过（done 2026-09-14：`packages/bundle/dsh-template-registry` 实装——tsdown 双入口（node esm + browser cjs ModuleLoader banner id=包名），alias 直连源码整体内联 client/host/surface（client.js 245KB 自包含、零 @yeisme/* require、无 process 全局引用）；client 入口 re-export 浏览器 apply；`test`=check:metadata+build+smoke；check-metadata 静态验 patch 行↔包名↔exports↔banner id；smoke 覆盖 ModuleLoader 装载/install 2 views+2 commands/重复 no-op/对称 dispose/reinstall/probe-fail（host seam 缺失→probe-only face 携带 needs_template_registry 原因+零注册）；check:bundles 29/29、check:plugins 全绿）
- [x] 4.1 无 CLI 场景验收：未安装 template-registry CLI 的 profile 下，pane 经 MCP `tools/list`/`inputSchema` 取得参数与恢复合同，重开后从投影恢复编译会话；依赖 3.3；失败重检恢复字段完整性。 | evidence: temp/integration-test-runs/<run-id>/ 脱敏证据（done 2026-09-14：驱动脚本 `packages/host/template-registry/scripts/smoke-no-cli-recovery.mjs`（package script `smoke:no-cli-recovery`）21 项检查全过，证据 `temp/integration-test-runs/template-registry-no-cli-recovery-2026-09-14T20-45-45-626Z-1932775/summary.json`——connected 相（real：发布二进制、固定 argv `mcp serve`）以真实 client compile controller 走 pin→填字段→显式确认→compile provider_calls=0，spawn seam 抽取真实 wire 流量折叠进 `templateRegistryCompile` 投影并落 `compile_sessions` 行；reopen 相（PATH 滤除一切可解析二进制的目录=无 CLI profile）探针 connect_failed 折叠 degraded+catalog 只读，pane 重开自投影 wire view+存储行恢复（字段/确认/digest/contractDigest/decisionRef/compileId/provider_calls 完整、跨 DSH 会话隔离），导出门未漂移 allowed、漂移 digest_stale、无 CLI 导出 host guard degraded 拒绝；零跳过守卫（缺任一检查即 failed）。旅程暴露并修复两处真实集成缺口：①owner 字段 wire 键为 `<step-id>.<输入名>`（`FIELD_INVALID`）——host 新增 `toWireFieldKey`/`fromWireFieldKey` 翻译+service 学习记忆 stepIds（`TemplateSessionSchema.stepIds` 可选默认空，pane/投影保持合同名规范形）；②`decision_ref` owner 字符集拒绝冒号（`USER_DECISION_REF_REQUIRED`）——client 方案分隔符改 `.`（`dsh.template-registry.confirm.v1.<session>.<revision>`）；回归：host 145 用例（含翻译/记忆/投影剥前缀新例）、client 45 用例、fixture 镜像 owner FIELD_INVALID 规则、wave2 `smoke:real-binary` 复跑 passed（`template-registry-wave2-real-binary-2026-09-14T20-42-10-600Z-1917802`））
- [x] 4.2 文档：插件 README、docs 集成页（目录/编译/降级/恢复与权限边界）；依赖 3.3；验证文档命令真实可运行；失败重检命令与输出契约。 | evidence: 文档合入，命令经实际执行核对（done 2026-09-14：`packages/bundle/dsh-template-registry/README.md` 重写为 结构/目录/编译/降级/恢复/权限边界/安装/验证 各节（含 4.1 两处 wire 契约注记），client README decision_ref 方案同步 dot 分隔；新增 docs 集成页 `docs/design/dsh-template-registry-panes.md`（目录/编译/降级三态/无 CLI 恢复链/权限边界/验证命令+旧二进制 catalog 隔离坑），`docs/README.md` 入口行补链；全部文档命令本会话实际执行并核对输出：host test 145/145（12 files）、client test 45/45（4 files）、bundle test `BUNDLE SMOKE: PASS`（client.js 245.99 kB 自包含）、`check:bundles` 29/29 PASS、`check:surfaces` 29 client/8 bundle 通过、`smoke:real-binary` passed、`smoke:no-cli-recovery` 21/21 ok；doc-sync 双语结构门 6/6 通过）
- [ ] 5.1 后续衔接（不阻塞本 change 关闭）：recipe 运行器、画布节点、垂直工作台模板驱动改造各立独立 change；3D 内容侧依赖 prompt-templates `official-3d-model-templates-beta-v1` 实施完成。
