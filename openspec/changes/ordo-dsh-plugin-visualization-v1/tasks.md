## 1. DSH 设计与依赖评审

- [x] 1.1 Owner: `client/deepseek-harness` maintainer；Lane: design；Dependencies: none；Scope: 核对 Ordo headless capacity、runtime/run/session/worktree 命令，DSH Cordis/client/bundle/ToolView seams，Workbench Harness Studio 现有 contract；Acceptance: 记录 `split-owner`，明确 Ordo/DSH/Workbench/Harness Plugins 边界；Validation: `openspec validate ordo-dsh-plugin-visualization-v1 --strict`；Expected: DSH change 可验证；Failure re-check: 若 owner 或公开扩展缝不明确，停止实现并回到 owner routing。
- [x] 1.2 Owner: `client/deepseek-harness` maintainer；Lane: design；Dependencies: 1.1；Scope: 完成 proposal、design、三项 capability specs、能力账本、状态机、双端线框和分期；Acceptance: required capability 未被静默删除，deliver-now/retain-next 明确；Validation: `openspec validate ordo-dsh-plugin-visualization-v1 --strict`；Expected: 3 个 capability 与 3 个 spec 目录一一对应；Failure re-check: capability/spec 名称或 requirement/scenario 不一致时修正文档。
- [x] 1.3 Owner: `client/deepseek-harness` maintainer；Lane: dependency-review；Dependencies: 1.2；Scope: 对照 `enterprise-harness-platform-v1` 的 action/receipt、tenant isolation、plugin supply chain 和 DSH/Workbench 互补边界；Acceptance: 当前 DSH change 只消费外部 owner contract，不声明 Ordo/Workbench canonical state；Validation: `openspec validate ordo-dsh-plugin-visualization-v1 --strict`；Expected: exit 0；Failure re-check: 发现 owner 越界时改回 handoff，不在 DSH 增加第二状态源。

## 2. Ordo 外部 owner handoff

- [x] 2.1 Owner: `client/deepseek-harness` maintainer；Lane: ordo-handoff；Dependencies: 1.3；Scope: 记录 Ordo 提供 snapshot/event/action/read service 的依赖字段和版本摘要；Exclusions: 不在 DSH 实现 Ordo read model、scheduler、reservation 或 lease authority；Acceptance: DSH host/client 只依赖 typed safe projection 和 owner receipt；Validation: `openspec validate ordo-dsh-plugin-visualization-v1 --strict`；Expected: DSH change 不新增 Ordo canonical code；Failure re-check: 缺字段回到 Ordo owner，不在 DSH 本地推导。Evidence: `docs/cookbook/adding-ordo-agent-ops-plugin.md` §8 的 Ordo read/action ledger 固定 `snapshot.v1alpha1`、`event.v1alpha1`、`harness.action.v1alpha1`、`harness.receipt.v1alpha1` 的安全字段、exact context、digest、unknown/reconcile 和 fail-closed 规则；未实现或缺字段仍回到 owner handoff。
- [x] 2.2 Owner: `client/deepseek-harness` maintainer；Lane: ordo-conformance；Dependencies: 2.1；Scope: 为 DSH adapter 准备 snapshot/event/unknown/reconcile/redaction conformance fixtures 的消费约定；Exclusions: fixture producer 归 Ordo owner；Acceptance: duplicate、gap、late result、unknown liveness 和 stale context 都有消费测试入口；Validation: `node_modules/.bin/tsx scripts/verify-md-links.ts`；Expected: 文档和 fixture 引用可解析；Failure re-check: fixture 需要 raw prompt、provider payload 或 host path 时拒绝并回到 safe projection。

## 3. DSH host、client 与 bundle 实现

- [x] 3.1 Owner: `client/deepseek-harness` maintainer；Lane: dsh-spec；Dependencies: 1.3, 2.1；Scope: 在当前 change 中冻结 host service/client UI/bundle 路径与 Cordis keys；Exclusions: 不修改 DSH core、不做同进程多 tenant；Acceptance: `dsh.client`、bundle/profile、ToolView、dispose/HMR 和 isolated runtime 均有 specs；Validation: `openspec validate ordo-dsh-plugin-visualization-v1 --strict`；Expected: exit 0；Failure re-check: 若需导入私有 store 或 core patch，重新选择公开 seam。
- [x] 3.2a Owner: `client/deepseek-harness` implementer；Lane: dsh-host-local-slice；Dependencies: 3.1；Scope: 实现只读 `ordoAgentOps/snapshot` Host Remote、可选 owner source 与 `needs_contract` fallback；Exclusions: 不实现 Ordo read model、event cursor、scheduler、reservation、lease 或 action；Acceptance: owner source 缺失时 fail closed，安全消息不含 URL、Bearer、token 或 host path；Validation: `CI=true pnpm exec tsc -p packages/host/ordo-agent-ops/tsconfig.json --noEmit` 与 `CI=true pnpm exec vitest run packages/host/ordo-agent-ops/tests/gateway.spec.ts`；Expected: 1 文件/3 tests 通过；Failure re-check: 不得把 fallback projection 写成真实 Ordo 状态。
- [x] 3.3a Owner: `client/deepseek-harness` implementer；Lane: dsh-ui-local-slice；Dependencies: 3.2a；Scope: 实现 compact sidebar consumer、single-flight、generation reset/dispose 与 `needs_contract` browser rendering；Exclusions: 不实现 approval/reconcile drawer、ToolView、Workbench deep-link authorization 或 mutation；Acceptance: late result 不回写，browser 只看到 safe projection，Workbench action 保持禁用；Validation: `CI=true pnpm exec tsc -p packages/client/ui-ordo-agent-ops/tsconfig.json --noEmit`、focused Vitest 与 `CI=true pnpm run build:lib:client`；Expected: focused tests、typecheck、client build exit 0；Failure re-check: 不从本地 loading/error 推导 terminal run 状态。
- [x] 3.2b Owner: `client/deepseek-harness` implementer；Lane: dsh-host-validation-local-slice；Dependencies: 3.2a；Scope: 在 Host Remote 边界对 owner snapshot 执行严格 schema、opaque ref、safe text、context/count 与 unknown-field 校验；无效或敏感 projection 统一降级为 `contract_mismatch`，owner 读取异常降级为 `offline`；Exclusions: 不实现 Ordo read model、event cursor、scheduler、reservation、lease 或 action；Acceptance: Host 不直接透传未验证 owner payload，负向测试覆盖敏感 ref、unknown field 与 owner exception；Validation: `CI=true pnpm exec tsc -p packages/host/ordo-agent-ops/tsconfig.json --noEmit` 与 `CI=true pnpm exec vitest run packages/host/ordo-agent-ops/tests/gateway.spec.ts`；Expected: focused typecheck 与 5 tests 通过；Failure re-check: 不把 schema 校验当作 Ordo owner/provider/deployment 证据。
- [x] 3.2c Owner: `client/deepseek-harness` implementer；Lane: dsh-host-context-containment-local-slice；Dependencies: 3.2b；Scope: 捕获并冻结 server-injected expected context，只在 ready/stale owner snapshot 的完整 context 与该绑定精确一致时暴露 facts；Exclusions: 不实现 AccessTicketBinding 组合、ticket claims propagation、generation cache、Ordo read model、scheduler、reservation、lease 或 action；Acceptance: expected context 缺失或非法返回 `needs_contract`，owner context 缺失、漂移、非法或非 ready/stale state 携带 facts 返回不含 run/capacity 的 `contract_mismatch`；Validation: `CI=true pnpm exec tsc -p packages/host/ordo-agent-ops/tsconfig.json --noEmit` 与 `CI=true pnpm exec vitest run packages/host/ordo-agent-ops/tests/gateway.spec.ts`；Expected: 10 tests 通过；Failure re-check: 不从 ticket 或 browser 参数推导 context，也不将 focused/local 结果表述为 provider/deployment/production 证据。
- [ ] 3.2 Owner: `client/deepseek-harness` implementer；Lane: dsh-host；Dependencies: 3.1, 2.2；Scope: 实现 Cordis host plugin、typed Ordo service/event、cursor 生命周期与 bounded cache；Acceptance: browser 无 token/base URL、tenant/runtime switch teardown、contract drift fail closed；Validation: `pnpm run typecheck && pnpm run test`；Expected: focused tests 与现有 gates exit 0；Failure re-check: complex subscription/state/error/protocol conversion 必须补简体中文注释，secret/path redaction 负向测试必须存在。Evidence (2026-08-18): unified bundle 新增 `ordo.agent_ops.event.v1alpha1` safe schema、`OrdoAgentOpsEventCursor` 与 6 个 focused tests，覆盖 anchor、duplicate、gap/reconcile、stream/context/membership/plugin/contract/runtime drift、entity-version regression 和 bounded history；真实 Cordis event source、subscription lifecycle、transport backoff 与 DSH profile/browser evidence 仍未接入，因此任务保持 open。（rebaselined 2026-08-29: retired `client/deepseek-harness` owner。分类 **external / retain-next**——本地已交付物由 harness-plugins 持有：`ordo.agent_ops.event.v1alpha1` safe schema、`OrdoAgentOpsEventCursor` 与 6 个 focused tests（supersede 本任务本地面）；剩余真实 Cordis event source、subscription lifecycle、transport backoff 依赖外部 Ordo owner 事件源，DSH profile/browser evidence 依赖官方 profile 通道，均入停车区，不派工。DAG 解锁条件：Ordo owner 事件源 + 官方 profile/browser evidence 到岗。 Recheck 2026-09-03: `[external-gate skipped]` 维持——Ordo owner 事件源与官方 profile/browser evidence 均未到岗（官方 npm @deepseek-ai/dsh-* 至 0.1.2-rc.1 无对应事件/投影 remote 面；本仓 focused 交付物由 unified bundle 持有不变）。） Recheck 2026-09-04: `[external-gate skipped]` 维持——Ordo owner 事件源与官方 profile/browser evidence 均未到岗（`@deepseek-ai` 至 0.1.2-rc.1 无对应 remote 事件/投影面，上游 HEAD `76fda729` 零新增）。 Recheck 2026-09-06: `[external-gate skipped]` 维持——Ordo owner 事件源与官方 profile/browser evidence 通道未到岗（上游 0.1.3-alpha.1 全树无对应 remote 事件/投影面）；ToolView/Workbench 深链 seam 仍缺；Workbench parity fixtures 仍由 owner 提供；strict validate 复验 exit 0。 Recheck 2026-09-07: `[external-gate skipped]` 相关外部门维持——上游无新 seam 发布（npm next 0.1.2-rc.1；alpha 0.1.3-alpha.2 五包 tarball 无 AgentOps 新面）。 Recheck 2026-09-09: 部分推进——本地面已由统一包全覆盖并复验（`pnpm --filter @yeisme/dsh-ordo-agent-ops run test` 60/60、typecheck exit 0：Cordis host plugin 引用计数 mount/teardown、typed `snapshot`/`decide` Remote、`ordo.agent_ops.event.v1alpha1` schema + `OrdoAgentOpsEventCursor` gap/drift/entity-regression/有界历史、contract drift fail-closed、browser 无 token/base URL 且 secret/path 负向测试在册、复杂状态机均带简体中文注释）；subscription lifecycle 亦已由本仓 `packages/client/ui-pane-domain` owner-source bridge 交付（push 订阅、断线 offline 不重试、恢复恰好一次权威重读、dispose 逆序释放，supersession 记录）；官方 profile 装载证据已到岗（2026-09-03 冒烟 `temp/integration-test-runs/dsh-ordo-official-cli-smoke-20260903T031410Z-47858/`：官方 CLI `plugin add` + `--dump-config` 单行 + web boot）。剩余外部门维持：真实 Ordo owner 事件源、transport backoff 与浏览器 event remote——上游 alpha 前移 0.1.5-alpha.1 全树相关 seam 0 命中（见 0d93016 复核）、npm next 仍 0.1.2-rc.1，保持 open。
- [ ] 3.3 Owner: `client/deepseek-harness` implementer；Lane: dsh-ui；Dependencies: 3.2；Scope: 实现 compact Agent Ops panel、attention badge、approval/reconcile drawer、ToolView 与 Workbench deep link；Acceptance: 与 Workbench semantic parity，键盘/a11y、stale/offline/contract_mismatch 完整；Validation: `pnpm run typecheck && pnpm run test && pnpm run build`；Expected: exit 0，bundle 可由 profile 安装/卸载；Failure re-check: UI 不得根据本地状态合成 terminal 或 restart action。 （rebaselined 2026-08-29: retired `client/deepseek-harness` owner。分类 **superseded(local) + external(remainder)**——compact sidebar/panel 本地面已交付并有 21/21 focused tests（supersede）；剩余 ToolView 注册与 Workbench deep link 属 Workbench/DSH 官方 owner seam，入停车区。DAG 解锁条件：官方 ToolView/Workbench slot。 Recheck 2026-09-03: `[external-gate skipped]` 维持——官方 npm 面无 ToolView/Workbench 深链 seam；注：官方 dsh 0.1.1-rc.2 内置 web app 已带 `shell.workspace.*` 官方 workspace slot（见 dsh-workbench-compose-v1 6.3 证据），Workbench deep-link/re-auth 面仍缺。） Recheck 2026-09-04: `[external-gate skipped]` 维持——官方 npm 面仍无 ToolView/Workbench 深链 seam（上游树 ToolView 仅 2026-07-23 toolview-dissolution 遗留 generic toolviews）。 Recheck 2026-09-06: `[external-gate skipped]` 维持——Ordo owner 事件源与官方 profile/browser evidence 通道未到岗（上游 0.1.3-alpha.1 全树无对应 remote 事件/投影面）；ToolView/Workbench 深链 seam 仍缺；Workbench parity fixtures 仍由 owner 提供；strict validate 复验 exit 0。 Recheck 2026-09-07: `[external-gate skipped]` 相关外部门维持——上游无新 seam 发布（npm next 0.1.2-rc.1；alpha 0.1.3-alpha.2 五包 tarball 无 AgentOps 新面）。 Recheck 2026-09-09: 部分推进——compact sidebar 面板（官方 `sidebar.footer.action` slot）、attention badge、approval/reconcile drawer（仅渲染 server-authored descriptor、过期即禁用）、六态渲染与键盘/a11y（popup 键盘模型+screen-reader 公告+reduced motion、Escape 焦点回归、图边等价语义文本行）均已交付并复验（统一包 60/60，其中 sidebar-offline 2/2、toolview 2/2、popup 4/4）；Workbench deep link 对照端 `client/yeisme-workbench` 已于 2026-09-07 退役（`docs/migrations/workbench-retirement/`）——面板保留诚实禁用的 Open in Studio 按钮，后继为 DSH 内 typed Agents deep-link（`ordoSubagentDeepLink`，canonical run 不被改写，证据 run `dsh-ordo-agent-team-pane-v1-20260909T025208Z-1609431`）；ToolView 已交付 fail-closed 纯展示组件但官方 `tool.call.toolview` 注册 seam 上游 0.1.5-alpha.1 仍 0 命中；「与 Workbench semantic parity」验收因对照端退役不可再证，语义单源冻结于 cookbook §8 与统一 schema。外部 seam 与退役对照端未决前保持 open。
- [ ] 3.4 Owner: `client/deepseek-harness` test engineer；Lane: dsh-verify；Dependencies: 3.3；Scope: 验证 profile dump、install/uninstall/reload、cursor gap、tenant runtime isolation、ToolView redaction 与 browser a11y；Acceptance: integration evidence 写入 DSH `temp/integration-test-runs/<run-id>/`；Validation: `pnpm run test && pnpm run build`；Expected: exit 0；Failure re-check: timeout 不等于 worker stopped，失败不得触发重复 writer。 （rebaselined 2026-08-29: retired owner。分类 **external**——profile dump/install/uninstall/reload、tenant isolation、ToolView redaction 与 browser a11y 证据依赖官方 profile/browser 通道，入停车区；本地可跑的安装/卸载冒烟已由 unified bundle conformance 覆盖。DAG 解锁条件：官方 profile/browser evidence。 Recheck 2026-09-03: `[external-gate skipped]` 维持——profile dump/install/reload 与 browser a11y 证据通道未到岗。） Recheck 2026-09-04: `[external-gate skipped]` 维持——官方 profile/browser evidence 通道未到岗。 Recheck 2026-09-06: `[external-gate skipped]` 维持——Ordo owner 事件源与官方 profile/browser evidence 通道未到岗（上游 0.1.3-alpha.1 全树无对应 remote 事件/投影面）；ToolView/Workbench 深链 seam 仍缺；Workbench parity fixtures 仍由 owner 提供；strict validate 复验 exit 0。 Recheck 2026-09-07: `[external-gate skipped]` 相关外部门维持——上游无新 seam 发布（npm next 0.1.2-rc.1；alpha 0.1.3-alpha.2 五包 tarball 无 AgentOps 新面）。 Recheck 2026-09-09: 重大推进——官方 CLI profile lifecycle 证据本轮新产：`temp/integration-test-runs/dsh-ordo-profile-lifecycle-20260909T045142Z-3334489/`（dsh 0.1.2-rc.1 隔离 DSH_HOME：install `+`/uninstall `-`/reinstall 全 exit 0，`--dump-config` 装载后含 ordo-agent-ops contribution、卸载后 0 行、重装后恢复——profile dump/install/uninstall/reload(config 级) 四项齐备；叠加 2026-09-03 web boot 冒烟）；cursor gap 与 ToolView redaction 有 focused 复验（event-cursor 6/6、toolview 2/2，统一包 60/60；fork 退役后实施方证据依现惯例落本仓 temp/integration-test-runs/）。仍缺：browser a11y 真浏览器证据（现仅组件级键盘/ARIA 模型测试）、in-session 热重载证据（需官方 web 会话）、真实 tenant runtime isolation 切换（控制面对照端已退役，仅 host context-drift fail-closed 测试在册）。保持 open。

## 4. Workbench 外部 owner handoff

- [x] 4.1 Owner: `client/deepseek-harness` maintainer；Lane: workbench-handoff；Dependencies: 3.1；Scope: 记录 Workbench 需要的 Ordo projection refs、context revision、deep-link re-auth 和 action parity；Exclusions: 不在 DSH 实现 Workbench Studio、Canvas canonical state 或多租户导航；Acceptance: DSH 面板只提供 compact summary 和安全 deep link；Validation: `node_modules/.bin/tsx scripts/verify-md-links.ts`；Expected: handoff 文档链接可解析；Failure re-check: deep link 被当作 authorization 时回到 DSH host/BFF 重新鉴权。Evidence: `docs/cookbook/adding-ordo-agent-ops-plugin.md` §8 明确 opaque refs/versions/freshness/reason/evidence、`Open in Studio` 仅作 navigation hint、Workbench 重新鉴权 exact context/resource，以及双方 action/receipt parity；Studio 与 Canvas 仍属外部 owner。
- [ ] 4.2 Owner: `client/deepseek-harness` maintainer；Lane: parity；Dependencies: 4.1, 3.3；Scope: 用共享 fixtures 对比 DSH 与 Workbench 的 status、reason、freshness、permission、approval、receipt 语义；Acceptance: 布局可以不同，动作资格和 owner refs 不得不同；Validation: `pnpm run test`；Expected: parity test exit 0；Failure re-check: UI 本地推导 terminal 或 action 时阻止交付。 （rebaselined 2026-08-29: retired owner。分类 **external**——parity fixtures 由 Workbench owner 提供（cookbook §8 已固定消费约定），本仓不单方面产出。DAG 解锁条件：Workbench parity fixtures 到岗。 Recheck 2026-09-03: `[external-gate skipped]` 维持——Workbench owner parity fixtures 未提供（cookbook §8 消费约定不变）。） Recheck 2026-09-04: `[external-gate skipped]` 维持——Workbench owner parity fixtures 未提供（cookbook §8 约定不变）。 Recheck 2026-09-06: `[external-gate skipped]` 维持——Ordo owner 事件源与官方 profile/browser evidence 通道未到岗（上游 0.1.3-alpha.1 全树无对应 remote 事件/投影面）；ToolView/Workbench 深链 seam 仍缺；Workbench parity fixtures 仍由 owner 提供；strict validate 复验 exit 0。 Recheck 2026-09-07: `[external-gate skipped]` 相关外部门维持——上游无新 seam 发布（npm next 0.1.2-rc.1；alpha 0.1.3-alpha.2 五包 tarball 无 AgentOps 新面）。 Recheck 2026-09-09: 对照端 `client/yeisme-workbench` 已于 2026-09-07 退役（`docs/migrations/workbench-retirement/`：Ordo 消费按 reuse-owner 归回 Ordo Agent Ops 单源，不迁 TaskService/审批账本）。parity fixtures 从未由 Workbench owner 提供（cookbook §8 消费约定仍冻结）；对照端退役后共享 fixture 对比既无生产方也无消费方，本任务只可在 closeout 按 superseded-by-retirement 处置、或以归档语义（workbench-ordo-managed-work-v1 通用 adapter/BFF 面与未交付的 `ordo.workbench.team_control.v1alpha1`）desk-check 记录，无法以共享 fixtures 勾选。如实保持 open。

## 5. Harness Plugins 与 Control Plane 外部 handoff

- [x] 5.1 Owner: `client/deepseek-harness` maintainer；Lane: plugin-pack-handoff；Dependencies: 3.1；Scope: 记录未来 Harness Plugins pack 需要的 DSH manifest、bundle digest、host compatibility 和 conformance 入口；Exclusions: 不在 DSH repo 创建 plugin catalog 或 release authority；Acceptance: DSH 只消费固定 release 和 profile composition；Validation: `openspec validate ordo-dsh-plugin-visualization-v1 --strict`；Expected: 无第二安装状态源；Failure re-check: plugin package 试图写 tenant state 时退回 Control Plane owner。Evidence: `docs/cookbook/adding-ordo-agent-ops-plugin.md` §8 固定 manifest/version、`dsh.bundle.patch`、host/client keys、compatibility range、Ordo/plugin digest、profile conformance 入口和 bundle-only 无 tenant state 的边界；本地 package metadata/patch 提供可追溯 seam，release/catalog authority 保持外部 owner。
- [x] 5.2 Owner: `client/deepseek-harness` maintainer；Lane: control-plane-handoff；Dependencies: 3.2；Scope: 记录 access-ticket/BFF audience、runtime binding、installation config revision 和 membership revoke 对 DSH host 的输入；Exclusions: 不在 DSH 实现 tenant database、OAuth issuer、secret store 或 BFF；Acceptance: host 能在 context/installation/runtime drift 时 fail closed；Validation: `node_modules/.bin/tsx scripts/verify-md-links.ts`；Expected: 外部 owner 责任清楚；Failure re-check: 缺少 revoke 或 audience 语义时禁用 mutation。Evidence: `docs/cookbook/adding-ordo-agent-ops-plugin.md` §8 固定 audience、tenant/workspace/runtime subject+generation、installation/membership revisions、policy/delegation 与 plugin/contract digests；revoke、audience/context/installation/runtime drift 统一使 cursor/action 失效并禁用 mutation，credentials、OAuth、BFF 与 durable revoke 仍由 Control Plane owner 提供。

## 6. DSH 最终验收

- [ ] 6.1 Owner: `client/deepseek-harness` maintainer；Lane: conformance；Dependencies: 3.4, 4.2；Scope: 对照三个 capability specs 检查 snapshot/event/gap/unknown/reconcile/tenant switch/a11y/large graph 证据；Acceptance: DSH contract、UI、ToolView 和 profile lifecycle 可追溯；Validation: `openspec validate ordo-dsh-plugin-visualization-v1 --strict` 加 DSH focused commands；Expected: 全部 exit 0；Failure re-check: worker 完成声明不等于验收，必须检查 diff 和 evidence。 （rebaselined 2026-08-29: retired owner。分类 **retain-next**——最终验收被 3.4（官方 profile/browser evidence）与 4.2（Workbench parity fixtures）外部阻塞，两 slot 到岗后一并执行。严格 strict validate 当前已绿。 Recheck 2026-09-03: `[external-gate skipped]` 维持——仍被 3.4/4.2 两个外部 slot 阻塞；strict validate 复验 exit 0。） Recheck 2026-09-04: `[external-gate skipped]` 维持——仍被 3.4/4.2 两个外部 slot 阻塞；strict validate 复验 exit 0。 Recheck 2026-09-06: `[external-gate skipped]` 维持——Ordo owner 事件源与官方 profile/browser evidence 通道未到岗（上游 0.1.3-alpha.1 全树无对应 remote 事件/投影面）；ToolView/Workbench 深链 seam 仍缺；Workbench parity fixtures 仍由 owner 提供；strict validate 复验 exit 0。 Recheck 2026-09-07: `[external-gate skipped]` 相关外部门维持——上游无新 seam 发布（npm next 0.1.2-rc.1；alpha 0.1.3-alpha.2 五包 tarball 无 AgentOps 新面）。 Recheck 2026-09-09: 依赖维持——3.4（browser a11y/in-session reload/真实 tenant isolation 证据缺口）与 4.2（Workbench 退役无对照端）均未闭合，本任务保持 open。已汇编证据矩阵供解锁后直用：snapshot/event/gap/unknown/reconcile=统一包 60/60（event-cursor 6/6）+ pane-domain fold（sequence_gap/context_changed→reconcile_required）；tenant switch=context drift fail-closed + teardown（client views 清零、domain service 摘除）；a11y=组件级键盘/ARIA/公告/reduced-motion；large graph=virtualization ariaRowcount=1000 bounded 80（run `dsh-ordo-agent-team-pane-v1-20260909T025208Z-1609431`，组件级）。真浏览器 a11y 与 1,000 节点首屏性能证据缺。strict validate 复验 exit 0。
- [ ] 6.2 Owner: `client/deepseek-harness` maintainer；Lane: closeout；Dependencies: 6.1；Scope: 更新 package README、cookbook、architecture、skills、Agent Note 和本地 OpenSpec 状态；Acceptance: 文档不宣称 V2 launch 已完成，不暴露 agent-only wrapper，不新增根仓库 OpenSpec task；Validation: `pnpm run doc-sync && pnpm run lint && git diff --check`；Expected: exit 0；Failure re-check: 生成目录或双语 gate 失败时修改源文档/配对，不编辑 generated output。 （rebaselined 2026-08-29: retired owner。分类 **retain-next**——closeout 跟随 6.1；外部 slot 到岗前 change 保持 active 不归档，strict validate 已绿。 Recheck 2026-09-03: `[external-gate skipped]` 维持——closeout 跟随 6.1，外部 slot 未到岗，保持 active。） Recheck 2026-09-04: `[external-gate skipped]` 维持——closeout 跟随 6.1，外部 slot 未到岗。 Recheck 2026-09-06: `[external-gate skipped]` 维持——Ordo owner 事件源与官方 profile/browser evidence 通道未到岗（上游 0.1.3-alpha.1 全树无对应 remote 事件/投影面）；ToolView/Workbench 深链 seam 仍缺；Workbench parity fixtures 仍由 owner 提供；strict validate 复验 exit 0。 Recheck 2026-09-07: `[external-gate skipped]` 相关外部门维持——上游无新 seam 发布（npm next 0.1.2-rc.1；alpha 0.1.3-alpha.2 五包 tarball 无 AgentOps 新面）。 Recheck 2026-09-09: `[external-gate skipped]` 维持——6.1 未闭合（3.4/4.2 外部缺口仍在），closeout 文档（README/cookbook/architecture/skills/Agent Note/本地 OpenSpec 状态）本轮未动；strict validate 复验 exit 0。

## 7. DSH skills 与文档

- [x] 7.1 Owner: `client/deepseek-harness` maintainer；Lane: dsh-skills；Dependencies: 1.2；Scope: 增加 `dsh-ordo-agent-ops` 与 `dsh-plugin-experience`，覆盖 tenant context、snapshot/event cursor、receipt/reconcile、Cordis host/client/bundle、ToolView、a11y 和测试证据；Acceptance: 两个 skill 都有完整 frontmatter、Codex invocation metadata 和真实验证命令；Validation: `node_modules/.bin/tsx scripts/verify-skill-invocation-metadata.ts`；Expected: skill policy pair aligned；Failure re-check: 手动触发和隐式触发策略不一致时修复 metadata。
- [x] 7.2 Owner: `client/deepseek-harness` maintainer；Lane: dsh-docs；Dependencies: 7.1；Scope: 增加双语 Ordo Agent Ops cookbook，更新 architecture、extension cookbook 入口和本地 Agent Note；Acceptance: 文档只描述 DSH owner boundary，不把 Ordo/Workbench canonical state 放入 DSH；Validation: `node_modules/.bin/tsx scripts/verify-translation-pairing.ts --write docs/architecture.md docs/cookbook/extension-cookbook.md docs/cookbook/adding-ordo-agent-ops-plugin.md .agents/notes/proposed/architecture/2026-08-14-ordo-agent-ops-dsh-adapter.md`；Expected: pairs recorded and structurally aligned；Failure re-check: code block、heading、list 或 link target 不一致时先修对应语言，再重新记录。
- [x] 7.3 Owner: `client/deepseek-harness` maintainer；Lane: docs-gate；Dependencies: 7.2；Scope: 运行文档、skills、OpenSpec 和 Markdown link gates；Acceptance: 新文档可被发现，根仓库没有新增 OpenSpec task；Validation: `pnpm run doc-sync && pnpm run lint && git diff --check && openspec validate ordo-dsh-plugin-visualization-v1 --strict`；Expected: exit 0；Failure re-check: 生成目录或双语 gate 失败时修改源文档/配对，不编辑 generated output。

## Current focused evidence (2026-08-14)

- 2026-08-16 scope rebase: all pending DSH host/client/verification work belongs in `packages/bundle/ordo-agent-ops/`; the rc.7 leaf packages remain compatibility shims, tests, and task state only. Event cursors, Workbench re-authentication, real ToolView registration, and an Ordo action source remain external owner handoffs.

- `packages/host/ordo-agent-ops/tests/gateway.spec.ts`: 10 tests passed, covering Remote registration, missing expected context/owner source, exact ready/stale passthrough, missing/drifting owner context, post-construction Context key replacement, non-ready facts, unsafe ref/unknown-field rejection and owner read exception mapping.
- Host validation boundary: `packages/host/ordo-agent-ops/src/validation.ts` rejects malformed schema, unsafe opaque refs/text, invalid counts/context, unknown fields and non-ready facts before the owner projection crosses the Remote boundary. The gateway captures a validated, detached and frozen expected context once; invalid expected context becomes `needs_contract`, while invalid, missing or drifting owner context becomes `contract_mismatch` and an owner read exception becomes `offline`.
- `packages/client/ui-ordo-agent-ops/tests/controller.client.spec.ts`: 9 tests passed, covering single-flight reads, late-answer drop, safe remote failure codes, idempotent duplicate suppression, cursor advance, version-regression and ref-rotation fail-close (`owner_cursor_drift`, no facts) with reconcile re-read on the next refresh, cursor retention across non-readable owner states, and disconnect handling that never synthesizes a terminal run state. The browser snapshot-axis cursor lives in `packages/client/ui-ordo-agent-ops/src/client/cursor.ts`; the unified Host bundle now also provides `packages/bundle/ordo-agent-ops/src/host/event-cursor.ts` and `tests/event-cursor.spec.ts` for validated event sequence/gap/drift/bounded-history consumption. A real owner event source, subscription lifecycle and browser event Remote remain deferred to the Ordo/DSH owner contract. Task 2.1's typed handoff ledger is recorded in the bilingual cookbook §8; it does not claim the external event owner is implemented.
- Consumer conformance conventions for owner fixtures are recorded in `docs/cookbook/adding-ordo-agent-ops-plugin.md` (task 2.2): duplicate, version/ref drift, reconcile re-read, late answer, disconnect and stale context entries point at the two spec files above, and fixtures that would require raw prompts, provider payloads, credentials or host paths are rejected back to the owner contract.
- `CI=true pnpm exec tsc -p packages/host/ordo-agent-ops/tsconfig.json --noEmit`、`CI=true pnpm exec tsc -p packages/client/ui-ordo-agent-ops/tsconfig.json --noEmit`、`CI=true pnpm exec vitest run packages/host/ordo-agent-ops/tests/gateway.spec.ts`（10/10）、`CI=true pnpm exec vitest run packages/client/ui-ordo-agent-ops/tests/controller.client.spec.ts packages/client/ui-ordo-agent-ops/tests/browser-plugin.client.spec.tsx packages/host/ordo-agent-ops/tests/gateway.spec.ts`（21/21）以及 `pnpm exec openspec validate ordo-dsh-plugin-visualization-v1 --strict --no-interactive` 均已通过。
- Repository gates: `CI=true pnpm run typecheck`、`CI=true pnpm run build:lib:host`、`CI=true pnpm run build:lib:client`、`verify-package-invariants`、`verify-package-readme-limitations`、`verify-export-jsdoc`、`verify-md-links`、`verify-client-catalog`、`verify-config-catalog` 与 `verify-doc-graphs` 通过。并发 change `dsh-ordo-command-interaction-v1` 与 `dsh-agent-composition-preview-v1` 的既有债务（`packages/host/ordo-commands/` 3 个 lint error、README model-experience 缺口与两个 change README 缺中文对）清偿后，`CI=true pnpm run doc-sync` 28/28、`CI=true pnpm run lint` 0/0、`git diff --check` 与 focused 21/21 复跑通过，任务 7.3 勾选；本次清偿只修债务与文档，未改变 ordo-commands 行为语义，也未实现该 change 的新功能任务。
- These results are `focused/local` and `browser/consumer` evidence only. The Ordo owner source, event-sequence cursor, ToolView, Workbench re-authenticated deep link, mutation and durable authority remain open.
- 2026-09-09 supersession audit (owner `client/deepseek-harness` retired 2026-08-20; substance lives in the unified `@yeisme/dsh-ordo-agent-ops` bundle): re-ran `CI=true pnpm --filter @yeisme/dsh-ordo-agent-ops run test` (60/60: popup 4, git-review 5, team-projection 10, event-cursor 6, cli-owner 6, host-consolidation 23, client-consolidation 2, toolview 2, sidebar-offline 2) plus the three shim packages (`@yeisme/dsh-host-ordo-commands` 28/28, `@yeisme/dsh-host-ordo-agent-ops` 14/14, `@yeisme/dsh-client-ui-ordo-agent-ops` 38/38) and `pnpm --filter @yeisme/dsh-ordo-agent-ops run typecheck` (exit 0). New official-CLI profile lifecycle evidence: `temp/integration-test-runs/dsh-ordo-profile-lifecycle-20260909T045142Z-3334489/` (dsh 0.1.2-rc.1, isolated `DSH_HOME`: install/uninstall/reinstall exit 0; `--dump-config` shows the ordo-agent-ops contribution while installed, zero ordo rows after removal, restored after reinstall), complementing the 2026-09-03 official smoke (`dsh-ordo-official-cli-smoke-20260903T031410Z-47858`: plugin add, dump-config, web boot). Tasks 3.2/3.3/3.4/4.2 stay open on the recorded external gaps (Ordo owner event source + transport backoff + official ToolView seam, all 0-hit upstream 0.1.5-alpha.1; real-browser a11y and in-session reload evidence; Workbench counterpart retired 2026-09-07 so fixture-based parity is no longer producible), and 6.1/6.2 remain dependency-blocked; strict validate re-checked exit 0.
- 2026-08-18 handoff closeout: cookbook §8 records the Ordo snapshot/event/action/receipt field ledger, Workbench opaque-ref + re-authenticated deep-link/parity rules, Harness Plugins fixed-release/profile/conformance inputs, and Control Plane audience/runtime/installation/membership-revoke drift inputs. Tasks 2.1, 4.1, 5.1, and 5.2 are documentation/handoff-only and are checked; external owner implementation remains open in tasks 3.2–3.4, 4.2, 6.1, and 6.2.
