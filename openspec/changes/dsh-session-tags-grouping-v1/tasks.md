## 1. 设计冻结与上游 handoff

- [x] 1.1 [Owner: Harness Plugins；Scope: `openspec/changes/dsh-session-tags-grouping-v1/`；Dependencies: none；Lane: design] 冻结 `split-owner`、多标签多组、插件 sidecar 和“仅通用 DSH seam”决策。Acceptance: proposal/design/两份 capability spec/任务账本齐全，必需能力无静默降级；Validation: `openspec validate dsh-session-tags-grouping-v1 --strict --no-interactive`；Expected: `Change 'dsh-session-tags-grouping-v1' is valid`；失败复查: 先修 capability 名称、Requirement/Scenario 格式和 owner ledger，不进入实现。
- [x] 1.2 [Owner: Harness Plugins upstream staging；Scope: `upstream-prs/session-grouping-provider/`；Dependencies: 1.1；Lane: upstream] 用 staging 目录建立 `changes.patch`、`new-files/`、`apply.sh`、README 和双语 Agent Note，禁止直接修改长期 DSH fork。Acceptance: `apply.sh` 可对干净上游 checkout 幂等应用，README 记录 base commit、文件清单和验证命令；Validation: `upstream-prs/session-grouping-provider/apply.sh <clean-dsh-checkout>`；Expected: patch/new files 全部应用且第二次执行明确拒绝或无重复写入；失败复查: 对照上游当前文件重生成 patch，禁止转为 DOM/整侧栏 fallback。
- [x] 1.3 [Owner: Harness Plugins；Scope: compatibility docs/tests；Dependencies: 1.1；Lane: design] 固定兼容分类：DSH TS API 为 additive experimental、Remote/storage/profile 为 additive；记录一个 RC 的 alpha 兼容窗口和回滚。Acceptance: contract tests pin 既有 `workspace`/`flat` 行为；Validation: `openspec validate dsh-session-tags-grouping-v1 --strict --no-interactive`；Expected: valid；失败复查: 若需要重命名/删除既有 symbol，停止实现并新增迁移/弃用方案。

## 2. DSH 通用分组 seam（upstream lane）

- [x] 2.1 [Owner: DSH upstream staging；Scope: `packages/client/ui-workspace`；Dependencies: 1.2；Lane: upstream] 新增 experimental `SessionGroupingProviderV1Alpha1` 类型与 effect-scoped `ctx.sessionGroupings` Cordis registry，包含唯一 id、稳定 snapshot、订阅和 session action。Acceptance: duplicate id fail loud，dispose 清除 provider，公开 export 不含 tags 语义；Validation: 在 `<clean-dsh-checkout>` 运行 `pnpm --filter @deepseek-ai/dsh-client-ui-workspace run typecheck`；Expected: exit 0；失败复查: 检查 Context merge、caller fiber 归属和 public export 路径。复杂 registry/lifecycle 逻辑 SHALL 写中文注释说明不变量和 unload 边界。
- [x] 2.2 [Owner: DSH upstream staging；Scope: Workspace Browser/store/tree/rows；Dependencies: 2.1；Lane: upstream] 将 provider 接入原生视图菜单、搜索、分组渲染、当前会话状态和 session actions；保留内建 `workspace`/`flat`。Acceptance: unknown provider 回退 workspace；外部分组隐藏 Workspace 专属动作；manual order 只写命名空间化 view account；Validation: 在 `<clean-dsh-checkout>` 运行 `pnpm exec vitest run packages/client/ui-workspace/tests`；Expected: focused suite 全绿；失败复查: 分别检查 menu selection、provider snapshot、可见性过滤和 order account，不放宽旧断言。
- [x] 2.3 [Owner: DSH upstream staging；Scope: ui-workspace tests/docs；Dependencies: 2.2；Lane: upstream] 增加 fake provider conformance：注册/冲突/HMR/卸载、多组重复、未知/归档过滤、搜索、manual/updated、旧持久化状态。Acceptance: 测试不依赖 Harness tags 包且可作为社区样例；Validation: `pnpm exec vitest run packages/client/ui-workspace/tests && pnpm --filter @deepseek-ai/dsh-client-ui-workspace run build`；Expected: tests/build exit 0；失败复查: 若测试需要私有 fixture，先补公开 test harness 或缩小 seam，不让第三方依赖内部模块。

## 3. Tags Host sidecar（可与 2.x 并行）

- [x] 3.1 [Owner: Harness Plugins；Scope: `packages/host/dsh-session-tags/`；Dependencies: 1.1；Lane: host] 初始化 `@yeisme/dsh-session-tags-host` 的 package、exports、tsdown/tsc/Vitest 配置和中文 README，依赖公开 storage-domain、session-persistence、Typert surface。Acceptance: 无 DSH 私有 import、无浏览器存储；Validation: `pnpm --filter @yeisme/dsh-session-tags-host run typecheck`；Expected: exit 0；失败复查: 检查 peer 版本与 ESM export，不修改根外其他 package。复杂 schema/协议转换 SHALL 写中文注释。
- [x] 3.2 [Owner: Harness Plugins；Scope: Host domain/spec/service；Dependencies: 3.1；Lane: host] 实现 `yeisme.session-tags.v1` sessions table、Session 生命周期身份校验、trim+NFKC、12 tags/64 bytes/control-char limits、行级队列和 opaque version。Acceptance: restart 恢复、stale lifecycle 不可见、空 tags 删除、no-op 不换版本；Validation: `pnpm --filter @yeisme/dsh-session-tags-host run test`；Expected: storage/normalization/lifecycle cases 全绿；失败复查: 检查 durability-before-memory、写入串行化和 immutable snapshot，禁止改用 SessionEvent。
- [x] 3.3 [Owner: Harness Plugins；Scope: Host Remote contracts；Dependencies: 3.2；Lane: host] 实现 `sessionTags.list`/`sessionTags.set` 与 `specVersion: '1.0'`，固定 `session-not-found`、`tags-invalid`、`version-conflict`、`storage-unavailable` typed failures。Acceptance: set 使用完整目标值 + `ifVersion`；冲突返回权威行；Validation: `pnpm --filter @yeisme/dsh-session-tags-host run test && pnpm --filter @yeisme/dsh-session-tags-host run build`；Expected: tests/build exit 0；失败复查: 对照 wire schema 与 owner receipt，任何 unknown/partial 结果不得自动重试。

## 4. Tags Client provider 与交互

- [x] 4.1 [Owner: Harness Plugins；Scope: `packages/client/ui-session-tags/`；Dependencies: 2.1, 3.3；Lane: client] 初始化 `@yeisme/dsh-client-ui-session-tags`，实现 generation-aware list controller、reset/focus/own-write 刷新和 dispose。Acceptance: 旧 generation 应答不可覆盖新连接，错误状态不伪造 tags；Validation: `pnpm --filter @yeisme/dsh-client-ui-session-tags run test`；Expected: controller tests 全绿；失败复查: 检查 in-flight fold、generation token 和卸载时 store 清理。复杂异步状态机 SHALL 写中文注释。
- [x] 4.2 [Owner: Harness Plugins；Scope: Client provider/projection；Dependencies: 4.1, 2.2；Lane: client] 注册 `yeisme.session-tags` provider，派生 locale 排序标签组、末尾“未标记”、多 tag 多组和安全 search terms。Acceptance: 同组不重复、跨组可重复、未知/归档由 Browser 过滤、tag mutation 不提升 recency；Validation: `pnpm --filter @yeisme/dsh-client-ui-session-tags run test`；Expected: projection cases 全绿；失败复查: 对照 provider snapshot 不变量，禁止在 Client 建第二份 canonical store。
- [x] 4.3 [Owner: Harness Plugins；Scope: `shell.overlay` 标签编辑器；Dependencies: 4.1, 3.3；Lane: client] 实现“管理标签”动作与可访问多选编辑器，支持既有标签建议、自由输入、移除、保存/取消/Escape、focus restore 和 CAS 冲突 reconcile。Acceptance: mutation 只调用 `sessionTags.set`；取消零写入；冲突不覆盖；Validation: `pnpm --filter @yeisme/dsh-client-ui-session-tags run test`；Expected: component/a11y tests 全绿；失败复查: 检查焦点、aria live error、busy/dispose 和完整目标 tags payload。
- [x] 4.4 [Owner: Harness Plugins；Scope: Client compatibility tests；Dependencies: 4.2, 4.3；Lane: client] 验证 capability probe：缺少 `ctx.sessionGroupings` 时不注册 provider/action，不替换侧栏。Acceptance: old-DSH fixture 无死按钮、无 runtime crash、Host package 可独立加载；Validation: `pnpm --filter @yeisme/dsh-client-ui-session-tags run test`；Expected: compatibility cases 全绿；失败复查: 删除任何 DOM fallback，改为等待/探测公开 service。

## 5. Bundle、profile 与社区接入

- [x] 5.1 [Owner: Harness Plugins；Scope: `packages/bundle/dsh-session-tags/`；Dependencies: 3.3, 4.4；Lane: integration] 创建 `@yeisme/dsh-session-tags` bundle 与 `cordis.patch.yml`，组合 Host/Client 行并将 peer range 锚定首个包含 seam 的 DSH 版本。Acceptance: clean build，profile row 无重复，旧版本给出明确 incompatibility；Validation: `pnpm --filter @yeisme/dsh-session-tags run typecheck && pnpm --filter @yeisme/dsh-session-tags run build`；Expected: exit 0；失败复查: 检查 patch ownership、peerDependencies 和 dsh.client.inject，禁止复制包实现到 bundle。
- [ ] 5.2 [Owner: Harness Plugins；Scope: integration runner/evidence；Dependencies: 5.1, 2.3；Lane: integration] 增加 disposable web profile 流程：安装 bundle、设置标签、刷新、按标签分组、搜索、多组打开、卸载、重装恢复。Acceptance: 每次成功/失败都写标准 evidence 且保留原 exit code；Validation: `pnpm --filter @yeisme/dsh-session-tags run test:integration`；Expected: exit 0，证据位于 `temp/integration-test-runs/<run-id>/`；失败复查: 先分类 introduced/pre-existing/environmental，再修 owned slice，不改无关业务。
- [ ] 5.3 [Owner: Harness Plugins；Scope: package/cookbook docs；Dependencies: 5.1；Lane: docs] 编写安装、能力矩阵、provider contract、Host owner、错误处理、卸载恢复和社区扩展示例。Acceptance: 命令可直接运行，示例只 import 发布 surface；Validation: `pnpm --filter @yeisme/dsh-session-tags run build && openspec validate dsh-session-tags-grouping-v1 --strict --no-interactive`；Expected: build exit 0 且 OpenSpec valid；失败复查: 清除本地 wrapper、私有路径、token/绝对路径和未验证承诺。

## 6. 最终验证与发布门槛

- [ ] 6.1 [Owner: Harness Plugins；Scope: owned packages + OpenSpec；Dependencies: 2.3, 3.3, 4.4, 5.2, 5.3；Lane: final] 在代码稳定后运行 focused tests、全仓 typecheck/test/build 和 strict OpenSpec validation，并检查 diff 只含 owned paths。Acceptance: 无新增失败，集成证据完整，合同分类仍为 additive；Validation: `pnpm run typecheck && pnpm run test && pnpm run build && openspec validate dsh-session-tags-grouping-v1 --strict --no-interactive && git diff --check`；Expected: 全部 exit 0；失败复查: 将失败归类为 introduced/pre-existing/concurrent/environmental/ambiguous，禁止为清全局告警修改无关逻辑。
- [ ] 6.2 [Owner: Root/User gate；Scope: upstream PR / candidate publish；Dependencies: 6.1；Lane: external] 经用户单独授权后提交 DSH upstream PR，并在 seam 发布后发布 `0.1.0-rc.1` 候选包。Acceptance: PR 指向 staging 证据，candidate peer range 与实际发布版一致，未执行生产部署；Validation: `dsh plugin --profile web add @yeisme/dsh-session-tags` 后完成安装冒烟；Expected: provider 可用且回滚命令 `dsh plugin --profile web remove @yeisme/dsh-session-tags` 成功；失败复查: 停止发布并保留本地 artifact，不静默换用 fork/DOM fallback。
