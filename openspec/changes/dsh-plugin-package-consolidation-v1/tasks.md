## 1. 合同基线与迁移护栏

- [x] 1.1 Owner: agent/harness-plugins maintainer；Lane: contract-baseline；Dependencies: none；Scope: packages/bundle/ordo-agent-ops/、三个旧 leaf package、packages/preset/agent-composition-preview/、package README 与本 change；Exclusions: 不迁移源码或改变 Ordo action 语义；Acceptance: 列出 package name、exports、dsh.client、dsh.bundle.patch、patch row、现有 consumer 与 0.1.0-rc.7/0.1.0-rc.8 兼容窗口；Validation: openspec validate dsh-plugin-package-consolidation-v1 --strict --no-interactive；Expected: 合同表与三个 capability spec 一致；Failure re-check: 发现未知公开 export 或外部 consumer 时先补入 migration ledger，不删除表面。
- [x] 1.2 Owner: agent/harness-plugins implementer；Lane: contract-baseline；Dependencies: 1.1；Scope: 新增 package conformance test helper 与现有 host/client/command tests；Exclusions: 不改 DSH core 或以手写 mock 替代 Loader；Acceptance: 基线能断言当前 legacy-only profile 恰有一个 bridge、/ordo、sidebar，且能识别当前四条 bundle row；Validation: pnpm run test；Expected: 基线测试通过，并在后续迁移中可显示真实回归；Failure re-check: 若测试无法穿过实际 Loader composition，改用真实 Cordis Loader fixture 而非放宽断言。
- [x] 1.3 Owner: agent/harness-plugins maintainer；Lane: license-audit；Dependencies: 1.1；Scope: packages/bundle/ordo-agent-ops/、NOTICE/Licenses 与来源说明；Exclusions: 不复制 dsh-web-ui 代码作为默认实施路径；Acceptance: 记录 no-copy 决策，或在任何复制发生时同时纳入 Apache-2.0 LICENSE、NOTICE、固定 commit/文件/范围来源说明和 tarball inclusion test；Validation: git diff --check；Expected: 不存在无来源复制或错误许可声明；Failure re-check: 来源或 NOTICE 不完整时移除复制内容或补齐许可后再继续。

## 2. 统一 Ordo Agent Ops package

- [x] 2.1 Owner: agent/harness-plugins implementer；Lane: unified-package；Dependencies: 1.1；Scope: packages/bundle/ordo-agent-ops/package.json、tsconfig、tsdown config、src/index.ts、src/client/index.ts、tests；Exclusions: 不改 package name @yeisme/dsh-ordo-agent-ops，不新增 ui-kit；Acceptance: package 同时构建 root Host face 与 ./client export，并声明 dsh.bundle.patch/dsh.client；生产 dependencies 不引用 workspace:^ 的旧 Ordo leaf package；Validation: pnpm --filter @yeisme/dsh-ordo-agent-ops run typecheck && pnpm --filter @yeisme/dsh-ordo-agent-ops run build；Expected: clean build 生成所有 export 的 JS、types 和 client bundle；Failure re-check: 任一 export 只在 workspace source plane 可解析时，修正 files/exports/build，不依赖 hoist 偶然成功。
- [x] 2.2 Owner: agent/harness-plugins implementer；Lane: unified-host；Dependencies: 2.1；Scope: packages/bundle/ordo-agent-ops/src/host/bridge.ts 及其 focused tests；Exclusions: 不在 adapter 新建 Ordo read model、scheduler、lease、approval ledger、FS/Git 或任意 fetch；Acceptance: bridge 保持 schema/context/opaque-ref/safe-text validation、safe projection、cache 与 dispose 语义；复杂 lifecycle、fail-closed 分支、protocol conversion 和负向 fixture 增量必须有简体中文注释；Validation: pnpm --filter @yeisme/dsh-ordo-agent-ops run test；Expected: ready/stale、context drift、unsafe payload、offline、late result 与 dispose 负向案例通过；Failure re-check: 浏览器可见 URL、token、host path、raw prompt 或 provider payload 时停止迁移并修复 Host boundary。
- [x] 2.3 Owner: agent/harness-plugins implementer；Lane: unified-host；Dependencies: 2.2；Scope: packages/bundle/ordo-agent-ops/src/host/commands.ts 及 /ordo focused tests；Exclusions: 不实现 action 执行、launch、cancel、redispatch、自动 reconcile 或 ToolView mutation；Acceptance: root package 注册唯一只读 /ordo 且与 bridge 共享同一 authoritative snapshot source；安全 ref/parser、命令 dispose 与 error rendering 语义保持不变；Validation: pnpm --filter @yeisme/dsh-ordo-agent-ops run test；Expected: 真实 Loader composition 可执行 /ordo，unsafe input 不被反射；Failure re-check: command 试图从本地状态合成 terminal/receipt 时回退为 owner projection。
- [x] 2.4 Owner: agent/harness-plugins implementer；Lane: unified-client；Dependencies: 2.1, 2.2；Scope: packages/bundle/ordo-agent-ops/src/client/sidebar.tsx、src/client/index.ts、CSS/locales 与 client tests；Exclusions: 不直接修改 Web Shell grid/DOM，不创建 browser domain store；Acceptance: sidebar 通过官方 slot 注册，显示 safe duty summary，支持 a11y、窄屏、stale/offline/contract_mismatch/unknown，并在 HMR/unload/generation switch 清理和 reset；Validation: pnpm --filter @yeisme/dsh-ordo-agent-ops run test && pnpm --filter @yeisme/dsh-ordo-agent-ops run build；Expected: client test 覆盖 late result drop、slot dispose 与 browser token absence；Failure re-check: client 需要 selector、gridTemplateColumns 或 MutationObserver 才能挂载时，停止并回到 DSH public seam。
- [x] 2.5 Owner: agent/harness-plugins implementer；Lane: unified-client；Dependencies: 2.4；Scope: packages/bundle/ordo-agent-ops/src/client/toolview.tsx 与 client registration；Exclusions: 不注册 placeholder action、approval decision 或伪造 receipt；Acceptance: module location 与 typed receipt/action boundary 可被 future slice 使用，但当前 profile 不渲染 ToolView，除非已有 server-authored action descriptor、approval binding、target version 与 owner receipt；Validation: pnpm --filter @yeisme/dsh-ordo-agent-ops run typecheck && pnpm --filter @yeisme/dsh-ordo-agent-ops run test；Expected: 无 descriptor 的情况下没有 mutation affordance；Failure re-check: ToolView 需要浏览器自行生成 success/approval 状态时保持 disabled 并回到 Ordo owner handoff。
- [x] 2.6 Owner: agent/harness-plugins implementer；Lane: unified-package；Dependencies: 2.2, 2.3, 2.4；Scope: packages/bundle/ordo-agent-ops/cordis.patch.yml、package README；Exclusions: 不插入旧 Ordo leaf package；Acceptance: patch 含唯一 @yeisme/dsh-ordo-agent-ops root row，并以独立命名 row 挂载直接依赖 @yeisme/dsh-agent-composition-preview；README 的安装、配置、inspect、remove 与限制使用真实命令；Validation: pnpm run test && git diff --check；Expected: patch parser 和 README contract check 通过；Failure re-check: 一条安装路径仍要求用户手动 add composition 或旧 leaf package 时阻止合并。

## 3. 独立 composition 与 preset 边界

- [ ] 3.1 Owner: agent/harness-plugins implementer；Lane: owner-boundary；Dependencies: 2.6；Scope: packages/bundle/ordo-agent-ops/package.json、cordis.patch.yml、packages/preset/agent-composition-preview/ 的 contract tests；Exclusions: 不移动 composition source、tests、version、release lifecycle 或 canonical facts 到 Ordo package；Acceptance: unified Ordo package 以直接版本化 dependency 和独立命名 patch row 消费 composition preview；composition package 保持单独实现、install/mount 与 owner；Validation: pnpm --filter @yeisme/dsh-agent-composition-preview run test && pnpm --filter @yeisme/dsh-ordo-agent-ops run test；Expected: 仅安装 Ordo 时没有未解析 composition/leaf package；Failure re-check: Ordo 需要 composition facts 时改为可选 typed contract，而不是重建/嵌入服务。（blocked: `packages/preset/agent-composition-preview` 已移回 client/deepseek-harness owner，本仓库无可运行的 composition package/artifact；需外部 owner 发布后恢复 direct dependency 与独立 patch row。Recheck 2026-09-03: npm registry `@yeisme/dsh-agent-composition-preview` 仍 404（`temp/integration-test-runs/dsh-ordo-official-cli-smoke-20260903T031410Z-47858/artifacts/blockers.json`）；`packages/preset/` 仍不存在；composition 依赖的 DSH additive read seam（standingFactsFor/compositionTextDigest/readPresetLineage/sources/sectionSources/attributions）在已发布 `@deepseek-ai/dsh-api-remotes`/`dsh-agent`/`dsh-session` 至 0.1.2-alpha.5 仍缺。阻塞未变。）
- [x] 3.2 Owner: agent/harness-plugins implementer；Lane: owner-boundary；Dependencies: 3.1；Scope: optional composition contract consumer 与 tests；Exclusions: 不给 Ordo 新增 maturity、risk、qualification、approval、receipt 或 composition cache owner；Acceptance: presence 只读消费安全 envelope，absence/schema mismatch 降级为 unavailable/needs_contract；Validation: pnpm run test；Expected: presence/absence/mismatch 三种测试均不产生本地推导事实；Failure re-check: 任何 fallback 读取 preset 文件、browser cache 或暴露 host path 时删除 fallback。Evidence (2026-08-19): `host-consolidation.spec.ts`（22 tests 全绿）已覆盖 mounted safe envelope、owner missing、owner exception 与 schema mismatch，均保持 qualify preview-only/no local facts；`src/host/composition-contract.ts` 按公开 `dsh.composition.preview.v0` 对 preset/health/drift/tools/prompt sections/projection units/permissions/digest/timestamp 做 strict schema 校验，并拒绝未知嵌套字段与 prompt/text 泄漏。本地 consumer acceptance 已闭环；3.1 的独立 composition package、正式 typed published contract 与 unified clean-profile dependency closure 仍由外部 owner 负责，不因 3.2 完成而误报。
- [x] 3.3 Owner: agent/harness-plugins maintainer；Lane: scope-guard；Dependencies: 3.1；Scope: package inventory、dependencies、patch、README；Exclusions: 不修改 packages/bundle/anchored-standard/，不创建 dsh-plugin-ui-kit；Acceptance: Anchored Standard 保持 Ordo dependency closure 外，Ordo tarball 不含文件树、预览器、SCM、FS/Git 写入、AionUI route/SSE 或 DOM grid hack；Validation: pnpm --filter @yeisme/dsh-anchored-standard run test && git diff --check；Expected: anchored-standard 的既有独立测试仍通过；Failure re-check: 发现范围蔓延时将能力移回其 owner 或另建 proposal。

## 4. 旧 package shim 与无重复挂载

- [x] 4.1 Owner: agent/harness-plugins implementer；Lane: compatibility-shims；Dependencies: 2.3, 2.4；Scope: packages/host/ordo-agent-ops/、packages/host/ordo-commands/、packages/client/ui-ordo-agent-ops/ 的 package manifest、thin entrypoint、types、invariant、README；Exclusions: 不删除旧 public root/subpath export，不复制 unified implementation；Acceptance: 三个 leaf 均在 0.1.0-rc.7 以 thin shim 形式继续可解析，委派给 unified package 的粒度模块并输出一次无敏感数据的 deprecation diagnostic；Validation: pnpm run typecheck && pnpm run test；Expected: legacy import/Loader tests 全绿；Failure re-check: shim 迫使 consumer 使用源码路径或未发布 subpath 时修复 exports，而不是删测试。
- [x] 4.2 Owner: agent/harness-plugins implementer；Lane: compatibility-shims；Dependencies: 4.1；Scope: shared logical contribution guard、Host/client lifecycle tests、real Loader/Web composition fixture；Exclusions: 不使用加载顺序假设、永久 global boolean、Web DOM 或静态禁用 legacy package；Acceptance: legacy-only、new-only、mixed 三个 profile matrix 各有恰好一个 bridge、/ordo、Remote 和 sidebar，mixed unload 后可在下一 generation 再挂载；Validation: pnpm run test；Expected: matrix 每个 case 通过并能检测 duplicate mount；Failure re-check: duplicate 仅被 catch/忽略而没有 disposer 时，改为 fiber-scoped registration guard。
- [x] 4.3 Owner: agent/harness-plugins maintainer；Lane: compatibility-docs；Dependencies: 4.1, 4.2；Scope: package README、docs/README.md、release note source 与 OpenSpec；Exclusions: 不发布、不 push、不删除 package；Acceptance: 文档声明 0.1.0-rc.7 deprecation window、0.1.0-rc.8 最早移除门、单一新安装命令和 rollback 到旧 bundle release；Validation: pnpm run test && git diff --check；Expected: 文档索引、翻译与链接检查通过；Failure re-check: 任何文档让用户手动安装 leaf package 或把 rollback 写成数据迁移时修正。

## 5. 打包、干净 profile 与最终验收

- [ ] 5.1 Owner: agent/harness-plugins test engineer；Lane: package-conformance；Dependencies: 2.6, 3.3, 4.2；Scope: tarball content、exports、manifest、patch 与 dependency closure integration test；Exclusions: 不使用 workspace-only import 作为发布证据；Acceptance: packed @yeisme/dsh-ordo-agent-ops 从空目录可解析 root/./client、patch、独立 composition direct dependency 和 required runtime deps，且未解析的旧 leaf package 不出现在 unified patch；Validation: pnpm run build && pnpm run test；Expected: pack/install conformance 通过且证据写入 temp/integration-test-runs/<run-id>/；Failure re-check: 若 pnpm linker 或 tarball 解析失败，修复 package manifest/pack files，不修改 DSH core。（blocked 2026-08-24: 3.1 composition package/artifact is missing from `packages/preset/`; packed clean-profile dependency closure cannot be proven without that published owner. Recheck 2026-09-03: `@yeisme/dsh-agent-composition-preview` 与 `@yeisme/dsh-ordo-agent-ops` 在 npm registry 均 404，且 runtime dependency `@yeisme/dsh-client-ui-surface` 亦未发布，空目录 registry 安装无法解析；官方 CLI 0.1.1-rc.2 的 link:-本地路径冒烟（add/dump/boot 全绿，evidence `dsh-ordo-official-cli-smoke-20260903T031410Z-47858`）不构成 pack conformance。阻塞未变。）
- [ ] 5.2 Owner: agent/harness-plugins test engineer；Lane: profile-e2e；Dependencies: 5.1；Scope: clean DSH profile install/reload/Loader/Web composition test；Exclusions: 不将手动 leaf install 作为成功条件；Acceptance: clean profile 只执行以下安装命令即可运行完整 Ordo Agent Ops 三个当前运行面：（blocked 2026-08-24: official clean `dsh plugin --profile web add` + independent composition row. Depends on 5.1/3.1. Official `dsh web` is not a plugin completion gate. Recheck 2026-09-03: 官方 dsh 0.1.1-rc.2 已验证接受绝对路径本地包 add、dump-config 单一 ordo root row（136 rows 无 unresolved/旧 leaf row）、web profile boot 无插件加载失败（evidence `temp/integration-test-runs/dsh-ordo-official-cli-smoke-20260903T031410Z-47858/`，harness `scripts/run-ordo-official-cli-smoke.mjs`）；但 npm 规格安装仍因两包 404 不可行、独立 composition row 仍缺，验收不满足。）

      dsh plugin --profile web add @yeisme/dsh-ordo-agent-ops

  Validation: dsh --profile web --dump-config && pnpm run test；Expected: config 有 unified Ordo root row 与独立 composition row、Host 与 /ordo 可用、sidebar 一份、无 unresolved composition 或旧 leaf package；Failure re-check: 先保留 candidate，检查 profile package.json、pnpm lock、bundle patch、tarball files 与 Loader diagnostics，禁止用补装 composition 或旧 leaf package 掩盖失败。（blocked 2026-08-24: official clean `dsh plugin --profile web add` + independent composition row. Depends on 5.1/3.1. Official `dsh web` is not a plugin completion gate. Recheck 2026-09-03: 官方 dsh 0.1.1-rc.2 已验证接受绝对路径本地包 add、dump-config 单一 ordo root row（136 rows 无 unresolved/旧 leaf row）、web profile boot 无插件加载失败（evidence `temp/integration-test-runs/dsh-ordo-official-cli-smoke-20260903T031410Z-47858/`，harness `scripts/run-ordo-official-cli-smoke.mjs`）；但 npm 规格安装仍因两包 404 不可行、独立 composition row 仍缺，验收不满足。）
- [ ] 5.3 Owner: agent/harness-plugins test engineer；Lane: profile-e2e；Dependencies: 5.2；Scope: legacy-only/new-only/mixed profile matrix、unload/HMR/generation reset、browser a11y；Exclusions: 不宣称外部 Ordo deployment 或 production runtime 已验证；Acceptance: 所有 profile/mount matrix 的 evidence 均脱敏，包含 summary.json、command.txt、stdout.log、stderr.log、env.json 与 artifacts；Validation: pnpm run typecheck && pnpm run test && pnpm run build；Expected: focused与最终全量 gates通过，失败仍保留原 exit code；Failure re-check: timeout 不是 worker stopped，先确认真实停止/duplicate 证据再修复。（blocked 2026-08-24: depends on 5.2 official profile matrix / browser evidence.） Recheck 2026-09-03: 5.2 阻塞未变，本任务维持 open；官方 CLI/Loader 层冒烟证据见 `dsh-ordo-official-cli-smoke-20260903T031410Z-47858`。
- [ ] 5.4 Owner: agent/harness-plugins maintainer；Lane: final-acceptance；Dependencies: 5.3；Scope: 全部本 change 文档、spec、tasks、diff；Exclusions: 不 archive change、不发布或修改 production state；Acceptance: Required Capability Ledger 全部 deliver-now 项有测试证据，retain-next ToolView 未被误报已实现，Anchored Standard 仍不计入 Ordo 范围；Validation: openspec validate dsh-plugin-package-consolidation-v1 --strict --no-interactive && pnpm run typecheck && pnpm run test && pnpm run build && git diff --check；Expected: 所有命令 exit 0，final review 记录 package/export/patch compatibility、deprecation window 和 rollback；Failure re-check: global gate 失败时归类为 introduced、pre-existing、concurrent、environmental 或 ambiguous，且不改无关业务逻辑。（blocked 2026-08-24: depends on 5.3. Strict validate already exits 0; change stays active.） Recheck 2026-09-03: strict validate 复验仍 exit 0；focused gates（unified 60/60、三 shim 28+14+38、check:bundles 27/27）全绿；change 保持 active。

## 当前验证状态

- 已完成：统一 package、三项 rc.7 shim、打包内容、legacy/new/mixed 去重与临时 clean profile 的两条 patch row 验证；`3.2` optional composition contract consumer 本地 acceptance 已闭环（strict `dsh.composition.preview.v0` consumer + 31 tests）；完整 package/profile conformance 仍待独立 composition owner 可运行后复跑。
- retain-next：ToolView 仍是未注册的安全展示 seam，不被描述为已开放 mutation。
- 2026-08-19 收束：`packages/bundle/ordo-agent-ops` typecheck/test/build 全绿（4 test files / 31 tests）；`src/host/composition-contract.ts` 只读消费安全 envelope，presence/absence/mismatch 不产生本地事实。
- 2026-08-18 收束：统一 package 的 `src/client/toolview.tsx` 已有 focused `toolview.client.spec.tsx` 覆盖 descriptor 缺失时无 mutation affordance，以及带 server-authored action/receipt 时的安全渲染；`2.5` 已勾选，仍不注册 placeholder action 或本地 receipt。
- 2026-08-16 收束：`3.1`、`5.1`–`5.4` 仍依赖独立 composition owner 和 DSH peer release；本次未构造本地 composition facts、未伪造 packed/profile 或 registry evidence。
- P1 repair（2026-08-16）：unified Host mount 使用 root-scoped 串行 lifecycle tail；final release 与 immediate reacquire/HMR overlap 不再让旧 disposer 删除新 generation 的 bridge/command。
- 发布前置条件（外部 owner）：当前 `@yeisme/dsh-agent-composition-preview@0.1.0-rc.5` 与 `@yeisme/dsh-ordo-agent-ops@0.1.0-rc.7` 均尚未发布到 npm；更重要的是，npm 当前可用的 DSH peer 最高仅为 `0.1.0-rc.6`，不提供 composition preview 已冻结设计所需的 `AgentPresets.standingFactsFor()`、`compositionTextDigest()`、`readPresetLineage()`、`ToolRuntime.sources()`、`SystemPrompt.sectionSources()` 与 `SessionProjectionRegistry.attributions()`。该需求已在独立 `dsh-agent-composition-preview-v1` change 的 projection-core 任务中归属于 DSH core 的 additive read seam，不能由 Ordo package 通过私有字段、伪造 source 或本地事实副本绕过。因此 `5.2`–`5.4` 保持未完成：须先由 DSH core owner 发布兼容 peer，再分别发布 composition preview 与 unified Ordo candidate，最后重跑真实单命令 clean-profile 验收；不得以手动安装旧 Ordo leaf package 掩盖该失败。
