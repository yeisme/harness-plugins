## Why

落实已批准的剧本到分镜制作包流程，接通现有做剧工作台、无限画布、领域工作台与恢复链路。已有 fixture 和局部合同证据不代替真实闭环。

## What Changes

统一制作对象与固定版本引用；复用 Auctra 文本、Scaena 分镜表与制作包 adapter；接通工作台画布的唯一保存 owner；补齐候选回填、关闭续接和 3D 预演草稿持久化。修复保存期间继续编辑被误标为已保存的问题。

## Impact

DSH 保存布局与预演草稿，Auctra 保存正文，Scaena 保存生产镜头与制作包，Ordo 负责跨领域执行。保留既有 schema、命令及 Pane。按代码、合同和真实使用分别验收，不调用付费模型作为默认测试。
