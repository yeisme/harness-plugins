# 实施与验收任务

由 scripts/openspec-tasks.py 维护状态。

- [x] 1.1 核实制作对象、Auctra/Scaena合同与原画布保存入口，冻结兼容与owner边界。 | evidence: design.md已核实Creator Studio画布唯一写者、Auctra恢复draft客户端、Scaena表编辑与草稿/正式制作包入口及Ordo未串联边界；源码盘点不冒充真实闭环。
- [ ] 2.1 接通嵌入画布与Creator Studio唯一读写owner；验证scope、缺席降级、保存重开。 | evidence: Creator Studio三方法整组probe，Pipeline只委托同一owner，30项定向测试含实际ProjectCanvasStore保存重开且无重复save；真实宿主双Pane验收未关闭。
- [x] 2.2 修复3D保存期间继续编辑的dirty与revision回执；验证未知后对账及再次保存。 | evidence: Scene3DController按提交editCount判断后续编辑，旧回执只推进revision；3D client 98项通过，集成证据temp/integration-test-runs/3d-director-20260913-043329-3010926。
- [ ] 3.1 剧本固定版本、分镜草案差异确认及Scaena表编辑接线。
- [x] 3.2 镜头/画布/Inspector选择与专业Pane固定引用联动、候选采用回填。 | evidence: 2026-09-14 专业Pane固定对象交接和采用回填落地：`pane-selection.ts` 固定引用 handoff/候选采用合同（fail-closed 解码、per-source 单调序列门、projectRef 栅栏、dispose 安全），`workbench-controller.ts` `applyPaneSelectionHandoff`（稳定引用解析：投影ref→CanvasBinding sceneObjectRef→canvas domainRef，路由进唯一选择真相）与 `applyCandidateAdoption`（按候选固定ref回填 envelope/对象列表 adopted+version，不重开Pane、不动无关节点、不改写画布草稿）。复用canonical镜头表（creator-studio local-scaena-table 的 shot_ref/scene_ref 语义为稳定引用来源，未二次实现镜头表）。发射侧同日接线：`pane-link-bus.ts` 客户端总线（envelope 由总线统一构造、per-source 单调序列在总线分配、校验失败不消耗序列、dispose 栅栏；cordis service `pipelinePaneLink` 由 drama client 提供于全插件生命周期），workbench controller 订阅总线按 schema 路由进 fail-closed applier，3D 视口用户 pick 经 push/echo 门检测后以 '3d-director' 固定 sceneObjectRef 发布（`emitScene3DPick`）；Scaena 镜头表 workspace 在 canonical 表打开后按 shot_ref 发布 handoff、scaena.table.* 完成回执按 breakdown_ref+candidate digest 发布采用（ui-creator-studio duck-typed 注入，bus 缺席时纯本地降级）。staging 三缺口修复并在真实宿主复验（run screenplay-continuity-20260914-103847-2446594，15/15）：①drama client apply 按 paneWorkbench/slots/remote 服务到达重挂载（immediately 条目先于兄弟服务应用的 launcher 缺失修复）+ 客户端自行 $mount creativePipeline/scene3dDirector Remote 命名空间（strict codec、RemoteResult 解包、命名空间属性读取守卫）+ 无变化签名跳过重注册；②pipeline-gateway 经进程内唯一 scene 域 owner 投影 scene3d 段（shots+bindings 由 canvasNodeRef 反指派生，校验失败整段省略；storage-domain 单开语义下不再自开第二域，canvasRead 同样委托 creatorStudio owner）；③drama bundle 本地挂载按 LocalStudioCLI 推导并提供 creativePipelineExpectedContext/scene3dDirectorExpectedContext（与画布唯一写者同租户/工作区/项目元组，显式集成优先，dispose 释放）。真实浏览器复验：launcher→做剧工作台→种子画布投影→选 Shot 04→开3D嵌入→双向 pick 收敛（存前+存后）→改 transform→真实 saveSceneWorkbench 保存→整页 reload 重开恢复 transform+预演 shot/keyframe。定向测试：pane-selection 10 例 + pane-link-bus 10 例（总线合同/控制器接入/3D pick 发射/回声栅栏）+ apply 2 例（服务到达重挂载、总线服务生命周期）+ scaena-pane-link 4 例（shot 稳定 ref 发布/无总线本地/完成回执采用/非 scaena 回执静默）+ gateway scene3d 4 例（投影/缺失省略/违约省略/域不符省略）+ bundle 2 例（本地上下文提供/显式集成优先）；ui-ai-drama-director 330、ui-creator-studio 247、host dsh-ai-drama-director 136、bundle 6、ui-3d-director 98、host dsh-3d-director 64 全绿。
- [ ] 3.3 Scaena草稿/正式制作包导出和重开恢复。
- [x] 4.1 协商并实现3D预演镜头和关键帧持久化，不改变旧scene严格合同。 | evidence: 2026-09-14 收口：修复scene-store历史环只保留document不保留shots的真实缺陷（环条目改为{document,shots}信封，旧裸document行兼容解析；`change-set.ts` rollback改经`readWorkbenchVersion`恢复历史shots，遗留无shots条目保持现状不伪造）。三条验收腿：浏览器=ui-visual `visual-3d-director.spec.ts` rollback场景（真实client构建+真实Controller驱动save→accept→再编辑→rollback，恢复previz且legacy严格输出无shots；证据 ui-visual-2026-09-14T08-13-43-516Z-1200588）；历史预演回滚=host P2集成（真实Controller/Gateway/Store/change-set log，v5恢复v2的document+shots）+scene-store 2例环/回滚测试；原生宿主恢复=`tests/screenplay-continuity-host.integration.spec.ts` 真实DSH staging JSON storage remount恢复 + 真实子进程close-reopen（构建产物host lib+真实staging storage，进程A保存退出、进程B冷启动恢复，legacy严格）。汇总证据 screenplay-continuity-20260914-081429-1204627（Host 64项）。已知组合缺口（非本change路径）：真实DSH web壳内做剧工作台Pane未注册进launcher、creativePipeline网关不投影scene3d段、无bundle提供两个expected-context服务（dsh-screenplay-smoke-2026-09-14T08-37-49-072Z 实测记录）；持久化seam本身已端到端验证。
- [ ] 5.1 一个项目两场景六镜头真实本地owner完整流程与故障恢复；记录fixture/real层级。
- [ ] 5.2 稳定后完成所属项目质量门、三宽度/IME/缩放及300混合节点持续性能验收。 | evidence: 本次增量全仓build/typecheck通过，protocol41、3D Host59/client98、工作台307项包测试及后增保存重开30项定向通过；9工作台+4导演台浏览器场景通过，check:plugins/check:surfaces绿。主流程、真实IME/浏览器缩放及300混合节点持续性能仍待。证据3d-director-20260913-043910-3070350、ui-visual-2026-09-13T04-37-53-676Z-3057992、ui-visual-2026-09-13T04-38-12-361Z-3052036。
