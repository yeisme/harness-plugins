# Tasks

## Group 1: Contracts and documentation

- [x] 1.1 Add typed scene graph, Shot, canvas binding, capability report, generation change set, and conflict contracts.（2026-09-11 合同冻结落点：`packages/host/pane-protocol/src/index.ts` 新增 `SCENE_3D_SCHEMA`/`SceneDocumentV1`/`ShotV1`/`CanvasBindingV1`/`GenerationChangeSetV1` 与 `SceneGraphSaveResult` conflict+draft 族；测试 `packages/host/pane-protocol/tests/scene-3d.spec.ts`）
- [x] 1.2 Define glTF/GLB import/export capability matrix and opaque extension preservation rules.（2026-09-11 合同冻结落点：`GltfCapabilityReportV1` + `GLTF_2_0_OFFICIAL_EXTENSIONS` 注册表；readable/editable/exportable/opaque-preserved 级别、无法保真导出必须 block+capability gap 的不变量已在 schema superRefine 与 scene-3d.spec.ts 固化；导入/导出器实现仍在 Group 2）
- [x] 1.3 Add 3D Director pane design using the unified visual system UI Contract.（2026-09-12 闭环：本 change `design.md` §UI Contract 已含 surface classification、State Matrix、Responsive、Accessibility、Visual Exceptions 全节；`scripts/check-ui-surface-contracts.mjs` 登记 `ui-3d-director: adopted` 与 `ShotTimeline.tsx` dynamicStyleAllowlist；`pnpm run check:surfaces` PASS（28 client / 8 bundle））
- [x] 1.4 Link this change from project canvas, creative workflow, creative program, and docs index.（2026-09-12 闭环：`docs/design/dsh-project-canvas.md` §3D 导演台联动、`docs/design/dsh-creative-workflow.md:49`、`docs/design/dsh-creative-studio-program.md` 能力表 3D 导演台行（本次补登）、`docs/README.md:78`）
- [ ] 1.5 Add the 3D Director skill and sync project skill references.（部分落地：skill 本体 `.agents/skills/dsh-3d-director-gltf-workbench/SKILL.md` 已同步到 `.agents/skills/` 与 `.claude/skills/` 两个 runtime 目标，根 `AGENTS.md:72` 已引用；缺口：skill 源声明（monorepo 侧 `.skills/profiles/targets/agent/harness-plugins.txt`）未含 `dsh-3d-director-gltf-workbench` 条目，下次 `skills.sh sync-target` 可能抹掉 runtime 副本——该文件在本仓与本 change 写入范围之外，需 monorepo 侧补登后才算闭环）

## Group 2: Host projection and persistence

- [x] 2.1 Implement safe host projection and revision validation.（2026-09-12 闭环：`packages/host/dsh-3d-director/src/gateway.ts` `SceneGraphGateway`（service key `scene3dDirector`）——server 注入 `scene3dDirectorExpectedContext` 并在每个 await 后重读 fence，scope 不符 forbidden、缺上下文 fail-closed unavailable；所有出站 payload 出 gateway 前重过 pane-protocol zod schema，违例整体降级不出部分真相；save 以 base revision 校验。证据：`tests/gateway.spec.ts` 19 例（含 round-trip、foreign scope、schema 出口闸）+ `tests/scene-store.spec.ts` 13 例（revision 递增、requestId 幂等重放、跨项目/伪造权限拒绝），包测试 49/49 绿）
- [x] 2.2 Implement versioned scene graph storage and snapshot export seam.（2026-09-12 闭环：`src/scene-store.ts` `SceneGraphStore` 经已批准的 `storageDomain` seam 按 [tenantRef, workspaceRef, projectRef, sceneRef] 键控持久化，保留有界历史环供回滚、remount 后从确认行恢复不共享内存缓存；快照导出 seam 为 gateway `exportGlb`——导出已提交版本为有界 base64（8MiB 上限，超出报 too_large 不截断），GLB 字节永不进投影。证据：`tests/scene-store.spec.ts`（history ring、remount 恢复、tenant 隔离）+ `tests/gateway.spec.ts`（exported/too_large/missing 各路径））
- [x] 2.3 Implement generation change-set preview/accept/reject/rollback descriptors.（2026-09-12 闭环：`src/change-set.ts` `GenerationChangeSetLog` append-only 记录器——record 仅允许 pending/preview/failed/partial/stale/unknown 初态，transition 重验 schema 不变量（preview→previewRef、accepted→artifactRef、rolled_back→rollbackRef、explained→reason）；accept 经同一 conflict-fenced save 提交新 revision 并保留前版，rollback 把保留版本作为新版本重提交，冲突冻结不改写 change set。证据：`tests/gateway.spec.ts` change-set 六例（record/不变量/accept→新 revision/rollback/冲突冻结/list Remote）。2026-09-12 追记：accept/reject/rollback/preview 已暴露为 gateway Remote（`previewChangeSet`/`acceptChangeSet`/`rejectChangeSet`/`rollbackChangeSet`），出口 fail-closed 重过 schema，客户端控制与证据见 3.4）
- [x] 2.4 Implement conflict freeze and owner reconcile handoff.（2026-09-12 闭环：base revision mismatch → `conflict` 结果保留调用方本地 draft（journal 保留、不覆盖、不重试、不替换 writer）；commit 未落定 → `unknown` + `reconcileScene` Remote 无二次内容写入地 settle；unsettled journal 阻塞后续 save。证据：`tests/scene-store.spec.ts`（并发写者一方冲突保留 draft、journal 恢复、未知提交 reconcile）+ `tests/gateway.spec.ts:167`（冲突不覆盖）+ 客户端 `tests/controller.spec.ts`（冻结/Reapply rebase/Discard 重载））

## Group 3: Client workbench

- [x] 3.1 Implement Shot navigation, canvas/viewport selection convergence, and inspector states.（2026-09-12 闭环：Shot 导航/检查器状态在 Wave C/D1 由 `packages/client/ui-3d-director`（Director3DSurface/ShotTimeline/Scene3DController，50 测试）落地；canvas⇄viewport 选区收敛在优先级 6 由做剧工作台嵌入闭环——`packages/client/ui-ai-drama-director/src/client/pipeline/`（envelope `scene3d` 节 + `selectScene3DNode`/`syncScene3DSelection` + Scene3DInspectorSection/Scene3DViewportRegion），证据 `packages/client/ui-ai-drama-director/tests/pipeline-scene3d.spec.tsx` + `pipeline-scene3d-pane.spec.tsx`（评审 F2 去重后两文件，原 pipeline-3d-embed.spec.tsx 已并入））（2026-09-12 优先级 6「3D 视口嵌入与双向选区」的 3D previsualization 切片已落地 Shot 导航与选区收敛：`packages/client/ui-3d-director/src/previz.ts`（step 采样 `sampleShotAtFrame`、display-only 预览文档 `buildShotPreviewDocument`、绑定收敛 `buildScene3DSelectionConvergence`/`buildShotNavItems`，预览永不回写 draft/save）、`src/ShotNavigator.tsx`（listbox 导航，delivery 状态与 `bound to selection` 文本高亮）、`Director3DViewport`/`three-engine` 新增 `emphasizedNodeIds`/`setEmphasized`（选中 > 强调 > 占位三级着色，场景树降级视图同等语义）；`Director3DSurface` 接线：选 Shot → 收敛选中其相机节点 + 播放头归零，picking 节点 → 导航高亮绑定 Shot（不隐式切换）。证据：`tests/previz.spec.ts` 9 例 + `tests/previz-surface.spec.tsx` 5 例，包测试 64/64 绿。剩余：Shot inspector 状态与嵌入工作台的双视图布局归后续波次/host seam）（2026-09-12 证据补录：createDraft 新场景草稿语义由 `packages/client/ui-3d-director/tests/controller.spec.ts` 的 `Scene3DController createDraft (new scene draft)` 5 例覆盖（missing 读取后建 version-0 合法草稿、按 base revision 0 保存落地 version 1、已有场景拒绝起稿、读取未落定前不伪造草稿、dispose 后无操作）；嵌入区 missing → 「New scene draft」渲染链由 `packages/client/ui-ai-drama-director/tests/pipeline-scene3d-pane.spec.tsx` 覆盖（owner 未提交场景时渲染空态并在点击后只起本地草稿、无任何 owner 写入））
- [ ] 3.2 Implement camera, timeline, transform, visibility, and keyframe editing.（2026-09-12 编辑面 UI 闭环（评审 F1）：`src/NodeTransformEditor.tsx` 选中节点数值编辑——translate/rotate（四元数）/scale 有界分量输入（非有限或超出 ±1e6 拒绝 + `role=alert` 原因文本，不入 draft）+ visibility toggle，编辑经 `controller.editNodeTransform`/`setNodeVisibility` 落 draft（不进 save），conflict-frozen 全禁用+原因；接入 `Director3DSurface`「Selected node」section 与做剧工作台嵌入区 `Scene3DBody`。关键帧编辑入口确认已接通：`ShotTimeline` onEditKeyframe → `controller.editShotKeyframe` draft（clamped）。嵌入区 draft 保存闭环（评审 F3，2026-09-12）：嵌入区头挂真实 Save 控件（clean 禁用+「No unsaved draft changes」原因、frozen 禁用+冻结原因、unknown 时另出 Reconcile save），选中真实控件而非只读说明——理由：嵌入区与独立 surface 共用同一 Scene3DController/saveScene 路径，编辑 affordance 已可在嵌入区产生 draft，若无 Save 入口 draft 只能到他面保存，违反诚实 affordance；冲突条（Reapply/Discard）本已在嵌入区渲染。证据：`tests/node-transform-editor.spec.tsx` 7 例（输入校验/越界/禁用矩阵/visibility/camera 边界）+ `tests/controller.spec.ts` transform/visibility/keyframe 诸例 + `packages/client/ui-ai-drama-director/tests/pipeline-scene3d-pane.spec.tsx` 嵌入编辑与 Save 两例。合同边界说明：SceneDocumentV1 无相机专有参数字段（fov/near/far），相机节点复用基础 transform/visibility 编辑面并在 UI 文本明示（design.md State Matrix 已登记）。剩余缺口：Shot/关键帧 draft 尚无持久化 seam（`scene3d-controller.ts` 注明 "Shots persist through a later seam"），故保持未勾） | evidence: 2026-09-13：预演持久化增量由dsh-screenplay-production-continuity-v1承接，新增可协商sceneWorkbenchRead/saveSceneWorkbench，原store统一CAS/journal/receipt保存shots；旧字段保持严格兼容。实际controller/gateway/store重挂载与提交丢失恢复通过，真实DSH关闭重开仍待，因此父任务不关闭。
- [x] 3.3 Implement glTF/GLB capability display and export blockers.（2026-09-12 闭环：`view-model.ts` `scene3DExportAvailability` 以 capability report 为唯一权威门控；`Director3DSurface` Export GLB 按钮在 `export.ready=false` 时禁用 + title 原因，`capability_blocked` 逐条展示 gap（extension/resourceRef + reason）不静默丢数据，导出中禁用防重复，失败给 bounded reason critical strip；controller 本地预检不发 remote 调用、host gap 透传、字节经 `onExportBytes` 回调离面。证据：`tests/surface.spec.tsx`（disabled+原因、gap 列表、critical strip）+ `tests/controller.spec.ts` 导出五例（本地阻断/host blocked/字节交接/契约违例 fail-closed/冻结拒绝）。残留：GLB 导入入口仍在 controller/host 层（`importGlb`），独立 surface 未挂导入 UI 按钮）
- [x] 3.4 Implement generation preview and auditable change-set controls.（2026-09-12 闭环（评审 F4）：host gateway 新增 Remote `previewChangeSet`/`acceptChangeSet`/`rejectChangeSet`/`rollbackChangeSet`（`packages/host/dsh-3d-director/src/gateway.ts`）——包 `GenerationChangeSetLog` 既有 transition/accept/rollback，携带 changeSet 的结果出口重过 `GenerationChangeSetSchema` fail-closed，context 每个 await 后重读 fence，foreign scope forbidden，conflict 冻结语义不变（accept 冲突不改写 change set）。client：`remote.ts` 控制方法为可选 + `hasScene3DChangeSetControls` probe 守卫（旧 host 缺方法时控件禁用+原因，不死按钮）；`Scene3DController` 新增 preview/accept/reject/rollback（全 fail-closed 解析；accept 提交当前 draft 复用 save 的 conflict 冻结路径；rollback 拒于 dirty draft、成功后重读 owner truth）；`src/ChangeSetPanel.tsx` 状态徽标（statusTone+文本）、preview 只读对比（inputRefs/operationSummary/patchDigest/base vs current revision/基线保留）、server-authored 可用性的 accept/reject/rollback（终态/缺 artifactRef/缺 rollbackRef/冻结/dirty 各带禁用原因），接入 `Director3DSurface`。证据：`packages/host/dsh-3d-director/tests/gateway.spec.ts` change-set controls 六例（preview 基线/accept 新 revision/conflict 冻结/reject 有界 reason/rollback 终态/无 context fail-closed）+ client `tests/changeset-controls.spec.ts` 8 例 + `tests/changeset-panel.spec.tsx` 12 例（面板状态矩阵：列表降级/空态、seam 缺失全禁用+原因、按 status 门控、冻结/dirty 禁用原因；动作路由：Accept/Reject/Rollback/Preview 与失败通知），2026-09-12 复测 host 58/58、client 96/96 绿）

## Group 4: Verification

- [x] 4.1 Add typed contract tests for core glTF/GLB and opaque extensions.（2026-09-12 闭环：`packages/host/dsh-3d-director/tests/gltf-engine.spec.ts` 17 例——GLB/glTF JSON → 契约合规 SceneDocumentV1、损坏字节 bounded 无内容 reason、字节上限前置拒绝、glTF JSON 永不外链 fetch、required 扩展（含 KHR_draco_mesh_compression）fail-closed、官方 glTF 2.0 扩展注册表确定性全覆盖、未知 vendor 扩展 fail-closed、opaque 保留才可导出、无法保留时 block+具体 gap 不静默丢数据、round-trip 保留节点/变换/层级/可见性/资源引用；包测试 49/49 绿）
- [x] 4.2 Add three reproducible Shot integration scenarios: import/edit, generation/revert, conflict freeze/export.（2026-09-12 闭环：`packages/host/dsh-3d-director/tests/shot-scenarios.integration.spec.ts` 三场景全绿（3/3）——A) Shot 锚点打开→选区收敛→时间线 scrub→`sampleShotAtFrame` step 采样预览，断言 display-only 不动 owner 真相；B) draft 编辑→save→commit 丢失注入→unknown→reconcile(not_applied 保留 draft)→重存→第二写者冲突注入→只读冻结（mutation/export 拒绝无副作用、owner 真相不被覆盖）→reapply rebase→保存恢复；C) 真实 GLB 字节经 host byte source 导入（真解析器）→capability 报告→真编码导出（onExportBytes 字节交接）+ `KHR_lights_punctual` 本地预检与 host wire 双路径 capability_blocked 具体 gap。全部真实调用 Scene3DController↔SceneGraphGateway（cordis Context + in-memory storageDomain），仅注入一次性 commit 丢失故障。证据 runner：`scripts/run-3d-director-integration.mjs`）
- [x] 4.3 Run `pnpm run typecheck`, `pnpm run test`, `pnpm run build`, `pnpm run check:bundles`, `pnpm run check:surfaces`, `pnpm run test:visual`, and `pnpm run check:plugins`.（2026-09-12 闭环：此前门禁已过（见原注）；本次补齐视觉证据——新增 `tests/ui-visual/visual-3d-director.spec.ts`（4 例全绿）+ fixture 页 `tests/ui-visual/3d-director-page.mjs`（真实 `@yeisme/dsh-client-ui-3d-director` 构建产物 + fixture remote，合成 SceneDocumentV1/Shot/CanvasBinding）+ `tests/ui-visual/server.mjs` 纯增量路由（`/3d-director`、`/3d-director-client.js`、pane-protocol/zod/three vendor）。矩阵：桌面默认（Shot 导航+视口降级树+时间线+change sets）、conflict-frozen 冻结条、export capability_blocked gaps、400px 窄屏无横向溢出。运行证据：`temp/integration-test-runs/ui-visual-2026-09-12T13-08-48-437Z-2709860/`（4 passed，截图 6 张）。注：`scripts/run-ui-visual-tests.mjs` 的 testFile 过滤正则放宽为允许数字（`[a-z0-9-]+`）以匹配 spec 文件名，既有命名不受影响）
- [x] 4.4 Run strict OpenSpec validation and record redacted evidence under `temp/integration-test-runs/<run-id>/`.（2026-09-12 闭环：`openspec validate dsh-3d-director-gltf-workbench-v1 --strict --no-interactive` 通过；脱敏运行证据落盘 `temp/integration-test-runs/3d-director-20260912-131018-2734151/`（yeisme.integration_test_evidence.v1 manifest：run_id、三场景、命令、退出码 0、fixture 标记 synthetic、视觉证据引用 ui-visual-2026-09-12T13-08-48-437Z-2709860 截图 glob；redaction self-check passed——无凭据/raw prompt/provider payload/绝对路径））
