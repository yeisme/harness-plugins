# dsh-mcp-inspector-v1

DSH 官方 web ui 的 MCP 调用活动只读视图插件（L1 零 seam）+ L2 server 目录 seam 设计。

- 交付：`packages/client/ui-mcp-inspector`（conversation.view tab）+ `packages/bundle/dsh-mcp-inspector`。
- 状态：in progress（2026-08-26 立项）。
- 完成门不含启动官方 `dsh web`（治理约定）；L2 seam 只设计不推送（用户 2026-08-26 确认）。
