# 任务分派

本文件遵循 Ordo OpenSpec 任务导入合同（供 `ordo goal target bind` 驱动），字段顺序为 Owner、Lane、Dependencies、Scope、Exclusions、Acceptance、Verification、Expected、Failure recheck、Automation。

## Lane: projection-core

- [x] 1.1 实现无会话组合投影 service。Owner: DSH implementer；Lane: projection-core；Dependencies: 1.2；Scope: `packages/preset/agent-composition-preview/`；Exclusions: 不创建 agent 与 session 与 turn、不引入模型可见面、不 fork core、不写持久化状态；Acceptance: project 与 smoke 复用 standingKeyFor 与 registries 完成投影与 digest 计算与三层 health 与 drift、broken preset 返回 typed composition_invalid、dispose 后无残留注册、invariant 可验证；Verification: `pnpm run typecheck && pnpm run test -- packages/preset/agent-composition-preview`；Expected: 全绿且 digest 与 dsh.composition.preview.v0 字段一致；Failure recheck: 若 registry 无 standing-scope 无 agent 读口则按设计 Open Question 1 加最小 additive 读口；Automation: safe-local。
- [x] 1.2 为 preset copy 增加 lineage 记录。Owner: DSH implementer；Lane: projection-core；Dependencies: none；Scope: `packages/preset/agent-presets/`；Exclusions: 不改变 copy 与 remove 的拒绝逻辑、不改变 mount 与 generation 语义；Acceptance: lineage.yml 只由 service 写入、schema 为 dsh.preset_lineage.v0、旧 copy 无 lineage 时 drift 报 unknown；Verification: `pnpm run typecheck && pnpm run test -- packages/preset/agent-presets`；Expected: 既有 copy 测试继续绿加新 lineage 测试绿；Failure recheck: 若 lineage.yml 影响 Loader 目录语义则改独立元数据路径并更新设计；Automation: safe-local。

## Lane: cli-surface

- [x] 2.1 实现 composition preview 与 smoke 命令。Owner: DSH implementer；Lane: cli-surface；Dependencies: 1.1；Scope: `apps/cli/`；Exclusions: 不在 app 层复制投影逻辑、不发起模型请求、不输出 raw prompt 与 schema 正文与绝对路径；Acceptance: dsh composition preview 输出单个 dsh.composition.preview.v0 envelope、smoke 输出 redacted dsh.composition.smoke.v0 摘要且 exit 0 只表示 mount 与投影与清理通过、agent 与 explain 遵守 CLI 输出合同；Verification: `pnpm run typecheck && pnpm run test`（命令级加 keyless snapshot）；Expected: envelope 通过 schema 校验且 smoke 负向 fixture 非零退出；Failure recheck: 命令复制投影逻辑到 app 层即回 core；Automation: safe-local。
- [x] 2.2 编写双语 README 与 cookbook。Owner: DSH docs；Lane: cli-surface；Dependencies: 2.1；Scope: `packages/preset/agent-composition-preview/README.md`、`docs/cookbook/agent-composition-preview.md`；Exclusions: 不写 Ordo risk 与 maturity 语义、不描述 owner 边界之外的行为；Acceptance: 新包 README 含 Known Limitations 章节、envelope 字段文档与 cookbook 双语配对一致；Verification: `pnpm run doc-sync`；Expected: exit 0 且配对记录一致；Failure recheck: code block 或 heading 或 link 不一致时先修源文档；Automation: safe-local。

## Lane: client-ui

- [x] 3.1 实现 picker 只读 Preview 面板。Owner: DSH client implementer；Lane: client-ui；Dependencies: 1.1；Scope: `packages/client/ui-agent-preset/`；Exclusions: 不启动 agent 与 session 与 turn、不修改 preset 或组合文件或默认选择、不本地计算 maturity；Acceptance: AgentPresetRow 与 AgentPresetSeat 增加 Preview 动作、展示 tools 与 prompt sections（id 无正文）与 permissions 与三层 health 与 drift 与 capability_digest 前 12 位、maturity 槽位仅渲染 Ordo 注入值、每次打开重新取投影；Verification: `pnpm run typecheck && pnpm run test -- packages/client/ui-agent-preset`；Expected: 既有 section 与 seat 测试继续绿加新面板测试绿；Failure recheck: 若需读私有 React store 则改走 host client bridge；Automation: safe-local。
- [x] 3.2 记录 ToolView 展示契约。Owner: DSH client implementer；Lane: client-ui；Dependencies: 3.1；Scope: `packages/client/ui-agent-preset/README.md`、`TOOLVIEW_CONTRACT.md`；Exclusions: 本切片不实现模型可见 agent_preview 工具、不新增 session event；Acceptance: 展示契约文档说明若后续实现模型工具则必须满足 model-visible 与 logged 对等并走 tool.call.toolview；Verification: `pnpm run test`；Expected: 无回归且无模型可见面变化；Failure recheck: 模型可见面变化必须有对应 session event；Automation: safe-local。

## Lane: verify

- [x] 4.1 端到端验证双面语义一致与 HMR 安全。Owner: DSH maintainer；Lane: verify；Dependencies: 2.1, 3.1；Scope: 不新增 tracked 实现、只产出验证证据；Exclusions: 不伪造 owner 证据、不改 Ordo 侧状态；Acceptance: 真实 profile 装载投影 service 与 client 模块、picker Preview 与 dsh composition preview 对同一 preset 语义一致、HMR reload 无残留、卸载恢复原状；Verification: `pnpm run build && pnpm run test:e2e`（无 key 自跳过）加 keyless snapshot；Expected: exit 0；Failure recheck: 语义漂移回到 conformance fixture；Automation: safe-local。
- [x] 4.2 校验 change 并撰写 Agent Note。Owner: DSH maintainer；Lane: verify；Dependencies: 4.1；Scope: `openspec/changes/dsh-agent-composition-preview-v1/`、`.agents/notes/`；Exclusions: 不修改已冻结的 spec 语义；Acceptance: openspec strict valid、Agent Note 记录 standing-scope 读口与 digest 规范化决策；Verification: `openspec validate dsh-agent-composition-preview-v1 --strict && git diff --check`；Expected: exit 0；Failure recheck: 生成目录被手改时回源修复；Automation: safe-local。

## Current focused evidence (2026-08-14)

- 实现落点：新包 `packages/preset/agent-composition-preview/`（service + `dsh.composition.preview.v0`/`dsh.composition.smoke.v0` 类型 + canonical digest + invariant），`packages/preset/agent-presets/` 新增 `copy()` lineage（`lineage.yml`，`dsh.preset_lineage.v0`）与 `standingFactsFor(id)`（scope key + composition stamp + generation，`standingKeyFor` 背后的同一推导）；为归属读取给 `dsh-tools`/`dsh-system-prompt`/`dsh-session-projection` 各加一个最小 additive 只读口（`sources()`/`sectionSources()`/`attributions()`）；`apps/cli` 新增 `dsh composition preview|smoke`（默认 web profile，`--port 0` + 静默 URL 行，stdout 单信封）；`packages/bundle/web-app` 组合 `agent-composition-preview` 行。
- 命令与结果：`CI=true pnpm exec vitest run packages/preset/agent-composition-preview`（15/15）、`CI=true pnpm exec vitest run packages/preset/agent-presets`（136/136）、`CI=true pnpm exec vitest run packages/core/tools packages/core/system-prompt packages/session/session-projection`（509/509）、`CI=true pnpm exec vitest run apps/cli/tests/args.spec.ts`（8/8）、`CI=true pnpm exec vitest run --config vitest.e2e.config.ts apps/cli/tests/composition.e2e.ts`（4/4，built bin 对真实 web profile 的 preview/smoke/负向/人类摘要）、`CI=true pnpm run build:lib:host`、`CI=true pnpm exec tsc -b apps/cli` 通过；手动 built-bin 运行：`standard`（25 工具全 preset 来源、permissions workspace-write/ask、单信封 stdout、exit 0）、`minimal`、`code`（26 工具含 transport 来源）、`smoke --preset minimal`（residue none、exit 0）、未知 preset（exit 1）。
- 证据分层：以上均为 `focused/local`；built-bin 对真实 profile 的运行属本地进程证据，不代表 Ordo adapter 消费、真实 Ordo owner 数据、provider/deployment 或 production。maturity/risk/qualified 字段不属于 DSH，未实现。
- 与冻结 spec 的差异（记录，未改 spec）：`dsh-composition-projection` spec 的 `source_plugin` 字段未交付——registry 层不携带注册插件身份，补齐需要 `dsh-tools`/`dsh-system-prompt` 注册表结构变更（超出本切片最小 additive 读口），已列为本 change 后续项；drift 比较使用 lineage 冻结的组合**文本** digest 而非 mount 级 `capability_digest`（语义超集：copy 或 source 任一侧编辑都判 `diverged`，且不依赖 mount 存在），决策记录在 Agent Note。
- 3.1/3.2/4.1 保持 deferred：picker 只读面板与 ToolView 契约属 client 切片（非 owner-gated，未排期到本轮）；4.2 的 strict validate 与 Agent Note 已完成，但依赖 4.1 未满足，保持 open。
