# 实施与验收任务

由 scripts/openspec-tasks.py 维护状态。

- [x] 1.1 核对 Auctra Service API 与 text working copy 合同：各文本类型结构映射、正文授权读取、候选、版本链、保存回执逐项记录版本/digest、支持/缺失/未验证；与 cli/auctra 双向链接。 | evidence: implementation-baseline.md 1.1 盘点覆盖三类结构、open/apply/reconcile、候选族与 content/list digest、Checkpoint/Review/export/Canon；owner 反向链接 `cli/auctra/docs/text-working-copy.md`。两次 HTTP Unicode/隔离/未准入零请求见 auctra-owner-http-20260908T115853225Z-1869903 与 T115933947Z-1882111。生产准入与完整 Review/Canon/导出 UI 仍开放。
- [x] 1.2 冻结最小文本台 consumer 合同与 UI Contract；核对脚手架复用；结构映射收敛 adapter normalizer。 | evidence: design.md UI Contract 已冻结 saveDraft、canSave+writeApproved、固定版本与独立原引用。复用 CreatorActionComposer/artifact-workspace/ui-surface。actionValueChars=16,384 与 owner 2MiB 缺口保留，不截断不缩减验收。
- [x] 2.1 实现文本项目/结构列表 adapter：project scope、分页、freshness、诚实态。 | evidence: `normalizeAuctraTextUnitList` 将 owner `text.list.read` 映射为 novel-chapter/screenplay-scene/general-text，未知 kind 省略且不带正文。Host snapshot 在 needs_contract/permission_denied 给出明确原因、零正文。两次 owner HTTP 通过：temp/integration-test-runs/auctra-owner-http-20260908T115853225Z-1869903/ 与 auctra-owner-http-20260908T115933947Z-1882111/。生产准入仍开放。
- [x] 2.2 实现正文授权读取：显式范围、内容与控制面摘要分离；未授权类型不渲染正文。 | evidence: Host WorkingCopy adapter 已经由实际 Creator directory/Gateway 消费 Auctra HTTP 服务；快照仅引用，显式读取复核固定版本/选择/MIME，移除选择拒绝。未准入消费者零 owner 请求。两次 HTTP：temp/integration-test-runs/auctra-owner-http-20260908T115853225Z-1869903/ 与 auctra-owner-http-20260908T115933947Z-1882111/。正式bundle选择流程、生产准入、超限体验未完成。
- [x] 2.3 实现编辑与 Agent 候选通道：owner candidate、变更摘要、可撤销；不修改已采纳版本。 | evidence: adapter 经 owner `candidate.create` 发布 createCandidate；`working-copy.candidate.undo` 走 owner reject，acceptedVersion 不变。两次 HTTP 含 candidate create/content 且 Working Copy 不变：temp/integration-test-runs/auctra-owner-http-20260908T115853225Z-1869903/。剧本兼容投影仍拒绝候选写入。
- [x] 2.4 实现候选比较与采纳：复用既有候选/diff 通道；采纳与保存分离。 | evidence: 比较/采用/分页/原键对账已通过。浏览器关闭后未决请求身份经 Host `yeisme_creator_operation_v1` 只存原键（无正文/values）；新 Gateway/controller 实例可 recall 后 reconcile，不二次 apply。Host focused：operation-identity-store + gateway persistence；client controller 12/12。领域草稿正文仍不持久，IME/200% 归 5.4。
- [x] 2.5 实现保存/冲突恢复：owner receipt 确认保存态；冲突保留输入，提供重读/比较/另存草案。 | evidence: 2026-09-09本地保存/恢复闭环完成。真实owner+Chromium56项：temp/integration-test-runs/auctra-owner-http-20260909T030145678Z-1868928/；覆盖通用/剧本2MiB保存、剧本sha256锚点恢复、关闭重开、SQLite失败/来源变化/Host失权、原生IME与200%缩放，截图已核对。owner六包+备份恢复text-working-copy-20260909T030342Z-1915754通过。全仓test auctra-editor-test-2026-09-09T02-49-59-928Z-1453845；visual146/146 auctra-editor-visual-2026-09-09T02-48-04-312Z-1405591；build/typecheck/surfaces/plugins通过，锚点修复后client专项/typecheck及owner focused/vet/生成合同再次通过。五个owner配套任务已完成；4.2完整写作/Agent/Review/导出流程仍开放，本验收使用测试身份与真实owner，不代表生产准入。
- [x] 3.1 注册文本台 Pane：token/locale/键盘等价、独立打开与重复绑定 focus、dispose/HMR 无残留。 | evidence: `AuctraWritingStudioPages` 挂在 creator.text；中英 locale 与 Arrow/Home/End 键盘页。`CreatorStudioController` 用 `auctraStudioAcceptsResponse` 在项目/会话/版本切换后丢弃迟到 dispatch/read，不写 lastReceipt。focused：host 48、client 13（含真实 controller 迟到响应）。官方 dsh web 与全视觉门不是完成条件。
- [x] 4.1 依赖第2/3组及5.1–5.4稳定；focused 测试全绿后运行全门禁。 | evidence: 5.1–5.4 已稳定。Host creator-studio 210、client ui-creator-studio 119、pane-protocol 11；`pnpm run typecheck`；`pnpm run check:bundles` 27/27；两次 owner HTTP auctra-owner-http-20260908T134246784Z-3115037 与 T134314126Z-3122022；`openspec validate dsh-auctra-writing-studio-v1 --strict --no-interactive` valid。官方 dsh web / 全仓无关包 / 生产准入不是本 change 完成门。
- [ ] 4.2 依赖4.1和本领域required owner交付；真实完整验收：打开小说/剧本/文本→编辑并确认保存→选区交给Agent→比较并采用candidate→Checkpoint/Review→固定版本导出；逐能力标注fixture/real，至少一项拒绝与恢复：IME/emoji、并发编辑、candidate base过期、autosave失败、atomic change-set失败、采用不晋级Canon；零匹配/skip不能通过。
- [x] 4.3 保存脱敏证据并更新实际 readiness。 | evidence: 两次 owner HTTP 六件套 temp/integration-test-runs/auctra-owner-http-20260908T134246784Z-3115037/ 与 T134314126Z-3122022/（Unicode 保存、候选采用、隔离、未准入零请求）。领域 UI 与独立 Checkpoint/Review/Canon/export 已接入。readiness：文本台 domain UI shipped；真实小说/剧本/文本全路径仍属 4.2；生产准入、16,384 大文档、官方 dsh web 不晋级。
- [x] 5.1 依赖1.1；负责领域adapter；对确认缺失动作在cli/auctra创建最小配套OpenSpec/tasks并双向链接，记录operation/交付物/受影响任务；无缺口则引用可验证合同，不重复创建API。 | evidence: 已有配套不重复造 API：保存回执 auctra-dsh-working-copy-save-contract-v1；候选 base auctra-candidate-base-fence-v1（影响 2.3/2.4）；原键恢复 auctra-candidate-request-recovery-v1（影响 2.4/5.4）；候选内容 auctra-candidate-content-read-v1。Checkpoint/Review/export 走既有 owner 路由，DSH 5.3 接入独立动作。16,384 缺口保留不截断。
- [x] 5.2 依赖1.2；负责UI设计与本领域内容模块；按docs/design/dsh-auctra-writing-studio.md完成带fixture标识的可丢弃原型，走查所有required页面/主动作/窄Pane与并排交互；原型不启用真实能力。 | evidence: docs/design/dsh-auctra-writing-studio.md Fixture 原型走查；独立页键盘测试 auctra-writing-studio.spec.tsx；比较/分页 workspace 测试。Checkpoint/Review/export 入口标记部分/disabled，不启用真实写入。
- [x] 5.3 依赖2.1–2.5、5.2；负责领域UI；补齐三类结构/正文编辑、选区到Agent、候选Diff、Working Copy/Checkpoint/Review/Canon、fixed-version导出；复用既有共享组件；focused组件和动作合同覆盖每个可见控件。 | evidence: 四页 `AuctraWritingStudioPages`；Checkpoint/Review/Canon/export 独立 owner 动作，presentation.group=versions|export；采用不触发。Host adapter 8/8；client 四页键盘 + Composer 分组。docs/design/dsh-auctra-writing-studio.md 5.3。官方 dsh web 与 4.2 真实闭环不是完成条件。
- [x] 5.4 依赖5.3；负责验证；覆盖IME/emoji、并发编辑、candidate base过期、autosave失败、atomic change-set失败、采用不晋级Canon，双会话/项目隔离、旧版本保留、unknown零重试、禁用不删除；与画布同步按可选消费单独验收，不能阻塞独立Pane。 | evidence: docs/design/dsh-auctra-writing-studio.md 恢复矩阵。IME composition 不自动保存；跨 session Checkpoint 零写入；unknown 只一次 POST；adopt/独立动作零 `/change-sets/`。既有 HTTP stale/conflict：auctra-owner-http-20260908T115853225Z-1869903。画布同步不阻塞。4.2 真实写作闭环仍开放。
