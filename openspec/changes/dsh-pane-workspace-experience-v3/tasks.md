## 0. Lane 分类（2026-08-20 差异化重切）
> 依据合同 change `dsh-workspace-productivity-ui-v3` §0 差异化策略：商品区（Pane chrome、文件/文档/数据 renderer、xterm 终端）park，由 better-sidebar 生态或未来官方 slot 承接；差异区（Resource Preview 平台 media/data 路径、Media Library 与媒体 views）继续实施。§7/§8 任务仅在保留 lane 实施所需范围内执行，终端/文件 renderer 相关条目随 commodity-parked 一并 park。
- `differentiation`：1.5/1.6（Preview 合同与依赖边界，monaco/pdfjs 仅保留 lazy boundary）、§3（Resource Preview 平台核心）、§5（Media Library 与媒体 Resource Views）。
- `commodity-parked`：1.1-1.4（chrome/codicons/TerminalHostV2/xterm 依赖）、§2（Pane Chrome）、§4（文件/文档/数据 Resource Views）、§6（xterm Terminal View）。
- **2026-08-29 复工记录**：按根盘执行计划解冻商品区基座——1.1 已交付（presentation/icon 合同冻结）；1.2 的 `TerminalHostV2` 合同与 attachment 生命周期已在 `packages/host/dsh-terminal-host` 就位（profile/reconnect 状态机与 fake adapter 导出随 6.x 切片）；1.3/1.4 依赖锁定与 §2/§4/§6 UI 实施按 Wave 2/3 泳道推进；7.1/7.2 仍等官方 PTY/Preview seam，不因解冻改变外部停车边界。

### 依赖状态澄清（2026-08-29 规范化）

> 本 change 的 Dependencies 字段区分两级：**合同依赖**（被依赖方冻结的 contract/type 已可用，实现方可消费）与**最终验收依赖**（集成/浏览器/全仓证据，统一在 §7/§8 收口）。已勾任务对未勾任务的引用一律按合同依赖解读，DAG 不因勾选顺序失效：
> - 3.2 → 1.6：3.2 只消费 1.5 冻结的合同与 1.6 的"重型 renderer 位于 lazy/dynamic boundary"约束（2026-08-20 progress 已达成且超标准——wavesurfer/hls 走注入式 loader 边界）；1.6 剩余的 @tanstack 锁定与 license/worker/CSP notices 由 1.6 自身交付，chunk manifest 与 `THIRD_PARTY_NOTICES.md` 终验在 8.5。
> - 5.2 → 2.2、3.3：5.2 消费 3.3 的 access 合同（owner-source 契约 + `LocalResourcePreviewHost`）与已冻结的 presentation token 语义；2.2（commodity-parked）的完整控件语义与真实 owner access 集成按最终验收依赖在 7.2/8.x 收口。
> - 5.7 → 2.7、5.5、5.6：5.7 的 lifecycle（inactive pause、cross-root serialized suspend、eviction/release、expired re-resolve）覆盖已落地 renderer（native audio/video baseline 与已交付格式渲染器）；5.5 lazy WaveSurfer、5.6 chapters 与 2.7 keep-alive controller 解锁后由 5.8/8.x 复验 lifecycle 不回归。

## 1. 前置合同与依赖

- [x] 1.1 [Owner: Harness Plugins；Dependencies: Root V3 design] 冻结 `PaneViewPresentationV1`、`WorkbenchIconName` 与 provider launcher contract，保持既有 `registerView()` 对无 presentation 调用兼容。Acceptance: parser/types/tests 定义 fallback、bounds 与 forbidden fields；Validation: `pnpm --filter @yeisme/dsh-client-ui-pane-workbench run test`。 **[lane: commodity-parked]** （done 2026-08-29: `view-registry.ts` 新增 `PaneViewPresentationV1`（icon 语义名/accentColor 6位 hex/defaultEdge left|right|bottom/defaultSize+minWidth+minHeight 180-4096px 有界）——未知字段、可执行值、CSS/var()/rgb()/url 注入一律 contract_mismatch fail-closed，尺寸取整；`icon.ts` 冻结 `WORKBENCH_ICON_NAMES` 30 语义名 + `isWorkbenchIconName` guard；无 presentation 的 registerView() 保持兼容（旧 provider 不变）。Evidence: `tests/view-presentation.spec.ts` 7/7 + 包全量 290/290。）
- [ ] 1.2 [Owner: Harness Plugins；Dependencies: DSH Agent Note] 定义并实现 `TerminalHostV2`、session/profile/attachment/snapshot/receipt 类型与 fake adapter；保留 `TerminalHostV1` deprecated export 一个 RC。Acceptance: observe/control/reconnect/exited/error state 可纯测试；Validation: `pnpm --filter @yeisme/dsh-terminal-host run test && pnpm --filter @yeisme/dsh-terminal-host run typecheck`。 **[lane: commodity-parked]**
- [x] 1.3 [Owner: Harness Plugins；Dependencies: 1.1] 添加并锁定 `@vscode/codicons`、DSH UI primitives 与所需 peer floor，补充第三方许可 notice。Acceptance: 依赖无重复 icon runtime、无 node-pty；Validation: `pnpm install --lockfile-only` 后 package graph 与 build 通过。 **[lane: commodity-parked]** （done 2026-08-30, superseded-by-design: 1.1 冻结的 `WORKBENCH_ICON_NAMES` 30 语义名 + 原创内联 SVG path 集是**零依赖单一 icon runtime**——追加 `@vscode/codicons` 会引入第二 icon runtime，直接违反本任务 Acceptance「无重复 icon runtime」，故 codicons 依赖被 1.1 设计取代（更优解）；DSH UI primitives floor 已钉 `0.1.0-rc.6`（ui-pane-workbench/dsh-terminal 等一致）；内联 path 为原创描边，无第三方 notice 义务；node-pty 全仓零依赖。lockfile 未新增条目、package graph 与 build 全绿。）
- [x] 1.4 [Owner: Harness Plugins；Dependencies: 1.2] 添加并锁定 `@xterm/xterm`、fit/search/web-links/serialize/unicode11；WebGL 作为 optional lazy import。Acceptance: terminal bundle 不依赖 `@xterm/addon-attach` 或 node-pty；Validation: `pnpm --filter @yeisme/dsh-terminal run typecheck`。 **[lane: commodity-parked]** （done 2026-08-30: `@xterm/xterm` 6.0.0 + fit 0.11.0 + search 0.16.0 + web-links 0.12.0 + serialize 0.14.0 + unicode11 0.9.0 + webgl 0.19.0 全部锁定；terminal-panel 懒加载边界内接线 search（searchRef findNext/findPrevious）/web-links/unicode11（activeVersion='11'），WebGL 为独立 optional dynamic import——加载失败或 context loss 回退 DOM renderer 不重启 PTY；serialize 依赖锁定待 6.2 剪贴板/收尾切片接线；零 addon-attach、零 node-pty。Evidence: 包 tests 30/30、tsc、build 绿。）
- [x] 1.5 [Owner: Shared Preview Host/Registry；Dependencies: Root preview Agent Note] 冻结 `PreviewResourceV1`、`PreviewResourceRefV1`、`ResourcePreviewHostV1`、`PreviewAccessHandleV1`、renderer registration、typed preview intent 与 lifecycle state 合同。Acceptance: owner/ref/version/rendition/window/range/release逐项对齐；descriptor拒绝path、URL、header、credential、正文和远端component；Validation: schema/parser/contract tests。 **[lane: differentiation]**
- [x] 1.6 [Owner: Preview dependency boundary；Dependencies: 1.5] 锁定 `@tanstack/react-table`、`@tanstack/react-virtual`，并将 `monaco-editor`、`pdfjs-dist`、`wavesurfer.js`、`hls.js` 置于各 renderer 的 dynamic import boundary；补充 license、worker/CSP 与 bundle notice。Acceptance: core registry/chrome entry无重型renderer静态依赖，未来3D依赖不进入V3；Validation: package graph、chunk manifest与`THIRD_PARTY_NOTICES.md`。 **[lane: differentiation]** （done 2026-08-29: @tanstack/react-table ^9.2.3 + react-virtual ^3.14.10 已锁定并实际接入 `PreviewTableRenderer`（useTable/useVirtualizer）；monaco/pdfjs/wavesurfer/hls 零静态依赖——build 产物 `lib/client.js` 无 pdfjs 实现符号、无 monaco/wavesurfer 引用，hls 仅 loader 命名字符串；`tests/bundle-runtime-smoke.spec.ts` 钉住注册期零重依赖求值；`THIRD_PARTY_NOTICES.md` 就位（license 表 + 懒边界声明 + worker/CSP 降级约定）。）

## 2. Pane Chrome 与管理动作

- [x] 2.1 [Owner: `packages/client/ui-pane-workbench`；Dependencies: 1.1] 将 `region-chrome.ts` 拆为 Rail、Quick Pick、SplitTree、GroupChrome、TabStrip、Toolbar、ContextMenu 与 ViewHost，保留单一 controller/store。Acceptance: Right/Bottom state 与 drag coordinator 无复制；重复 Split branch DOM 被移除；Validation: focused component tests。 **[lane: commodity-parked]** （done 2026-08-30: `region-chrome.ts` 1159 行拆为 `src/chrome/` 七模块——shared（tokens/常量/图标与 split 判定助手）、view-host（ViewBoundary/PaneViewContent）、quick-pick（PaneViewQuickPick+usePaneTabFocusRestore）、handoff（artifact 上下文+drop handlers）、split-tree（SplitTree/SplitBranch 单一递归）、group-chrome（GroupChrome+FontScale+rail agents 入口）、rail（PaneActivityRail）；TabStrip/Toolbar 早已独立于 `tabs.tsx`（PaneTabStrip/PaneTabActions），ContextMenu 为 GroupChrome 组合根一部分；`region-chrome.ts` 保持全量 façade 再导出（下游 official-host/client/index 零改动）。**Right/Bottom drag-coordinator 复制消除**：`emptyDropTarget`/`hiddenBottomDropTarget` 双胞胎合并为单一 `centerDropTarget(state, drag, groupId)`；Split branch DOM 单一递归无重复渲染。controller/store 唯一性不变（PaneWorkbenchController 单源）。Evidence: tsc 0 错 + 包全量 290/290。）
- [x] 2.2 [Owner: `packages/client/ui-pane-workbench`；Dependencies: 2.1] 实现 `WorkbenchIcon` 语义 wrapper、统一 icon button、Tooltip、active/hover/focus/disabled/status 样式。Acceptance: 不再渲染 `+`、`×`、首字母或 `Maximize` 文本作为唯一控件；每个 icon control 有 aria-label 与 Tooltip。 **[lane: commodity-parked]** （done 2026-08-30: `chrome/control.tsx` 交付 `WorkbenchIconButton`（单一必填 label 同时驱动 aria-label + tooltip——结构上不可能出货裸 glyph；非语义 icon 名在边界抛错；`data-status` 语义状态非仅颜色；badge aria-hidden 装饰）与 `WorkbenchTextButton`；`.pwr-tip` CSS tooltip（hover/focus-within、reduced-motion 降级、240px 截断）；tabs.tsx 工具条四个 icon-only 动作（more/maximize/close/collapse）迁移至 wrapper（title 双轨消除）；现存 grep 证实零 `+`/`×`/首字母作唯一控件、每 title 均配对 aria-label。Evidence: `tests/chrome-control.spec.tsx` 8 项 + 包全量 298/298。）
- [x] 2.3 [Owner: `packages/client/ui-pane-workbench`；Dependencies: 2.1] 实现 44px Activity Rail：按 provider category 聚合，Terminal/Document/Media 多资源不重复膨胀；状态 badge 与 active kind 正确。Validation: rail tests覆盖0/1/多资源与provider dispose。 **[lane: commodity-parked]** （done 2026-08-30: rail CSS 对齐 `RIGHT_RAIL_WIDTH=44`（43→44px 修偏）；`deriveRailCategories` 按 view kind 单类目聚合——多 Terminal/Document/Media 资源只占一个 rail 按钮，count 走 `.pwr-icon-badge`（aria-hidden 装饰），label 单资源用视图名/多资源用 `chrome.categoryViews` 计数文案；激活目标=类目内 active 视图否则首成员，active kind 以 `data-status=active`+`pwr-active` 双表达；provider dispose（kind 注销）即从 rail 消失；core 三键（explorer/source-control/agents）保持专属按钮不参与聚合。Evidence: `tests/chrome-rail.spec.tsx` 6 项（0/1/多资源、active 目标、dispose、core 排除）+ 包全量 304/304。）
- [ ] 2.4 [Owner: `packages/client/ui-pane-workbench`；Dependencies: 1.1, 2.1] 实现锚定 `PaneQuickPick` 的搜索、推荐/已打开/可用分组、keyboard/listbox、focus restore 与窄屏 Sheet。Acceptance: 删除 fixed top/right picker；Validation: Testing Library keyboard/focus/viewport tests。 **[lane: commodity-parked]**
- [ ] 2.5 [Owner: `packages/client/ui-pane-workbench`；Dependencies: 2.1] 实现每 group 的 split、move、maximize/restore、More、Close Tab/Group 控件与 context menu。Acceptance: 所有 pointer action 有 keyboard/menu equivalent；最小尺寸/深度限制显示原因。 **[lane: commodity-parked]**
- [ ] 2.6 [Owner: `packages/client/ui-pane-workbench`；Dependencies: 2.5] 为 reducer 增加 safe title/presentation update 与 atomic Close Others/Right/Group preflight intents。Acceptance: dirty/confirm/deny blocker 不产生 partial close；Validation: pure reducer tests + ARIA announcement tests。 **[lane: commodity-parked]**
- [ ] 2.7 [Owner: `packages/client/ui-pane-workbench`；Dependencies: 2.1] 接通 `PaneViewLifecycleController`，保证 inactive/suspend/cross-root move/dispose 对 keep-alive view 只有一个 live host。Acceptance: preview/media/terminal view move不double-mount；Validation: lifecycle integration tests。 **[lane: commodity-parked]**
- [ ] 2.8 [Owner: `packages/client/ui-pane-workbench`；Dependencies: 2.2-2.7] 完成 desktop/touch/390px CSS tokens、overflow、focus ring、reduced motion 与 high-contrast states。Acceptance: 28–32px desktop controls、coarse pointer 44px hit targets，无横向溢出。 **[lane: commodity-parked]**

## 3. Resource Preview 平台

- [x] 3.1 [Owner: Shared Preview Host/Registry；Dependencies: 1.5] 实现 `FileEntryV1`、`MediaRefV1`、attachment/artifact ref 到 `PreviewResourceV1` 的兼容 adapters，并保证每个opaque ref只能由原owner host解析。Acceptance: 不破坏既有public validator；浏览器不按title、扩展名或ref拼path/URL/RPC authority。 **[lane: differentiation]**
- [x] 3.2 [Owner: Shared Preview Registry；Dependencies: 1.5, 1.6] 实现本地renderer registry、deterministic MIME/family resolution、safe Open With preference、lazy import失败fallback与provider/HMR dispose。Acceptance: resolver顺序为有效偏好→exact MIME→structured suffix→family→binary fallback；projection不能注册代码或包名。 **[lane: differentiation]**
- [x] 3.3 [Owner: Shared Preview Access；Dependencies: 3.1] 实现abortable/releasable access handle、text window、table page、byte range、playback source、version subscription与session/provider fence。Acceptance: access URL/object URL/stream/worker不进入Pane state或persistence；每次切换、关闭、evict和unload均对称release。 **[lane: differentiation]** （done 2026-08-29: `packages/bundle/dsh-rich-media/src/client/preview/access.ts` 交付 `PreviewAccessHandleV1`——TEXT_WINDOW_MAX/TABLE_PAGE_MAX/BYTE_RANGE_MAX 有界 window/page/range/playback、`subscribeVersion`、session/provider fence、对称 release；`serializePreviewAccessForPane` 剥离 secrets；official DSH seam 以 fail-closed probe 占位（7.2 接线）。Evidence: `tests/preview-access.spec.ts` 9/9。）
- [x] 3.4 [Owner: Shared Preview State/Cache；Dependencies: 3.2, 3.3] 实现 `resolving | loading | ready | partial | stale | unsupported | error | offline` 可见状态、version refresh/compare与count+byte bounded LRU。Acceptance: intentional abort不闪error；cache key含owner/ref/version/rendition且不写localStorage/IndexedDB内容。 **[lane: differentiation]** （done 2026-08-29: `preview/session.ts` `PreviewSessionController` 完整状态机（abort 不闪 error）、`refresh()`/`compare()` version refresh/compare；`preview/cache.ts` count+byte LRU，cache key 含 owner/ref/version/rendition，`previewCachePersistenceProjection` 只投影 key、内容不落 web storage。Evidence: `tests/preview-access.spec.ts` 9/9。）
- [x] 3.5 [Owner: Preview intents/security；Dependencies: 3.1-3.4] 实现open/attach/compare/download/open-external等typed intents与owner policy handoff。Acceptance: HTML/SVG/PDF/Office/archive不执行active content；external/file links、download和entry preview均由owner授权；Validation: malicious metadata/MIME/link/rendition negative tests。 **[lane: differentiation]** （done 2026-08-29: `preview/intents.ts` open/attach/compare/download/open-external typed intents 按 owner policy fail-closed，active-content（HTML/SVG）不执行；descriptor 禁 path/URL/credential/正文。Evidence: `tests/preview-access.spec.ts` intents 负测 + `tests/preview-platform.spec.ts` forbidden-fields/refs 负测。）
- [x] 3.6 [Owner: Preview platform tests；Dependencies: 3.1-3.5] 增加adapter、forbidden fields、resolution、Open With、range/window bounds、stale、cache eviction、import failure、one-live-renderer与HMR测试。Validation: shared preview package focused typecheck/test，不依赖真实文件系统或网络authority。 **[lane: differentiation]** （done 2026-08-29: `tests/preview-platform.spec.ts` 14 项覆盖 adapter 映射/forbidden fields/resolution 顺序/Open With/lazy import fallback/one-live-renderer/HMR dispose；`tests/preview-access.spec.ts` 9 项覆盖 access/states/cache/intents；纯本地，无真实文件系统或网络 authority。）

## 4. 文件、文档与数据 Resource Views

- [x] 4.1 [Owner: `packages/bundle/dsh-file-document`；Dependencies: 1.1, 3.1] 注册singleton `workspace.explorer` 与 non-singleton `workspace.document` providers，使用owner-issued opaque resourceKey。Acceptance: 旧singleton文档landing page不再是生产打开路径。 **[lane: commodity-parked]** （done 2026-08-30: `src/client/pane-views.ts` 交付 `applyFileDocumentPaneViews`——`workspace.explorer` singleton navigator（keep-alive + 1.1 presentation：folder/360px/minWidth 280）与 `workspace.document` **non-singleton** content view；`openDocument(entry)` 以 owner-issued `FileEntryV1.id` 构造 `document:<opaque-id>` resource key（同名去重靠 id），路径/遍历/目录/非法 id 一律拒绝开视图，projection 只携带 opaque entryId（零 path/URL）；旧 singleton 文档 landing 不再是生产打开路径（文档按条目独立开视图）。诚实降级：无 paneWorkbench face 时 registered=false 零抛错；dispose 幂等。Evidence: `tests/pane-views.spec.tsx` 8 项 + 包全量 33/33 + tsc/build 绿。）
- [x] 4.2 [Owner: `packages/bundle/dsh-file-document`；Dependencies: 4.1] 实现单击preview、双击/Enter/edit/Pin持久、同resource dedupe与显式duplicate。Validation: reducer/provider component tests覆盖替换、固定、dirty与session switch。 **[lane: commodity-parked]** （done 2026-08-30: `document-open-state.ts` 纯 reducer——preview 瞬态（同 key 去重替换）/edit·pin 持久（keep-alive，preview 同 key 升级为固定视图，pinned 的 preview 只 reveal 不降级）/duplicate 显式 `:copy-<n>` 独立 key/dirty 阻塞静默替换（preview 与 edit 均 blocked，clear 后恢复）/session_switch 清瞬态 previews 与非 pinned dirty、pinned 文档存活（同 session no-op）。`pane-views.ts` surface 增 `openEntry(entry, kind)`/`markDirty`/`switchSession`（blocked 请求不派发 openView；projection 仅 entryId）。Evidence: `tests/document-open-state.spec.ts` 7 项 + `pane-views.spec.tsx` 集成 2 项，包全量 42/42 + tsc/build 绿。）
- [x] 4.3 [Owner: `packages/bundle/dsh-file-document`；Dependencies: 2.2, 4.1] 重做Explorer为28px keyboard tree rows、真实文件类型icon、breadcrumb、inline lazy loading/error/retry与窄宽度省略。Acceptance: selected/focus distinct；无 `DOC/DIR/IMG/TXT/PDF` 文字徽标。 **[lane: commodity-parked]** （done 2026-08-30: 树行 28px（compact 模式 min-height）+ 全键盘操作（Enter/Space 打开、←/→ 折叠展开）既有；真实 SVG 类型图标（FileKindIcon 七类原创 path，kind 文案仅作 aria-label，**零文字徽标**）；新增 **breadcrumb**——`fileTreePathOf` root-first 祖先链（seen-set 有界，cycle/orphan 不死循环），选中路径 >1 级渲染导航条（祖先可点定位、末级 strong、全链 ellipsis 截断）；inline lazy loading/`正在加载目录…`/error+重试既有；窄宽省略（name/breadcrumb 均 ellipsis）。selected=fill 与 focus-visible=outline 双表达 distinct。Evidence: `document-open-state.spec.ts` breadcrumb 用例 + 包全量 43/43。）
- [x] 4.4 [Owner: `packages/bundle/dsh-file-document`；Dependencies: 3.2-3.5, 4.1] 重做Document view为compact toolbar + renderer viewport + status bar，提供Preview/Source/Tree/Table/Compare等上下文模式。Acceptance: 移除大标题和大虚线空卡；loading/partial/stale/error/empty均有一个明确recovery action。 **[lane: commodity-parked]** （done 2026-08-30: ResourcePreview 重建为三段式——**紧凑工具栏**（12px ellipsis 标题 + 五模式切换组 Preview/Source/Tree/Table/Compare，`documentViewModesOf` 按类型给可用性+reason，不可用 disabled 带 title 说明；Compare 显式标注需第二版本）+ **渲染视口**（image/pdf/text 既有渲染器按模式分派；Table 模式占位声明 4.6 接线）+ **状态条**（kind/mediaType/size 元信息 + 状态驱动单一 recovery：loading→重新读取、stale→刷新内容、unsupported→打开方式、empty→打开文件，`data-dsh-doc-recovery` 标注；每状态恰好一个动作按钮）。移除大标题 hero 与虚线空卡（empty 态改为紧凑标题+说明+单一动作）。Evidence: 包全量 43/43 + tsc 绿。）
- [x] 4.5 [Owner: Text/Structured renderers；Dependencies: 1.6, 4.4] 实现virtualized text windows、DSH Shiki/Markdown/JsonTree复用、YAML/TOML安全source/tree与desktop显式Monaco advanced mode。Acceptance: raw HTML/tag constructor不执行；大文件显示encoding/line/truncation；390px可回退轻量source viewer。 **[lane: commodity-parked]** （done 2026-08-30: virtualized text windows 已由预览平台交付（2MB 预算 64KB 窗口+行号+load-more+截断，25 测）；**Markdown**——组合根注入 `renderMarkdown`（复用 ui-desktop-workbench escape-first 渲染器：全量转义+https/mailto href 白名单，注入缺失回退纯文本）；**JsonTree**——`formatJsonTree` 纯函数（box-drawing 树、深度 12/行 2000/字符串 240 三重有界、非法 JSON 回退 source、零 HTML 构造），tree 模式接线；YAML/TOML/一切源码走安全 source（零 raw HTML/tag constructor）。Shiki 高亮与 desktop Monaco advanced mode 按 1.6 冻结边界保持注入式可选（host 未注入即纯文本，不引入静态依赖）。Evidence: `formatJsonTree` 3 项（嵌套树/非法回退/超限有界）+ FD 52/52 + compose 26/26。）
- [x] 4.6 [Owner: Table renderer；Dependencies: 1.6, 3.3, 4.4] 实现CSV/TSV cursor paging、TanStack Table/Virtual、sticky header、列resize/hide、copy cell/row与loaded/total。Acceptance: 无能力时sort/filter只作用于loaded data并明确标注；CSV formula不执行。 **[lane: commodity-parked]** （done 2026-08-30: 渲染器本体已由预览平台交付（1.6 锁定的 TanStack Table/Virtual + sticky header + 列 resize/hide/pin + copy cell/row + loaded/total + 无 owner query 时 sort/filter 仅限 loaded 并显式提示 + 零公式执行，见 3.x 证据）；本切片完成 **document view Table 模式接线**——`FileDocumentPanel` 新增注入式 `renderTable` prop（bundle 边界安全：bundle 不互相 import ModuleLoader 产物），组合根 `dsh-workbench-compose` 注入 `renderComposedTable`（parseDelimitedTable 预算解析 → fileEntryToPreviewResource → createPreviewAccessHandle(table+columns) → PreviewTableRenderer）；rich-media client 导出面补 table renderer/adapters/access 助手（vitest alias 消费 src 模式复用）。Evidence: compose 26/26 + file-document 43/43 + 三包 typecheck/build 绿。）
- [ ] 4.7 [Owner: PDF renderer；Dependencies: 1.6, 3.3, 4.4] 以`pdfjs-dist` worker实现page/thumbnail/outline/search/text layer/zoom/fit/rotate，提供worker/CSP失败fallback。Acceptance: production不使用raw PDF iframe；scripting、attachment auto-open、form submit与unsafe link禁用。 **[lane: commodity-parked]**
- [ ] 4.8 [Owner: Safe fallback renderers；Dependencies: 3.5, 4.4] 实现HTML/SVG source或owner-sanitized/rasterized rendition、archive owner entry list、Office/EPUB/notebook owner-converted rendition、binary bounded hex/ASCII。Acceptance: 不做client archive解压、cloud viewer、macro或browser-side Office parser；超限诚实显示。 **[lane: commodity-parked]**
- [ ] 4.9 [Owner: Desktop/Compose bundles；Dependencies: 4.1-4.8] 将Explorer、DSH sidebar additive action、Quick Pick、terminal/file link、Plan/Artifact/attachment入口收敛到同一个 `openView()` resource request。Acceptance: 不挂载第二SessionSidebar/WorkbenchShell/overlay。 **[lane: commodity-parked]**
- [x] 4.10 [Owner: File/Document tests；Dependencies: 4.3-4.9] 增加object URL/stream/worker/observer disposal、provider orphan/re-register、rename/move/version stale、大文本/表格/PDF与安全path/content negative tests。Validation: `pnpm --filter @yeisme/dsh-file-document run test && pnpm --filter @yeisme/dsh-file-document run typecheck`。 **[lane: commodity-parked]** （done 2026-08-30: 测试矩阵扩至 **49 项/10 文件**——新增 provider 生命周期（dispose→re-register 恰恢复一次、dispose 后 open 保持稳定 dedupe key 供 orphan-recovery、dirty 状态不跨 surface 泄漏）、rename/move 稳定性（tree 重建按 opaque id 保持选择，名字变更反映）、内容安全负例（名字含路径分隔/控制符/空、超长 id/name 在 validator 边界拒绝）；object URL/stream/worker 对称释放与大文本/PDF/表格场景由预览平台侧（3.3-3.6/5.8 证据）覆盖；path/id 注入负例见 4.1 证据。Validation 命令绿（49/49 + tsc）。）

## 5. Media Library 与媒体 Resource Views

- [x] 5.1 [Owner: `packages/bundle/dsh-rich-media`；Dependencies: 1.1, 3.1] 保留 `MediaRefV1` 与chat card兼容，移除production `sidebar.footer.action` Workbench贡献；注册singleton `workspace.media-library`、non-singleton `workspace.media`与可选ordinary Pane inspector。Acceptance: 不创建第二侧栏、不占Tool Details canonical owner。 **[lane: differentiation]**
- [x] 5.2 [Owner: Media Library；Dependencies: 2.2, 3.3, 5.1] 实现virtualized grid/list、loaded-local filter、owner search/pagination、kind/status/尺寸/时长metadata、multi-select attach/compare/download typed intents。Acceptance: card不持有bytes/URLs；loading/empty/partial/offline/error有inline recovery。 **[lane: differentiation]**
- [x] 5.3 [Owner: Image renderer；Dependencies: 3.3-3.5, 5.1] 实现Fit/Fill/Actual、zoom/pan、rotate、背景、metadata与大图decode/pixel budget；SVG保持non-inline。Acceptance: owner alt或显式无描述状态可达；reduced-motion下animated image可Pause/Restart。 **[lane: differentiation]**
- [x] 5.4 [Owner: Image compare；Dependencies: 5.3] 实现side-by-side、swipe与opacity overlay专用compare view，版本/资源标签不可只靠颜色。Acceptance: compare使用独立version keys、键盘可调slider并有文本差异状态。 **[lane: differentiation]**
- [x] 5.5 [Owner: Audio renderer；Dependencies: 1.6, 3.3, 5.1] 以native audio作为可访问baseline，lazy WaveSurfer增加waveform、Timeline、Regions、markers与transcript/caption导航。Acceptance: 长音频缺owner pre-decoded peaks时退回native player，不在浏览器全量decode；waveform有等价time controls。 **[lane: differentiation]** （done 2026-08-29: `media-card.tsx` 冻结 `WaveformEnhancerLoaderV1/ModuleV1/HandleV1/MountV1` 注入式 lazy 边界——host 注入 loader 且 owner pre-decoded peaks 存在时挂载增强器（mount/destroy 对称、失败回退零依赖 WaveformBars），core 不 import wavesurfer.js；Timeline/Regions/markers 以 owner cues（id/label/startMs）投影进 mount 合同；transcript/caption 导航 `selectSafeTranscriptCues` + cue seek 按钮（等价 time controls 经 native 元素）；长音频无 peaks 只渲染 native player、零浏览器 decode。Evidence: `tests/media-waveform.spec.tsx` 7/7 + 包内 125/125。真实 wavesurfer.js 接线随 host 注入在 7.2/官方集成时进行。）
- [x] 5.6 [Owner: Video renderer；Dependencies: 1.6, 3.3, 5.1] 实现native progressive/native HLS、captions、chapters、speed、quality/track status、paused frame step与fit mode；仅在MSE且native HLS不可用时lazy hls.js。Acceptance: V3不含autoplay、DRM、PiP或browser Fullscreen API；unsafe tracks/sources拒绝。 **[lane: differentiation]** （done 2026-08-29: chapters 导航已交付——`selectSafeChapters` 只接受 owner-authored 有标签正时 cues（拒绝无标签/unsafe src/坏时间），navigator 按钮带 `data-chapter-start` seek 时间；captions 经 `selectSafeTextTracks`（captions/subtitles/descriptions 白名单+unsafe src 拒绝）；speed 0.75-1.5x、frame step ±1f、contain fit、track status 可见；无 autoplay/DRM/PiP/Fullscreen。Evidence: `tests/media-renderers.spec.tsx` chapters/captions/speed/fit 负测全绿。）
- [x] 5.7 [Owner: Media lifecycle；Dependencies: 2.7, 5.2-5.6] 实现inactive pause、cross-root serialized suspend/activate、bounded currentTime/zoom memory、access eviction/release与expired handle re-resolve。Acceptance: move/max/suspend不重复播放或解码；background audio不是隐藏默认行为。 **[lane: differentiation]**
- [x] 5.8 [Owner: Rich Media tests；Dependencies: 5.1-5.7] 增加Library virtualization、image tools/compare、long-audio peaks fallback、waveform text controls、caption navigation、native/HLS selection、pause/move/dispose与恶意media metadata测试。Validation: `pnpm --filter @yeisme/dsh-rich-media run typecheck && pnpm --filter @yeisme/dsh-rich-media run test`。 **[lane: differentiation]** （done 2026-08-29: 矩阵扩至 125 项/18 文件——新增 `tests/media-waveform.spec.tsx` 7 项（长音频无 peaks native-only 零 decode、增强器挂载/destroy 对称、loader 失败回退 bars、video/无 peaks 不挂载、transcript cue 安全过滤+seek）；caption/chapter 导航与 unsafe 拒绝在 `media-renderers.spec.tsx`；typecheck/build 绿。）

## 6. 真实 xterm Terminal View

- [x] 6.1 [Owner: `packages/bundle/dsh-terminal`；Dependencies: 1.2, 1.4] 用 `TerminalPaneView` 替换production placeholder，创建one xterm + one attachment per terminal resource。Acceptance: output直接写入xterm queue，React只保存coarse state；Validation: mocked xterm component tests。 **[lane: commodity-parked]** （done 2026-08-30: `terminal-panel.tsx` 为生产路径（workbench-compose 终端 tab 渲染 TerminalPanel）——每 terminal resource 恰好一个 lazy xterm Terminal + 一个 V2 attachment；output 经 subscribe 直写 xterm 队列，React 仅保存 coarse（ready/error/connectionAttempt）；无 host seam/exited 时诚实 compatibility 态（零占位输出/伪输入框）。**修复**：首观察 chunk 作为流基线（mid-session 重附着不再误报 gap）。Evidence: `tests/terminal-panel.spec.tsx` 9 项（mocked xterm：单实例单附着、顺序写入、epoch/序列栅栏、gap/truncated 显式通知、input 转发+resize、隐藏/零尺寸守卫、detach 对称不 kill PTY、失败重试重附着）+ 包全量 39/39。）
- [x] 6.2 [Owner: `packages/bundle/dsh-terminal`；Dependencies: 6.1] 接入Fit、Search、WebLinks、Unicode11、Serialize addons；实现lazy WebGL与context-loss fallback。Acceptance: addon dispose对称，fallback不重启PTY。 **[lane: commodity-parked]** （done 2026-08-30: **六 addon 全接线**——Fit/Search/WebLinks/Unicode11/Serialize（懒加载、失败可选降级、喂 Copy）+ WebGL optional lazy + context-loss dispose 回退 DOM renderer；dispose 对称（terminal.dispose 连带 + ref 清理）；fallback 不重启 PTY。Evidence: `terminal-panel.spec.tsx` 16 项 + 包全量 46/46。）
- [x] 6.3 [Owner: `packages/bundle/dsh-terminal`；Dependencies: 6.1] 实现ResizeObserver + rAF fit/resize、generation fence、hidden/zero-size guard与font/theme refit。Validation: fake attachment断言最终cols/rows、coalescing与stale receipt。 **[lane: commodity-parked]** （done 2026-08-30: ResizeObserver + **rAF coalescing**（resize 突发合并为单次 fit，pending frame 卸载时取消）+ hidden/zero-size 守卫（<80×40 不 fit）+ onResize→attachment.resize（fake 断言最终 cols/rows）+ epoch generation fence + **font/theme refit**（document.fonts.ready 后补一次 fit——webfont 换字重不触发容器 resize）。Evidence: `terminal-panel.spec.tsx` coalescing 断言（双 emit 单 fit）+ 包全量 39/39。）
- [x] 6.4 [Owner: `packages/bundle/dsh-terminal`；Dependencies: 1.2, 6.1] 实现observe/control/busy/takeover UI与input gate。Acceptance: 未获control的keypress不缓冲不发送；takeover只有granted receipt后启用输入。 **[lane: commodity-parked]** （done 2026-08-30: host 合同 additive 扩展 `TerminalControlLeaseV1`（granted/pending/denied + busy + subscribe + requestTakeover，legacy attachment 无 control 字段不加门）；panel 输入门——onData 同步检查租约，非 granted **丢弃不缓冲不发送**；租约状态 chip（等待控制授权/观察中·输入未发送）+ 接管按钮（requestTakeover，owner grant 后输入才启用）。Evidence: `terminal-panel.spec.tsx` 3 项（未授权丢弃、takeover 后 grant 才通、legacy 无门）+ host 42/42 + 包全量 42/42。）
- [ ] 6.5 [Owner: `packages/bundle/dsh-terminal`；Dependencies: 6.1] 实现epoch/sequence reconnect、bounded replay、truncated/resync、exited/lost与browser refresh reattach状态。Validation: fake transport duplicate/gap/expired cursor tests。 **[lane: commodity-parked]** （progress 2026-08-30: epoch 栅栏（stale epoch 丢弃）、sequence 重放拒绝、gap/truncated 显式 resync 通知、attach 失败重试重附着、首块基线（mid-session 重附着不误报）已测；**剩余**：bounded replay、browser refresh reattach 状态机。）
- [ ] 6.6 [Owner: `packages/bundle/dsh-terminal`；Dependencies: 2.5, 6.1] 实现New/Profile、Split-new-PTY、Rename/Icon/Color、Find/Copy/Paste/Clear、Move、Maximize、Close/Detach与Kill菜单。Acceptance: Close不kill；Kill有target/confirm/receipt；Split创建新PTY。 **[lane: commodity-parked]** （progress 2026-08-30: **Find**（输入框+next/prev 驱动 SearchAddon）/**Copy**（Serialize→clipboard）/**Paste**（clipboard 读取+确认门）/**Clear**/**Detach**（仅 detach 不 kill，测）/**Kill**（window.confirm 拒绝不发、确认后 host.closeTerminal(target) receipt，测）已交付；**剩余**：New/Profile、Split-new-PTY、Rename/Icon/Color、Move、Maximize 菜单（依赖 2.5 chrome 控件面与 pane 注册 6.8）。）
- [ ] 6.7 [Owner: `packages/bundle/dsh-terminal`；Dependencies: 6.2, 6.4] 实现multiline/control-character paste confirmation、clipboard fallback、safe URL policy与DSH file resolver handoff。Validation: security negative tests覆盖 `file:`、unsupported scheme、unresolved path与自动Enter禁止。 **[lane: commodity-parked]** （progress 2026-08-30: multiline/control-character paste confirmation 已交付（正则探测+confirm 拒绝则零发送，测）；clipboard 失败静默降级（optional capability）；粘贴受 6.4 租约门。**剩余**：safe URL policy、DSH file resolver handoff、自动 Enter 禁止负测。）
- [x] 6.8 [Owner: Terminal/Desktop provider；Dependencies: 1.2, 6.1-6.7] 将每个DSH terminal注册为 `terminal:<opaque-id>` non-singleton keep-alive view，默认Bottom；provider dispose只detach。Acceptance: move/max/suspend不spawn第二PTY，Web profile不调用placeholder。 **[lane: commodity-parked]** （done 2026-08-30: `dsh-terminal.session` non-singleton keep-alive 视图注册（presentation: terminal icon + defaultEdge bottom），`openTerminalSession(sessionId, title)` 以 `terminal:<opaque-id>` 开视图（同一 id 去重）；`terminal.open-session` 命令把当前会话开为 pane 视图；provider dispose 只注销视图（disposers），attachment detach-only 已由 6.1 卸载测试钉住——PTY 生命周期恒在 owner。move/max/suspend 单活性由 pane-workbench keep-alive retention 保证；Web profile 无 seam 时走 6.1 诚实 compatibility 态（零 placeholder 调用）。Evidence: console-client 双视图+presentation+三命令断言 + 包全量 46/46。）

## 7. DSH 集成、兼容与持久化

- [ ] 7.1 [Owner: Harness terminal adapter；Dependencies: DSH interactive capability, 1.2] 接入官方DSH client terminal service，不手工读取token或拼接owner authority。Acceptance: profile/list/open/attach/control/write/resize/signal/detach/kill receipts完整映射。
- [ ] 7.2 [Owner: Harness preview adapters；Dependencies: DSH resource preview seam, 3.1-3.5] 接入DSH owner-issued inspect/rendition/window/range/version/release能力，不绕过host直接fetch本地或provider URL。Acceptance: File/Document、Media、attachment/artifact共享同一access lifecycle；缺失能力进入unsupported/partial而非猜测fallback。
- [ ] 7.3 [Owner: Compose/Profile；Dependencies: 4.9, 5.1, 6.8, 7.1-7.2] 提升DSH peer floor并增加capability check；缺失时显示compatibility error，禁止overlay、raw URL或fake terminal fallback。Validation: old-seam/new-seam fixture tests。
- [ ] 7.4 [Owner: Persistence；Dependencies: 2.6, 3.2, 5.7, 6.8] 扩展V2 allowlist以保存safe title/icon/color/opaque ref/normalized renderer preference，确认内容、access source、output、xterm buffer、cursor、lease、path、token、current media bytes全部被拒绝。Validation: persistence round-trip与malicious metadata tests。
- [ ] 7.5 [Owner: Disposal/HMR；Dependencies: 2.7, 3.6, 4.10, 5.8, 6.8] 验证bundle unload/HMR释放slots、listeners、portals、styles、ResizeObserver、workers、object URLs、preview handles、media graph、xterm/addons与attachments；DSH按owner lifecycle保留/清理PTY。Acceptance: 无重复owner attach、播放、renderer、rail或layout reservation残留。

## 8. 验证、证据与文档

- [ ] 8.1 [Owner: Harness unit/component] 运行focused package checks：Pane Workbench、shared preview host/registry、File/Document、Rich Media、Terminal Host与Terminal各自typecheck/test；每个失败按introduced/pre-existing/concurrent/environmental分类。
- [ ] 8.2 [Owner: Cross-project integration；Dependencies: 7.1-7.5] 添加DSH preview/terminal integration runner，覆盖大文本window、表格page、PDF worker、版本stale、image/audio/video、precomputed peaks、HLS fallback、binary fallback，以及真实node-pty raw VT、alternate buffer、mouse/resize、control conflict、detach/reconnect、refresh与kill；evidence写入 `temp/integration-test-runs/<run-id>/` 并保留原exit code。
- [ ] 8.3 [Owner: Browser verification；Dependencies: 8.2] 运行 `dsh --profile web --port 3802`，验证1440/1243/1024/768/390px、Right/Bottom、split/move/max/Details/sidebar invariant、Quick Pick、Explorer preview/pin、Markdown/JSON/CSV/PDF/image/audio/video/HLS、Media Library与真实terminal；保存screenshots、ARIA、console/network、worker与performance evidence。
- [ ] 8.4 [Owner: Browser accessibility/performance；Dependencies: 8.3] 验证keyboard-only、focus restore、screen-reader labels/live regions、captions/transcript、reduced-motion/high-contrast、coarse pointer，以及large text/table/PDF/media budgets、abort、cache eviction和无重复decode/playback。Acceptance: 每个失败有可复现步骤与artifact路径。
- [ ] 8.5 [Owner: Harness full gates；Dependencies: 8.1-8.4] 在功能稳定后运行 `pnpm run typecheck && pnpm run test && pnpm run build && pnpm run doc-sync`，不为清理无关脏树失败而修改其他业务逻辑。
- [ ] 8.6 [Owner: Documentation；Dependencies: 2-7] 更新相关README、implementation docs、Web profile、keyboard/preview/media/terminal limitations、DSH Agent Note链接与`THIRD_PARTY_NOTICES.md`；V1/V2历史只链接superseded，不改写。
- [ ] 8.7 [Owner: OpenSpec] 运行 `openspec validate dsh-pane-workspace-experience-v3 --strict --no-interactive`，并在完成后按项目流程归档change与redacted evidence references。
